---
phase: 24-wysiwyg-frontend-editor
plan: 04
type: execute
wave: 4
depends_on: [24-01, 24-02, 24-03]
files_modified:
  - genossi_bin/tests/e2e_tests.rs
  - .planning/phases/24-wysiwyg-frontend-editor/24-UAT-CHECKLIST.md
autonomous: false
requirements: [EDIT-01, EDIT-02, EDIT-03, EDIT-04, EDIT-05]
must_haves:
  truths:
    - "A new e2e HTTP test proves POST /api/mail/preview with body_html returns a rendered body_html field in the response (Plan 24-01 Task 1 seam)"
    - "A new e2e HTTP test proves POST /api/inbox/{id}/reply with body_html reaches the MailJob and lands as sanitized HTML in the store (Plan 24-01 Task 2 seam)"
    - "A UAT checklist file lists 10+ manual verification steps covering EDIT-01..05 end-to-end (styleWithCSS=false Bold check, paste-clean check, submit-preserves-formatting check, live-preview HTML check, link-dialog check)"
    - "The Vorstand-facing smoke test is documented step-by-step: start backend + dx serve, open compose, type Bold, verify innerHTML in DevTools, click Send, verify body_html payload in Network tab, verify preview shows rendered HTML"
  artifacts:
    - "2 new e2e HTTP tests in genossi_bin/tests/e2e_tests.rs (preview seam + inbox reply seam)"
    - ".planning/phases/24-wysiwyg-frontend-editor/24-UAT-CHECKLIST.md (Vorstand-facing verification steps)"
  key_links:
    - "Plan 24-01 Task 1 backend seam → preview_body_html_round_trips_to_response e2e test"
    - "Plan 24-01 Task 2 backend seam → inbox_reply_body_html_sanitized_and_persisted e2e test"
    - "Plans 24-02 + 24-03 frontend components → UAT checklist manual steps"
---

<objective>
Close the phase with end-to-end validation: two automated e2e HTTP tests that pin the new backend seams (preview HTML render, inbox reply body_html sanitize-on-store) and a Vorstand-facing UAT checklist that walks through the visible frontend behaviors (styleWithCSS enforcement, paste plaintext, live HTML preview, link dialog). This plan is `autonomous: false` because the UAT step is human-verified.

Purpose: The pure-Rust helpers (is_valid_link_url, exec_command_* facades) are already unit-tested in Plans 24-02/24-03. What's left is the boundary proof: (1) automated e2e that the wire actually reaches the backend correctly, and (2) manual UAT that the browser-side behavior (contenteditable, execCommand, paste, in-app dialog) actually works end-to-end for a real Vorstand.

Output: 2 new tests in `genossi_bin/tests/e2e_tests.rs` following the pattern established by Phase 23 Plan 04's `bulk_mail_body_html_sanitized_and_persisted`. One new UAT checklist file in the phase directory listing the ~10 manual verification steps.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-wysiwyg-frontend-editor/24-CONTEXT.md
@.planning/phases/24-wysiwyg-frontend-editor/24-RESEARCH.md
@.planning/phases/24-wysiwyg-frontend-editor/24-01-PLAN.md
@.planning/phases/24-wysiwyg-frontend-editor/24-02-PLAN.md
@.planning/phases/24-wysiwyg-frontend-editor/24-03-PLAN.md
@.planning/phases/23-html-mail-backend/23-04-SUMMARY.md
@genossi_bin/tests/e2e_tests.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add e2e HTTP test — preview_body_html_round_trips_to_response</name>
  <files>genossi_bin/tests/e2e_tests.rs</files>
  <read_first>
    - genossi_bin/tests/e2e_tests.rs — grep for `bulk_mail_body_html_sanitized_and_persisted` (Phase 23 Plan 04 test) to find the canonical e2e test scaffolding (test_server bootstrap, in-memory sqlite, reqwest client, seed data helpers, JSON assertion pattern)
    - .planning/phases/23-html-mail-backend/23-04-SUMMARY.md — coverage §"e2e_tests (3)" describes the pattern
    - .planning/phases/24-wysiwyg-frontend-editor/24-01-PLAN.md Task 1 — the preview_mail seam contract
  </read_first>
  <action>
    Add a new #[tokio::test] `preview_body_html_round_trips_to_response` in genossi_bin/tests/e2e_tests.rs, sibling to the existing `bulk_mail_body_html_sanitized_and_persisted` test. Setup: spawn the test server, seed one Member (first_name "Max", last_name "Muster"), acquire the base URL. POST to `/api/mail/preview` with a JSON body `{ "subject": "Betreff", "body": "Hallo {{ first_name }}", "member_id": "<uuid>", "body_html": "<p>Hallo <b>{{ first_name }}</b></p>" }`. Assert HTTP 200. Parse the JSON response and assert three things: (a) `response.body == "Hallo Max"` (existing plain path unchanged), (b) `response.body_html` is Some, (c) `response.body_html.unwrap()` contains the string `"<b>Max</b>"` (author markup preserved, member variable interpolated). Add a second small assertion: POST the SAME preview request WITHOUT the `body_html` key at all, and assert the response JSON does NOT contain a `body_html` key at all (skip_serializing_if wire-compat proof — mirrors Phase 23's `bulk_mail_body_html_none_stays_backward_compatible`). Use the same test-server bootstrap helpers already imported in the file; do NOT add new test-infra. If the file already has a shared `spawn_server_with_seed()` helper, reuse it verbatim.
  </action>
  <verify>
    <automated>cargo test -p genossi_bin --test e2e_tests preview_body_html_round_trips_to_response 2>&1 | tail -25</automated>
  </verify>
  <done>
    Test passes; assertion (c) confirms the rendered HTML contains `<b>Max</b>` proving both the autoescape env round-trip AND the member-variable interpolation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add e2e HTTP test — inbox_reply_body_html_sanitized_and_persisted</name>
  <files>genossi_bin/tests/e2e_tests.rs</files>
  <read_first>
    - genossi_bin/tests/e2e_tests.rs — the `bulk_mail_body_html_sanitized_and_persisted` test at whatever line it lives; use as template
    - genossi_mail/src/inbox_rest.rs lines 403-510 (reply_inbox handler)
    - .planning/phases/24-wysiwyg-frontend-editor/24-01-PLAN.md Task 2 — the ReplyRequest.body_html seam contract
  </read_first>
  <action>
    Add a new #[tokio::test] `inbox_reply_body_html_sanitized_and_persisted` in genossi_bin/tests/e2e_tests.rs. Setup: spawn test server + in-memory DB, seed ONE inbound mail via the InboxDao (`InboundMail` literal with `from_address = "customer@example.com"`, `subject = "Anfrage"`, `body_text = "…"`, sensible defaults; grep the existing tests for a helper like `seed_inbound_mail` and reuse it — if none exists, insert directly via a raw sqlx query following the pattern used by the digest tests). POST `/api/inbox/{mail_id}/reply` with a JSON body containing `body_html: "<script>alert(1)</script><p>Reply <b>ok</b></p>"`. Assert HTTP 202 (the existing reply-response status per inbox_rest.rs line 410). Parse the ReplyResponseTO to get `job_id`. GET `/api/mail/jobs/{job_id}` to load the created MailJob. Assert the response's `body_html` field is Some and its value: (a) does NOT contain `<script>`, (b) DOES contain `<p>Reply <b>ok</b></p>` (proves ammonia sanitize gate held at store boundary + preserved safe author markup). Second assertion pass: POST the same reply without a body_html key at all, then GET the job, and assert `body_html: None` — proves the None path stays None (backward-compat). If cross-crate test setup makes this too heavy (e.g. no existing helper seeds InboundMail), fall back to a smaller service-level test in `genossi_mail/src/inbox.rs` `#[cfg(test)]` that directly calls `InboxServiceImpl::reply(mail_id, subj, body, vec![], vec![], Some(html))` and asserts the persisted MailJob.body_html was sanitized — this is the same pattern already established by Plan 24-01 Task 2's `reply_sanitizes_body_html_on_store` unit test but exercised through the service layer round-trip. Prefer the full e2e HTTP path if the helpers already exist; document the choice inline.
  </action>
  <verify>
    <automated>cargo test -p genossi_bin --test e2e_tests inbox_reply_body_html_sanitized_and_persisted 2>&1 | tail -25</automated>
  </verify>
  <done>
    Test passes; the assertion pair proves script stripped + safe markup preserved + None-path stays None.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author UAT checklist for Vorstand-facing manual verification</name>
  <files>.planning/phases/24-wysiwyg-frontend-editor/24-UAT-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/24-wysiwyg-frontend-editor/24-RESEARCH.md §"Phase Requirements → Test Map" (rows 3-5 marked "Manual UAT") and §"Wave 0 Gaps" checklist
    - .planning/phases/23-html-mail-backend/ — grep for any prior phase's UAT-CHECKLIST.md for the format convention (h2 checkbox rows, one bullet per verification)
    - genossi-frontend/CLAUDE.md — the run-instructions (Tailwind watch + dx serve + backend startup)
  </read_first>
  <action>
    Create `.planning/phases/24-wysiwyg-frontend-editor/24-UAT-CHECKLIST.md` (a new documentation artifact, not a code file). Structure: `# Phase 24 UAT Checklist` header, then a `## Setup` section describing how to launch (backend `cargo run --bin genossi` on :3000 with mock_auth; frontend `dx serve` on :8080; per `run-rust-backend-and-frontend` project skill), then a `## Verification Steps` section with a numbered checkbox list of ~12 items. Each item MUST have: a) what to click / type / observe, b) the expected outcome, c) which requirement (EDIT-01..05) it satisfies. Concrete items to include: (1) Massenmail-Compose page loads WysiwygEditor without console errors [EDIT-01]. (2) Typing plain text updates the plain-text side of the on_change tuple (verify via DevTools by inspecting Dioxus DOM state or by clicking Send and checking the Network tab payload `body` field) [EDIT-01, EDIT-03]. (3) Clicking Bold, then typing → DOM contains `<b>...</b>` in innerHTML, NOT `<span style="font-weight: bold">` — verify via DevTools Elements panel [EDIT-02, styleWithCSS=false invariant]. (4) Copy formatted text from Word/browser (`**bold**` + line breaks), paste into editor — resulting innerHTML has only the text, no `<span>`/`<b>`/`<p style>` from the source [EDIT-04]. (5) Click Link toolbar button — in-app modal opens (NOT window.prompt); enter URL `https://example.com` and text; verify `<a href="https://example.com">...</a>` in innerHTML [EDIT-01, D-06]. (6) Select existing text, click Link, enter URL — anchor wraps the selection (Pitfall 6 selection-preservation check) [EDIT-01, D-06]. (7) Enter invalid URL `javascript:alert(1)` in link dialog — Insert button stays disabled [EDIT-02 defense-in-depth]. (8) Select member in TemplatePreview — verify live preview renders HTML formatting (bold text is bold, not literal `<b>`) [EDIT-05]. (9) Send bulk mail with formatted body — check received mail is multipart/alternative with both plain and HTML parts (send to a test address, inspect raw source in the receiving MUA) [EDIT-03 + Phase 23 HTML-01]. (10) Open inbox reply form → same 3 checks (Bold produces `<b>`, paste is plain, HTML preview matches) [EDIT-01,02,04,05]. (11) Open a Mail Template with existing body_html, verify editor loads the HTML content and toolbar buttons work on it [EDIT-01, EDIT-03]. (12) Save the template with formatted body → reload → verify body_html round-tripped and re-renders in editor [EDIT-03, wire integrity]. Also include a "Known limitations" section: (a) `execCommand` is deprecated but stable for our command subset; (b) `contenteditable` state after external Signal reset (TemplateVarButtons inserting into reply_form) may lag one render — see Plan 24-03 Task 3 comment. Also include a "Regression check" section pointing at the Phase 23 backend tests (they must still pass; run `cargo test -p genossi_mail` and `cargo test -p genossi_bin --test e2e_tests`).
  </action>
  <verify>
    <automated>ls -l .planning/phases/24-wysiwyg-frontend-editor/24-UAT-CHECKLIST.md; grep -c '\- \[' .planning/phases/24-wysiwyg-frontend-editor/24-UAT-CHECKLIST.md; grep -cE 'EDIT-0[1-5]' .planning/phases/24-wysiwyg-frontend-editor/24-UAT-CHECKLIST.md</automated>
  </verify>
  <done>
    File exists; contains at least 10 checkbox items; every EDIT-01..05 requirement is referenced at least once; file is committed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Vorstand smoke test — run the UAT checklist against a real browser</name>
  <files>.planning/phases/24-wysiwyg-frontend-editor/24-UAT-CHECKLIST.md</files>
  <what-built>
    Full Phase 24 implementation: WysiwygEditor component in all 3 compose flows; HTML live preview; backend preview seam; sanitize-on-store for inbox reply body_html; 2 new e2e tests; UAT checklist.
  </what-built>
  <how-to-verify>
    1. Follow `.planning/phases/24-wysiwyg-frontend-editor/24-UAT-CHECKLIST.md` § Setup to launch backend + frontend (project skill `run-rust-backend-and-frontend`).
    2. Walk through each of the 12+ verification steps in the checklist.
    3. Report which steps passed and which (if any) failed. For any failure, capture: (a) which requirement was hit, (b) DevTools screenshot of the failing innerHTML or Network payload, (c) suggested fix location.
    4. If steps 3 (styleWithCSS) or 4 (paste-plain) fail, that is a hard fail — those are the two invariants that make the ammonia gate work at all.
    5. If step 5 (in-app modal opens instead of window.prompt) fails, that is a D-06 hard fail.
    6. Cross-cutting sanity: also run `cargo test --workspace` and confirm no regression outside the new tests. Note the pre-existing `test_mail_preview_repayment_no_entries_does_not_default_to_one` failure carried over from Phase 22 stays failing — that is documented in STATE.md and NOT a Phase 24 regression.
  </how-to-verify>
  <action>Human-verified UAT — no autonomous action. Follow the how-to-verify steps above using the UAT-CHECKLIST.md as the source of truth.</action>
  <verify>Manual verification per how-to-verify steps 1-6. No automated command runs in this checkpoint task; the automated coverage lives in Tasks 1-2 (e2e tests) and prior Plans' cargo check/test gates.</verify>
  <done>All EDIT-01..05 requirements are visibly demonstrable to the Vorstand user in the running frontend (backend :3000 + dx :8080), OR any failing item is documented with a follow-up path (bug ticket / plan revision). Steps 3, 4, 5 MUST pass (those are ammonia+D-06 invariants); non-critical failures may be deferred with human sign-off.</done>
  <resume-signal>Type "approved" (all UAT items passed) or list the failing item numbers with observed vs expected.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test HTTP client → REST endpoint | e2e tests exercise the same axum routes real clients use |
| Vorstand browser → all three compose flows | UAT covers the full user-facing surface: compose, reply, template edit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-10 | Tampering | e2e sanitize-in-store test | high | mitigate | The new inbox_reply_body_html test pins the ammonia gate for the reply path. If a future refactor accidentally drops the sanitize call from InboxService::reply, this test fails immediately (analog to how the Phase 23 create_job test pins that gate). |
| T-24-11 | Tampering | UAT step 3 (styleWithCSS false) | high | accept | Manual browser check; cannot be automated without wasm-bindgen-test browser CI. Accepted as human-gated because Vorstand-facing production readiness is the gate. |
| T-24-12 | Tampering | UAT step 4 (paste-plain) | medium | accept | Same rationale as T-24-11; ammonia gate is defense-in-depth on the backend regardless. |
</threat_model>

<verification>
Overall plan-04 checks after all 4 tasks complete:

- `cargo test -p genossi_bin --test e2e_tests preview_body_html_round_trips_to_response` — passes.
- `cargo test -p genossi_bin --test e2e_tests inbox_reply_body_html_sanitized_and_persisted` — passes.
- `cargo test --workspace` — the delta from Phase 23 baseline is +2 new tests passing, 0 regressions (the pre-existing test_mail_preview_repayment_no_entries_does_not_default_to_one failure documented in STATE.md is unchanged, not caused by Phase 24).
- `.planning/phases/24-wysiwyg-frontend-editor/24-UAT-CHECKLIST.md` exists with ≥ 10 checkbox items and covers EDIT-01..05.
- Human verification: Vorstand-facing UAT run passes at least steps 3, 4, and 5 (the ammonia-critical + D-06 checks); non-critical steps can be documented as follow-ups if they fail.
</verification>

<success_criteria>
- Backend preview seam (Plan 24-01 Task 1) is pinned by an e2e HTTP test proving the round-trip HTML render.
- Backend inbox-reply sanitize seam (Plan 24-01 Task 2) is pinned by an e2e HTTP test proving script-strip + safe-markup preservation.
- UAT checklist documents the manual browser-side verifications the automated tests can't cover.
- Human checkpoint confirms the Vorstand-facing behavior works end-to-end (styleWithCSS=false, paste-plain, in-app modal, HTML preview).
- No regression in the workspace test suite beyond the documented pre-existing Phase-22 failure.
</success_criteria>

<output>
Create `.planning/phases/24-wysiwyg-frontend-editor/24-04-SUMMARY.md` when done using the summary.md template. Capture: 2 new e2e tests, 1 UAT checklist artifact, human-verification outcome. Attach any UAT failure notes to the SUMMARY so the phase-audit / milestone-close step can inspect them.
</output>
