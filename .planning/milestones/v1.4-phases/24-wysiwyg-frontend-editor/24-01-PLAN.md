---
phase: 24-wysiwyg-frontend-editor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - genossi_mail/src/rest.rs
  - genossi_mail/src/inbox_rest.rs
  - genossi_mail/src/inbox.rs
  - genossi-frontend/src/api.rs
  - genossi-frontend/Cargo.toml
  - genossi-frontend/src/i18n/mod.rs
  - genossi-frontend/src/i18n/de.rs
  - genossi-frontend/src/i18n/en.rs
autonomous: true
requirements: [EDIT-01, EDIT-04, EDIT-05]
must_haves:
  truths:
    - "POST /api/mail/preview with body_html=<p>Hallo {{ first_name }}</p> returns body_html rendered via autoescaping env"
    - "POST /api/inbox/{id}/reply with body_html=Some(html) reaches InboxService::reply and persists MailJob.body_html via sanitize-on-store"
    - "Legacy clients that omit body_html continue to serialize/deserialize without error (skip_serializing_if=Option::is_none)"
    - "Frontend Cargo.toml has web-sys features ClipboardEvent and DataTransfer enabled"
    - "genossi-frontend/src/i18n/mod.rs defines 19 new Key::MailEditor* variants translated in de.rs AND en.rs"
  artifacts:
    - "PreviewRequest.body_html field (Option<String>)"
    - "PreviewResponse.body_html field (Option<String>)"
    - "ReplyRequest.body_html field (Option<String>)"
    - "InboxService::reply signature carries body_html: Option<String>"
    - "Frontend api::PreviewRequest + PreviewResponse + reply_inbox_mail carry body_html"
  key_links:
    - "preview_mail handler → template::render_html_template (autoescape env, Phase 23 D-04)"
    - "reply_inbox handler → InboxService::reply → sanitize_body_html_opt + MailJob{body_html} (Phase 23 D-03 EP wire)"
    - "genossi-frontend Cargo.toml web-sys features → ClipboardEvent for onpaste handler (Plan 24-02)"
---

<objective>
Backend and shared foundation seam for the WYSIWYG editor. Extends the mail preview and inbox-reply APIs with body_html carriage (mirroring the send-path pattern already landed in Phase 23 Plan 04), adds the web-sys features the new editor component needs (ClipboardEvent, DataTransfer), and lands all 19 new i18n keys in both locales up-front so Plan 24-02 can compile without weaving cross-cutting concerns.

Purpose: Every downstream plan (24-02 editor component, 24-03 migration+preview) needs at least one of these seams. Landing them together in a single Wave-1 plan avoids three separate cross-cutting sub-plans and keeps the executor's context window tight (~30% context estimate: mostly straight-line edits with clear grep-boundaries).

Output: PreviewRequest/Response carry `body_html: Option<String>` (per EDIT-05 seam noted in RESEARCH.md Pitfall 8), the `preview_mail` handler renders HTML via `render_html_template` when the request supplies `body_html`, the inbox `ReplyRequest` + `InboxService::reply` + frontend `reply_inbox_mail` carry `body_html` so Plan 24-03's reply-form migration can post it, and the frontend has ClipboardEvent/DataTransfer features + all 19 `Key::MailEditor*` variants ready to use.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-wysiwyg-frontend-editor/24-CONTEXT.md
@.planning/phases/24-wysiwyg-frontend-editor/24-RESEARCH.md
@.planning/phases/23-html-mail-backend/23-04-SUMMARY.md
@genossi_mail/src/rest.rs
@genossi_mail/src/inbox_rest.rs
@genossi_mail/src/inbox.rs
@genossi_mail/src/template.rs
@genossi_mail/src/sanitize.rs
@genossi-frontend/src/api.rs
@genossi-frontend/Cargo.toml
@genossi-frontend/src/i18n/mod.rs
@genossi-frontend/src/i18n/de.rs
@genossi-frontend/src/i18n/en.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend PreviewRequest/Response with body_html + render HTML in preview_mail handler (backend)</name>
  <files>genossi_mail/src/rest.rs</files>
  <read_first>
    - genossi_mail/src/rest.rs lines 250-296 (PreviewRequest + PreviewResponse struct definitions with existing body_html on TestMailWithTemplateRequest for the serde pattern)
    - genossi_mail/src/rest.rs lines 630-765 (preview_mail handler — existing ctx-build path, render_template calls at 734 and 742)
    - genossi_mail/src/template.rs lines 71-155 (render_template + render_html_template — same signature, different env)
    - .planning/phases/24-wysiwyg-frontend-editor/24-RESEARCH.md §"Pitfall 8: Backend preview_mail liefert HEUTE kein body_html" — full seam spec (per D-04)
    - .planning/phases/23-html-mail-backend/23-04-SUMMARY.md — establishes the `#[serde(default, skip_serializing_if = "Option::is_none")]` pattern on every body_html field for wire backward-compat
  </read_first>
  <action>
    Extend `PreviewRequest` (rest.rs ~line 258) with a new field `pub body_html: Option<String>` decorated with `#[serde(default, skip_serializing_if = "Option::is_none")]` and a `#[schema(example = "<p>Hallo {{ first_name }}</p>")]` doc line. Extend `PreviewResponse` (rest.rs ~line 276) with a matching `pub body_html: Option<String>` field using the same serde attributes (mirrors the wire-compat pattern used on TestMailWithTemplateRequest at line 252). In the `preview_mail` handler (rest.rs ~line 643-765), after `rendered_body` is computed and BEFORE the `PreviewResponse` literal is constructed, add a branch: if `body.body_html.is_some()` unwrap the source, call `crate::template::render_html_template(&html_src, &ctx)` and either push the error into `errors` (prefix `HTML: `) or capture the rendered string in a `let rendered_body_html: Option<String>`. Set the new field on `PreviewResponse` (analog to how `send_test_mail_with_template` at rest.rs ~line 918 already renders body_html via render_html_template, per D-04). Do NOT sanitize here — this is a read-only render preview, the sanitize gate lives at the store boundary (Phase 23 D-03). Add two unit tests in the existing `#[cfg(test)] mod tests` block (rest.rs bottom): `preview_response_serializes_without_body_html_when_none` (assert JSON output does NOT contain `body_html` key when field is `None`) and `preview_response_serializes_with_body_html_when_some` (assert JSON output DOES contain `body_html` when set) — mirrors the pattern established in Phase 23 Plan 04 (`mail_template_to_serializes_without_body_html_when_none`, per 23-04-SUMMARY.md patterns-established §1). Update the openapi `components(schemas(...))` list at rest.rs ~line 415 already includes PreviewRequest and PreviewResponse — no change needed. Backward-compat rule (per D-04): if the request does NOT include `body_html`, the response also does NOT include `body_html` (Option remains None).
  </action>
  <verify>
    <automated>cargo test -p genossi_mail --lib preview_response_serializes_ 2>&1 | tail -20</automated>
  </verify>
  <done>
    Two new preview_response_serializes_* tests pass; `grep -n 'pub body_html' genossi_mail/src/rest.rs` shows body_html on both PreviewRequest AND PreviewResponse; `cargo check -p genossi_mail` clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add body_html to ReplyRequest + InboxService::reply signature + wire sanitize-on-store (backend)</name>
  <files>genossi_mail/src/inbox_rest.rs, genossi_mail/src/inbox.rs</files>
  <read_first>
    - genossi_mail/src/inbox_rest.rs lines 74-93 (ReplyRequest struct, ReplyResponseTO) and lines 403-510 (reply_inbox handler)
    - genossi_mail/src/inbox.rs lines 460-472 (InboxService trait `reply` signature) and lines 647-712 (impl `reply` where MailJob is constructed with `body_html: None` at line 691)
    - genossi_mail/src/service.rs — grep for `sanitize_body_html_opt` free helper (Phase 23 Plan 04 introduced it in service.rs; it wraps ammonia::clean on `Option<String>`)
    - .planning/phases/23-html-mail-backend/23-04-SUMMARY.md — pattern for D-03 EP wiring (sanitize once at the entry point, worker never re-sanitizes)
    - genossi_mail/src/inbox.rs lines 1381-1690 (existing reply_* tests to be updated with the new positional argument)
  </read_first>
  <action>
    Extend `ReplyRequest` (inbox_rest.rs ~line 75) with `pub body_html: Option<String>` under `#[serde(default, skip_serializing_if = "Option::is_none")]`. In `reply_inbox` handler (inbox_rest.rs ~line 414-510), thread the new field into the `svc.reply(...)` call as an additional positional argument (see next). Update `InboxService::reply` trait signature (inbox.rs ~line 464-471) to accept `body_html: Option<String>` as the last parameter (before or after the existing tail — put it AFTER `static_document_ids: Vec<Uuid>` for minimal diff). Update the impl (inbox.rs ~line 647-712) so the `MailJob` literal at line 673-692 uses `body_html: crate::service::sanitize_body_html_opt(body_html.as_deref())` instead of the hard-coded `None`, mirroring the D-03 EP wire established in Phase 23 Plan 04 (create_job / send_test_mail_with_body). Import the sanitize helper if not already visible. Update all four `reply_creates_*` tests in inbox.rs (lines 1381, 1433, 1473, 1545, 1635) to pass `None` as the new body_html argument — mechanical positional-argument update, no behavior change. Add ONE new test `reply_sanitizes_body_html_on_store` next to the existing tests: call `.reply(mail_id, "s", "b", vec![], vec![], Some("<script>alert(1)</script><p>ok</p>".to_string()))` and assert the persisted MailJob.body_html equals the ammonia-sanitized string (script gone, p preserved) — parallels 23-04-SUMMARY's `create_job_sanitizes_body_html` pattern.
  </action>
  <verify>
    <automated>cargo test -p genossi_mail --lib reply_sanitizes_body_html_on_store 2>&1 | tail -20; cargo test -p genossi_mail --lib reply_creates 2>&1 | tail -30</automated>
  </verify>
  <done>
    New `reply_sanitizes_body_html_on_store` test passes; all four existing `reply_creates_*` tests still pass with the mechanical `None` positional argument; `grep -n 'pub body_html' genossi_mail/src/inbox_rest.rs` shows the field on ReplyRequest.
  </done>
</task>

<task type="auto">
  <name>Task 3: Mirror body_html on frontend api::PreviewRequest/PreviewResponse + reply_inbox_mail (frontend api layer)</name>
  <files>genossi-frontend/src/api.rs</files>
  <read_first>
    - genossi-frontend/src/api.rs lines 866-976 (SendMailRequest, SendBulkMailRequest, PreviewRequest, PreviewResponse, preview_mail helper — the body_html pattern is ALREADY set on SendMailRequest et al. from Phase 23 for reference)
    - genossi-frontend/src/api.rs lines 1469-1493 (reply_inbox_mail helper — builds inline JSON payload with subject/body/attachment_ids/static_document_ids)
    - .planning/phases/24-wysiwyg-frontend-editor/24-CONTEXT.md D-01, D-04 — the frontend api-layer contract MUST carry body_html both ways
  </read_first>
  <action>
    Extend frontend `api::PreviewRequest` (api.rs ~line 935) with `pub body_html: Option<String>` under `#[serde(default, skip_serializing_if = "Option::is_none")]` — mirrors the backend field 1:1. Extend `api::PreviewResponse` (api.rs ~line 945) with `pub body_html: Option<String>` under `#[serde(default)]` (backward-compat: older backend responses without the field must deserialize). Change the `preview_mail(...)` helper signature (api.rs ~line 959) to accept an additional `body_html: Option<&str>` parameter (positional, AFTER `repayment_phase_id`) and set it on the request literal — this keeps existing callers callable with `None` as a mechanical addition. Update the `reply_inbox_mail(...)` helper (api.rs ~line 1469) to accept `body_html: Option<&str>` as the new last parameter, and inject `"body_html": body_html` into the inline `serde_json::json!({...})` payload only when Some (use `.map(...)` + `serde_json::Value` merge, or extend the literal with a conditional key using an `if let` pattern). Update all callers within this same task where they compile-break: `preview_mail` is invoked in genossi-frontend/src/component/mail_compose/template_preview.rs (grep) and `reply_inbox_mail` in genossi-frontend/src/component/inbox/reply_form.rs — both callers pass `None` for the new body_html parameter (they will be re-wired to real Signals in Plan 24-03; here we only make the type signature compile). Add a small unit test at api.rs bottom: `preview_request_serializes_without_body_html_when_none` (assert JSON output does NOT contain `body_html`) and `preview_request_serializes_with_body_html_when_some` (assert JSON contains it). Wire pattern per D-04 seam described in 24-RESEARCH.md Pitfall 8.
  </action>
  <verify>
    <automated>cargo check -p genossi-frontend 2>&1 | tail -20; cargo test -p genossi-frontend --lib preview_request_serializes 2>&1 | tail -20</automated>
  </verify>
  <done>
    `cargo check -p genossi-frontend` clean; two `preview_request_serializes_*` tests pass; `grep -n 'pub body_html' genossi-frontend/src/api.rs` shows field on both PreviewRequest and PreviewResponse.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add web-sys ClipboardEvent + DataTransfer features to genossi-frontend/Cargo.toml</name>
  <files>genossi-frontend/Cargo.toml</files>
  <read_first>
    - genossi-frontend/Cargo.toml lines 39-73 (existing web-sys feature list — HtmlElement, HtmlInputElement, Node, Document, Window, Location, Clipboard already enabled)
    - .planning/phases/24-wysiwyg-frontend-editor/24-RESEARCH.md "web-sys Feature-Additionen in genossi-frontend/Cargo.toml" — the exact two additions needed for the paste handler in Plan 24-02
  </read_first>
  <action>
    Insert two lines into the `[dependencies.web-sys] features = [ ... ]` list in genossi-frontend/Cargo.toml (~line 40-73): `"ClipboardEvent",` and `"DataTransfer",` — place them near the existing Clipboard entry (~line 70) with a short trailing comment `# Phase 24: onpaste handler for WysiwygEditor` on each. Do NOT touch any other feature or version pin. Verify the file still parses as valid TOML and `cargo check -p genossi-frontend` passes without warnings about missing features. Rationale (per D-07 + Pattern 1 in 24-RESEARCH.md): the Plan 24-02 WysiwygEditor's `onpaste` handler needs `web_sys::ClipboardEvent::clipboard_data()` returning `Option<DataTransfer>` to call `.get_data("text/plain")`.
  </action>
  <verify>
    <automated>grep -c 'ClipboardEvent' genossi-frontend/Cargo.toml; grep -c 'DataTransfer' genossi-frontend/Cargo.toml; cargo check -p genossi-frontend 2>&1 | tail -5</automated>
  </verify>
  <done>
    Both `ClipboardEvent` and `DataTransfer` appear exactly once in the web-sys features list; `cargo check -p genossi-frontend` reports no errors.
  </done>
</task>

<task type="auto">
  <name>Task 5: Add 19 new Key::MailEditor* variants + translations in de.rs and en.rs</name>
  <files>genossi-frontend/src/i18n/mod.rs, genossi-frontend/src/i18n/de.rs, genossi-frontend/src/i18n/en.rs</files>
  <read_first>
    - genossi-frontend/src/i18n/mod.rs lines 46-800 (Key enum body — enum variants Mail* clustered around line 240-350)
    - genossi-frontend/src/i18n/de.rs lines 215-350 (existing German MailEditor-adjacent translations)
    - genossi-frontend/src/i18n/en.rs lines 215-350 (existing English MailEditor-adjacent translations)
    - .planning/phases/24-wysiwyg-frontend-editor/24-RESEARCH.md §"i18n Keys Required" — the exact table of 19 keys with German and English text
  </read_first>
  <action>
    Add 19 new variants to the `enum Key` in genossi-frontend/src/i18n/mod.rs, clustered after the existing Mail-related variants (near `MailBody` at line 243 or after `MailTemplateTestDummyRepaymentHint` at line 277) with a `// Phase 24: WYSIWYG editor labels` section comment: `MailEditorBold`, `MailEditorItalic`, `MailEditorUnderline`, `MailEditorStrike`, `MailEditorUnorderedList`, `MailEditorOrderedList`, `MailEditorHeading1`, `MailEditorHeading2`, `MailEditorHeading3`, `MailEditorParagraph`, `MailEditorBlockquote`, `MailEditorLink`, `MailEditorUnlink`, `MailEditorLinkDialogTitle`, `MailEditorLinkUrlLabel`, `MailEditorLinkTextLabel`, `MailEditorLinkInsert`, `MailEditorLinkCancel`, `MailEditorPreviewHtml`. Add matching arms to the `translate` function in genossi-frontend/src/i18n/de.rs and genossi-frontend/src/i18n/en.rs using the exact strings from 24-RESEARCH.md §"i18n Keys Required" (German: Fett, Kursiv, Unterstrichen, Durchgestrichen, Aufzählung, Nummerierte Liste, Überschrift 1/2/3, Absatz, Zitat, Link, Link entfernen, Link einfügen, URL, Anzeige-Text (optional), Einfügen, Abbrechen, HTML-Vorschau — English: Bold, Italic, Underline, Strikethrough, Bulleted list, Numbered list, Heading 1/2/3, Paragraph, Blockquote, Link, Remove link, Insert link, URL, Display text (optional), Insert, Cancel, HTML preview). Both locales MUST have all 19 variants covered (genossi-frontend/CLAUDE.md: German fell back to English previously due to Locale::En bug — verify each arm uses the correct locale return branch). Do NOT modify any existing translation. Do NOT add a Key::MailBody variant (already exists at line 243).
  </action>
  <verify>
    <automated>grep -c 'MailEditor' genossi-frontend/src/i18n/mod.rs; grep -c 'MailEditor' genossi-frontend/src/i18n/de.rs; grep -c 'MailEditor' genossi-frontend/src/i18n/en.rs; cargo check -p genossi-frontend 2>&1 | grep -E 'non-exhaustive|missing' | head -5</automated>
  </verify>
  <done>
    All three files show >= 19 MailEditor* occurrences; `cargo check -p genossi-frontend` clean (no non-exhaustive-match warnings on the translate function).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (preview endpoint) | Frontend posts author-supplied `body_html` string; backend renders via autoescape env |
| client → API (inbox reply endpoint) | Frontend posts author-supplied `body_html` string; backend sanitizes via ammonia at store boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-01 | Tampering | preview_mail handler | medium | mitigate | Render via `render_html_template` autoescape env (Phase 23 D-04) — member/user values are HTML-escaped; author markup passes through as structure only. No new attack surface: preview is read-only, output never persists. |
| T-24-02 | Tampering | InboxService::reply body_html | high | mitigate | `sanitize_body_html_opt` at store boundary (Phase 23 D-03 EP wire) — ammonia strips `<script>`, `javascript:` URLs, event handlers before the MailJob is created. New reply body_html wire re-uses the vetted sanitize path from create_job. |
| T-24-03 | Information Disclosure | preview_mail response | low | accept | Preview is admin-authenticated (same handler, same route guards as before). Returning rendered HTML back to the caller does not expose any new data — the caller already has the source. |
</threat_model>

<verification>
Overall phase-01 checks after all 5 tasks complete:

- `cargo test -p genossi_mail --lib` — ALL tests pass (baseline + 3 new: two preview_response_serializes + one reply_sanitizes_body_html_on_store).
- `cargo test -p genossi-frontend --lib preview_request_serializes` — 2 new tests pass.
- `cargo check -p genossi-frontend` and `cargo check -p genossi_mail` — no errors, no non-exhaustive-match warnings.
- `grep -c 'pub body_html' genossi_mail/src/rest.rs` returns >= 6 (existing 4 on Send*/Test*/Template*/MailJobTO + 2 new on Preview*).
- `grep -c 'pub body_html' genossi_mail/src/inbox_rest.rs` returns >= 1 (new on ReplyRequest).
- `grep -c 'MailEditor' genossi-frontend/src/i18n/de.rs` and `.../en.rs` each return >= 19.
- `grep -c 'ClipboardEvent' genossi-frontend/Cargo.toml` returns 1; same for `DataTransfer`.
</verification>

<success_criteria>
- PreviewRequest and PreviewResponse both carry `body_html: Option<String>` with skip_serializing_if wire-compat.
- preview_mail handler renders body_html through render_html_template (autoescape env) when the request supplies it; response omits the field otherwise (backward-compatible with older frontends).
- ReplyRequest carries body_html; InboxService::reply signature accepts body_html and sanitizes it at store boundary via sanitize_body_html_opt; new sanitize test proves the ammonia gate holds.
- Frontend api::PreviewRequest / PreviewResponse / preview_mail helper / reply_inbox_mail helper all thread body_html end-to-end.
- genossi-frontend Cargo.toml includes ClipboardEvent + DataTransfer web-sys features (unblocks Plan 24-02 onpaste handler).
- 19 new Key::MailEditor* variants defined once in mod.rs and translated in BOTH de.rs and en.rs.
- No pre-existing test regresses; all four existing `reply_creates_*` tests still pass with the mechanical positional-None update.
</success_criteria>

<output>
Create `.planning/phases/24-wysiwyg-frontend-editor/24-01-SUMMARY.md` when done, using the summary.md template. Capture: three new fields (body_html on PreviewRequest, PreviewResponse, ReplyRequest), one extended trait signature (InboxService::reply), 19 new i18n keys, two Cargo web-sys features. Include the sanitize-gate wire proof (test names) and note that Plan 24-02 depends on this SUMMARY landing.
</output>
