---
phase: 23-html-mail-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/sqlite/20260702000000_mail_templates_add_body_html.sql
  - migrations/sqlite/20260702000001_mail_jobs_add_body_html.sql
  - migrations/sqlite/20260702000002_mail_recipients_add_rendered_html_body.sql
  - genossi_mail/src/dao.rs
  - genossi_mail/src/dao_sqlite.rs
autonomous: true
requirements:
  - HTML-03
tags: [rust, sqlite, dao, migration, schema]

must_haves:
  truths:
    - "Three forward-only ADD COLUMN migrations exist in migrations/sqlite/ and apply cleanly on a fresh SQLite DB (per D-06/D-07/D-08)."
    - "MailJob struct carries body_html: Option<Arc<str>> (D-06/D-07)."
    - "MailTemplate struct carries body_html: Option<Arc<str>> (D-06)."
    - "MailRecipient struct carries rendered_html_body: Option<Arc<str>> (D-08)."
    - "Legacy rows (body_html IS NULL) survive a DAO round-trip and read back as None — no coercion to Some(\"\") (D-09, RESEARCH Pitfall 4)."
    - "A MailJob roundtrip with body_html=Some(\"<b>test</b>\") persists and reads back byte-identically."
    - "Existing genossi_mail lib test suite still passes (backward-compat contract)."
  artifacts:
    - "migrations/sqlite/20260702000000_mail_templates_add_body_html.sql"
    - "migrations/sqlite/20260702000001_mail_jobs_add_body_html.sql"
    - "migrations/sqlite/20260702000002_mail_recipients_add_rendered_html_body.sql"
    - "genossi_mail/src/dao.rs (MailJob.body_html, MailTemplate.body_html, MailRecipient.rendered_html_body added)"
    - "genossi_mail/src/dao_sqlite.rs (INSERT/SELECT/UPDATE column lists updated for all three tables + inline CREATE TABLE test scaffolds)"
  key_links:
    - "MailJob.body_html field feeds Plan 02 render + Plan 03 build_message + Plan 04 worker persistence path."
    - "MailRecipient.rendered_html_body field is written by the worker (Plan 04) in the same UPDATE statement as rendered_body — column order in dao_sqlite.rs UPDATE must include the new column."
    - "New TEXT NULL columns must appear in dao_sqlite.rs INSERT column list, INSERT VALUES bind order, SELECT column list, and — for mail_recipients — UPDATE column list."
---

<objective>
Add three forward-only `ADD COLUMN … TEXT NULL` migrations for `mail_templates.body_html`, `mail_jobs.body_html`, and `mail_recipients.rendered_html_body`. Extend the DAO structs (`MailTemplate`, `MailJob`, `MailRecipient`) with the matching optional fields, wire them into every INSERT/SELECT/UPDATE statement in `dao_sqlite.rs`, and update the in-file `CREATE TABLE` test scaffolds so DAO tests still round-trip. Prove the NULL-legacy semantics (D-09, RESEARCH Pitfall 4).

Purpose: Foundation for HTML mail. Without persisted `body_html` on both `mail_jobs` and `mail_templates`, and without a `rendered_html_body` slot on `mail_recipients`, Plan 02 (render) and Plan 04 (worker persist) have nowhere to write. Legacy rows must survive as `None` — the "text-only" contract from HTML-03.

Output: Three new .sql migration files + two Rust source-file edits (`dao.rs` structs, `dao_sqlite.rs` queries + tests). Pure DAO layer — no service, no REST, no MIME. All Phase-22 charset/encoding behavior remains untouched.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-html-mail-backend/23-CONTEXT.md
@.planning/phases/23-html-mail-backend/23-RESEARCH.md
@genossi_mail/src/dao.rs
@genossi_mail/src/dao_sqlite.rs
@migrations/sqlite/20260603100000_mail_job_attach_repayment_letter.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add three forward-only migrations for body_html / rendered_html_body</name>
  <files>migrations/sqlite/20260702000000_mail_templates_add_body_html.sql, migrations/sqlite/20260702000001_mail_jobs_add_body_html.sql, migrations/sqlite/20260702000002_mail_recipients_add_rendered_html_body.sql</files>
  <read_first>
    - .planning/phases/23-html-mail-backend/23-CONTEXT.md § "Schema / Persistenz (HTML-01, HTML-03)" (D-06/D-07/D-08)
    - .planning/phases/23-html-mail-backend/23-RESEARCH.md § "Open Questions #3 Migration filenames" (exact timestamps + names)
    - migrations/sqlite/20260603100000_mail_job_attach_repayment_letter.sql (forward-only ADD COLUMN vorbild — comment-header style + trailing statement)
    - migrations/sqlite/20260614000000_mail_recipient_rendered_subject_body.sql if present, else browse /migrations/sqlite for the newest ADD COLUMN pattern
  </read_first>
  <behavior>
    - Migration 20260702000000: `ALTER TABLE mail_templates ADD COLUMN body_html TEXT NULL;` (D-06)
    - Migration 20260702000001: `ALTER TABLE mail_jobs ADD COLUMN body_html TEXT NULL;` (D-07)
    - Migration 20260702000002: `ALTER TABLE mail_recipients ADD COLUMN rendered_html_body TEXT NULL;` (D-08)
    - Each file has a comment header stating: purpose (HTML-03 / D-06/D-07/D-08), forward-only (no down migration — SQLite pre-3.35 cannot drop columns), NULL-legacy semantics (existing rows read back as NULL → text-only mail).
    - `sqlx migrate run --database-url sqlite:genossi.db --source migrations/sqlite` succeeds without warnings on a fresh DB.
  </behavior>
  <action>Create three .sql files in `migrations/sqlite/` with timestamps `20260702000000`, `20260702000001`, `20260702000002`. Each contains exactly one `ALTER TABLE … ADD COLUMN … TEXT NULL;` statement plus a 6-10 line comment header analogous to `20260603100000_mail_job_attach_repayment_letter.sql` (purpose sentence + "forward-only, no down migration" note + NULL-legacy explanation). NO `DEFAULT` clause (existing rows read back as NULL, matching D-09). Do NOT touch the older migration; do NOT introduce a new CREATE TABLE. After creating the files, run `sqlx migrate run --database-url sqlite:target/23-migration-test.db --source migrations/sqlite` against a scratch DB, then `sqlite3 target/23-migration-test.db ".schema mail_templates" ".schema mail_jobs" ".schema mail_recipients"` and verify the new columns appear. Delete the scratch DB when done.</action>
  <verify>
    <automated>test -f migrations/sqlite/20260702000000_mail_templates_add_body_html.sql && test -f migrations/sqlite/20260702000001_mail_jobs_add_body_html.sql && test -f migrations/sqlite/20260702000002_mail_recipients_add_rendered_html_body.sql && grep -q 'ADD COLUMN body_html TEXT' migrations/sqlite/20260702000000_mail_templates_add_body_html.sql && grep -q 'ADD COLUMN body_html TEXT' migrations/sqlite/20260702000001_mail_jobs_add_body_html.sql && grep -q 'ADD COLUMN rendered_html_body TEXT' migrations/sqlite/20260702000002_mail_recipients_add_rendered_html_body.sql</automated>
  </verify>
  <acceptance_criteria>
    - `test -f migrations/sqlite/20260702000000_mail_templates_add_body_html.sql` exits 0.
    - `test -f migrations/sqlite/20260702000001_mail_jobs_add_body_html.sql` exits 0.
    - `test -f migrations/sqlite/20260702000002_mail_recipients_add_rendered_html_body.sql` exits 0.
    - `grep -c 'ADD COLUMN body_html TEXT' migrations/sqlite/20260702000000_mail_templates_add_body_html.sql` returns `1`.
    - `grep -c 'ADD COLUMN body_html TEXT' migrations/sqlite/20260702000001_mail_jobs_add_body_html.sql` returns `1`.
    - `grep -c 'ADD COLUMN rendered_html_body TEXT' migrations/sqlite/20260702000002_mail_recipients_add_rendered_html_body.sql` returns `1`.
    - `grep -v '^--' migrations/sqlite/20260702000000_mail_templates_add_body_html.sql | grep -c 'DEFAULT'` returns `0` (no DEFAULT clause on the ALTER; NULL is the semantic).
    - `grep -v '^--' migrations/sqlite/20260702000001_mail_jobs_add_body_html.sql | grep -c 'DEFAULT'` returns `0`.
    - `grep -v '^--' migrations/sqlite/20260702000002_mail_recipients_add_rendered_html_body.sql | grep -c 'DEFAULT'` returns `0`.
    - Applying migrations against a fresh scratch DB via `sqlx migrate run` succeeds (exit 0) and the three columns are visible in `.schema` output.
  </acceptance_criteria>
  <done>Three .sql files exist, each contains exactly one `ADD COLUMN … TEXT NULL` statement (no DEFAULT), each has a comment header, and `sqlx migrate run` succeeds on a fresh scratch DB.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend DAO structs with body_html / rendered_html_body fields (dao.rs)</name>
  <files>genossi_mail/src/dao.rs</files>
  <read_first>
    - genossi_mail/src/dao.rs (lines 27-49 MailJob; lines 51-73 MailRecipient; lines 215-224 MailTemplate)
    - .planning/phases/23-html-mail-backend/23-CONTEXT.md § "Schema / Persistenz" (D-06/D-07/D-08)
    - .planning/phases/22-8bit-shared-mail-body-helper/22-02-SUMMARY.md § "New shared MIME factory" (Arc<str> pattern for optional strings, `Option<Arc<str>>`)
  </read_first>
  <behavior>
    - `MailJob.body_html: Option<Arc<str>>` field added (D-07).
    - `MailTemplate.body_html: Option<Arc<str>>` field added (D-06).
    - `MailRecipient.rendered_html_body: Option<Arc<str>>` field added (D-08).
    - Field position: alphabetical / grouped with sibling optional fields (place `body_html` right after `body`; place `rendered_html_body` right after `rendered_body`).
    - `PartialEq`/`Eq`/`Debug`/`Clone` derives still apply (Option<Arc<str>> supports them all).
    - Test builders/mocks in the SAME file (`dao.rs::tests` if any) still compile.
  </behavior>
  <action>Edit `genossi_mail/src/dao.rs`. In `struct MailJob` (~line 28-49) add `pub body_html: Option<Arc<str>>,` immediately after the `pub body: Arc<str>,` line. In `struct MailTemplate` (~line 215-224) add `pub body_html: Option<Arc<str>>,` immediately after the `pub body: Arc<str>,` line. In `struct MailRecipient` (~line 51-73) add `pub rendered_html_body: Option<Arc<str>>,` immediately after the `pub rendered_body: Option<Arc<str>>,` line. Do NOT touch the trait definitions (`MailJobDao`, `MailRecipientDao`, `MailTemplateDao`) — the four required methods still have the same signatures; only the DTO shape changes. Do NOT introduce any new `bool` field (project rule "Immer Enum statt bool" — presence-vs-absence is already modeled by `Option::None`/`Some`). Run `cargo build -p genossi_mail` — the workspace WILL fail to build until Task 3 updates `dao_sqlite.rs` to populate the new fields; that is expected and Task 3 closes the loop.</action>
  <verify>
    <automated>grep -A2 'pub struct MailJob' genossi_mail/src/dao.rs | grep -c 'body_html: Option<Arc<str>>' | grep -q '^0$' && exit 1; grep -c 'body_html: Option<Arc<str>>' genossi_mail/src/dao.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub body_html: Option<Arc<str>>' genossi_mail/src/dao.rs` returns `2` (once inside MailJob, once inside MailTemplate).
    - `grep -c 'pub rendered_html_body: Option<Arc<str>>' genossi_mail/src/dao.rs` returns `1`.
    - `grep -c 'pub body_html: bool' genossi_mail/src/dao.rs` returns `0` (project rule: enum/Option, never bool for presence).
    - `awk '/pub struct MailJob \{/,/^\}/' genossi_mail/src/dao.rs | grep -n 'body:\|body_html:' | head -2` shows `body:` then `body_html:` in that order.
    - `awk '/pub struct MailRecipient \{/,/^\}/' genossi_mail/src/dao.rs | grep -n 'rendered_body:\|rendered_html_body:'` shows `rendered_body:` then `rendered_html_body:` in that order.
  </acceptance_criteria>
  <done>Three new optional-string fields added to the three DAO structs, positioned next to their sibling text field, using `Option<Arc<str>>` per the established pattern. `cargo check -p genossi_mail --lib` may still fail (dao_sqlite.rs not yet updated) — Task 3 restores build-green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire body_html / rendered_html_body into dao_sqlite.rs and add roundtrip tests</name>
  <files>genossi_mail/src/dao_sqlite.rs</files>
  <read_first>
    - genossi_mail/src/dao_sqlite.rs lines 60-100 (`MailJobRow` struct + `From` impl for MailJob)
    - genossi_mail/src/dao_sqlite.rs lines 120-175 (mail_jobs INSERT/SELECT column lists)
    - genossi_mail/src/dao_sqlite.rs lines 210-380 (`MailRecipientRow`, INSERT, SELECT, UPDATE for mail_recipients)
    - genossi_mail/src/dao_sqlite.rs lines 1090-1230 (`MailTemplateRow`, mail_templates INSERT/UPDATE/SELECT)
    - genossi_mail/src/dao_sqlite.rs lines 1290-1400 (in-file `CREATE TABLE mail_templates/mail_jobs/mail_recipients` scaffolds inside `#[cfg(test)]` — MUST also gain the new column, otherwise DAO tests won't roundtrip)
    - genossi_mail/src/dao_sqlite.rs lines 1720-1870 (existing `test_recipient_update_persists_rendered_subject_body` — copy pattern for new test)
  </read_first>
  <behavior>
    - `MailJobRow` and `MailTemplateRow` gain a `body_html: Option<String>` field mapped from the DB; `MailRecipientRow` gains `rendered_html_body: Option<String>`.
    - `From<MailJobRow>` / `From<MailTemplateRow>` / `From<MailRecipientRow>` for the corresponding DAO struct maps `Option<String>` → `Option<Arc<str>>` via `.as_deref().map(Arc::from)` (mirroring the existing `rendered_body` pattern).
    - INSERT statements for mail_jobs / mail_templates / mail_recipients include the new column in the column list AND a matching `?` in VALUES; the `.bind()` chain gains `.bind(job.body_html.as_deref())` / analog for the other two.
    - SELECT statements (find_by_id, all, find_by_job_id, find_recipients_without_rendered, find_by_name, dump_all) include the new column in the column list.
    - UPDATE statement for `mail_recipients` (line ~331) includes `rendered_html_body = ?` in the SET clause AND a matching `.bind(recipient.rendered_html_body.as_deref())` — position it directly after the `rendered_body = ?` binding to match the write path Plan 04 needs.
    - The mail_templates `INSERT` and `UPDATE` statements also gain `body_html` column + bind.
    - In-file `CREATE TABLE mail_templates` / `mail_jobs` / `mail_recipients` scaffolds inside `#[cfg(test)] mod tests` gain `body_html TEXT` / `rendered_html_body TEXT` (NULL by default; NO NOT NULL, NO DEFAULT).
    - Existing seed helpers/factories inside `#[cfg(test)] mod tests` (search for lines like `rendered_body: None,`) gain `body_html: None,` / `rendered_html_body: None,` so tests compile.
    - New test `mail_job_body_html_roundtrip`: insert a `MailJob` with `body_html: Some(Arc::from("<b>Hallo</b>"))`, read back via `find_by_id`, assert byte-identical roundtrip.
    - New test `mail_job_body_html_null_roundtrip`: insert with `body_html: None`, read back, assert `None` (NOT `Some("")` — RESEARCH Pitfall 4).
    - New test `mail_recipient_update_persists_rendered_html_body`: mirrors existing `test_recipient_update_persists_rendered_subject_body` but sets `rendered_html_body: Some(Arc::from("<p>Rendered</p>"))` in the UPDATE and asserts the readback.
    - Whole crate compiles: `cargo build -p genossi_mail`.
    - All prior lib tests still pass.
  </behavior>
  <action>Edit `genossi_mail/src/dao_sqlite.rs`. Add `body_html: Option<String>` to `MailJobRow` and `MailTemplateRow`; add `rendered_html_body: Option<String>` to `MailRecipientRow`. Update the corresponding `From` impls to project into `Option<Arc<str>>` via `.as_deref().map(Arc::from)` (copy the `rendered_body` line's pattern verbatim). In every INSERT statement string, extend the parenthesised column list to include the new column (append at the end of the parenthesised list to avoid renumbering existing binds), extend the VALUES `(?, ?, …)` with one more `?`, and add the matching `.bind(entity.body_html.as_deref())` / `.bind(recipient.rendered_html_body.as_deref())` at the end of the bind chain. For mail_recipients UPDATE (~line 331), append `, rendered_html_body = ?` to the SET clause AND insert `.bind(recipient.rendered_html_body.as_deref())` immediately after the existing `.bind(recipient.rendered_body.as_deref())` (position matters — the bind order must match the SET-clause order). Extend every SELECT column list (5 sites: mail_jobs find_by_id/all, mail_recipients find_by_job_id/next_pending/find_recipients_without_rendered, mail_templates dump_all/find_by_id/all/find_by_name). Inside the `#[cfg(test)] mod tests` block, extend the inline `CREATE TABLE` scaffolds for `mail_templates`, `mail_jobs`, `mail_recipients` with `body_html TEXT` and `rendered_html_body TEXT` (both nullable, no DEFAULT). Fix every test-helper factory / seed function inside the same test module that currently writes `rendered_body: None,` by adding `body_html: None,` (for MailJob) / `rendered_html_body: None,` (for MailRecipient) — use `grep -n 'rendered_body: None' genossi_mail/src/dao_sqlite.rs` to enumerate. Append three new tests to the tests module: `mail_job_body_html_roundtrip`, `mail_job_body_html_null_roundtrip`, `mail_recipient_update_persists_rendered_html_body` — model the first two on the existing `test_recipient_update_persists_rendered_subject_body` and the third by cloning the same test and switching the field. Do NOT modify the `body TEXT NOT NULL` column for `mail_jobs` (existing behavior — text body always required per HTML-02). Do NOT modify the audited-entity trait pattern (Mail entities are NOT audited; CLAUDE.md confirms Auditable applies only to Member/MemberAction/MemberDocument/Application). Run `DATABASE_URL=sqlite:genossi.db cargo sqlx prepare` locally after tests pass — this is a follow-up step handled by the operator, not the test.</action>
  <verify>
    <automated>cargo test -p genossi_mail --lib dao_sqlite:: 2>&1 | tee /tmp/23-01-t3.log | tail -30 && grep -q '0 failed' /tmp/23-01-t3.log</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p genossi_mail` exits 0.
    - `cargo test -p genossi_mail --lib` exits 0 with `0 failed`.
    - `grep -c 'body_html' genossi_mail/src/dao_sqlite.rs` >= 12 (col-list, bind, row struct, from impl for mail_jobs and mail_templates each; plus tests) — a smoke count that rejects "forgot half the sites".
    - `grep -c 'rendered_html_body' genossi_mail/src/dao_sqlite.rs` >= 8 (col-list, bind, row struct, from impl, UPDATE, tests).
    - `grep -c 'fn mail_job_body_html_roundtrip' genossi_mail/src/dao_sqlite.rs` returns `1`.
    - `grep -c 'fn mail_job_body_html_null_roundtrip' genossi_mail/src/dao_sqlite.rs` returns `1`.
    - `grep -c 'fn mail_recipient_update_persists_rendered_html_body' genossi_mail/src/dao_sqlite.rs` returns `1`.
    - `grep -c 'body_html TEXT NOT NULL' genossi_mail/src/dao_sqlite.rs` returns `0` (all new columns are NULL — column is nullable per D-06/D-07/D-08).
    - `grep -c 'rendered_html_body TEXT NOT NULL' genossi_mail/src/dao_sqlite.rs` returns `0`.
    - `cargo clippy -p genossi_mail --lib` exits 0 (no new warnings introduced by this task).
  </acceptance_criteria>
  <done>`cargo build -p genossi_mail` and `cargo test -p genossi_mail --lib` pass. Three new tests prove NULL-legacy semantics and byte-identical roundtrip for both `body_html` (mail_jobs, mail_templates) and `rendered_html_body` (mail_recipients). All prior tests continue to pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite storage → DAO struct | Persisted TEXT bytes deserialize to Option<Arc<str>>. Legacy rows (pre-migration) load as None. No user-supplied bytes cross this boundary in Plan 01 — plans 02-04 handle sanitize-on-store. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-01 | Tampering | mail_recipients.rendered_html_body write path | low | mitigate | Nullable column; Plan 04 worker only writes `Some(_)` when `job.body_html.is_some()` (RESEARCH Pitfall 4). Plan 01 test `mail_job_body_html_null_roundtrip` proves NULL survives roundtrip in this plan. |
| T-23-02 | Information Disclosure | Migration applied to production DB with existing rows | low | accept | Forward-only ADD COLUMN … NULL is a zero-touch schema change: existing rows read back as NULL, no data mutated. Confirmed by scratch-DB apply in Task 1. |
</threat_model>

<verification>
- All three migrations apply cleanly on a fresh SQLite DB.
- `cargo build -p genossi_mail && cargo test -p genossi_mail --lib` exits 0 with 3 new tests passing.
- Legacy roundtrip test asserts `None` (not `Some("")`) — closes RESEARCH Pitfall 4.
- Column count grep smoke tests reject "forgot half the sites".
</verification>

<success_criteria>
- Three .sql migration files exist and apply cleanly.
- Three DAO structs each gained one `Option<Arc<str>>` field, positioned next to their sibling text field.
- `dao_sqlite.rs` INSERT/SELECT/UPDATE column lists include the new columns in matching bind order.
- In-file test-scaffold `CREATE TABLE`s cover the new columns.
- Three new roundtrip tests (Some/None body_html on mail_jobs; rendered_html_body UPDATE on mail_recipients) pass.
- Whole `genossi_mail --lib` test suite passes (no regressions).
</success_criteria>

<output>
Create `.planning/phases/23-html-mail-backend/23-01-SUMMARY.md` when done, listing:
- Files created (3 migrations)
- Files modified (dao.rs, dao_sqlite.rs)
- Field additions (3 struct fields)
- New tests (3 roundtrip tests)
- Any test-scaffold changes needed for build-green
- `jj log` hashes of the atomic per-task commits
</output>
