---
phase: 15-service-rest-kuendigung-aufstockung
plan: 03
type: execute
wave: 3
depends_on: [15-02]
files_modified:
  - genossi_service_impl/src/membership_adjust.rs
autonomous: true
requirements: [UPGD-01, UPGD-02, UPGD-03, UPGD-04, PERM-01, PERM-02, AUDT-01]
tags: [rust, service-impl, audit-macro, transaction, increase-shares]

must_haves:
  truths:
    - "increase_shares() pruefen ADMIN_PRIVILEGE per check_permission, validiert das Datum, prueft EntityNotFound, blockt gekuendigte Mitglieder mit HTTP 400 (UPGD-04), validiert shares > 0, erzeugt MemberAction::Aufstockung via audited_create! mit Process-String 'member-adjust.upgrade' und erhoeht Member.current_shares atomar via audited_update! in derselben Tx (UPGD-03)"
    - "MemberAction::Aufstockung hat shares_change=+shares, transfer_member_id=None, effective_date=None (UPGD-02 sofort wirksam), date=willensbekundung_date"
    - "Member.current_shares-Mutation laeuft ueber generischen MemberDao::update + audited_update!-Macro (NICHT targeted DAO-method, D-15-03)"
    - "Member.version wird per uuid_service.new_v4() vor audited_update! gebumpt (Optimistic-Locking)"
    - "Kein direkter `member_action_dao.create(...)` / `member_dao.update(...)` ausserhalb der `audited_*!`-Macros (AUDT-01 Grep-Gate; jetzt fuer beide DAOs verifiziert)"
    - "Keine Member.exit_date-Mutation in increase_shares — Aufstockung beeinflusst kein Exit-Date (Schutz vor Behavior-Drift)"
    - "Mindestens 4 Service-Unit-Tests gruen (Happy-Path, Cancelled-Member-Block, Permission-Denied, invalid-shares)"
  artifacts:
    - path: "genossi_service_impl/src/membership_adjust.rs"
      provides: "MembershipAdjustServiceImpl::increase_shares() + Service-Unit-Tests"
      contains: "ActionType::Aufstockung"
  key_links:
    - from: "MembershipAdjustServiceImpl::increase_shares"
      to: "audited_create! macro auf member_action_dao"
      via: "crate::audited_create!(self, self.member_action_dao, &action_entity, UPGRADE_PROCESS, &user_id, tx)"
      pattern: "audited_create!\\([\\s\\S]*?UPGRADE_PROCESS"
    - from: "MembershipAdjustServiceImpl::increase_shares"
      to: "audited_update! macro auf member_dao (current_shares-Bump)"
      via: "crate::audited_update!(self, self.member_dao, member_entity.id, &updated_entity, UPGRADE_PROCESS, &user_id, tx)"
      pattern: "audited_update!\\([\\s\\S]*?UPGRADE_PROCESS"
---

<objective>
`MembershipAdjustService::increase_shares` implementieren — atomare Multi-Write-Operation (MemberAction::Aufstockung-Create + Member.current_shares-Update) in einer Transaction.

Purpose:
- Implementiert UPGD-01..04 + PERM-01 + AUDT-01.
- Etabliert das `audited_update!`-Macro-Pattern fuer Member-Field-Mutation in v1.2 (D-15-03 — generisches `MemberDao::update`, kein targeted DAO-Method).
- Reuses die Plan-01-`validate_willensbekundung_date` und die Plan-02-`gen_service_impl!`-Struktur (inkl. Plan-02-TestDeps + MockTest*-Mocks).

Output:
- Vervollstaendigtes `genossi_service_impl/src/membership_adjust.rs` mit `increase_shares`-Body (ersetzt den Stub aus Plan 02) + 4 Service-Unit-Tests.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md

VCS: this project uses jj (Jujutsu). The executor should NOT run git commands directly; rely on GSD's commit step which already wraps jj-aware commits.
</execution_context>

<context>
@.planning/phases/15-service-rest-kuendigung-aufstockung/15-CONTEXT.md
@.planning/phases/15-service-rest-kuendigung-aufstockung/15-PATTERNS.md
@.planning/phases/15-service-rest-kuendigung-aufstockung/15-02-PLAN.md
@.planning/phases/15-service-rest-kuendigung-aufstockung/15-02-SUMMARY.md
@CLAUDE.md

<interfaces>
From `genossi_service_impl/src/audit_macros.rs:42-79` (audited_update!-Signatur):
```
audited_update!($self:expr, $dao:expr, $entity_id:expr, $new_entity:expr, $process:expr, $user_id:expr, $tx:expr)
```

Macro-Verhalten:
- Loads old entity via `$dao.find_by_id($entity_id, $tx.clone()).await?.ok_or(EntityNotFound)?`
- Calls `$dao.update($new_entity, $process, $tx.clone()).await?`
- Builds audit-entries fuer geaenderte Felder via `build_update_entries(&old, $new_entity, ...)` (nur diff)

From `genossi_dao/src/member.rs:111` (MemberDao::update — generisch):
```rust
async fn update(&self, entity: &MemberEntity, process: &str, tx: Self::Transaction) -> Result<(), DaoError>;
```

From `genossi_dao/src/member.rs:69-95` (MemberEntity-Felder relevant):
```rust
pub struct MemberEntity {
    pub id: Uuid,
    pub current_shares: i32,      // wird +shares modifiziert
    pub exit_date: Option<time::Date>,  // muss None bleiben (UPGD-04 -> Block wenn Some)
    pub version: Uuid,            // wird via uuid_service.new_v4() gebumpt (Optimistic-Locking)
    // ... weitere Felder
}
```

From `genossi_service/src/member.rs:12-38` (Member Domain-Struct):
```rust
pub struct Member {
    pub id: Uuid,
    pub current_shares: i32,        // <-- direkter pub-Feld-Zugriff (Assertions auf member.current_shares moeglich)
    pub exit_date: Option<time::Date>,
    // ... weitere Felder
}
```
Wichtig: `Member.current_shares` ist `pub i32` — KEIN Accessor noetig, direkter Field-Zugriff `member.current_shares` ist die korrekte Assertion-Form.

From `genossi_dao/src/member_action.rs` (ActionType::Aufstockung — existing):
```rust
ActionType::Aufstockung
```

From Plan 02 (Wave 2 outputs):
- `MembershipAdjustServiceImpl` struct via `gen_service_impl!` (6 Deps)
- `CANCEL_PROCESS` und `UPGRADE_PROCESS` Konstanten (UPGRADE_PROCESS wird hier zum ersten Mal genutzt)
- Stub-Impl von `increase_shares` (gibt InternalError zurueck) — wird hier ersetzt
- **Vollstaendiges `#[cfg(test)] mod service_tests`-Modul mit TestDeps, MockTest*-Daos, build_service-Helper, sample_member_entity — Plan 03 fuegt die 4 increase_shares-Tests in dieses existing Modul ein.**
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: increase_shares-Body + 4 Service-Unit-Tests</name>
  <files>genossi_service_impl/src/membership_adjust.rs</files>
  <read_first>
    - genossi_service_impl/src/membership_adjust.rs (Plan-02-Output mit Stub-Impl von increase_shares + service_tests-Modul mit TestDeps/MockTest*)
    - genossi_service_impl/src/audit_macros.rs (Z. 42-79 — audited_update!-Macro-Signatur)
    - genossi_service_impl/src/member_action.rs (Z. 360-400 — MemberActionService::update als Vorbild fuer audited_update!-Anwendung)
    - genossi_service_impl/src/member.rs (Z. 412-712 — service_tests-Modul-Pattern als Vorbild)
    - genossi_dao/src/member.rs (Z. 111 — MemberDao::update Signatur; Z. 69-95 — MemberEntity-Felder)
    - genossi_service/src/member.rs (Z. 12-38 — Member.current_shares ist `pub i32`)
    - .planning/phases/15-service-rest-kuendigung-aufstockung/15-PATTERNS.md Section Pattern 3c (Adaption fuer increase_shares) + Section Pattern 5 (audited_update!)
    - .planning/phases/15-service-rest-kuendigung-aufstockung/15-CONTEXT.md D-15-03, D-15-15, specifics (UPGD)
  </read_first>
  <behavior>
    increase_shares-Sequence:
    1. `tx = use_transaction(tx).await?`
    2. `user_id = current_user_id(context.clone()).await?.unwrap_or("SYSTEM")`
    3. `check_permission(ADMIN_PRIVILEGE, context).await?` (PERM-01)
    4. **Pre-validation**: `shares > 0` Check -> ValidationError wenn shares <= 0 (Discretion aus CONTEXT)
    5. **PERM-02**: `today` + `validate_willensbekundung_date(willensbekundung_date, today)`
    6. `member_entity = member_dao.find_by_id(...).await?.ok_or(EntityNotFound)?`
    7. **UPGD-04**: `if member_entity.exit_date.is_some() { return Err(ServiceError::ValidationError(vec![ValidationFailureItem { field: "member_id", message: "cannot upgrade cancelled member" }])) }` -> HTTP 400
    8. `new_action = MemberAction { action_type: ActionType::Aufstockung, shares_change: +shares, transfer_member_id: None, effective_date: None, date: willensbekundung_date, ... }` (UPGD-02 sofort wirksam -> effective_date = None)
    9. `audited_create!(self, self.member_action_dao, &(&new_action).into(), UPGRADE_PROCESS, &user_id, tx)` (AUDT-01)
    10. **UPGD-03 (atomar in derselben Tx)**: `updated_entity = member_entity.clone(); updated_entity.current_shares += shares; updated_entity.version = uuid_service.new_v4().await; audited_update!(self, self.member_dao, member_entity.id, &updated_entity, UPGRADE_PROCESS, &user_id, tx)` (D-15-03)
    11. `transaction_dao.commit(tx).await?`
    12. `Ok((new_action, Member::from(&updated_entity)))`

    Unit-Tests (alle 4 vollstaendig auszuformulieren):
    - `test_increase_shares_happy_path`: aktiver Member mit current_shares=2 + shares=3 -> Action ActionType::Aufstockung, shares_change=3, member.current_shares=5
    - `test_increase_shares_cancelled_member_blocked`: Mock-Member mit exit_date=Some(...) -> ValidationError mit message containing "cancelled" (HTTP 400)
    - `test_increase_shares_permission_denied`: check_permission gibt PermissionDenied -> propagiert (HTTP 401 via global Mapping)
    - `test_increase_shares_invalid_shares_zero`: shares=0 -> ValidationError mit field="shares"
  </behavior>
  <action>
    1. Bestehenden `increase_shares`-Stub aus Plan 02 (`Err(ServiceError::InternalError(Arc::from("increase_shares — Plan 03")))`) durch die volle Implementation ersetzen:

```rust
async fn increase_shares(
    &self,
    member_id: Uuid,
    shares: i32,
    willensbekundung_date: time::Date,
    context: Authentication<Self::Context>,
    tx: Option<Self::Transaction>,
) -> Result<(MemberAction, Member), ServiceError> {
    let tx = self.transaction_dao.use_transaction(tx).await?;

    let user_id = self
        .permission_service
        .current_user_id(context.clone())
        .await?
        .unwrap_or_else(|| "SYSTEM".to_string());

    self.permission_service
        .check_permission(ADMIN_PRIVILEGE, context)
        .await?;

    // Pre-validation: shares > 0 (Discretion per CONTEXT §claude_discretion)
    if shares <= 0 {
        return Err(ServiceError::ValidationError(vec![ValidationFailureItem {
            field: Arc::from("shares"),
            message: Arc::from("shares must be greater than 0"),
        }]));
    }

    // PERM-02: Datum-Bounds-Validierung
    let today = time::OffsetDateTime::now_utc().date();
    let date_errors = validate_willensbekundung_date(willensbekundung_date, today);
    if !date_errors.is_empty() {
        return Err(ServiceError::ValidationError(date_errors));
    }

    let member_entity = self
        .member_dao
        .find_by_id(member_id, tx.clone())
        .await?
        .ok_or(ServiceError::EntityNotFound(member_id))?;

    // UPGD-04: blockt gekuendigte Member -> HTTP 400 via ValidationError
    if member_entity.exit_date.is_some() {
        return Err(ServiceError::ValidationError(vec![ValidationFailureItem {
            field: Arc::from("member_id"),
            message: Arc::from("cannot upgrade cancelled member"),
        }]));
    }

    let now = time::OffsetDateTime::now_utc();
    let new_action = MemberAction {
        id: self.uuid_service.new_v4().await,
        member_id,
        action_type: ActionType::Aufstockung,
        date: willensbekundung_date,
        shares_change: shares, // UPGD-03 positiv
        transfer_member_id: None,
        effective_date: None, // UPGD-02 sofort wirksam (kein H1/H2)
        comment: None,
        created: time::PrimitiveDateTime::new(now.date(), now.time()),
        deleted: None,
        version: self.uuid_service.new_v4().await,
    };

    // AUDT-01: Action via audited_create!
    let action_entity: MemberActionEntity = (&new_action).into();
    crate::audited_create!(
        self,
        self.member_action_dao,
        &action_entity,
        UPGRADE_PROCESS,
        &user_id,
        tx
    );

    // UPGD-03 atomar: Member.current_shares + Optimistic-Lock-Version
    // D-15-03: generischer MemberDao::update + audited_update! statt targeted DAO-method
    let mut updated_entity = member_entity.clone();
    updated_entity.current_shares += shares;
    updated_entity.version = self.uuid_service.new_v4().await;

    crate::audited_update!(
        self,
        self.member_dao,
        member_entity.id,
        &updated_entity,
        UPGRADE_PROCESS,
        &user_id,
        tx
    );

    self.transaction_dao.commit(tx).await?;

    Ok((new_action, Member::from(&updated_entity)))
}
```

    2. Im existing `#[cfg(test)] mod service_tests` aus Plan 02 die 4 increase_shares-Tests ergaenzen. **Datum-Fragility-Fix (analog Plan 02)**: Test-Datum wird relativ zu `OffsetDateTime::now_utc().date()` abgeleitet.

```rust
// ---------- Test 5: Happy-Path Aufstockung ----------
#[tokio::test]
async fn test_increase_shares_happy_path() {
    let today = time::OffsetDateTime::now_utc().date();
    let willensbekundung = today
        .replace_month(time::Month::March).expect("March in today.year() exists")
        .replace_day(15).expect("Day 15 always valid");

    let member_id = Uuid::new_v4();

    let mut member_dao = MockTestMemberDao::new();
    let mid = member_id;
    // find_by_id wird 2x aufgerufen: 1x Service-Body, 1x audited_update! Macro (find_by_id auf member_dao fuer old-entity-load)
    member_dao.expect_find_by_id()
        .times(2)
        .returning(move |_, _| {
            let mut m = sample_member_entity(mid, None);
            m.current_shares = 2;
            Ok(Some(m))
        });
    // audited_update! ruft member_dao.update mit current_shares=5 (=2+3) und Process="member-adjust.upgrade"
    member_dao.expect_update()
        .withf(|entity, process, _| {
            process == "member-adjust.upgrade" && entity.current_shares == 5
        })
        .times(1)
        .returning(|_, _, _| Ok(()));

    let mut member_action_dao = MockTestMemberActionDao::new();
    // audited_create! ruft create() mit Process="member-adjust.upgrade"
    member_action_dao.expect_create()
        .withf(|_entity, process, _tx| process == "member-adjust.upgrade")
        .times(1)
        .returning(|_, _, _| Ok(()));

    let mut audit_log_dao = MockTestAuditLogDao::new();
    // 2x get_latest_hash + create_entries (1x fuer Action, 1x fuer Member-Diff)
    audit_log_dao.expect_get_latest_hash().returning(|_| Ok(None));
    audit_log_dao.expect_create_entries().returning(|_, _| Ok(()));

    let mut permission_service = MockTestPermissionService::new();
    permission_service.expect_current_user_id()
        .returning(|_| Ok(Some("admin".to_string())));
    permission_service.expect_check_permission()
        .withf(|priv_, _| priv_ == "admin")
        .returning(|_, _| Ok(()));

    let service = build_service(
        member_dao, member_action_dao, audit_log_dao,
        permission_service, setup_tx_dao(),
    );

    let result = service.increase_shares(
        member_id, 3, willensbekundung, Authentication::Full, None
    ).await;

    let (action, member) = result.expect("increase_shares should succeed");
    assert_eq!(action.action_type, ActionType::Aufstockung);
    assert_eq!(action.shares_change, 3);
    assert_eq!(action.effective_date, None); // UPGD-02 sofort wirksam
    // Direkter pub-Feld-Zugriff per genossi_service::member::Member (siehe interfaces).
    // Erwartung: 2 (sample) + 3 (shares) = 5.
    assert_eq!(member.current_shares, 5);
}

// ---------- Test 6: Cancelled-Member-Block ----------
#[tokio::test]
async fn test_increase_shares_cancelled_member_blocked() {
    let today = time::OffsetDateTime::now_utc().date();
    let willensbekundung = today
        .replace_month(time::Month::March).unwrap()
        .replace_day(15).unwrap();
    let already_exit = time::Date::from_calendar_date(
        today.year(), time::Month::December, 31
    ).unwrap();

    let member_id = Uuid::new_v4();

    let mut member_dao = MockTestMemberDao::new();
    let mid = member_id;
    member_dao.expect_find_by_id()
        .times(1)  // Block bricht VOR audited_create!/audited_update! ab
        .returning(move |_, _| Ok(Some(sample_member_entity(mid, Some(already_exit)))));

    // KEINE create/update-Calls erwartet
    let member_action_dao = MockTestMemberActionDao::new();
    let audit_log_dao = MockTestAuditLogDao::new();

    let mut permission_service = MockTestPermissionService::new();
    permission_service.expect_current_user_id()
        .returning(|_| Ok(Some("admin".to_string())));
    permission_service.expect_check_permission()
        .returning(|_, _| Ok(()));

    let service = build_service(
        member_dao, member_action_dao, audit_log_dao,
        permission_service, setup_tx_dao(),
    );

    let result = service.increase_shares(
        member_id, 3, willensbekundung, Authentication::Full, None
    ).await;

    match result {
        Err(ServiceError::ValidationError(errs)) => {
            assert!(
                errs.iter().any(|e| e.message.contains("cancelled")),
                "expected ValidationError containing 'cancelled', got: {:?}",
                errs
            );
        }
        other => panic!("Expected ValidationError, got {:?}", other),
    }
}

// ---------- Test 7: Permission Denied ----------
#[tokio::test]
async fn test_increase_shares_permission_denied() {
    let today = time::OffsetDateTime::now_utc().date();
    let willensbekundung = today
        .replace_month(time::Month::March).unwrap()
        .replace_day(15).unwrap();

    let member_dao = MockTestMemberDao::new();
    let member_action_dao = MockTestMemberActionDao::new();
    let audit_log_dao = MockTestAuditLogDao::new();

    let mut permission_service = MockTestPermissionService::new();
    permission_service.expect_current_user_id()
        .returning(|_| Ok(Some("user".to_string())));
    permission_service.expect_check_permission()
        .returning(|_, _| Err(ServiceError::PermissionDenied));

    let service = build_service(
        member_dao, member_action_dao, audit_log_dao,
        permission_service, setup_tx_dao(),
    );

    let result = service.increase_shares(
        Uuid::new_v4(), 3, willensbekundung, Authentication::Full, None
    ).await;

    assert!(
        matches!(result, Err(ServiceError::PermissionDenied)),
        "expected PermissionDenied (mapped to HTTP 401 via genossi_rest/src/lib.rs:115), got {:?}",
        result
    );
}

// ---------- Test 8: Invalid shares (shares=0) ----------
#[tokio::test]
async fn test_increase_shares_invalid_shares_zero() {
    let today = time::OffsetDateTime::now_utc().date();
    let willensbekundung = today
        .replace_month(time::Month::March).unwrap()
        .replace_day(15).unwrap();

    let member_dao = MockTestMemberDao::new();
    let member_action_dao = MockTestMemberActionDao::new();
    let audit_log_dao = MockTestAuditLogDao::new();

    let mut permission_service = MockTestPermissionService::new();
    permission_service.expect_current_user_id()
        .returning(|_| Ok(Some("admin".to_string())));
    permission_service.expect_check_permission()
        .returning(|_, _| Ok(()));

    let service = build_service(
        member_dao, member_action_dao, audit_log_dao,
        permission_service, setup_tx_dao(),
    );

    let result = service.increase_shares(
        Uuid::new_v4(), 0, willensbekundung, Authentication::Full, None
    ).await;

    match result {
        Err(ServiceError::ValidationError(errs)) => {
            assert!(
                errs.iter().any(|e| &*e.field == "shares"),
                "expected ValidationError with field='shares', got: {:?}",
                errs
            );
        }
        other => panic!("Expected ValidationError with field=shares, got {:?}", other),
    }
}
```

    Im Pre-Test-Check: Der bisherige Stub `Err(ServiceError::InternalError(Arc::from("increase_shares — Plan 03")))` muss vollstaendig durch die obige Implementation ersetzt werden. `grep -c 'increase_shares — Plan 03' genossi_service_impl/src/membership_adjust.rs` muss 0 zurueckgeben.
  </action>
  <verify>
    <automated>cargo test -p genossi_service_impl --lib membership_adjust::service_tests::test_increase_shares 2>&amp;1 | tail -20 &amp;&amp; bash -c "grep -v '^//' /home/neosam/programming/rust/projects/genossi3/genossi_service_impl/src/membership_adjust.rs | grep -v '^ \\*' | grep -cE 'self\\.member_action_dao\\.create\\(|self\\.member_dao\\.update\\('" &amp;&amp; grep -cE 'audited_update!\s*\([\s\S]{0,200}UPGRADE_PROCESS' /home/neosam/programming/rust/projects/genossi3/genossi_service_impl/src/membership_adjust.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p genossi_service_impl --lib membership_adjust::service_tests::test_increase_shares_happy_path` exits 0
    - `cargo test -p genossi_service_impl --lib membership_adjust::service_tests::test_increase_shares_cancelled_member_blocked` exits 0
    - `cargo test -p genossi_service_impl --lib membership_adjust::service_tests::test_increase_shares_permission_denied` exits 0
    - `cargo test -p genossi_service_impl --lib membership_adjust::service_tests::test_increase_shares_invalid_shares_zero` exits 0
    - `grep -c 'ActionType::Aufstockung' genossi_service_impl/src/membership_adjust.rs` >= 1
    - `grep -c 'shares_change: shares' genossi_service_impl/src/membership_adjust.rs` >= 1
    - `grep -c 'effective_date: None' genossi_service_impl/src/membership_adjust.rs` >= 1 (UPGD-02)
    - `grep -c 'audited_update!' genossi_service_impl/src/membership_adjust.rs` >= 1 (D-15-03)
    - `grep -c 'audited_create!' genossi_service_impl/src/membership_adjust.rs` >= 2 (jeweils 1 fuer cancel und upgrade)
    - AUDT-01-Grep-Gate (no direct create/update bypass): `bash -c "grep -v '^//' genossi_service_impl/src/membership_adjust.rs | grep -v '^ \\*' | grep -cE 'self\\.member_action_dao\\.create\\(|self\\.member_dao\\.update\\('"` returns 0
    - `grep -c 'increase_shares — Plan 03' genossi_service_impl/src/membership_adjust.rs` returns 0 (Stub wurde ersetzt)
    - Roadmap-Constraint exit_date-Touch: `grep -c 'member_dao\\.update_dates\\|member_dao\\.update_migrated' genossi_service_impl/src/membership_adjust.rs` returns 0 (kein direkter exit_date-Pfad in Phase 15)
    - Direkter Feld-Assertion in Test 5: `grep -c 'member.current_shares, 5' genossi_service_impl/src/membership_adjust.rs` >= 1 (BLOCKER 4: Kommentar wurde durch konkrete assertion ersetzt)
    - `cargo test -p genossi_service_impl --lib membership_adjust` exits 0 (alle Tests inkl. Pure-Function- und beide Service-Test-Suiten gruen)
  </acceptance_criteria>
  <done>
    `increase_shares` ist atomar (1 Tx, 2 audited Writes — Action + Member). 4 Service-Unit-Tests gruen (alle 4 vollstaendig copy-paste-executable). Happy-Path Test 5 assertet konkret `member.current_shares == 5` per pub-Feld-Zugriff (kein Platzhalter-Kommentar). Audit-Trail dokumentiert sowohl Aufstockungs-Action als auch Member.current_shares-Diff im selben Process-String. Wave 4 (Plan 04 REST+E2E) kann beide Service-Methoden komplett konsumieren.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REST -> Service (upgrade) | Member-ID + shares + willensbekundung_date kommen aus HTTP-Layer als untrusted Input |
| Service -> DAO | atomare Transaction mit 2 audited Writes (Action + Member); Tx-rollback bei Fehler in einem Write |
| Service -> Audit-Log | Process-String `member-adjust.upgrade` ist konstant fuer beide Writes (Action + Member.current_shares); Linkbarkeit via process-Filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-01 | Spoofing | check_permission(ADMIN_PRIVILEGE) | mitigate | Test `test_increase_shares_permission_denied` verifiziert Propagation (HTTP 401 via global Mapping). Erste DAO-touchende Operation. |
| T-15-03-02 | Tampering | shares-Argument < 0 (Anteils-Reduktion-Bypass) | mitigate | Pre-Validation `if shares <= 0 -> ValidationError`. Test `test_increase_shares_invalid_shares_zero` belegt. Phase 15 erzeugt KEINE shares-reduzierende Action (das ist Phase 16 partial_repayment / Phase 17 transfer). |
| T-15-03-03 | Tampering | upgrade auf gekuendigtes Mitglied | mitigate | UPGD-04: `member.exit_date.is_some() -> ValidationError`. Test `test_increase_shares_cancelled_member_blocked` verifiziert dass `member_action_dao.create` NICHT aufgerufen wird (MockDao ohne expect_create). |
| T-15-03-04 | Tampering | direct Member.update bypass | mitigate | AUDT-01-Grep-Gate: `member_dao.update(` darf nicht ausserhalb von `audited_update!` aufgerufen werden. `audited_update!`-Macro Body hat hardkodiertes `$dao.update(...)`. |
| T-15-03-05 | Repudiation | Member.current_shares-Diff nicht im Audit | mitigate | `audited_update!` ruft `build_update_entries(&old, &new, ...)` — alte und neue current_shares werden als 2 Felder im Audit-Log abgelegt; Hash-Chain verlinkt. |
| T-15-03-06 | DoS | shares=i32::MAX overflow | accept | `member_entity.current_shares += shares` ist i32-Arithmetik; Overflow waere `i32::MAX + 1` -> panic im debug, wraps im release. Praktisch nie >1000 Anteile pro Mitglied -> akzeptables Risiko. Falls Bedarf: spaeter checked_add adden (Tech-Debt-Item). |
| T-15-03-07 | Race condition | parallel increase_shares auf gleichem Member | mitigate | Optimistic-Locking via `Member.version`-Bump in `audited_update!`. DAO `MemberDao::update` setzt WHERE version = old_version voraus (Standard-Pattern aus Phase 7/8). Plan-04-E2E darf einen Stale-Retry-Test ergaenzen, falls Zeit. |
| T-15-03-08 | Information Disclosure | Member.exit_date error message | accept | "cannot upgrade cancelled member" enthaelt keine PII. Generischer Text. |
</threat_model>

<verification>
- `cargo build -p genossi_service_impl` exits 0
- `cargo test -p genossi_service_impl --lib membership_adjust` exits 0 (alle Pure-Function-Tests aus Plan 01 + alle cancel_membership-Tests aus Plan 02 + alle 4 increase_shares-Tests)
- AUDT-01-Grep-Gate: 0 direkte DAO-create/update-Calls ausserhalb der Macros
- UPGD-04 + UPGD-02-Korrektheit per Code-Inspect verifiziert (effective_date=None, exit_date-Check)
- Stub-Kommentar "increase_shares — Plan 03" ist entfernt
- Test 5 assertion auf `member.current_shares == 5` direkt verifiziert (BLOCKER 4 fix)
</verification>

<success_criteria>
- 4 increase_shares-Tests gruen (alle 4 vollstaendig ausformuliert mit konkreten Assertions, keine Platzhalter-Kommentare)
- Member.current_shares-Mutation in derselben Tx wie MemberAction::Aufstockung-Create (atomar)
- Member.version-Bump ueber uuid_service.new_v4() (Optimistic-Locking)
- Wave 4 (Plan 04) kann beide REST-Endpoints (`/cancel` + `/increase-shares`) gegen die fertigen Service-Methoden testen
</success_criteria>

<output>
After completion, create `.planning/phases/15-service-rest-kuendigung-aufstockung/15-03-SUMMARY.md` per template at `.claude/get-shit-done/templates/summary.md`.
</output>
