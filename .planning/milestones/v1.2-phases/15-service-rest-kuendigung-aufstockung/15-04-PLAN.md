---
phase: 15-service-rest-kuendigung-aufstockung
plan: 04
type: execute
wave: 4
depends_on: [15-03]
files_modified:
  - genossi_rest_types/src/lib.rs
  - genossi_rest/src/membership_adjust.rs
  - genossi_rest/src/member.rs
  - genossi_rest/src/lib.rs
  - genossi_bin/src/lib.rs
  - genossi_bin/tests/membership_adjust_e2e.rs
autonomous: true
requirements: [CANC-01, CANC-03, CANC-04, CANC-05, UPGD-01, UPGD-02, UPGD-03, UPGD-04, PERM-01, PERM-02, AUDT-01]
tags: [rust, axum, rest, e2e-test, openapi, di-wiring]

must_haves:
  truths:
    - "REST-Endpoints `POST /api/members/{id}/cancel` und `POST /api/members/{id}/increase-shares` sind via Axum registriert"
    - "CancelMembershipRequestTO und IncreaseSharesRequestTO sind in `genossi_rest_types` mit ISO8601-Date-Required-Serde definiert (D-15-10)"
    - "MembershipAdjustResponseTO mit { action: MemberActionTO, member: MemberTO } ist definiert (D-15-11)"
    - "RestStateDef-Trait hat MembershipAdjustService-Associated-Type + Trait-Methode (D-15-16)"
    - "RestStateImpl in genossi_bin/src/lib.rs hat membership_adjust_service-Slot + Konstruktor-Wiring (D-15-16)"
    - "9+ E2E-Tests sind gruen: 5 fuer Kuendigung (Happy-Path-H1, Happy-Path-H2, Permission-Denied, Already-Cancelled, Audit-Chain-Verify) + 4 fuer Aufstockung (Happy-Path, Cancelled-Member-Block, Permission-Denied, Audit-Chain-Verify) + 2 Datum-Bounds-Edge-Cases"
    - "v1.1-Audit-Hashchain bleibt valid: `/api/audit/verify` returned `valid=true` nach allen Phase-15-Operationen"
    - "Bestehende v1.1-E2E-Tests bleiben gruen (kein Regression)"
    - "HTTP 401 fuer PermissionDenied (NICHT 403): Per genossi_rest/src/lib.rs:115 globalem Mapping (`ServiceError::PermissionDenied -> RestError::Unauthorized -> 401`). D-15-12-Enumeration ('403 (No Admin Privilege)') widerspricht der eigenen Why-Klausel ('konsistent mit existing RestError-Mapping') und der ROADMAP-Success-Criteria ('401'). Plaene folgen Why-Klausel + ROADMAP. CONTEXT.md wird post-phase korrigiert (403-Eintrag entfernen). E2E-Tests assertieren StatusCode::UNAUTHORIZED (401)."
  artifacts:
    - path: "genossi_rest_types/src/lib.rs"
      provides: "CancelMembershipRequestTO + IncreaseSharesRequestTO + MembershipAdjustResponseTO"
      contains: "pub struct CancelMembershipRequestTO"
    - path: "genossi_rest/src/membership_adjust.rs"
      provides: "cancel_membership + increase_shares Axum-Handler"
      contains: "pub async fn cancel_membership"
    - path: "genossi_rest/src/member.rs"
      provides: "Sub-Routes-Registration vor /{id}-catch-all (defensive Konvention)"
      contains: "/{id}/cancel"
    - path: "genossi_rest/src/lib.rs"
      provides: "RestStateDef::MembershipAdjustService Associated-Type + Trait-Methode"
      contains: "type MembershipAdjustService"
    - path: "genossi_bin/src/lib.rs"
      provides: "MembershipAdjustService DI-Wiring in RestStateImpl"
      contains: "membership_adjust_service: Arc<MembershipAdjustService>"
    - path: "genossi_bin/tests/membership_adjust_e2e.rs"
      provides: "9+ E2E-Tests fuer Phase 15"
      contains: "test_cancel_membership_happy_path_h1"
  key_links:
    - from: "genossi_rest/src/member.rs::generate_route"
      to: "membership_adjust::cancel_membership + increase_shares"
      via: ".route('/{id}/cancel', post(cancel_membership)).route('/{id}/increase-shares', post(increase_shares))"
      pattern: "/\\{id\\}/cancel"
    - from: "genossi_rest/src/membership_adjust.rs::cancel_membership"
      to: "rest_state.membership_adjust_service()"
      via: "Service-Aufruf in error_handler-Wrapper"
      pattern: "membership_adjust_service\\(\\)"
    - from: "genossi_bin/src/lib.rs::RestStateImpl::new"
      to: "MembershipAdjustServiceImpl::new mit 6 Deps"
      via: "Arc::new + .clone() von vorhandenen Daos"
      pattern: "MembershipAdjustServiceImpl"
---

<objective>
REST-Endpoints fuer beide Phase-15-Operationen + DI-Wiring + 9 E2E-Tests + 2 Datum-Bounds-Edge-Case-Tests.

Purpose:
- Macht die Service-Methoden aus Plan 02 + 03 ueber HTTP erreichbar (Frontend-Phase-18-Vorlage).
- Verifiziert die volle Stack-Konsistenz inkl. Audit-Hash-Chain via `/api/audit/verify`.
- Stellt sicher dass `recalc_dates`-Hook das `Member.exit_date` korrekt setzt (CANC-04-end-to-end-Beweis).

Output:
- Erweitertes `genossi_rest_types/src/lib.rs` (3 neue TOs)
- Neue Datei `genossi_rest/src/membership_adjust.rs` (2 Axum-Handler)
- Erweitertes `genossi_rest/src/member.rs` (Sub-Route-Registration)
- Erweitertes `genossi_rest/src/lib.rs` (RestStateDef-Trait-Erweiterung + OpenAPI-Tag)
- Erweitertes `genossi_bin/src/lib.rs` (DI-Wiring)
- Neue Datei `genossi_bin/tests/membership_adjust_e2e.rs` (9+ E2E-Tests + 2 Datum-Bounds-Edge-Cases)
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md

VCS: this project uses jj (Jujutsu). The executor should NOT run git commands directly; rely on GSD's commit step which already wraps jj-aware commits.
</execution_context>

<context>
@.planning/phases/15-service-rest-kuendigung-aufstockung/15-CONTEXT.md
@.planning/phases/15-service-rest-kuendigung-aufstockung/15-PATTERNS.md
@.planning/phases/15-service-rest-kuendigung-aufstockung/15-03-SUMMARY.md
@CLAUDE.md

<interfaces>
From `genossi_rest_types/src/lib.rs:186-191` (Date-Required-Serde-Pattern):
```rust
#[serde(
    serialize_with = "iso8601_date_required::serialize",
    deserialize_with = "iso8601_date_required::deserialize"
)]
#[schema(example = "2024-01-15")]
pub join_date: time::Date,
```

From `genossi_rest/src/member.rs:29-55` (generate_route — Pattern fuer Sub-Routes):
```rust
pub fn generate_route<RestState: RestStateDef>() -> Router<RestState> {
    Router::new()
        .route("/", get(get_all_members::<RestState>))
        .route("/transfer-recipients", get(get_transfer_recipients::<RestState>))
        .route("/import", post(import_members::<RestState>))
        .route("/not-reached-by/{job_id}", get(get_members_not_reached_by::<RestState>))
        // PATH-PARAMETER ROUTES LAST (D-14-08-Konvention):
        .route("/{id}", get(get_member::<RestState>))
        .route("/", post(create_member::<RestState>))
        .route("/{id}", put(update_member::<RestState>))
        .route("/{id}", delete(delete_member::<RestState>))
}
```

WICHTIG (`/{id}/actions`-Mount-Strategie): `/{id}/actions` ist NICHT in `member::generate_route` mounted, sondern als SEPARATER nested router in `genossi_rest/src/lib.rs:584` via `.nest("/api/members/{member_id}/actions", member_action::generate_route())`. Phase-15-Routes `/{id}/cancel` und `/{id}/increase-shares` werden hingegen INSIDE `member::generate_route` deklariert, weil sie keine Sub-Resource-Sammlung sind — sie sind Action-Endpoints auf einem konkreten Member. Kein Mount-Konflikt zwischen den Phase-15-Routes und dem `/{member_id}/actions`-Nest.

From `genossi_rest/src/member_action.rs:120-147` (POST-Handler-Vorbild):
```rust
#[instrument(skip(rest_state))]
#[utoipa::path(post, tag = "Member Actions", ...)]
pub async fn create_member_action<RestState: RestStateDef>(
    rest_state: State<RestState>,
    Extension(context): Extension<Context>,
    Path(member_id): Path<Uuid>,
    Json(mut action): Json<MemberActionTO>,
) -> Response {
    error_handler(
        (async {
            let action = MemberActionTO::from(&rest_state.member_action_service()
                .create(&(&action).into(), extract_auth_context(Some(context))?, None).await?);
            Ok(Response::builder().status(200)
                .header("Content-Type", "application/json")
                .body(Body::new(serde_json::to_string(&action)?))
                .unwrap())
        }).await,
    )
}
```

From `genossi_bin/tests/transfer_recipients_e2e.rs` (E2E-Setup-Pattern):
```rust
#![cfg(feature = "mock_auth")]
use genossi_bin::RestStateImpl;
use genossi_rest::test_server::test_support::{start_test_server, TestServer};
// ... setup() via in-memory sqlite + migrations + RestStateImpl::new ...
```

From `genossi_rest/src/lib.rs:104-120` (global ServiceError -> RestError mapping, BLOCKER 5 Resolution):
```rust
impl From<genossi_service::ServiceError> for RestError {
    fn from(e: genossi_service::ServiceError) -> Self {
        match e {
            ServiceError::EntityNotFound(_) => RestError::NotFound,           // 404
            ServiceError::ValidationError(items) => RestError::BadRequest(_), // 400
            ServiceError::PermissionDenied => RestError::Unauthorized,        // 401 (NOT 403)
            ServiceError::Conflict(msg) => RestError::Conflict(_),            // 409
            _ => RestError::InternalError(_),                                 // 500
        }
    }
}
```

D-15-12-Konflikt-Resolution: CONTEXT.md D-15-12 nennt "403 (No Admin Privilege)", aber die Why-Klausel "konsistent mit existing RestError-Mapping" + ROADMAP Success-Criteria sagen 401. Plaene folgen 401 (Why-Klausel + ROADMAP); die "403"-Erwaehnung ist ein Transkriptionsfehler in CONTEXT.md.

From Plan 02 + 03 (Wave 2 + 3 outputs):
- `MembershipAdjustServiceImpl<Deps>` mit `cancel_membership` + `increase_shares` fully implementiert
- `MembershipAdjustServiceDeps`-Trait
- Service-Tests gruen
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Request/Response-DTOs + REST-Handler + Route-Registration + DI-Wiring</name>
  <files>genossi_rest_types/src/lib.rs, genossi_rest/src/membership_adjust.rs, genossi_rest/src/member.rs, genossi_rest/src/lib.rs, genossi_bin/src/lib.rs</files>
  <read_first>
    - genossi_rest_types/src/lib.rs (Z. 1-50 Header + ISO8601-Serde + Z. 186-191 join_date-Pattern + Z. 348-461 MemberSlimTO/MemberActionTO-Vorbilder)
    - genossi_rest/src/member.rs (Z. 29-55 generate_route; Z. 117-143 get_transfer_recipients als Vorbild fuer Query-Handler-Stil) — **WARNING 2 fix**: pruefen wo `/{id}/actions` mounted ist (Erwartung: NICHT hier, sondern in lib.rs:584 via `.nest("/api/members/{member_id}/actions", ...)`). Phase-15-Sub-Routes leben dagegen IN `member::generate_route`.
    - genossi_rest/src/lib.rs (Z. 580-590 — `.nest(...)`-Stack; bestaetigt dass `/{member_id}/actions` als separater Mount existiert)
    - genossi_rest/src/member_action.rs (Z. 120-147 create_member_action als Vorbild fuer POST-Handler mit Path+Json)
    - genossi_rest/src/lib.rs (RestStateDef-Trait — neuer Associated-Type + Trait-Methode hinzufuegen; OpenAPI-nest unveraendert)
    - genossi_bin/src/lib.rs (suche per grep: `member_action_service` — alle Vorkommen sind das Vorbild)
    - .planning/phases/15-service-rest-kuendigung-aufstockung/15-PATTERNS.md Section Pattern 6 + 7 + 8
    - .planning/phases/15-service-rest-kuendigung-aufstockung/15-CONTEXT.md D-15-09..12, D-15-16
  </read_first>
  <behavior>
    - Test 1: `cargo build` im Workspace kompiliert clean (alle 6 modifizierten Files konsistent)
    - Test 2: REST-Route `POST /api/members/{id}/cancel` ist erreichbar (per OpenAPI-Doc-Verify)
    - Test 3: REST-Route `POST /api/members/{id}/increase-shares` ist erreichbar
    - Test 4: Sub-Routes leben in `member::generate_route` UND koexistieren ohne Compile-Fehler mit dem `/{member_id}/actions`-Nest aus lib.rs (verschiedene Pfad-Praefixe)
  </behavior>
  <action>
    **1. DTOs in `genossi_rest_types/src/lib.rs` ergaenzen (Position: nach den existing Request-DTOs; D-15-10, D-15-11):**

```rust
#[derive(Clone, Debug, Serialize, Deserialize, ToSchema)]
pub struct CancelMembershipRequestTO {
    #[serde(
        serialize_with = "iso8601_date_required::serialize",
        deserialize_with = "iso8601_date_required::deserialize"
    )]
    #[schema(example = "2026-06-15")]
    pub willensbekundung_date: time::Date,
}

#[derive(Clone, Debug, Serialize, Deserialize, ToSchema)]
pub struct IncreaseSharesRequestTO {
    #[serde(
        serialize_with = "iso8601_date_required::serialize",
        deserialize_with = "iso8601_date_required::deserialize"
    )]
    #[schema(example = "2026-06-15")]
    pub willensbekundung_date: time::Date,
    #[schema(example = 2)]
    pub shares: i32,
}

#[derive(Clone, Debug, Serialize, Deserialize, ToSchema)]
pub struct MembershipAdjustResponseTO {
    pub action: MemberActionTO,
    pub member: MemberTO,
}
```

    **2. Neue Datei `genossi_rest/src/membership_adjust.rs` anlegen mit beiden Handlern (D-15-09):**

```rust
use std::sync::Arc;
use axum::{
    body::Body,
    extract::{Path, State},
    response::Response,
    Extension, Json,
};
use tracing::instrument;
use uuid::Uuid;

use genossi_rest_types::{
    CancelMembershipRequestTO, IncreaseSharesRequestTO, MemberActionTO, MemberTO,
    MembershipAdjustResponseTO,
};

use crate::error::error_handler;
use crate::{extract_auth_context, Context, RestStateDef};

#[instrument(skip(rest_state))]
#[utoipa::path(
    post,
    tag = "Members",
    path = "/{id}/cancel",
    params(("id" = Uuid, Path, description = "Member ID")),
    request_body = CancelMembershipRequestTO,
    responses(
        (status = 200, description = "Cancellation successful", body = MembershipAdjustResponseTO),
        (status = 400, description = "Validation error (date bounds, etc.)"),
        (status = 401, description = "Unauthorized — kein Login oder keine admin-Rolle (per global ServiceError::PermissionDenied -> RestError::Unauthorized Mapping in genossi_rest/src/lib.rs:115)"),
        (status = 404, description = "Member not found"),
        (status = 409, description = "Member already cancelled"),
    ),
)]
pub async fn cancel_membership<RestState: RestStateDef>(
    rest_state: State<RestState>,
    Extension(context): Extension<Context>,
    Path(member_id): Path<Uuid>,
    Json(req): Json<CancelMembershipRequestTO>,
) -> Response {
    error_handler(
        (async {
            let (action, member) = rest_state
                .membership_adjust_service()
                .cancel_membership(
                    member_id,
                    req.willensbekundung_date,
                    extract_auth_context(Some(context))?,
                    None,
                )
                .await?;
            let response = MembershipAdjustResponseTO {
                action: MemberActionTO::from(&action),
                member: MemberTO::from(&member),
            };
            Ok(Response::builder()
                .status(200)
                .header("Content-Type", "application/json")
                .body(Body::new(serde_json::to_string(&response)?))
                .unwrap())
        })
        .await,
    )
}

#[instrument(skip(rest_state))]
#[utoipa::path(
    post,
    tag = "Members",
    path = "/{id}/increase-shares",
    params(("id" = Uuid, Path, description = "Member ID")),
    request_body = IncreaseSharesRequestTO,
    responses(
        (status = 200, description = "Increase successful", body = MembershipAdjustResponseTO),
        (status = 400, description = "Validation error (date, shares<=0, cancelled member)"),
        (status = 401, description = "Unauthorized (per global mapping)"),
        (status = 404, description = "Member not found"),
    ),
)]
pub async fn increase_shares<RestState: RestStateDef>(
    rest_state: State<RestState>,
    Extension(context): Extension<Context>,
    Path(member_id): Path<Uuid>,
    Json(req): Json<IncreaseSharesRequestTO>,
) -> Response {
    error_handler(
        (async {
            let (action, member) = rest_state
                .membership_adjust_service()
                .increase_shares(
                    member_id,
                    req.shares,
                    req.willensbekundung_date,
                    extract_auth_context(Some(context))?,
                    None,
                )
                .await?;
            let response = MembershipAdjustResponseTO {
                action: MemberActionTO::from(&action),
                member: MemberTO::from(&member),
            };
            Ok(Response::builder()
                .status(200)
                .header("Content-Type", "application/json")
                .body(Body::new(serde_json::to_string(&response)?))
                .unwrap())
        })
        .await,
    )
}
```

    Modul-Registration in `genossi_rest/src/lib.rs` ergaenzen: `pub mod membership_adjust;` (Position: alphabetisch).

    **3. Sub-Routes in `genossi_rest/src/member.rs::generate_route` registrieren — UNMITTELBAR VOR der `.route("/{id}", ...)`-Sequenz (WARNING 1 fix — eindeutige Begruendung):**

```rust
        // Phase 15 v1.2 Sub-Routes mit Path-Parameter {id}/cancel + {id}/increase-shares.
        //
        // Axum-Routing-Note: Axum 0.8 matched literale Segmente vor Path-Param-Segmenten
        // INNERHALB desselben nested Routers. `/{id}/cancel` und `/{id}/increase-shares`
        // koennen daher in beliebiger Reihenfolge relativ zu `/{id}` deklariert werden.
        // Plan deklariert sie VOR `/{id}` rein als defensive Konvention (D-14-08-Lesson),
        // um Regressionen vorzubeugen, falls Router-Tree-Annahmen sich aendern.
        .route(
            "/{id}/cancel",
            post(crate::membership_adjust::cancel_membership::<RestState>),
        )
        .route(
            "/{id}/increase-shares",
            post(crate::membership_adjust::increase_shares::<RestState>),
        )
        // PATH-PARAMETER ROUTES LAST (unveraendert):
        .route("/{id}", get(get_member::<RestState>))
        // ...
```

    Hinweis zum `/{member_id}/actions`-Nest: Dieser ist als SEPARATER Top-Level-Mount in `genossi_rest/src/lib.rs:584` deklariert (`.nest("/api/members/{member_id}/actions", member_action::generate_route())`). Phase-15-Routes leben innerhalb von `member::generate_route` und kollidieren NICHT mit dem `/actions`-Nest, weil Axum die Mounts an unterschiedlichen Path-Praefixen aufloest.

    **4. RestStateDef-Trait in `genossi_rest/src/lib.rs` erweitern (D-15-16):**

Im RestStateDef-Trait-Block (existing — sucht via grep "trait RestStateDef") folgendes hinzufuegen:

```rust
type MembershipAdjustService: genossi_service::membership_adjust::MembershipAdjustService<
    Context = ContextType,
    Transaction = <Self::TransactionDao as genossi_dao::TransactionDao>::Transaction,
> + Send
    + Sync
    + 'static;

fn membership_adjust_service(&self) -> Arc<Self::MembershipAdjustService>;
```

Die genauen Bound-Constraints (Context-Type-Aliase, Transaction-Verknuepfung) muessen von einem existing RestStateDef-Slot wie `member_action_service` 1:1 gespiegelt werden. Bei der Erstellung des neuen Slots am bestehenden `member_action_service`-Trait-Slot orientieren (z.B. `grep -A5 'type MemberActionService' genossi_rest/src/lib.rs`).

OpenApi-Annotation in `lib.rs` (utoipa-Schemas-Liste): `CancelMembershipRequestTO`, `IncreaseSharesRequestTO`, `MembershipAdjustResponseTO` aufnehmen. Paths-Block: `crate::membership_adjust::cancel_membership` und `crate::membership_adjust::increase_shares` aufnehmen.

    **5. DI-Wiring in `genossi_bin/src/lib.rs` ergaenzen (D-15-16):**

Schritt A — Deps-Struct + Type-Alias nach `MemberActionServiceDependencies` (existing Position aus PATTERNS.md Section Pattern 8):

```rust
pub struct MembershipAdjustServiceDependencies;
unsafe impl Send for MembershipAdjustServiceDependencies {}
unsafe impl Sync for MembershipAdjustServiceDependencies {}

impl genossi_service_impl::membership_adjust::MembershipAdjustServiceDeps
    for MembershipAdjustServiceDependencies
{
    type Context = Context;
    type Transaction = Transaction;
    type MemberActionDao = MemberActionDao;
    type MemberDao = MemberDao;
    type AuditLogDao = AuditLogDao;
    type PermissionService = PermissionService;
    type UuidService = UuidService;
    type TransactionDao = TransactionDao;
}

type MembershipAdjustService =
    genossi_service_impl::membership_adjust::MembershipAdjustServiceImpl<MembershipAdjustServiceDependencies>;
```

Schritt B — RestStateImpl-Slot ergaenzen (neben existing `member_action_service: Arc<MemberActionService>`):

```rust
membership_adjust_service: Arc<MembershipAdjustService>,
```

Schritt C — Konstruktion in `RestStateImpl::new()` (neben existing `let member_action_service = Arc::new(...)`):

```rust
let membership_adjust_service = Arc::new(
    genossi_service_impl::membership_adjust::MembershipAdjustServiceImpl {
        member_action_dao: member_action_dao.clone(),
        member_dao: member_dao.clone(),
        audit_log_dao: audit_log_dao.clone(),
        permission_service: permission_service.clone(),
        uuid_service: uuid_service.clone(),
        transaction_dao: transaction_dao.clone(),
    },
);
```

Schritt D — Self-Init im `Self { ... }`-Block ergaenzen:

```rust
membership_adjust_service,
```

Schritt E — RestStateDef-Trait-Impl ergaenzen:

```rust
type MembershipAdjustService = MembershipAdjustService;

fn membership_adjust_service(&self) -> Arc<Self::MembershipAdjustService> {
    self.membership_adjust_service.clone()
}
```
  </action>
  <verify>
    <automated>cargo build 2>&amp;1 | tail -10 &amp;&amp; grep -nE 'pub struct (CancelMembershipRequestTO|IncreaseSharesRequestTO|MembershipAdjustResponseTO)' /home/neosam/programming/rust/projects/genossi3/genossi_rest_types/src/lib.rs &amp;&amp; grep -nE '/\{id\}/(cancel|increase-shares)' /home/neosam/programming/rust/projects/genossi3/genossi_rest/src/member.rs &amp;&amp; grep -nE 'membership_adjust_service: Arc<MembershipAdjustService>' /home/neosam/programming/rust/projects/genossi3/genossi_bin/src/lib.rs &amp;&amp; grep -nE '"/\{id\}/(actions|cancel|increase-shares)"' /home/neosam/programming/rust/projects/genossi3/genossi_rest/src/member.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` (gesamter Workspace) exit code 0
    - `grep -c 'pub struct CancelMembershipRequestTO' genossi_rest_types/src/lib.rs` returns 1
    - `grep -c 'pub struct IncreaseSharesRequestTO' genossi_rest_types/src/lib.rs` returns 1
    - `grep -c 'pub struct MembershipAdjustResponseTO' genossi_rest_types/src/lib.rs` returns 1
    - `grep -c 'pub async fn cancel_membership' genossi_rest/src/membership_adjust.rs` returns 1
    - `grep -c 'pub async fn increase_shares' genossi_rest/src/membership_adjust.rs` returns 1
    - `grep -c '/{id}/cancel' genossi_rest/src/member.rs` returns 1 (Route registriert)
    - `grep -c '/{id}/increase-shares' genossi_rest/src/member.rs` returns 1
    - Reihenfolge-Check (defensive Konvention, WARNING 1): `awk '/\/\{id\}\/cancel/{cancel=NR}/\.route\(\"\/\{id\}\", get\(get_member/{getid=NR}END{exit !(cancel<getid)}' genossi_rest/src/member.rs` exits 0 (cancel-Route-Zeile < get_member-Zeile)
    - WARNING 2 Mount-Coexistence: `grep -nE '"/\{id\}/(actions|cancel|increase-shares)"' genossi_rest/src/member.rs` MUSS mind. 2 Lines liefern (`/cancel` und `/increase-shares` im member.rs; `/actions` lebt SEPARAT in lib.rs:584 und ist im member.rs nicht sichtbar). Korrektur: `grep -nE '"/\{id\}/(cancel|increase-shares)"' genossi_rest/src/member.rs` MUSS ≥ 2 Lines liefern; das gesamte Codeziel kompiliert ohne Konflikt mit `/{member_id}/actions`-Nest.
    - `grep -c 'pub mod membership_adjust;' genossi_rest/src/lib.rs` returns 1
    - `grep -c 'membership_adjust_service: Arc<MembershipAdjustService>' genossi_bin/src/lib.rs` returns 1
    - `grep -c 'MembershipAdjustServiceDependencies' genossi_bin/src/lib.rs` >= 2
    - OpenAPI hat `cancel_membership` und `increase_shares`: `grep -cE 'membership_adjust::(cancel_membership|increase_shares)' genossi_rest/src/lib.rs` >= 2
  </acceptance_criteria>
  <done>
    REST-Endpoints + DI-Wiring komplett. `cargo build` clean. Task 2 kann E2E-Tests gegen den vollen Stack ausfuehren.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 9+ E2E-Tests in membership_adjust_e2e.rs + Audit-Chain-Verify</name>
  <files>genossi_bin/tests/membership_adjust_e2e.rs</files>
  <read_first>
    - genossi_bin/tests/transfer_recipients_e2e.rs (komplett — Setup-Pattern, helper-Funktionen, MockAuth-Flow)
    - genossi_rest_types/src/lib.rs (MemberTO + MemberActionTO + neue Phase-15-TOs)
    - genossi_rest/src/test_server.rs (start_test_server + TestServer-API)
    - genossi_rest/src/lib.rs (Z. 104-120 — globales ServiceError->RestError-Mapping; bestaetigt 401 fuer PermissionDenied)
    - .planning/phases/15-service-rest-kuendigung-aufstockung/15-PATTERNS.md Section Pattern 9
    - .planning/phases/15-service-rest-kuendigung-aufstockung/15-CONTEXT.md §specifics (Test-Naming)
  </read_first>
  <behavior>
    11 E2E-Tests (gemaess Roadmap-Success-Criteria: 5 Kuendigung + 4 Aufstockung + 2 Datum-Bounds-Edge-Cases):

    **Kuendigung:**
    1. `test_cancel_membership_happy_path_h1`: willensbekundung=this-year-March-15 -> 200, action.effective_date=this-year-12-31, member.exit_date=this-year-12-31, action.shares_change=0, action.action_type="Austritt"
    2. `test_cancel_membership_happy_path_h2`: willensbekundung=this-year-August-15 -> 200, action.effective_date=next-year-12-31, member.exit_date=next-year-12-31
    3. `test_cancel_membership_permission_denied`: ohne admin-Rolle (mock_auth ohne admin) -> 401 (BLOCKER 5: per global Mapping in genossi_rest/src/lib.rs:115)
    4. `test_cancel_membership_already_cancelled`: 1. cancel -> 200, 2. cancel -> 409
    5. `test_cancel_membership_audit_chain_verify`: nach cancel -> `GET /api/audit/verify` returns `valid=true`, `GET /api/audit/member_action/{action_id}` listet Audit-Entries mit `process="member-adjust.cancel"`

    **Aufstockung:**
    6. `test_increase_shares_happy_path`: aktiver Member current_shares=1 + shares=3 -> 200, action.action_type="Aufstockung", action.shares_change=3, member.current_shares=4
    7. `test_increase_shares_cancelled_member_blocked`: 1. cancel -> 200, 2. increase_shares -> 400 mit ValidationError-Body
    8. `test_increase_shares_permission_denied`: ohne admin -> 401
    9. `test_increase_shares_audit_chain_verify`: nach increase_shares -> audit-verify valid; Audit-Entries fuer beide Mutations (Action + Member.current_shares) mit `process="member-adjust.upgrade"`

    **Datum-Bounds-Edge-Cases:**
    10. `test_cancel_membership_date_in_previous_year_rejected`: willensbekundung=YYYY-12-31 (Vorjahr) -> 400 mit ValidationError-Body
    11. `test_cancel_membership_date_in_overnext_year_rejected`: willensbekundung=YYYY+2-01-01 -> 400

    Hinweis fuer Edge-Case-Tests 10+11: today() im Service ist aktuelles UTC-Datum. Tests benutzen Datums-Berechnung relativ zu `time::OffsetDateTime::now_utc().date()`. Beispiel: `let two_years_ago = time::Date::from_calendar_date(today.year() - 2, time::Month::January, 1).unwrap();` -> muss 400 ergeben.

    **WARNING 3 — Runtime-Note:** Jeder Test laeuft Migrations auf einer frischen In-Memory-SQLite via `setup()`. Mit ~10 Migrations x 11 Tests ist die erwartete Runtime ~30-60s total. Akzeptabel fuer CI; kein Fixture-Sharing-Refactor noetig.
  </behavior>
  <action>
    1. Neue Datei `genossi_bin/tests/membership_adjust_e2e.rs` anlegen mit dem Setup-Skelett aus `transfer_recipients_e2e.rs`:

```rust
#![cfg(feature = "mock_auth")]

use genossi_bin::RestStateImpl;
use genossi_rest::test_server::test_support::{start_test_server, TestServer};
use genossi_rest_types::{
    CancelMembershipRequestTO, IncreaseSharesRequestTO, MemberTO,
};
use reqwest::StatusCode;
use serde_json::Value;
use sqlx::SqlitePool;
use std::sync::Arc;

async fn setup() -> TestServer {
    let pool = Arc::new(
        SqlitePool::connect("sqlite::memory:")
            .await
            .expect("Failed to create in-memory database"),
    );

    sqlx::migrate!("../migrations/sqlite")
        .run(&*pool)
        .await
        .expect("Failed to run migrations");

    let rest_state = RestStateImpl::new(pool);
    start_test_server(rest_state).await
}

fn sample_member(member_number: i64, first_name: &str) -> Value {
    serde_json::json!({
        "member_number": member_number,
        "first_name": first_name,
        "last_name": "Test",
        "join_date": "2020-01-01",
        "shares_at_joining": 1,
        "current_shares": 1,
        "current_balance": 0,
        "action_count": 0,
        "migrated": false,
        "status": "Normal"
    })
}

async fn create_active_member(
    client: &reqwest::Client,
    server: &TestServer,
    member_number: i64,
    first_name: &str,
) -> MemberTO {
    let m = sample_member(member_number, first_name);
    let response = client
        .post(server.url("/api/members"))
        .json(&m)
        .send()
        .await
        .expect("create_member POST failed");
    assert!(response.status().is_success(),
        "create_member expected 2xx, got {}: {}",
        response.status(), response.text().await.unwrap_or_default());
    response.json().await.expect("decode MemberTO")
}

// Helpers fuer Phase-15-Bodies
fn cancel_body(willensbekundung: &str) -> Value {
    serde_json::json!({ "willensbekundung_date": willensbekundung })
}

fn increase_body(willensbekundung: &str, shares: i32) -> Value {
    serde_json::json!({
        "willensbekundung_date": willensbekundung,
        "shares": shares
    })
}
```

    2. Die 11 Test-Funktionen ergaenzen. Verbatim-Test-1 als Vorlage:

```rust
#[tokio::test]
async fn test_cancel_membership_happy_path_h1() {
    let server = setup().await;
    let client = reqwest::Client::new();
    let m = create_active_member(&client, &server, 1001, "Kuendigung").await;
    let member_id = m.id.expect("created member must have id");

    let today = time::OffsetDateTime::now_utc().date();
    let h1_date = time::Date::from_calendar_date(today.year(), time::Month::March, 15).unwrap();
    let h1_str = h1_date.to_string();

    let resp = client
        .post(server.url(&format!("/api/members/{}/cancel", member_id)))
        .json(&cancel_body(&h1_str))
        .send()
        .await
        .expect("POST cancel failed");

    assert_eq!(resp.status(), StatusCode::OK, "body: {}", resp.text().await.unwrap_or_default());
    let response: Value = resp.json().await.expect("decode response");
    let action = &response["action"];
    let member = &response["member"];

    assert_eq!(action["action_type"], "Austritt");
    assert_eq!(action["shares_change"], 0);
    // H1: effective_date = 31.12. desselben Jahres
    let expected_effective = format!("{}-12-31", today.year());
    assert_eq!(action["effective_date"], expected_effective);
    // Member.exit_date wurde von recalc_dates gesetzt (CANC-04)
    assert_eq!(member["exit_date"], expected_effective);
}

#[tokio::test]
async fn test_cancel_membership_happy_path_h2() {
    let server = setup().await;
    let client = reqwest::Client::new();
    let m = create_active_member(&client, &server, 1002, "H2").await;
    let member_id = m.id.expect("id");

    let today = time::OffsetDateTime::now_utc().date();
    let h2_date = time::Date::from_calendar_date(today.year(), time::Month::August, 15).unwrap();

    let resp = client
        .post(server.url(&format!("/api/members/{}/cancel", member_id)))
        .json(&cancel_body(&h2_date.to_string()))
        .send().await.expect("POST cancel failed");
    assert_eq!(resp.status(), StatusCode::OK);
    let response: Value = resp.json().await.expect("decode");
    let expected_effective = format!("{}-12-31", today.year() + 1);
    assert_eq!(response["action"]["effective_date"], expected_effective);
    assert_eq!(response["member"]["exit_date"], expected_effective);
}

#[tokio::test]
async fn test_cancel_membership_already_cancelled() {
    let server = setup().await;
    let client = reqwest::Client::new();
    let m = create_active_member(&client, &server, 1003, "Doppel").await;
    let member_id = m.id.expect("id");

    let today = time::OffsetDateTime::now_utc().date();
    let h1_str = time::Date::from_calendar_date(today.year(), time::Month::March, 15).unwrap().to_string();

    let resp1 = client.post(server.url(&format!("/api/members/{}/cancel", member_id)))
        .json(&cancel_body(&h1_str)).send().await.expect("first cancel");
    assert_eq!(resp1.status(), StatusCode::OK);

    let resp2 = client.post(server.url(&format!("/api/members/{}/cancel", member_id)))
        .json(&cancel_body(&h1_str)).send().await.expect("second cancel");
    assert_eq!(resp2.status(), StatusCode::CONFLICT);
}

#[tokio::test]
async fn test_cancel_membership_audit_chain_verify() {
    let server = setup().await;
    let client = reqwest::Client::new();
    let m = create_active_member(&client, &server, 1004, "AuditTest").await;
    let member_id = m.id.expect("id");

    let today = time::OffsetDateTime::now_utc().date();
    let h1_str = time::Date::from_calendar_date(today.year(), time::Month::March, 15).unwrap().to_string();

    let resp = client.post(server.url(&format!("/api/members/{}/cancel", member_id)))
        .json(&cancel_body(&h1_str)).send().await.expect("cancel failed");
    assert_eq!(resp.status(), StatusCode::OK);

    // Audit-Hash-Chain global valid
    let verify_resp = client.get(server.url("/api/audit/verify")).send().await.expect("verify");
    assert_eq!(verify_resp.status(), StatusCode::OK);
    let verify_body: Value = verify_resp.json().await.expect("verify json");
    assert_eq!(verify_body["valid"], true, "audit-chain corrupted after cancel: {:?}", verify_body);
}
```

    Restliche 7 Tests (4 Aufstockung + permission_denied + 2 Datum-Bounds) folgen demselben Schema.

    **Permission-Denied-Tests (BLOCKER 5):** Nutzung der `mock_auth`-Feature-Flag-Variante mit Auth-Header ohne admin-Rolle (oder via existing Pattern aus v1.1-E2E-Tests). Falls `mock_auth` per Default ADMIN_PRIVILEGE gewaehrt, dann beim Test ein Setup mit explicit non-admin-Context bauen (siehe v1.1-Beispiele aus `genossi_bin/tests/` per `grep -rn "PermissionDenied\|expect_check_permission.*Err" genossi_bin/tests/`). E2E-Tests assertieren **StatusCode::UNAUTHORIZED (401)**, per globalem ServiceError::PermissionDenied -> RestError::Unauthorized Mapping in genossi_rest/src/lib.rs:115.

    **Test 6** (`test_increase_shares_happy_path`) verbatim:

```rust
#[tokio::test]
async fn test_increase_shares_happy_path() {
    let server = setup().await;
    let client = reqwest::Client::new();
    let m = create_active_member(&client, &server, 1005, "Upgrade").await;
    let member_id = m.id.expect("id");

    let today = time::OffsetDateTime::now_utc().date();
    let date_str = time::Date::from_calendar_date(today.year(), time::Month::March, 15).unwrap().to_string();

    let resp = client.post(server.url(&format!("/api/members/{}/increase-shares", member_id)))
        .json(&increase_body(&date_str, 3))
        .send().await.expect("upgrade");
    assert_eq!(resp.status(), StatusCode::OK);
    let response: Value = resp.json().await.expect("decode");
    assert_eq!(response["action"]["action_type"], "Aufstockung");
    assert_eq!(response["action"]["shares_change"], 3);
    assert_eq!(response["action"]["effective_date"], Value::Null); // UPGD-02
    // current_shares = 1 (sample) + 3 = 4
    assert_eq!(response["member"]["current_shares"], 4);
}
```

    **Test 7** (cancelled_member_blocked):

```rust
#[tokio::test]
async fn test_increase_shares_cancelled_member_blocked() {
    let server = setup().await;
    let client = reqwest::Client::new();
    let m = create_active_member(&client, &server, 1006, "BlockedUp").await;
    let member_id = m.id.expect("id");

    let today = time::OffsetDateTime::now_utc().date();
    let h1_str = time::Date::from_calendar_date(today.year(), time::Month::March, 15).unwrap().to_string();

    // 1. cancel
    let r1 = client.post(server.url(&format!("/api/members/{}/cancel", member_id)))
        .json(&cancel_body(&h1_str)).send().await.expect("cancel");
    assert_eq!(r1.status(), StatusCode::OK);

    // 2. increase_shares -> 400
    let r2 = client.post(server.url(&format!("/api/members/{}/increase-shares", member_id)))
        .json(&increase_body(&h1_str, 3)).send().await.expect("upgrade");
    assert_eq!(r2.status(), StatusCode::BAD_REQUEST);
}
```

    **Tests 10+11** (Datum-Bounds):

```rust
#[tokio::test]
async fn test_cancel_membership_date_in_previous_year_rejected() {
    let server = setup().await;
    let client = reqwest::Client::new();
    let m = create_active_member(&client, &server, 1007, "VorjahrTest").await;
    let member_id = m.id.expect("id");

    let today = time::OffsetDateTime::now_utc().date();
    let two_years_ago = time::Date::from_calendar_date(today.year() - 2, time::Month::June, 15).unwrap();

    let resp = client.post(server.url(&format!("/api/members/{}/cancel", member_id)))
        .json(&cancel_body(&two_years_ago.to_string())).send().await.expect("cancel");
    assert_eq!(resp.status(), StatusCode::BAD_REQUEST);
}

#[tokio::test]
async fn test_cancel_membership_date_in_overnext_year_rejected() {
    let server = setup().await;
    let client = reqwest::Client::new();
    let m = create_active_member(&client, &server, 1008, "UebernaechstTest").await;
    let member_id = m.id.expect("id");

    let today = time::OffsetDateTime::now_utc().date();
    let overnext_year = time::Date::from_calendar_date(today.year() + 2, time::Month::January, 15).unwrap();

    let resp = client.post(server.url(&format!("/api/members/{}/cancel", member_id)))
        .json(&cancel_body(&overnext_year.to_string())).send().await.expect("cancel");
    assert_eq!(resp.status(), StatusCode::BAD_REQUEST);
}
```

    **Permission-Denied-Tests (BLOCKER 5 fix — explizit 401 assertieren):**

```rust
#[tokio::test]
async fn test_cancel_membership_permission_denied() {
    // Setup einen non-admin Auth-Context. Per existing v1.1-E2E-Patterns
    // (siehe genossi_bin/tests/ — grep nach PermissionDenied oder
    // expect_check_permission). Wenn mock_auth Default-Admin gibt, dann
    // explizit ein non-admin-Context-Header setzen.
    let server = setup().await;
    let client = reqwest::Client::new(); // TODO: Header fuer non-admin setzen, abhaengig vom v1.1-Setup
    // ... Member anlegen ...
    // POST /cancel -> expect 401 (NICHT 403; siehe BLOCKER 5 + must_haves)
    // Falls existing v1.1-Test diesen Toggle nicht trivial bietet:
    // `#[ignore]` + Doc-Kommentar `// TODO Phase 18: deferred mock_auth-Rolle-Toggle`
    // ABER zuerst pruefen ob existing Test einen non-admin-Setup demonstriert.
    todo!("Implement per v1.1-Pattern; assert StatusCode::UNAUTHORIZED (401)");
}

#[tokio::test]
async fn test_increase_shares_permission_denied() {
    // Identisch zu test_cancel_membership_permission_denied: assert 401.
    todo!("Implement per v1.1-Pattern; assert StatusCode::UNAUTHORIZED (401)");
}
```

    Hinweis: Die `todo!()`-Stubs sind keine echten Stubs — sie sind ein expliziter Marker fuer den Executor, das v1.1-non-admin-Setup-Pattern zu spiegeln. Falls die `mock_auth`-Feature keine non-admin-Variante bietet, darf der Test mit `#[ignore]` + Doc-Kommentar "TODO Phase 18: deferred mock_auth-Rolle-Toggle" markiert werden. Zuerst pruefen ob ein existing v1.1-E2E-Test einen non-admin-Setup demonstriert; per `grep -rn "non.admin\|expect_check_permission.*Err" genossi_bin/tests/`.

    **WARNING 3 Note**: Jeder Test laeuft Migrations auf einer frischen In-Memory-SQLite via `setup()`. Mit ~10 Migrations x 11 Tests ist die erwartete Runtime ~30-60s total. Akzeptabel fuer CI.
  </action>
  <verify>
    <automated>cargo test -p genossi_bin --test membership_adjust_e2e 2>&amp;1 | tail -30 &amp;&amp; cargo test --test e2e_tests 2>&amp;1 | tail -10 || cargo test -p genossi_bin 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p genossi_bin --test membership_adjust_e2e test_cancel_membership_happy_path_h1` exits 0
    - `cargo test -p genossi_bin --test membership_adjust_e2e test_cancel_membership_happy_path_h2` exits 0
    - `cargo test -p genossi_bin --test membership_adjust_e2e test_cancel_membership_already_cancelled` exits 0
    - `cargo test -p genossi_bin --test membership_adjust_e2e test_cancel_membership_audit_chain_verify` exits 0
    - `cargo test -p genossi_bin --test membership_adjust_e2e test_increase_shares_happy_path` exits 0
    - `cargo test -p genossi_bin --test membership_adjust_e2e test_increase_shares_cancelled_member_blocked` exits 0
    - `cargo test -p genossi_bin --test membership_adjust_e2e test_increase_shares_audit_chain_verify` exits 0
    - `cargo test -p genossi_bin --test membership_adjust_e2e test_cancel_membership_date_in_previous_year_rejected` exits 0
    - `cargo test -p genossi_bin --test membership_adjust_e2e test_cancel_membership_date_in_overnext_year_rejected` exits 0
    - 9+ Tests insgesamt — `cargo test -p genossi_bin --test membership_adjust_e2e -- --list 2>&1 | grep -c '^test '` >= 9
    - Bestehende v1.1-E2E bleibt gruen: `cargo test -p genossi_bin --test transfer_recipients_e2e` exits 0
    - `grep -c 'process="member-adjust.cancel"\|"member-adjust.cancel"' genossi_bin/tests/membership_adjust_e2e.rs` >= 1 (Audit-Process-String pruefend)
    - BLOCKER 5 Assertion-Sanity-Check: `grep -cE 'StatusCode::UNAUTHORIZED|StatusCode::(401)' genossi_bin/tests/membership_adjust_e2e.rs` >= 1 (Permission-Denied-Tests assertieren 401, NICHT 403). Falls Tests `#[ignore]` markiert sind, gilt der Check nur fuer aktive Tests.
    - Roadmap-SC-Cargo-Test-Gate: `cargo test --test e2e_tests` exits 0 (falls test-target existiert, sonst skip)
  </acceptance_criteria>
  <done>
    Alle 9+ E2E-Tests gruen. Audit-Hash-Chain bleibt valid. Permission-Denied-Tests assertieren 401 (per globalem Mapping in genossi_rest/src/lib.rs:115). Phase 15 ist abgeschlossen — bereit fuer Phase 16 (Teil-Rueckgabe + Auto-Anlegen-Phase).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/Client -> REST (POST /cancel, POST /increase-shares) | Untrusted JSON Bodies — ISO8601-Date-Serde validiert Date-Format, serde validiert Strukturen |
| REST -> Service | Authentication-Context aus mock_auth-Middleware oder OIDC-Middleware; Context wird verbatim weitergereicht |
| E2E -> audit-verify | Test verifiziert `/api/audit/verify` Hash-Chain end-to-end |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-01 | Spoofing | mock_auth in production | accept | `#![cfg(feature = "mock_auth")]` Test-Datei wird in production-Build NICHT kompiliert (feature-flag-Gate). Production nutzt OIDC. |
| T-15-04-02 | Tampering | Sub-Route /{id}/cancel vor /{id} | mitigate | Doc-Kommentar in member.rs: defensive Konvention (Axum matched literale vor path-param-Segmenten in derselben nested Router; Reihenfolge ist technisch egal, aber Konvention reduziert Regression-Risiko). |
| T-15-04-03 | Repudiation | audit-chain corruption | mitigate | `test_cancel_membership_audit_chain_verify` und `test_increase_shares_audit_chain_verify` verifizieren `/api/audit/verify -> valid=true` nach jeder Phase-15-Operation. |
| T-15-04-04 | Information Disclosure | Validation-Error-Body PII | accept | ServiceError -> RestError-Mapping macht generischen 400-Body; keine Member-IDs, keine Audit-Hashes im Error-Body. |
| T-15-04-05 | Denial of Service | overlong shares value | accept | `shares: i32` per serde — JSON-Body deserialisiert max. i32. Extreme Werte fallen entweder durch shares > 0 (i32-Min) oder bleiben innerhalb i32-Range. |
| T-15-04-06 | Elevation of Privilege | ADMIN_PRIVILEGE-Bypass via missing middleware | mitigate | RestStateDef nutzt `extract_auth_context` (existing). Falls Context fehlt -> 401. Test `test_cancel_membership_permission_denied` verifiziert (assertet 401 per globalem Mapping). |
| T-15-04-07 | Tampering | UUID-Injection in member_id-Pfadparameter | mitigate | Axum `Path<Uuid>` parsed strikt; ungueltiges UUID -> 400 von Axum direkt. Test optional ergaenzen. |
| T-15-04-08 | Information Disclosure | OpenAPI exposed Schema | accept | Schemas sind oeffentliche Vertrags-Information; keine internen Felder offengelegt (keine Hash, keine Tx-Internals). |
</threat_model>

<verification>
- `cargo build` (gesamter Workspace) exit code 0
- `cargo test -p genossi_bin --test membership_adjust_e2e` exits 0 (alle 9+ Tests)
- `cargo test -p genossi_bin --test transfer_recipients_e2e` exits 0 (kein Regression in Phase 14 E2E)
- `cargo test -p genossi_rest` exits 0 (kein Regression in REST-Layer)
- `cargo test -p genossi_service_impl` exits 0 (alle Service-Tests aus Plan 01-03 gruen)
- Roadmap-SC erfuellt: 5 Kuendigung + 4 Aufstockung + 2 Datum-Bounds-Tests vorhanden
- BLOCKER 5: 401-Assertion in Permission-Denied-Tests verifiziert
</verification>

<success_criteria>
- Phase 15 ist abgeschlossen
- v1.1-Audit-Hashchain bleibt valid (`/api/audit/verify`)
- v1.1-E2E-Tests bleiben gruen
- AUDT-01 Grep-Gate erfuellt (von Plan 03 vererbt; Plan 04 fuegt keine direkten DAO-Calls hinzu)
- 11 neue E2E-Tests gruen + 4 Service-Unit-Tests aus Plan 02 + 4 aus Plan 03 + 6 Pure-Function-Tests aus Plan 01
- Permission-Denied-Tests assertieren explizit 401 (NICHT 403; per globalem Mapping in genossi_rest/src/lib.rs:115)
- Phase 16 (Teil-Rueckgabe) kann auf der MembershipAdjustService-Trait-Foundation aufsetzen — `partial_repayment` als 3. Methode ergaenzen
</success_criteria>

<output>
After completion, create `.planning/phases/15-service-rest-kuendigung-aufstockung/15-04-SUMMARY.md` per template at `.claude/get-shit-done/templates/summary.md`.
</output>
