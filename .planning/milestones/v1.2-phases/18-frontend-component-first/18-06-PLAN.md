---
phase: 18-frontend-component-first
plan: 06
type: execute
wave: 2
depends_on: ["18-01", "18-02", "18-03", "18-04", "18-05"]
files_modified:
  - genossi-frontend/src/component/membership_adjust_modal.rs
  - genossi-frontend/src/component/mod.rs
autonomous: true
requirements:
  - UI-02
  - UI-04
  - CANC-06
requirements_addressed:
  - UI-02
  - UI-04
  - CANC-06
tags:
  - frontend
  - component
  - modal

must_haves:
  truths:
    - "`genossi-frontend/src/component/membership_adjust_modal.rs` enthaelt `pub fn MembershipAdjustModal` Component mit Props `(member: MemberTO, today: time::Date, on_close: EventHandler<()>, on_success: EventHandler<()>)`."
    - "Modal hat ein `ModalStep` enum mit 5 Varianten (`SubChoice, Cancel, PartialRepayment, Transfer, Upgrade`) und match-rsx fuer jede Sub-View (D-18-02, D-18-03)."
    - "Sub-Choice-View hat 4 flat Buttons in Tailwind `grid grid-cols-1 sm:grid-cols-2 gap-4`-Layout (D-18-01)."
    - "Jede der 4 Operation-Sub-Views nutzt `r#type: \"button\"` + onclick fuer Submit (C-18-CF-03 Dioxus-Reload-Bug-Mitigation)."
    - "Jede Sub-View hat eine Vorschau-Box (`bg-blue-50 border-blue-200`) mit Live-Update aus Form-State (D-18-05, D-18-06)."
    - "Jede Sub-View initialisiert ihr Datum-Signal mit `use_signal(|| Some(today))` — erfuellt SC-2 'default today()'."
    - "Transfer-Sub-View detected Voll-Uebertrag live (`is_voll_uebertrag(shares, from.current_shares)`) und zeigt orange Warnung in der Vorschau (D-18-07)."
    - "Pure-Helper sind exportiert und unit-tested: `compute_effective_date_mirror` (6 Tests), `is_voll_uebertrag` (3 Tests), `to_member_to` (1 Test), `format_date_german` (2 Tests)."
    - "Transfer-Sub-View laedt Recipients via `use_resource(api::get_transfer_recipients(..., from_id))` und reicht sie als `members_override` an MemberSearch (D-18-14, L-5 Mitigation aus Plan 03)."
    - "Modal-Submit ruft on_success.call(()) bei 200-Response; ErrorAlert (intern) bei API-Fehler (D-18-08); Submit-Button-`disabled` waehrend submitting."
    - "Modal-Body nutzt 4 dedizierte Sub-Funktionen (`render_cancel_sub_view`, `render_partial_sub_view`, `render_transfer_sub_view`, `render_upgrade_sub_view`) mit `i18n: I18n`-Parameter (I18n hat `impl Clone for I18n` in i18n/mod.rs:834 — verifiziert)."
  falsehoods:
    - "Sub-View-Submit nutzt `<form onsubmit>` oder `r#type: \"submit\"` — bricht Phase-18 Page-Reload-Hotfix-Pattern."
    - "Modal duplicated MemberSearch-RSX inline statt members_override-Adapter zu nutzen — verletzt Component-First-HART."
    - "Modal nutzt `i18n.t_format!` oder `i18n.t_with_args` — diese Macros existieren nicht (Landmine L-4)."
    - "Datum-Signal startet mit `None` — bricht SC-2 'default today()'."
  artifacts:
    - path: "genossi-frontend/src/component/membership_adjust_modal.rs"
      provides: "MembershipAdjustModal component + 4 Sub-Views + Pure-Helpers + Tests"
      contains: "pub fn MembershipAdjustModal"
      min_lines: 400
    - path: "genossi-frontend/src/component/mod.rs"
      provides: "Re-Export of MembershipAdjustModal + Pure-Helpers"
      contains: "pub use membership_adjust_modal"
  key_links:
    - from: "genossi-frontend/src/component/membership_adjust_modal.rs"
      to: "genossi-frontend/src/api.rs"
      via: "api::cancel_membership(...) / api::transfer_shares(...) etc"
      pattern: "api::(cancel_membership|increase_shares|partial_repayment|transfer_shares|get_transfer_recipients)"
    - from: "genossi-frontend/src/component/membership_adjust_modal.rs"
      to: "genossi-frontend/src/component/fiscal_year_date_input.rs"
      via: "FiscalYearDateInput { value, on_change, today }"
      pattern: "FiscalYearDateInput"
    - from: "genossi-frontend/src/component/membership_adjust_modal.rs"
      to: "genossi-frontend/src/component/member_search.rs"
      via: "MemberSearch { members_override: Some(adapted), on_select, ... }"
      pattern: "members_override:"
---

<objective>
Plan 06 ist das **Herzstueck** von Phase 18:
1. **`MembershipAdjustModal`-Component** in neuer Datei `genossi-frontend/src/component/membership_adjust_modal.rs` — Single-File mit `ModalStep`-Enum-State + 4 Sub-Views (Cancel/Partial/Transfer/Upgrade) + Pure-Helpers + Unit-Tests.
2. **Re-Export** in `component/mod.rs`.

Purpose: Closing-Anker fuer UI-02 (Modal als shared Component), UI-04 (Vorschau-Section), CANC-06 (Vorschau-Confirm via Live-Preview), SC-2 (Default-today() im Modal-Body).
Output: ~600-700 LOC Modal-File + 12+ Unit-Tests + Re-Exports.

Diese Plan ist bewusst gross (gegen Standard-Sizing), weil:
- Die 4 Sub-Views teilen Pure-Helpers (is_voll_uebertrag, compute_effective_date_mirror, to_member_to, format_date_german) — Splitting wuerde duplicate-extraction erzwingen.
- ModalStep-Enum-State (D-18-03) verlangt explizit Single-File-Architektur.
- Submit-Pattern ist 4x identisch — Re-Implementierung ueber 4 Plans wuerde Plan-Anzahl ohne Wartungs-Nutzen aufblaehen.
Context-Budget-Risiko: ~40-50% (akzeptabel; Plan 05 wurde separiert, dadurch fokussiert sich Plan 06 nur auf Modal+Helpers).

**Deterministische Architektur-Entscheidung (kein Fallback):**
Sub-Views als 4 dedizierte private `fn render_*_sub_view(i18n: I18n, ...)`-Funktionen. Faktischer Befund (verifiziert):
- `genossi-frontend/src/i18n/mod.rs:834-840` enthaelt `impl Clone for I18n { fn clone(&self) -> Self { Self { locale: self.locale } } }`.
- `use_i18n()` (i18n/mod.rs:847-849) returnt `I18N.read().clone()` — bereits ein geklonter `I18n`-Wert.
- Sub-Funktionen koennen `i18n: I18n` per Wert annehmen und intern `.t(Key::X)` aufrufen.

Daher: Sub-Funktionen + `i18n.clone()` Pattern wird verwendet. KEINE Inline-Match-Variante. KEINE Fallback-Pfade im Action-Block.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-frontend-component-first/18-CONTEXT.md
@.planning/phases/18-frontend-component-first/18-PATTERNS.md
@.planning/phases/18-frontend-component-first/18-UI-SPEC.md
@.planning/phases/18-frontend-component-first/18-01-SUMMARY.md
@.planning/phases/18-frontend-component-first/18-02-SUMMARY.md
@.planning/phases/18-frontend-component-first/18-03-SUMMARY.md
@.planning/phases/18-frontend-component-first/18-04-SUMMARY.md
@.planning/phases/18-frontend-component-first/18-05-SUMMARY.md

<interfaces>
<!-- From Plan 01 (rest-types DTOs): -->
```rust
use rest_types::{
    MemberTO, MemberSlimTO, MemberActionTO, SalutationTO, MemberStatusTO,
    PartialRepaymentResponseTO, TransferSharesResponseTO,
};
```

<!-- From Plan 04 (FiscalYearDateInput component): -->
```rust
use crate::component::{FiscalYearDateInput, is_valid_fiscal_year_date};
```

<!-- From Plan 03 (MemberSearch members_override prop): -->
```rust
#[component]
pub fn MemberSearch(
    on_select: EventHandler<Option<Uuid>>,
    selected_id: Option<Uuid>,
    exclude_id: Option<Uuid>,
    #[props(default)] members_override: Option<Vec<MemberTO>>,
) -> Element;
```

<!-- From Plan 05 (API client functions): -->
```rust
use crate::api::{self, AppError};
// api::cancel_membership / increase_shares / partial_repayment / transfer_shares / get_transfer_recipients
```

<!-- AppError Clone-Status (verifiziert): -->
```rust
// genossi-frontend/src/api.rs:14
#[derive(Debug, Clone, PartialEq)]
pub struct AppError { pub status: Option<u16>, pub message: String, pub detail: Option<String> }
// → AppError hat #[derive(Clone)] — kann in Signal<Option<AppError>> verwendet und geklont werden.
```

<!-- I18n Clone-Status (verifiziert): -->
```rust
// genossi-frontend/src/i18n/mod.rs:834-840
impl Clone for I18n { fn clone(&self) -> Self { Self { locale: self.locale } } }
pub fn use_i18n() -> I18n { I18N.read().clone() }
// → I18n implementiert Clone manuell — kann per Wert/.clone() in Sub-Funktionen weitergereicht werden.
```

<!-- Existing ErrorAlert component (component/error_alert.rs:5-7): -->
```rust
#[component]
pub fn ErrorAlert(error: AppError, on_dismiss: Option<EventHandler<()>>) -> Element;
```

<!-- Existing Modal wrapper (component/modal.rs:3-7): -->
```rust
pub struct ModalProps { pub children: Element }
#[component]
pub fn Modal(props: ModalProps) -> Element;
```

<!-- Existing CONFIG global (service/config): -->
```rust
use crate::service::config::CONFIG;
let config = CONFIG.read().clone();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MembershipAdjustModal component + 4 Sub-Views + Pure-Helpers + Tests</name>
  <read_first>
    - genossi-frontend/src/component/repayment_entry_paidout_confirm.rs (komplett — strukturelles Vorbild, Submit-Loop, Tests)
    - genossi-frontend/src/component/error_alert.rs (Signatur fuer Mount innerhalb Modal — verifiziert: ErrorAlert(error: AppError, on_dismiss))
    - genossi-frontend/src/component/member_search.rs (NEUE members_override-Signatur aus Plan 03)
    - genossi-frontend/src/component/fiscal_year_date_input.rs (aus Plan 04)
    - genossi-frontend/src/api.rs (NEUE Funktionen aus Plan 05) + verifizieren `#[derive(Debug, Clone, PartialEq)] pub struct AppError` (Zeile 14)
    - genossi-frontend/src/i18n/mod.rs (Zeile mit `MembershipAdjust*` Keys — aus Plan 04 + `impl Clone for I18n` Zeile 834-840)
    - .planning/phases/18-frontend-component-first/18-PATTERNS.md (Section 1 — komplett, alle Submit-Pattern + Pure-Helper-Vorlagen)
    - .planning/phases/18-frontend-component-first/18-UI-SPEC.md (Component Inventory + Interaction & State Contract)
    - .planning/phases/18-frontend-component-first/18-CONTEXT.md (D-18-01..14 — alle Entscheidungen)
    - genossi_service_impl/src/membership_adjust.rs Zeilen 700-760 (Backend compute_effective_date — wir spiegeln 1:1)
  </read_first>
  <files>genossi-frontend/src/component/membership_adjust_modal.rs</files>
  <behavior>
    Pure-Helper-Tests (im selben File):
    - **compute_effective_date_mirror** 6 Tests:
      - `(2026-06-30)` → `(2026, 2026-12-31)` (H1 boundary inclusive)
      - `(2026-07-01)` → `(2027, 2027-12-31)` (H2 boundary)
      - `(2026-12-31)` → `(2027, 2027-12-31)` (H2 year-end)
      - `(2026-01-01)` → `(2026, 2026-12-31)` (H1 year-start)
      - `(2024-02-29)` → `(2024, 2024-12-31)` (Schaltjahr H1)
      - `(2026-06-15)` → `(2026, 2026-12-31)` (mid-H1)
    - **is_voll_uebertrag** 3 Tests:
      - `is_voll_uebertrag(5, 5)` == `true` (eq)
      - `is_voll_uebertrag(3, 5)` == `false` (lt)
      - `is_voll_uebertrag(0, 5)` == `false` (edge zero — shares=0 invalid)
    - **to_member_to** 1 Test:
      - Input MemberSlimTO mit id, member_number, first_name, last_name, salutation=Some(Herr), title=None
      - Output MemberTO: `id=Some(input.id)`, alle PII-Felder (email, phone, street, postal_code, city, bank_account etc.) = None/default; current_shares=0, status=Normal
    - **format_date_german** 2 Tests:
      - `format_date_german(2026-06-15)` == "15.06.2026"
      - `format_date_german(2026-12-31)` == "31.12.2026"
  </behavior>
  <action>
Neue Datei `genossi-frontend/src/component/membership_adjust_modal.rs` mit folgendem Aufbau (~600-700 LOC):

```rust
//! Phase 18 — MembershipAdjustModal: Single-Component-Modal mit 4 flat Sub-Choice-Buttons
//! und 4 Operation-Sub-Views (Kuendigung, Teil-Rueckgabe, Uebertrag, Aufstockung).
//!
//! Architektur (D-18-02 + D-18-03):
//! - ModalStep-Enum + match-rsx im selben File
//! - Single-File ~600 LOC ist explizit gewollt; Sub-Component-Split haette Prop-Drilling-Overhead
//! - Component-First-Prinzip: Modal selbst ist die Shared-Component, interne Sub-Views sind privat
//! - Sub-Views als private `fn render_*_sub_view(i18n: I18n, ...)` (I18n: Clone verifiziert in i18n/mod.rs:834-840)
//!
//! SC-2 Default-today(): Jede Sub-View initialisiert ihr Datum-Signal als
//! `use_signal(|| Some(today))` — Datepicker zeigt direkt heute vor. Caller (Page) reicht
//! `today` als Prop in den Modal hinein.
//!
//! Closing-Anker fuer Roadmap-SC-1 (Modal als shared Component), SC-3 (Vorschau-Section), CANC-06 (Vorschau-Confirm).

use dioxus::prelude::*;
use rest_types::{
    MemberSlimTO, MemberStatusTO, MemberTO,
    PartialRepaymentResponseTO, TransferSharesResponseTO,
};
use uuid::Uuid;

use crate::api::{self, AppError};
use crate::component::{ErrorAlert, FiscalYearDateInput, MemberSearch, is_valid_fiscal_year_date};
use crate::i18n::{use_i18n, I18n, Key};
use crate::service::config::CONFIG;

// ─── Pure helpers (testable) ────────────────────────────────────────

/// Phase 18 D-18 Claude's-Discretion — Frontend mirror of Backend `compute_effective_date`
/// (genossi_service_impl/src/membership_adjust.rs:700-720).
///
/// Returns `(target_fiscal_year, effective_date)` where effective_date is the December 31
/// of either the current calendar year (H1: Jan-Jun) or the next year (H2: Jul-Dec).
///
/// Used in Cancel + PartialRepayment previews to show "Stichtag" and "Phase FY{YYYY}"
/// without backend round-trip. Backend stays single source of truth — the actual Action
/// uses Backend's compute_effective_date.
pub fn compute_effective_date_mirror(willensbekundung: time::Date) -> (i32, time::Date) {
    let year = willensbekundung.year();
    if willensbekundung.month() <= time::Month::June {
        (year, time::Date::from_calendar_date(year, time::Month::December, 31).unwrap())
    } else {
        (year + 1, time::Date::from_calendar_date(year + 1, time::Month::December, 31).unwrap())
    }
}

/// Phase 18 D-18-07 — Returns true iff transferring `shares` from a source with
/// `from_current_shares` would leave the source at exactly 0 shares (= full transfer).
/// Requires `shares >= 1` (zero or negative shares are invalid, return false).
pub fn is_voll_uebertrag(shares: i32, from_current_shares: i32) -> bool {
    shares >= 1 && from_current_shares - shares == 0
}

/// Phase 18 D-18-13 — Adapter: MemberSlimTO (DSGVO-konformer Slim, 7 fields) → MemberTO
/// (full struct, all PII fields set to None/default).
/// Used to feed MemberSearch the Transfer-Recipients without leaking PII.
pub fn to_member_to(slim: &MemberSlimTO) -> MemberTO {
    MemberTO {
        id: Some(slim.id),
        member_number: slim.member_number,
        first_name: slim.first_name.clone(),
        last_name: slim.last_name.clone(),
        salutation: slim.salutation.clone(),
        title: slim.title.clone(),
        email: None,
        company: None,
        comment: None,
        street: None,
        house_number: None,
        postal_code: None,
        city: None,
        join_date: time::Date::from_calendar_date(1970, time::Month::January, 1).unwrap(),
        shares_at_joining: 0,
        current_shares: 0,
        current_balance: 0,
        action_count: 0,
        migrated: false,
        exit_date: None,
        bank_account: None,
        status: MemberStatusTO::Normal,
        created: None,
        deleted: None,
        version: None,
    }
}

/// Phase 18 — Format `time::Date` as `DD.MM.YYYY` (always German format, independent of i18n locale,
/// because the preview text is mixed German + numbers and uses German format consistently).
pub fn format_date_german(d: &time::Date) -> String {
    format!("{:02}.{:02}.{:04}", d.day(), d.month() as u8, d.year())
}

// ─── Modal-Step state machine ───────────────────────────────────────

#[derive(Clone, Copy, PartialEq, Debug)]
enum ModalStep {
    SubChoice,
    Cancel,
    PartialRepayment,
    Transfer,
    Upgrade,
}

// ─── Component ──────────────────────────────────────────────────────

#[component]
pub fn MembershipAdjustModal(
    member: MemberTO,
    today: time::Date,
    on_close: EventHandler<()>,
    on_success: EventHandler<()>,
) -> Element {
    let i18n = use_i18n();
    let mut step = use_signal(|| ModalStep::SubChoice);

    // SC-2 — Default today(): each Sub-View initialises with `Some(today)`.
    // Shared signal across sub-views (persists when user goes back and re-enters).
    let date_signal = use_signal::<Option<time::Date>>(|| Some(today));
    // Operation-specific state (reset on Sub-View switch via Sub-Choice renderer).
    let shares_signal = use_signal::<i32>(|| 1);
    let recipient_id_signal = use_signal::<Option<Uuid>>(|| None);

    let submitting = use_signal(|| false);
    let error_signal = use_signal::<Option<AppError>>(|| None);

    let header_title = i18n.t(Key::MembershipAdjustModalTitle).to_string();

    rsx! {
        div { class: "flex flex-col gap-4",
            // ── Modal header (always visible) ──
            div { class: "flex items-center justify-between border-b border-gray-200 pb-3",
                h2 { class: "text-xl font-semibold text-gray-900",
                    "{header_title}"
                }
                button {
                    r#type: "button",
                    class: "text-gray-500 hover:text-gray-700 px-2 py-1",
                    onclick: move |_| on_close.call(()),
                    "✕"
                }
            }

            // ── Error display (always above Sub-View body) — AppError is Clone (api.rs:14 verified) ──
            {
                let err_opt = error_signal.read().clone();
                if let Some(err) = err_opt {
                    let mut err_sig = error_signal;
                    rsx! {
                        ErrorAlert {
                            error: err,
                            on_dismiss: Some(EventHandler::new(move |_| err_sig.set(None))),
                        }
                    }
                } else {
                    rsx! {}
                }
            }

            // ── Step body ── (I18n: Clone verified in i18n/mod.rs:834-840)
            {
                let current = *step.read();
                match current {
                    ModalStep::SubChoice => render_sub_choice(
                        i18n.clone(),
                        step,
                        shares_signal,
                        recipient_id_signal,
                    ),
                    ModalStep::Cancel => render_cancel_sub_view(
                        i18n.clone(),
                        member.clone(),
                        today,
                        date_signal,
                        submitting,
                        error_signal,
                        on_success,
                        step,
                    ),
                    ModalStep::PartialRepayment => render_partial_sub_view(
                        i18n.clone(),
                        member.clone(),
                        today,
                        date_signal,
                        shares_signal,
                        submitting,
                        error_signal,
                        on_success,
                        step,
                    ),
                    ModalStep::Transfer => render_transfer_sub_view(
                        i18n.clone(),
                        member.clone(),
                        today,
                        date_signal,
                        shares_signal,
                        recipient_id_signal,
                        submitting,
                        error_signal,
                        on_success,
                        step,
                    ),
                    ModalStep::Upgrade => render_upgrade_sub_view(
                        i18n.clone(),
                        member.clone(),
                        today,
                        date_signal,
                        shares_signal,
                        submitting,
                        error_signal,
                        on_success,
                        step,
                    ),
                }
            }
        }
    }
}

// ─── Sub-Choice (4 flat Buttons) ────────────────────────────────────

fn render_sub_choice(
    i18n: I18n,
    mut step: Signal<ModalStep>,
    mut shares_signal: Signal<i32>,
    mut recipient_id_signal: Signal<Option<Uuid>>,
) -> Element {
    // Reset operation-specific fields on every Sub-Choice display.
    // (date_signal is NOT reset — persists across sub-view re-entry per CONTEXT.md Discretion)
    shares_signal.set(1);
    recipient_id_signal.set(None);

    let q = i18n.t(Key::MembershipAdjustSubChoiceQuestion).to_string();
    let cancel_label = i18n.t(Key::MembershipAdjustSubChoiceCancel).to_string();
    let cancel_desc = i18n.t(Key::MembershipAdjustSubChoiceCancelDesc).to_string();
    let partial_label = i18n.t(Key::MembershipAdjustSubChoicePartialRepayment).to_string();
    let partial_desc = i18n.t(Key::MembershipAdjustSubChoicePartialRepaymentDesc).to_string();
    let transfer_label = i18n.t(Key::MembershipAdjustSubChoiceTransfer).to_string();
    let transfer_desc = i18n.t(Key::MembershipAdjustSubChoiceTransferDesc).to_string();
    let upgrade_label = i18n.t(Key::MembershipAdjustSubChoiceUpgrade).to_string();
    let upgrade_desc = i18n.t(Key::MembershipAdjustSubChoiceUpgradeDesc).to_string();

    rsx! {
        div { class: "flex flex-col gap-4",
            p { class: "text-sm text-gray-700", "{q}" }
            div { class: "grid grid-cols-1 sm:grid-cols-2 gap-4",
                button {
                    r#type: "button",
                    class: "flex flex-col items-start p-6 border-2 border-gray-200 rounded-lg hover:border-blue-500 hover:bg-blue-50 transition text-left",
                    onclick: move |_| step.set(ModalStep::Cancel),
                    span { class: "text-lg font-semibold", "{cancel_label}" }
                    span { class: "text-sm text-gray-600 mt-1", "{cancel_desc}" }
                }
                button {
                    r#type: "button",
                    class: "flex flex-col items-start p-6 border-2 border-gray-200 rounded-lg hover:border-blue-500 hover:bg-blue-50 transition text-left",
                    onclick: move |_| step.set(ModalStep::PartialRepayment),
                    span { class: "text-lg font-semibold", "{partial_label}" }
                    span { class: "text-sm text-gray-600 mt-1", "{partial_desc}" }
                }
                button {
                    r#type: "button",
                    class: "flex flex-col items-start p-6 border-2 border-gray-200 rounded-lg hover:border-blue-500 hover:bg-blue-50 transition text-left",
                    onclick: move |_| step.set(ModalStep::Transfer),
                    span { class: "text-lg font-semibold", "{transfer_label}" }
                    span { class: "text-sm text-gray-600 mt-1", "{transfer_desc}" }
                }
                button {
                    r#type: "button",
                    class: "flex flex-col items-start p-6 border-2 border-gray-200 rounded-lg hover:border-blue-500 hover:bg-blue-50 transition text-left",
                    onclick: move |_| step.set(ModalStep::Upgrade),
                    span { class: "text-lg font-semibold", "{upgrade_label}" }
                    span { class: "text-sm text-gray-600 mt-1", "{upgrade_desc}" }
                }
            }
        }
    }
}

// ─── Sub-View: Cancel ───────────────────────────────────────────────

fn render_cancel_sub_view(
    i18n: I18n,
    member: MemberTO,
    today: time::Date,
    mut date_signal: Signal<Option<time::Date>>,
    mut submitting: Signal<bool>,
    mut error_signal: Signal<Option<AppError>>,
    on_success: EventHandler<()>,
    mut step: Signal<ModalStep>,
) -> Element {
    let back_label = i18n.t(Key::MembershipAdjustBack).to_string();
    let title = i18n.t(Key::MembershipAdjustCancelTitle).to_string();
    let date_label = i18n.t(Key::MembershipAdjustCancelDateLabel).to_string();
    let preview_label = i18n.t(Key::MembershipAdjustPreviewLabel).to_string();
    let submit_label = i18n.t(Key::MembershipAdjustCancelSubmit).to_string();
    let cancel_label = i18n.t(Key::MembershipAdjustCancelButton).to_string();

    // Preview text live
    let preview_text = {
        let date_val = date_signal.read().clone();
        match date_val {
            None => String::new(),
            Some(d) => {
                if !is_valid_fiscal_year_date(d, today) {
                    String::new()
                } else {
                    let (fy, eff) = compute_effective_date_mirror(d);
                    let half_year = if d.month() <= time::Month::June {
                        i18n.t(Key::MembershipAdjustHalfYearH1).to_string()
                    } else {
                        i18n.t(Key::MembershipAdjustHalfYearH2).to_string()
                    };
                    let template = i18n.t(Key::MembershipAdjustCancelPreview);
                    let name = format!("{} {}", member.first_name, member.last_name);
                    template
                        .replace("{name}", &name)
                        .replace("{shares}", &member.current_shares.to_string())
                        .replace("{effective_date}", &format_date_german(&eff))
                        .replace("{half_year}", &half_year)
                        .replace("{fiscal_year}", &fy.to_string())
                }
            }
        }
    };

    let member_id = member.id;
    let date_for_submit = *date_signal.read();
    let is_valid = date_for_submit
        .map_or(false, |d| is_valid_fiscal_year_date(d, today));
    let is_submitting = *submitting.read();
    let disabled = !is_valid || is_submitting;

    rsx! {
        div { class: "flex flex-col gap-4",
            // Sub-view header
            div { class: "flex items-center gap-2",
                button {
                    r#type: "button",
                    class: "text-blue-600 hover:text-blue-800 text-sm",
                    onclick: move |_| step.set(ModalStep::SubChoice),
                    "{back_label}"
                }
                span { class: "text-gray-400", "·" }
                h3 { class: "text-xl font-semibold text-red-700", "{title}" }
            }

            // Form
            div { class: "flex flex-col gap-2",
                label { class: "block text-sm font-medium text-gray-700", "{date_label}" }
                FiscalYearDateInput {
                    value: date_signal,
                    on_change: move |d: time::Date| { date_signal.set(Some(d)); },
                    today: today,
                }
            }

            // Preview
            if !preview_text.is_empty() {
                div { class: "mt-2 p-4 bg-blue-50 border border-blue-200 rounded",
                    h4 { class: "text-xs font-semibold uppercase text-blue-900 mb-2", "{preview_label}" }
                    p { class: "text-sm", "{preview_text}" }
                }
            }

            // Action row
            div { class: "flex gap-2 justify-end mt-2",
                button {
                    r#type: "button",
                    class: "px-4 py-2 text-gray-700 hover:bg-gray-100 rounded",
                    disabled: is_submitting,
                    onclick: move |_| step.set(ModalStep::SubChoice),
                    "{cancel_label}"
                }
                button {
                    r#type: "button",
                    class: "px-6 py-2 bg-red-600 hover:bg-red-700 text-white rounded font-semibold disabled:bg-gray-300 disabled:cursor-not-allowed",
                    disabled: disabled,
                    onclick: move |_| {
                        let Some(id) = member_id else { return; };
                        let Some(d) = date_for_submit else { return; };
                        submitting.set(true);
                        error_signal.set(None);
                        spawn(async move {
                            let config = CONFIG.read().clone();
                            match api::cancel_membership(&config, id, d).await {
                                Ok(_resp) => {
                                    submitting.set(false);
                                    on_success.call(());
                                }
                                Err(e) => {
                                    submitting.set(false);
                                    error_signal.set(Some(e));
                                }
                            }
                        });
                    },
                    if is_submitting { "…" } else { "{submit_label}" }
                }
            }
        }
    }
}

// ─── Sub-View: PartialRepayment ─────────────────────────────────────

fn render_partial_sub_view(
    i18n: I18n,
    member: MemberTO,
    today: time::Date,
    mut date_signal: Signal<Option<time::Date>>,
    mut shares_signal: Signal<i32>,
    mut submitting: Signal<bool>,
    mut error_signal: Signal<Option<AppError>>,
    on_success: EventHandler<()>,
    mut step: Signal<ModalStep>,
) -> Element {
    let back_label = i18n.t(Key::MembershipAdjustBack).to_string();
    let title = i18n.t(Key::MembershipAdjustPartialRepaymentTitle).to_string();
    let date_label = i18n.t(Key::MembershipAdjustPartialRepaymentDateLabel).to_string();
    let shares_label = i18n.t(Key::MembershipAdjustPartialRepaymentSharesLabel).to_string();
    let preview_label = i18n.t(Key::MembershipAdjustPreviewLabel).to_string();
    let submit_label = i18n.t(Key::MembershipAdjustPartialRepaymentSubmit).to_string();
    let cancel_label = i18n.t(Key::MembershipAdjustCancelButton).to_string();
    let shares_positive_err = i18n.t(Key::MembershipAdjustSharesMustBePositive).to_string();
    let shares_exceed_err = i18n.t(Key::MembershipAdjustPartialRepaymentSharesExceed).to_string();

    let shares_now = *shares_signal.read();
    let current = member.current_shares as i32;

    let inline_error_text: Option<String> = if shares_now <= 0 {
        Some(shares_positive_err.clone())
    } else if shares_now >= current {
        Some(shares_exceed_err.clone())
    } else {
        None
    };

    let preview_text = {
        let date_val = date_signal.read().clone();
        match date_val {
            None => String::new(),
            Some(d) if !is_valid_fiscal_year_date(d, today) => String::new(),
            Some(_) if inline_error_text.is_some() => String::new(),
            Some(d) => {
                let (fy, eff) = compute_effective_date_mirror(d);
                let new_shares = current - shares_now;
                let template = i18n.t(Key::MembershipAdjustPartialRepaymentPreview);
                let name = format!("{} {}", member.first_name, member.last_name);
                template
                    .replace("{name}", &name)
                    .replace("{current_shares}", &current.to_string())
                    .replace("{new_shares}", &new_shares.to_string())
                    .replace("{effective_date}", &format_date_german(&eff))
                    .replace("{fiscal_year}", &fy.to_string())
            }
        }
    };

    let member_id = member.id;
    let date_for_submit = *date_signal.read();
    let is_valid = date_for_submit.map_or(false, |d| is_valid_fiscal_year_date(d, today))
        && shares_now >= 1
        && shares_now < current;
    let is_submitting = *submitting.read();
    let disabled = !is_valid || is_submitting;

    rsx! {
        div { class: "flex flex-col gap-4",
            div { class: "flex items-center gap-2",
                button {
                    r#type: "button",
                    class: "text-blue-600 hover:text-blue-800 text-sm",
                    onclick: move |_| step.set(ModalStep::SubChoice),
                    "{back_label}"
                }
                span { class: "text-gray-400", "·" }
                h3 { class: "text-xl font-semibold text-gray-900", "{title}" }
            }

            div { class: "flex flex-col gap-2",
                label { class: "block text-sm font-medium text-gray-700", "{date_label}" }
                FiscalYearDateInput {
                    value: date_signal,
                    on_change: move |d: time::Date| { date_signal.set(Some(d)); },
                    today: today,
                }
            }

            div { class: "flex flex-col gap-2",
                label { class: "block text-sm font-medium text-gray-700", "{shares_label}" }
                input {
                    r#type: "number",
                    min: "1",
                    max: "{current}",
                    value: "{shares_now}",
                    class: "w-full px-3 py-2 border border-gray-300 rounded focus:ring-2 focus:ring-blue-500",
                    oninput: move |e| {
                        if let Ok(n) = e.value().parse::<i32>() {
                            shares_signal.set(n);
                        }
                    }
                }
                if let Some(err) = inline_error_text.as_ref() {
                    span { class: "text-red-600 text-sm", "{err}" }
                }
            }

            if !preview_text.is_empty() {
                div { class: "mt-2 p-4 bg-blue-50 border border-blue-200 rounded",
                    h4 { class: "text-xs font-semibold uppercase text-blue-900 mb-2", "{preview_label}" }
                    p { class: "text-sm", "{preview_text}" }
                }
            }

            div { class: "flex gap-2 justify-end mt-2",
                button {
                    r#type: "button",
                    class: "px-4 py-2 text-gray-700 hover:bg-gray-100 rounded",
                    disabled: is_submitting,
                    onclick: move |_| step.set(ModalStep::SubChoice),
                    "{cancel_label}"
                }
                button {
                    r#type: "button",
                    class: "px-6 py-2 bg-red-600 hover:bg-red-700 text-white rounded font-semibold disabled:bg-gray-300 disabled:cursor-not-allowed",
                    disabled: disabled,
                    onclick: move |_| {
                        let Some(id) = member_id else { return; };
                        let Some(d) = date_for_submit else { return; };
                        let n = shares_now;
                        submitting.set(true);
                        error_signal.set(None);
                        spawn(async move {
                            let config = CONFIG.read().clone();
                            match api::partial_repayment(&config, id, n, d).await {
                                Ok(_resp) => {
                                    submitting.set(false);
                                    on_success.call(());
                                }
                                Err(e) => {
                                    submitting.set(false);
                                    error_signal.set(Some(e));
                                }
                            }
                        });
                    },
                    if is_submitting { "…" } else { "{submit_label}" }
                }
            }
        }
    }
}

// ─── Sub-View: Transfer ─────────────────────────────────────────────

fn render_transfer_sub_view(
    i18n: I18n,
    member: MemberTO,
    today: time::Date,
    mut date_signal: Signal<Option<time::Date>>,
    mut shares_signal: Signal<i32>,
    mut recipient_id_signal: Signal<Option<Uuid>>,
    mut submitting: Signal<bool>,
    mut error_signal: Signal<Option<AppError>>,
    on_success: EventHandler<()>,
    mut step: Signal<ModalStep>,
) -> Element {
    let back_label = i18n.t(Key::MembershipAdjustBack).to_string();
    let title = i18n.t(Key::MembershipAdjustTransferTitle).to_string();
    let date_label = i18n.t(Key::MembershipAdjustTransferDateLabel).to_string();
    let shares_label = i18n.t(Key::MembershipAdjustTransferSharesLabel).to_string();
    let recipient_label = i18n.t(Key::MembershipAdjustTransferRecipientLabel).to_string();
    let recipient_load_err = i18n.t(Key::MembershipAdjustTransferRecipientLoadError).to_string();
    let no_recipients = i18n.t(Key::MembershipAdjustNoRecipients).to_string();
    let loading_label = i18n.t(Key::MembershipAdjustLoading).to_string();
    let preview_label = i18n.t(Key::MembershipAdjustPreviewLabel).to_string();
    let submit_label = i18n.t(Key::MembershipAdjustTransferSubmit).to_string();
    let cancel_label = i18n.t(Key::MembershipAdjustCancelButton).to_string();
    let self_error = i18n.t(Key::MembershipAdjustTransferSelfError).to_string();

    let from_id_opt = member.id;
    let from_id = match from_id_opt {
        Some(id) => id,
        None => return rsx! { div { "Member has no id; cannot transfer." } },
    };

    let recipients_resource = use_resource(move || async move {
        let config = CONFIG.read().clone();
        api::get_transfer_recipients(&config, from_id).await
    });

    let shares_now = *shares_signal.read();
    let current = member.current_shares as i32;
    let recipient_id_val = *recipient_id_signal.read();

    // Self-Transfer-Block (Frontend mirror of TRSF-07 backend)
    let inline_self_err: Option<String> = if recipient_id_val == Some(from_id) {
        Some(self_error.clone())
    } else {
        None
    };
    let inline_shares_err: Option<String> = if shares_now < 1 || shares_now > current {
        Some(i18n.t(Key::MembershipAdjustSharesMustBePositive).to_string())
    } else {
        None
    };

    // Voll-Uebertrag detection (D-18-07)
    let is_full = is_voll_uebertrag(shares_now, current);

    // Preview text (live)
    let preview_text = {
        let date_val = date_signal.read().clone();
        let recipient_to: Option<MemberTO> = match (&*recipients_resource.read(), recipient_id_val) {
            (Some(Ok(list)), Some(rid)) => list.iter().find(|s| s.id == rid).map(to_member_to),
            _ => None,
        };
        match (date_val, recipient_to) {
            (None, _) | (_, None) => String::new(),
            (Some(d), Some(_)) if !is_valid_fiscal_year_date(d, today)
                || inline_self_err.is_some() || inline_shares_err.is_some() => String::new(),
            (Some(d), Some(to)) => {
                let from_new = current - shares_now;
                let to_current = to.current_shares as i32;
                let to_new = to_current + shares_now;
                let template = i18n.t(Key::MembershipAdjustTransferPreview);
                let from_name = format!("{} {}", member.first_name, member.last_name);
                let to_name = format!("{} {}", to.first_name, to.last_name);
                template
                    .replace("{from_name}", &from_name)
                    .replace("{from_shares}", &current.to_string())
                    .replace("{from_new}", &from_new.to_string())
                    .replace("{to_name}", &to_name)
                    .replace("{to_shares}", &to_current.to_string())
                    .replace("{to_new}", &to_new.to_string())
                    .replace("{transfer_date}", &format_date_german(&d))
            }
        }
    };

    let voll_warning_text = if is_full && !preview_text.is_empty() {
        let date_val = date_signal.read().clone();
        match date_val {
            Some(d) => {
                let template = i18n.t(Key::MembershipAdjustTransferFullExitWarning);
                let from_name = format!("{} {}", member.first_name, member.last_name);
                template
                    .replace("{from_name}", &from_name)
                    .replace("{transfer_date}", &format_date_german(&d))
            }
            None => String::new(),
        }
    } else {
        String::new()
    };

    let date_for_submit = *date_signal.read();
    let is_valid = date_for_submit.map_or(false, |d| is_valid_fiscal_year_date(d, today))
        && shares_now >= 1
        && shares_now <= current
        && recipient_id_val.is_some()
        && recipient_id_val != Some(from_id);
    let is_submitting = *submitting.read();
    let disabled = !is_valid || is_submitting;

    rsx! {
        div { class: "flex flex-col gap-4",
            div { class: "flex items-center gap-2",
                button {
                    r#type: "button",
                    class: "text-blue-600 hover:text-blue-800 text-sm",
                    onclick: move |_| step.set(ModalStep::SubChoice),
                    "{back_label}"
                }
                span { class: "text-gray-400", "·" }
                h3 { class: "text-xl font-semibold text-gray-900", "{title}" }
            }

            div { class: "flex flex-col gap-2",
                label { class: "block text-sm font-medium text-gray-700", "{date_label}" }
                FiscalYearDateInput {
                    value: date_signal,
                    on_change: move |d: time::Date| { date_signal.set(Some(d)); },
                    today: today,
                }
            }

            div { class: "flex flex-col gap-2",
                label { class: "block text-sm font-medium text-gray-700", "{shares_label}" }
                input {
                    r#type: "number",
                    min: "1",
                    max: "{current}",
                    value: "{shares_now}",
                    class: "w-full px-3 py-2 border border-gray-300 rounded focus:ring-2 focus:ring-blue-500",
                    oninput: move |e| {
                        if let Ok(n) = e.value().parse::<i32>() {
                            shares_signal.set(n);
                        }
                    }
                }
                if let Some(err) = inline_shares_err.as_ref() {
                    span { class: "text-red-600 text-sm", "{err}" }
                }
            }

            div { class: "flex flex-col gap-2",
                label { class: "block text-sm font-medium text-gray-700", "{recipient_label}" }
                {
                    match &*recipients_resource.read() {
                        None => rsx! { p { class: "text-sm text-gray-500", "{loading_label}" } },
                        Some(Err(_)) => rsx! { p { class: "text-sm text-red-600", "{recipient_load_err}" } },
                        Some(Ok(list)) if list.is_empty() => rsx! {
                            p { class: "text-sm text-gray-500", "{no_recipients}" }
                        },
                        Some(Ok(list)) => {
                            let adapted: Vec<MemberTO> = list.iter().map(to_member_to).collect();
                            rsx! {
                                MemberSearch {
                                    on_select: move |maybe_id: Option<Uuid>| {
                                        recipient_id_signal.set(maybe_id);
                                    },
                                    selected_id: recipient_id_val,
                                    exclude_id: Some(from_id),
                                    members_override: Some(adapted),
                                }
                            }
                        }
                    }
                }
                if let Some(err) = inline_self_err.as_ref() {
                    span { class: "text-red-600 text-sm", "{err}" }
                }
            }

            if !preview_text.is_empty() {
                div { class: "mt-2 p-4 bg-blue-50 border border-blue-200 rounded",
                    h4 { class: "text-xs font-semibold uppercase text-blue-900 mb-2", "{preview_label}" }
                    p { class: "text-sm", "{preview_text}" }
                    if !voll_warning_text.is_empty() {
                        p { class: "mt-2 text-orange-700 font-bold", "{voll_warning_text}" }
                    }
                }
            }

            div { class: "flex gap-2 justify-end mt-2",
                button {
                    r#type: "button",
                    class: "px-4 py-2 text-gray-700 hover:bg-gray-100 rounded",
                    disabled: is_submitting,
                    onclick: move |_| step.set(ModalStep::SubChoice),
                    "{cancel_label}"
                }
                button {
                    r#type: "button",
                    class: "px-6 py-2 bg-red-600 hover:bg-red-700 text-white rounded font-semibold disabled:bg-gray-300 disabled:cursor-not-allowed",
                    disabled: disabled,
                    onclick: move |_| {
                        let Some(d) = date_for_submit else { return; };
                        let Some(to_id) = recipient_id_val else { return; };
                        let n = shares_now;
                        submitting.set(true);
                        error_signal.set(None);
                        spawn(async move {
                            let config = CONFIG.read().clone();
                            match api::transfer_shares(&config, from_id, to_id, n, d).await {
                                Ok(_resp) => {
                                    submitting.set(false);
                                    on_success.call(());
                                }
                                Err(e) => {
                                    submitting.set(false);
                                    error_signal.set(Some(e));
                                }
                            }
                        });
                    },
                    if is_submitting { "…" } else { "{submit_label}" }
                }
            }
        }
    }
}

// ─── Sub-View: Upgrade ──────────────────────────────────────────────

fn render_upgrade_sub_view(
    i18n: I18n,
    member: MemberTO,
    today: time::Date,
    mut date_signal: Signal<Option<time::Date>>,
    mut shares_signal: Signal<i32>,
    mut submitting: Signal<bool>,
    mut error_signal: Signal<Option<AppError>>,
    on_success: EventHandler<()>,
    mut step: Signal<ModalStep>,
) -> Element {
    let back_label = i18n.t(Key::MembershipAdjustBack).to_string();
    let title = i18n.t(Key::MembershipAdjustUpgradeTitle).to_string();
    let date_label = i18n.t(Key::MembershipAdjustUpgradeDateLabel).to_string();
    let shares_label = i18n.t(Key::MembershipAdjustUpgradeSharesLabel).to_string();
    let preview_label = i18n.t(Key::MembershipAdjustPreviewLabel).to_string();
    let submit_label = i18n.t(Key::MembershipAdjustUpgradeSubmit).to_string();
    let cancel_label = i18n.t(Key::MembershipAdjustCancelButton).to_string();
    let shares_err = i18n.t(Key::MembershipAdjustSharesMustBePositive).to_string();

    let shares_now = *shares_signal.read();
    let current = member.current_shares as i32;

    let inline_err: Option<String> = if shares_now < 1 { Some(shares_err) } else { None };

    let preview_text = {
        let date_val = date_signal.read().clone();
        match date_val {
            None => String::new(),
            Some(d) if !is_valid_fiscal_year_date(d, today) => String::new(),
            Some(_) if inline_err.is_some() => String::new(),
            Some(d) => {
                let new_shares = current + shares_now;
                let template = i18n.t(Key::MembershipAdjustUpgradePreview);
                let name = format!("{} {}", member.first_name, member.last_name);
                template
                    .replace("{name}", &name)
                    .replace("{current_shares}", &current.to_string())
                    .replace("{new_shares}", &new_shares.to_string())
                    .replace("{date}", &format_date_german(&d))
            }
        }
    };

    let member_id = member.id;
    let date_for_submit = *date_signal.read();
    let is_valid = date_for_submit.map_or(false, |d| is_valid_fiscal_year_date(d, today))
        && shares_now >= 1;
    let is_submitting = *submitting.read();
    let disabled = !is_valid || is_submitting;

    rsx! {
        div { class: "flex flex-col gap-4",
            div { class: "flex items-center gap-2",
                button {
                    r#type: "button",
                    class: "text-blue-600 hover:text-blue-800 text-sm",
                    onclick: move |_| step.set(ModalStep::SubChoice),
                    "{back_label}"
                }
                span { class: "text-gray-400", "·" }
                h3 { class: "text-xl font-semibold text-gray-900", "{title}" }
            }

            div { class: "flex flex-col gap-2",
                label { class: "block text-sm font-medium text-gray-700", "{date_label}" }
                FiscalYearDateInput {
                    value: date_signal,
                    on_change: move |d: time::Date| { date_signal.set(Some(d)); },
                    today: today,
                }
            }

            div { class: "flex flex-col gap-2",
                label { class: "block text-sm font-medium text-gray-700", "{shares_label}" }
                input {
                    r#type: "number",
                    min: "1",
                    value: "{shares_now}",
                    class: "w-full px-3 py-2 border border-gray-300 rounded focus:ring-2 focus:ring-blue-500",
                    oninput: move |e| {
                        if let Ok(n) = e.value().parse::<i32>() {
                            shares_signal.set(n);
                        }
                    }
                }
                if let Some(err) = inline_err.as_ref() {
                    span { class: "text-red-600 text-sm", "{err}" }
                }
            }

            if !preview_text.is_empty() {
                div { class: "mt-2 p-4 bg-blue-50 border border-blue-200 rounded",
                    h4 { class: "text-xs font-semibold uppercase text-blue-900 mb-2", "{preview_label}" }
                    p { class: "text-sm", "{preview_text}" }
                }
            }

            div { class: "flex gap-2 justify-end mt-2",
                button {
                    r#type: "button",
                    class: "px-4 py-2 text-gray-700 hover:bg-gray-100 rounded",
                    disabled: is_submitting,
                    onclick: move |_| step.set(ModalStep::SubChoice),
                    "{cancel_label}"
                }
                button {
                    r#type: "button",
                    class: "px-6 py-2 bg-red-600 hover:bg-red-700 text-white rounded font-semibold disabled:bg-gray-300 disabled:cursor-not-allowed",
                    disabled: disabled,
                    onclick: move |_| {
                        let Some(id) = member_id else { return; };
                        let Some(d) = date_for_submit else { return; };
                        let n = shares_now;
                        submitting.set(true);
                        error_signal.set(None);
                        spawn(async move {
                            let config = CONFIG.read().clone();
                            match api::increase_shares(&config, id, n, d).await {
                                Ok(_resp) => {
                                    submitting.set(false);
                                    on_success.call(());
                                }
                                Err(e) => {
                                    submitting.set(false);
                                    error_signal.set(Some(e));
                                }
                            }
                        });
                    },
                    if is_submitting { "…" } else { "{submit_label}" }
                }
            }
        }
    }
}

// ─── Tests for pure helpers ─────────────────────────────────────────

#[cfg(test)]
mod tests {
    use super::*;
    use time::{Date, Month};

    fn d(year: i32, month: Month, day: u8) -> Date {
        Date::from_calendar_date(year, month, day).unwrap()
    }

    // ── compute_effective_date_mirror — 6 tests ──

    #[test]
    fn compute_effective_h1_june_30_boundary() {
        let (fy, eff) = compute_effective_date_mirror(d(2026, Month::June, 30));
        assert_eq!(fy, 2026);
        assert_eq!(eff, d(2026, Month::December, 31));
    }

    #[test]
    fn compute_effective_h2_july_1_boundary() {
        let (fy, eff) = compute_effective_date_mirror(d(2026, Month::July, 1));
        assert_eq!(fy, 2027);
        assert_eq!(eff, d(2027, Month::December, 31));
    }

    #[test]
    fn compute_effective_h2_year_end() {
        let (fy, eff) = compute_effective_date_mirror(d(2026, Month::December, 31));
        assert_eq!(fy, 2027);
        assert_eq!(eff, d(2027, Month::December, 31));
    }

    #[test]
    fn compute_effective_h1_year_start() {
        let (fy, eff) = compute_effective_date_mirror(d(2026, Month::January, 1));
        assert_eq!(fy, 2026);
        assert_eq!(eff, d(2026, Month::December, 31));
    }

    #[test]
    fn compute_effective_h1_leap_year_feb_29() {
        let (fy, eff) = compute_effective_date_mirror(d(2024, Month::February, 29));
        assert_eq!(fy, 2024);
        assert_eq!(eff, d(2024, Month::December, 31));
    }

    #[test]
    fn compute_effective_h1_mid_year() {
        let (fy, eff) = compute_effective_date_mirror(d(2026, Month::June, 15));
        assert_eq!(fy, 2026);
        assert_eq!(eff, d(2026, Month::December, 31));
    }

    // ── is_voll_uebertrag — 3 tests ──

    #[test]
    fn voll_uebertrag_eq_returns_true() {
        assert!(is_voll_uebertrag(5, 5));
    }

    #[test]
    fn voll_uebertrag_lt_returns_false() {
        assert!(!is_voll_uebertrag(3, 5));
    }

    #[test]
    fn voll_uebertrag_zero_shares_returns_false() {
        assert!(!is_voll_uebertrag(0, 5));
    }

    // ── to_member_to — 1 test ──

    #[test]
    fn to_member_to_drops_pii_fields() {
        let slim = MemberSlimTO {
            id: Uuid::from_u128(99),
            member_number: 42,
            salutation: None,
            title: None,
            first_name: "Anna".into(),
            last_name: "Weber".into(),
        };
        let mt = to_member_to(&slim);
        assert_eq!(mt.id, Some(slim.id));
        assert_eq!(mt.member_number, 42);
        assert_eq!(mt.first_name, "Anna");
        assert!(mt.email.is_none(), "email must be None");
        assert!(mt.street.is_none(), "street must be None");
        assert!(mt.postal_code.is_none(), "postal_code must be None");
        assert!(mt.city.is_none(), "city must be None");
        assert!(mt.bank_account.is_none(), "bank_account must be None");
        assert_eq!(mt.current_shares, 0);
        assert_eq!(mt.status, MemberStatusTO::Normal);
    }

    // ── format_date_german — 2 tests ──

    #[test]
    fn format_date_german_simple() {
        assert_eq!(format_date_german(&d(2026, Month::June, 15)), "15.06.2026");
    }

    #[test]
    fn format_date_german_year_end() {
        assert_eq!(format_date_german(&d(2026, Month::December, 31)), "31.12.2026");
    }
}
```

**Hinweis** zu MemberTO-Field-Set: Falls `MemberTO`-Felder im Frontend-`rest-types` (Plan 01 hat sie nicht veraendert) von der Liste oben abweichen, der Executor pruefen via `grep -n "pub struct MemberTO" -A 30 genossi-frontend/rest-types/src/lib.rs` und passt das `to_member_to`-Body an alle vorhandenen Felder an (Defaults entsprechend).
  </action>
  <verify>
    <automated>cd genossi-frontend &amp;&amp; cargo check 2>&amp;1 | tail -20 &amp;&amp; cargo test --lib component::membership_adjust_modal 2>&amp;1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Datei `genossi-frontend/src/component/membership_adjust_modal.rs` existiert
    - `grep -c "pub fn MembershipAdjustModal" genossi-frontend/src/component/membership_adjust_modal.rs` == 1
    - `grep -c "pub fn compute_effective_date_mirror" genossi-frontend/src/component/membership_adjust_modal.rs` == 1
    - `grep -c "pub fn is_voll_uebertrag" genossi-frontend/src/component/membership_adjust_modal.rs` == 1
    - `grep -c "pub fn to_member_to" genossi-frontend/src/component/membership_adjust_modal.rs` == 1
    - `grep -c "pub fn format_date_german" genossi-frontend/src/component/membership_adjust_modal.rs` == 1
    - `grep -c "enum ModalStep" genossi-frontend/src/component/membership_adjust_modal.rs` == 1
    - Alle 5 ModalStep-Varianten: `grep -E "ModalStep::(SubChoice|Cancel|PartialRepayment|Transfer|Upgrade)" genossi-frontend/src/component/membership_adjust_modal.rs | wc -l` &gt;= 10
    - **SC-2 Default-today()**: `grep -c "use_signal::<Option<time::Date>>(|| Some(today))" genossi-frontend/src/component/membership_adjust_modal.rs` == 1
    - Dioxus-Reload-Bug-Pattern (C-18-CF-03): `grep -v '^//' genossi-frontend/src/component/membership_adjust_modal.rs | grep -c 'r#type: "button"'` &gt;= 8
    - **Negative-Gate** kein Form-Reload-Anti-Pattern: `grep -v '^//' genossi-frontend/src/component/membership_adjust_modal.rs | grep -c 'r#type: "submit"'` == 0
    - **Negative-Gate** keine inline-Form-Submit: `grep -v '^//' genossi-frontend/src/component/membership_adjust_modal.rs | grep -c 'onsubmit'` == 0
    - **Negative-Gate** kein nicht-existierendes Macro: `grep -c "t_format!\|t_with_args" genossi-frontend/src/component/membership_adjust_modal.rs` == 0
    - API-Calls verwendet: `grep -E "api::(cancel_membership|increase_shares|partial_repayment|transfer_shares|get_transfer_recipients)" genossi-frontend/src/component/membership_adjust_modal.rs | wc -l` &gt;= 5
    - MemberSearch mit override: `grep -c "members_override:" genossi-frontend/src/component/membership_adjust_modal.rs` &gt;= 1
    - FiscalYearDateInput integration: `grep -c "FiscalYearDateInput {" genossi-frontend/src/component/membership_adjust_modal.rs` &gt;= 4
    - Vorschau-Box-Pattern: `grep -c "bg-blue-50 border border-blue-200 rounded" genossi-frontend/src/component/membership_adjust_modal.rs` &gt;= 4
    - Voll-Uebertrag-Warnung: `grep -c "text-orange-700" genossi-frontend/src/component/membership_adjust_modal.rs` &gt;= 1
    - I18n: Clone Pattern: `grep -c "i18n.clone()" genossi-frontend/src/component/membership_adjust_modal.rs` &gt;= 4
    - `cd genossi-frontend &amp;&amp; cargo check` exit 0
    - `cd genossi-frontend &amp;&amp; cargo test --lib component::membership_adjust_modal` zeigt mindestens 12 passed (6 + 3 + 1 + 2)
  </acceptance_criteria>
  <done>MembershipAdjustModal mit 5 ModalStep-Varianten implementiert; 12+ Pure-Helper-Tests gruen; Dioxus-Reload-Bug-Pattern eingehalten; integriert mit Plan 01-05 (rest-types, toast, MemberSearch override, FiscalYearDateInput, alle i18n Keys, API-Funktionen); SC-2 Default-today() im Modal-Body.</done>
</task>

<task type="auto">
  <name>Task 2: Re-Export von MembershipAdjustModal in component/mod.rs</name>
  <read_first>
    - genossi-frontend/src/component/mod.rs (vor allem das Phase-18-Section-Block, das Plan 04 hinzugefuegt hat)
  </read_first>
  <files>genossi-frontend/src/component/mod.rs</files>
  <action>
Innerhalb des bestehenden Phase-18-Sections (von Plan 04 angelegt), folgenden Block ergaenzen:

```rust
// ─── Phase 18 ─── MembershipAdjustModal (Plan 06) ────
pub mod membership_adjust_modal;
pub use membership_adjust_modal::{
    MembershipAdjustModal,
    compute_effective_date_mirror,
    is_voll_uebertrag,
    to_member_to,
    format_date_german,
};
```

Reihenfolge im Phase-18-Section: erst `fiscal_year_date_input` (Plan 04), dann `toast`-Re-Exports (Plan 04), dann `membership_adjust_modal` (Plan 06). Bewusst getrennt damit die Section sauber lesbar bleibt.
  </action>
  <verify>
    <automated>cd genossi-frontend &amp;&amp; cargo check 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub mod membership_adjust_modal;" genossi-frontend/src/component/mod.rs` == 1
    - `grep -c "MembershipAdjustModal" genossi-frontend/src/component/mod.rs` &gt;= 2
    - `grep -c "compute_effective_date_mirror\|is_voll_uebertrag\|to_member_to\|format_date_german" genossi-frontend/src/component/mod.rs` &gt;= 4
    - Plan-04-Re-Exports unveraendert: `grep -c "pub mod fiscal_year_date_input;" genossi-frontend/src/component/mod.rs` == 1
    - Plan-04-Re-Exports unveraendert: `grep -c "pub use toast::{show_success_toast" genossi-frontend/src/component/mod.rs` == 1
    - `cd genossi-frontend &amp;&amp; cargo check` exit 0
  </acceptance_criteria>
  <done>MembershipAdjustModal von Page importierbar via `use crate::component::MembershipAdjustModal;`; Plan-04-Re-Exports unveraendert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser↔Modal-Form | User-Input (Datum, Anteile, Recipient-Pick) wird Frontend-validiert UND Server-Layer-validiert (Defense-in-Depth) |
| Modal↔API | JSON-Bodies via reqwest::Client zu Vorstand-only Backend-Endpoints (ADMIN_PRIVILEGE Funnel aus Phase 15 D-15-01) |
| WASM↔ErrorAlert | API-Error-Body wird in Dioxus `"{err.message}"` interpoliert (Text-Escape, kein XSS) |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-06-01 | Tampering | Self-Transfer-Bypass via DevTools (Browser-Validation umgangen) | low | mitigate | Frontend `inline_self_err` als UI-Feedback + Backend `TRSF-07` Service-Layer-Guard (Phase 17) ist autoritativ; Submit-Button-Disabled ist nur UX. |
| T-18-06-02 | Tampering | Negative shares values via DevTools | low | mitigate | Frontend `inline_shares_err` + Backend D-15-06/D-16-07 Range-Validation (HTTP 400). |
| T-18-06-03 | Information Disclosure | Recipient-List enthaelt PII | low | mitigate | Backend `get_transfer_recipients` liefert nur `MemberSlimTO` (7-Feld-Whitelist, D-14-12); `to_member_to`-Adapter setzt alle PII-Felder auf None. |
| T-18-06-04 | XSS | Member-Names in Vorschau-Text via `.replace()` interpoliert | low | accept | Dioxus RSX `"{preview_text}"` rendert als Text, nicht als HTML. |
| T-18-06-05 | Repudiation | Submit ohne Audit-Trail | low | accept | Alle 4 Backend-Endpoints (Phase 15/16/17) nutzen `audited_*!`-Macros. |
| T-18-06-06 | Denial of Service | Wiederholtes Submit-Klick | low | mitigate | `submitting.set(true)` + Button-disabled-State; max 1 Inflight-Request pro Sub-View. |
| T-18-06-07 | Spoofing | Privilege-Escalation via API-Direct-Call | low | accept | Backend-Endpoints sind ADMIN_PRIVILEGE-protected. |

Keine `high`-Severity-Threats.
</threat_model>

<verification>
- `cargo check` gruen ohne Warnings
- 12+ Pure-Helper-Unit-Tests gruen
- Dioxus-Reload-Bug-Pattern: `r#type: "button"` &gt;=8x; `r#type: "submit"`==0; `onsubmit`==0
- Alle 5 API-Funktionen aus api.rs werden konsumiert
- MemberSearch mit members_override-Override aufgerufen (TransferSubView)
- FiscalYearDateInput in 4 Sub-Views integriert
- SC-2 Default-today() erfuellt
</verification>

<success_criteria>
- MembershipAdjustModal shared Component existiert und ist importierbar
- 4 Sub-Views funktionsfaehig mit Live-Vorschau
- Plan 07 (Page-Integration) ist entblockt
</success_criteria>

<output>
After completion, create `.planning/phases/18-frontend-component-first/18-06-SUMMARY.md` with:
- Modal-LOC-Count
- Test-Output von `cargo test --lib component::membership_adjust_modal`
- Verifikation aller Negative-Gates (kein r#type:"submit", kein t_format!, ...)
- Bestaetigung SC-2 Default-today() (use_signal(|| Some(today)))
- Bestaetigung: Plan 07 (Page) kann den Modal mounten
</output>
