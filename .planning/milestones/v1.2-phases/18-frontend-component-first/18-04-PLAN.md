---
phase: 18-frontend-component-first
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - genossi-frontend/src/component/fiscal_year_date_input.rs
  - genossi-frontend/src/i18n/mod.rs
  - genossi-frontend/src/i18n/de.rs
  - genossi-frontend/src/i18n/en.rs
  - genossi-frontend/src/component/mod.rs
autonomous: true
requirements:
  - UI-02
  - UI-03
requirements_addressed:
  - UI-02
  - UI-03
tags:
  - frontend
  - component
  - i18n
  - datepicker

must_haves:
  truths:
    - "`FiscalYearDateInput`-Component existiert in `genossi-frontend/src/component/fiscal_year_date_input.rs` mit Props `(value: Signal<Option<Date>>, on_change: EventHandler<Date>, today: Date)` (D-18-10)."
    - "Datepicker hat `min={today.year}-01-01` und `max={today.year + 1}-12-31` (D-18-09 + UI-03)."
    - "Pure-Helper `is_valid_fiscal_year_date(date, today) -> bool` existiert mit 4 Unit-Tests (current-year, next-year, prev-year reject, year-after-next reject)."
    - "`i18n/mod.rs` enthaelt mindestens 30 neue Phase-18-Key-Varianten."
    - "`i18n/de.rs` UND `i18n/en.rs` haben fuer JEDEN neuen Key ein match-arm (KEINE Schluessel ohne DE+EN-Translation)."
    - "Translation-Test `phase_18_keys_have_distinct_de_en_translations` faengt vertauschte/leere/gleiche DE-EN-Eintraege ab (Whitelist: H1, H2, Locale-identische Marker)."
    - "`component/mod.rs` re-exportiert `FiscalYearDateInput`, `is_valid_fiscal_year_date`, `ToastVariant`, `show_success_toast`, `SuccessToastContainer`."
    - "Modal-Body (Plan 06) initialisiert das Datum-Signal mit `use_signal(|| Some(today))` — SC-2 'default today()'. FiscalYearDateInput-Props bleiben minimal (kein zusaetzlicher default-Prop)."
  falsehoods:
    - "Es existiert eine `cs.rs`-Datei oder ein `Locale::Cs`-Variant — Phase 18 DARF nichts dafuer hinzufuegen (C-18-CF-02 HART)."
    - "Out-of-Range-Datum (z.B. heute = 2026-06-15, eingegeben = 2024-01-01) wird als valide akzeptiert (Border bleibt grau)."
    - "DE-Translation und EN-Translation eines Phase-18-Keys sind identisch (ausser explizite Whitelist: H1, H2)."
  artifacts:
    - path: "genossi-frontend/src/component/fiscal_year_date_input.rs"
      provides: "FiscalYearDateInput component + is_valid_fiscal_year_date pure helper + 4 unit tests"
      contains: "pub fn FiscalYearDateInput"
    - path: "genossi-frontend/src/i18n/mod.rs"
      provides: "Phase-18 Key enum variants + Translation-Symmetrie-Test"
      contains: "MembershipAdjustButtonLabel"
    - path: "genossi-frontend/src/i18n/de.rs"
      provides: "DE translations for all Phase-18 keys"
      contains: "Mitgliedschaft anpassen"
    - path: "genossi-frontend/src/i18n/en.rs"
      provides: "EN translations for all Phase-18 keys"
      contains: "Adjust membership"
    - path: "genossi-frontend/src/component/mod.rs"
      provides: "Re-exports for FiscalYearDateInput, ToastVariant, SuccessToastContainer"
      contains: "pub use fiscal_year_date_input"
  key_links:
    - from: "genossi-frontend/src/component/fiscal_year_date_input.rs"
      to: "genossi-frontend/src/component/membership_adjust_modal.rs (Plan 06)"
      via: "FiscalYearDateInput { value: date_signal, on_change, today }"
      pattern: "FiscalYearDateInput"
    - from: "genossi-frontend/src/i18n/mod.rs"
      to: "genossi-frontend/src/component/membership_adjust_modal.rs (Plan 06)"
      via: "i18n.t(Key::MembershipAdjust...)"
      pattern: "MembershipAdjust"
---

<objective>
Drei foundational Frontend-Assets fuer Phase 18:
1. **FiscalYearDateInput**-Component (neue Datei) — wiederverwendbarer Datepicker mit GJ-Bounds (UI-03, D-18-09..11).
2. **Alle Phase-18 i18n-Keys** in `mod.rs` + DE + EN Translations + Symmetrie-Test (UI-04 + UI-SPEC Copywriting Contract).
3. **Re-Exports** in `component/mod.rs` fuer FiscalYearDateInput + Toast-Erweiterungen aus Plan 02.

Purpose: Plan 06 (Modal) und Plan 07 (Page) sind erst implementierbar, wenn Datepicker und i18n-Keys existieren. Alle drei Assets sind in Plan 04 zusammengefasst, weil sie keine Datei-Konflikte erzeugen und parallel zu P01-P03 + P05 in Wave 1 laufen koennen.

SC-2-Hinweis (Roadmap "default today()"): Die Default-Today-Verantwortung liegt beim CALLER (Modal-Body in Plan 06), nicht beim FiscalYearDateInput selbst. Pattern (b) aus Revision: Modal initialisiert `date_signal = use_signal(|| Some(today))` pro Sub-View — haelt Component-Props minimal und expliziert das "today" am Aufrufer.

Output: 1 neue Datei + 4 modifizierte Dateien; 30+ i18n-Keys mit DE+EN; FiscalYearDateInput mit 4 Pure-Helper-Tests + 1 Translation-Symmetrie-Test.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-frontend-component-first/18-CONTEXT.md
@.planning/phases/18-frontend-component-first/18-PATTERNS.md
@.planning/phases/18-frontend-component-first/18-UI-SPEC.md
@genossi-frontend/CLAUDE.md

<interfaces>
<!-- Existing helpers from page/member_details.rs:30-32, 63-79 (duplicated minimally per L-7 recommendation). -->
```rust
fn format_date_input(d: &time::Date) -> String { ... }  // "YYYY-MM-DD"
fn parse_date_input(s: &str) -> Option<time::Date> { ... }
```

<!-- Existing Locale + Key + I18n (genossi-frontend/src/i18n/mod.rs:9-12, 46-706, 712-788): -->
```rust
pub enum Locale { En, De }   // NUR diese 2 Varianten (C-18-CF-02 HART)
pub enum Key { /* ~700 existing variants */ }
impl I18n {
    pub fn t(&self, key: Key) -> Rc<str>;
    pub fn format_date(&self, d: &time::Date) -> String;  // "DD.MM.YYYY" fuer De
    // KEIN t_format!/t_with_args — Format-Args via `.replace("{name}", &val)` (Landmine L-4)
}
```

<!-- Existing component/mod.rs Section-Pattern (Zeile 112-118): -->
```rust
// ─── Phase 12 Plan 12-10 ─── RepaymentEntryPaidOutConfirm (UI-05) ────
pub mod repayment_entry_paidout_confirm;
pub use repayment_entry_paidout_confirm::{sum_payout_amounts, RepaymentEntryPaidOutConfirm};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FiscalYearDateInput-Component + is_valid_fiscal_year_date Pure-Helper + 4 Tests</name>
  <read_first>
    - genossi-frontend/src/component/repayment_entry_paidout_confirm.rs (Imports + #[component] pattern + #[cfg(test)] mod tests Block)
    - genossi-frontend/src/page/member_details.rs Zeilen 30-32 + 63-79 (format_date_input + parse_date_input — wir duplizieren minimal, da pub(crate) Refactor Cross-File-Risk)
    - .planning/phases/18-frontend-component-first/18-PATTERNS.md (Section 2 + Section "L-4")
    - .planning/phases/18-frontend-component-first/18-UI-SPEC.md (Component Inventory + Color Section fuer border-red-500 / border-gray-300)
    - genossi_service_impl/src/membership_adjust.rs Zeilen 739-756 (Backend `is_valid_fiscal_year_date` — wir spiegeln Logik)
  </read_first>
  <files>genossi-frontend/src/component/fiscal_year_date_input.rs</files>
  <behavior>
    - Test 1 `is_valid_fiscal_year_date_current_year`: `today = 2026-06-15`, `date = 2026-08-01` → `true` (current FY).
    - Test 2 `is_valid_fiscal_year_date_next_year`: `today = 2026-06-15`, `date = 2027-03-01` → `true` (next FY).
    - Test 3 `is_valid_fiscal_year_date_prev_year_rejected`: `today = 2026-06-15`, `date = 2025-12-31` → `false` (prev FY out of range).
    - Test 4 `is_valid_fiscal_year_date_year_after_next_rejected`: `today = 2026-06-15`, `date = 2028-01-01` → `false` (FY+2 out of range).
  </behavior>
  <action>
Neue Datei `genossi-frontend/src/component/fiscal_year_date_input.rs` anlegen, exakter Inhalt:

```rust
//! Phase 18 D-18-09..11 + UI-03 — Wiederverwendbarer Datepicker mit Geschaeftsjahr-Bounds.
//!
//! Erlaubter Bereich: aktuelles Kalenderjahr UND naechstes Kalenderjahr (heuristic mirror
//! des Backend-Validators `genossi_service_impl/src/membership_adjust.rs:739-756`).
//! Frontend-Validation ist Defense-in-Depth — Backend ist Single-Source-of-Truth.
//!
//! Default-Today-Pattern (SC-2 "default today()"): Die Default-today()-Verantwortung liegt
//! beim CALLER (Modal-Body initialisiert `use_signal(|| Some(today))`). Diese Component
//! akzeptiert deshalb `value: Signal<Option<Date>>` ohne separaten `default`-Prop —
//! der Caller kontrolliert den Initialwert explizit.

use dioxus::prelude::*;

use crate::i18n::{use_i18n, Key};

/// Phase 18 — Mirror of page/member_details.rs:30-32 (minimal duplication per PATTERNS L-7).
fn format_date_input(d: &time::Date) -> String {
    format!("{:04}-{:02}-{:02}", d.year(), d.month() as u8, d.day())
}

/// Phase 18 — Mirror of page/member_details.rs:63-79.
fn parse_date_input(s: &str) -> Option<time::Date> {
    let parts: Vec<&str> = s.split('-').collect();
    if parts.len() != 3 {
        return None;
    }
    let year: i32 = parts[0].parse().ok()?;
    let month: u8 = parts[1].parse().ok()?;
    let day: u8 = parts[2].parse().ok()?;
    let month: time::Month = month.try_into().ok()?;
    time::Date::from_calendar_date(year, month, day).ok()
}

/// Phase 18 D-18-11 — Pure helper, frontend mirror of backend `is_valid_fiscal_year_date`
/// (genossi_service_impl/src/membership_adjust.rs:739-756).
/// Returns `true` if `date.year()` equals `today.year()` or `today.year() + 1`.
pub fn is_valid_fiscal_year_date(date: time::Date, today: time::Date) -> bool {
    let current_fy = today.year();
    date.year() == current_fy || date.year() == current_fy + 1
}

#[component]
pub fn FiscalYearDateInput(
    value: Signal<Option<time::Date>>,
    on_change: EventHandler<time::Date>,
    today: time::Date,
) -> Element {
    let i18n = use_i18n();
    let min_year = today.year();
    let max_year = today.year() + 1;
    let min_str = format!("{:04}-01-01", min_year);
    let max_str = format!("{:04}-12-31", max_year);

    let value_read = value.read();
    let current_value_str = value_read.as_ref().map(format_date_input).unwrap_or_default();
    let is_oor = value_read.as_ref()
        .map_or(false, |d| !is_valid_fiscal_year_date(*d, today));
    drop(value_read);

    let border_class = if is_oor { "border-red-500" } else { "border-gray-300" };

    // L-4 Mitigation: i18n.t() returns Rc<str>; format-args via .replace()
    let helper_template = i18n.t(Key::FiscalYearDateInputHelper);
    let helper_text = helper_template
        .replace("{min_year}", &min_year.to_string())
        .replace("{max_year}", &max_year.to_string());
    let oor_text = i18n.t(Key::FiscalYearDateOutOfRange).to_string();

    rsx! {
        div { class: "flex flex-col gap-1",
            input {
                r#type: "date",
                min: "{min_str}",
                max: "{max_str}",
                value: "{current_value_str}",
                class: "w-full px-3 py-2 border {border_class} rounded focus:ring-2 focus:ring-blue-500",
                oninput: move |e| {
                    let s = e.value();
                    if s.is_empty() {
                        value.set(None);
                    } else if let Some(d) = parse_date_input(&s) {
                        value.set(Some(d));
                        on_change.call(d);
                    }
                }
            }
            if is_oor {
                span { class: "text-red-600 text-sm", "{oor_text}" }
            }
            span { class: "text-gray-500 text-xs", "{helper_text}" }
        }
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use time::{Date, Month};

    fn d(year: i32, month: Month, day: u8) -> Date {
        Date::from_calendar_date(year, month, day).unwrap()
    }

    #[test]
    fn is_valid_fiscal_year_date_current_year() {
        let today = d(2026, Month::June, 15);
        assert!(is_valid_fiscal_year_date(d(2026, Month::August, 1), today));
        assert!(is_valid_fiscal_year_date(d(2026, Month::January, 1), today));
        assert!(is_valid_fiscal_year_date(d(2026, Month::December, 31), today));
    }

    #[test]
    fn is_valid_fiscal_year_date_next_year() {
        let today = d(2026, Month::June, 15);
        assert!(is_valid_fiscal_year_date(d(2027, Month::March, 1), today));
        assert!(is_valid_fiscal_year_date(d(2027, Month::December, 31), today));
    }

    #[test]
    fn is_valid_fiscal_year_date_prev_year_rejected() {
        let today = d(2026, Month::June, 15);
        assert!(!is_valid_fiscal_year_date(d(2025, Month::December, 31), today));
        assert!(!is_valid_fiscal_year_date(d(2025, Month::January, 1), today));
    }

    #[test]
    fn is_valid_fiscal_year_date_year_after_next_rejected() {
        let today = d(2026, Month::June, 15);
        assert!(!is_valid_fiscal_year_date(d(2028, Month::January, 1), today));
        assert!(!is_valid_fiscal_year_date(d(2030, Month::June, 15), today));
    }

    #[test]
    fn parse_date_input_round_trip() {
        let d_in = d(2026, Month::June, 15);
        let s = format_date_input(&d_in);
        assert_eq!(s, "2026-06-15");
        let d_out = parse_date_input(&s).unwrap();
        assert_eq!(d_out, d_in);
    }

    #[test]
    fn parse_date_input_rejects_garbage() {
        assert!(parse_date_input("").is_none());
        assert!(parse_date_input("not-a-date").is_none());
        assert!(parse_date_input("2026-13-01").is_none());
    }
}
```
  </action>
  <verify>
    <automated>cd genossi-frontend &amp;&amp; cargo check 2>&amp;1 | tail -10 &amp;&amp; cargo test --lib component::fiscal_year_date_input 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Datei `genossi-frontend/src/component/fiscal_year_date_input.rs` existiert
    - `grep -c "pub fn FiscalYearDateInput" genossi-frontend/src/component/fiscal_year_date_input.rs` == 1
    - `grep -c "pub fn is_valid_fiscal_year_date" genossi-frontend/src/component/fiscal_year_date_input.rs` == 1
    - `grep -c "fn is_valid_fiscal_year_date_current_year" genossi-frontend/src/component/fiscal_year_date_input.rs` == 1
    - `grep -c "fn is_valid_fiscal_year_date_next_year" genossi-frontend/src/component/fiscal_year_date_input.rs` == 1
    - `grep -c "fn is_valid_fiscal_year_date_prev_year_rejected" genossi-frontend/src/component/fiscal_year_date_input.rs` == 1
    - `grep -c "fn is_valid_fiscal_year_date_year_after_next_rejected" genossi-frontend/src/component/fiscal_year_date_input.rs` == 1
    - `grep -c "r#type: \"date\"" genossi-frontend/src/component/fiscal_year_date_input.rs` &gt;= 1
    - `cd genossi-frontend &amp;&amp; cargo test --lib component::fiscal_year_date_input` zeigt mindestens 6 passed (4 + 2 parse-Tests)
  </acceptance_criteria>
  <done>FiscalYearDateInput-Component implementiert; Pure-Helper `is_valid_fiscal_year_date` mit 4 Edge-Case-Tests gruen; native &lt;input type="date"&gt; mit min/max-Attributen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Phase-18 i18n-Keys in mod.rs + DE + EN Translations + DE/EN-Symmetrie-Test</name>
  <read_first>
    - genossi-frontend/src/i18n/mod.rs (komplett — Key enum 46-706, plus I18n impl 712-788)
    - genossi-frontend/src/i18n/de.rs (Match-Arm-Pattern, letzte ~40 Zeilen)
    - genossi-frontend/src/i18n/en.rs (Match-Arm-Pattern, letzte ~40 Zeilen)
    - .planning/phases/18-frontend-component-first/18-UI-SPEC.md Zeilen 119-173 (Copywriting Contract — alle DE+EN Strings)
    - .planning/phases/18-frontend-component-first/18-PATTERNS.md Section 5 (Key-Enum-Erweiterung-Pattern)
  </read_first>
  <files>genossi-frontend/src/i18n/mod.rs, genossi-frontend/src/i18n/de.rs, genossi-frontend/src/i18n/en.rs</files>
  <behavior>
    - Symmetrie-Test `phase_18_keys_have_distinct_de_en_translations` iteriert ueber ALLE Phase-18-Keys.
    - Pro Key: `lookup(Locale::De, Key::X) != lookup(Locale::En, Key::X)`.
    - Whitelist `WHITELIST_IDENTICAL_DE_EN` enthaelt absichtlich gleiche Marker (H1, H2 — sind Halbjahres-Codes mit identischer Form).
    - Test failt mit klarer Message: `panic!("Key {key:?}: DE und EN sind identisch ('{de}'); falls beabsichtigt, in WHITELIST_IDENTICAL_DE_EN ergaenzen.")` bei vertauschten/gleichen Strings.
    - Test failt mit klarer Message: `panic!("Key {key:?}: DE ist leer.")` bei leeren Strings.
  </behavior>
  <action>

### 2a) `genossi-frontend/src/i18n/mod.rs` — Key-Enum erweitern

Direkt VOR dem schliessenden `}` des `pub enum Key { ... }` Blocks folgende Variantenliste einfuegen (Reihenfolge wie UI-SPEC Copywriting Contract):

```rust
    // ─── Phase 18 ─── MembershipAdjustModal + FiscalYearDateInput ────
    /// Button-Label auf der Member-Detail-Page.
    MembershipAdjustButtonLabel,
    /// Modal-Titel (top-level).
    MembershipAdjustModalTitle,
    /// Sub-Choice-Frage ueber den 4 flat Buttons.
    MembershipAdjustSubChoiceQuestion,
    /// Sub-Choice-Button-Labels + Descriptions.
    MembershipAdjustSubChoiceCancel,
    MembershipAdjustSubChoiceCancelDesc,
    MembershipAdjustSubChoicePartialRepayment,
    MembershipAdjustSubChoicePartialRepaymentDesc,
    MembershipAdjustSubChoiceTransfer,
    MembershipAdjustSubChoiceTransferDesc,
    MembershipAdjustSubChoiceUpgrade,
    MembershipAdjustSubChoiceUpgradeDesc,
    /// Sub-View-Header + globale Buttons.
    MembershipAdjustBack,
    MembershipAdjustCancelButton,
    MembershipAdjustPreviewLabel,
    /// Cancel Sub-View.
    MembershipAdjustCancelTitle,
    MembershipAdjustCancelDateLabel,
    /// Format-Args: {name}, {shares}, {effective_date}, {half_year}, {fiscal_year}.
    MembershipAdjustCancelPreview,
    MembershipAdjustHalfYearH1,
    MembershipAdjustHalfYearH2,
    MembershipAdjustCancelSubmit,
    MembershipAdjustCancelSuccess,
    /// Partial-Repayment Sub-View.
    MembershipAdjustPartialRepaymentTitle,
    MembershipAdjustPartialRepaymentDateLabel,
    MembershipAdjustPartialRepaymentSharesLabel,
    /// Format-Args: {name}, {current_shares}, {new_shares}, {effective_date}, {fiscal_year}.
    MembershipAdjustPartialRepaymentPreview,
    /// Format-Args: {fiscal_year}.
    MembershipAdjustPartialRepaymentAutoCreateHint,
    MembershipAdjustPartialRepaymentSubmit,
    MembershipAdjustPartialRepaymentSuccess,
    /// Format-Args: {fiscal_year}.
    MembershipAdjustPartialRepaymentSuccessAutoCreate,
    /// Transfer Sub-View.
    MembershipAdjustTransferTitle,
    MembershipAdjustTransferDateLabel,
    MembershipAdjustTransferSharesLabel,
    MembershipAdjustTransferRecipientLabel,
    MembershipAdjustTransferRecipientLoadError,
    /// Format-Args: {from_name}, {from_shares}, {from_new}, {to_name}, {to_shares}, {to_new}, {transfer_date}.
    MembershipAdjustTransferPreview,
    /// Format-Args: {from_name}, {transfer_date}.
    MembershipAdjustTransferFullExitWarning,
    MembershipAdjustTransferSubmit,
    MembershipAdjustTransferSuccess,
    /// Upgrade Sub-View.
    MembershipAdjustUpgradeTitle,
    MembershipAdjustUpgradeDateLabel,
    MembershipAdjustUpgradeSharesLabel,
    /// Format-Args: {name}, {current_shares}, {new_shares}, {date}.
    MembershipAdjustUpgradePreview,
    MembershipAdjustUpgradeSubmit,
    MembershipAdjustUpgradeSuccess,
    /// Loading + Empty + Validation.
    MembershipAdjustLoading,
    MembershipAdjustNoRecipients,
    MembershipAdjustSharesMustBePositive,
    MembershipAdjustPartialRepaymentSharesExceed,
    MembershipAdjustTransferSelfError,
    /// Generic Success-Toast wenn nicht-spezifischer Op (Fallback fuer Page-Integration).
    MembershipAdjustSuccess,
    /// FiscalYearDateInput Component-Keys.
    /// Format-Args: {min_year}, {max_year}.
    FiscalYearDateInputHelper,
    FiscalYearDateOutOfRange,
```

**46 neue Varianten** insgesamt. Ueberprueft: keine Duplikate mit existing Key-Varianten (alle prefix `MembershipAdjust` oder `FiscalYearDate` — eindeutig fuer Phase 18).

### 2b) `genossi-frontend/src/i18n/de.rs` — DE-Translations

Am ENDE des `match key { ... }`-Blocks (VOR dem schliessenden `}`) folgende Arms hinzufuegen. **Exakte Strings aus UI-SPEC Zeile 123-173 — DE-Spalte.**

```rust
        // ─── Phase 18 ─── MembershipAdjustModal + FiscalYearDateInput ────
        Key::MembershipAdjustButtonLabel => "Mitgliedschaft anpassen".into(),
        Key::MembershipAdjustModalTitle => "Mitgliedschaft anpassen".into(),
        Key::MembershipAdjustSubChoiceQuestion => "Welche Anpassung möchtest du vornehmen?".into(),
        Key::MembershipAdjustSubChoiceCancel => "Kündigung".into(),
        Key::MembershipAdjustSubChoiceCancelDesc => "Mitgliedschaft beenden — Stichtag automatisch berechnet".into(),
        Key::MembershipAdjustSubChoicePartialRepayment => "Teil-Rückgabe".into(),
        Key::MembershipAdjustSubChoicePartialRepaymentDesc => "Anteile zurückgeben — Mitgliedschaft bleibt aktiv".into(),
        Key::MembershipAdjustSubChoiceTransfer => "Übertrag".into(),
        Key::MembershipAdjustSubChoiceTransferDesc => "Anteile an ein anderes Mitglied übertragen".into(),
        Key::MembershipAdjustSubChoiceUpgrade => "Aufstocken".into(),
        Key::MembershipAdjustSubChoiceUpgradeDesc => "Mitglied zeichnet zusätzliche Anteile".into(),
        Key::MembershipAdjustBack => "← Zurück zur Auswahl".into(),
        Key::MembershipAdjustCancelButton => "Abbrechen".into(),
        Key::MembershipAdjustPreviewLabel => "VORSCHAU".into(),
        Key::MembershipAdjustCancelTitle => "Kündigung".into(),
        Key::MembershipAdjustCancelDateLabel => "Willensbekundungs-Datum".into(),
        Key::MembershipAdjustCancelPreview => "{name}: {shares} Anteile (unverändert) · Stichtag: {effective_date} ({half_year}) · Auszahlung in Phase FY{fiscal_year}".into(),
        Key::MembershipAdjustHalfYearH1 => "H1".into(),
        Key::MembershipAdjustHalfYearH2 => "H2".into(),
        Key::MembershipAdjustCancelSubmit => "Kündigung auslösen".into(),
        Key::MembershipAdjustCancelSuccess => "Kündigung wurde ausgelöst.".into(),
        Key::MembershipAdjustPartialRepaymentTitle => "Teil-Rückgabe".into(),
        Key::MembershipAdjustPartialRepaymentDateLabel => "Willensbekundungs-Datum".into(),
        Key::MembershipAdjustPartialRepaymentSharesLabel => "Anzahl Anteile".into(),
        Key::MembershipAdjustPartialRepaymentPreview => "{name}: {current_shares} → {new_shares} Anteile (nach Auszahlung) · Stichtag: {effective_date} · Phase FY{fiscal_year}".into(),
        Key::MembershipAdjustPartialRepaymentAutoCreateHint => "⚠ Auszahlungsphase FY{fiscal_year} wird automatisch angelegt".into(),
        Key::MembershipAdjustPartialRepaymentSubmit => "Teil-Rückgabe einreichen".into(),
        Key::MembershipAdjustPartialRepaymentSuccess => "Teil-Rückgabe wurde eingetragen.".into(),
        Key::MembershipAdjustPartialRepaymentSuccessAutoCreate => "Teil-Rückgabe wurde eingetragen. Phase FY{fiscal_year} wurde automatisch angelegt.".into(),
        Key::MembershipAdjustTransferTitle => "Übertrag".into(),
        Key::MembershipAdjustTransferDateLabel => "Übertrags-Datum".into(),
        Key::MembershipAdjustTransferSharesLabel => "Anzahl Anteile".into(),
        Key::MembershipAdjustTransferRecipientLabel => "Empfänger-Mitglied".into(),
        Key::MembershipAdjustTransferRecipientLoadError => "Empfänger-Liste konnte nicht geladen werden. Bitte erneut versuchen.".into(),
        Key::MembershipAdjustTransferPreview => "{from_name}: {from_shares} → {from_new} Anteile · {to_name}: {to_shares} → {to_new} Anteile · Datum: {transfer_date}".into(),
        Key::MembershipAdjustTransferFullExitWarning => "⚠ Voll-Übertrag — {from_name} tritt am {transfer_date} aus".into(),
        Key::MembershipAdjustTransferSubmit => "Übertrag ausführen".into(),
        Key::MembershipAdjustTransferSuccess => "Übertrag wurde ausgeführt.".into(),
        Key::MembershipAdjustUpgradeTitle => "Aufstockung".into(),
        Key::MembershipAdjustUpgradeDateLabel => "Willensbekundungs-Datum".into(),
        Key::MembershipAdjustUpgradeSharesLabel => "Anzahl zusätzlicher Anteile".into(),
        Key::MembershipAdjustUpgradePreview => "{name}: {current_shares} → {new_shares} Anteile · Datum: {date}".into(),
        Key::MembershipAdjustUpgradeSubmit => "Anteile aufstocken".into(),
        Key::MembershipAdjustUpgradeSuccess => "Aufstockung wurde eingetragen.".into(),
        Key::MembershipAdjustLoading => "Lädt …".into(),
        Key::MembershipAdjustNoRecipients => "Keine aktiven Empfänger-Mitglieder gefunden.".into(),
        Key::MembershipAdjustSharesMustBePositive => "Anzahl Anteile muss größer als 0 sein.".into(),
        Key::MembershipAdjustPartialRepaymentSharesExceed => "Du kannst nicht mehr Anteile zurückgeben als das Mitglied besitzt. Für Voll-Rückgabe nutze „Kündigung".".into(),
        Key::MembershipAdjustTransferSelfError => "Empfänger muss ein anderes Mitglied sein.".into(),
        Key::MembershipAdjustSuccess => "Anpassung wurde erfolgreich gespeichert.".into(),
        Key::FiscalYearDateInputHelper => "Erlaubt: GJ {min_year} oder {max_year}".into(),
        Key::FiscalYearDateOutOfRange => "Datum liegt außerhalb des erlaubten Geschäftsjahres-Bereichs.".into(),
```

### 2c) `genossi-frontend/src/i18n/en.rs` — EN-Translations

Am Ende des `match key`-Blocks (gleich wie de.rs Struktur) folgende Arms hinzufuegen. **Exakte Strings aus UI-SPEC Zeile 123-173 — EN-Spalte.**

```rust
        // ─── Phase 18 ─── MembershipAdjustModal + FiscalYearDateInput ────
        Key::MembershipAdjustButtonLabel => "Adjust membership".into(),
        Key::MembershipAdjustModalTitle => "Adjust membership".into(),
        Key::MembershipAdjustSubChoiceQuestion => "Which adjustment do you want to make?".into(),
        Key::MembershipAdjustSubChoiceCancel => "Cancellation".into(),
        Key::MembershipAdjustSubChoiceCancelDesc => "End the membership — effective date is calculated automatically".into(),
        Key::MembershipAdjustSubChoicePartialRepayment => "Partial repayment".into(),
        Key::MembershipAdjustSubChoicePartialRepaymentDesc => "Return shares — membership stays active".into(),
        Key::MembershipAdjustSubChoiceTransfer => "Transfer".into(),
        Key::MembershipAdjustSubChoiceTransferDesc => "Transfer shares to another member".into(),
        Key::MembershipAdjustSubChoiceUpgrade => "Increase shares".into(),
        Key::MembershipAdjustSubChoiceUpgradeDesc => "Member subscribes additional shares".into(),
        Key::MembershipAdjustBack => "← Back to selection".into(),
        Key::MembershipAdjustCancelButton => "Cancel".into(),
        Key::MembershipAdjustPreviewLabel => "PREVIEW".into(),
        Key::MembershipAdjustCancelTitle => "Cancellation".into(),
        Key::MembershipAdjustCancelDateLabel => "Date of intent declaration".into(),
        Key::MembershipAdjustCancelPreview => "{name}: {shares} shares (unchanged) · Effective: {effective_date} ({half_year}) · Payout in phase FY{fiscal_year}".into(),
        Key::MembershipAdjustHalfYearH1 => "H1".into(),
        Key::MembershipAdjustHalfYearH2 => "H2".into(),
        Key::MembershipAdjustCancelSubmit => "Trigger cancellation".into(),
        Key::MembershipAdjustCancelSuccess => "Cancellation triggered.".into(),
        Key::MembershipAdjustPartialRepaymentTitle => "Partial repayment".into(),
        Key::MembershipAdjustPartialRepaymentDateLabel => "Date of intent declaration".into(),
        Key::MembershipAdjustPartialRepaymentSharesLabel => "Number of shares".into(),
        Key::MembershipAdjustPartialRepaymentPreview => "{name}: {current_shares} → {new_shares} shares (after payout) · Effective: {effective_date} · Phase FY{fiscal_year}".into(),
        Key::MembershipAdjustPartialRepaymentAutoCreateHint => "⚠ Payout phase FY{fiscal_year} will be auto-created".into(),
        Key::MembershipAdjustPartialRepaymentSubmit => "Submit partial repayment".into(),
        Key::MembershipAdjustPartialRepaymentSuccess => "Partial repayment recorded.".into(),
        Key::MembershipAdjustPartialRepaymentSuccessAutoCreate => "Partial repayment recorded. Phase FY{fiscal_year} was auto-created.".into(),
        Key::MembershipAdjustTransferTitle => "Transfer".into(),
        Key::MembershipAdjustTransferDateLabel => "Transfer date".into(),
        Key::MembershipAdjustTransferSharesLabel => "Number of shares".into(),
        Key::MembershipAdjustTransferRecipientLabel => "Recipient member".into(),
        Key::MembershipAdjustTransferRecipientLoadError => "Could not load recipient list. Please retry.".into(),
        Key::MembershipAdjustTransferPreview => "{from_name}: {from_shares} → {from_new} shares · {to_name}: {to_shares} → {to_new} shares · Date: {transfer_date}".into(),
        Key::MembershipAdjustTransferFullExitWarning => "⚠ Full transfer — {from_name} will exit on {transfer_date}".into(),
        Key::MembershipAdjustTransferSubmit => "Execute transfer".into(),
        Key::MembershipAdjustTransferSuccess => "Transfer executed.".into(),
        Key::MembershipAdjustUpgradeTitle => "Increase".into(),
        Key::MembershipAdjustUpgradeDateLabel => "Date of intent declaration".into(),
        Key::MembershipAdjustUpgradeSharesLabel => "Number of additional shares".into(),
        Key::MembershipAdjustUpgradePreview => "{name}: {current_shares} → {new_shares} shares · Date: {date}".into(),
        Key::MembershipAdjustUpgradeSubmit => "Increase shares".into(),
        Key::MembershipAdjustUpgradeSuccess => "Increase recorded.".into(),
        Key::MembershipAdjustLoading => "Loading …".into(),
        Key::MembershipAdjustNoRecipients => "No active recipient members found.".into(),
        Key::MembershipAdjustSharesMustBePositive => "Number of shares must be greater than 0.".into(),
        Key::MembershipAdjustPartialRepaymentSharesExceed => "Cannot return more shares than the member holds. Use \"Cancellation\" for full repayment.".into(),
        Key::MembershipAdjustTransferSelfError => "Recipient must be a different member.".into(),
        Key::MembershipAdjustSuccess => "Adjustment saved successfully.".into(),
        Key::FiscalYearDateInputHelper => "Allowed: FY {min_year} or {max_year}".into(),
        Key::FiscalYearDateOutOfRange => "Date is outside the allowed fiscal-year range.".into(),
```

### 2d) `genossi-frontend/src/i18n/mod.rs` — Translation-Symmetrie-Test

Im bestehenden `#[cfg(test)] mod tests`-Block am Ende der Datei folgenden Test hinzufuegen (vor dem schliessenden `}` des `mod tests`):

```rust
    /// Phase 18 — Stellt sicher dass JEDER Phase-18-Key in DE und EN UNTERSCHIEDLICHE,
    /// nicht-leere Strings hat. Faengt Copy-Paste-Fehler zwischen `de.rs` und `en.rs`
    /// sowie vertauschte oder fehlende Translations.
    ///
    /// Whitelist `WHITELIST_IDENTICAL_DE_EN`: Keys, deren DE+EN absichtlich identisch sind
    /// (z.B. internationale Codes wie H1/H2).
    #[test]
    fn phase_18_keys_have_distinct_de_en_translations() {
        // Alle 46 Phase-18-Keys (siehe Key-Enum-Erweiterung).
        let phase_18_keys: &[Key] = &[
            Key::MembershipAdjustButtonLabel,
            Key::MembershipAdjustModalTitle,
            Key::MembershipAdjustSubChoiceQuestion,
            Key::MembershipAdjustSubChoiceCancel,
            Key::MembershipAdjustSubChoiceCancelDesc,
            Key::MembershipAdjustSubChoicePartialRepayment,
            Key::MembershipAdjustSubChoicePartialRepaymentDesc,
            Key::MembershipAdjustSubChoiceTransfer,
            Key::MembershipAdjustSubChoiceTransferDesc,
            Key::MembershipAdjustSubChoiceUpgrade,
            Key::MembershipAdjustSubChoiceUpgradeDesc,
            Key::MembershipAdjustBack,
            Key::MembershipAdjustCancelButton,
            Key::MembershipAdjustPreviewLabel,
            Key::MembershipAdjustCancelTitle,
            Key::MembershipAdjustCancelDateLabel,
            Key::MembershipAdjustCancelPreview,
            Key::MembershipAdjustHalfYearH1,
            Key::MembershipAdjustHalfYearH2,
            Key::MembershipAdjustCancelSubmit,
            Key::MembershipAdjustCancelSuccess,
            Key::MembershipAdjustPartialRepaymentTitle,
            Key::MembershipAdjustPartialRepaymentDateLabel,
            Key::MembershipAdjustPartialRepaymentSharesLabel,
            Key::MembershipAdjustPartialRepaymentPreview,
            Key::MembershipAdjustPartialRepaymentAutoCreateHint,
            Key::MembershipAdjustPartialRepaymentSubmit,
            Key::MembershipAdjustPartialRepaymentSuccess,
            Key::MembershipAdjustPartialRepaymentSuccessAutoCreate,
            Key::MembershipAdjustTransferTitle,
            Key::MembershipAdjustTransferDateLabel,
            Key::MembershipAdjustTransferSharesLabel,
            Key::MembershipAdjustTransferRecipientLabel,
            Key::MembershipAdjustTransferRecipientLoadError,
            Key::MembershipAdjustTransferPreview,
            Key::MembershipAdjustTransferFullExitWarning,
            Key::MembershipAdjustTransferSubmit,
            Key::MembershipAdjustTransferSuccess,
            Key::MembershipAdjustUpgradeTitle,
            Key::MembershipAdjustUpgradeDateLabel,
            Key::MembershipAdjustUpgradeSharesLabel,
            Key::MembershipAdjustUpgradePreview,
            Key::MembershipAdjustUpgradeSubmit,
            Key::MembershipAdjustUpgradeSuccess,
            Key::MembershipAdjustLoading,
            Key::MembershipAdjustNoRecipients,
            Key::MembershipAdjustSharesMustBePositive,
            Key::MembershipAdjustPartialRepaymentSharesExceed,
            Key::MembershipAdjustTransferSelfError,
            Key::MembershipAdjustSuccess,
            Key::FiscalYearDateInputHelper,
            Key::FiscalYearDateOutOfRange,
        ];

        // Whitelist: Keys, deren DE+EN absichtlich identisch sind.
        // H1/H2 sind internationale Halbjahres-Codes; gleiche Form in beiden Sprachen erwartet.
        let whitelist_identical: &[Key] = &[
            Key::MembershipAdjustHalfYearH1,
            Key::MembershipAdjustHalfYearH2,
        ];

        let de = I18n::new(Locale::De);
        let en = I18n::new(Locale::En);

        for key in phase_18_keys {
            let de_str = de.t(key.clone()).to_string();
            let en_str = en.t(key.clone()).to_string();

            assert!(
                !de_str.is_empty(),
                "Key {:?}: DE-Translation ist leer.",
                key
            );
            assert!(
                !en_str.is_empty(),
                "Key {:?}: EN-Translation ist leer.",
                key
            );

            if whitelist_identical.iter().any(|w| w == key) {
                // Whitelisted: identische Strings absichtlich.
                continue;
            }

            assert_ne!(
                de_str, en_str,
                "Key {:?}: DE und EN sind identisch ('{}'). \
                 Pruefe ob Copy-Paste-Fehler vorliegt oder ergaenze die Whitelist.",
                key, de_str
            );
        }
    }
```

### Compile-Gate

`cargo check` MUSS gruen sein. Falls Compile-Error wegen `match key` non-exhaustiv → der Executor verifiziert, dass jede neue Key-Variante in BEIDEN `de.rs` + `en.rs` einen Arm hat.
  </action>
  <verify>
    <automated>cd genossi-frontend &amp;&amp; cargo check 2>&amp;1 | tail -30 &amp;&amp; cargo test --lib i18n::tests::phase_18_keys_have_distinct_de_en_translations 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MembershipAdjustButtonLabel," genossi-frontend/src/i18n/mod.rs` == 1
    - `grep -c "MembershipAdjust" genossi-frontend/src/i18n/mod.rs` &gt;= 45
    - `grep -c "FiscalYearDateInputHelper," genossi-frontend/src/i18n/mod.rs` == 1
    - `grep -c "FiscalYearDateOutOfRange," genossi-frontend/src/i18n/mod.rs` == 1
    - `grep -c "Key::MembershipAdjust" genossi-frontend/src/i18n/de.rs` &gt;= 45
    - `grep -c "Key::MembershipAdjust" genossi-frontend/src/i18n/en.rs` &gt;= 45
    - `grep -c "Key::FiscalYearDateInputHelper" genossi-frontend/src/i18n/de.rs` == 1
    - `grep -c "Key::FiscalYearDateInputHelper" genossi-frontend/src/i18n/en.rs` == 1
    - `grep -c "Mitgliedschaft anpassen" genossi-frontend/src/i18n/de.rs` &gt;= 2
    - `grep -c "Adjust membership" genossi-frontend/src/i18n/en.rs` &gt;= 2
    - **Negative-Gate**: Datei `genossi-frontend/src/i18n/cs.rs` existiert NICHT (`test ! -f genossi-frontend/src/i18n/cs.rs`)
    - **Negative-Gate**: Locale-Enum hat KEIN `Cs`-Variant: `grep -c "Locale::Cs" genossi-frontend/src/i18n/mod.rs` == 0
    - `cd genossi-frontend &amp;&amp; cargo check` exit 0
    - **Translation-Symmetrie-Test**: `grep -c "fn phase_18_keys_have_distinct_de_en_translations" genossi-frontend/src/i18n/mod.rs` == 1
    - **Translation-Symmetrie-Test gruen**: `cd genossi-frontend &amp;&amp; cargo test --lib i18n::tests::phase_18_keys_have_distinct_de_en_translations` exit 0
    - **Key-Count-Parity**: `grep -c "Key::MembershipAdjust\|Key::FiscalYearDate" genossi-frontend/src/i18n/de.rs` ist gleich `grep -c "Key::MembershipAdjust\|Key::FiscalYearDate" genossi-frontend/src/i18n/en.rs` (beide Translations vollstaendig — `match` exhaustiveness ist Compile-Gate, dieser Check verifiziert symmetrische Erweiterung)
  </acceptance_criteria>
  <done>Alle 46 i18n-Keys in mod.rs deklariert; alle 46 in de.rs UND en.rs uebersetzt; KEIN cs.rs/Cs-Variant; cargo check gruen; Translation-Symmetrie-Test gruen (verhindert vertauschte/leere/gleiche DE-EN-Strings).</done>
</task>

<task type="auto">
  <name>Task 3: component/mod.rs Re-Exports erweitern</name>
  <read_first>
    - genossi-frontend/src/component/mod.rs (komplett — Section-Header-Pattern, insbes. Zeilen 60-130)
    - .planning/phases/18-frontend-component-first/18-PATTERNS.md Section 8 (Re-Export-Pattern)
  </read_first>
  <files>genossi-frontend/src/component/mod.rs</files>
  <action>
Am ENDE der Datei (nach den letzten `pub use` Re-Exports) folgenden Block anhaengen:

```rust
// ─── Phase 18 ─── FiscalYearDateInput + Toast-Erweiterungen ────
pub mod fiscal_year_date_input;
pub use fiscal_year_date_input::{FiscalYearDateInput, is_valid_fiscal_year_date};

// Phase 18 — Toast-Erweiterungen aus Plan 02 (toast.rs) re-exportieren.
// `show_toast` + `ToastContainer` sind bereits oben in der Phase-4-Sektion re-exportiert.
pub use toast::{show_success_toast, SuccessToastContainer, ToastVariant};
```

NOTE: `pub mod membership_adjust_modal;` und sein Re-Export werden erst in Plan 06 hinzugefuegt (Wave 2). Hier NUR `fiscal_year_date_input` und die Toast-Erweiterungen.

Wichtig: existierender `pub use toast::{show_toast, ToastContainer};` (Zeile 87 ungefaehr) BLEIBT unveraendert. Neue Re-Exports kommen ZUSAETZLICH ans File-Ende.
  </action>
  <verify>
    <automated>cd genossi-frontend &amp;&amp; cargo check 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub mod fiscal_year_date_input;" genossi-frontend/src/component/mod.rs` == 1
    - `grep -c "pub use fiscal_year_date_input::{FiscalYearDateInput, is_valid_fiscal_year_date}" genossi-frontend/src/component/mod.rs` == 1
    - `grep -c "pub use toast::{show_success_toast, SuccessToastContainer, ToastVariant}" genossi-frontend/src/component/mod.rs` == 1
    - `grep -c "pub use toast::{show_toast, ToastContainer}" genossi-frontend/src/component/mod.rs` == 1 (existing re-export unchanged)
    - `cd genossi-frontend &amp;&amp; cargo check` exit 0
  </acceptance_criteria>
  <done>FiscalYearDateInput und Toast-Erweiterungen sind von `crate::component::...` importierbar; bestehende Re-Exports unveraendert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser↔Component (FiscalYearDateInput) | Browser-native `<input type="date">` validiert min/max client-side; Frontend prueft zusaetzlich via `is_valid_fiscal_year_date`; Backend ist Single-Source-of-Truth (Defense-in-Depth) |
| Component↔i18n | `i18n.t()` returnt statische `Rc<str>` aus DE/EN-Files — KEIN User-Input fliesst in i18n |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-04-01 | Tampering | Datepicker-Input umgeht min/max via DevTools | low | mitigate | Frontend `is_valid_fiscal_year_date` als zweite Verteidigungslinie; Backend (Phase 15 D-15-05..08) ist autoritative Pruefung — HTTP 400 bei out-of-range. |
| T-18-04-02 | Information Disclosure | i18n-Strings enthalten PII | low | accept | Alle Phase-18-Translations sind generische Templates mit `{placeholder}`-Args; Pure-Template-Strings sind kein PII-Carrier. Replacement happens at render-time. |
| T-18-04-03 | XSS via i18n placeholder | low | accept | Dioxus RSX interpoliert Text-Werte, nicht HTML. `.replace("{name}", &val)` produziert nur Strings, die als Text gerendert werden. |

Keine `high`-Severity-Threats.
</threat_model>

<verification>
- `cargo check` gruen
- FiscalYearDateInput-Component Pure-Helper-Tests gruen (6 Tests: 4 fiscal-year-validation + 2 parse-roundtrip)
- 46 i18n-Keys in mod.rs + de.rs + en.rs vollstaendig
- Translation-Symmetrie-Test gruen (faengt vertauschte/leere/gleiche DE-EN-Strings)
- Re-Exports in component/mod.rs vorhanden
</verification>

<success_criteria>
- FiscalYearDateInput component nutzbar via `crate::component::FiscalYearDateInput`
- Alle Phase-18 i18n-Keys verfuegbar via `Key::MembershipAdjust...` und `Key::FiscalYearDate...`
- Translation-Symmetrie-Test verhindert DE/EN-Drift
- ToastVariant + show_success_toast + SuccessToastContainer (aus Plan 02) re-exportiert
- KEIN cs.rs, KEIN Locale::Cs (HARTER Constraint)
</success_criteria>

<output>
After completion, create `.planning/phases/18-frontend-component-first/18-04-SUMMARY.md` with:
- Liste der 46 i18n-Keys (gruppiert nach Sub-View)
- Test-Output von `cargo test --lib component::fiscal_year_date_input` und Translation-Symmetrie-Test
- Verifikation Negative-Gates (kein cs.rs, kein Cs-Variant)
- Hinweis: SC-2 "default today()" wird in Plan 06 implementiert (Modal-Body initialisiert `use_signal(|| Some(today))`)
- Bestaetigung: Plan 06 (Modal) kann alle Keys + Component + Toast-Helpers nutzen
</output>
