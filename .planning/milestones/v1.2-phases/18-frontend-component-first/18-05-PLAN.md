---
phase: 18-frontend-component-first
plan: 05
type: execute
wave: 1
depends_on: ["18-01"]
files_modified:
  - genossi-frontend/src/api.rs
autonomous: true
requirements:
  - UI-02
  - CANC-06
requirements_addressed:
  - UI-02
  - CANC-06
tags:
  - frontend
  - api-client

must_haves:
  truths:
    - "`genossi-frontend/src/api.rs` enthaelt 5 neue Funktionen: `cancel_membership`, `increase_shares`, `partial_repayment`, `transfer_shares`, `get_transfer_recipients`."
    - "Alle 5 API-Funktionen folgen dem bestehenden Pattern (`format!`+`config.backend`+`reqwest::Client`+`check_response`)."
    - "Alle 5 API-Funktionen propagieren `AppError` (kein `unwrap` auf Network/JSON)."
    - "URL-Builder-Tests verifizieren die 5 URL-Pfade (`/api/members/{id}/cancel`, `/increase-shares`, `/partial-repayment`, `/transfer-shares`, `/transfer-recipients?exclude_self={id}`) ohne Mock-Server."
  falsehoods:
    - "API-Funktionen nutzen `unwrap()` auf Network-Errors — muss `AppError` propagieren (Pattern aus `update_member`)."
    - "URL-Pfade enthalten Typos (z.B. `cancle` statt `cancel`) — URL-Builder-Tests faengen das ab."
  artifacts:
    - path: "genossi-frontend/src/api.rs"
      provides: "5 neue API-Client-Funktionen + 5 URL-Builder-Tests"
      contains: "pub async fn cancel_membership"
  key_links:
    - from: "genossi-frontend/src/api.rs"
      to: "genossi-frontend/src/component/membership_adjust_modal.rs (Plan 06)"
      via: "api::cancel_membership / increase_shares / partial_repayment / transfer_shares / get_transfer_recipients"
      pattern: "api::(cancel_membership|increase_shares|partial_repayment|transfer_shares|get_transfer_recipients)"
---

<objective>
5 neue API-Client-Funktionen in `genossi-frontend/src/api.rs` als Foundation fuer das `MembershipAdjustModal` aus Plan 06.

Purpose: Plan 06 (Modal) braucht die API-Funktionen zum Aufruf der Phase-15/16/17-Backend-Endpoints. Plan 05 ist bewusst klein (ein Task, ~100 LOC + 5 URL-Builder-Tests) und parallelisierbar mit Plan 01-04 in Wave 1 (depends_on nur auf 18-01 fuer die DTOs).
Output: ~100 LOC API-Erweiterung + 5 URL-Builder-Tests.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-frontend-component-first/18-CONTEXT.md
@.planning/phases/18-frontend-component-first/18-PATTERNS.md
@.planning/phases/18-frontend-component-first/18-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (rest-types DTOs): -->
```rust
use rest_types::{
    MemberTO, MemberSlimTO, MemberActionTO, SalutationTO,
    CancelMembershipRequestTO, IncreaseSharesRequestTO, MembershipAdjustResponseTO,
    PartialRepaymentRequestTO, PartialRepaymentResponseTO,
    TransferSharesRequestTO, TransferSharesResponseTO,
};
```

<!-- Existing AppError + Config + reqwest pattern (api.rs:14-29, 122, 188-225): -->
```rust
#[derive(Debug, Clone, PartialEq)]
pub struct AppError { pub status: Option<u16>, pub message: String, pub detail: Option<String> }
pub async fn check_response(response: reqwest::Response) -> Result<reqwest::Response, AppError>;
pub async fn update_member(config: &Config, member: MemberTO) -> Result<MemberTO, AppError> {
    let url = format!("{}/api/members/{}", config.backend, member.id.unwrap());
    let response = reqwest::Client::new().put(url).json(&member).send().await?;
    let response = check_response(response).await?;
    Ok(response.json().await?)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 5 API-Client-Funktionen + 5 URL-Builder-Tests in api.rs hinzufuegen</name>
  <read_first>
    - genossi-frontend/src/api.rs (komplett lesen — Imports + AppError + check_response + bestehende `update_member`/`create_member` Funktionen als Pattern)
    - .planning/phases/18-frontend-component-first/18-PATTERNS.md Section 3 (api.rs Pattern + alle 5 Funktions-Skelette)
    - genossi-frontend/rest-types/src/lib.rs (verifizieren dass alle Plan-01-DTOs vorhanden sind)
  </read_first>
  <files>genossi-frontend/src/api.rs</files>
  <behavior>
    - `cancel_membership(config, member_id, willensbekundung_date)` POSTet `CancelMembershipRequestTO` an `/api/members/{id}/cancel` und returned `MembershipAdjustResponseTO`.
    - `increase_shares(config, member_id, shares, willensbekundung_date)` POSTet `IncreaseSharesRequestTO` an `/api/members/{id}/increase-shares`.
    - `partial_repayment(config, member_id, shares, willensbekundung_date)` POSTet `PartialRepaymentRequestTO` an `/api/members/{id}/partial-repayment` und returned `PartialRepaymentResponseTO`.
    - `transfer_shares(config, from_id, to_member_id, shares, transfer_date)` POSTet `TransferSharesRequestTO` an `/api/members/{from_id}/transfer-shares`.
    - `get_transfer_recipients(config, exclude_self)` GETtet `/api/members/transfer-recipients?exclude_self={uuid}` und returned `Vec<MemberSlimTO>`.
    - Alle 5 Funktionen propagieren `AppError` (kein `unwrap` auf Network/JSON).
    - URL-Builder-Tests: pro Funktion ein `#[test]`, der das `format!(...)`-Template mit fixen Werten (`base="http://x"`, `id="00000000-0000-0000-0000-000000000001"`) baut und das Ergebnis exakt vergleicht. Kein Mock-Server, kein async, keine Netzwerk-Aufrufe.
  </behavior>
  <action>
1. **Imports erweitern** (ungefaehr Zeile 4-6):

```rust
use rest_types::{
    MemberActionTO, MemberDocumentTO, MemberTO, MigrationStatusTO, UserTO, ValidationResultTO,
    // Phase 18:
    MemberSlimTO,
    CancelMembershipRequestTO, IncreaseSharesRequestTO, MembershipAdjustResponseTO,
    PartialRepaymentRequestTO, PartialRepaymentResponseTO,
    TransferSharesRequestTO, TransferSharesResponseTO,
};
```

2. **Am ENDE der Datei (vor dem `#[cfg(test)] mod tests`-Block, falls vorhanden)** folgenden Block anhaengen:

```rust
// ─── Phase 18 ─── Membership-Adjust API-Client-Funktionen ────

/// Phase 18 / Phase 15 D-15-11 — POST /api/members/{id}/cancel
/// Cancels membership; returns the new MemberAction (Austritt) + updated Member.
pub async fn cancel_membership(
    config: &Config,
    member_id: Uuid,
    willensbekundung_date: time::Date,
) -> Result<MembershipAdjustResponseTO, AppError> {
    info!("Cancelling membership {member_id}");
    let url = format!("{}/api/members/{member_id}/cancel", config.backend);
    let body = CancelMembershipRequestTO { willensbekundung_date };
    let response = reqwest::Client::new().post(url).json(&body).send().await?;
    let response = check_response(response).await?;
    Ok(response.json().await?)
}

/// Phase 18 / Phase 15 D-15-15 — POST /api/members/{id}/increase-shares
pub async fn increase_shares(
    config: &Config,
    member_id: Uuid,
    shares: i32,
    willensbekundung_date: time::Date,
) -> Result<MembershipAdjustResponseTO, AppError> {
    info!("Increasing shares for {member_id} by {shares}");
    let url = format!("{}/api/members/{member_id}/increase-shares", config.backend);
    let body = IncreaseSharesRequestTO { willensbekundung_date, shares };
    let response = reqwest::Client::new().post(url).json(&body).send().await?;
    let response = check_response(response).await?;
    Ok(response.json().await?)
}

/// Phase 18 / Phase 16 D-16-16 — POST /api/members/{id}/partial-repayment
/// Returns entry + updated Member + optionally auto-created Phase.
pub async fn partial_repayment(
    config: &Config,
    member_id: Uuid,
    shares: i32,
    willensbekundung_date: time::Date,
) -> Result<PartialRepaymentResponseTO, AppError> {
    info!("Partial repayment for {member_id}: {shares} shares");
    let url = format!("{}/api/members/{member_id}/partial-repayment", config.backend);
    let body = PartialRepaymentRequestTO { willensbekundung_date, shares };
    let response = reqwest::Client::new().post(url).json(&body).send().await?;
    let response = check_response(response).await?;
    Ok(response.json().await?)
}

/// Phase 18 / Phase 17 C-17-CF-07 — POST /api/members/{from_id}/transfer-shares
/// Atomically creates UebertragungAbgabe + UebertragungEmpfang (+ optional Austritt for full transfer).
pub async fn transfer_shares(
    config: &Config,
    from_id: Uuid,
    to_member_id: Uuid,
    shares: i32,
    transfer_date: time::Date,
) -> Result<TransferSharesResponseTO, AppError> {
    info!("Transferring {shares} shares from {from_id} to {to_member_id}");
    let url = format!("{}/api/members/{from_id}/transfer-shares", config.backend);
    let body = TransferSharesRequestTO { to_member_id, shares, transfer_date };
    let response = reqwest::Client::new().post(url).json(&body).send().await?;
    let response = check_response(response).await?;
    Ok(response.json().await?)
}

/// Phase 18 / Phase 14 D-14-12 — GET /api/members/transfer-recipients?exclude_self={uuid}
/// Returns DSGVO-konformer slim list of active members (exit_date IS NULL),
/// excluding the requested `from`-member.
pub async fn get_transfer_recipients(
    config: &Config,
    exclude_self: Uuid,
) -> Result<Vec<MemberSlimTO>, AppError> {
    info!("Fetching transfer recipients (exclude_self={exclude_self})");
    let url = format!(
        "{}/api/members/transfer-recipients?exclude_self={exclude_self}",
        config.backend
    );
    let response = reqwest::Client::new().get(url).send().await?;
    let response = check_response(response).await?;
    Ok(response.json().await?)
}
```

3. **URL-Builder-Tests** (pro neuer Funktion ein Test). Tests verifizieren die `format!(...)`-Templates ohne Netzwerk-Aufruf — faengt Pfad-Typos ab. Am ENDE der Datei, in einen neuen `#[cfg(test)] mod phase_18_api_url_tests`-Block:

```rust
#[cfg(test)]
mod phase_18_api_url_tests {
    use uuid::Uuid;

    const BASE: &str = "http://test.local";
    const ID: u128 = 0x0000_0000_0000_0000_0000_0000_0000_0001;

    #[test]
    fn cancel_membership_url_template() {
        let member_id = Uuid::from_u128(ID);
        let url = format!("{}/api/members/{member_id}/cancel", BASE);
        assert_eq!(
            url,
            "http://test.local/api/members/00000000-0000-0000-0000-000000000001/cancel"
        );
    }

    #[test]
    fn increase_shares_url_template() {
        let member_id = Uuid::from_u128(ID);
        let url = format!("{}/api/members/{member_id}/increase-shares", BASE);
        assert_eq!(
            url,
            "http://test.local/api/members/00000000-0000-0000-0000-000000000001/increase-shares"
        );
    }

    #[test]
    fn partial_repayment_url_template() {
        let member_id = Uuid::from_u128(ID);
        let url = format!("{}/api/members/{member_id}/partial-repayment", BASE);
        assert_eq!(
            url,
            "http://test.local/api/members/00000000-0000-0000-0000-000000000001/partial-repayment"
        );
    }

    #[test]
    fn transfer_shares_url_template() {
        let from_id = Uuid::from_u128(ID);
        let url = format!("{}/api/members/{from_id}/transfer-shares", BASE);
        assert_eq!(
            url,
            "http://test.local/api/members/00000000-0000-0000-0000-000000000001/transfer-shares"
        );
    }

    #[test]
    fn get_transfer_recipients_url_template() {
        let exclude_self = Uuid::from_u128(ID);
        let url = format!(
            "{}/api/members/transfer-recipients?exclude_self={exclude_self}",
            BASE
        );
        assert_eq!(
            url,
            "http://test.local/api/members/transfer-recipients?exclude_self=00000000-0000-0000-0000-000000000001"
        );
    }
}
```

Diese Tests sind reine String-Builder-Tests — sie verifizieren KEINE Netzwerk-Calls, sondern nur dass die URL-Templates korrekt zusammengebaut werden (Pfad-Typo-Detection). Funktionale Verifikation erfolgt in ManualUAT (Plan 07).
  </action>
  <verify>
    <automated>cd genossi-frontend &amp;&amp; cargo check 2>&amp;1 | tail -10 &amp;&amp; cargo test --lib api::phase_18_api_url_tests 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub async fn cancel_membership" genossi-frontend/src/api.rs` == 1
    - `grep -c "pub async fn increase_shares" genossi-frontend/src/api.rs` == 1
    - `grep -c "pub async fn partial_repayment" genossi-frontend/src/api.rs` == 1
    - `grep -c "pub async fn transfer_shares" genossi-frontend/src/api.rs` == 1
    - `grep -c "pub async fn get_transfer_recipients" genossi-frontend/src/api.rs` == 1
    - `grep -c "CancelMembershipRequestTO\|IncreaseSharesRequestTO\|PartialRepaymentRequestTO\|TransferSharesRequestTO\|MembershipAdjustResponseTO\|PartialRepaymentResponseTO\|TransferSharesResponseTO\|MemberSlimTO" genossi-frontend/src/api.rs` &gt;= 8
    - `grep -c "fn cancel_membership_url_template\|fn increase_shares_url_template\|fn partial_repayment_url_template\|fn transfer_shares_url_template\|fn get_transfer_recipients_url_template" genossi-frontend/src/api.rs` == 5
    - `cd genossi-frontend &amp;&amp; cargo check` exit 0
    - `cd genossi-frontend &amp;&amp; cargo test --lib api::phase_18_api_url_tests` zeigt `5 passed`
  </acceptance_criteria>
  <done>5 API-Funktionen typecheck-clean; Pattern-konsistent mit existierenden update_member/create_member; alle Plan-01-DTOs importiert; 5 URL-Builder-Tests gruen (faengt Pfad-Typos).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend↔Backend HTTP | JSON-Bodies via `reqwest::Client` zu Vorstand-only Backend-Endpoints (ADMIN_PRIVILEGE-Funnel aus Phase 15 D-15-01) |
| WASM↔reqwest | AppError-Konvertierung via `impl From<reqwest::Error> for AppError` (api.rs:39-71) — keine `unwrap()` auf Network-Errors |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-05-01 | Tampering | URL-Pfad-Typo (`cancle` statt `cancel`) wuerde leise 404 zurueckliefern | low | mitigate | URL-Builder-Tests verifizieren exakte Pfad-Templates; CI bricht ab bei Drift. |
| T-18-05-02 | Information Disclosure | exclude_self-UUID im Query-String wird im Browser-History gespeichert | low | accept | UUIDs sind nicht-PII; Member-IDs sind ohnehin in Vorstand-Workflows sichtbar. |
| T-18-05-03 | Spoofing | Privilege-Escalation via API-Direct-Call | low | accept | Backend-Endpoints sind ADMIN_PRIVILEGE-protected (Phase 15 D-15-01); ohne valid Admin-Session HTTP 401/403. |

Keine `high`-Severity-Threats.
</threat_model>

<verification>
- `cargo check` gruen ohne Warnings
- 5 URL-Builder-Tests gruen
- Pattern-konsistent mit `update_member`/`create_member` (kein `unwrap()` auf Network)
</verification>

<success_criteria>
- 5 API-Funktionen in api.rs
- 5 URL-Builder-Tests gruen
- Plan 06 (Modal) kann jetzt importieren
</success_criteria>

<output>
After completion, create `.planning/phases/18-frontend-component-first/18-05-SUMMARY.md` with:
- Liste der 5 hinzugefuegten API-Funktionen mit Signatur
- Test-Output von `cargo test --lib api::phase_18_api_url_tests` (5 passed)
- Bestaetigung: Plan 06 (Modal) kann jetzt alle 5 Funktionen importieren
</output>
