---
phase: 14-dao-domain-foundation
plan: 03
subsystem: service-layer-trait-extension
tags:
  - service
  - member
  - transfer
  - trsf-06
  - tdd
dependency-graph:
  requires:
    - genossi_service::permission::ADMIN_PRIVILEGE (existing const)
    - genossi_dao::member::MemberDao::all (existing default-impl, filters deleted IS NULL)
    - genossi_service_impl::MemberServiceImpl gen_service_impl! macro (existing DI wiring)
  provides:
    - MemberService::list_transfer_recipients trait method (admin-gated, filters exit_date + self)
    - MemberServiceImpl::list_transfer_recipients impl
    - 3 unit tests for the new method
  affects:
    - Phase 14 Plan 04 (REST endpoint wraps this service method)
    - genossi_service::MockMemberService (auto-regenerated by automock)
tech-stack:
  added: []
  patterns:
    - permission-funnel (use_transaction -> check_permission -> DAO -> commit)
    - explicit .expect_all() to defuse mockall default-impl override
    - admin-gate witness via .withf(|priv_, _| priv_ == "admin")
key-files:
  created: []
  modified:
    - genossi_service/src/member.rs (+13 lines: trait method signature + doc comment)
    - genossi_service_impl/src/member.rs (+440 lines: 1 import + 22-line impl + 418-line test module)
decisions:
  - "Import ADMIN_PRIVILEGE from genossi_service::permission instead of re-declaring locally (Open-Question 1 -> Import)"
  - "Permission funnel order: use_transaction -> check_permission(ADMIN_PRIVILEGE) -> member_dao.all -> commit (Pattern from repayment_phase.rs:99-108)"
  - "Filter combines two predicates: e.exit_date.is_none() && e.id != exclude_member_id; soft-delete filter inherited from member_dao.all() default-impl"
  - "Local mock! blocks bound to TestTransaction (Debug bound for gen_service_impl!) — same approach as assembly.rs::tests"
metrics:
  duration: "~13 minutes (commit timestamps)"
  completed: "2026-06-04"
  tasks-completed: 2
  files-modified: 2
  tests-added: 3
  tests-passing: 3
---

# Phase 14 Plan 03: list_transfer_recipients Service Method Summary

One-liner: Extends MemberService trait with admin-gated `list_transfer_recipients(exclude_self) -> Arc<[Member]>` filtering soft-deleted, cancelled (`exit_date IS NOT NULL`), and self members; ships with 3 mockall-based unit tests using TDD RED/GREEN cycle.

## What changed

### genossi_service/src/member.rs (+13 lines)
- Added `async fn list_transfer_recipients(exclude_member_id: Uuid, context, tx) -> Result<Arc<[Member]>, ServiceError>` to the `MemberService` trait.
- Doc-comment references TRSF-06 requirement and notes admin-only permission gate.
- Inserted directly after `get_all` (line ~117) so automock-regenerated `MockMemberService` includes the method automatically.

### genossi_service_impl/src/member.rs (+440 lines)
- Added import `use genossi_service::permission::{Authentication, PermissionService, ADMIN_PRIVILEGE};` (extending existing line).
- Implemented `list_transfer_recipients` inside `impl<Deps: MemberServiceDeps> MemberService for MemberServiceImpl<Deps>`:
  - Permission funnel order: `use_transaction` -> `check_permission(ADMIN_PRIVILEGE, context)` -> `member_dao.all()` -> `commit`.
  - Filter: `e.exit_date.is_none() && e.id != exclude_member_id` (soft-delete filter inherited from `MemberDao::all` default-impl).
  - Returns `Arc<[Member]>` via `iter().filter(...).map(Member::from).collect()`.
- Added `#[cfg(test)] mod tests` block (~418 lines) with:
  - Local `TestTransaction` (Debug) + `mock!` blocks for `TestTxDao`, `TestMemberDao`, `TestMemberActionDao`, `TestAuditLogDao`, `TestPermissionService`.
  - `TestDeps` struct implementing `MemberServiceDeps` wired to the local mocks.
  - Helpers: `setup_mock_tx_dao()`, `sample_member_entity(id, exit_date)`, `build_service(member_dao, permission_service)`.
  - 3 tests:
    1. `test_list_transfer_recipients_happy_path_filters_self` — 3 active members, exclude `m_a` -> 2 returned (m_b, m_c). Admin-gate witness via `.withf(|priv_, _ctx| priv_ == "admin")`.
    2. `test_list_transfer_recipients_all_cancelled_returns_empty` — 3 members all with `exit_date = Some(...)` -> 0 returned.
    3. `test_list_transfer_recipients_only_self_returns_empty` — 1 active member matching `exclude_self` -> 0 returned.

## ADMIN_PRIVILEGE confirmation

`ADMIN_PRIVILEGE` is imported from the canonical source `genossi_service::permission` (line 28 declares `pub const ADMIN_PRIVILEGE: &str = "admin"`), NOT re-declared locally. The grep witnesses:

- `grep -c "use genossi_service::permission::.*ADMIN_PRIVILEGE" genossi_service_impl/src/member.rs` -> 1 (import line)
- `grep -c "ADMIN_PRIVILEGE" genossi_service_impl/src/member.rs` -> 4 (1 import + 1 impl usage + 2 test references in mock setup comments)
- `grep -c "check_permission(ADMIN_PRIVILEGE" genossi_service_impl/src/member.rs` -> 1

No `const ADMIN_PRIVILEGE` declaration in the file — verified via `grep -n "const ADMIN_PRIVILEGE" genossi_service_impl/src/member.rs` returning empty.

## Test results

```
cargo test -p genossi_service_impl --lib member::tests::test_list_transfer_recipients
test member::tests::test_list_transfer_recipients_only_self_returns_empty ... ok
test member::tests::test_list_transfer_recipients_happy_path_filters_self ... ok
test member::tests::test_list_transfer_recipients_all_cancelled_returns_empty ... ok
test result: ok. 3 passed; 0 failed; 0 ignored; 0 measured; 355 filtered out
```

Regression check on the whole `member` module: `cargo test -p genossi_service_impl --lib member` -> `88 passed; 0 failed`.

**Admin-gate witness (happy-path test):**
```rust
permission_service
    .expect_check_permission()
    .withf(|priv_, _ctx| priv_ == "admin")  // <-- Regression-proof against privilege swap
    .returning(|_, _| Ok(()));
```
Confirmed via `grep -cE 'withf.*priv_ == "admin"'` -> 1.

## Mockall pitfall (Pitfall 2) — guarded

The `MemberDao::all` method has a default-impl via `dump_all` in the trait. `#[automock]` and `mock!` blocks IGNORE default-impls — the generated mock returns a default error if `.expect_all()` is not set.

**Guard applied in all 3 tests:**
```rust
member_dao
    .expect_all()
    .returning(move |_| Ok(Arc::from(entities_clone.clone())));
```

Each test explicitly calls `.expect_all()` with a closure returning a fresh `Arc<[MemberEntity]>` — confirmed via `grep -c "expect_all()" genossi_service_impl/src/member.rs` -> 3 (one per test).

## TDD gate compliance

Plan 14-03 task 2 has `tdd="true"`. Gate sequence in commit history:

1. **RED**: `06dd20b test(14-03): add failing tests for list_transfer_recipients (RED)` — added 3 tests against `todo!()`-stub impl; all 3 panicked at runtime.
2. **GREEN**: `bd98d5b feat(14-03): implement list_transfer_recipients in MemberServiceImpl (GREEN)` — replaced stub with real impl; all 3 tests pass.

No REFACTOR commit (no cleanup needed — impl is clean from the start).

## Deviations from plan

None — plan executed exactly as written, including the recommended `.withf(|priv_, _ctx| priv_ == "admin")` admin-gate witness.

The only minor adjustment was using a local `TestTransaction` + `mock!` blocks pattern (lifted from `assembly.rs::tests`) instead of `MockMemberDao` from `genossi_dao` directly. This was necessary because `MockTransaction` (from `genossi_dao`) does not implement `Debug`, but the `gen_service_impl!` macro requires `Transaction: Debug`. This is the documented workaround across the codebase and matches the plan's `<read_first>` direction to "read existing tests to see exact setup" — assembly.rs is the closest established pattern.

## Verification commands executed

- `cargo build -p genossi_service` -> success after Task 1 trait extension.
- `cargo build -p genossi_service_impl` -> fails after Task 1 (expected: missing impl).
- `cargo build -p genossi_service_impl --tests` -> success after fixing `query`/`count` AuditLogDao signature mismatches in the local mock.
- `cargo test -p genossi_service_impl --lib member::tests::test_list_transfer_recipients` -> 3 failed (RED), then 3 passed (GREEN).
- `cargo test -p genossi_service_impl --lib member` -> 88 passed, 0 failed.
- `cargo build` (full workspace) -> success.
- `rustfmt --edition 2021 --check genossi_service/src/member.rs genossi_service_impl/src/member.rs` -> clean (no output).
- `cargo clippy -p genossi_service -p genossi_service_impl --all-targets` -> 2 pre-existing warnings on `membership_adjust.rs` dead-code (from Plan 14-01), no new warnings from Plan 14-03.

## Self-Check: PASSED

- Files created/modified exist: genossi_service/src/member.rs, genossi_service_impl/src/member.rs — both verified via `[ -f ... ]`.
- Commits exist: 2b6c4c0 (Task 1 trait), 06dd20b (Task 2 RED), bd98d5b (Task 2 GREEN) — all verified via `git log`.
- Trait method signature matches `<interfaces>` block in plan exactly.
- Permission funnel order matches `repayment_phase.rs:99-108` pattern.
- All 3 acceptance-criteria grep checks pass.
- All 3 tests pass; 88 member-module tests pass, 0 regressions.

## Note for next plan

Plan 14-04 will wrap `MemberServiceImpl::list_transfer_recipients` in a REST handler at `GET /api/members/transfer-recipients?exclude_self={uuid}` with `MemberSlimTO` response, plus an E2E test. Critical reminders for Plan 14-04:

1. **Sub-route ordering** — `/transfer-recipients` MUST be declared BEFORE `/{id}` in `member::generate_route()` (Pitfall 1).
2. **PermissionDenied -> 401, not 403** — Utoipa annotation must list `401`, not `403` (Pitfall 4).
3. **E2E exit_date setup** — Use 3-step pattern (POST member -> POST Austritt action -> GET member) to make `recalc_dates` set `exit_date` (Pitfall 3). Direct `exit_date` in MemberTO body is ignored.
4. **MemberSlimTO** — New TO in `genossi_rest_types`, no `impl From<&MemberTO>` (PII guard), 6 fields only.
