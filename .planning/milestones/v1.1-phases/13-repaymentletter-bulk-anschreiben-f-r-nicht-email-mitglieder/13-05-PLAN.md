---
phase: 13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder
plan: 05
type: execute
wave: 4
depends_on: [04]
files_modified:
  - genossi_rest/src/repayment_letter.rs
  - genossi_rest/src/lib.rs
  - genossi_bin/src/lib.rs
autonomous: true
requirements: [BRIEF-01]
tags: [phase-13, rest-handler, openapi, di-wiring, direct-download]

must_haves:
  truths:
    - "POST /api/repayment-phase/{phase_id}/letters/generate Endpoint existiert und antwortet mit application/pdf"
    - "Response hat Content-Disposition: attachment; filename=\"auszahlungs_anschreiben_GJ_{fy}.pdf\""
    - "Response hat zusaetzlichen Header X-Document-Count: N mit Anzahl der eindeutigen Members nach Aggregation (D-13-04 — fuer Frontend-Toast-Pluralisierung)"
    - "Body-Schema GenerateLettersRequest { entry_ids: Vec<Uuid> } ist als ToSchema fuer OpenAPI definiert"
    - "OpenAPI dokumentiert alle 6 Status-Codes: 200, 400 (validation/mismatch), 401, 403 (helper), 404 (phase), 409 (phase_not_active)"
    - "Lokales map_letter_error mapped ServiceError::PermissionDenied → 403 Forbidden (Phase 11 D-11 Pattern)"
    - "Mount unter /api/repayment-phase via Router::nest (Axum-konsistent mit Phase 11)"
    - "RepaymentLetterRestState-Trait ist als RestStateImpl-Bound AN MINDESTENS 2 STELLEN in genossi_rest/src/lib.rs erforderlich (analog repayment_export-Pattern)"
    - "DI-Wiring in genossi_bin/src/lib.rs RestStateImpl::new mit Single-Arc-per-Process-Pattern: KEIN NEUES Arc::new(SomeDao::new(self.pool.clone())) — nur .clone() von existierenden lokalen Arcs"
  artifacts:
    - path: "genossi_rest/src/repayment_letter.rs"
      provides: "REST-Handler + GenerateLettersRequest + RepaymentLetterRestState-Trait + ApiDoc"
      contains: "generate_letters"
    - path: "genossi_rest/src/lib.rs"
      provides: "Router-Mount + ApiDoc-Nest + Trait-Bound (an >=2 Stellen)"
      contains: "repayment_letter::"
    - path: "genossi_bin/src/lib.rs"
      provides: "DI-Wiring: RepaymentLetterServiceDependencies + RepaymentContextResolverDependencies + 2 neue Arc-Felder in RestStateImpl + ggf. document_storage-Konstruktor falls noch nicht vorhanden"
      contains: "repayment_letter_service"
  key_links:
    - from: "genossi_rest/src/repayment_letter.rs::generate_letters"
      to: "RepaymentLetterService::generate"
      via: "rest_state.repayment_letter_service().generate(...)"
      pattern: "repayment_letter_service.*generate"
    - from: "genossi_rest/src/repayment_letter.rs::generate_letters"
      to: "X-Document-Count Header"
      via: "result.document_ids.len()"
      pattern: "X-Document-Count"
    - from: "genossi_bin/src/lib.rs"
      to: "RepaymentLetterServiceImpl"
      via: "Arc::new mit allen 10 DAO-Deps via .clone()"
      pattern: "RepaymentLetterServiceImpl"
---

<objective>
REST-Layer + DI-Wiring: macht den Letter-Service ueber HTTP erreichbar. Direct-Download-Pattern aus Phase 11 1:1; OpenAPI-Dokumentation mit 6 Status-Codes; zusaetzlicher `X-Document-Count` Header fuer Frontend-Toast-Pluralisierung (D-13-04 Aggregation → Anzahl Briefe != Anzahl Entries); DI-Wiring im Binary-Layer mit Single-Arc-Sharing (Phase-7/10/11-Konsistenz).

**Wichtige Aenderungen vs. Initial-Plan:**
- Task 0 (NEU, ERSTE Aktion): Pre-Flight-Grep verifiziert, ob `document_storage`, `uuid_service`, `audit_log_dao`, `member_document_dao` bereits als lokale Arcs in `RestStateImpl::new()` existieren. Falls `document_storage` fehlt → Task 1 erweitert um Konstruktor + .clone() pro Consumer. Falls die anderen fehlen → STOP, Plan-Discretion-Konsultation
- REST-Response: zusaetzlicher `X-Document-Count: N` Header (N = `result.document_ids.len()`, Anzahl der unique Members nach Aggregation — NICHT entry_ids.len())
- Trait-Bound-Acceptance verschaerft auf >=2 (analog repayment_export hat 2 Stellen)

Purpose: D-13-02 (Direct-Download in Response, kein Persist-then-Fetch) + D-13-03 (Selektions-Body {entry_ids: [...]}) + D-13-04 (Aggregation-Count fuer Frontend-Toast).
Output:
- REST-Handler `POST /api/repayment-phase/{phase_id}/letters/generate`
- ToSchema-Request-Body + OpenAPI mit 6 Response-Codes + X-Document-Count Header
- Lokales `map_letter_error` (Phase 11 D-11 Pattern)
- DI-Wiring: 2 neue Deps-Aliases + 2 neue Arc-Felder in `RestStateImpl` + 2 neue Trait-Impls + ggf. document_storage-Konstruktor falls in Pre-Flight als fehlend identifiziert
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder/13-CONTEXT.md
@.planning/phases/13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder/13-RESEARCH.md
@.planning/phases/13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder/13-PATTERNS.md

<interfaces>
<!-- Plan 04 Output: RepaymentLetterService Trait + RepaymentLetterBundle struct (mit document_ids: Vec<Uuid>) -->

<!-- Existing Phase 11 REST handler — genossi_rest/src/repayment_export.rs:96-156 -->
```rust
pub async fn export_repayment<RestState: RestStateDef + RepaymentExportRestState>(
    rest_state: State<RestState>,
    Extension(context): Extension<Context>,
    Path((phase_id, format_str)): Path<(Uuid, String)>,
    Query(query): Query<ExportQuery>,
) -> Response {
    error_handler((async {
        let auth = extract_auth_context(Some(context))?;
        let export = rest_state.repayment_export_service()
            .export(phase_id, format, include, auth).await
            .map_err(map_export_error)?;
        let cd = http_util::content_disposition_attachment(&export.filename);
        Ok(Response::builder()
            .status(200)
            .header("Content-Type", export.content_type)
            .header("Content-Disposition", &cd)
            .body(Body::from(export.bytes)).unwrap())
    }).await)
}
```

<!-- Phase 11 RestState-Trait — genossi_rest/src/repayment_export.rs:84-90 -->
```rust
pub trait RepaymentExportRestState: Clone + Send + Sync + 'static {
    type RepaymentExportService: RepaymentExportService<Context = crate::ContextType>
        + Send + Sync + 'static;
    fn repayment_export_service(&self) -> std::sync::Arc<Self::RepaymentExportService>;
}
```

<!-- Existing Mount in genossi_rest/src/lib.rs -->
- Z. 5:   pub mod repayment_export;
- Z. 279: (path = "/api/repayment-phase/{phase_id}/export", api = repayment_export::ApiDoc),
- Z. 449: + repayment_export::RepaymentExportRestState
- Z. 654: .nest("/api/repayment-phase", repayment_export::generate_export_route::<RestState>())
- Z. 782: + repayment_export::RepaymentExportRestState (zweite Trait-Bound)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Pre-Flight Single-Arc-Gate — verifiziere existierende lokale Arcs in genossi_bin/src/lib.rs</name>
  <files>(nur Grep, kein Write)</files>
  <read_first>
    - genossi_bin/src/lib.rs:540-600 (RestStateImpl-Struct + Felder)
    - genossi_bin/src/lib.rs:800-990 (RestStateImpl::new — lokale `let`-Bindings)
  </read_first>
  <action>
    **KEIN Code-Write. Diese Task ist ein Verifikationsschritt vor Task 1.**

    Fuehre folgende Greps aus und dokumentiere die Resultate im SUMMARY:

    ```bash
    # 1. Existiert `document_storage` als lokales Arc in RestStateImpl::new()?
    rg 'let document_storage' genossi_bin/src/lib.rs

    # 2. Existieren die anderen benoetigten Arcs?
    rg 'let uuid_service' genossi_bin/src/lib.rs
    rg 'let audit_log_dao' genossi_bin/src/lib.rs
    rg 'let member_document_dao' genossi_bin/src/lib.rs
    rg 'let member_dao' genossi_bin/src/lib.rs
    rg 'let repayment_phase_dao' genossi_bin/src/lib.rs
    rg 'let repayment_entry_dao' genossi_bin/src/lib.rs
    rg 'let permission_service' genossi_bin/src/lib.rs
    rg 'let transaction_dao' genossi_bin/src/lib.rs
    rg 'let pdf_generator' genossi_bin/src/lib.rs
    rg 'let template_storage' genossi_bin/src/lib.rs

    # 3. Wie viele Konstruktoren fuer FilesystemDocumentStorage?
    rg 'Arc::new\(FilesystemDocumentStorage' genossi_bin/src/lib.rs | wc -l

    # 4. Single-Arc-Regression-Check (Phase 10 P07): wie viele Arc::new(SomeDao::new(...)) existieren?
    rg 'Arc::new\([A-Z][A-Za-z]*Dao::new' genossi_bin/src/lib.rs | wc -l
    ```

    **Entscheidungsmatrix:**

    | Pre-Flight-Resultat | Konsequenz |
    |---|---|
    | Alle 10 Arcs (incl. document_storage) existieren | → Task 1 nutzt nur `.clone()` pro Dependency, KEINE neuen Konstruktoren |
    | document_storage existiert NICHT als lokales Arc | → Task 1 fuegt EINEN `let document_storage = Arc::new(FilesystemDocumentStorage::new(...))` hinzu, an einer einzigen Stelle (vor RestStateImpl::new-Konstruktion) |
    | uuid_service/audit_log_dao/member_document_dao fehlt | → STOP, Plan-Discretion: Phase 10 P07 hat diese Arcs eingefuehrt; falls fehlend → Bug-Report, NICHT in Plan 05 zusaetzlich erzeugen |
    | `Arc::new(FilesystemDocumentStorage` > 1 | → STOP, Plan-Discretion: ein Single-Arc-per-Process-Violation — Plan-04-Wiring darf das NICHT verschlimmern |
    | `Arc::new([A-Z][A-Za-z]*Dao::new` count > existing baseline nach Plan 05 | → cargo build sauber muss bestehen + Phase 10 P07 SUMMARY-Pattern erneut konsultiert werden |

    Dokumentiere Resultat im SUMMARY mit konkreten Zeilen-Referenzen aus genossi_bin/src/lib.rs.
  </action>
  <verify>
    <automated>echo "verifikation-only, kein code-write"</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY enthaelt Pre-Flight-Resultat aller 10 Grep-Calls
    - SUMMARY nennt baseline-Zaehler `Arc::new([A-Z][A-Za-z]*Dao::new` count (z.B. "existing: 7")
    - SUMMARY dokumentiert ob document_storage als lokales Arc existiert (yes/no)
    - SUMMARY entscheidet zwischen "alle Arcs vorhanden → nur .clone()" und "document_storage muss in Task 1 angelegt werden"
  </acceptance_criteria>
  <done>
    Pre-Flight-Greps ausgefuehrt, Entscheidungsmatrix angewendet, SUMMARY enthaelt klare Anweisung fuer Task 3 (Konstruktor noetig oder nur .clone()?).
  </done>
</task>

<task type="auto">
  <name>Task 1: REST-Handler + GenerateLettersRequest + RepaymentLetterRestState + ApiDoc + Route-Generator + X-Document-Count Header</name>
  <files>genossi_rest/src/repayment_letter.rs</files>
  <read_first>
    - genossi_rest/src/repayment_export.rs (gesamte Datei — Vorbild fuer Handler, ToSchema, RestState-Trait, ApiDoc, generate_route)
    - genossi_rest/src/lib.rs:80-150 (error_handler + RestError-Conversion)
    - genossi_rest/src/lib.rs:1-50 (Imports + Common-Types)
    - genossi_service/src/repayment_letter.rs (Plan 04 Output: Trait-Header + RepaymentLetterBundle mit document_ids)
    - .planning/phases/13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder/13-RESEARCH.md §Code Examples (Handler-Skelett)
    - .planning/phases/13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder/13-PATTERNS.md §`genossi_rest/src/repayment_letter.rs`
  </read_first>
  <action>
    **Neue Datei `genossi_rest/src/repayment_letter.rs`:**

    ```rust
    //! Phase 13 D-13-02/03/04: REST-Handler fuer POST /api/repayment-phase/{phase_id}/letters/generate.
    //! Direct-Download-Pattern (Phase 11 D-13-02 1:1).
    //! X-Document-Count Header dient dem Frontend zur Pluralisierung des Erfolgs-Toasts.

    use std::sync::Arc;

    use axum::{
        body::Body,
        extract::{Path, State},
        response::Response,
        routing::post,
        Extension, Json, Router,
    };
    use serde::Deserialize;
    use tracing::instrument;
    use utoipa::{OpenApi, ToSchema};
    use uuid::Uuid;

    use genossi_service::repayment_letter::{
        RepaymentLetterBundle, RepaymentLetterService,
    };
    use genossi_service::ServiceError;

    use crate::{
        error_handler, extract_auth_context, http_util, Context, RestError, RestStateDef,
    };

    /// Phase 11 D-11 Pattern 1:1: PermissionDenied → 403 Forbidden mit Body "forbidden".
    /// Andere ServiceError-Varianten ueber globales From<ServiceError> for RestError mapping.
    fn map_letter_error(e: ServiceError) -> RestError {
        match e {
            ServiceError::PermissionDenied => RestError::Forbidden("forbidden".to_string()),
            other => other.into(),
        }
    }

    /// Request-Body fuer Bulk-Brief-Generierung.
    /// D-13-03: flache Liste von repayment_entry_id's. Server gruppiert per member_id (D-13-04).
    #[derive(Debug, Deserialize, ToSchema)]
    pub struct GenerateLettersRequest {
        /// IDs der RepaymentEntries fuer die Briefe generiert werden sollen.
        /// MUESSEN alle zur phase_id im URL-Pfad gehoeren — sonst 400 BadRequest.
        pub entry_ids: Vec<Uuid>,
    }

    /// Trait fuer den State-Provider — analog Phase 11 Pattern.
    pub trait RepaymentLetterRestState: Clone + Send + Sync + 'static {
        type RepaymentLetterService: RepaymentLetterService<Context = crate::ContextType>
            + Send + Sync + 'static;
        fn repayment_letter_service(&self) -> Arc<Self::RepaymentLetterService>;
    }

    /// POST /api/repayment-phase/{phase_id}/letters/generate
    ///
    /// Generiert Briefe fuer multi-selektierte RepaymentEntries und liefert ein
    /// gebuendeltes Druck-PDF (D-13-01). Pro betroffenem Member entsteht zusaetzlich
    /// ein persistiertes auditiertes MemberDocument.
    ///
    /// Response-Header:
    /// - `Content-Type: application/pdf`
    /// - `Content-Disposition: attachment; filename="..."`
    /// - `X-Document-Count: N` — Anzahl der unique Members nach Aggregation (D-13-04).
    ///   Frontend nutzt diesen Wert fuer die Pluralisierung des Erfolgs-Toasts,
    ///   weil `entry_ids.len()` nach Aggregation falsch waere.
    #[utoipa::path(
        post,
        path = "/api/repayment-phase/{phase_id}/letters/generate",
        params(
            ("phase_id" = Uuid, Path, description = "RepaymentPhase UUID — phase muss Open oder Closed sein"),
        ),
        request_body = GenerateLettersRequest,
        responses(
            (status = 200, description = "Bundle-PDF aller Anschreiben — Content-Type application/pdf, Content-Disposition attachment, X-Document-Count: N (Anzahl Briefe nach Aggregation)", content_type = "application/pdf"),
            (status = 400, description = "Validation-Fehler: entry_ids leer, entry_phase_mismatch, oder unknown entry_ids"),
            (status = 401, description = "Session ungueltig oder fehlt"),
            (status = 403, description = "Auth gueltig, aber kein Vorstand (Helfer-Auth)"),
            (status = 404, description = "RepaymentPhase mit dieser ID nicht gefunden"),
            (status = 409, description = "RepaymentPhase im Preparation-Status — phase_not_active"),
        ),
        tag = "RepaymentLetter"
    )]
    #[instrument(skip(rest_state))]
    pub async fn generate_letters<RestState: RestStateDef + RepaymentLetterRestState>(
        rest_state: State<RestState>,
        Extension(context): Extension<Context>,
        Path(phase_id): Path<Uuid>,
        Json(body): Json<GenerateLettersRequest>,
    ) -> Response {
        error_handler(
            (async {
                let auth = extract_auth_context(Some(context))?;

                if body.entry_ids.is_empty() {
                    return Err(RestError::BadRequest(
                        "entry_ids must not be empty".to_string(),
                    ));
                }

                let entry_ids: Arc<[Uuid]> = body.entry_ids.into();

                let result: RepaymentLetterBundle = rest_state
                    .repayment_letter_service()
                    .generate(phase_id, entry_ids, auth)
                    .await
                    .map_err(map_letter_error)?;

                let cd = http_util::content_disposition_attachment(&result.filename);

                // D-13-04: X-Document-Count = Anzahl der unique Members nach Aggregation.
                // Frontend (Plan 06) liest diesen Header fuer die Toast-Pluralisierung,
                // weil entry_ids.len() nach Aggregation der falsche Zaehler waere.
                let document_count = result.document_ids.len();

                Ok(Response::builder()
                    .status(200)
                    .header("Content-Type", "application/pdf")
                    .header("Content-Disposition", &cd)
                    .header("X-Document-Count", document_count.to_string())
                    .body(Body::from(result.bundle_bytes))
                    .unwrap())
            })
            .await,
        )
    }

    /// Router-Generator — Mount via `.nest("/api/repayment-phase", generate_letter_route::<...>())`.
    pub fn generate_letter_route<RestState: RestStateDef + RepaymentLetterRestState>(
    ) -> Router<RestState> {
        Router::new().route(
            "/{phase_id}/letters/generate",
            post(generate_letters::<RestState>),
        )
    }

    #[derive(OpenApi)]
    #[openapi(
        paths(generate_letters),
        components(schemas(GenerateLettersRequest)),
        tags(
            (name = "RepaymentLetter",
             description = "Phase 13: Bulk-PDF-Anschreiben fuer Nicht-Email-Mitglieder. Vorstand-only.")
        )
    )]
    pub struct ApiDoc;
    ```

    **WICHTIG**: `ContextType` ist die existierende Typ-Definition aus `genossi_rest/src/lib.rs`. Falls die Type-Definition anders heisst, an die Phase-11-Vorlage anpassen.
  </action>
  <verify>
    <automated>cargo build -p genossi_rest</automated>
  </verify>
  <acceptance_criteria>
    - File existiert: `test -f genossi_rest/src/repayment_letter.rs`
    - `rg 'pub async fn generate_letters' genossi_rest/src/repayment_letter.rs` returns 1
    - `rg 'pub struct GenerateLettersRequest' genossi_rest/src/repayment_letter.rs` returns 1
    - `rg 'pub trait RepaymentLetterRestState' genossi_rest/src/repayment_letter.rs` returns 1
    - `rg '#\[derive\(OpenApi\)\]' genossi_rest/src/repayment_letter.rs` returns 1
    - `rg 'pub fn generate_letter_route' genossi_rest/src/repayment_letter.rs` returns 1
    - `rg 'map_letter_error' genossi_rest/src/repayment_letter.rs` returns >=2 (def + usage)
    - `rg 'PermissionDenied => RestError::Forbidden' genossi_rest/src/repayment_letter.rs` returns 1
    - `rg 'application/pdf' genossi_rest/src/repayment_letter.rs` returns >=2 (Header + OpenAPI)
    - `rg 'Content-Disposition' genossi_rest/src/repayment_letter.rs` returns >=1
    - **X-Document-Count Header (D-13-04):**
      - `rg '"X-Document-Count"' genossi_rest/src/repayment_letter.rs` returns 1 (Response-Header)
      - `rg 'document_ids\.len\(\)' genossi_rest/src/repayment_letter.rs` returns 1 (Count-Quelle)
    - `rg 'status = 200' genossi_rest/src/repayment_letter.rs` returns 1
    - `rg 'status = 400' genossi_rest/src/repayment_letter.rs` returns 1
    - `rg 'status = 403' genossi_rest/src/repayment_letter.rs` returns 1
    - `rg 'status = 404' genossi_rest/src/repayment_letter.rs` returns 1
    - `rg 'status = 409' genossi_rest/src/repayment_letter.rs` returns 1
    - `cargo build -p genossi_rest` exit 0 (Handler kompiliert mit Trait-Constraints)
  </acceptance_criteria>
  <done>
    REST-Handler-Datei vollstaendig; ToSchema-Body + OpenAPI mit 6 Status-Codes + X-Document-Count Header + RestState-Trait + Route-Generator + ApiDoc; cargo build sauber.
  </done>
</task>

<task type="auto">
  <name>Task 2: Router-Mount + ApiDoc-Nest + Trait-Bound (an >=2 Stellen) in genossi_rest/src/lib.rs</name>
  <files>genossi_rest/src/lib.rs</files>
  <read_first>
    - genossi_rest/src/lib.rs (gesamte Datei — mind. Z. 1-50 fuer Module-Declarations, Z. 270-290 fuer ApiDoc-Nesting, Z. 440-460 + 770-790 fuer Trait-Bounds (BEIDE STELLEN), Z. 650-670 fuer Router-Mounts)
    - .planning/phases/13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder/13-PATTERNS.md §`genossi_rest/src/lib.rs`
  </read_first>
  <action>
    **4 Edit-Stellen in `genossi_rest/src/lib.rs`** — alle gespiegelt vom existierenden `repayment_export`-Pattern. Trait-Bound MUSS an MINDESTENS 2 Stellen ergaenzt werden (analog repayment_export Z. ~449 + Z. ~782).

    **1. Module-Deklaration (vermutlich Z. 5 nach `pub mod repayment_export;`):**
    ```rust
    pub mod repayment_export;
    pub mod repayment_letter;   // ★ Phase 13 NEU
    ```

    **2. ApiDoc-Nest (Z. ~279, direkt nach Export-Eintrag):**
    ```rust
    (path = "/api/repayment-phase/{phase_id}/export", api = repayment_export::ApiDoc),
    (path = "/api/repayment-phase/{phase_id}/letters", api = repayment_letter::ApiDoc),  // ★ Phase 13
    ```

    **3. Trait-Bounds — BEIDE Stellen pflegen (Z. ~449 + Z. ~782 oder wo immer `+ repayment_export::RepaymentExportRestState` steht):**

    Suche zuerst ALLE Stellen mit `repayment_export::RepaymentExportRestState` via:
    ```bash
    rg '\+ repayment_export::RepaymentExportRestState' genossi_rest/src/lib.rs
    ```

    Erwartet: 2+ Treffer. AN JEDER STELLE direkt darunter ergaenzen:
    ```rust
    + repayment_export::RepaymentExportRestState
    + repayment_letter::RepaymentLetterRestState   // ★ Phase 13
    ```

    **4. Router-Mount (Z. ~654 nach `.nest("/api/repayment-phase", repayment_export::...))`):**
    ```rust
    .nest(
        "/api/repayment-phase",
        repayment_export::generate_export_route::<RestState>(),
    )
    .nest(
        "/api/repayment-phase",
        repayment_letter::generate_letter_route::<RestState>(),  // ★ Phase 13
    )
    ```

    Axum 0.8.3 erlaubt mehrere `.nest`-Aufrufe mit demselben Prefix, solange die inneren Pfade-Segmente eindeutig sind. `/{phase_id}/letters/generate` ist disjunkt von `/{phase_id}/export/{format}`.

    KEINE Aenderungen an anderen Modulen.
  </action>
  <verify>
    <automated>cargo build -p genossi_rest</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'pub mod repayment_letter' genossi_rest/src/lib.rs` returns 1
    - `rg 'repayment_letter::ApiDoc' genossi_rest/src/lib.rs` returns 1
    - **Trait-Bound-Acceptance verschaerft (>=2):** `rg 'repayment_letter::RepaymentLetterRestState' genossi_rest/src/lib.rs` returns >=2 (an allen Stellen wo `repayment_export::RepaymentExportRestState` steht)
    - `rg 'repayment_letter::generate_letter_route' genossi_rest/src/lib.rs` returns 1
    - `rg '\.nest\(\s*"/api/repayment-phase"' genossi_rest/src/lib.rs | wc -l` returns >=2 (export + letter)
    - **Parity-Check**: `rg 'repayment_letter::RepaymentLetterRestState' genossi_rest/src/lib.rs | wc -l` == `rg 'repayment_export::RepaymentExportRestState' genossi_rest/src/lib.rs | wc -l` (gleiche Anzahl Bounds)
    - `cargo build -p genossi_rest` exit 0
    - `cargo build -p genossi_rest --all-features` exit 0
  </acceptance_criteria>
  <done>
    Mount + ApiDoc-Nest + Trait-Bounds an ALLEN relevanten Stellen (>=2); cargo build durch.
  </done>
</task>

<task type="auto">
  <name>Task 3: DI-Wiring in genossi_bin/src/lib.rs — 2 Deps-Aliases + Resolver + Letter-Service-Construction + 2 Trait-Impls + (falls Pre-Flight: document_storage-Konstruktor)</name>
  <files>genossi_bin/src/lib.rs</files>
  <read_first>
    - Pre-Flight-Resultat aus Task 0 (im SUMMARY der laufenden Phase dokumentiert)
    - genossi_bin/src/lib.rs:280-340 (Deps-Alias-Pattern — RepaymentExportServiceDependencies als 1:1-Vorbild)
    - genossi_bin/src/lib.rs:540-580 (RestStateImpl-Struct-Definition — Felder-Pattern)
    - genossi_bin/src/lib.rs:850-900 (RestStateImpl::new — Service-Construction-Pattern, repayment_export_service)
    - genossi_bin/src/lib.rs:960-990 (RestStateImpl::new-Return — Struct-Literal mit allen Feldern)
    - genossi_bin/src/lib.rs:1500-1550 (Trait-Impl-Pattern — `impl genossi_rest::repayment_export::RepaymentExportRestState for RestStateImpl`)
    - .planning/phases/13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder/13-PATTERNS.md §`genossi_bin/src/lib.rs`
    - .planning/STATE.md Eintrag "Plan 10-07: Option A enforced" (Single-Arc-per-DAO-per-Process Pattern)
  </read_first>
  <action>
    **5 (ggf. 6) Edit-Stellen in `genossi_bin/src/lib.rs`** — Single-Arc-per-Process-Pattern (Phase 10 P07 Lektion).

    **Stelle 0 (NUR falls Task 0 ergeben hat, dass document_storage NICHT als lokales Arc existiert):**

    Direkt VOR der RestStateImpl::new-Konstruktion einen lokalen Arc anlegen:
    ```rust
    // Phase 13 D-13-01: FilesystemDocumentStorage als Single-Arc-per-Process (Phase 10 P07 Lektion).
    // Wird von repayment_letter_service via .clone() bezogen.
    let document_storage: Arc<genossi_service_impl::document_storage::FilesystemDocumentStorage> =
        Arc::new(genossi_service_impl::document_storage::FilesystemDocumentStorage::new(
            std::path::PathBuf::from(/* config-Pfad — Implementer ermittelt aus env-var oder config-Struct */),
        ));
    ```

    Falls Task 0 ergeben hat dass document_storage bereits existiert: diesen Schritt UEBERSPRINGEN.

    **Stelle 1: Deps-Aliases direkt nach `RepaymentExportServiceDependencies` (vermutlich Z. ~316):**

    ```rust
    // Phase 13 D-13-04 / D-13-10: RepaymentContextResolver Deps.
    pub struct RepaymentContextResolverDependencies;
    unsafe impl Send for RepaymentContextResolverDependencies {}
    unsafe impl Sync for RepaymentContextResolverDependencies {}

    impl genossi_service_impl::repayment_context::RepaymentContextResolverDeps
        for RepaymentContextResolverDependencies
    {
        type Transaction = Transaction;
        type RepaymentPhaseDao = RepaymentPhaseDao;
        type RepaymentEntryDao = RepaymentEntryDao;
    }

    type RepaymentContextResolver =
        genossi_service_impl::repayment_context::RepaymentContextResolverImpl<
            RepaymentContextResolverDependencies,
        >;

    // Phase 13 D-13-01..11: RepaymentLetterService Deps.
    pub struct RepaymentLetterServiceDependencies;
    unsafe impl Send for RepaymentLetterServiceDependencies {}
    unsafe impl Sync for RepaymentLetterServiceDependencies {}

    impl genossi_service_impl::repayment_letter::RepaymentLetterServiceDeps
        for RepaymentLetterServiceDependencies
    {
        type Context = Context;
        type Transaction = Transaction;
        type RepaymentPhaseDao = RepaymentPhaseDao;
        type RepaymentEntryDao = RepaymentEntryDao;
        type MemberDao = MemberDao;
        type MemberDocumentDao = MemberDocumentDao;
        type AuditLogDao = AuditLogDao;
        type PermissionService = PermissionService;
        type TransactionDao = TransactionDao;
        type UuidService = UuidService;
        type RepaymentContextResolver = RepaymentContextResolver;
        type DocumentStorage = genossi_service_impl::document_storage::FilesystemDocumentStorage;
    }

    type RepaymentLetterService =
        genossi_service_impl::repayment_letter::RepaymentLetterServiceImpl<
            RepaymentLetterServiceDependencies,
        >;
    ```

    **WICHTIG**: Falls die existierenden Type-Aliases (`Context`, `Transaction`, `RepaymentPhaseDao`, etc.) im File anders heissen, an die echten Namen anpassen.

    **Stelle 2: RestStateImpl-Felder ergaenzen (vermutlich Z. ~549-552):**

    ```rust
    pub struct RestStateImpl {
        // ... existing fields ...
        pub repayment_export_service: Arc<RepaymentExportService>,
        pub repayment_context_resolver: Arc<RepaymentContextResolver>,   // ★ Phase 13
        pub repayment_letter_service: Arc<RepaymentLetterService>,       // ★ Phase 13
        // ... bestehende Felder bleiben
    }
    ```

    **Stelle 3: Service-Construction in `RestStateImpl::new()` (direkt nach `repayment_export_service` Block, Z. ~880) — NUR .clone() pro Dependency:**

    ```rust
    // Phase 13 D-13-04 / D-13-10: RepaymentContextResolver
    let repayment_context_resolver = Arc::new(
        genossi_service_impl::repayment_context::RepaymentContextResolverImpl::<
            RepaymentContextResolverDependencies,
        > {
            repayment_phase_dao: repayment_phase_dao.clone(),
            repayment_entry_dao: repayment_entry_dao.clone(),
        },
    );

    // Phase 13 D-13-01..11: RepaymentLetterService — Single-Arc-per-Process Pattern (Phase 10 P07).
    // ALLE Dependencies via .clone() von existierenden lokalen Arcs.
    // KEIN neues Arc::new(SomeDao::new(...)) — das wuerde Phase 10 P07 verletzen.
    let repayment_letter_service = Arc::new(
        genossi_service_impl::repayment_letter::RepaymentLetterServiceImpl::<
            RepaymentLetterServiceDependencies,
        > {
            repayment_phase_dao: repayment_phase_dao.clone(),
            repayment_entry_dao: repayment_entry_dao.clone(),
            member_dao: member_dao.clone(),
            member_document_dao: member_document_dao.clone(),
            audit_log_dao: audit_log_dao.clone(),
            permission_service: permission_service.clone(),
            transaction_dao: transaction_dao.clone(),
            uuid_service: uuid_service.clone(),
            repayment_context_resolver: repayment_context_resolver.clone(),
            document_storage: document_storage.clone(),
            pdf_generator: pdf_generator.clone(),
            template_base: Arc::new(template_storage.base_path().to_path_buf()),
        },
    );
    ```

    **Stelle 4: Struct-Literal-Return in `RestStateImpl::new()` (vermutlich Z. ~975-988):**

    ```rust
    RestStateImpl {
        // ... existing fields ...
        repayment_export_service,
        repayment_context_resolver,    // ★ Phase 13
        repayment_letter_service,      // ★ Phase 13
        // ... rest
    }
    ```

    **Stelle 5: Trait-Impl `RepaymentLetterRestState` (direkt nach `impl RepaymentExportRestState for RestStateImpl`, Z. ~1531):**

    ```rust
    impl genossi_rest::repayment_letter::RepaymentLetterRestState for RestStateImpl {
        type RepaymentLetterService = RepaymentLetterService;
        fn repayment_letter_service(&self) -> Arc<Self::RepaymentLetterService> {
            self.repayment_letter_service.clone()
        }
    }
    ```
  </action>
  <verify>
    <automated>cargo build -p genossi_bin --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'pub struct RepaymentContextResolverDependencies' genossi_bin/src/lib.rs` returns 1
    - `rg 'pub struct RepaymentLetterServiceDependencies' genossi_bin/src/lib.rs` returns 1
    - `rg 'type RepaymentContextResolver\s*=' genossi_bin/src/lib.rs` returns 1
    - `rg 'type RepaymentLetterService\s*=' genossi_bin/src/lib.rs` returns 1
    - `rg 'repayment_context_resolver: Arc' genossi_bin/src/lib.rs` returns >=2 (RestStateImpl field + struct construction)
    - `rg 'repayment_letter_service: Arc' genossi_bin/src/lib.rs` returns >=2
    - `rg 'RepaymentContextResolverImpl' genossi_bin/src/lib.rs` returns >=1 (Construction)
    - `rg 'RepaymentLetterServiceImpl' genossi_bin/src/lib.rs` returns >=1
    - `rg 'impl genossi_rest::repayment_letter::RepaymentLetterRestState' genossi_bin/src/lib.rs` returns 1
    - **Single-Arc-Gate (KRITISCH, Phase 10 P07 + Pre-Flight):**
      - `rg 'let document_storage' genossi_bin/src/lib.rs` returns >=1 (existiert als lokales Arc)
      - `rg 'Arc::new\(FilesystemDocumentStorage' genossi_bin/src/lib.rs | wc -l` returns 1 (GENAU 1 Konstruktor)
      - `rg 'Arc::new\(MemberDocumentDao::new\(self\.pool' genossi_bin/src/lib.rs | wc -l` returns 0 (kein neues Arc fuer MemberDocumentDao)
      - `rg 'Arc::new\([A-Z][A-Za-z]*Dao::new' genossi_bin/src/lib.rs | wc -l` ≤ baseline aus Pre-Flight-Task-0 (kein NEUER DAO-Arc — alle via .clone())
    - `cargo build -p genossi_bin` exit 0
    - `cargo build -p genossi_bin --all-features` exit 0
    - `cargo build --workspace` exit 0 (Cross-Crate-Verifikation)
  </acceptance_criteria>
  <done>
    DI-Wiring vollstaendig; 5 (ggf. 6) Edit-Stellen ausgefuehrt; Workspace-Build sauber; Single-Arc-Pattern eingehalten (Single-Arc-Gate gruen).
  </done>
</task>

</tasks>

<threat_model>
- **Mount-Path-Konflikt (`/letters` ueberlappt mit `/export`)**: LOW — Axum nutzt Path-Matching pro Segment; `/{phase_id}/letters/generate` ist disjunkt von `/{phase_id}/export/{format}`. Mitigation: Tests in Plan 07 verifizieren beide Endpoints koexistieren.
- **Trait-Bound-Vergessen → Compile-Error**: LOW — wenn `RepaymentLetterRestState` an einer Stelle fehlt, faellt cargo build mit klarem Trait-Bound-Fehler. Mitigation: Acceptance grept >=2 Stellen + Parity-Check mit repayment_export.
- **DI-Wiring Multi-Arc-Konflikt (mehrere Arcs der gleichen DAO)**: MEDIUM — Phase 10 P07 Lektion: 2 Arcs derselben DAO im selben Process koennten Audit-Hashchain-Race produzieren. Mitigation: Single-Arc-Gate in Acceptance + Pre-Flight-Task 0 verifiziert Baseline + Phase 10 P07 SUMMARY-Pattern als Anker.
- **Permission-Bypass im Handler (vergessen, map_letter_error zu rufen)**: HIGH — wenn der Handler `.into()` statt `map_letter_error` verwendet, wird `PermissionDenied` faelschlich als 401 (Unauthorized) mapped statt 403. Mitigation: Acceptance grept `PermissionDenied => RestError::Forbidden`; Plan 07 E2E-Test 3 (Permission-Denied) verifiziert end-to-end.
- **OpenAPI-Doku-Drift (Status-Codes falsch dokumentiert)**: LOW — Body-Schema + Status-Codes sind im Code als Annotations definiert; cargo build verifiziert Compile aber nicht semantische Korrektheit. Mitigation: 5 status=...-Greps in Acceptance.
- **X-Document-Count-Drift (Header-Wert != document_ids.len())**: LOW — Header wird direkt aus `result.document_ids.len()` gesetzt. Mitigation: Acceptance-Grep verifiziert die Quelle; Plan 07 E2E-Test 1 (Happy-Path) verifiziert end-to-end (Header lesen + auf erwarteten Count pruefen).
</threat_model>

<verification>
- Task 0 Pre-Flight-Resultat im SUMMARY dokumentiert
- cargo build -p genossi_rest exit 0
- cargo build -p genossi_bin --all-features exit 0
- cargo build --workspace exit 0
- Single-Arc-Gate sauber (FilesystemDocumentStorage genau 1x; kein neuer DAO-Arc)
- Trait-Bound-Parity zu repayment_export
</verification>

<success_criteria>
1. POST /api/repayment-phase/{phase_id}/letters/generate ist im OpenAPI sichtbar
2. Server akzeptiert echte HTTP-Requests (verifiziert in Plan 07 E2E-Tests)
3. Response enthaelt X-Document-Count Header mit Anzahl unique Members nach Aggregation
4. RestStateImpl ist um 2 neue Arc-Felder erweitert, Single-Arc-Pattern eingehalten
</success_criteria>

<output>
After completion, create `.planning/phases/13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder/13-05-SUMMARY.md` — MUSS Pre-Flight-Task-0-Resultat (welche Arcs existierten, ob document_storage neu angelegt wurde) dokumentieren.
</output>
