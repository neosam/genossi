---
phase: 13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder
plan: 06
type: execute
wave: 5
depends_on: [05]
files_modified:
  - genossi-frontend/src/api.rs
  - genossi-frontend/src/component/repayment_entry_list.rs
  - genossi-frontend/src/page/repayment_phase_details.rs
  - genossi-frontend/src/i18n/de.rs
  - genossi-frontend/src/i18n/en.rs
  - genossi-frontend/src/i18n/keys.rs
autonomous: true
requirements: [BRIEF-01, UI-03, UI-06]
tags: [phase-13, frontend, dioxus, component-first, button-pattern-d01, blob-download, bulk-action, selection-preservation]

must_haves:
  truths:
    - "API-Client-Funktion generate_repayment_letters(config, phase_id, entry_ids) in genossi-frontend/src/api.rs verfuegbar"
    - "API-Client liest X-Document-Count Header aus Response und gibt ihn an den Caller zurueck (Tuple Result<(String, usize), AppError> ODER struct GeneratedLettersResult)"
    - "Bulk-Button 'Anschreiben erzeugen' in RepaymentEntryList-Component direkt unter Massenmail-Button"
    - "Button-Pattern Phase 12 D-01 MANDATORY: r#type: \"button\" + onclick, KEIN <form onsubmit>"
    - "Click triggert POST mit entry_ids der Multi-Selektion → Browser-Save des Bundle-PDFs via Blob+createObjectURL"
    - "Browser-Download-Filename: auszahlungs_anschreiben_GJ_{fiscal_year}.pdf"
    - "Toast nach Erfolg nutzt X-Document-Count Wert (Anzahl Briefe nach Aggregation), NICHT entry_ids.len(): 'N Brief(e) erzeugt. ...'"
    - "i18n-Strings haben getrennte Singular/Plural-Form ODER {count}-Placeholder fuer korrekte deutsche Grammatik"
    - "D-13-09 Selection-Preservation: NACH erfolgreichem Brief-Download bleibt selected_ids UNVERAENDERT (kein clear() ODER Vec::new()) — Vorstand kann sofort auf den Phase-8-Batch 'Als angeschrieben markieren' weiter klicken"
    - "i18n-Keys fuer Button-Label + Toast-Message in DE + EN registriert"
    - "Grep-Gate: rg 'button\\s*\\{' in beiden Files OHNE r#type-Treffer == 0 (Phase 12 D-02)"
    - "Phase 13 D-13-03: entry_ids (NICHT member_ids) werden geschickt — Server aggregiert via Resolver"
  artifacts:
    - path: "genossi-frontend/src/api.rs"
      provides: "generate_repayment_letters fn (liefert blob_url + document_count aus X-Document-Count Header)"
      contains: "generate_repayment_letters"
    - path: "genossi-frontend/src/component/repayment_entry_list.rs"
      provides: "Bulk-Button 'Anschreiben erzeugen' + on_letter_request EventHandler-Prop"
      contains: "on_letter_request"
    - path: "genossi-frontend/src/page/repayment_phase_details.rs"
      provides: "on_letter_request Handler + Browser-Save-Trigger + Toast mit document_count + Selection-Preservation"
      contains: "on_letter_request"
    - path: "genossi-frontend/src/i18n/keys.rs"
      provides: "Key::RepaymentEntryBulkLetterButton + Key::RepaymentLetterToastSingular + Key::RepaymentLetterToastPlural (oder placeholder-Variante)"
      contains: "RepaymentEntryBulkLetterButton"
  key_links:
    - from: "RepaymentEntryList component"
      to: "repayment_phase_details page"
      via: "EventHandler<Vec<Uuid>> bubble"
      pattern: "on_letter_request"
    - from: "repayment_phase_details page"
      to: "api::generate_repayment_letters"
      via: "fetch + blob + X-Document-Count Header lesen"
      pattern: "generate_repayment_letters"
    - from: "Browser-Save"
      to: "create_element('a') + set_attribute('download') + click + revoke_object_url"
      via: "web-sys"
      pattern: "create_object_url"
---

<objective>
Frontend Phase-12-Komplement: neuer Bulk-Action-Button "Anschreiben erzeugen" neben "Massenmail" in der RepaymentEntryList-Component, mit Browser-Save des Bundle-PDFs und Toast-Hinweis auf D-13-09 (Vorstand muss Status separat toggeln). Multi-Select-Auswahl bleibt nach Download erhalten, damit der Vorstand direkt mit "Als angeschrieben markieren" fortfahren kann.

**Wichtige Aenderungen vs. Initial-Plan:**
- API-Client liest X-Document-Count Header und gibt ihn an den Caller zurueck (Tuple oder Result-Struct)
- Toast-Text nutzt diesen Count (Server-Wert nach D-13-04-Aggregation), NICHT `entry_ids.len()` (waere falsch)
- i18n-Strings sind Singular/Plural-aware (Deutsch verlangt "1 Brief" vs "N Briefe")
- on_letter_request-Handler clearet selected_ids NACH dem Download NICHT — D-13-09-Compliance (Vorstand muss weiter klicken koennen)

Purpose: Schliesst den User-Journey von Phase 12 → Phase 13: Multi-Select → Klick → Druck-PDF.
Output:
- API-Client `generate_repayment_letters(config, phase_id, entry_ids) -> Result<GeneratedLettersResult, AppError>` (returnt blob_url + document_count)
- RepaymentEntryList-Component: neuer EventHandler-Prop `on_letter_request` + Bulk-Button
- repayment_phase_details Page-Handler `on_letter_request` mit spawn + Browser-Save + Toast (mit document_count) + Selection-Preservation
- i18n-Keys + Strings in DE + EN (Singular + Plural)
- Component-First-Konformitaet via Grep-Gate (Phase 12 D-02)
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder/13-CONTEXT.md
@.planning/phases/13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder/13-RESEARCH.md
@.planning/phases/13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder/13-PATTERNS.md

<interfaces>
<!-- Plan 05 Output: X-Document-Count Header in Response, document_ids.len() Wert -->

<!-- Existing Massenmail-Bulk-Button — genossi-frontend/src/component/repayment_entry_list.rs:226-249 -->
```rust
button {
    r#type: "button",  // Phase 12 D-01 Pflicht
    class: if selected_count == 0 {
        "bg-gray-200 text-gray-500 px-3 py-2 rounded text-sm cursor-not-allowed min-h-[44px]"
    } else {
        "bg-blue-600 hover:bg-blue-700 text-white px-3 py-2 rounded text-sm min-h-[44px]"
    },
    disabled: selected_count == 0,
    onclick: move |_| {
        let selected_set = selected_ids.read().clone();
        let member_ids: Vec<Uuid> = entries
            .read()
            .iter()
            .filter(|e| selected_set.contains(&e.id))
            .map(|e| e.member_id)
            .collect();
        on_mail_request.call(member_ids);
    },
    "{i18n.t(Key::RepaymentEntryBulkMailButton)} ({selected_count})"
}
```

<!-- Existing on_mail_request Page-Handler — genossi-frontend/src/page/repayment_phase_details.rs:244-258 -->
```rust
on_mail_request: move |ids: Vec<uuid::Uuid>| {
    if ids.is_empty() { return; }
    let url = build_mail_redirect_url(phase_id, &ids);
    if let Some(window) = web_sys::window() {
        let _ = window.location().set_href(&url);
    }
},
```

<!-- Existing Browser-Save-Pattern — genossi-frontend/src/page/assembly_details.rs:362-395 -->
```rust
spawn(async move {
    let cfg = CONFIG.read().clone();
    match api::export_attendance_url(&cfg, assembly_id, &fmt, &inc).await {
        Ok(blob_url) => { /* <a download>.click() + revoke_object_url */ }
        Err(e) => { /* AppError handling */ }
    }
});
```

<!-- Header-Lesen Pattern (web-sys): -->
<!-- resp.headers().get("X-Document-Count").and_then(|h| h.parse::<usize>().ok()) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API-Client generate_repayment_letters (liest X-Document-Count) + i18n-Keys + DE/EN Strings (Singular + Plural)</name>
  <files>genossi-frontend/src/api.rs, genossi-frontend/src/i18n/keys.rs, genossi-frontend/src/i18n/de.rs, genossi-frontend/src/i18n/en.rs</files>
  <read_first>
    - genossi-frontend/src/api.rs (mind. Z. 1880-1950 — export_attendance_url als Vorbild + map_web_response_error)
    - genossi-frontend/src/api.rs (Z. 500-550 — render_template_pdf als zweites Vorbild mit POST+JSON-Body)
    - genossi-frontend/src/i18n/keys.rs (gesamte Datei — Key-Enum-Pattern)
    - genossi-frontend/src/i18n/de.rs + en.rs (Pattern fuer Key→String-Mapping)
    - .planning/phases/13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder/13-RESEARCH.md §Pattern 7 (Frontend Browser-Save Blob+createObjectURL)
    - .planning/phases/13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder/13-PATTERNS.md §`genossi-frontend/src/api.rs`
  </read_first>
  <action>
    **Schritt 1: API-Client-Funktion in `genossi-frontend/src/api.rs`** — direkt nach `export_attendance_url` einfuegen:

    ```rust
    /// Phase 13 D-13-02 / D-13-03 / D-13-04: POST /api/repayment-phase/{phase_id}/letters/generate.
    ///
    /// Sendet entry_ids als JSON-Body, empfaengt Bundle-PDF als binary + X-Document-Count Header.
    /// Caller triggert `<a download>`-Click mit `blob_url` und nutzt `document_count` fuer
    /// die Toast-Pluralisierung (Server aggregiert via Resolver: document_count = unique members,
    /// NICHT entry_ids.len()).
    ///
    /// Returns: GeneratedLettersResult { blob_url, document_count }. Caller MUSS
    /// `web_sys::Url::revoke_object_url(&blob_url)` nach dem `.click()` aufrufen.
    pub struct GeneratedLettersResult {
        pub blob_url: String,
        pub document_count: usize,
    }

    pub async fn generate_repayment_letters(
        config: &Config,
        phase_id: uuid::Uuid,
        entry_ids: Vec<uuid::Uuid>,
    ) -> Result<GeneratedLettersResult, AppError> {
        use wasm_bindgen::JsCast;
        use wasm_bindgen_futures::JsFuture;

        let url = format!(
            "{}/api/repayment-phase/{}/letters/generate",
            config.backend, phase_id
        );

        let body = serde_json::json!({ "entry_ids": entry_ids }).to_string();

        let opts = web_sys::RequestInit::new();
        opts.set_method("POST");
        opts.set_body(&wasm_bindgen::JsValue::from_str(&body));

        let headers = web_sys::Headers::new()
            .map_err(|e| AppError::new(None, "Verbindungsfehler", Some(format!("{:?}", e))))?;
        headers
            .set("Content-Type", "application/json")
            .map_err(|e| AppError::new(None, "Verbindungsfehler", Some(format!("{:?}", e))))?;
        opts.set_headers(&headers);

        let request = web_sys::Request::new_with_str_and_init(&url, &opts)
            .map_err(|e| AppError::new(None, "Verbindungsfehler", Some(format!("{:?}", e))))?;

        let window = web_sys::window()
            .ok_or_else(|| AppError::new(None, "Verbindungsfehler", None))?;
        let resp_value = JsFuture::from(window.fetch_with_request(&request))
            .await
            .map_err(|e| AppError::new(None, "Verbindungsfehler", Some(format!("{:?}", e))))?;

        let resp: web_sys::Response = resp_value
            .dyn_into()
            .map_err(|_| AppError::new(None, "Verbindungsfehler", None))?;

        if !resp.ok() {
            return Err(map_web_response_error(&resp).await);
        }

        // X-Document-Count Header lesen (D-13-04: Server-Aggregation-Count).
        // Fallback auf entry_ids.len() falls Header fehlt (defensiv — sollte nicht passieren).
        let document_count: usize = resp
            .headers()
            .get("X-Document-Count")
            .ok()
            .flatten()
            .and_then(|s| s.parse::<usize>().ok())
            .unwrap_or(entry_ids.len());

        let blob_promise = resp
            .blob()
            .map_err(|e| AppError::new(None, "Verbindungsfehler", Some(format!("{:?}", e))))?;
        let blob = JsFuture::from(blob_promise)
            .await
            .map_err(|e| AppError::new(None, "Verbindungsfehler", Some(format!("{:?}", e))))?;
        let blob: web_sys::Blob = blob
            .dyn_into()
            .map_err(|_| AppError::new(None, "Verbindungsfehler", None))?;

        let blob_url = web_sys::Url::create_object_url_with_blob(&blob)
            .map_err(|e| AppError::new(None, "Verbindungsfehler", Some(format!("{:?}", e))))?;

        Ok(GeneratedLettersResult { blob_url, document_count })
    }
    ```

    **Anmerkung zur Header-Lese-API:** `web_sys::Response::headers().get(name)` gibt typischerweise `Result<Option<String>, JsValue>` zurueck. Bei abweichender Signatur in der installierten web-sys-Version: Implementer passt das Pattern an, behaelt aber die Semantik bei (Header lesen, parsen, Fallback `entry_ids.len()`).

    **Schritt 2: i18n-Keys in `genossi-frontend/src/i18n/keys.rs`** ergaenzen — drei neue Varianten (Singular/Plural + Filename-Prefix):
    ```rust
    pub enum Key {
        // ... existing variants ...
        RepaymentEntryBulkLetterButton,
        /// Singular-Form: "Brief erzeugt. Vergiss nicht ..."
        RepaymentLetterToastSingular,
        /// Plural-Form: "{count} Briefe erzeugt. Vergiss nicht ..." (mit {count}-Placeholder)
        RepaymentLetterToastPlural,
        RepaymentLetterFilenamePrefix,
    }
    ```

    **Schritt 3: DE-Strings in `genossi-frontend/src/i18n/de.rs`** ergaenzen:
    ```rust
    Key::RepaymentEntryBulkLetterButton => "Anschreiben erzeugen".to_string(),
    Key::RepaymentLetterToastSingular => "1 Brief erzeugt. Vergiss nicht, den Eintrag anschliessend als angeschrieben zu markieren.".to_string(),
    Key::RepaymentLetterToastPlural => "{count} Briefe erzeugt. Vergiss nicht, die Eintraege anschliessend als angeschrieben zu markieren.".to_string(),
    Key::RepaymentLetterFilenamePrefix => "auszahlungs_anschreiben".to_string(),
    ```

    **Schritt 4: EN-Strings in `genossi-frontend/src/i18n/en.rs`** (best-effort-Translation; primaere Audience ist DE):
    ```rust
    Key::RepaymentEntryBulkLetterButton => "Generate letters".to_string(),
    Key::RepaymentLetterToastSingular => "1 letter generated. Don't forget to mark the entry as contacted afterwards.".to_string(),
    Key::RepaymentLetterToastPlural => "{count} letters generated. Don't forget to mark the entries as contacted afterwards.".to_string(),
    Key::RepaymentLetterFilenamePrefix => "auszahlungs_anschreiben".to_string(),
    ```

    KEINE Aenderungen an Component oder Page in dieser Task.
  </action>
  <verify>
    <automated>cd genossi-frontend && cargo check 2>&1 | head -50; cd ..</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'pub async fn generate_repayment_letters' genossi-frontend/src/api.rs` returns 1
    - `rg 'pub struct GeneratedLettersResult' genossi-frontend/src/api.rs` returns 1
    - `rg 'pub document_count: usize' genossi-frontend/src/api.rs` returns 1
    - `rg 'entry_ids' genossi-frontend/src/api.rs` returns >=2 (Signatur + Body-Konstruktion)
    - `rg '"/api/repayment-phase/{}/letters/generate"' genossi-frontend/src/api.rs` returns 1
    - **X-Document-Count Header lesen:**
      - `rg '"X-Document-Count"' genossi-frontend/src/api.rs` returns 1
      - `rg '\.parse::<usize>' genossi-frontend/src/api.rs | wc -l` >= existing+1 (Header parsen)
    - `rg 'set_method\("POST"\)' genossi-frontend/src/api.rs | wc -l` >= existing+1 (mind. eine neue POST-Stelle)
    - `rg 'create_object_url_with_blob' genossi-frontend/src/api.rs | wc -l` >= existing+1
    - `rg 'RepaymentEntryBulkLetterButton' genossi-frontend/src/i18n/keys.rs` returns 1
    - `rg 'RepaymentLetterToastSingular' genossi-frontend/src/i18n/keys.rs` returns 1
    - `rg 'RepaymentLetterToastPlural' genossi-frontend/src/i18n/keys.rs` returns 1
    - `rg 'Anschreiben erzeugen' genossi-frontend/src/i18n/de.rs` returns 1
    - `rg '1 Brief erzeugt' genossi-frontend/src/i18n/de.rs` returns 1 (Singular)
    - `rg '\{count\} Briefe erzeugt' genossi-frontend/src/i18n/de.rs` returns 1 (Plural mit Placeholder)
    - `rg 'Generate letters' genossi-frontend/src/i18n/en.rs` returns 1
    - `cd genossi-frontend && cargo check 2>&1 | tail -20` returns no error related to repayment_letter (existing warnings ok)
  </acceptance_criteria>
  <done>
    API-Funktion liefert blob_url + document_count; i18n-Keys (Singular + Plural) + DE/EN-Strings; cargo check sauber.
  </done>
</task>

<task type="auto">
  <name>Task 2: Bulk-Button "Anschreiben erzeugen" + on_letter_request Prop in RepaymentEntryList-Component</name>
  <files>genossi-frontend/src/component/repayment_entry_list.rs</files>
  <read_first>
    - genossi-frontend/src/component/repayment_entry_list.rs (gesamte Datei oder mind. Z. 100-300 — Props-Struct + Massenmail-Button als 1:1-Vorbild)
    - genossi-frontend/CLAUDE.md (Component-First-Direktive + Button-Pattern Phase 12 D-01)
    - .planning/phases/13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder/13-PATTERNS.md §`genossi-frontend/src/component/repayment_entry_list.rs:220-296`
  </read_first>
  <action>
    **In `genossi-frontend/src/component/repayment_entry_list.rs`:**

    **Schritt 1: Neuer Prop in der Props-Struct** (vermutlich Z. ~135-150 wo `on_mail_request` definiert ist):
    ```rust
    /// Phase 13 D-13-03: Callback fuer Bulk-Letter-Request.
    /// Receives entry_ids (NICHT member_ids — Server aggregiert via Resolver).
    pub on_letter_request: EventHandler<Vec<uuid::Uuid>>,
    ```

    **Schritt 2: Neuer Bulk-Button** direkt UNTER dem existierenden Massenmail-Button (Z. ~226-249) — innerhalb derselben Action-Bar-`div`:
    ```rust
    // Phase 13 D-13-01..03: Bulk-Letter-Button. KRITISCH: r#type: "button" Phase 12 D-01.
    button {
        r#type: "button",
        class: if selected_count == 0 {
            "bg-gray-200 text-gray-500 px-3 py-2 rounded text-sm cursor-not-allowed min-h-[44px]"
        } else {
            // Discretion: Purple/Indigo-Style differenziert visuell vom Mail-Button (Blau).
            "bg-purple-600 hover:bg-purple-700 text-white px-3 py-2 rounded text-sm min-h-[44px]"
        },
        disabled: selected_count == 0,
        onclick: move |_| {
            // D-13-03: entry_ids (NICHT member_ids) — Server aggregiert via Resolver D-13-04.
            let selected_set = selected_ids.read().clone();
            let ids: Vec<uuid::Uuid> = entries
                .read()
                .iter()
                .filter(|e| selected_set.contains(&e.id))
                .map(|e| e.id)
                .collect();
            on_letter_request.call(ids);
        },
        "{i18n.t(Key::RepaymentEntryBulkLetterButton)} ({selected_count})"
    }
    ```

    **WICHTIG**: Der Button MUSS:
    - `r#type: "button"` als ALLERERSTES Attribut (Phase 12 D-01 / D-02 Grep-Gate)
    - `disabled: selected_count == 0` (UX-Konvention)
    - `onclick: move |_| { ... }` (KEIN form-onsubmit)
    - Per-Entry-ID-Collection (NICHT member_id-Mapping wie beim Mail-Button!)
    - **KEINE Modifikation des `selected_ids`-Signals im onclick-Handler** — Selection bleibt erhalten (D-13-09 Selection-Preservation)

    Keine anderen Aenderungen an dem Component (Tabellen-Body, Filter, Sort bleiben).
  </action>
  <verify>
    <automated>cd genossi-frontend && cargo check 2>&1 | tail -30; cd ..</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'on_letter_request: EventHandler<Vec<uuid::Uuid>>' genossi-frontend/src/component/repayment_entry_list.rs` returns 1
    - `rg 'on_letter_request' genossi-frontend/src/component/repayment_entry_list.rs` returns >=2 (Prop-Decl + onclick-Body)
    - `rg 'RepaymentEntryBulkLetterButton' genossi-frontend/src/component/repayment_entry_list.rs` returns 1
    - `rg 'bg-purple-600' genossi-frontend/src/component/repayment_entry_list.rs` returns 1 (visuelle Diff zu Mail-Blau)
    - **Phase 12 D-02 Grep-Gate**: `rg 'button\s*\{' genossi-frontend/src/component/repayment_entry_list.rs | rg -v 'r#type:' | wc -l` returns 0
    - **D-13-09 Selection-Preservation (Component-Ebene):** der NEUE Bulk-Letter-Button-onclick darf KEIN `selected_ids.write()` ausfuehren —
      `rg 'on_letter_request' genossi-frontend/src/component/repayment_entry_list.rs -A 10 | rg 'selected_ids\.write\(\)\.clear|selected_ids\.write\(\) =' | wc -l` returns 0
    - `cd genossi-frontend && cargo check 2>&1` exit 0
  </acceptance_criteria>
  <done>
    Component um Prop + Button erweitert; Phase-12-D-02-Grep-Gate sauber; Selection wird im onclick NICHT verworfen.
  </done>
</task>

<task type="auto">
  <name>Task 3: Page-Handler on_letter_request mit Browser-Save + Toast (X-Document-Count + Pluralisierung) + Selection-Preservation in repayment_phase_details.rs</name>
  <files>genossi-frontend/src/page/repayment_phase_details.rs</files>
  <read_first>
    - genossi-frontend/src/page/repayment_phase_details.rs (gesamte Datei — mind. on_mail_request-Block als Pattern + RepaymentEntryList-Mount + Toast-Setup)
    - genossi-frontend/src/page/assembly_details.rs:355-400 (ExportTab Browser-Save als 1:1-Vorbild)
    - .planning/phases/13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder/13-PATTERNS.md §`genossi-frontend/src/page/repayment_phase_details.rs:240-260`
  </read_first>
  <action>
    **In `genossi-frontend/src/page/repayment_phase_details.rs`:**

    **Schritt 1: Neuer `on_letter_request`-Handler** als Prop an `RepaymentEntryList` (direkt neben `on_mail_request`):

    ```rust
    RepaymentEntryList {
        entries,
        selected_ids,
        // ... bestehende Props ...
        on_mail_request: move |ids: Vec<uuid::Uuid>| {
            // ... existing handler ...
        },
        on_letter_request: move |entry_ids: Vec<uuid::Uuid>| {
            // Phase 13 D-13-02: Direct-Download des Bundle-PDFs.
            if entry_ids.is_empty() { return; }

            let phase_id_for_spawn = phase_id;
            let fiscal_year_for_spawn = phase().map(|p| p.fiscal_year).unwrap_or(0);
            // KEIN entry_count = entry_ids.len() vorab — Server-Wert nach Aggregation nutzen (D-13-04).

            spawn(async move {
                let cfg = CONFIG.read().clone();
                match api::generate_repayment_letters(&cfg, phase_id_for_spawn, entry_ids).await {
                    Ok(result) => {
                        // result.blob_url + result.document_count (aus X-Document-Count Header).
                        // Browser-Save: <a download> click + revoke_object_url (Phase 6/11 Pattern).
                        if let Some(window) = web_sys::window() {
                            if let Some(document) = window.document() {
                                if let Ok(elem) = document.create_element("a") {
                                    let _ = elem.set_attribute("href", &result.blob_url);
                                    let dl_filename = format!(
                                        "auszahlungs_anschreiben_GJ_{}.pdf",
                                        fiscal_year_for_spawn
                                    );
                                    let _ = elem.set_attribute("download", &dl_filename);
                                    use wasm_bindgen::JsCast;
                                    if let Ok(html_elem) = elem.dyn_into::<web_sys::HtmlElement>() {
                                        html_elem.click();
                                    }
                                }
                                let _ = web_sys::Url::revoke_object_url(&result.blob_url);
                            }
                        }

                        // Erfolgs-Toast: Singular/Plural-aware mit Server-Document-Count (D-13-04).
                        let i18n_for_toast = i18n();
                        let toast_msg = if result.document_count == 1 {
                            i18n_for_toast.t(Key::RepaymentLetterToastSingular)
                        } else {
                            i18n_for_toast
                                .t(Key::RepaymentLetterToastPlural)
                                .replace("{count}", &result.document_count.to_string())
                        };
                        show_toast(&mut toast_messages, &mut toast_counter, toast_msg);

                        // D-13-09 Selection-Preservation:
                        // selected_ids wird hier NICHT modifiziert. Der Vorstand kann sofort
                        // den Phase-8-Batch-Button "Als angeschrieben markieren" mit der
                        // gleichen Selektion ausloesen. Backend toggelt den Status NICHT
                        // automatisch — symmetrisch zu Phase 10 (Mail).
                    }
                    Err(e) => {
                        // AppError-Mapping (analog assembly_details.rs).
                        let msg = e.message.clone();
                        show_toast(&mut toast_messages, &mut toast_counter, msg);
                    }
                }
            });
        },
        // ... bestehende Props ...
    }
    ```

    **Schritt 2: Page-Top Imports falls noch nicht vorhanden:**
    ```rust
    use wasm_bindgen::JsCast;
    // CONFIG, api, i18n, Key, show_toast, toast_messages, toast_counter sollten bereits importiert sein
    ```

    **WICHTIG fuer KORREKTE Funktion:**
    - `phase()` (Signal) muss in Scope sein — falls dort eine Resource liegt, ggf. anders abrufen (z.B. `phase_signal.read().as_ref().map(|p| p.fiscal_year)`). Implementer prueft konkrete Signal-Konstruktion vor Ort.
    - `show_toast` ist die existierende Helper-Funktion fuer Toast-Anzeige.
    - `toast_messages`, `toast_counter` sind die existierenden Toast-State-Signals.
    - `spawn` muss `dioxus::prelude::spawn` sein.

    **KRITISCH — D-13-09 Selection-Preservation:**

    Der `on_letter_request`-Handler darf an KEINER Stelle `selected_ids.write().clear()`, `selected_ids.set(Vec::new())` oder aequivalente Selection-Reset-Calls ausfuehren — weder vor noch nach dem spawn-Block, weder im Ok-Branch noch im Err-Branch. Acceptance verifiziert das via Grep.
  </action>
  <verify>
    <automated>cd genossi-frontend && cargo check 2>&1 | tail -30; cd ..</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'on_letter_request:' genossi-frontend/src/page/repayment_phase_details.rs` returns 1
    - `rg 'generate_repayment_letters' genossi-frontend/src/page/repayment_phase_details.rs` returns 1
    - `rg 'auszahlungs_anschreiben_GJ_' genossi-frontend/src/page/repayment_phase_details.rs` returns 1
    - `rg 'create_element\("a"\)' genossi-frontend/src/page/repayment_phase_details.rs | wc -l` >= existing+1
    - `rg 'revoke_object_url' genossi-frontend/src/page/repayment_phase_details.rs | wc -l` >= existing+1
    - **X-Document-Count Pipeline (Server → Toast):**
      - `rg 'result\.document_count' genossi-frontend/src/page/repayment_phase_details.rs | wc -l` >= 2 (mind. Singular-Branch-Check + Plural-replace)
      - `rg 'RepaymentLetterToastSingular' genossi-frontend/src/page/repayment_phase_details.rs` returns 1
      - `rg 'RepaymentLetterToastPlural' genossi-frontend/src/page/repayment_phase_details.rs` returns 1
      - `rg '\.replace\("\{count\}"' genossi-frontend/src/page/repayment_phase_details.rs` returns 1 (Plural-Placeholder)
      - `rg 'entry_ids\.len\(\)' genossi-frontend/src/page/repayment_phase_details.rs -A 1 -B 1 | rg 'RepaymentLetter|toast' | wc -l` returns 0 (entry_ids.len darf nicht als Toast-Count missbraucht werden)
    - **D-13-09 Selection-Preservation (Grep-Gate, KRITISCH):**
      - Innerhalb des on_letter_request-Handler-Blocks darf KEIN Selection-Reset auftauchen.
        Suche zuerst die Zeilen des Handlers via Grep-Range:
        `rg -n 'on_letter_request: move' genossi-frontend/src/page/repayment_phase_details.rs`
        Dann verifiziere (gesamte Datei kontextfrei):
        `rg 'selected_ids\.write\(\)\.clear|selected_ids\.set\(Vec::new|select_ids\.clear|set_selected.*\(.*Vec::new' genossi-frontend/src/page/repayment_phase_details.rs` returns 0
        (Das gilt fuer die GESAMTE Datei — falls ein bestehender Mail-Handler die Selection clearet, ist das eine separate Frage. Aber: Plan 06 darf KEINEN NEUEN clear einfuegen.)
    - **Phase 12 D-02 Grep-Gate (kombinierte Files):** `rg 'button\s*\{' genossi-frontend/src/component/repayment_entry_list.rs genossi-frontend/src/page/repayment_phase_details.rs | rg -v 'r#type:' | wc -l` returns 0
    - `cd genossi-frontend && cargo check 2>&1` exit 0
    - `cd genossi-frontend && cargo build --release 2>&1 | tail -5` exit 0 (full release build verifiziert WASM-Compile)
  </acceptance_criteria>
  <done>
    Page wired Component, ruft API, liest X-Document-Count, triggert Browser-Save, zeigt Singular/Plural-Toast, bewahrt Selection (D-13-09); Phase 12 D-02 Grep-Gate kombiniert sauber.
  </done>
</task>

</tasks>

<threat_model>
- **Phase 12 D-01 Verletzung (Button ohne r#type fuehrt zu Page-Reload-Bug)**: HIGH — Mitigation: r#type:"button" als allererstes Attribut + kombinierter Grep-Gate `rg 'button\s*\{' ... | rg -v 'r#type:' | wc -l == 0` ueber beide Files.
- **CSRF / Cross-Origin Request**: ACCEPT — Genossi nutzt OIDC + Same-Origin; Backend setzt CORS.
- **PII-Leak im Toast (Member-Daten im Frontend-Toast)**: LOW — Toast enthaelt nur Count + i18n-String, KEINE Member-Namen.
- **Browser-Memory-Leak durch nicht-revoke'd Blob-URLs**: MEDIUM — Mitigation: `revoke_object_url` direkt nach `.click()`.
- **Doppelter-Klick → Race / 2 Bulk-Calls**: LOW — Mitigation: `disabled: selected_count == 0` + 200er-Server-Limit. Falls Vorstand 2× klickt: 2 Bundles, 2N MemberDocuments (D-13-08 explizit erlaubt).
- **i18n-Drift (DE/EN-Keys sind nicht symmetrisch)**: LOW — Mitigation: 3 neuen Keys werden in beiden Lokalisierungen gepflegt.
- **Toast-Count-Inkonsistenz (entry_ids.len vs. aggregierte Anzahl Briefe)**: HIGH (D-13-04 user-trust) — Wenn Vorstand 3 Entries (2 fuer Member X, 1 fuer Member Y) selektiert, zeigt der Toast "3 Briefe erzeugt" — das stimmt nicht (es sind 2 Briefe). Mitigation: API liest X-Document-Count-Header (= unique Members nach Aggregation), Toast nutzt diesen Wert. Acceptance-Greps verifizieren beide Stellen.
- **Toast-Grammatik (1 Briefe falsch)**: LOW (UX-Quality) — Mitigation: Singular/Plural-i18n-Keys mit {count}-Placeholder; Handler waehlt je nach document_count.
- **D-13-09 Selection-Loss (Vorstand muss neu selektieren)**: MEDIUM (UX + D-13-09 user-trust) — Wenn Selection nach Download geclearet wird, muss Vorstand erneut alle Entries auswaehlen um "Als angeschrieben markieren" auszufuehren. Das macht den D-13-09-Workflow (Brief → Status-Toggle separat) muehsam. Mitigation: Selection wird NICHT modifiziert; Grep-Gate verhindert versehentliche Reset-Calls.
- **D-13-09 Compliance (Vorstand vergisst Status-Toggle)**: ACCEPT (Frontend-Hinweis ueber Toast) — Toast bewahrt den User; harte Frontend-Gating-UI waere Over-Engineering.
</threat_model>

<verification>
- cd genossi-frontend && cargo check exit 0
- cd genossi-frontend && cargo build --release exit 0
- Phase 12 D-02 Grep-Gate (kombiniert beide Files) returns 0
- i18n-Keys (Singular + Plural) in DE + EN registriert
- X-Document-Count fliesst Server → API → Toast (3 Greps)
- D-13-09 Selection-Preservation Grep-Gate sauber
</verification>

<success_criteria>
1. Vorstand-User sieht "Anschreiben erzeugen"-Button neben "Massenmail" auf der Einträge-Tab
2. Klick triggert Bundle-PDF-Download im Browser
3. Toast zeigt die KORREKTE Anzahl Briefe (Server-Aggregations-Count nach D-13-04) mit grammatikalisch korrekter Singular/Plural-Form
4. Multi-Select-Auswahl bleibt erhalten — Vorstand kann sofort "Als angeschrieben markieren" auf der gleichen Selektion ausfuehren (D-13-09 Workflow)
5. WASM-Build kompiliert sauber (Release-Mode-Check)
</success_criteria>

<output>
After completion, create `.planning/phases/13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder/13-06-SUMMARY.md`
</output>
