---
phase: 09
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - genossi_rest/src/repayment_entry.rs
autonomous: true
requirements: [PAYO-01, PAYO-04]
estimated_time: 30min
tags: [rest, openapi, action-endpoint, no-body]

decisions_addressed:
  - "D-07: Single-only in Phase 9 — Plan registriert EXAKT EINEN Action-Endpoint POST /api/repayment-entry/{id}/mark-paid-out, KEINE Batch-Variante (Cascade ist sicherheitskritisch/irreversibel, Confirm-Dialog UI-05 ist pro Eintrag konzipiert; Batch ist explizit deferred zu Phase 12)"

must_haves:
  truths:
    - "POST /api/repayment-entry/{id}/mark-paid-out ist als Axum-Route registriert"
    - "Handler hat KEIN Request-Body (Action-Endpoint-Pattern, Phase 7 D-02/D-03)"
    - "D-07: Single-only in Phase 9 — EXAKT EIN Action-Endpoint (/{id}/mark-paid-out), KEINE Batch-Variante (kein /batch-mark-paid-out o.ae.); Cascade ist sicherheitskritisch/irreversibel, Batch deferred zu Phase 12"
    - "OpenAPI/Utoipa-Annotation listet alle 5 erwarteten Status-Codes auf: 200, 400, 404, 409, 500 (CONTEXT D-06)"
    - "Handler ruft service.mark_paid_out(id, auth) und liefert RepaymentEntryTO bei 200 (CONTEXT D-05)"
    - "ApiDoc paths-Liste enthaelt mark_paid_out (Swagger-UI zeigt den Endpoint)"
  artifacts:
    - path: "genossi_rest/src/repayment_entry.rs"
      provides: "mark_paid_out Axum-Handler + Route-Registration + ApiDoc-Update"
      contains: "pub async fn mark_paid_out<RestState"
  key_links:
    - from: "POST /api/repayment-entry/{id}/mark-paid-out"
      to: "RepaymentEntryService::mark_paid_out"
      via: "rest_state.repayment_entry_service().mark_paid_out(id, auth).await"
      pattern: "repayment_entry_service\\(\\)\\.mark_paid_out\\("
    - from: "Handler-Return"
      to: "RepaymentEntryTO Response-Body"
      via: "RepaymentEntryTO::from(&entry) + serde_json::to_string"
      pattern: "RepaymentEntryTO::from\\(&entry\\)"
---

<objective>
REST-Exposure des in 09-01 implementierten Cascade-Service. Ein neuer Axum-Handler `mark_paid_out` wird mit `#[utoipa::path]`-Annotation registriert (alle 5 Status-Codes 200/400/404/409/500 dokumentiert). Die Route `/{id}/mark-paid-out` wird in `generate_route()` angehaengt; `mark_paid_out` wird der `ApiDoc::paths`-Liste hinzugefuegt. Action-Endpoint-Pattern aus Phase 7 (`open_repayment_phase`, `close_repayment_phase`) wird wortgleich uebernommen — kein Request-Body, kein Version-Body-Feld.

Purpose: Der Service ist nutzlos ohne REST-Exposure. Plan 09-04 (E2E-Tests) braucht den lebenden HTTP-Endpoint. Swagger-UI muss den neuen Endpoint anzeigen (Phase 12 Frontend referenziert ihn von dort).

Output: 1 Datei-Modifikation (~45 LOC: Handler ~35 LOC + Route ~1 Zeile + ApiDoc-Erweiterung ~1 Zeile).
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-auszahlungs-buchung-atomisch-auditiert/09-CONTEXT.md
@.planning/phases/09-auszahlungs-buchung-atomisch-auditiert/09-RESEARCH.md
@.planning/phases/09-auszahlungs-buchung-atomisch-auditiert/09-PATTERNS.md
@.planning/phases/09-auszahlungs-buchung-atomisch-auditiert/09-01-PLAN.md
@genossi_rest/src/repayment_entry.rs
@genossi_rest/src/repayment_phase.rs

<interfaces>
<!-- Key types/contracts the executor will need. -->

Phase 7 Pattern — `open_repayment_phase` from genossi_rest/src/repayment_phase.rs:234-268:
```rust
#[instrument(skip(rest_state))]
#[utoipa::path(
    post,
    tag = "RepaymentPhases",
    path = "/{id}/open",
    params(("id" = Uuid, Path, description = "RepaymentPhase ID")),
    responses(
        (status = 200, description = "Opened", body = RepaymentPhaseTO),
        (status = 401, description = "Unauthorized"),
        (status = 404, description = "Not found"),
        (status = 409, description = "Conflict (status not Preparation)"),
    ),
)]
pub async fn open_repayment_phase<RestState: RestStateDef + RepaymentPhaseRestState>(
    rest_state: State<RestState>,
    Extension(context): Extension<Context>,
    Path(id): Path<Uuid>,
) -> Response {
    error_handler(
        (async {
            let auth = crate::extract_auth_context(Some(context))?;
            let phase = rest_state
                .repayment_phase_service()
                .open_repayment_phase(id, auth)
                .await?;
            let to = RepaymentPhaseTO::from(&phase);
            Ok(Response::builder()
                .status(200)
                .header("Content-Type", "application/json")
                .body(Body::new(serde_json::to_string(&to)?))
                .unwrap())
        })
        .await,
    )
}
```

Existing `generate_route` in genossi_rest/src/repayment_entry.rs:302-316 (Phase 8):
```rust
pub fn generate_route<RestState: RestStateDef + RepaymentEntryRestState>() -> Router<RestState> {
    Router::new()
        .route("/batch-status", post(batch_toggle_status::<RestState>))
        .route("/", get(list_repayment_entries::<RestState>).post(create_repayment_entry::<RestState>))
        .route(
            "/{id}",
            get(get_repayment_entry::<RestState>)
                .put(update_repayment_entry::<RestState>)
                .delete(delete_repayment_entry::<RestState>),
        )
}
```

Existing ApiDoc in genossi_rest/src/repayment_entry.rs:318-338:
```rust
#[derive(OpenApi)]
#[openapi(
    paths(
        list_repayment_entries,
        create_repayment_entry,
        get_repayment_entry,
        update_repayment_entry,
        delete_repayment_entry,
        batch_toggle_status,
    ),
    components(schemas(
        RepaymentEntryTO,
        RepaymentEntryStatusTO,
        CreateRepaymentEntryRequest,
        UpdateRepaymentEntryRequest,
        BatchStatusRequest,
        CloseConflictResponse,
        BatchFailureResponse,
    ))
)]
pub struct ApiDoc;
```

Global error mapping in genossi_rest/src/lib.rs:97-113 (Phase 8):
- ServiceError::ValidationError → RestError::BadRequest → HTTP 400 (PAYO-03)
- ServiceError::EntityNotFound → RestError::NotFound → HTTP 404
- ServiceError::Conflict → RestError::Conflict → HTTP 409 (PAYO-04, race-loser)
- ServiceError::InternalError → RestError::InternalError → HTTP 500 (BL-01 Re-Read-None)
- ServiceError::PermissionDenied → RestError::Unauthorized → HTTP 401
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Implementiere mark_paid_out REST-Handler + Route + ApiDoc</name>
  <files>genossi_rest/src/repayment_entry.rs</files>
  <read_first>
    - genossi_rest/src/repayment_entry.rs (komplett — Z. 1-50 fuer Imports, Z. 144-177 fuer bestehende Handler-Struktur, Z. 302-316 fuer generate_route, Z. 318-338 fuer ApiDoc)
    - genossi_rest/src/repayment_phase.rs Z. 234-268 (Vorlage `open_repayment_phase`) + Z. 337-351 (`generate_route` mit `.route("/{id}/open", ...)`)
    - .planning/phases/09-auszahlungs-buchung-atomisch-auditiert/09-RESEARCH.md §"Frage 7: REST-Router-Registration" (Z. 176-243) + §"REST Handler Sketch" (Z. 606-674)
    - .planning/phases/09-auszahlungs-buchung-atomisch-auditiert/09-PATTERNS.md §"genossi_rest/src/repayment_entry.rs — Handler + Route + ApiDoc"
  </read_first>
  <action>
**Edit 1 — Handler-Funktion `mark_paid_out` am Ende des Handler-Blocks (nach `batch_toggle_status` Handler, vor `generate_route`):**

Vollstaendiger Code-Block (~35 LOC):

```rust
#[instrument(skip(rest_state))]
#[utoipa::path(
    post,
    tag = "RepaymentEntries",
    path = "/{id}/mark-paid-out",
    params(("id" = Uuid, Path, description = "RepaymentEntry ID")),
    responses(
        (status = 200,
         description = "Entry marked as PaidOut. Cascade: MemberAction::Verkauf created with \
                       shares_change=-N (where N=share_count_to_pay_out), Member.current_shares \
                       reduced by N, Member.action_count incremented by 1. All three writes commit \
                       in a single SQLite transaction with shared audit-process \
                       'repayment-entry.mark-paid-out'. Final per PAYO-04 (no toggle-back).",
         body = RepaymentEntryTO),
        (status = 400,
         description = "Validation Error (PAYO-03): Member.current_shares < entry.share_count_to_pay_out. \
                       Response body lists field='share_count_to_pay_out' with both values."),
        (status = 401, description = "Unauthorized (missing or invalid admin auth)"),
        (status = 404, description = "Entry not found or soft-deleted (checked before any write)"),
        (status = 409,
         description = "Conflict: entry status is not Open/Contacted (PAYO-04 — PaidOut is final), \
                       OR phase status is not Open (Defense-in-Depth), \
                       OR concurrent race produced version-mismatch on the entry update \
                       (loser of tokio::join! race per SC #5)."),
        (status = 500,
         description = "Internal consistency error: Re-Read after audited_update! returned None \
                       (Phase-8 BL-01 pattern — same-Tx invariant broken; should never happen \
                       in correctly-functioning DAO layer)."),
    ),
)]
pub async fn mark_paid_out<RestState: RestStateDef + RepaymentEntryRestState>(
    rest_state: State<RestState>,
    Extension(context): Extension<Context>,
    Path(id): Path<Uuid>,
) -> Response {
    error_handler(
        (async {
            let auth = crate::extract_auth_context(Some(context))?;
            let entry = rest_state
                .repayment_entry_service()
                .mark_paid_out(id, auth)
                .await?;
            let to = RepaymentEntryTO::from(&entry);
            Ok(Response::builder()
                .status(200)
                .header("Content-Type", "application/json")
                .body(Body::new(serde_json::to_string(&to)?))
                .unwrap())
        })
        .await,
    )
}
```

**WICHTIG bei utoipa-Annotation:**
- KEIN `request_body = ...`-Eintrag (CONTEXT D-03; Pitfall #8 — utoipa meckert sonst bzgl. fehlender Schema).
- `path = "/{id}/mark-paid-out"` — NICHT mit fuehrendem `/api/repayment-entry/` (das wird via Router-Nesting in `genossi_rest/src/lib.rs` praefixiert; siehe bestehende `path = "/{id}"`-Eintraege in derselben Datei).
- `tag = "RepaymentEntries"` — exakt der bereits in der Datei verwendete Tag (Konsistenz fuer Swagger-UI-Gruppierung).

**Edit 2 — Route-Registration in `generate_route()` (am Ende der Builder-Chain):**

Bestehende Route-Definition (Z. 302-316) erweitern. Nach der letzten existierenden `.route(...)` (also nach dem `/{id}`-Block mit get/put/delete) eine NEUE Zeile anhaengen. **WICHTIG (D-07): EXAKT EINE neue Route, KEINE Batch-Variante (`/batch-mark-paid-out` o.ae.) — Batch ist explizit deferred zu Phase 12.**

```rust
pub fn generate_route<RestState: RestStateDef + RepaymentEntryRestState>() -> Router<RestState> {
    Router::new()
        .route("/batch-status", post(batch_toggle_status::<RestState>))
        .route(
            "/",
            get(list_repayment_entries::<RestState>).post(create_repayment_entry::<RestState>),
        )
        .route(
            "/{id}",
            get(get_repayment_entry::<RestState>)
                .put(update_repayment_entry::<RestState>)
                .delete(delete_repayment_entry::<RestState>),
        )
        // Phase 9 (PAYO-01 / D-07): Single-only Action-Endpoint fuer atomare
        // Auszahlungs-Cascade. KEINE Batch-Variante (deferred zu Phase 12 —
        // Cascade ist sicherheitskritisch/irreversibel + UI-05 Confirm-Dialog
        // ist pro Eintrag konzipiert).
        // Reihenfolge ist hier egal (Axum matcht nach Pfad-Spezifitaet —
        // `/{id}/mark-paid-out` ist spezifischer als `/{id}`), aber Konvention
        // setzt Action-Endpoints ans Ende. Vorbild: repayment_phase.rs::generate_route
        // mit `.route("/{id}/open", ...)` / `.route("/{id}/close", ...)`.
        .route("/{id}/mark-paid-out", post(mark_paid_out::<RestState>))
}
```

**Edit 3 — ApiDoc-Erweiterung (Z. 318-338, paths-Liste):**

In der `#[openapi(paths(...))]`-Liste der `ApiDoc`-Struct nach `batch_toggle_status,` eine NEUE Zeile einfuegen:

```rust
#[derive(OpenApi)]
#[openapi(
    paths(
        list_repayment_entries,
        create_repayment_entry,
        get_repayment_entry,
        update_repayment_entry,
        delete_repayment_entry,
        batch_toggle_status,
        // Phase 9 (PAYO-01):
        mark_paid_out,
    ),
    components(schemas(
        RepaymentEntryTO,
        RepaymentEntryStatusTO,
        CreateRepaymentEntryRequest,
        UpdateRepaymentEntryRequest,
        BatchStatusRequest,
        CloseConflictResponse,
        BatchFailureResponse,
    ))
)]
pub struct ApiDoc;
```

**Keine neuen `components(schemas(...))`-Eintraege noetig** — Response = bestehender `RepaymentEntryTO`, kein neues Request-/Response-DTO.

**Compile-Verifikation:** Nach diesen 3 Edits muss `cargo build -p genossi_rest` clean kompilieren. Wenn ein Compile-Fehler bzgl. `Body::new(serde_json::to_string(&to)?)` auftritt: das ist das Standardmuster aus `open_repayment_phase`; pruefe `use`-Statements am Anfang der Datei (`use axum::body::Body;`, `use axum::http::Response;`, `use axum::Json;` etc. — sollten alle bereits vorhanden sein wegen bestehender Handler).

Wenn `RepaymentEntryTO::from(&entry)`-Konvertierung nicht existiert (sollte aber, weil `update_repayment_entry`-Handler das gleich macht): pruefe `genossi_rest_types/src/lib.rs` fuer `impl From<&RepaymentEntry> for RepaymentEntryTO`.

`cargo build -p genossi_bin` wird WEITERHIN fehlschlagen (Plan 09-03 heilt das per DI-Wiring).
  </action>
  <verify>
    <automated>cargo build -p genossi_rest 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - genossi_rest/src/repayment_entry.rs contains "pub async fn mark_paid_out<RestState"
    - genossi_rest/src/repayment_entry.rs contains "path = \"/{id}/mark-paid-out\""
    - genossi_rest/src/repayment_entry.rs contains "tag = \"RepaymentEntries\""
    - genossi_rest/src/repayment_entry.rs contains ".route(\"/{id}/mark-paid-out\", post(mark_paid_out::<RestState>))"
    - genossi_rest/src/repayment_entry.rs contains "repayment_entry_service()" AND "mark_paid_out(id, auth)" (in same handler)
    - D-07 Sanity-Check: KEINE Batch-Mark-Paid-Out-Route: `grep -E "(batch.*mark.paid.out|mark.paid.out.*batch)" /home/neosam/programming/rust/projects/genossi3/genossi_rest/src/repayment_entry.rs | wc -l` outputs 0
    - utoipa-Annotation listet alle 5 status codes: `grep -E "status = (200|400|401|404|409|500)" genossi_rest/src/repayment_entry.rs | wc -l` outputs at least 5 (the new handler alone adds 6 lines: 200, 400, 401, 404, 409, 500 — counting all handlers in the file)
    - genossi_rest/src/repayment_entry.rs ApiDoc paths-Liste contains "mark_paid_out," (mit comma, weil als 7. Eintrag in der Liste)
    - utoipa-Annotation enthaelt KEIN `request_body =`: `grep -A 20 "fn mark_paid_out" genossi_rest/src/repayment_entry.rs | grep -c "request_body" | head -1` outputs 0
    - `cargo build -p genossi_rest` exits 0
    - `cargo test -p genossi_rest --lib repayment_entry 2>&amp;1 | grep "test result"` enthaelt "0 failed" (kein Test-Regress bei bestehenden REST-Tests; ggf. 0 tests run wenn keine in dieser Datei)
  </acceptance_criteria>
  <done>
    - mark_paid_out-Handler ist als POST-Route `/{id}/mark-paid-out` registriert.
    - OpenAPI-Doku enthaelt alle 5 Status-Codes (200/400/401/404/409/500).
    - genossi_rest kompiliert clean.
    - Swagger-UI wuerde den Endpoint anzeigen (verifiziert ueber ApiDoc-paths-Liste, nicht direkt durch UI-Test in diesem Plan).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/curl → POST `/api/repayment-entry/{id}/mark-paid-out` | Public Endpoint (auth-required via OIDC), erste Verteidigungslinie |
| REST-Handler → Service | Authentifizierung via OIDC; `extract_auth_context` propagiert Auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-02-01 | Elevation of Privilege | mark_paid_out REST-Handler | MEDIUM | mitigate | `extract_auth_context(Some(context))?` als erster Schritt im Handler — propagiert Auth zum Service. ADMIN_PRIVILEGE-Check passiert in Plan 09-01 Service-Layer. Wenn Plan 09-02 versehentlich `context` nicht propagiert (z.B. `auth = ()` hardcoded), fehlt der Permission-Check downstream. Verify: grep zeigt `auth` Variable wird an `mark_paid_out(id, auth)` uebergeben. |
| T-09-02-02 | Repudiation (Audit-Bypass via direkte REST-Manipulation) | REST-Layer | LOW | mitigate (in service layer) | REST-Layer ruft NUR Service auf; Audit-Trail kommt aus Service via audited_*!-Macros. REST kann den Audit nicht umgehen — alle Schreibvorgaenge laufen ueber `repayment_entry_service.mark_paid_out`. Acceptance: REST-Handler hat KEINE direkten `*_dao.*`-Aufrufe. |
| T-09-02-03 | Information Disclosure (Verbose Error Body) | REST-Layer Error-Mapping | LOW | accept | Globales `From<ServiceError> for RestError` mapped Errors zu HTTP Status + minimaler Body. Internal-Error-Body enthaelt formatted message ("Re-Read returned None for Member ..."). Diese Message koennte bei einem 500er an den Client gelangen. Rationale: 500er ist ein interner Bug, Operator braucht den Kontext via Logs UND Response; kein externer Angreifer-Wert in der Message (kein UUIDs anderer User, keine Secrets). |
| T-09-02-04 | Denial of Service (Unaufgeforderter Cascade) | REST-Endpoint Rate-Limiting | LOW | accept | Globales `tower_governor` Rate-Limiting (siehe `genossi_bin/src/lib.rs` `api_rate_layer`) deckt diesen Endpoint mit ab (60 burst, 1/sec refill). Da Endpoint admin-only ist, ist DoS-Risiko bereits durch Auth-Gate eingegrenzt. |

</threat_model>

<verification>

Build:
```bash
cargo build -p genossi_rest
cargo test -p genossi_rest --lib repayment_entry
```

Endpoint-Verifikation via grep (statt CLI-Start):
```bash
grep -E "^\.route\(\"/" /home/neosam/programming/rust/projects/genossi3/genossi_rest/src/repayment_entry.rs
grep -c "fn mark_paid_out" /home/neosam/programming/rust/projects/genossi3/genossi_rest/src/repayment_entry.rs
# Expected: >= 1
```

Manueller Swagger-Test (deferred zu Plan 09-04 wenn Server gestartet werden kann):
- Server starten via `cargo run --bin genossi`
- GET http://localhost:3000/swagger-ui/
- Suche nach Tag "RepaymentEntries" — sollte einen neuen Eintrag `POST /api/repayment-entry/{id}/mark-paid-out` zeigen mit 5 Status-Codes in der Response-Sektion.

</verification>

<success_criteria>

- [ ] Handler `mark_paid_out` implementiert mit korrekter utoipa-Annotation (alle 5 Status-Codes)
- [ ] Route `/{id}/mark-paid-out` als POST in `generate_route()` registriert
- [ ] `mark_paid_out` in ApiDoc paths-Liste
- [ ] genossi_rest kompiliert clean
- [ ] Bestehende REST-Tests gruen (kein Regress)
- [ ] Kein `request_body`-Eintrag in der utoipa-Annotation (Action-Endpoint)
- [ ] genossi_bin compile schlaegt erwartet fehl bis Plan 09-03

</success_criteria>

<output>
Nach Plan-Ausfuehrung: `.planning/phases/09-auszahlungs-buchung-atomisch-auditiert/09-02-SUMMARY.md` mit Diff-Statistik, `cargo build -p genossi_rest`-Output, Handler-Funktionssignatur-Excerpt, und Begruendung etwaiger Abweichungen.
</output>
