---
phase: 10-massenmail-anbindung-template-variablen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/sqlite/20260601000000_extend_mail_job_template_phase.sql
  - genossi_mail/src/dao.rs
  - genossi_mail/src/dao_sqlite.rs
autonomous: true
requirements:
  - MAIL-01
  - MAIL-02
decisions_addressed:
  - D-03 — Job-weit repayment_phase_id (Migration add column auf mail_jobs)
  - D-12 — Migration mail_job.template_id (Worker referenziert es fuer MemberDocument)

must_haves:
  truths:
    - "D-03: Job-weite repayment_phase_id als Option<Uuid> in MailJob und mail_jobs-Tabelle (BLOB NULL)"
    - "D-12: Job-weite template_id als Option<Uuid> in MailJob und mail_jobs-Tabelle (BLOB NULL); Worker referenziert es fuer MemberDocument"
    - "MailJob persistiert template_id und repayment_phase_id (Option<Uuid>)"
    - "Bestehende MailJob-Zeilen lesen weiterhin (NULL in beiden neuen Spalten = backward-kompatibel)"
    - "DAO INSERT speichert beide neuen Felder; find_by_id liest beide Felder zurueck"
  artifacts:
    - path: "migrations/sqlite/20260601000000_extend_mail_job_template_phase.sql"
      provides: "ALTER TABLE mail_jobs ADD COLUMN template_id BLOB NULL + repayment_phase_id BLOB NULL"
      contains: "ALTER TABLE mail_jobs ADD COLUMN template_id BLOB NULL"
    - path: "genossi_mail/src/dao.rs"
      provides: "MailJob struct mit template_id + repayment_phase_id"
      contains: "pub template_id: Option<Uuid>"
    - path: "genossi_mail/src/dao_sqlite.rs"
      provides: "MailJobDb + INSERT/SELECT um neue Spalten erweitert"
      contains: "template_id"
  key_links:
    - from: "MailJobDaoSqlite::create"
      to: "mail_jobs.template_id, mail_jobs.repayment_phase_id"
      via: "sqlx::query bind"
      pattern: "INSERT INTO mail_jobs.*template_id.*repayment_phase_id"
    - from: "MailJobDb::TryFrom"
      to: "MailJob struct"
      via: "parse_optional_uuid"
      pattern: "parse_optional_uuid\\(.*template_id"
---

<objective>
Migration und DAO-Erweiterung fuer mail_job: zwei neue optionale BLOB-Felder template_id und repayment_phase_id, sodass spaetere Plans (10.03 Service-Signatur, 10.06 Worker) diese Job-weite Repayment-Context-Information ablegen koennen.

Purpose: Worker braucht template_id fuer MemberDocument-Referenz (D-12) und repayment_phase_id fuer Aggregations-Logik (D-03/D-04).
Output: Migration + erweiterte MailJob-Entity + erweitertes SQLite-Schema-Mapping.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-massenmail-anbindung-template-variablen/10-CONTEXT.md
@.planning/phases/10-massenmail-anbindung-template-variablen/10-PATTERNS.md

<!-- Migration-Style-Analog -->
@migrations/sqlite/20260506000000_add_code_to_helper_token.sql

<!-- MailJob Entity zum Erweitern -->
@genossi_mail/src/dao.rs

<!-- SQLite-Impl mit parse_optional_uuid Helper -->
@genossi_mail/src/dao_sqlite.rs

<interfaces>
<!-- Existing parse_optional_uuid helper (genossi_mail/src/dao_sqlite.rs:46-52) -->
```rust
fn parse_optional_uuid(value: &Option<Vec<u8>>) -> Result<Option<Uuid>, MailDaoError> {
    match value {
        Some(bytes) => Ok(Some(parse_uuid(bytes)?)),
        None => Ok(None),
    }
}
```

<!-- Existing MailJob struct (genossi_mail/src/dao.rs:27-40) -->
```rust
pub struct MailJob {
    pub id: Uuid,
    pub created: time::PrimitiveDateTime,
    pub deleted: Option<time::PrimitiveDateTime>,
    pub version: Uuid,
    pub subject: Arc<str>,
    pub body: Arc<str>,
    pub status: Arc<str>,
    pub total_count: i64,
    pub sent_count: i64,
    pub failed_count: i64,
    pub reply_to_inbound_mail_id: Option<Uuid>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration anlegen (template_id, repayment_phase_id)</name>
  <files>migrations/sqlite/20260601000000_extend_mail_job_template_phase.sql</files>
  <read_first>
    - migrations/sqlite/20260506000000_add_code_to_helper_token.sql (ADR-Header-Pattern, forward-only)
    - migrations/sqlite/20260403000003_create_mail_jobs_table.sql (current mail_jobs schema)
    - .planning/phases/10-massenmail-anbindung-template-variablen/10-CONTEXT.md (D-03, D-12)
    - .planning/phases/10-massenmail-anbindung-template-variablen/10-PATTERNS.md "Migration: mail_job + mail_recipient" section
  </read_first>
  <action>
Erstelle neue Migration `migrations/sqlite/20260601000000_extend_mail_job_template_phase.sql` mit exakt diesem Inhalt:

```sql
-- ADR Phase 10 (D-12 / D-03): mail_jobs gets two optional refs so the worker can
-- (a) record which template was used (template_id -> MemberDocument.template_id)
-- and (b) merge job-wide repayment context into per-recipient render.
--
-- FK clauses are documentary only (project does not enable PRAGMA foreign_keys=ON;
-- consistent with repayment_entry migration line 4-7). ON DELETE SET NULL semantics
-- enforced at service layer: deleting a template or phase must not break audit.
--
-- Backward-compat: existing mail_jobs rows have NULL in both columns; legacy code
-- paths (test-mail, single-send, non-repayment bulk-send) keep NULL.
--
-- No down-migration: SQLite < 3.35 has no DROP COLUMN.

ALTER TABLE mail_jobs ADD COLUMN template_id BLOB NULL;
ALTER TABLE mail_jobs ADD COLUMN repayment_phase_id BLOB NULL;
```

Wichtig:
- Dateiname EXAKT `20260601000000_extend_mail_job_template_phase.sql` (Tagespraefix 2026-06-01, Zeit 00:00:00 - keine Kollision mit existierenden Migrations)
- NULL-able BLOB Spalten (kein DEFAULT noetig, SQLite faellt automatisch auf NULL)
- KEINE zusaetzlichen Indizes in dieser Migration (Performance ggf. spaeter ueber separate Migration)
- Tabellenname ist Plural `mail_jobs` (siehe `20260403000003_create_mail_jobs_table.sql`)
  </action>
  <verify>
    <automated>cargo sqlx prepare --workspace --check 2>/dev/null || cargo build -p genossi_mail 2>&1 | grep -v warning | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f migrations/sqlite/20260601000000_extend_mail_job_template_phase.sql`
    - Grep: `grep -c "ALTER TABLE mail_jobs ADD COLUMN template_id BLOB NULL" migrations/sqlite/20260601000000_extend_mail_job_template_phase.sql` returns 1
    - Grep: `grep -c "ALTER TABLE mail_jobs ADD COLUMN repayment_phase_id BLOB NULL" migrations/sqlite/20260601000000_extend_mail_job_template_phase.sql` returns 1
    - Grep: `grep -c "DROP COLUMN\|DROP TABLE" migrations/sqlite/20260601000000_extend_mail_job_template_phase.sql` returns 0 (no down-migration / no destructive ops)
    - `cargo build -p genossi_mail` succeeds (sqlx migrations are applied compile-time only via runtime migration system, no breakage expected)
  </acceptance_criteria>
  <done>Migration-Datei vorhanden, beide ALTER-Statements present, kein DROP/down-Migration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MailJob struct + SQLite-Impl Erweiterung</name>
  <files>genossi_mail/src/dao.rs, genossi_mail/src/dao_sqlite.rs</files>
  <read_first>
    - genossi_mail/src/dao.rs (MailJob lines 27-40 zum Erweitern)
    - genossi_mail/src/dao_sqlite.rs (MailJobDb lines 60-73, TryFrom 75-94, INSERT 115-131, find_by_id, all)
    - .planning/phases/10-massenmail-anbindung-template-variablen/10-PATTERNS.md sections "MailJob Struct" and "MailJobDaoSqlite Erweiterung"
  </read_first>
  <behavior>
    - Test 1 (in genossi_mail/src/dao_sqlite.rs Tests-Modul oder separat): Roundtrip — create MailJob mit template_id=Some(uuid), repayment_phase_id=Some(uuid) -> find_by_id -> beide Felder identisch ausgelesen.
    - Test 2: Roundtrip — create MailJob mit template_id=None, repayment_phase_id=None -> find_by_id -> beide Felder None.
    - Diese Roundtrip-Tests laufen gegen `sqlite::memory:`-Pool (Pattern bereits in dao_sqlite.rs etabliert; falls noch keine `#[cfg(test)] mod tests` existiert, einer wird angelegt mit `setup_pool()`-Helper - oder Tests werden in `genossi_bin/tests/e2e_tests.rs` als E2E-Smoke-Test angehangen).
  </behavior>
  <action>
**Schritt 1 (genossi_mail/src/dao.rs):** Erweitere die `MailJob`-Struktur (lines 27-40) um exakt diese zwei neuen Felder am Ende (positional nach `reply_to_inbound_mail_id`):

```rust
pub struct MailJob {
    pub id: Uuid,
    pub created: time::PrimitiveDateTime,
    pub deleted: Option<time::PrimitiveDateTime>,
    pub version: Uuid,
    pub subject: Arc<str>,
    pub body: Arc<str>,
    pub status: Arc<str>,
    pub total_count: i64,
    pub sent_count: i64,
    pub failed_count: i64,
    pub reply_to_inbound_mail_id: Option<Uuid>,
    // Phase 10 D-12: optional template reference (NULL for ad-hoc / non-template mails)
    pub template_id: Option<Uuid>,
    // Phase 10 D-03: optional repayment-phase reference for worker-side aggregation
    pub repayment_phase_id: Option<Uuid>,
}
```

**Schritt 2 (genossi_mail/src/dao_sqlite.rs):**

(a) Erweitere `MailJobDb` (lines 60-73) um zwei Felder am Ende:
```rust
struct MailJobDb {
    // ... existing fields stay
    reply_to_inbound_mail_id: Option<Vec<u8>>,
    template_id: Option<Vec<u8>>,
    repayment_phase_id: Option<Vec<u8>>,
}
```

(b) Erweitere `impl TryFrom<&MailJobDb> for MailJob` (lines 75-94) — fuege am Ende der `Ok(MailJob { ... })` Initialisierung hinzu:
```rust
template_id: parse_optional_uuid(&db.template_id)?,
repayment_phase_id: parse_optional_uuid(&db.repayment_phase_id)?,
```

(c) Erweitere alle SELECT-Statements (3 Stellen: `find_by_id`, `all`, evtl. weitere). Fuege `template_id, repayment_phase_id` als letzte Spalten in der SELECT-Liste ein. Beispiel:
```rust
"SELECT id, created, deleted, version, subject, body, status, \
 total_count, sent_count, failed_count, reply_to_inbound_mail_id, \
 template_id, repayment_phase_id \
 FROM mail_jobs WHERE id = ?"
```

(d) Erweitere `INSERT` (lines 115-131): VALUES-Liste bekommt zwei zusaetzliche `?`-Platzhalter; bind() zwei zusaetzliche Aufrufe am Ende. Konvertiere `Option<Uuid>` zu `Option<Vec<u8>>` via `.as_ref().map(|u| u.as_bytes().to_vec())` (Pattern bereits etabliert fuer `reply_to_inbound_mail_id`):

```rust
"INSERT INTO mail_jobs (id, created, deleted, version, subject, body, status, \
 total_count, sent_count, failed_count, reply_to_inbound_mail_id, \
 template_id, repayment_phase_id) \
 VALUES (?, ?, NULL, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)"
```
(13 placeholders ohne `deleted`-NULL-Position).

(e) `update()`-Methode (lines 166-183) wird NICHT erweitert — `template_id` und `repayment_phase_id` sind immutable nach create (siehe PATTERNS.md "update() does NOT need template_id/repayment_phase_id columns").

**Schritt 3:** Schreibe 2 Unit-Tests im `#[cfg(test)] mod tests` von `genossi_mail/src/dao_sqlite.rs` (falls noch nicht existent, lege ihn unter dem letzten Code-Block an mit `setup_pool` von `sqlx::sqlite::SqlitePoolOptions`):

```rust
#[tokio::test]
async fn test_mail_job_roundtrip_with_template_and_phase_ids() {
    let pool = setup_pool().await;  // helper im Test-Modul
    let dao = MailJobDaoSqlite::new(Arc::new(pool));
    let template_id = Uuid::new_v4();
    let phase_id = Uuid::new_v4();
    let job = MailJob {
        id: Uuid::new_v4(),
        created: time::PrimitiveDateTime::new(time::OffsetDateTime::now_utc().date(),
                                              time::OffsetDateTime::now_utc().time()),
        deleted: None,
        version: Uuid::new_v4(),
        subject: Arc::from("Subj"),
        body: Arc::from("Body"),
        status: Arc::from("running"),
        total_count: 0,
        sent_count: 0,
        failed_count: 0,
        reply_to_inbound_mail_id: None,
        template_id: Some(template_id),
        repayment_phase_id: Some(phase_id),
    };
    dao.create(&job).await.unwrap();
    let loaded = dao.find_by_id(job.id).await.unwrap();
    assert_eq!(loaded.template_id, Some(template_id));
    assert_eq!(loaded.repayment_phase_id, Some(phase_id));
}

#[tokio::test]
async fn test_mail_job_roundtrip_with_null_template_and_phase_ids() {
    let pool = setup_pool().await;
    let dao = MailJobDaoSqlite::new(Arc::new(pool));
    let job = MailJob { /* same as above but template_id: None, repayment_phase_id: None */ };
    dao.create(&job).await.unwrap();
    let loaded = dao.find_by_id(job.id).await.unwrap();
    assert_eq!(loaded.template_id, None);
    assert_eq!(loaded.repayment_phase_id, None);
}
```

Falls bereits ein `setup_pool` oder `setup_test_pool` Helper existiert (grep im Test-Modul oder genossi_dao_impl_sqlite/src/sqlite_tests.rs), nutze diesen. Sonst lege ihn lokal an. Migrations werden via `sqlx::migrate!("../../migrations/sqlite").run(&pool).await.unwrap();` angewandt - exakter Pfad relativ zum Cargo-Manifest ist `../migrations/sqlite`.

**Wichtig:** Alle bestehenden Aufrufer von `MailJob { ... }` (sowohl in `service.rs` Z. 268-280 als auch in evtl. Test-Code) brechen jetzt mit "missing field". Diese werden in Plan 10.03 (MailService::create_job) gefixt. In diesem Plan: nur kompiliere `cargo build -p genossi_mail` mit `cargo check` und akzeptiere die Compile-Fehler in service.rs (nicht aber dao.rs/dao_sqlite.rs) — Plan 10.03 ist explizit dafuer da.

Alternative falls Cross-Cutting-Compile-Block: Setze in `service.rs:265-280` die zwei neuen Felder beim MailJob-Construct als `template_id: None, repayment_phase_id: None` als Zwischen-Stand. Plan 10.03 ersetzt diese Defaults dann mit echten Werten aus der Service-Signatur.
  </action>
  <verify>
    <automated>cargo test -p genossi_mail --lib dao_sqlite::tests::test_mail_job_roundtrip 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Grep: `grep -c "pub template_id: Option<Uuid>" genossi_mail/src/dao.rs` returns 1
    - Grep: `grep -c "pub repayment_phase_id: Option<Uuid>" genossi_mail/src/dao.rs` returns 1
    - Grep: `grep -c "template_id" genossi_mail/src/dao_sqlite.rs` returns >= 4 (struct field + TryFrom + SELECT + INSERT)
    - Grep: `grep -c "repayment_phase_id" genossi_mail/src/dao_sqlite.rs` returns >= 4
    - Command: `cargo build -p genossi_mail` succeeds (service.rs MailJob-init mit None/None Workaround damit kompiliert)
    - Command: `cargo test -p genossi_mail --lib test_mail_job_roundtrip_with_template_and_phase_ids` exit 0
    - Command: `cargo test -p genossi_mail --lib test_mail_job_roundtrip_with_null_template_and_phase_ids` exit 0
    - Grep: `grep -c "template_id" genossi_mail/src/dao_sqlite.rs` matches `INSERT INTO mail_jobs` line includes both new column names — verify: `grep -A2 'INSERT INTO mail_jobs' genossi_mail/src/dao_sqlite.rs | grep -c 'template_id'` >= 1
  </acceptance_criteria>
  <done>MailJob struct hat 2 neue Felder, SQLite-Impl persistiert sie, 2 Roundtrip-Tests gruen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service -> DAO | MailJob.create accepted from service layer; values transit untrusted until validated upstream (service-layer responsibility) |
| DAO -> DB | sqlx parameterized queries (no SQL injection vector) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | T (Tampering) | mail_jobs migration | mitigate | Forward-only migration; BLOB type matches existing PK convention; NULL-default preserves backward-compat for existing rows |
| T-10-01-02 | I (Information Disclosure) | mail_jobs.repayment_phase_id | accept | Phase-IDs are non-secret routing info; no PII exposed via this column |
| T-10-01-03 | S (Spoofing) | mail_jobs.template_id binding | mitigate | parse_optional_uuid validates BLOB-to-Uuid conversion; corrupt data returns ParseError, not a spoofed UUID |
| T-10-01-04 | R (Repudiation) | New columns NOT in audit_fields() | accept | MailJob is NOT Auditable (operational entity); template_id/repayment_phase_id are derived from already-audited entities (MailTemplate, RepaymentPhase) — no separate audit needed |
</threat_model>

<verification>
After both tasks complete:
- `cargo build -p genossi_mail` succeeds
- `cargo test -p genossi_mail --lib` succeeds, includes 2 new roundtrip tests
- `cargo clippy -p genossi_mail --all-targets` no new warnings
- `cargo fmt -- --check` clean
- File `migrations/sqlite/20260601000000_extend_mail_job_template_phase.sql` exists with both ALTER statements
</verification>

<success_criteria>
- MailJob struct has 2 new Option<Uuid> fields
- SQLite migration adds 2 new BLOB NULL columns to mail_jobs
- DAO read/write paths handle the new fields end-to-end
- 2 roundtrip tests pass (Some + None variants)
- Backward-compat: bestehende MailJob-Aufrufer kompilieren entweder ueber Default-None oder werden in Plan 10.03 gefixt
</success_criteria>

<output>
After completion, create `.planning/phases/10-massenmail-anbindung-template-variablen/10-01-SUMMARY.md`
</output>
