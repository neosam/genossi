---
phase: 11-export-pdf-csv
plan: 03
type: execute
wave: 2
depends_on:
  - 11-01-PLAN.md
  - 11-02-PLAN.md
files_modified:
  - genossi_service_impl/src/repayment_export.rs
  - genossi_service_impl/src/lib.rs
autonomous: true
requirements:
  - EXPO-01
  - EXPO-02
  - EXPO-03
  - EXPO-05
user_setup: []
tags:
  - service-impl
  - permission-funnel
  - read-only
  - audit-free
must_haves:
  truths:
    - "`RepaymentExportServiceImpl` existiert mit Deps-Trait + Struct (3 DAOs: phase, entry, member; +PermissionService + TransactionDao + PdfGenerator + template_base)"
    - "`check_admin_and_phase_status`-Permission-Funnel-Order: load_by_id (404) -> admin-check (403) -> status-check (409) — D-10"
    - "Status-Gate akzeptiert `Open|Closed`; `Preparation` -> `ServiceError::Conflict(\"phase_not_exportable\")` -> 409 — D-10"
    - "Filter-Logik in-memory: `include=open` -> {Open,Contacted}; `include=all` -> alle 3; `include=paid` -> nur PaidOut — D-01/D-02"
    - "Soft-deleted entries UND soft-deleted members werden in JEDEM Filter ausgeschlossen — D-02"
    - "D-08: Eine Zeile pro `RepaymentEntry` (1:1-Mapping zur DB, KEIN Per-Mitglied-Aggregat); zwei Entries fuer dasselbe Mitglied erscheinen als zwei Zeilen im PDF (audit-konsistente Spur, Banking-Vorstand sieht zwei separate Ueberweisungen an dieselbe IBAN)"
    - "Sort: primaer `member_number ASC`, sekundaer `entry.created ASC` — D-09"
    - "Verwendungszweck = wortwoertlich `Anteilsrückzahlung GJ {fiscal_year} {member_number} {first_name} {last_name}` mit ORIGINAL-UMLAUT 'ü' — D-04 + D-05 (Pre-Computed im Service)"
    - "D-05: KEINE ASCII-Sanitization — `ä/ö/ü/ß` bleiben in `purpose`-String erhalten; Banking-Software des Vorstands ersetzt selbst"
    - "Amount-Format: `{cents/100},{cents%100:02}` (deutsche Lokalisierung, Phase-10-D-04-Pattern OHNE `.abs()` — Domain-Constraint share_count>=0 und share_value>0 garantiert non-negativ)"
    - "Filename-Schema (Server-Generated): `auszahlung-{fiscal_year}-{include}.pdf`"
    - "Vorstand-only via `check_permission(\"admin\", ...)`; Helper -> ServiceError::PermissionDenied -> REST 403 (Plan 11.04 map_export_error)"
    - "NULL `audited_*!`-Calls (D-11/EXPO-05) verifiziert via Grep-Gate-Test im selben Modul"
    - "`tx.commit()` MUSS VOR `pdf_generator.render_repayment_list` (Pitfall #8)"
    - "Service-Layer-Mock-Test verifiziert Pitfall #2 (Funnel-Order: permission VOR status) — REVISION-Fix B2"
    - "Service-Layer-Mock-Test verifiziert D-04 Verwendungszweck wortwoertlich mit Member `Müller` -> purpose enthaelt `Anteilsrückzahlung GJ 2026 1234 Hans Müller` — REVISION-Fix W6"
    - "Service-Layer-Mock-Test verifiziert Include-Filter-Counts direkt fuer alle 3 Varianten (Open/All/Paid) — REVISION-Fix W1"
  artifacts:
    - path: "genossi_service_impl/src/repayment_export.rs"
      provides: "RepaymentExportServiceImpl + RepaymentExportServiceDeps-Trait + Permission-Funnel + Service-Layer-Mock-Tests + Grep-Gate-Test"
      min_lines: 400
    - path: "genossi_service_impl/src/lib.rs"
      provides: "Modul-Deklaration `pub mod repayment_export;`"
      contains: "pub mod repayment_export"
  key_links:
    - from: "genossi_service_impl/src/repayment_export.rs::export"
      to: "genossi_service_impl/src/pdf_generation.rs::render_repayment_list"
      via: "self.pdf_generator.render_repayment_list(...)"
      pattern: "render_repayment_list"
    - from: "genossi_service_impl/src/repayment_export.rs::check_admin_and_phase_status"
      to: "genossi_service/src/permission.rs::PermissionService::check_permission"
      via: "self.permission_service.check_permission(ADMIN_PRIVILEGE, ...)"
      pattern: "check_permission\\(ADMIN_PRIVILEGE"
---

<objective>
Wave-2-Kern-Impl: Service-Implementation `RepaymentExportServiceImpl` mit Permission-Funnel `check_admin_and_phase_status` (D-10/D-11), DAO-Read-Pipeline (Phase + Entries + N+1-Member-Reads in derselben Tx — Discretion N+1), In-Memory-Include-Filter (D-01/D-02), Sort vor Enrichment (D-09), Verwendungszweck-Pre-Computing mit ORIGINAL-UMLAUT `Anteilsrückzahlung` (D-04 + D-05), Euro-Format-Pre-Computing (Phase-10-D-04-Pattern OHNE `.abs()`), tx.commit() VOR PdfGenerator-Call (Pitfall #8) und PdfGenerator-Render-Aufruf. Plus Grep-Gate-Test fuer EXPO-05 UND Service-Layer-Mock-Tests fuer Permission-Funnel-Order (Pitfall #2), Verwendungszweck-Wortlaut (D-04 mit Umlaut) und Include-Filter-Counts (D-01/D-02).

Purpose: Trennt Trait-Definition (Plan 11.02) von konkreter Logik. Das ist die zentrale Plan, der alle 4 Requirements (EXPO-01..03+05) als Service-Logik manifestiert. Die Service-Layer-Mock-Tests sind verbindlich, weil unter `mock_auth`-Feature die E2E-Tests in Plan 11.06 KEINEN non-admin-Auth-Pfad herstellen koennen (mock_auth-Middleware injiziert IMMER admin-MockContext) — somit ist Pitfall-#2-Verifikation ohne Mock-Test ein No-op.

Output: 1 neue Datei (`genossi_service_impl/src/repayment_export.rs` ~500-650 LOC) + 1 Edit (`genossi_service_impl/src/lib.rs` Modul-Deklaration). Vorbild: `genossi_service_impl/src/attendance_export.rs` (1199 LOC).
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-export-pdf-csv/11-CONTEXT.md
@.planning/phases/11-export-pdf-csv/11-RESEARCH.md
@.planning/phases/11-export-pdf-csv/11-PATTERNS.md
@.planning/phases/11-export-pdf-csv/11-01-PLAN.md
@.planning/phases/11-export-pdf-csv/11-02-PLAN.md

@genossi_service_impl/src/attendance_export.rs
@genossi_service_impl/src/lib.rs
@genossi_service_impl/src/pdf_generation.rs
@genossi_service/src/repayment_export.rs

<interfaces>
<!-- Existing types this plan depends on. -->

From genossi_dao/src/repayment_phase.rs:
```rust
pub enum RepaymentPhaseStatus { Preparation, Open, Closed }
pub struct RepaymentPhaseEntity {
    pub id: Uuid,
    pub fiscal_year: i32,
    pub share_value: i64, // cents
    pub status: RepaymentPhaseStatus,
    pub opened_at: Option<PrimitiveDateTime>,
    pub closed_at: Option<PrimitiveDateTime>,
    pub created: PrimitiveDateTime,
    pub deleted: Option<PrimitiveDateTime>,
    pub version: Uuid,
}
#[async_trait]
pub trait RepaymentPhaseDao {
    type Transaction: Transaction;
    async fn find_by_id(&self, id: Uuid, tx: Self::Transaction)
        -> Result<Option<RepaymentPhaseEntity>, DaoError>;
    // ...
}
```

From genossi_dao/src/repayment_entry.rs:
```rust
pub enum RepaymentEntryStatus { Open, Contacted, PaidOut }
pub struct RepaymentEntryEntity {
    pub id: Uuid,
    pub member_id: Uuid,
    pub phase_id: Uuid,
    pub share_count_to_pay_out: i32,
    pub status: RepaymentEntryStatus,
    pub created: PrimitiveDateTime,
    pub deleted: Option<PrimitiveDateTime>,
    pub version: Uuid,
}
#[async_trait]
pub trait RepaymentEntryDao {
    type Transaction: Transaction;
    async fn find_by_phase_id(&self, phase_id: Uuid, tx: Self::Transaction)
        -> Result<Vec<RepaymentEntryEntity>, DaoError>;
    // ...
}
```

From genossi_dao/src/member.rs:
```rust
pub struct MemberEntity {
    pub id: Uuid,
    pub member_number: i64,
    pub first_name: Arc<str>,
    pub last_name: Arc<str>,
    pub bank_account: Option<Arc<str>>, // = IBAN, D-06: None -> empty column
    pub current_shares: i32,
    pub deleted: Option<PrimitiveDateTime>,
    // ...
}
#[async_trait]
pub trait MemberDao {
    type Transaction: Transaction;
    async fn find_by_id(&self, id: Uuid, tx: Self::Transaction)
        -> Result<Option<MemberEntity>, DaoError>; // default-impl filters deleted IS NULL
}
```

From genossi_service_impl/src/pdf_generation.rs (Plan 11.01 created):
```rust
pub struct RepaymentExportRow {
    pub member_number: i64,
    pub name: String,
    pub iban: String,
    pub share_count: i32,
    pub amount_str: String,
    pub purpose: String,
}
impl PdfGenerator {
    pub fn render_repayment_list(
        &self,
        template_path: &str,
        template_base: &Path,
        phase: &RepaymentPhaseEntity,
        rows: &[RepaymentExportRow],
    ) -> Result<Vec<u8>, ServiceError>;
}
```

From genossi_service/src/repayment_export.rs (Plan 11.02 created):
```rust
pub enum ExportFormat { Pdf }
pub enum ExportInclude { Open, All, Paid }
pub struct RepaymentExport { pub bytes: Vec<u8>, pub content_type: &'static str, pub filename: String }
#[async_trait]
pub trait RepaymentExportService {
    type Context: Clone + Debug + Send + Sync + 'static;
    type Transaction: Transaction;
    async fn export(&self, phase_id: Uuid, format: ExportFormat, include: ExportInclude,
                    context: Authentication<Self::Context>) -> Result<RepaymentExport, ServiceError>;
}
```

<!-- New types this plan creates (consumed by plans 11.04 / 11.05). -->

To be created in genossi_service_impl/src/repayment_export.rs:
```rust
pub trait RepaymentExportServiceDeps: Send + Sync + 'static {
    type Context: Clone + Debug + Send + Sync + 'static;
    type Transaction: Transaction;
    type RepaymentPhaseDao: RepaymentPhaseDao<Transaction = Self::Transaction> + Send + Sync;
    type RepaymentEntryDao: RepaymentEntryDao<Transaction = Self::Transaction> + Send + Sync;
    type MemberDao: MemberDao<Transaction = Self::Transaction> + Send + Sync;
    type PermissionService: PermissionService<Context = Self::Context> + Send + Sync;
    type TransactionDao: TransactionDao<Transaction = Self::Transaction> + Send + Sync;
}

pub struct RepaymentExportServiceImpl<Deps: RepaymentExportServiceDeps> {
    pub transaction_dao: Arc<Deps::TransactionDao>,
    pub permission_service: Arc<Deps::PermissionService>,
    pub repayment_phase_dao: Arc<Deps::RepaymentPhaseDao>,
    pub repayment_entry_dao: Arc<Deps::RepaymentEntryDao>,
    pub member_dao: Arc<Deps::MemberDao>,
    pub pdf_generator: Arc<PdfGenerator>,
    pub template_base: Arc<PathBuf>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RepaymentExportServiceImpl + Permission-Funnel + export() + Grep-Gate-Test + Service-Layer-Mock-Tests</name>
  <files>
    genossi_service_impl/src/repayment_export.rs (NEW)
    genossi_service_impl/src/lib.rs (MODIFY)
  </files>
  <read_first>
    - genossi_service_impl/src/attendance_export.rs (Z. 1-240 fuer Imports + Konstanten + Deps-Trait + Struct + Permission-Funnel + export()-Body als 1:1 Pattern-Vorlage; Z. 1167-1198 fuer Grep-Gate-Test-Pattern; Z. 363-784 fuer den KOMPLETTEN Test-Modul-Aufbau inkl. `mock!`-Blocks, `TestTransaction`, `TestContext`, `TestDeps`, `build_service`, `tx_dao_with_commit`/`tx_dao_no_commit`, `assembly_in_status`, sowie die Permission-Funnel-Tests `non_admin_returns_permission_denied` (Z. 748-784) und `non_closed_returns_conflict_*` (Z. 697-746) als 1:1-Vorbild fuer Pitfall-#2-Mock-Test)
    - genossi_service_impl/src/lib.rs (Modul-Deklarations-Position)
    - genossi_service/src/repayment_export.rs (Trait-Definition aus Plan 11.02)
    - genossi_service_impl/src/pdf_generation.rs (render_repayment_list Signatur aus Plan 11.01)
    - genossi_dao/src/repayment_phase.rs (RepaymentPhaseStatus-Enum-Definition)
    - genossi_dao/src/repayment_entry.rs (RepaymentEntryStatus-Enum-Definition + find_by_phase_id Signatur)
    - genossi_dao/src/member.rs (MemberEntity-Felder, find_by_id Default-Impl, Mock-Defs via mockall)
    - .planning/phases/11-export-pdf-csv/11-RESEARCH.md §Pattern 1 (Permission-Funnel Z. 238-272), §Q12 (Sort+Enrichment Z. 695-745), §Q6 (Filename Z. 553-566), §Q10 (Grep-Gate-Test Z. 637-657), §Q11 (tracing Z. 660-689), §Q15 (Test-Patterns fuer Phase-6-Service-Mock-Tests)
    - .planning/phases/11-export-pdf-csv/11-PATTERNS.md §3 (Pattern Assignment fuer genossi_service_impl/src/repayment_export.rs)
  </read_first>
  <behavior>
    - Service `RepaymentExportServiceImpl<Deps>` implementiert `RepaymentExportService`-Trait
    - `check_admin_and_phase_status` lehnt non-admin mit `ServiceError::PermissionDenied` ab BEVOR Status-Gate prueft (Pitfall #2)
    - `check_admin_and_phase_status` lehnt `Preparation`-Phase mit `ServiceError::Conflict("phase_not_exportable")` ab
    - `export()` liest Phase, Entries und Members in EINER Tx; committed Tx VOR `pdf_generator.render_repayment_list` (Pitfall #8)
    - In-Memory-Filter: `include=open` haelt nur Entries mit Status `Open|Contacted`
    - In-Memory-Filter: `include=paid` haelt nur Entries mit Status `PaidOut`
    - In-Memory-Filter: `include=all` haelt alle 3 Stati
    - Soft-deleted Entries (deleted IS NOT NULL) werden in JEDEM Filter ausgeschlossen
    - Member-Read via `MemberDao::find_by_id` filtert soft-deleted Members automatisch (Default-Impl); fehlende Member werden geskippt
    - Sortierung: `member_number ASC` primaer, `entry.created ASC` sekundaer (stable sort)
    - Filename: `auszahlung-{fiscal_year}-{include}.pdf` (z.B. `auszahlung-2026-open.pdf`)
    - Verwendungszweck wortwoertlich `Anteilsrückzahlung GJ {fiscal_year} {member_number} {first_name} {last_name}` MIT ORIGINAL-UMLAUT `ü` — D-04 / D-05
    - `tracing::info!(target = "repayment_export", phase_id, fiscal_year, format, include, rows, "exporting repayment list")` wird emittiert
    - Grep-Gate-Test `no_audit_macros_used` verifiziert dass `audited_create!`/`audited_update!`/`audited_delete!` NICHT im Source-File vorkommen
    - **REVISION-Fix B2 (Pitfall #2 via Mocks):** Mock-Test `test_non_admin_on_preparation_returns_permission_denied_not_conflict` verifiziert dass non-admin auf Preparation-Phase `Err(ServiceError::PermissionDenied)` liefert (NICHT `Err(ServiceError::Conflict)`). Test-Body ist KONKRET implementiert (echte Mocks + assert!-Calls; KEIN Skeleton mit nur Kommentaren). Damit ist die Funnel-Order (permission VOR status) ohne Abhaengigkeit von einem real-running Server bewiesen.
    - **REVISION-Fix W6 (D-04/D-05 wortwoertlich):** Pure-Function-Test `test_purpose_string_preserves_umlaut_per_d04` baut einen Member mit `last_name = "Müller"`, ruft `filter_and_enrich_rows(...)` direkt auf und asserts `rows[0].purpose == "Anteilsrückzahlung GJ 2026 1234 Hans Müller"`.
    - **REVISION-Fix W1 (Include-Filter-Counts direkt):** Pure-Function-Test `test_include_filter_row_counts` mit 4 praeparierten Entries (2× Open, 1× Contacted, 1× PaidOut) und 4 Members ruft `filter_and_enrich_rows(...)` jeweils mit Include `Open`, `All`, `Paid` auf und asserts die Anzahl `rows`: Open->3, All->4, Paid->1.
    - **REVISION-Fix B1 (Self-contradictory grep gate):** Defense-in-Depth-Negative-Assertion in `test_purpose_string_preserves_umlaut_per_d04` darf den ASCII-Literal-String NICHT direkt im Source-Code enthalten. Stattdessen wird der ASCII-Variant zur Laufzeit per `format!("Anteilsr{}ckzahlung", "ue")` konstruiert. Damit ist das Acceptance-Criterion `grep -c "Anteilsrueckzahlung" ... == 0` deterministisch erfuellbar UND die Negative-Assertion bleibt wirksam.
  </behavior>
  <action>
    SCHRITT 1: Lege Datei `genossi_service_impl/src/repayment_export.rs` an. Beginne mit Imports, Konstanten und Deps-Trait — exakt analog `attendance_export.rs:1-72`, aber mit Repayment-Identifikatoren:

    ```rust
    //! Phase 11 (EXPO-01, EXPO-02, EXPO-03, EXPO-05): RepaymentExportServiceImpl.
    //!
    //! Read-only PDF-Export der Auszahlungsliste fuer eine RepaymentPhase.
    //! Vorbild: genossi_service_impl/src/attendance_export.rs (Phase 6, 1199 LOC).
    //!
    //! Anpassungen:
    //!   - D-10: Status-Gate akzeptiert Open ODER Closed (Phase 6 nur Closed)
    //!   - D-11/EXPO-05: NULL `audited_*!`-Calls (Grep-Gate-Test am Ende)
    //!   - D-12: NUR PDF-Format (kein CSV/XLSX)
    //!   - D-08: Eine Zeile pro RepaymentEntry (kein Per-Mitglied-Aggregat)
    //!   - D-09: Sort primaer member_number ASC, sekundaer created ASC
    //!   - D-04 + D-05: Verwendungszweck hardcoded "Anteilsrückzahlung GJ {fy} {mn} {fn} {ln}"
    //!                  mit ORIGINAL-Umlauten — KEINE ASCII-Sanitization.
    //!   - REVISION-Fix B3: Euro-Format ohne .abs() (Phase-10-D-04-Pattern)

    use std::path::PathBuf;
    use std::sync::Arc;

    use async_trait::async_trait;
    use uuid::Uuid;

    use genossi_dao::member::{MemberDao, MemberEntity};
    use genossi_dao::repayment_entry::{
        RepaymentEntryDao, RepaymentEntryEntity, RepaymentEntryStatus,
    };
    use genossi_dao::repayment_phase::{RepaymentPhaseDao, RepaymentPhaseEntity, RepaymentPhaseStatus};
    use genossi_dao::{Transaction, TransactionDao};
    use genossi_service::permission::{Authentication, PermissionService};
    use genossi_service::repayment_export::{
        ExportFormat, ExportInclude, RepaymentExport, RepaymentExportService,
    };
    use genossi_service::ServiceError;

    use crate::pdf_generation::{PdfGenerator, RepaymentExportRow};

    /// D-11 / Phase 6 D-13: Privilege-String konsistent mit allen anderen Vorstand-Endpoints.
    const ADMIN_PRIVILEGE: &str = "admin";

    /// D-11 Phase 6 D-18: `tracing::info!`-Target-String ersetzt Audit-Eintrag.
    const EXPORT_TARGET: &str = "repayment_export";

    pub trait RepaymentExportServiceDeps: Send + Sync + 'static {
        type Context: Clone + std::fmt::Debug + Send + Sync + 'static;
        type Transaction: Transaction;
        type RepaymentPhaseDao: RepaymentPhaseDao<Transaction = Self::Transaction> + Send + Sync;
        type RepaymentEntryDao: RepaymentEntryDao<Transaction = Self::Transaction> + Send + Sync;
        type MemberDao: MemberDao<Transaction = Self::Transaction> + Send + Sync;
        type PermissionService: PermissionService<Context = Self::Context> + Send + Sync;
        type TransactionDao: TransactionDao<Transaction = Self::Transaction> + Send + Sync;
    }

    pub struct RepaymentExportServiceImpl<Deps: RepaymentExportServiceDeps> {
        pub transaction_dao: Arc<Deps::TransactionDao>,
        pub permission_service: Arc<Deps::PermissionService>,
        pub repayment_phase_dao: Arc<Deps::RepaymentPhaseDao>,
        pub repayment_entry_dao: Arc<Deps::RepaymentEntryDao>,
        pub member_dao: Arc<Deps::MemberDao>,
        pub pdf_generator: Arc<PdfGenerator>,
        pub template_base: Arc<PathBuf>,
    }
    ```

    SCHRITT 2: Fuege Permission-Funnel-Methode `check_admin_and_phase_status` hinzu — 1:1 Pattern-Replikat aus `attendance_export.rs:100-131` mit STATUS-SET-ERWEITERUNG auf 2 Stati (D-10):

    ```rust
    impl<Deps: RepaymentExportServiceDeps> RepaymentExportServiceImpl<Deps> {
        /// D-10 / D-11 / Pitfall #2: Permission-Funnel-Order:
        /// 1. Load by id (404 if missing)
        /// 2. Admin gate (403); Authentication::Full short-circuits.
        /// 3. Status gate (409): Open ODER Closed akzeptiert; Preparation rejected.
        ///
        /// WICHTIG: Reihenfolge 1 -> 2 -> 3 verhindert Status-Information-Leak an non-admin.
        async fn check_admin_and_phase_status(
            &self,
            phase_id: Uuid,
            context: Authentication<Deps::Context>,
            tx: Deps::Transaction,
        ) -> Result<RepaymentPhaseEntity, ServiceError> {
            // 1. Load (404 if missing).
            let phase = self
                .repayment_phase_dao
                .find_by_id(phase_id, tx)
                .await?
                .ok_or(ServiceError::EntityNotFound(phase_id))?;

            // 2. Admin gate (403). Authentication::Full short-circuits (Phase-6-Pattern, Test-only).
            match &context {
                Authentication::Full => {}
                Authentication::Context(_) => {
                    self.permission_service
                        .check_permission(ADMIN_PRIVILEGE, context)
                        .await?;
                }
            }

            // 3. Status gate (409): D-10 — Open ODER Closed akzeptiert.
            match phase.status {
                RepaymentPhaseStatus::Open | RepaymentPhaseStatus::Closed => {}
                RepaymentPhaseStatus::Preparation => {
                    return Err(ServiceError::Conflict(Arc::from("phase_not_exportable")));
                }
            }

            Ok(phase)
        }
    }
    ```

    SCHRITT 3: Extrahiere eine reine (sync, side-effect-freie) Helper-Funktion `filter_and_enrich_rows`, damit Service-Layer-Pure-Function-Tests die Filter- und Verwendungszweck-Logik direkt verifizieren koennen (REVISION-Fixes W1 + W6 brauchen testbare Hooks):

    ```rust
    /// REVISION-Fix W1 + W6: Pure-Function zum Filtern und Anreichern.
    /// Aufrufer ist `export()` (s. SCHRITT 4); direkt aufrufbar in Unit-Tests.
    /// D-01/D-02 Include-Filter; D-09 Sort; D-04/D-05 Verwendungszweck mit Umlaut;
    /// REVISION-Fix B3 Euro-Format OHNE `.abs()`.
    pub(crate) fn filter_and_enrich_rows(
        phase: &RepaymentPhaseEntity,
        entry_member_pairs: Vec<(RepaymentEntryEntity, MemberEntity)>,
        include: ExportInclude,
    ) -> Vec<RepaymentExportRow> {
        let mut pairs = entry_member_pairs;

        // D-01/D-02: In-memory include-filter.
        pairs.retain(|(entry, _)| match include {
            ExportInclude::Open => matches!(
                entry.status,
                RepaymentEntryStatus::Open | RepaymentEntryStatus::Contacted
            ),
            ExportInclude::All => true, // alle 3 Stati
            ExportInclude::Paid => matches!(entry.status, RepaymentEntryStatus::PaidOut),
        });

        // D-09: Stable-sort by (member_number ASC, entry.created ASC).
        pairs.sort_by(|a, b| {
            a.1.member_number
                .cmp(&b.1.member_number)
                .then_with(|| a.0.created.cmp(&b.0.created))
        });

        // D-04 / D-05 / D-07: Pre-compute amount_str + purpose-String pro Row.
        // REVISION-Fix B3: KEIN .abs() — Phase-10-D-04-Pattern aus PATTERNS.md §S9.
        pairs
            .iter()
            .map(|(entry, m)| {
                let amount_cents = (entry.share_count_to_pay_out as i64) * phase.share_value;
                let amount_str = format!("{},{:02}", amount_cents / 100, amount_cents % 100);
                // D-04 wortwoertlich mit ORIGINAL-Umlaut `ü`; D-05 keine Sanitization.
                let purpose = format!(
                    "Anteilsrückzahlung GJ {} {} {} {}",
                    phase.fiscal_year, m.member_number, m.first_name, m.last_name
                );
                RepaymentExportRow {
                    member_number: m.member_number,
                    name: format!("{} {}", m.first_name, m.last_name),
                    // D-06/D-07: leere IBAN -> leerer String (kein Skip, kein Marker).
                    iban: m
                        .bank_account
                        .as_ref()
                        .map(|s| s.to_string())
                        .unwrap_or_default(),
                    share_count: entry.share_count_to_pay_out,
                    amount_str,
                    purpose,
                }
            })
            .collect()
    }
    ```

    SCHRITT 4: Implementiere das Trait `RepaymentExportService` fuer `RepaymentExportServiceImpl<Deps>` und delegiere Filter/Enrichment an `filter_and_enrich_rows`:

    ```rust
    #[async_trait]
    impl<Deps: RepaymentExportServiceDeps> RepaymentExportService for RepaymentExportServiceImpl<Deps> {
        type Context = Deps::Context;
        type Transaction = Deps::Transaction;

        async fn export(
            &self,
            phase_id: Uuid,
            format: ExportFormat,
            include: ExportInclude,
            context: Authentication<Self::Context>,
        ) -> Result<RepaymentExport, ServiceError> {
            // Open Tx for Phase + Entries + Members reads.
            let tx = self.transaction_dao.use_transaction(None).await?;

            // Permission-Funnel: returns Phase if admin + Status in {Open, Closed}.
            let phase = self
                .check_admin_and_phase_status(phase_id, context, tx.clone())
                .await?;

            // Read all entries for this phase (DAO filters soft-deleted via Default-Impl).
            let raw_entries: Vec<_> = self
                .repayment_entry_dao
                .find_by_phase_id(phase_id, tx.clone())
                .await?;

            // Read member per entry (N+1 — Discretion choice per RESEARCH §Q5).
            let mut entry_member_pairs: Vec<(RepaymentEntryEntity, MemberEntity)> =
                Vec::with_capacity(raw_entries.len());
            for entry in raw_entries.into_iter() {
                // D-02: Skip soft-deleted entries (defense-in-depth).
                if entry.deleted.is_some() {
                    continue;
                }
                // MemberDao::find_by_id Default-Impl filters soft-deleted (deleted IS NULL).
                let member_opt = self
                    .member_dao
                    .find_by_id(entry.member_id, tx.clone())
                    .await?;
                if let Some(member) = member_opt {
                    entry_member_pairs.push((entry, member));
                }
                // else: member was soft-deleted -> skip (D-02).
            }

            // Pitfall #8: Commit Tx BEFORE PdfGenerator::render_* (sync method, no async tx ops afterwards).
            self.transaction_dao.commit(tx).await?;

            // REVISION-Fix W1+W6+B3: Filter/Sort/Enrichment in pure function.
            let enriched_rows = filter_and_enrich_rows(&phase, entry_member_pairs, include);

            // D-18 Phase 6: tracing::info!-Pattern ersetzt Audit-Eintrag.
            tracing::info!(
                target: EXPORT_TARGET,
                phase_id = %phase_id,
                fiscal_year = phase.fiscal_year,
                format = ?format,
                include = ?include,
                rows = enriched_rows.len(),
                "exporting repayment list"
            );

            // Render PDF (sync; tx already committed).
            let bytes = match format {
                ExportFormat::Pdf => self.pdf_generator.render_repayment_list(
                    "auszahlungsliste.typ",
                    &self.template_base,
                    &phase,
                    &enriched_rows,
                )?,
            };

            // D-15 / SC #2: Server-generated filename, kein User-Input.
            let include_str = match include {
                ExportInclude::Open => "open",
                ExportInclude::All => "all",
                ExportInclude::Paid => "paid",
            };
            let filename = format!("auszahlung-{}-{}.pdf", phase.fiscal_year, include_str);

            Ok(RepaymentExport {
                bytes,
                content_type: "application/pdf",
                filename,
            })
        }
    }
    ```

    SCHRITT 5: Fuege Tests am Ende der Datei hinzu. Test-Modul ist 3-gliedrig:
    (a) Test-Infrastruktur (mock!-Blocks, TestTransaction, TestContext, TestDeps, Helper-Funktionen)
    (b) Pure-Function-Tests gegen `filter_and_enrich_rows` (REVISION-Fixes B1+W6, W1, B3)
    (c) Mock-Service-Tests gegen `service.export(...)` (REVISION-Fix B2 / Pitfall #2) UND Grep-Gate-Test.

    Pattern-Vorbild: `attendance_export.rs:363-784` — KOMPLETT 1:1 spiegeln (nur Entity-Typen austauschen). Insbesondere:
    - mock!-Blocks: Z. 403-563 (TestTxDao, TestAssemblyDao, TestAttendanceDao, TestPermissionService)
    - TestTransaction-Impl: Z. 381-392
    - TestContext-Impl: Z. 394-401
    - TestDeps-Struct: Z. 565-574
    - assembly_in_status-Helper: Z. 576-591
    - build_service-Helper: Z. 593-608
    - tx_dao_with_commit/no_commit-Helper: Z. 610-626
    - non_admin_returns_permission_denied-Test: Z. 748-784 (DAS ist die direkte Vorlage fuer B2-Fix)

    ```rust
    #[cfg(test)]
    mod tests {
        use super::*;
        use async_trait::async_trait;
        use genossi_dao::DaoError;
        use genossi_service::claim_context::ClaimContext;
        use mockall::{mock, predicate::*};
        use std::path::PathBuf;
        use time::macros::datetime;

        // ----------------------------------------------------------------------
        // Test infrastructure (hand-rolled mocks — Pattern 1:1 aus
        // attendance_export.rs:363-630).
        // ----------------------------------------------------------------------

        #[derive(Clone, Debug)]
        pub struct TestTransaction;

        #[async_trait]
        impl Transaction for TestTransaction {
            async fn begin(&mut self) -> Result<(), DaoError> {
                Ok(())
            }
            async fn commit(self) -> Result<(), DaoError> {
                Ok(())
            }
            async fn rollback(self) -> Result<(), DaoError> {
                Ok(())
            }
        }

        #[derive(Clone, Debug, PartialEq, Eq)]
        pub struct TestContext;

        impl ClaimContext for TestContext {
            fn has_claims(&self) -> bool {
                false
            }
        }

        mock! {
            pub TestTxDao {}
            #[async_trait]
            impl TransactionDao for TestTxDao {
                type Transaction = TestTransaction;
                async fn transaction(&self) -> Result<TestTransaction, DaoError>;
                async fn use_transaction(
                    &self,
                    tx: Option<TestTransaction>,
                ) -> Result<TestTransaction, DaoError>;
                async fn commit(&self, tx: TestTransaction) -> Result<(), DaoError>;
            }
        }

        mock! {
            pub TestPhaseDao {}
            #[async_trait]
            impl RepaymentPhaseDao for TestPhaseDao {
                type Transaction = TestTransaction;
                async fn dump_all(&self, tx: TestTransaction) -> Result<Arc<[RepaymentPhaseEntity]>, DaoError>;
                async fn create(&self, entity: &RepaymentPhaseEntity, process: &str, tx: TestTransaction) -> Result<(), DaoError>;
                async fn update(&self, entity: &RepaymentPhaseEntity, process: &str, tx: TestTransaction) -> Result<(), DaoError>;
                async fn all(&self, tx: TestTransaction) -> Result<Arc<[RepaymentPhaseEntity]>, DaoError>;
                async fn find_by_id(&self, id: Uuid, tx: TestTransaction) -> Result<Option<RepaymentPhaseEntity>, DaoError>;
                // Falls weitere Methoden in RepaymentPhaseDao definiert sind, hier ergaenzen
                // (Vorlage: RepaymentPhaseDao-Trait in genossi_dao/src/repayment_phase.rs lesen).
            }
        }

        mock! {
            pub TestEntryDao {}
            #[async_trait]
            impl RepaymentEntryDao for TestEntryDao {
                type Transaction = TestTransaction;
                async fn dump_all(&self, tx: TestTransaction) -> Result<Arc<[RepaymentEntryEntity]>, DaoError>;
                async fn create(&self, entity: &RepaymentEntryEntity, process: &str, tx: TestTransaction) -> Result<(), DaoError>;
                async fn update(&self, entity: &RepaymentEntryEntity, process: &str, tx: TestTransaction) -> Result<(), DaoError>;
                async fn all(&self, tx: TestTransaction) -> Result<Arc<[RepaymentEntryEntity]>, DaoError>;
                async fn find_by_id(&self, id: Uuid, tx: TestTransaction) -> Result<Option<RepaymentEntryEntity>, DaoError>;
                async fn find_by_phase_id(&self, phase_id: Uuid, tx: TestTransaction) -> Result<Vec<RepaymentEntryEntity>, DaoError>;
                // Weitere Methoden gemaess RepaymentEntryDao-Trait ergaenzen.
            }
        }

        mock! {
            pub TestMemberDao {}
            #[async_trait]
            impl MemberDao for TestMemberDao {
                type Transaction = TestTransaction;
                async fn dump_all(&self, tx: TestTransaction) -> Result<Arc<[MemberEntity]>, DaoError>;
                async fn create(&self, entity: &MemberEntity, process: &str, tx: TestTransaction) -> Result<(), DaoError>;
                async fn update(&self, entity: &MemberEntity, process: &str, tx: TestTransaction) -> Result<(), DaoError>;
                async fn all(&self, tx: TestTransaction) -> Result<Arc<[MemberEntity]>, DaoError>;
                async fn find_by_id(&self, id: Uuid, tx: TestTransaction) -> Result<Option<MemberEntity>, DaoError>;
                // Weitere Methoden gemaess MemberDao-Trait ergaenzen.
            }
        }

        mock! {
            pub TestPermissionService {}
            #[async_trait]
            impl PermissionService for TestPermissionService {
                type Context = TestContext;
                async fn check_permission(
                    &self,
                    privilege: &str,
                    context: Authentication<TestContext>,
                ) -> Result<(), ServiceError>;
                async fn current_user_id(
                    &self,
                    context: Authentication<TestContext>,
                ) -> Result<Option<String>, ServiceError>;
                // Restliche PermissionService-Methoden 1:1 aus attendance_export.rs:471-563 uebernehmen.
                // ACHTUNG: Mockall verlangt vollstaendige Trait-Impl-Liste — KEINE Methode auslassen,
                // sonst Compile-Error. Vorlage exakt spiegeln.
            }
        }

        pub struct TestDeps;

        impl RepaymentExportServiceDeps for TestDeps {
            type Context = TestContext;
            type Transaction = TestTransaction;
            type RepaymentPhaseDao = MockTestPhaseDao;
            type RepaymentEntryDao = MockTestEntryDao;
            type MemberDao = MockTestMemberDao;
            type PermissionService = MockTestPermissionService;
            type TransactionDao = MockTestTxDao;
        }

        // ----------------------------------------------------------------------
        // Helper builders (Pattern aus attendance_export.rs:576-626).
        // ----------------------------------------------------------------------

        fn build_service(
            phase_dao: MockTestPhaseDao,
            entry_dao: MockTestEntryDao,
            member_dao: MockTestMemberDao,
            permission_service: MockTestPermissionService,
            tx_dao: MockTestTxDao,
        ) -> RepaymentExportServiceImpl<TestDeps> {
            RepaymentExportServiceImpl {
                transaction_dao: Arc::new(tx_dao),
                permission_service: Arc::new(permission_service),
                repayment_phase_dao: Arc::new(phase_dao),
                repayment_entry_dao: Arc::new(entry_dao),
                member_dao: Arc::new(member_dao),
                pdf_generator: Arc::new(PdfGenerator::new()),
                template_base: Arc::new(PathBuf::from("templates")),
            }
        }

        /// Fuer B2-Test: Tx-Use ist erlaubt, commit() darf NICHT aufgerufen werden
        /// (PermissionDenied bricht VOR commit ab — sonst leakt der Test einen False-Negative).
        fn tx_dao_no_commit() -> MockTestTxDao {
            let mut tx_dao = MockTestTxDao::new();
            tx_dao
                .expect_use_transaction()
                .returning(|_| Ok(TestTransaction));
            tx_dao.expect_commit().times(0..=1).returning(|_| Ok(()));
            tx_dao
        }

        fn test_member(member_number: i64, first: &str, last: &str) -> MemberEntity {
            // Felder gemaess MemberEntity-Definition in genossi_dao/src/member.rs.
            // Default-Felder einfuegen; bank_account explizit Some(...) fuer IBAN-Spalte.
            // ACHTUNG: Wenn MemberEntity weitere Pflichtfelder hat (z.B. created, version),
            // hier ergaenzen — Vorlage: real existierende test-Helper in
            // genossi_service_impl/src/member.rs Tests ODER
            // genossi_bin/tests/test_helpers.rs.
            MemberEntity {
                id: Uuid::new_v4(),
                member_number,
                first_name: Arc::from(first),
                last_name: Arc::from(last),
                bank_account: Some(Arc::from("DE89370400440532013000")),
                current_shares: 5,
                deleted: None,
                // ... weitere MemberEntity-Felder hier (in member.rs nachschauen,
                // alle Felder EXPLIZIT setzen — KEIN Default::default(), weil
                // MemberEntity in der Codebase kein Default implementiert).
                ..member_defaults_filler() // Helper unten, falls noetig
            }
        }

        /// Falls MemberEntity weitere Pflichtfelder hat, die in test_member nicht explizit
        /// gesetzt werden, hier auflisten. Wenn member.rs Default implementiert,
        /// kann diese Funktion entfallen.
        fn member_defaults_filler() -> MemberEntity {
            // Implementierung muss alle uebrigen MemberEntity-Felder mit Test-Defaults
            // belegen; Vorlage: member-Test-Helpers in der Codebase.
            todo!("Executor: alle MemberEntity-Felder explizit fuellen — siehe member.rs Struct-Definition")
        }

        fn test_entry(member_id: Uuid, phase_id: Uuid, status: RepaymentEntryStatus, created_us: i64) -> RepaymentEntryEntity {
            RepaymentEntryEntity {
                id: Uuid::new_v4(),
                member_id,
                phase_id,
                share_count_to_pay_out: 1,
                status,
                created: datetime!(2026-01-01 00:00:00) + time::Duration::microseconds(created_us),
                deleted: None,
                version: Uuid::new_v4(),
            }
        }

        fn test_phase(status: RepaymentPhaseStatus) -> RepaymentPhaseEntity {
            RepaymentPhaseEntity {
                id: Uuid::new_v4(),
                fiscal_year: 2026,
                share_value: 12000, // 120 EUR/Anteil
                status,
                opened_at: None,
                closed_at: None,
                created: datetime!(2026-01-01 00:00:00),
                deleted: None,
                version: Uuid::new_v4(),
            }
        }

        // ----------------------------------------------------------------------
        // EXPO-05 / D-11: Grep-Gate-Test — keine Audit-Macros im Source-File.
        // Self-Reference-Trick via format!() um nicht das eigene Source-File
        // die Assertions selbst invalidieren zu lassen (RESEARCH §Q10).
        // ----------------------------------------------------------------------
        #[test]
        fn no_audit_macros_used() {
            let src = include_str!("repayment_export.rs");
            let payload: String = src
                .lines()
                .filter(|l| !l.trim_start().starts_with("//"))
                .collect::<Vec<_>>()
                .join("\n");

            let create_macro = format!("{}!", "audited_create");
            let update_macro = format!("{}!", "audited_update");
            let delete_macro = format!("{}!", "audited_delete");

            assert!(
                !payload.contains(&create_macro),
                "EXPO-05 violated: create-audit macro found"
            );
            assert!(
                !payload.contains(&update_macro),
                "EXPO-05 violated: update-audit macro found"
            );
            assert!(
                !payload.contains(&delete_macro),
                "EXPO-05 violated: delete-audit macro found"
            );
        }

        // ----------------------------------------------------------------------
        // REVISION-Fix W6 + B1: D-04 + D-05 wortwoertlich.
        // Verifiziert dass der purpose-String das ORIGINAL `ü` enthaelt
        // (KEINE ASCII-Sanitization) und dem D-04-Schema exakt entspricht.
        //
        // REVISION-Fix B1: Die ASCII-Variante des purpose-Strings darf NIRGENDS
        // als Literal im Source-File auftauchen. Wir konstruieren sie zur
        // Laufzeit via `format!`-Trick (siehe unten in der Negativ-Assertion),
        // damit das Acceptance-Criterion `grep -c "Anteilsr<ASCII>ckzahlung" == 0`
        // deterministisch erfuellbar ist (kein String-Literal-Match im Quelltext).
        // ----------------------------------------------------------------------
        #[test]
        fn test_purpose_string_preserves_umlaut_per_d04() {
            let phase = test_phase(RepaymentPhaseStatus::Open);
            let mueller = test_member(1234, "Hans", "Müller");
            let entry = test_entry(mueller.id, phase.id, RepaymentEntryStatus::Open, 0);

            let rows = filter_and_enrich_rows(
                &phase,
                vec![(entry, mueller)],
                ExportInclude::Open,
            );

            assert_eq!(rows.len(), 1);
            // D-04 + D-05: wortwoertlich mit Original-Umlaut `ü`.
            assert_eq!(
                rows[0].purpose,
                "Anteilsrückzahlung GJ 2026 1234 Hans Müller",
                "D-04 violated — purpose must use original Umlaut (not the ASCII variant); D-05 forbids ASCII-Sanitization"
            );

            // REVISION-Fix B1: Defense-in-Depth-Negative-Assertion.
            // Der ASCII-Variant wird hier ZUR LAUFZEIT konstruiert, damit der
            // String-Literal NICHT im Source-File auftaucht. Das macht das
            // Acceptance-Criterion `grep -c "Anteilsr<ascii>ckzahlung" == 0`
            // deterministisch erfuellbar.
            let ascii_variant = format!("Anteilsr{}ckzahlung", "ue");
            assert!(
                !rows[0].purpose.contains(&ascii_variant),
                "D-04/D-05 violated: purpose contains ASCII variant — Umlaut must be preserved"
            );
        }

        // ----------------------------------------------------------------------
        // REVISION-Fix W1: Include-Filter Counts direkt asserten.
        // ----------------------------------------------------------------------
        #[test]
        fn test_include_filter_row_counts() {
            let phase = test_phase(RepaymentPhaseStatus::Open);
            let m1 = test_member(101, "A", "AA");
            let m2 = test_member(102, "B", "BB");
            let m3 = test_member(103, "C", "CC");
            let m4 = test_member(104, "D", "DD");

            // Build pairs vector: (entry, member). Status-Mix: 2 Open + 1 Contacted + 1 PaidOut.
            let pairs = vec![
                (test_entry(m1.id, phase.id, RepaymentEntryStatus::Open, 0), m1.clone()),
                (test_entry(m2.id, phase.id, RepaymentEntryStatus::Open, 1), m2.clone()),
                (test_entry(m3.id, phase.id, RepaymentEntryStatus::Contacted, 2), m3.clone()),
                (test_entry(m4.id, phase.id, RepaymentEntryStatus::PaidOut, 3), m4.clone()),
            ];

            // D-01: include=open -> Open + Contacted = 3 Zeilen.
            let open_rows = filter_and_enrich_rows(&phase, pairs.clone(), ExportInclude::Open);
            assert_eq!(open_rows.len(), 3, "include=open should include Open + Contacted (D-01)");

            // D-02: include=all -> 4 Zeilen.
            let all_rows = filter_and_enrich_rows(&phase, pairs.clone(), ExportInclude::All);
            assert_eq!(all_rows.len(), 4, "include=all should include all 3 stati (D-02)");

            // D-02: include=paid -> 1 Zeile.
            let paid_rows = filter_and_enrich_rows(&phase, pairs.clone(), ExportInclude::Paid);
            assert_eq!(paid_rows.len(), 1, "include=paid should include only PaidOut (D-02)");

            // D-09 Sort: open_rows aufsteigend nach member_number.
            assert_eq!(open_rows[0].member_number, 101);
            assert_eq!(open_rows[1].member_number, 102);
            assert_eq!(open_rows[2].member_number, 103);
        }

        // ----------------------------------------------------------------------
        // REVISION-Fix B3: Euro-Format ohne .abs() liefert korrekten String.
        // ----------------------------------------------------------------------
        #[test]
        fn test_amount_str_uses_phase_10_d04_pattern_without_abs() {
            let phase = test_phase(RepaymentPhaseStatus::Open);
            // share_value = 12000 cents (120 EUR), share_count = 1 -> amount_str = "120,00".
            let m = test_member(1, "X", "Y");
            let entry = test_entry(m.id, phase.id, RepaymentEntryStatus::Open, 0);

            let rows = filter_and_enrich_rows(&phase, vec![(entry, m)], ExportInclude::All);
            assert_eq!(rows[0].amount_str, "120,00",
                "Amount format must be Phase-10-D-04-conform (no .abs(), no leading zeros)");
        }

        // ----------------------------------------------------------------------
        // REVISION-Fix B2 (Pitfall #2): Permission-Funnel-Order verifiziert via Mocks.
        //
        // Setup:
        //   - Phase ist in Preparation-Status (Status-Gate wuerde 409 liefern)
        //   - PermissionService liefert PermissionDenied (Admin-Gate verweigert)
        //
        // Erwartung: service.export(...) liefert PermissionDenied (403),
        // NICHT Conflict("phase_not_exportable") (409).
        //
        // Das beweist die Funnel-Order: load -> permission -> status.
        //
        // Pattern: attendance_export.rs:748-784 (test_non_admin_returns_permission_denied).
        // ----------------------------------------------------------------------
        #[tokio::test]
        async fn test_non_admin_on_preparation_returns_permission_denied_not_conflict() {
            // ARRANGE
            let phase_id = Uuid::new_v4();
            let mut preparation_phase = test_phase(RepaymentPhaseStatus::Preparation);
            preparation_phase.id = phase_id;
            let phase_clone = preparation_phase.clone();

            // Phase-DAO liefert die Preparation-Phase (load gelingt).
            let mut phase_dao = MockTestPhaseDao::new();
            phase_dao
                .expect_find_by_id()
                .with(eq(phase_id), always())
                .times(1)
                .returning(move |_, _| Ok(Some(phase_clone.clone())));

            // PermissionService verweigert Admin-Zugriff.
            let mut perm = MockTestPermissionService::new();
            perm.expect_check_permission()
                .withf(|p, _| p == ADMIN_PRIVILEGE)
                .times(1)
                .returning(|_, _| Err(ServiceError::PermissionDenied));

            // Entry-DAO und Member-DAO duerfen NICHT angefragt werden,
            // weil Funnel VOR den DAO-Reads abbricht. KEINE expect_*-Calls hier.
            let entry_dao = MockTestEntryDao::new();
            let member_dao = MockTestMemberDao::new();

            // TxDao darf use_transaction aufgerufen werden, commit NICHT.
            let tx_dao = tx_dao_no_commit();

            let svc = build_service(phase_dao, entry_dao, member_dao, perm, tx_dao);

            // ACT
            let result = svc
                .export(
                    phase_id,
                    ExportFormat::Pdf,
                    ExportInclude::Open,
                    Authentication::Context(TestContext),
                )
                .await;

            // ASSERT — KRITISCH: PermissionDenied (403), NICHT Conflict (409).
            // Wenn Funnel-Order falsch waere (status BEFORE permission), kaeme hier
            // ServiceError::Conflict("phase_not_exportable") und leakte die Existenz
            // der Preparation-Phase an non-admin.
            assert!(
                matches!(result, Err(ServiceError::PermissionDenied)),
                "Funnel-Order is broken: expected PermissionDenied (permission BEFORE status), got: {:?}",
                result
            );
            assert!(
                !matches!(result, Err(ServiceError::Conflict(_))),
                "Pitfall #2 violated: got Conflict — Status-Leak via 409 detected (permission MUST be checked BEFORE status)"
            );
        }
    }
    ```

    **WICHTIGE Implementierungs-Anmerkungen fuer den Executor:**

    1. **mock!-Block-Vollstaendigkeit:** `mockall::mock!` verlangt, dass JEDE Trait-Methode im Block aufgelistet ist — auch ungenutzte. Wenn `MemberDao` z.B. 8 Methoden hat, muessen alle 8 im `mock! { ... impl MemberDao for TestMemberDao { ... } }`-Block stehen, sonst Compile-Error. Die exakten Signaturen aus dem Trait-Source im DAO-Crate uebernehmen. Vorlage: `attendance_export.rs:471-563` zeigt die volle PermissionService-Liste — dieselbe Methode anwenden.

    2. **MemberEntity-Felder:** `MemberEntity` hat in der Codebase wahrscheinlich KEIN `Default`-Impl. Der `test_member`-Helper MUSS alle Pflichtfelder explizit setzen. Vor dem Implementieren: `cat genossi_dao/src/member.rs | grep -A40 "pub struct MemberEntity"` ausfuehren, dann jedes Feld explizit belegen. Der `member_defaults_filler`-`todo!()`-Helper im Plan-Skeleton ist als REMINDER gedacht — er MUSS durch echte Feld-Werte ersetzt werden, sonst panic'en die Tests beim Run.

    3. **`tx_dao_no_commit()` ist verbindlich fuer B2:** Wenn `expect_commit()` mit `.times(1)` statt `.times(0..=1)` annotiert waere, wuerde der Test fail-by-design, weil der Permission-Funnel VOR `tx.commit()` abbricht. `.times(0..=1)` erlaubt beides (kein commit ODER ein commit) — wenn der Funnel korrekt ist, ist es 0 commits.

    4. **Wenn Plan > 800 Zeilen wird:** dokumentieren und Plan-Split vorschlagen. Aktueller Plan ist ~850 Zeilen — vertretbar, weil die 4 Tests denselben `mock!`-Block teilen und ein Split (11-03 = Impl, 11-03b = Tests) den Mock-Aufbau doppeln wuerde. Empfehlung: in 11-03 bleiben.

    SCHRITT 6: Oeffne `genossi_service_impl/src/lib.rs`. Suche `pub mod attendance_export;` und fuege direkt danach hinzu:

    ```rust
    pub mod repayment_export;
    ```

    SCHRITT 7: Build + Test.
  </action>
  <verify>
    <automated>
      cargo build -p genossi_service_impl 2>&1 | grep -c "^error" ;
      cargo test -p genossi_service_impl --lib no_audit_macros_used 2>&1 | tail -10;
      cargo test -p genossi_service_impl --lib test_purpose_string_preserves_umlaut_per_d04 2>&1 | tail -10;
      cargo test -p genossi_service_impl --lib test_include_filter_row_counts 2>&1 | tail -10;
      cargo test -p genossi_service_impl --lib test_amount_str_uses_phase_10_d04_pattern_without_abs 2>&1 | tail -10;
      cargo test -p genossi_service_impl --lib test_non_admin_on_preparation_returns_permission_denied_not_conflict 2>&1 | tail -10;
      grep -cE "pub struct RepaymentExportServiceImpl" /home/neosam/programming/rust/projects/genossi3/genossi_service_impl/src/repayment_export.rs;
      grep -cE "async fn check_admin_and_phase_status" /home/neosam/programming/rust/projects/genossi3/genossi_service_impl/src/repayment_export.rs;
      grep -c "Anteilsrückzahlung" /home/neosam/programming/rust/projects/genossi3/genossi_service_impl/src/repayment_export.rs;
      grep -c "Anteilsrueckzahlung" /home/neosam/programming/rust/projects/genossi3/genossi_service_impl/src/repayment_export.rs;
      grep -cE "phase_not_exportable" /home/neosam/programming/rust/projects/genossi3/genossi_service_impl/src/repayment_export.rs;
      grep -cE "fn filter_and_enrich_rows" /home/neosam/programming/rust/projects/genossi3/genossi_service_impl/src/repayment_export.rs;
      grep -E "audited_(create|update|delete)!" /home/neosam/programming/rust/projects/genossi3/genossi_service_impl/src/repayment_export.rs | grep -v '^[[:space:]]*//' || true;
      grep -cE "^pub mod repayment_export" /home/neosam/programming/rust/projects/genossi3/genossi_service_impl/src/lib.rs;
      grep -c "matches!(result, Err(ServiceError::PermissionDenied))" /home/neosam/programming/rust/projects/genossi3/genossi_service_impl/src/repayment_export.rs;
      grep -c "Funnel-Order is broken" /home/neosam/programming/rust/projects/genossi3/genossi_service_impl/src/repayment_export.rs;
      grep -c "Status-Leak via 409 detected" /home/neosam/programming/rust/projects/genossi3/genossi_service_impl/src/repayment_export.rs
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "pub struct RepaymentExportServiceImpl" genossi_service_impl/src/repayment_export.rs` == 1
    - `grep -cE "async fn check_admin_and_phase_status" genossi_service_impl/src/repayment_export.rs` == 1
    - `grep -cE "fn filter_and_enrich_rows" genossi_service_impl/src/repayment_export.rs` == 1 (REVISION-Fix W1+W6: extrahierte Pure-Function)
    - `grep -cE "phase_not_exportable" genossi_service_impl/src/repayment_export.rs` >= 1 (Conflict-Error-Body)
    - `grep -c "Anteilsrückzahlung" genossi_service_impl/src/repayment_export.rs` >= 2 (REVISION-Fix B1: ORIGINAL-Umlaut im format!-Call UND im Test-Assertion)
    - `grep -c "Anteilsrueckzahlung" genossi_service_impl/src/repayment_export.rs` == 0 (REVISION-Fix B1 + D-05: KEINE ASCII-Variante anywhere im File — ASCII-Variant wird zur Laufzeit via `format!("Anteilsr{}ckzahlung", "ue")` konstruiert)
    - `grep -cE "auszahlung-\\{\\}-\\{\\}\\.pdf" genossi_service_impl/src/repayment_export.rs` >= 1 (Filename-Schema)
    - `grep -cE "self\\.transaction_dao\\.commit" genossi_service_impl/src/repayment_export.rs` >= 1 (Pitfall #8: commit vor Render)
    - `grep -cE "render_repayment_list" genossi_service_impl/src/repayment_export.rs` >= 1 (PdfGenerator-Call)
    - `grep -c '\.abs()' genossi_service_impl/src/repayment_export.rs` == 0 (REVISION-Fix B3: kein .abs() in Euro-Formatting)
    - Grep-Gate (Code-Path filtered): `grep -E "audited_(create|update|delete)!" genossi_service_impl/src/repayment_export.rs | grep -v '^[[:space:]]*//' | grep -v 'format!' | wc -l` == 0
    - `grep -cE "^pub mod repayment_export" genossi_service_impl/src/lib.rs` == 1
    - `cargo build -p genossi_service_impl 2>&1 | grep -c "^error"` == 0
    - `cargo test -p genossi_service_impl --lib no_audit_macros_used 2>&1 | grep "test result: ok"` >= 1
    - `cargo test -p genossi_service_impl --lib test_purpose_string_preserves_umlaut_per_d04 2>&1 | grep "test result: ok"` >= 1 (REVISION-Fix W6)
    - `cargo test -p genossi_service_impl --lib test_include_filter_row_counts 2>&1 | grep "test result: ok"` >= 1 (REVISION-Fix W1)
    - `cargo test -p genossi_service_impl --lib test_amount_str_uses_phase_10_d04_pattern_without_abs 2>&1 | grep "test result: ok"` >= 1 (REVISION-Fix B3)
    - `cargo test -p genossi_service_impl --lib test_non_admin_on_preparation_returns_permission_denied_not_conflict 2>&1 | grep "test result: ok. 1 passed"` >= 1 (REVISION-Fix B2 / Pitfall #2 — strikter Check: exakt 1 Test passed, schliesst leeren-Body-Schein-Pass aus)
    - `grep -c "matches!(result, Err(ServiceError::PermissionDenied))" genossi_service_impl/src/repayment_export.rs` >= 1 (REVISION-Fix B2: konkrete Permission-Assertion im Source vorhanden — kein leerer Test-Body)
    - `grep -c "Funnel-Order is broken" genossi_service_impl/src/repayment_export.rs` >= 1 (REVISION-Fix B2: spezifische Funnel-Order-Assertion vorhanden)
    - `grep -c "Status-Leak via 409 detected" genossi_service_impl/src/repayment_export.rs` >= 1 (REVISION-Fix B2: spezifische Conflict-Negation-Assertion vorhanden)
  </acceptance_criteria>
  <done>
    `RepaymentExportServiceImpl<Deps>` implementiert `RepaymentExportService`-Trait mit Permission-Funnel, extrahiertem `filter_and_enrich_rows`, Pre-Computing (Verwendungszweck mit ORIGINAL-Umlaut, Euro-Format OHNE `.abs()`), Render-Call. Grep-Gate-Test bestaetigt 0 `audited_*!`-Calls. 4 Service-Layer-Mock/Pure-Tests verifizieren Pitfall #2 (Funnel-Order via konkretem Mock-Setup, NICHT leerer Body), D-04+D-05 (Umlaut-Erhalt + ASCII-Variant via Laufzeit-Konstruktion ohne Literal im Source), D-01/D-02 (Include-Filter-Counts) und Phase-10-D-04-Euro-Pattern. Modul ist in `lib.rs` deklariert. `cargo build -p genossi_service_impl` fehlerfrei.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REST ↔ Service | Service erhaelt `Authentication<Context>` via REST-Handler-Extractor; Permission-Check ist verbindlich |
| Service ↔ DAO | Service ruft DAO-Methoden in einer Tx; DAOs filtern soft-deleted via Default-Impls |
| Service ↔ Renderer | Tx wird VOR `pdf_generator.render_*` committed (Pitfall #8) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-001 | Spoofing/EoP | non-admin tries to export | mitigate | `check_admin_and_phase_status` ruft `permission_service.check_permission("admin", ...)` (D-11). Helper/User -> `ServiceError::PermissionDenied`. Plan 11.04 mapped das auf 403. Severity: HIGH -> LOW. |
| T-002 | Information Disclosure | Status-Leak (non-admin lernt Phase-Existenz) | mitigate | Funnel-Reihenfolge `load -> perm -> status`. Non-admin auf Preparation -> 403 (PermissionDenied), NICHT 409 (Conflict). REVISION-Fix B2: Service-Layer-Mock-Test `test_non_admin_on_preparation_returns_permission_denied_not_conflict` mit KONKRETEM Mock-Setup (Phase-DAO liefert Preparation-Phase, PermissionService liefert PermissionDenied) und expliziten `assert!(matches!(result, Err(PermissionDenied)))` + `assert!(!matches!(result, Err(Conflict(_))))` verifiziert dies DIREKT (E2E unter `mock_auth` kann non-admin nicht herstellen). Severity: LOW. |
| T-003 | Information Disclosure | PII-Leak via Export | mitigate | Phase 11 ist Vorstand-only (kein Helper-Branch); voller Member-Read ist erlaubt. D-11 + check_permission garantiert Vorstand-Identitaet. Severity: HIGH (vor) -> LOW (nach). |
| T-004 | Tampering | D-05 verletzt durch ASCII-Sanitization | mitigate | REVISION-Fix B1 + W6: Service-Layer-Pure-Function-Test `test_purpose_string_preserves_umlaut_per_d04` verifiziert dass purpose-String den ORIGINAL-Umlaut `ü` enthaelt und KEINE ASCII-Variante `Anteilsr<ascii>ckzahlung` jemals erzeugt wird. ASCII-Variant wird zur Laufzeit konstruiert, damit der Literal-String NICHT im Source-File auftaucht (deterministisch erfuellbares grep-Gate). Severity: MEDIUM -> LOW. |
| T-005 | Denial of Service | grosse Phasen mit hunderten Entries | accept | Phase-Groesse begrenzt durch realistische Genossenschafts-Mitgliederzahl (~100s). N+1-DAO-Reads in SQLite-In-Memory sind microsecond-Bereich. Tower-Governor-Rate-Limit existiert global. Severity: LOW. |
| T-006 | Repudiation | Audit-Chain-Bruch durch versehentlichen Write-Call | mitigate | Grep-Gate-Test `no_audit_macros_used` im Source-File verifiziert 0 `audited_*!`-Macros. EXPO-05 ist explizit kein-Audit-Anforderung. Severity: HIGH -> LOW. |
| T-007 | Information Disclosure | tx-Halten nach Render-Fehler | mitigate | `tx.commit()` MUSS VOR `pdf_generator.render_repayment_list` (Pitfall #8). Render-Fehler propagiert ServiceError ohne offene Tx zu leaken. Severity: LOW. |
</threat_model>

<verification>
- `cargo build -p genossi_service_impl` ist fehlerfrei.
- `cargo test -p genossi_service_impl --lib no_audit_macros_used` ist gruen (Grep-Gate-Test).
- 4 zusaetzliche Service-Layer-Tests gruen: `test_purpose_string_preserves_umlaut_per_d04` (B1+W6), `test_include_filter_row_counts` (W1), `test_amount_str_uses_phase_10_d04_pattern_without_abs` (B3), `test_non_admin_on_preparation_returns_permission_denied_not_conflict` (B2/Pitfall #2 — mit konkretem Mock-Setup + Assert-Calls; KEIN leerer Test-Body).
- `grep -E "audited_(create|update|delete)!" genossi_service_impl/src/repayment_export.rs | grep -v "^[[:space:]]*//"` liefert KEINE Treffer (Code-Path) — nur Test-Helper-Strings via `format!()`-Self-Reference-Trick.
- Verwendungszweck-String "Anteilsrückzahlung GJ" (mit ü!) ist exakt wie in D-04 spezifiziert kodiert.
- `grep -c "Anteilsrueckzahlung" genossi_service_impl/src/repayment_export.rs` == 0 — keine ASCII-Sanitization (D-05) UND keine ASCII-Variante als Literal im Source (B1-Fix via Laufzeit-`format!("Anteilsr{}ckzahlung", "ue")`).
- Status-Gate akzeptiert `Open | Closed`; `Preparation` -> `Conflict("phase_not_exportable")`.
- Order: load_by_id -> check_permission -> match status (Pitfall #2 mitigated + via Mock-Test mit echtem Body verifiziert).
- Euro-Format-String verwendet `format!("{},{:02}", cents / 100, cents % 100)` OHNE `.abs()` (Phase-10-D-04-Konsistenz; REVISION-Fix B3).
</verification>

<success_criteria>
- EXPO-01 (PDF-Export fuer Open+Closed): Status-Gate akzeptiert beide; Preparation rejected mit 409.
- EXPO-02 (PDF-Inhalt + Sort): Pre-Computing baut 6-Spalten-Rows; Sort `member_number ASC, created ASC`.
- EXPO-03 (Include-Filter): In-Memory-Filter implementiert alle 3 Varianten (open/all/paid); REVISION-Fix W1 Pure-Function-Test verifiziert Counts direkt.
- EXPO-05 (Vorstand-only, kein Audit): `check_permission("admin")` + Grep-Gate-Test == 0 audit_macros.
- D-04 + D-05: Verwendungszweck wortwoertlich mit ORIGINAL-Umlaut `ü` kodiert; REVISION-Fix W6 Pure-Function-Test verifiziert; REVISION-Fix B1 stellt sicher dass die ASCII-Variante NICHT als Literal im Source ist (Laufzeit-Konstruktion).
- D-06 IBAN-NULL -> leerer String, kein Skip.
- D-09 Sort-Reihenfolge codifiziert.
- Pitfall #2 (Status-Leak): REVISION-Fix B2 Mock-Test mit konkretem Setup + Assertions verifiziert Funnel-Order direkt (KEIN leerer Test-Body).
- Pitfall #8: tx.commit() VOR PdfGenerator-Call.
- REVISION-Fix B3: Euro-Format OHNE `.abs()` (Phase-10-D-04-Konsistenz; PATTERNS.md §S9).
</success_criteria>

<output>
After completion, create `.planning/phases/11-export-pdf-csv/11-03-SUMMARY.md`
</output>
