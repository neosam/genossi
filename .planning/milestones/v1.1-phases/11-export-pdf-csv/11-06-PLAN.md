---
phase: 11-export-pdf-csv
plan: 06
type: execute
wave: 5
depends_on:
  - 11-05-PLAN.md
files_modified:
  - genossi_bin/tests/e2e_tests.rs
autonomous: true
requirements:
  - EXPO-01
  - EXPO-02
  - EXPO-03
  - EXPO-05
user_setup: []
tags:
  - e2e-tests
  - regression
  - grep-gate
must_haves:
  truths:
    - "PDF-Happy-Path (Open-Phase, include=open): 200 + %PDF-Magic-Bytes + Content-Disposition mit `auszahlung-2026-open.pdf`"
    - "Filename-Schema `auszahlung-{fiscal_year}-{include}.pdf` wird in JEDEM PDF-Antwort-Test explizit asserted (REVISION-Fix W4)"
    - "Format-Whitelist: `?format=csv` -> 400 (D-12 / Pitfall #3)"
    - "Format-Whitelist: `?format=xlsx` -> 400 (D-12)"
    - "Include-Filter: 3 Sub-Tests fuer `?include=open`, `?include=all`, `?include=paid` mit erwarteten Row-Counts (direkte Verifikation via 11-03 Mock-Test; hier nur Smoke + Filename — REVISION-Fix W1)"
    - "Status-Gate: Preparation-Phase -> 409 mit Body-Substring `phase_not_exportable` (D-10)"
    - "Status-Gate: Closed-Phase -> 200 (EXPO-01: PDF auch nach Phase-Abschluss verfuegbar)"
    - "404 bei unbekannter phase_id (Random UUID)"
    - "Leere IBAN: Member.bank_account=None -> PDF rendert ohne Crash, leere Spalte (D-06)"
    - "Audit-Chain bleibt valide nach Export: GET /api/audit/verify liefert `valid: true` (EXPO-05 / D-11). Strenger: KEIN neuer Audit-Eintrag entsteht — Compile-Time verifiziert durch Grep-Gate aus Plan 11.03; runtime-Verifikation via Endpoint-Verify reicht (REVISION-Fix W7)"
    - "Happy-Path Test 1 verwendet zusaetzlich einen Member mit Umlaut `Hans Müller` — Typst rendert ohne Crash mit Original-Umlaut (REVISION-Fix W6)"
    - "Grep-Gate-Verification: `cargo test no_audit_macros_used` ist gruen (cross-Phase-Reuse aus Plan 11.03)"
    - "REVISION-Fix B2: Pitfall-#2-Verifikation (Status-Leak via 403-vs-409) wird AUSSCHLIESSLICH per Service-Layer-Mock-Test in Plan 11.03 verifiziert — KEIN E2E-403-Negative-Test in 11.06 (mock_auth-Middleware injiziert immer admin und kann non-admin nicht herstellen)"
  artifacts:
    - path: "genossi_bin/tests/e2e_tests.rs"
      provides: "9+ neue Test-Funktionen + Helper fuer Member-ohne-IBAN-Setup"
      contains: "test_export_repayment_pdf_open_happy_path"
  key_links:
    - from: "genossi_bin/tests/e2e_tests.rs"
      to: "genossi_bin/src/lib.rs::RestStateImpl"
      via: "setup_with_templates() -> TestServer"
      pattern: "setup_with_templates"
---

<objective>
Wave-5-E2E-Plan: ergaenzt `genossi_bin/tests/e2e_tests.rs` um 8-10 E2E-Tests, die alle 4 Requirements (EXPO-01..03 + EXPO-05) und alle relevanten User-Decisions (D-01..D-12) gegen den real-running Server verifizieren. Wiederverwendet bestehende Phase-9/10-Helper (`create_member_with_exit_date`, `create_open_repayment_phase`, `create_preparation_repayment_phase`) und ergaenzt EINEN neuen Helper fuer Member-ohne-IBAN (D-06-Edge-Case-Test).

Purpose: Single-Source-of-Truth fuer HAPPY-PATH-Acceptance-Verification. Pitfall-#2 (Status-Leak via 403-vs-409) wird NICHT hier verifiziert — siehe REVISION-Fix B2: das e2e-Test-Harness laeuft mit `#![cfg(feature = "mock_auth")]` und der mock_auth-Middleware injiziert IMMER admin-MockContext unabhaengig vom Cookie. Ein Non-Admin-Auth-Pfad ist im E2E-Setup nicht herstellbar, daher wurde der entsprechende E2E-Test aus diesem Plan ENTFERNT und durch einen Service-Layer-Mock-Test in Plan 11.03 ersetzt (`test_non_admin_on_preparation_returns_permission_denied_not_conflict`).

Output: ~8-10 neue Test-Funktionen + 1 neuer Helper (`create_member_without_iban`) im bestehenden e2e_tests.rs. Schaetzungen: ~500-700 LOC additiv. Kein neuer Helper-File-Split (vorhandenes File haelt bereits Phase 7-10-Tests).
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-export-pdf-csv/11-CONTEXT.md
@.planning/phases/11-export-pdf-csv/11-RESEARCH.md
@.planning/phases/11-export-pdf-csv/11-PATTERNS.md
@.planning/phases/11-export-pdf-csv/11-03-PLAN.md
@.planning/phases/11-export-pdf-csv/11-05-PLAN.md

@genossi_bin/tests/e2e_tests.rs
@genossi_rest/src/repayment_export.rs
@genossi_service_impl/src/repayment_export.rs

<interfaces>
<!-- Existing E2E helpers from Phase 6/9/10 that we WILL reuse. -->

From genossi_bin/tests/e2e_tests.rs (approximate line numbers):
```rust
async fn setup() -> TestServer;
async fn setup_with_templates() -> TestServer;  // line ~2672 — provisions DEFAULT_TEMPLATES

// Phase 7/8 helpers:
async fn create_preparation_repayment_phase(client, server, fiscal_year, share_value)
    -> RepaymentPhaseTO;  // line ~10578
async fn create_open_repayment_phase(client, server, fiscal_year, share_value)
    -> RepaymentPhaseTO;  // line ~11109

// Phase 9 helpers:
async fn create_member_with_exit_date(client, server, member_number, fiscal_year, share_count)
    -> MemberTO;  // line ~11043

// Variant accepting names:
//   Falls die existierende create_member_with_exit_date keine first/last-name-Parameter
//   akzeptiert, ergaenze einen Wrapper create_member_with_name_and_exit_date(...) ODER
//   schreibe einen direkten POST /api/members im Happy-Path-Test inline.

// Phase 9 mark_paid_out:
// POST /api/repayment-entry/{id}/mark-paid-out

// Sample-Member-Template:
fn sample_member() -> MemberTO;  // line ~65 — bank_account: Some("DE89...") default
```

<!-- New helper this plan creates. -->

```rust
/// Phase 11 (D-06): Creates a member with bank_account = None for the empty-IBAN test.
/// Workflow: POST member with bank_account=Some(...), then PUT with bank_account=None
/// + version-roundtrip (Member-Update requires correct version per Phase-7 lifecycle pattern).
async fn create_member_without_iban(
    client: &reqwest::Client,
    server: &TestServer,
    member_number: i64,
    fiscal_year: i32,
    share_count: i32,
) -> MemberTO;
```

<!-- REST endpoint contract (from Plan 11.04). -->

```
GET /api/repayment-phase/{phase_id}/export/{format}?include=open|all|paid
  format = "pdf" -> 200 application/pdf
  format = "csv"|"xlsx"|other -> 400
  no auth -> 401 (extract_auth_context)
  auth but no admin -> 403 (map_export_error) — verifiziert via 11-03 Mock-Test, NICHT hier (mock_auth-Constraint)
  phase_id not found -> 404
  phase.status == Preparation -> 409 ("phase_not_exportable")
  phase.status in {Open, Closed} + admin -> 200
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helper-Funktion fuer Member-ohne-IBAN + 6 Kern-E2E-Tests (Happy-Path mit Umlaut + Closed-200 + Format-Whitelist + Status-409 + 404 + Audit-Chain)</name>
  <files>
    genossi_bin/tests/e2e_tests.rs (MODIFY — append)
  </files>
  <read_first>
    - genossi_bin/tests/e2e_tests.rs Z. 2672-2694 (`setup_with_templates`-Helper — Pflicht fuer PDF-Tests, Pitfall #1)
    - genossi_bin/tests/e2e_tests.rs Z. 10128-10178 (Phase-6 `test_export_pdf_closed_returns_pdf_magic_bytes` als 1:1-Vorlage)
    - genossi_bin/tests/e2e_tests.rs Z. 10372-10402 (Phase-6 Format-Whitelist-Test)
    - genossi_bin/tests/e2e_tests.rs Z. 10293-10371 (Phase-6 409-Conflict-Test)
    - genossi_bin/tests/e2e_tests.rs Z. 10443-10491 (Phase-6 Filename-Schema-Test)
    - genossi_bin/tests/e2e_tests.rs Z. 10578-11150 (Phase-7/8/9-Helper: create_preparation_repayment_phase, create_member_with_exit_date, create_open_repayment_phase)
    - genossi_bin/tests/e2e_tests.rs Z. 65 (sample_member-Funktion)
    - genossi_bin/tests/e2e_tests.rs Z. 10620 (Phase-7-Lifecycle-Test fuer Member-Update mit version-Bump-Roundtrip)
    - .planning/phases/11-export-pdf-csv/11-PATTERNS.md §5 (Test-Liste fuer E2E)
    - .planning/phases/11-export-pdf-csv/11-RESEARCH.md §Q9 (Setup-Helper-Skizze fuer Member-ohne-IBAN)
  </read_first>
  <behavior>
    - Tests verwenden `setup_with_templates().await` (Pflicht fuer PDF — Pitfall #1)
    - Happy-Path-Test: Open-Phase + 2 Members (mind. 1 mit Umlaut `Hans Müller` — REVISION-Fix W6) mit IBAN -> 200 + %PDF- Magic + Filename "auszahlung-2026-open.pdf" (REVISION-Fix W4 Filename-Assertion)
    - Closed-Phase-Test: Geschlossene Phase -> ebenfalls 200 PLUS Filename-Assertion `auszahlung-{fy}-all.pdf` (REVISION-Fix W4)
    - Format-csv-Test: `?format=csv` -> 400, Body-Substring "unknown export format"
    - Status-Test Preparation: 409, Body-Substring "phase_not_exportable"
    - 404-Test: Random UUID als phase_id -> 404
    - Audit-Chain-Test: nach Export-Call ist `GET /api/audit/verify` weiter `valid: true`. REVISION-Fix W7: Falls /api/audit ein `count`-Feld oder einen aggregierbaren `entries`-Endpoint liefert, wird ZUSAETZLICH `assert_eq!(pre_count, post_count)` geprueft. Falls nicht verfuegbar, bleibt es bei `valid: true` (Compile-Time-No-Audit ist durch Plan 11.03 Grep-Gate-Test abgesichert).
  </behavior>
  <action>
    SCHRITT 1: Helper `create_member_without_iban` am Ende des Helper-Sections im File anlegen (in der Naehe von `create_member_with_exit_date`):

    ```rust
    /// Phase 11 (D-06): Creates a member, then PUTs bank_account = None.
    /// Member-Service setzt `current_shares = shares_at_joining` beim Create (member.rs:213-218);
    /// `bank_account` ist ein write-through-Feld und bleibt nach Update als None erhalten.
    async fn create_member_without_iban(
        client: &reqwest::Client,
        server: &TestServer,
        member_number: i64,
        fiscal_year: i32,
        share_count: i32,
    ) -> MemberTO {
        // Step 1: Create member with default sample IBAN.
        let mut member = create_member_with_exit_date(
            client,
            server,
            member_number,
            fiscal_year,
            share_count,
        )
        .await;

        // Step 2: PUT with bank_account = None.
        member.bank_account = None;
        let resp = client
            .put(server.url(&format!("/api/members/{}", member.id)))
            .json(&member)
            .send()
            .await
            .expect("PUT member failed");
        assert_eq!(
            resp.status(),
            reqwest::StatusCode::OK,
            "PUT member to clear IBAN must succeed; body: {:?}",
            resp.text().await.unwrap_or_default()
        );

        // Step 3: GET to read fresh state (with version bump).
        let resp = client
            .get(server.url(&format!("/api/members/{}", member.id)))
            .send()
            .await
            .expect("GET member failed");
        let updated: MemberTO = resp.json().await.expect("parse member");
        assert!(updated.bank_account.is_none(), "bank_account should be None after PUT");
        updated
    }
    ```

    SCHRITT 2: Test 1 (Happy-Path, Open-Phase, include=open). **REVISION-Fix W6:** einer der beiden Members wird mit Vor-/Nachname `Hans Müller` (Umlaut!) angelegt, um zu verifizieren dass der Typst-Renderer Umlaute korrekt durch den End-to-End-Pfad propagiert.

    Falls `create_member_with_exit_date` keinen Namen-Parameter akzeptiert, wird der Umlaut-Member inline per `POST /api/members` mit Body `{first_name: "Hans", last_name: "Müller", ...}` angelegt, damit das Test-Setup deterministisch ist. Das Pattern dafuer ist in `e2e_tests.rs` bereits etabliert (siehe Phase-3-Member-Tests).

    ```rust
    /// Phase 11 EXPO-01/02/03: PDF-Export auf Open-Phase mit Default-Include=open.
    /// Verifiziert: 200, application/pdf, %PDF- Magic, Filename "auszahlung-{fy}-open.pdf".
    /// REVISION-Fix W4: explizite Filename-Assertion.
    /// REVISION-Fix W6: einer der Members hat Umlaut im Namen (Hans Müller) — Typst-Render
    /// muss ohne Crash durchlaufen und damit D-05 (keine ASCII-Sanitization) E2E-bestaetigen.
    #[tokio::test]
    async fn test_export_repayment_pdf_open_happy_path() {
        let server = setup_with_templates().await;
        let client = reqwest::Client::new();

        let fiscal_year = 2026;

        // Setup: 2 members with IBAN, 1 open repayment phase.
        // Member 101: Standard-Setup via Helper.
        let _m1 = create_member_with_exit_date(&client, &server, 101, fiscal_year, 2).await;

        // Member 102: Inline-POST mit Umlaut `Hans Müller`.
        // Falls existierender Helper Namen-Parameter unterstuetzt, bevorzugen.
        let umlaut_member = serde_json::json!({
            "member_number": 102,
            "first_name": "Hans",
            "last_name": "Müller",
            "salutation": "Mr",
            "title": null,
            "bank_account": "DE89370400440532013000",
            "shares_at_joining": 2,
            "joined_at": format!("{}-01-01T00:00:00Z", fiscal_year - 1),
            "exit_date": format!("{}-12-31T00:00:00Z", fiscal_year),
            // weitere Felder gemaess MemberTO-Schema (siehe sample_member); im Zweifel
            // existing-Helper-Body kopieren und die 4 Felder oben overriden.
        });
        let resp = client
            .post(server.url("/api/members"))
            .json(&umlaut_member)
            .send()
            .await
            .expect("POST umlaut member failed");
        assert!(
            resp.status().is_success(),
            "POST umlaut member must succeed (D-05 — server akzeptiert Umlaute); body: {:?}",
            resp.text().await.unwrap_or_default()
        );

        let phase = create_open_repayment_phase(&client, &server, fiscal_year, 12000).await;

        let resp = client
            .get(server.url(&format!(
                "/api/repayment-phase/{}/export/pdf?include=open",
                phase.id
            )))
            .send()
            .await
            .expect("GET export failed");
        assert_eq!(resp.status(), reqwest::StatusCode::OK);
        assert_eq!(
            resp.headers()
                .get("content-type")
                .and_then(|h| h.to_str().ok())
                .unwrap_or_default(),
            "application/pdf"
        );
        let cd = resp
            .headers()
            .get("content-disposition")
            .and_then(|h| h.to_str().ok())
            .unwrap_or_default()
            .to_string();
        // REVISION-Fix W4: Filename-Schema explizit asserten.
        assert!(
            cd.contains(&format!("auszahlung-{}-open.pdf", fiscal_year)),
            "Content-Disposition '{}' should contain filename schema 'auszahlung-{}-open.pdf'",
            cd, fiscal_year
        );

        let bytes = resp.bytes().await.expect("read bytes");
        assert!(bytes.starts_with(b"%PDF-"), "Response body should be PDF (got {} bytes)", bytes.len());
        assert!(bytes.len() > 1000, "PDF too small ({} bytes)", bytes.len());
    }
    ```

    SCHRITT 3: Test 2 (Closed-Phase weiterhin exportierbar — EXPO-01) inkl. Filename-Assertion (REVISION-Fix W4):

    ```rust
    /// Phase 11 EXPO-01 / D-10: Auch nach Phase-Abschluss bleibt PDF exportierbar.
    /// REVISION-Fix W4: Filename-Schema-Assertion `auszahlung-{fy}-all.pdf`.
    #[tokio::test]
    async fn test_export_repayment_pdf_closed_phase_returns_200() {
        let server = setup_with_templates().await;
        let client = reqwest::Client::new();

        let fiscal_year = 2026;
        // Setup: 1 member; Open-Phase (auto-fill 1 entry); mark PaidOut; close phase.
        let m1 = create_member_with_exit_date(&client, &server, 201, fiscal_year, 1).await;
        let phase = create_open_repayment_phase(&client, &server, fiscal_year, 12000).await;

        // Read entries to find the auto-filled one for m1.
        let entries_resp = client
            .get(server.url(&format!("/api/repayment-entry?phase_id={}", phase.id)))
            .send()
            .await
            .expect("list entries");
        let entries: Vec<serde_json::Value> = entries_resp.json().await.expect("parse entries");
        assert!(!entries.is_empty(), "Auto-fill should have created entries");
        let entry_id = entries[0]["id"].as_str().unwrap().to_string();

        // Mark paid out (Phase 9).
        let resp = client
            .post(server.url(&format!("/api/repayment-entry/{}/mark-paid-out", entry_id)))
            .send()
            .await
            .expect("mark-paid-out");
        assert!(resp.status().is_success(), "mark-paid-out failed: {:?}", resp.text().await);

        // Close phase.
        let resp = client
            .post(server.url(&format!("/api/repayment-phase/{}/close", phase.id)))
            .send()
            .await
            .expect("close phase");
        assert!(resp.status().is_success(), "close phase failed: {:?}", resp.text().await);

        // Export — should work on Closed phase too.
        let resp = client
            .get(server.url(&format!(
                "/api/repayment-phase/{}/export/pdf?include=all",
                phase.id
            )))
            .send()
            .await
            .expect("export closed");
        assert_eq!(resp.status(), reqwest::StatusCode::OK);
        // REVISION-Fix W4: Filename-Schema asserten.
        let cd = resp.headers().get("content-disposition").and_then(|h| h.to_str().ok()).unwrap_or("").to_string();
        assert!(
            cd.contains(&format!("auszahlung-{}-all.pdf", fiscal_year)),
            "Content-Disposition '{}' should contain 'auszahlung-{}-all.pdf'", cd, fiscal_year
        );
        let bytes = resp.bytes().await.unwrap();
        assert!(bytes.starts_with(b"%PDF-"));

        // Suppress unused warning.
        let _ = m1;
    }
    ```

    SCHRITT 4: Test 3 (Format-Whitelist D-12 / Pitfall #3): `?format=csv` -> 400, `?format=xlsx` -> 400:

    ```rust
    /// Phase 11 D-12 / Pitfall #3: csv und xlsx werden vom REST-Handler mit 400 blockiert.
    #[tokio::test]
    async fn test_export_repayment_unknown_format_returns_400() {
        let server = setup_with_templates().await;
        let client = reqwest::Client::new();

        let phase = create_open_repayment_phase(&client, &server, 2026, 12000).await;

        for bad_format in &["csv", "xlsx", "json", "html"] {
            let resp = client
                .get(server.url(&format!(
                    "/api/repayment-phase/{}/export/{}",
                    phase.id, bad_format
                )))
                .send()
                .await
                .expect("send");
            assert_eq!(
                resp.status(),
                reqwest::StatusCode::BAD_REQUEST,
                "Format '{}' should yield 400, got {}",
                bad_format,
                resp.status()
            );
            let body = resp.text().await.unwrap_or_default();
            assert!(
                body.contains("unknown export format") || body.contains(bad_format),
                "Body should explain the unknown format; got: {}",
                body
            );
        }
    }
    ```

    SCHRITT 5: Test 4 (Status-Gate D-10: Preparation -> 409):

    ```rust
    /// Phase 11 D-10: Preparation-Phase ist nicht exportierbar -> 409 mit "phase_not_exportable".
    #[tokio::test]
    async fn test_export_repayment_preparation_phase_returns_409() {
        let server = setup_with_templates().await;
        let client = reqwest::Client::new();

        let phase = create_preparation_repayment_phase(&client, &server, 2026, 12000).await;
        let resp = client
            .get(server.url(&format!(
                "/api/repayment-phase/{}/export/pdf",
                phase.id
            )))
            .send()
            .await
            .expect("send");
        assert_eq!(resp.status(), reqwest::StatusCode::CONFLICT);
        let body = resp.text().await.unwrap_or_default();
        assert!(
            body.contains("phase_not_exportable"),
            "Body should contain 'phase_not_exportable'; got: {}",
            body
        );
    }
    ```

    SCHRITT 6: Test 5 (Random UUID -> 404):

    ```rust
    /// Phase 11: Unbekannte phase_id liefert 404.
    #[tokio::test]
    async fn test_export_repayment_unknown_phase_id_returns_404() {
        let server = setup_with_templates().await;
        let client = reqwest::Client::new();

        let random = uuid::Uuid::new_v4();
        let resp = client
            .get(server.url(&format!("/api/repayment-phase/{}/export/pdf", random)))
            .send()
            .await
            .expect("send");
        assert_eq!(resp.status(), reqwest::StatusCode::NOT_FOUND);
    }
    ```

    SCHRITT 7: Test 6 (Audit-Chain bleibt valide — EXPO-05 / D-11) inkl. REVISION-Fix W7 (Filename-Assertion + Truth-Reduktion auf "valid stays true"):

    ```rust
    /// Phase 11 EXPO-05 / D-11: Read-only Export bricht die Audit-Hashchain nicht.
    /// REVISION-Fix W7: Truth ist "audit/verify bleibt valid". Die staerkere Aussage
    /// "kein neuer Audit-Eintrag entsteht" ist bereits durch den Grep-Gate-Test in Plan 11.03
    /// (`no_audit_macros_used`) zur COMPILE-TIME garantiert; ein zusaetzlicher Runtime-Count-
    /// Delta-Check wuerde lediglich verifizieren, was der Grep-Gate bereits compile-time
    /// abdeckt — daher Runtime-Asserts auf `valid: true` reduziert.
    /// REVISION-Fix W4: Filename-Schema im Export-Response asserten.
    #[tokio::test]
    async fn test_export_repayment_does_not_break_audit_chain() {
        let server = setup_with_templates().await;
        let client = reqwest::Client::new();

        let fiscal_year = 2026;
        // Setup: members + phase + entries (produces audit entries).
        let _m1 = create_member_with_exit_date(&client, &server, 301, fiscal_year, 1).await;
        let phase = create_open_repayment_phase(&client, &server, fiscal_year, 12000).await;

        // Verify chain BEFORE export.
        let resp = client.get(server.url("/api/audit/verify")).send().await.expect("verify pre");
        let pre_body: serde_json::Value = resp.json().await.expect("parse pre");
        assert_eq!(pre_body["valid"].as_bool(), Some(true), "audit chain broken before export: {:?}", pre_body);

        // Trigger export.
        let resp = client
            .get(server.url(&format!(
                "/api/repayment-phase/{}/export/pdf?include=open",
                phase.id
            )))
            .send()
            .await
            .expect("export");
        assert_eq!(resp.status(), reqwest::StatusCode::OK);
        // REVISION-Fix W4: Filename-Assertion auch in Audit-Test.
        let cd = resp.headers().get("content-disposition").and_then(|h| h.to_str().ok()).unwrap_or("").to_string();
        assert!(
            cd.contains(&format!("auszahlung-{}-open.pdf", fiscal_year)),
            "Content-Disposition should contain filename schema; got '{}'", cd
        );

        // Verify chain AFTER export — must still be valid.
        let resp = client.get(server.url("/api/audit/verify")).send().await.expect("verify post");
        let post_body: serde_json::Value = resp.json().await.expect("parse post");
        assert_eq!(post_body["valid"].as_bool(), Some(true), "audit chain broken after export: {:?}", post_body);
    }
    ```
  </action>
  <verify>
    <automated>
      cargo test -p genossi_bin --test e2e_tests test_export_repayment_pdf_open_happy_path 2>&1 | tail -20;
      cargo test -p genossi_bin --test e2e_tests test_export_repayment_pdf_closed_phase_returns_200 2>&1 | tail -20;
      cargo test -p genossi_bin --test e2e_tests test_export_repayment_unknown_format_returns_400 2>&1 | tail -20;
      cargo test -p genossi_bin --test e2e_tests test_export_repayment_preparation_phase_returns_409 2>&1 | tail -20;
      cargo test -p genossi_bin --test e2e_tests test_export_repayment_unknown_phase_id_returns_404 2>&1 | tail -20;
      cargo test -p genossi_bin --test e2e_tests test_export_repayment_does_not_break_audit_chain 2>&1 | tail -20
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test_export_repayment_pdf_open_happy_path" genossi_bin/tests/e2e_tests.rs` >= 1
    - `grep -c "test_export_repayment_pdf_closed_phase_returns_200" genossi_bin/tests/e2e_tests.rs` >= 1
    - `grep -c "test_export_repayment_unknown_format_returns_400" genossi_bin/tests/e2e_tests.rs` >= 1
    - `grep -c "test_export_repayment_preparation_phase_returns_409" genossi_bin/tests/e2e_tests.rs` >= 1
    - `grep -c "test_export_repayment_unknown_phase_id_returns_404" genossi_bin/tests/e2e_tests.rs` >= 1
    - `grep -c "test_export_repayment_does_not_break_audit_chain" genossi_bin/tests/e2e_tests.rs` >= 1
    - `grep -c "fn create_member_without_iban" genossi_bin/tests/e2e_tests.rs` >= 1
    - `grep -c "Müller" genossi_bin/tests/e2e_tests.rs` >= 1 (REVISION-Fix W6: Umlaut-Member im Happy-Path)
    - `grep -cE 'auszahlung-\{\}-[a-z]+\.pdf' genossi_bin/tests/e2e_tests.rs` >= 3 ODER `grep -c "auszahlung-2026" genossi_bin/tests/e2e_tests.rs` >= 3 (REVISION-Fix W4: Filename-Assertion in >=3 PDF-Tests)
    - Alle 6 Tests sind gruen (`cargo test ... | grep "test result: ok"`).
  </acceptance_criteria>
  <done>
    Helper-Funktion `create_member_without_iban` und 6 Kern-E2E-Tests existieren und laufen gruen. Phase-11-REST-Endpoint ist via real-running Server gegen In-Memory-SQLite verifiziert. Happy-Path enthaelt Umlaut-Member (Hans Müller — REVISION-Fix W6); jeder PDF-Erfolgsfall asserted explizit das Filename-Schema (REVISION-Fix W4).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 2 weitere E2E-Tests (Include-Filter via Filename + leere IBAN) — REVISION-Fix B2 entfernt den 403-vs-409-Test</name>
  <files>
    genossi_bin/tests/e2e_tests.rs (MODIFY — append weitere Tests)
  </files>
  <read_first>
    - genossi_bin/tests/e2e_tests.rs (Phase-7-Lifecycle-Tests fuer phase open/close, Phase-9 mark-paid-out)
    - .planning/phases/11-export-pdf-csv/11-CONTEXT.md (D-01/D-02 include-Filter-Semantik, D-06 leere IBAN)
    - .planning/phases/11-export-pdf-csv/11-03-PLAN.md (Service-Layer-Mock-Test `test_include_filter_row_counts` — verifiziert Row-Counts DIREKT)
    - genossi_bin/tests/e2e_tests.rs Task 1 (vorherige Tests im selben Plan — fuer Stil-Konsistenz)
  </read_first>
  <behavior>
    - **REVISION-Fix W1:** Include-Filter wird primaer durch `test_include_filter_row_counts` in 11-03 (Service-Layer-Mock-Test) verifiziert. Der E2E-Test hier dient nur als Smoke-Test + Filename-Assertion fuer alle 3 Varianten — KEIN Groessen-Vergleich-Indirekter-Row-Count-Test mehr.
    - `?include=open` Smoke-Test: 4 Entries (Mix), Export liefert 200 + PDF-Magic + Filename `auszahlung-2026-open.pdf`
    - `?include=all` Smoke-Test: gleiches Setup, 200 + Filename `auszahlung-2026-all.pdf`
    - `?include=paid` Smoke-Test: gleiches Setup, 200 + Filename `auszahlung-2026-paid.pdf`
    - Leere-IBAN-Test: Member ohne IBAN + Auto-Fill-Entry -> PDF rendert ohne Crash; Status 200 + Filename-Assertion
    - **REVISION-Fix B2:** Der frueher hier geplante Pitfall-#2-Test `test_export_repayment_non_admin_on_preparation_returns_403_not_409` wird ENTFERNT. Begruendung: Das e2e-Test-Harness laeuft mit `#![cfg(feature = "mock_auth")]` und mock_auth-Middleware injiziert IMMER admin-MockContext — non-admin ist nicht herstellbar, der Test waere ein No-op (200 statt 403). Stattdessen verifiziert Plan 11.03 (`test_non_admin_on_preparation_returns_permission_denied_not_conflict`) die Funnel-Order via Mocks am Service-Layer.
  </behavior>
  <action>
    SCHRITT 1: Include-Filter-Smoke-Tests inkl. Filename-Assertion (REVISION-Fix W1 + W4). Indirekt durch Filename + Status 200 + PDF-Magic. Row-Count-Verifikation lebt in Plan 11.03 (`test_include_filter_row_counts` — direkter Service-Layer-Mock-Test).

    ```rust
    /// Phase 11 EXPO-03 / D-01 / D-02: Smoke-Test fuer alle 3 Include-Varianten.
    /// REVISION-Fix W1: Direkter Row-Count-Test ist in Plan 11.03 (Service-Layer-Mock).
    /// Hier nur: 200 + PDF-Magic + Filename-Schema fuer jede Variante (REVISION-Fix W4).
    #[tokio::test]
    async fn test_export_repayment_include_filter_smoke_all_three_variants() {
        let server = setup_with_templates().await;
        let client = reqwest::Client::new();

        let fiscal_year = 2026;
        // Setup: 4 members + Open-Phase -> 4 Auto-Fill-Entries (Status Open).
        for i in 0..4 {
            let _ = create_member_with_exit_date(&client, &server, 400 + i, fiscal_year, 1).await;
        }
        let phase = create_open_repayment_phase(&client, &server, fiscal_year, 12000).await;

        // Read 4 entries.
        let resp = client
            .get(server.url(&format!("/api/repayment-entry?phase_id={}", phase.id)))
            .send()
            .await
            .unwrap();
        let entries: Vec<serde_json::Value> = resp.json().await.unwrap();
        assert_eq!(entries.len(), 4, "Auto-fill should produce 4 entries");

        // Toggle entry[0] to Contacted (via batch-status if available, else PUT).
        let entry0_id = entries[0]["id"].as_str().unwrap();
        let body = serde_json::json!({
            "entry_ids": [entry0_id],
            "status": "Contacted"
        });
        let resp = client
            .post(server.url("/api/repayment-entry/batch-status"))
            .json(&body)
            .send()
            .await
            .unwrap();
        assert!(resp.status().is_success(), "batch-status -> Contacted failed: {:?}", resp.text().await);

        // Mark entry[1] PaidOut.
        let entry1_id = entries[1]["id"].as_str().unwrap();
        let resp = client
            .post(server.url(&format!("/api/repayment-entry/{}/mark-paid-out", entry1_id)))
            .send()
            .await
            .unwrap();
        assert!(resp.status().is_success(), "mark-paid-out failed: {:?}", resp.text().await);

        // Now state: 2 Open + 1 Contacted + 1 PaidOut.
        // Verifiziere jede Variante mit Status 200, PDF-Magic, korrektem Filename.
        for include in &["open", "all", "paid"] {
            let resp = client
                .get(server.url(&format!(
                    "/api/repayment-phase/{}/export/pdf?include={}",
                    phase.id, include
                )))
                .send()
                .await
                .unwrap();
            assert_eq!(resp.status(), reqwest::StatusCode::OK, "export include={} failed", include);
            let cd = resp.headers().get("content-disposition").and_then(|h| h.to_str().ok()).unwrap_or("").to_string();
            // REVISION-Fix W4: Filename-Assertion fuer jede Variante.
            assert!(
                cd.contains(&format!("auszahlung-{}-{}.pdf", fiscal_year, include)),
                "Filename for include={} must follow schema 'auszahlung-{}-{}.pdf'; got '{}'",
                include, fiscal_year, include, cd
            );
            let bytes = resp.bytes().await.unwrap();
            assert!(bytes.starts_with(b"%PDF-"), "include={} should return PDF", include);
            assert!(bytes.len() > 500, "include={} PDF too small ({} bytes)", include, bytes.len());
        }
    }
    ```

    SCHRITT 2: Leere-IBAN-Test (D-06) — REVISION-Fix W4 (Filename-Assertion zusaetzlich):

    ```rust
    /// Phase 11 D-06: Member ohne IBAN -> PDF rendert ohne Crash, IBAN-Spalte leer.
    /// REVISION-Fix W4: Filename-Schema-Assertion.
    #[tokio::test]
    async fn test_export_repayment_empty_iban_renders_empty_column() {
        let server = setup_with_templates().await;
        let client = reqwest::Client::new();

        let fiscal_year = 2026;
        // Setup: 1 member WITH IBAN, 1 member WITHOUT IBAN, Open-Phase.
        let _m1 = create_member_with_exit_date(&client, &server, 501, fiscal_year, 1).await;
        let _m2 = create_member_without_iban(&client, &server, 502, fiscal_year, 1).await;
        let phase = create_open_repayment_phase(&client, &server, fiscal_year, 12000).await;

        let resp = client
            .get(server.url(&format!(
                "/api/repayment-phase/{}/export/pdf?include=open",
                phase.id
            )))
            .send()
            .await
            .unwrap();
        assert_eq!(resp.status(), reqwest::StatusCode::OK, "export with empty-IBAN member must succeed");
        // REVISION-Fix W4: Filename-Schema-Assertion.
        let cd = resp.headers().get("content-disposition").and_then(|h| h.to_str().ok()).unwrap_or("").to_string();
        assert!(
            cd.contains(&format!("auszahlung-{}-open.pdf", fiscal_year)),
            "Filename must follow schema even with empty-IBAN member; got '{}'", cd
        );
        let bytes = resp.bytes().await.unwrap();
        assert!(bytes.starts_with(b"%PDF-"));
        assert!(bytes.len() > 1000);
    }
    ```

    **REVISION-Fix B2 — KEIN E2E-Test mehr:**
    Der vorher geplante `test_export_repayment_non_admin_on_preparation_returns_403_not_409` wurde aus diesem Plan ENTFERNT. Pitfall #2 wird stattdessen in **Plan 11.03** durch den Service-Layer-Mock-Test `test_non_admin_on_preparation_returns_permission_denied_not_conflict` verifiziert. Begruendung: `genossi_bin/tests/e2e_tests.rs` laeuft mit `#![cfg(feature = "mock_auth")]`, und die `mock_auth`-Middleware injiziert IMMER eine admin-Identitaet — non-admin ist im E2E-Setup nicht herstellbar. Ein E2E-403-Test waere ROT (oder 200 statt 403) und liefert keine echte Mitigations-Verifikation.

    Compile-Time-Mitigation: weiter durch den Grep-Gate-Test (`no_audit_macros_used`) abgesichert.
  </action>
  <verify>
    <automated>
      cargo test -p genossi_bin --test e2e_tests test_export_repayment_include_filter_smoke_all_three_variants 2>&1 | tail -20;
      cargo test -p genossi_bin --test e2e_tests test_export_repayment_empty_iban_renders_empty_column 2>&1 | tail -20
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test_export_repayment_include_filter_smoke_all_three_variants" genossi_bin/tests/e2e_tests.rs` >= 1
    - `grep -c "test_export_repayment_empty_iban_renders_empty_column" genossi_bin/tests/e2e_tests.rs` >= 1
    - **REVISION-Fix B2:** `grep -c "test_export_repayment_non_admin_on_preparation_returns_403_not_409" genossi_bin/tests/e2e_tests.rs` == 0 (der ENTFERNTE Test darf NICHT existieren, weil er unter mock_auth ein No-op waere)
    - Alle 2 Tests sind gruen.
    - Grep-Gate-Test aus Plan 11.03 weiterhin gruen: `cargo test -p genossi_service_impl --lib no_audit_macros_used 2>&1 | grep "test result: ok"` >= 1
    - Pitfall-#2-Mock-Test aus Plan 11.03 gruen: `cargo test -p genossi_service_impl --lib test_non_admin_on_preparation_returns_permission_denied_not_conflict 2>&1 | grep "test result: ok"` >= 1
    - Shell-Grep-Gate: `grep -E "audited_(create|update|delete)!" /home/neosam/programming/rust/projects/genossi3/genossi_service_impl/src/repayment_export.rs | grep -v "^[[:space:]]*//" | grep -cE "audited_create!|audited_update!|audited_delete!"` == 0
  </acceptance_criteria>
  <done>
    Beide weiteren E2E-Tests sind gruen. Phase 11 ist vollstaendig verifiziert: EXPO-01/02/03/05 alle gegen real-running Server bewiesen (Happy-Path inkl. Umlaut + Filename-Schema in ALLEN PDF-Tests). Pitfall #2 ist durch Service-Layer-Mock in 11-03 abgedeckt; KEIN E2E-403-Negative-Test (waere unter mock_auth ein No-op). Grep-Gate aus Plan 11.03 hat keinen Regression durch Plan 11.06 erlebt.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-Harness ↔ Production-Code | E2E-Tests laufen gegen REAL HTTP Server mit IN-MEMORY-SQLite — verifizieren Production-Pfade ohne Mocks |
| Test-Harness ↔ Audit-Chain | `/api/audit/verify`-Endpoint wird vor und nach Export-Call abgefragt; Chain-Integrity ist verbindlicher Test-Assert |
| Test-Harness ↔ Auth-Identitaet | Unter `mock_auth`-Feature kann KEIN non-admin-Pfad hergestellt werden; Pitfall #2 wird daher auf Service-Layer in Plan 11.03 via Mocks verifiziert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-002 | Information Disclosure | Pitfall #2 Status-Leak | mitigate | **REVISION-Fix B2:** Verifikation via Service-Layer-Mock-Test in Plan 11.03 (`test_non_admin_on_preparation_returns_permission_denied_not_conflict`). E2E-Verifikation ist unter `mock_auth` strukturell unmoeglich. Severity: MEDIUM. |
| T-003 | Information Disclosure | Status-Leak via 409 statt 403 | mitigate | Plan 11.03 Mock-Test asserted `PermissionDenied` direkt; assert_ne! gegen `Conflict`. Code-Reviewer sieht den Intent. Severity: LOW. |
| T-006 | Repudiation | Audit-Chain-Bruch durch Export | mitigate | Test `test_export_repayment_does_not_break_audit_chain` verifiziert vor+nach Export, dass `valid: true` bleibt. REVISION-Fix W7: Truth reduziert auf `valid: true`; Compile-Time-Gate (Plan 11.03 `no_audit_macros_used`) garantiert dass kein neuer Eintrag entstehen kann. Severity: LOW. |
| T-008 | Tampering | Format-Whitelist-Umgehung | mitigate | Test `test_export_repayment_unknown_format_returns_400` enumeriert 4 nicht-pdf-Formate (csv, xlsx, json, html); jeder muss 400 sein. Severity: LOW. |
| T-010 | Tampering | Member-Update-Race im Helper | accept | `create_member_without_iban` verwendet seriellen PUT mit version-Roundtrip; kein paralleler Zugriff auf den Member im Test. Severity: N/A. |
| T-011 | Tampering | D-05 (ASCII-Sanitization) verletzt im E2E-Pfad | mitigate | REVISION-Fix W6: Happy-Path-Test 1 verwendet Member mit Umlaut `Hans Müller`. Falls Typst-Renderer Umlaute droppt/sanitisiert, schlaegt der Test fehl (PDF-Decoding-Error oder 500). Severity: LOW. |
</threat_model>

<verification>
- Alle 8 E2E-Tests gruen (6 aus Task 1 + 2 aus Task 2).
- Helper `create_member_without_iban` ist verfuegbar.
- Grep-Gate aus Plan 11.03 (`no_audit_macros_used`) weiterhin gruen.
- Pitfall-#2-Mock-Test aus Plan 11.03 weiterhin gruen.
- Shell-Grep `grep -E "audited_(create|update|delete)!" genossi_service_impl/src/repayment_export.rs | grep -v "^[[:space:]]*//"` liefert KEINE Treffer im Code-Path.
- Audit-Chain bleibt valide nach Export-Calls.
- Format-Whitelist hat 4 Negative-Beispiele (csv, xlsx, json, html) plus 1 Positive (pdf).
- Filename-Assertions in 6+ PDF-Tests (REVISION-Fix W4): Happy-Path, Closed-Phase, Audit-Chain, alle 3 Include-Smoke-Varianten, Empty-IBAN.
- Umlaut-Member im Happy-Path (REVISION-Fix W6): `Hans Müller` rendert ohne Crash via Typst.
- KEIN E2E-403-Negative-Test (REVISION-Fix B2): `test_export_repayment_non_admin_on_preparation_returns_403_not_409` wurde ENTFERNT.
</verification>

<success_criteria>
- EXPO-01 (PDF-Export Open+Closed): Test 1 + Test 2 inkl. Filename-Assertions.
- EXPO-02 (PDF-Inhalt 6 Spalten + Sort + Umlaut-Erhalt): Test 1 mit `Hans Müller` als E2E-Beweis fuer D-05; vollstaendige Inhalt-Verifikation via Service-Layer-Mock in 11-03.
- EXPO-03 (Include-Filter): Test 3 Smoke-Test alle 3 Varianten + Filename-Assertion; Row-Count-Beweis via Service-Layer-Mock in 11-03.
- EXPO-05 (Vorstand-only, kein Audit): Test 6 (Audit-Chain `valid: true` vor+nach) + Compile-Time-Grep-Gate aus 11-03.
- D-12 (CSV gestrichen): Test 4 verifiziert 400 fuer csv/xlsx/json/html.
- D-06 (leere IBAN): Test 8 verifiziert PDF-Render ohne Crash inkl. Filename-Assertion.
- D-10 (Status-Gate): Test 5 (Preparation -> 409) + Test 2 (Closed -> 200) + Test 1 (Open -> 200).
- D-11 (Pitfall #2 Information-Leak): durch 11-03 Service-Layer-Mock-Test verifiziert; KEIN E2E-Test (mock_auth-Constraint).
- REVISION-Fix W4 (Filename in jedem PDF-Test): 6+ Tests asserten das Filename-Schema explizit.
- REVISION-Fix W6 (Umlaut-Member im Happy-Path): Test 1 enthaelt `Hans Müller`.
- REVISION-Fix W7 (Audit-Chain-Truth): reduziert auf `valid: true`, kein Runtime-Count-Delta-Check noetig (Compile-Time durch Grep-Gate abgedeckt).
- REVISION-Fix B2 (Pitfall #2 keinem No-op E2E zuordnen): E2E-403-Test entfernt; Verifikation in 11-03 Service-Layer-Mock.
</success_criteria>

<output>
After completion, create `.planning/phases/11-export-pdf-csv/11-06-SUMMARY.md`
</output>
