---
phase: 07-repaymentphase-backend-foundation
plan: 05
type: execute
wave: 5
depends_on: [04]
files_modified:
  - genossi_bin/tests/e2e_tests.rs
autonomous: true
requirements:
  - PHAS-01
  - PHAS-02   # E2E open → status=Open + opened_at set; Auto-Befüllung kommt Phase 8
  - PHAS-03   # E2E close → status=Closed + closed_at set; Pending-Validation kommt Phase 8
  - PHAS-04
  - PHAS-05
user_setup: []

must_haves:
  truths:
    - "Lifecycle-E2E: create → open → update share_value → close läuft erfolgreich; alle Schritte liefern 200/201; finale Phase hat Status=Closed mit gesetzten opened_at/closed_at-Timestamps"
    - "Nach dem Lifecycle-Run ist `/api/audit/verify` weiterhin `valid=true` mit `broken_links=[]` (ROADMAP SC#4)"
    - "Audit-Log für die Phase enthält 4 distinkte Prozesse: `repayment-phase.create`, `repayment-phase.open`, `repayment-phase.update`, `repayment-phase.close`"
    - "Der update share_value-Schritt erzeugt mindestens 1 Audit-Entry mit Feld-Diff `share_value` (ROADMAP SC#5)"
    - "Edit-Matrix-Negative-Paths verifiziert: fiscal_year-Change in Open → 409, Reverse Open-from-Closed → 409, Reverse Close-from-Preparation → 409, DELETE im Open → 409, Version-Mismatch im PUT → 409"
    - "Field-Validation-Negative-Paths verifiziert: fiscal_year=1999 → 400, share_value=0 → 400"
  artifacts:
    - path: "genossi_bin/tests/e2e_tests.rs"
      provides: "Mindestens 7 neue E2E-Tests am Datei-Ende (Lifecycle + 6 Negative-Paths) inkl. Test-Helper"
      contains: "test_repayment_phase_lifecycle_audit_chain_intact"
  key_links:
    - from: "neue E2E-Tests"
      to: "/api/repayment-phase (Plan 04 Routen)"
      via: "reqwest::Client mit server.url(\"/api/repayment-phase\")"
      pattern: "/api/repayment-phase"
    - from: "Lifecycle-Test"
      to: "/api/audit/verify (existierende Phase-1+ Audit-Verifikation)"
      via: "VerifyResponseTO-JSON"
      pattern: "/api/audit/verify"
---

<objective>
Phase 7 Plan 05 (Wave 5) — End-to-End-Tests gegen den real laufenden HTTP-Server, die alle 5 ROADMAP-Success-Criteria und alle Phase-7-Edit-Matrix-/Lifecycle-/Validation-Decisions verifizieren.

Purpose: Schließt die Build-Pyramide ab. Nach diesem Plan ist Phase 7 als fertige Foundation verifiziert — Plan 8 (RepaymentEntry + Auto-Befüllung) kann hier aufbauen ohne Sorge, dass das Aggregat-Verhalten nicht stimmt.

Output:
- Erweiterung `genossi_bin/tests/e2e_tests.rs` um 7 neue Tests am Datei-Ende
- Mindestens 1 Test-Helper-Funktion `create_preparation_repayment_phase(...)` falls hilfreich
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-repaymentphase-backend-foundation/07-CONTEXT.md
@.planning/phases/07-repaymentphase-backend-foundation/07-PATTERNS.md
@.planning/phases/07-repaymentphase-backend-foundation/07-04-PLAN.md
@CLAUDE.md

<!-- 1:1-E2E-Vorlagen — Executor liest BEVOR er die neuen Tests schreibt -->
@genossi_bin/tests/e2e_tests.rs
@genossi_rest/src/test_server.rs
@genossi_rest_types/src/lib.rs
@genossi_rest/src/repayment_phase.rs

<interfaces>
<!-- Test-Server-Helper (existierend, aus Phase 1+; wird von Plan 04 mit RepaymentPhaseRestState-Bound erweitert) -->
From genossi_rest/src/test_server.rs:
```rust
pub async fn start_test_server() -> TestServer { /* random port, in-memory SQLite */ }
pub struct TestServer { pub addr: SocketAddr, ... }
impl TestServer { pub fn url(&self, path: &str) -> String { ... } }
```

<!-- Existierender Audit-Verify-Endpoint und seine Response-Form -->
From genossi_rest/src/audit_log.rs (existierend seit Phase 1 Plan 03):
- `GET /api/audit/verify` liefert `{ valid: bool, broken_links: Vec<_>, total_entries: usize }`
- `GET /api/audit/{entity_type}/{entity_id}` liefert `Vec<AuditLogEntryTO>` mit `process: String, fields: Vec<{field, old_value, new_value}>`

<!-- TOs aus Plan 04 Task 1 -->
From genossi_rest_types/src/lib.rs (Plan 04 Task 1):
- `RepaymentPhaseTO { id, fiscal_year, share_value, status, opened_at, closed_at, created, deleted, version }`
- `RepaymentPhaseStatusTO::{Preparation, Open, Closed}` (Serde-JSON-Strings sind die Variant-Namen)
- `CreateRepaymentPhaseRequest { fiscal_year: i32, share_value: i64 }`
- `UpdateRepaymentPhaseRequest { fiscal_year: i32, share_value: i64, version: Uuid }`

<!-- Existierendes Assembly-E2E-Pattern als Vorlage -->
From genossi_bin/tests/e2e_tests.rs (`test_assembly_lifecycle_audit_chain_intact` Z. 8361-8514 lt. PATTERNS.md §11):
- Pattern: `setup().await → reqwest::Client::new() → POST create → assert 201 + JSON-Body → POST /open → assert 200 → POST /close → assert 200 → GET /api/audit/verify → assert valid=true`
- Conflict-Pattern: gleicher Setup + falscher Status-Übergang → `assert_eq!(response.status(), StatusCode::CONFLICT)`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 7 neue E2E-Tests in `genossi_bin/tests/e2e_tests.rs` ergänzen (Lifecycle + 6 Negative-Paths)</name>
  <files>genossi_bin/tests/e2e_tests.rs</files>
  <read_first>
    - genossi_bin/tests/e2e_tests.rs (suche per Grep: `grep -n 'test_assembly_lifecycle_audit_chain_intact\|test_close_assembly_from_preparation\|test_open_assembly_from_closed' genossi_bin/tests/e2e_tests.rs` — dann lese diese Test-Blöcke; das `setup()`-Helper-Pattern und die Imports am Datei-Anfang ebenfalls lesen)
    - genossi_rest_types/src/lib.rs (RepaymentPhaseTO-Felder + Status-Enum-JSON-Repräsentation)
    - genossi_rest/src/test_server.rs (start_test_server-Signatur — Plan 04 Task 2 hat die Trait-Bounds erweitert)
    - .planning/phases/07-repaymentphase-backend-foundation/07-PATTERNS.md §11 (E2E-Test-Anforderungen-Liste; Punkte 1-7)
    - .planning/phases/07-repaymentphase-backend-foundation/07-CONTEXT.md §domain (E2E-Test-Spec)
  </read_first>
  <behavior>
    - Test 1 (`test_repayment_phase_lifecycle_audit_chain_intact`) — entspricht ROADMAP SC#4 + SC#5:
        1. `start_test_server()`; reqwest-Client.
        2. POST `/api/repayment-phase` mit `{"fiscal_year":2026,"share_value":12000}` → 201; aus Body `RepaymentPhaseTO` → `phase_id`, assert status=Preparation, opened_at=None, closed_at=None.
        3. POST `/api/repayment-phase/{phase_id}/open` (kein Body) → 200; assert status=Open, opened_at=Some(_), closed_at=None.
        4. GET `/api/repayment-phase/{phase_id}` → liest aktuelle Version-UUID (`version_v1`) für den nächsten PUT.
        5. PUT `/api/repayment-phase/{phase_id}` mit Body `{"fiscal_year":2026,"share_value":13000,"version":"<version_v1>"}` (Edit-Matrix D-04: share_value in Open EDIT, fiscal_year UNCHANGED) → 200; assert share_value=13000, status=Open (unverändert), version != version_v1 (Version-Bump).
        6. POST `/api/repayment-phase/{phase_id}/close` → 200; assert status=Closed, closed_at=Some(_).
        7. GET `/api/audit/verify` → `valid=true`, `broken_links.is_empty()`, `total_entries >= 4` (ROADMAP SC#4).
        8. GET `/api/audit/repayment_phase/{phase_id}` → Liste der Audit-Einträge; assertions:
            - Processes-Set enthält genau `{"repayment-phase.create", "repayment-phase.open", "repayment-phase.update", "repayment-phase.close"}` (4 distinkte).
            - Mindestens 1 Eintrag mit Prozess `"repayment-phase.update"` hat ein Feld-Diff mit `field="share_value"`, `old_value=Some("12000")`, `new_value=Some("13000")` (ROADMAP SC#5: share_value-Korrektur erzeugt Audit-Entry).
    - Test 2 (`test_update_repayment_phase_fiscal_year_in_open_returns_conflict`) — D-04/D-07:
        1. POST create (Preparation) → POST /open (Open).
        2. PUT mit verändertem `fiscal_year=2027` (statt 2026), correct version → 409 Conflict.
        3. Body-Substring enthält `"fiscal_year"` (Konsistenz-Check der Conflict-Message).
    - Test 3 (`test_close_repayment_phase_from_preparation_returns_conflict`) — D-05/D-06:
        1. POST create (Preparation).
        2. POST `/{id}/close` direkt → 409 Conflict.
    - Test 4 (`test_open_repayment_phase_from_closed_returns_conflict`) — D-06:
        1. POST create → POST /open → POST /close (Closed).
        2. POST `/{id}/open` erneut → 409 Conflict.
    - Test 5 (`test_delete_repayment_phase_in_open_returns_conflict`) — D-09:
        1. POST create → POST /open (Open).
        2. DELETE `/{id}` → 409 Conflict.
    - Test 6 (`test_validation_fiscal_year_out_of_range_returns_400`) — D-11:
        1. POST `/api/repayment-phase` mit `{"fiscal_year":1999,"share_value":12000}` → 400 Bad Request (Service-ValidationError → RestError::BadRequest).
        2. Body-Substring enthält `"fiscal_year"`.
    - Test 7 (`test_validation_share_value_zero_returns_400`) — D-12:
        1. POST mit `{"fiscal_year":2026,"share_value":0}` → 400.
        2. Body-Substring enthält `"share_value"`.

    Optional (nice-to-have, falls Plan-Budget zulässt — NICHT zwingend für die acceptance_criteria):
    - Bonus 1 (`test_update_repayment_phase_version_mismatch_returns_conflict`): create → PUT mit zufälliger UUID als version → 409 mit Substring `"Version mismatch"`.
    - Bonus 2 (`test_delete_repayment_phase_in_preparation_succeeds`): create (Preparation) → DELETE → 204; danach GET liefert 404 (soft-deleted).
  </behavior>
  <action>
**Schritt A:** Suche per Grep die exakte Position von `test_assembly_lifecycle_audit_chain_intact` und seinen Block + Imports am Datei-Anfang. Lese die Test-Funktion + die Imports (insbesondere für `AssemblyTO`/`VerifyResponseTO`/`AuditLogEntryTO`/`StatusCode`). Ergänze in den Imports am Datei-Anfang die Phase-7-TOs (`RepaymentPhaseTO`, `RepaymentPhaseStatusTO` aus `genossi_rest_types`).

**Schritt B:** Ergänze einen Test-Helper am sinnvollsten Platz (z.B. nahe der existierenden `create_open_assembly_with_members`-Helper-Funktion):

```rust
async fn create_preparation_repayment_phase(
    client: &reqwest::Client,
    server_url: impl Fn(&str) -> String,
    fiscal_year: i32,
    share_value: i64,
) -> RepaymentPhaseTO {
    let body = serde_json::json!({
        "fiscal_year": fiscal_year,
        "share_value": share_value,
    });
    let response = client
        .post(server_url("/api/repayment-phase"))
        .json(&body)
        .send()
        .await
        .unwrap();
    assert_eq!(response.status(), reqwest::StatusCode::CREATED);
    response.json().await.unwrap()
}
```
(Helper ist optional; Tests können auch inline POSTen. Beides ist akzeptabel — Executor entscheidet auf Test-Lesbarkeits-Grundlage.)

**Schritt C:** Füge die 7 Tests am Datei-Ende an. Verwende `#[tokio::test]`. Setup-Pattern aus assembly-Lifecycle-Test kopieren (`setup().await` oder `start_test_server().await` — je nachdem was der bestehende Pattern verwendet; lesen, nicht raten).

**Wichtige Details, die nicht übersehen werden dürfen:**
1. **JSON-Serialisierung von `RepaymentPhaseStatusTO`:** Default-Serde-Variant-Naming ist PascalCase (Plan 04 Task 1 hat keinen `#[serde(rename_all)]` gesetzt — analog `AssemblyStatusTO`). Im JSON-Body steht `"Preparation"`/`"Open"`/`"Closed"`. Assertion-Pattern: `assert_eq!(phase.status, RepaymentPhaseStatusTO::Preparation)` nach `.json().await.unwrap()`.
2. **Version-UUID Extraktion für PUT:** Nach POST `/{id}/open` enthält das Response-JSON die neue `version`-UUID (UPDATE bumpt sie). Für den nächsten PUT muss diese frische UUID verwendet werden — entweder direkt aus dem Open-Response oder via GET nach dem Open. Beides funktioniert; explizites GET ist robuster.
3. **Audit-Response-Format:** GET `/api/audit/{entity_type}/{entity_id}` mit `entity_type="repayment_phase"`. Falls die existierende Endpoint-Signatur deviates (z.B. `{entity_type}/{id}` mit Bindestrich-Trennung), lese kurz `genossi_rest/src/audit_log.rs` und passe entsprechend an. Pattern aus Assembly-Test (Z. 1208-1214 lt. PATTERNS.md §11): `client.get(server.url(&format!("/api/audit/assembly/{}", assembly_id)))`.
4. **Feld-Diff-Assertion in Test 1 Schritt 8:** `AuditLogEntryTO` hat `fields: Vec<{field, old_value, new_value}>`. Iterate über `entries.iter().flat_map(|e| e.fields.iter())` und suche `field == "share_value" && new_value == Some("13000".to_string())`. Falls die exakte Struktur abweicht (z.B. `old_value: Option<String>` vs `Option<serde_json::Value>`), lese kurz `genossi_rest_types/src/lib.rs` für die TO-Definition und passe an.
5. **Tests müssen unabhängig laufen können:** jeder Test ruft `start_test_server()` neu auf (in-memory SQLite). Keine geteilten Zustände.
6. **Verifiziere nichts, was Phase 8 erst bringt:** keine RepaymentEntries, keine Auto-Befüllung beim open, keine close-Validierung gegen pending Entries.
  </action>
  <verify>
    <automated>grep -c 'fn test_repayment_phase_lifecycle_audit_chain_intact' genossi_bin/tests/e2e_tests.rs | grep -q '^1$' && grep -c 'fn test_update_repayment_phase_fiscal_year_in_open_returns_conflict' genossi_bin/tests/e2e_tests.rs | grep -q '^1$' && grep -c 'fn test_close_repayment_phase_from_preparation_returns_conflict' genossi_bin/tests/e2e_tests.rs | grep -q '^1$' && grep -c 'fn test_open_repayment_phase_from_closed_returns_conflict' genossi_bin/tests/e2e_tests.rs | grep -q '^1$' && grep -c 'fn test_delete_repayment_phase_in_open_returns_conflict' genossi_bin/tests/e2e_tests.rs | grep -q '^1$' && grep -c 'fn test_validation_fiscal_year_out_of_range_returns_400' genossi_bin/tests/e2e_tests.rs | grep -q '^1$' && grep -c 'fn test_validation_share_value_zero_returns_400' genossi_bin/tests/e2e_tests.rs | grep -q '^1$' && grep -c '/api/repayment-phase' genossi_bin/tests/e2e_tests.rs | grep -qE '^[5-9]|^[0-9]{2,}' && grep -c '/api/audit/verify' genossi_bin/tests/e2e_tests.rs | grep -qE '^[1-9]' && cargo test --test e2e_tests -p genossi_bin -- repayment_phase 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - Alle 7 Tests namens-greppable in `genossi_bin/tests/e2e_tests.rs` (1 Treffer pro Funktion)
    - `grep -c '/api/repayment-phase' genossi_bin/tests/e2e_tests.rs` ≥ 5 (jeder Test trifft mindestens einen RepaymentPhase-Endpoint)
    - `grep -c '/api/audit/verify' genossi_bin/tests/e2e_tests.rs` ≥ 1 (Lifecycle-Test ruft Audit-Verify auf — ROADMAP SC#4)
    - `cargo test --test e2e_tests -p genossi_bin -- repayment_phase` — alle 7 neuen Tests grün; bestehende Tests bleiben grün
    - Anzahl der bestehenden Tests sinkt nicht (Plan 04 hat den `start_test_server`-Trait-Bound erweitert; alte Assembly-/Member-/Attendance-Tests müssen weiterhin laufen)
  </acceptance_criteria>
  <done>7 grüne E2E-Tests; Audit-Chain-Verifikation grün nach Lifecycle; alle 5 ROADMAP-Success-Criteria sind end-to-end nachgewiesen; Phase 7 ist verifikations-vollständig.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-Code ↔ Test-Server | In-memory SQLite, random ports, isolierte DBs pro Test |
| Test-Assertions ↔ Production-Verhalten | Tests verifizieren die Public REST-API; keine internen Mocks im E2E-Layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Tampering | Test "verifiziert" Erfolg, ohne die Audit-Chain tatsächlich zu validieren | mitigate | Test 1 ruft EXPLIZIT `/api/audit/verify` auf und assertet `valid=true` UND `broken_links.is_empty()` UND `total_entries >= 4`. Plus ruft `/api/audit/repayment_phase/{id}` und assertet 4 distinkte Prozess-Namen — Defense gegen "stiller Audit-Drop" (Threat T-07-03-01 aus Plan 03). |
| T-07-05-02 | Repudiation | Test 1 Schritt 8 assertet nur Existenz der Audit-Einträge, nicht den Feld-Diff für share_value | mitigate | Behavior-Spec Punkt 5 + acceptance_criteria fordern expliziten Feld-Diff-Check: `new_value == Some("13000")` für `field == "share_value"`. ROADMAP SC#5 ist damit nachgewiesen — nicht nur "ein Audit-Entry existiert", sondern "ein Audit-Entry für die share_value-Korrektur existiert mit dem korrekten neuen Wert". |
| T-07-05-03 | Information Disclosure | Test-Logs leaken Vorstand-Credentials/Session-Cookies | accept | Test-Server läuft mit `mock_auth`-Feature (kein OIDC); Mock-Context wird per Test-Default verwendet (Pattern aus assembly-E2E-Test). Kein Production-Credential im Test-Code. |
| T-07-05-04 | Denial of Service | Lifecycle-Test mit vielen sequentiellen HTTP-Calls überschreitet Rate-Limit (Phase-3 STATE.md: 60 burst, 1/sec refill) | mitigate | Test 1 macht 6 sequenzielle HTTP-Calls; Negative-Tests jeweils 1-3. Kein Test überschreitet 10 Calls — fern vom Burst-Limit. Falls Probleme auftreten, ist die Lösung NICHT Test-Verkleinerung, sondern Rate-Limit-Bypass in der Test-Konfig (existierender Pattern aus Phase 3). |
</threat_model>

<verification>
- `cargo test --test e2e_tests -p genossi_bin -- repayment_phase` — alle 7 neuen Tests grün
- Volles E2E-Testset bleibt grün: `cargo test --test e2e_tests -p genossi_bin` ist grün und die Anzahl der ausgeführten Tests >= Vor-Phase-7-Baseline + 7
- Audit-Chain ist nach allen Tests valide (Test 1 verifiziert es explizit; weitere Tests modifizieren die Chain nicht maliziös)
</verification>

<success_criteria>
- **ROADMAP SC#4 (E2E-Test create → open → close-Lifecycle erfolgreich; Audit-Chain via `/api/audit/verify` valide):** vollständig erfüllt durch Test 1
- **ROADMAP SC#5 (`share_value`-Korrektur in `Offen` erzeugt genau einen Audit-Eintrag pro Feld; `fiscal_year` nach `Offen` read-only):** Test 1 Schritt 8 prüft den share_value-Audit-Diff; Test 2 prüft, dass fiscal_year-Change in Open via 409 geblockt wird
- **PHAS-01..05:** alle End-to-End nachgewiesen
- Phase 7 ist abgeschlossen und Phase 8 kann starten
</success_criteria>

<output>
Nach Abschluss erstellen unter `.planning/phases/07-repaymentphase-backend-foundation/07-05-SUMMARY.md`:
- 7 neue E2E-Tests committed
- Anzahl bestehender + neuer Tests (Baseline + 7)
- Audit-Chain-Verifikations-Ergebnis aus Test 1 (valid=true, total_entries >= 4)
- Verlinkung zu PHASE-LEVEL-VERIFICATION: ROADMAP §Phase 7 Status auf `complete` setzen; REQUIREMENTS.md PHAS-01/04/05 auf `complete`, PHAS-02/03 auf `skeleton (Phase 8 vervollständigt)` markieren
- Empfehlung an Phase 8: hier weiterbauen, indem in `open_repayment_phase` die Auto-Befüllung + in `close_repayment_phase` die Pending-Validation ergänzt wird (Code-Kommentare sind als Anker da)
</output>
