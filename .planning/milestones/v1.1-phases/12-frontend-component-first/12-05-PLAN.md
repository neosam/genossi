---
phase: 12-frontend-component-first
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - genossi-frontend/src/page/repayment_phase_details.rs
autonomous: true
requirements: [UI-02]
tags: [frontend, page, detail, tabs, lifecycle]

must_haves:
  truths:
    - "Detail-Page lädt Phase via api::get_repayment_phase(id) und nutzt TabStrip mit 3 fixen Tabs (D-06: alle Tabs IMMER sichtbar — kein dynamischer 4. Tab wie assembly_details.rs)"
    - "Header: Titel mit fiscal_year + RepaymentPhaseStatusBadge — KEINE Lifecycle-Buttons im Header (D-03)"
    - "BasicsTab-Inline-Component zeigt fiscal_year, share_value (read-only Display, Edit kommt in Plan 12-06), und Lifecycle-Action-Tile (D-03) mit ÖFFNEN-Button (Vorbereitung→Offen, KEIN Confirm laut D-07) und SCHLIESSEN-Button (Offen→Abgeschlossen, MIT Confirm D-07)"
    - "Schließen-Confirm-Modal MIT roter Bestätigung (Pattern aus basics_tab.rs Z. 215-249)"
    - "409 CloseConflictResponse-Body wird im Fehler-Fall via serde_json::from_str deserialisiert und als ErrorAlert mit pending_count + pending_member_numbers angezeigt (D-04 + Open-Question 5: ErrorAlert mit Details-Expand statt nur Toast). Toast-Fallback wenn detail-Body nicht parsen lässt."
    - "EntriesTab und ExportTab sind in dieser Wave Stubs (Plan 12-08 + 12-14 implementieren sie); Stubs zeigen 'TODO Plan 12-08' bzw. 'TODO Plan 12-14'; Tab-Switching funktioniert"
    - "D-06: Bei status=Preparation zeigen EntriesTab und ExportTab eine Hinweis-Box 'Phase noch nicht geöffnet' (i18n Key::RepaymentEntriesNotOpenYet / RepaymentExportNotOpenYet); bei status=Open/Closed zeigen sie ihren Stub-Body"
    - "Optimistic-Locking-Reload nach Open/Close-Success (Pitfall 7 + Phase-8-CR-01): load_phase wird erneut aufgerufen, statt die Response-Version zu verwenden"
    - "D-09: Nach Open-Success KEIN Auto-Tab-Switch zum Einträge-Tab — Page reloaded den Phase-State, Tab bleibt auf 'basics'"
    - "ToastContainer + ErrorAlert beide für Fehler verfügbar (Plan-Discretion: Toast für non-409-Errors; ErrorAlert mit Detail-Expand für 409 CloseConflictResponse)"
    - "D-28: Tab-Strip via existierende component::TabStrip (verifiziert in 12-RESEARCH.md §Reuse Asset Inventory — component/tab_strip.rs existiert mit Props { tabs, active_key, on_change, children }); KEINE Extraktion aus assembly_details.rs nötig (Fallback-Klausel von D-28 entfällt). Direct-Reuse macht Phase 12 zur zweiten Detail-Page mit Tab-Layout und manifestiert den Component-First-Anker."
  artifacts:
    - path: "genossi-frontend/src/page/repayment_phase_details.rs"
      provides: "#[component] pub fn RepaymentPhaseDetails(id: String) -> Element; #[component] fn BasicsTab(...); EntriesTab/ExportTab als inline-fn Stubs"
      contains: "TabStrip"
      min_lines: 200
  key_links:
    - from: "Detail-Page header"
      to: "RepaymentPhaseStatusBadge"
      via: "Component-Reuse"
      pattern: "RepaymentPhaseStatusBadge \\{ status: phase.status"
    - from: "BasicsTab Schließen-Action"
      to: "/api/repayment-phase/{id}/close"
      via: "api::close_repayment_phase nach Confirm"
      pattern: "api::close_repayment_phase"
    - from: "BasicsTab"
      to: "TabStrip body branch on 'basics'"
      via: "match active_key"
      pattern: "\"basics\" =>"
    - from: "Page"
      to: "RequirePrivilege admin"
      via: "Auth-Wrap"
      pattern: "RequirePrivilege"
---

<objective>
UI-02 Skelett: Detail-Page lädt Phase, zeigt 3-Tab-Layout (Stammdaten + Einträge + Export) konsistent mit `assembly_details.rs`. Stamm-Tab hat Lifecycle-Action-Tile (D-03) mit Öffnen + Schließen + Confirm-Modal + 409-CloseConflictResponse-Handling (D-04). EntriesTab und ExportTab sind in dieser Phase Stubs — Plan 12-08 + 12-14 ersetzen sie.

Purpose: Vorstand kann eine Phase öffnen UND schließen (Lifecycle-Cover). Phase-State-Reload nach jeder Mutation ist Pflicht (Phase 8 CR-01-Pattern). Das ist die kritische Detail-Page-Vorlage für 12-06 (share_value-Inline-Edit) und 12-08 (RepaymentEntryList-Mount).

Output: Ersetzt repayment_phase_details.rs-Stub aus Plan 12-03 mit voller Detail-Page-Implementation.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-frontend-component-first/12-CONTEXT.md
@.planning/phases/12-frontend-component-first/12-RESEARCH.md
@.planning/phases/12-frontend-component-first/12-PATTERNS.md
@.planning/phases/12-frontend-component-first/12-01-PLAN.md
@.planning/phases/12-frontend-component-first/12-02-PLAN.md

<interfaces>
<!-- API aus Plan 12-01 -->
pub async fn get_repayment_phase(config, id: Uuid) -> Result<RepaymentPhaseTO, AppError>;
pub async fn open_repayment_phase(config, id: Uuid) -> Result<RepaymentPhaseTO, AppError>;
pub async fn close_repayment_phase(config, id: Uuid) -> Result<RepaymentPhaseTO, AppError>;  // 409+CloseConflictResponse möglich
pub struct CloseConflictResponse { error: String, pending_count: usize, pending_member_numbers: Vec<String> }

<!-- Components -->
crate::component::{Modal, TabDef, TabStrip, ToastContainer, TopBar, show_toast, RepaymentPhaseStatusBadge, ErrorAlert};

<!-- i18n-Keys -->
Key::RepaymentPhases, RepaymentPhaseTabBasics, RepaymentPhaseTabEntries, RepaymentPhaseTabExport,
Key::RepaymentPhaseOpen, RepaymentPhaseClose, RepaymentPhaseCloseConfirmTitle, RepaymentPhaseCloseConfirmText,
Key::RepaymentPhaseCloseBlocked, RepaymentEntriesNotOpenYet, RepaymentExportNotOpenYet
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure-Funcs für Status-driven Render-Logik + Unit-Tests</name>
  <read_first>
    - genossi-frontend/src/component/member_search.rs (Z. 9-35 für pure-fn pattern)
    - .planning/phases/12-frontend-component-first/12-CONTEXT.md (D-03, D-05, D-08 Edit-Matrix für Phase-Status)
    - .planning/phases/12-frontend-component-first/12-RESEARCH.md "Validation Architecture — Wave 0 Gaps" — `is_share_value_editable`, `should_show_lifecycle_button`
  </read_first>
  <behavior>
    - Test: should_show_open_button(Preparation) == true
    - Test: should_show_open_button(Open) == false
    - Test: should_show_open_button(Closed) == false
    - Test: should_show_close_button(Open) == true
    - Test: should_show_close_button(Preparation) == false
    - Test: should_show_close_button(Closed) == false
    - Test: is_share_value_editable(Preparation) == true (Plan 12-06 wird das nutzen)
    - Test: is_share_value_editable(Open) == true
    - Test: is_share_value_editable(Closed) == false
  </behavior>
  <action>
    In `genossi-frontend/src/page/repayment_phase_details.rs` (innerhalb des File-Headers, vor der `#[component] pub fn RepaymentPhaseDetails`):
    ```rust
    /// D-03 + D-08: Öffnen-Button ist nur im Status `Vorbereitung` sichtbar.
    fn should_show_open_button(status: RepaymentPhaseStatusTO) -> bool {
        matches!(status, RepaymentPhaseStatusTO::Preparation)
    }

    /// D-03 + D-08: Schließen-Button ist nur im Status `Offen` sichtbar.
    fn should_show_close_button(status: RepaymentPhaseStatusTO) -> bool {
        matches!(status, RepaymentPhaseStatusTO::Open)
    }

    /// D-05 + D-08: `share_value` ist im Status `Abgeschlossen` read-only.
    pub(crate) fn is_share_value_editable(status: RepaymentPhaseStatusTO) -> bool {
        !matches!(status, RepaymentPhaseStatusTO::Closed)
    }
    ```

    Tests in `#[cfg(test)] mod tests` der gleichen Datei:
    ```rust
    #[test]
    fn open_button_only_in_preparation() {
        assert!(should_show_open_button(RepaymentPhaseStatusTO::Preparation));
        assert!(!should_show_open_button(RepaymentPhaseStatusTO::Open));
        assert!(!should_show_open_button(RepaymentPhaseStatusTO::Closed));
    }

    #[test]
    fn close_button_only_in_open() {
        assert!(!should_show_close_button(RepaymentPhaseStatusTO::Preparation));
        assert!(should_show_close_button(RepaymentPhaseStatusTO::Open));
        assert!(!should_show_close_button(RepaymentPhaseStatusTO::Closed));
    }

    #[test]
    fn share_value_readonly_in_closed() {
        assert!(is_share_value_editable(RepaymentPhaseStatusTO::Preparation));
        assert!(is_share_value_editable(RepaymentPhaseStatusTO::Open));
        assert!(!is_share_value_editable(RepaymentPhaseStatusTO::Closed));
    }
    ```

    `pub(crate)` auf `is_share_value_editable`, weil Plan 12-06 die Funktion bei der Inline-Edit-Implementation reused.
  </action>
  <verify>
    <automated>cargo test -p genossi-frontend --lib page::repayment_phase_details::tests</automated>
  </verify>
  <done>
    - cargo test -p genossi-frontend --lib page::repayment_phase_details::tests zeigt 3 PASS
    - Funktionen exist und exhaustive (kein _-Branch)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Detail-Page voll mit TabStrip + BasicsTab + Schließen-Confirm + 409 Body-Parse</name>
  <read_first>
    - genossi-frontend/src/page/assembly_details.rs (KOMPLETT 1-563 — Vorlage; insbesondere Z. 17-50 für Imports/Component-Init, Z. 83-150 TabStrip-Pattern, Z. 322-511 Tab-Body-Patterns)
    - genossi-frontend/src/component/basics_tab.rs (KOMPLETT 1-250 — Lifecycle-Action-Tile + Schließen-Confirm)
    - genossi-frontend/src/component/error_alert.rs (Z. 1-48 für ErrorAlert mit on_dismiss + detail-Expand)
    - genossi-frontend/src/api.rs (CloseConflictResponse aus Plan 12-01; AppError.detail enthält den rohen Body)
    - .planning/phases/12-frontend-component-first/12-PATTERNS.md §2 (Detail-Page) + §6 (Confirm-Modal-Pattern aus basics_tab) + §Pattern-Toast/ErrorAlert
    - .planning/phases/12-frontend-component-first/12-CONTEXT.md (D-03 Lifecycle im Stamm-Tab, D-04 Toast/ErrorAlert für 409, D-06 statisches 3-Tab-Layout, D-07 Schließen-Confirm, D-08 Closed=read-only, D-09 kein Auto-Tab-Switch)
  </read_first>
  <action>
    Vollständig ersetzen `genossi-frontend/src/page/repayment_phase_details.rs`. Die Datei wächst zu ca. 300 Zeilen (deutlich kleiner als assembly_details.rs, weil EntriesTab + ExportTab Stubs sind):

    ```rust
    //! Repayment phase detail page (Phase 12 Plan 12-05, UI-02) — admin-only.
    //!
    //! Layout: TabStrip mit 3 fixen Tabs (Stammdaten / Einträge / Export — D-06).
    //! Header zeigt Titel + Status-Badge OHNE Lifecycle-Buttons (D-03).
    //! Stamm-Tab hat die Lifecycle-Action-Tile (D-03) mit Öffnen + Schließen.
    //! Schließen hat Confirm-Modal (D-07). Öffnen NICHT (reversibel über Edit/Delete der Entries).
    //!
    //! Status-driven render:
    //!   Preparation → Einträge-Tab + Export-Tab zeigen "Phase noch nicht geöffnet"
    //!   Open        → alle Tabs aktiv; Schließen-Button sichtbar
    //!   Closed      → alle Felder read-only (D-08); kein Lifecycle-Button
    //!
    //! Plan 12-06 erweitert um `share_value`-Inline-Edit (D-05).
    //! Plan 12-08 ersetzt EntriesTab-Stub mit RepaymentEntryList.
    //! Plan 12-14 ersetzt ExportTab-Stub mit Include-Filter + PDF-Download.

    use dioxus::prelude::*;
    use std::str::FromStr;
    use uuid::Uuid;

    use crate::api::{
        self, AppError, CloseConflictResponse, RepaymentPhaseStatusTO, RepaymentPhaseTO,
    };
    use crate::auth::RequirePrivilege;
    use crate::component::repayment_format::format_payout_eur;
    use crate::component::{
        ErrorAlert, Modal, RepaymentPhaseStatusBadge, TabDef, TabStrip, ToastContainer, TopBar,
        show_toast,
    };
    use crate::i18n::{use_i18n, Key};
    use crate::page::access_denied::AccessDeniedPage;
    use crate::service::config::CONFIG;

    fn should_show_open_button(status: RepaymentPhaseStatusTO) -> bool {
        matches!(status, RepaymentPhaseStatusTO::Preparation)
    }
    fn should_show_close_button(status: RepaymentPhaseStatusTO) -> bool {
        matches!(status, RepaymentPhaseStatusTO::Open)
    }
    pub(crate) fn is_share_value_editable(status: RepaymentPhaseStatusTO) -> bool {
        !matches!(status, RepaymentPhaseStatusTO::Closed)
    }

    /// Parst den 409-Detail-Body in eine CloseConflictResponse. Liefert
    /// None wenn der Body keine valide CloseConflictResponse ist (z.B.
    /// nicht-409-Error oder generischer Body).
    fn parse_close_conflict(err: &AppError) -> Option<CloseConflictResponse> {
        if err.status != Some(409) {
            return None;
        }
        let body = err.detail.as_deref()?;
        serde_json::from_str::<CloseConflictResponse>(body).ok()
    }

    #[component]
    pub fn RepaymentPhaseDetails(id: String) -> Element {
        let i18n = use_i18n();
        let phase_id = match Uuid::from_str(&id) {
            Ok(u) => u,
            Err(_) => return rsx! { div { class: "p-4 text-red-600", "Invalid phase id" } },
        };

        let mut phase = use_signal(|| Option::<RepaymentPhaseTO>::None);
        let mut loading = use_signal(|| true);
        let mut active_tab = use_signal(|| "basics".to_string());
        let mut toast_messages = use_signal(|| Vec::<(u64, String)>::new());
        let mut toast_counter = use_signal(|| 0u64);
        // D-04 + Open-Question 5: 409 CloseConflictResponse via ErrorAlert mit Detail-Expand
        let mut close_conflict = use_signal(|| Option::<CloseConflictResponse>::None);

        let load_phase = move || {
            spawn(async move {
                loading.set(true);
                let config = CONFIG.read().clone();
                match api::get_repayment_phase(&config, phase_id).await {
                    Ok(p) => phase.set(Some(p)),
                    Err(e) => show_toast(&mut toast_messages, &mut toast_counter, e.message),
                }
                loading.set(false);
            });
        };

        use_effect(move || {
            load_phase();
        });

        rsx! {
            RequirePrivilege {
                privilege: "admin",
                fallback: rsx! { AccessDeniedPage { required_privilege: "admin".to_string() } },
                TopBar {}
                div { class: "container mx-auto px-4 py-6",
                    if *loading.read() {
                        p { class: "text-gray-500 text-center py-8", "{i18n.t(Key::Loading)}" }
                    } else if let Some(p) = phase.read().clone() {
                        // D-03: Header mit Titel + Status-Badge, KEINE Lifecycle-Buttons
                        div { class: "flex items-center justify-between mb-4",
                            h1 { class: "text-2xl font-bold",
                                "{i18n.t(Key::RepaymentPhases)} {p.fiscal_year}"
                            }
                            RepaymentPhaseStatusBadge { status: p.status }
                        }

                        // D-04: 409 CloseConflictResponse als ErrorAlert mit Detail-Expand
                        if let Some(cc) = close_conflict.read().clone() {
                            ErrorAlert {
                                error: AppError {
                                    status: Some(409),
                                    message: format!(
                                        "{}: {} Einträge noch nicht ausbezahlt",
                                        i18n.t(Key::RepaymentPhaseCloseBlocked),
                                        cc.pending_count,
                                    ),
                                    detail: Some(format!(
                                        "Betroffene Mitglieder: {}",
                                        cc.pending_member_numbers.join(", ")
                                    )),
                                },
                                on_dismiss: Some(EventHandler::new(move |_| close_conflict.set(None))),
                            }
                        }

                        // D-06: TabStrip mit IMMER 3 sichtbaren Tabs (anders als assembly_details.rs)
                        {
                            let tab_defs = vec![
                                TabDef { key: "basics",  label: i18n.t(Key::RepaymentPhaseTabBasics).to_string() },
                                TabDef { key: "entries", label: i18n.t(Key::RepaymentPhaseTabEntries).to_string() },
                                TabDef { key: "export",  label: i18n.t(Key::RepaymentPhaseTabExport).to_string() },
                            ];
                            let active_key = active_tab.read().clone();
                            let status_value = p.status;
                            let phase_for_basics = p.clone();
                            let phase_for_entries = p.clone();
                            let phase_for_export = p.clone();
                            rsx! {
                                TabStrip {
                                    tabs: tab_defs,
                                    active_key: active_key.clone(),
                                    on_change: move |k: String| active_tab.set(k),
                                    match active_key.as_str() {
                                        "basics" => rsx! {
                                            BasicsTab {
                                                phase: phase_for_basics,
                                                on_changed: move |_| load_phase(),
                                                on_close_conflict: move |cc: CloseConflictResponse| close_conflict.set(Some(cc)),
                                                on_error: move |msg: String| show_toast(&mut toast_messages, &mut toast_counter, msg),
                                            }
                                        },
                                        "entries" => match status_value {
                                            RepaymentPhaseStatusTO::Preparation => rsx! {
                                                div { class: "text-center py-12 text-gray-500",
                                                    "{i18n.t(Key::RepaymentEntriesNotOpenYet)}"
                                                }
                                            },
                                            // Plan 12-08 ersetzt diesen Stub mit RepaymentEntryList
                                            _ => rsx! {
                                                div { class: "text-center py-12 text-gray-500",
                                                    "TODO Plan 12-08: RepaymentEntryList für phase_id={phase_for_entries.id}"
                                                }
                                            },
                                        },
                                        "export" => match status_value {
                                            RepaymentPhaseStatusTO::Preparation => rsx! {
                                                div { class: "text-center py-12 text-gray-500",
                                                    "{i18n.t(Key::RepaymentExportNotOpenYet)}"
                                                }
                                            },
                                            // Plan 12-14 ersetzt diesen Stub mit Format-Picker + Download
                                            _ => rsx! {
                                                div { class: "text-center py-12 text-gray-500",
                                                    "TODO Plan 12-14: Export-Tab für phase_id={phase_for_export.id}"
                                                }
                                            },
                                        },
                                        _ => rsx! {},
                                    }
                                }
                            }
                        }
                    } else {
                        p { class: "text-red-600 text-center py-8", "Phase not found" }
                    }
                }
                ToastContainer { messages: toast_messages }
            }
        }
    }

    /// Stamm-Daten-Tab — Read-Only-Display in Wave 3.
    /// Plan 12-06 erweitert um `share_value`-Inline-Edit (D-05).
    #[component]
    fn BasicsTab(
        phase: RepaymentPhaseTO,
        on_changed: EventHandler<()>,
        on_close_conflict: EventHandler<CloseConflictResponse>,
        on_error: EventHandler<String>,
    ) -> Element {
        let i18n = use_i18n();
        let mut show_close_confirm = use_signal(|| false);
        let phase_id = phase.id;
        let phase_status = phase.status;

        rsx! {
            div { class: "flex flex-col gap-6",
                // Stamm-Daten-Block (read-only in Wave 3; Plan 12-06 erweitert)
                div { class: "grid grid-cols-2 gap-4",
                    div {
                        span { class: "text-sm text-gray-500", "{i18n.t(Key::RepaymentPhaseFiscalYear)}" }
                        p { class: "text-lg font-semibold", "{phase.fiscal_year}" }
                    }
                    div {
                        span { class: "text-sm text-gray-500", "{i18n.t(Key::RepaymentPhaseShareValue)}" }
                        p { class: "text-lg font-semibold", "{format_payout_eur(1, phase.share_value)}" }
                    }
                }

                // D-03 + D-08: Lifecycle-Action-Tile als große Kachel
                if should_show_open_button(phase_status) {
                    div { class: "rounded border border-gray-200 bg-blue-50 p-4 flex items-center justify-between",
                        div {
                            p { class: "font-semibold text-blue-900", "{i18n.t(Key::RepaymentPhaseOpen)}" }
                            p { class: "text-sm text-blue-700",
                                "Beim Öffnen werden alle Vorjahres-Austritte als Einträge angelegt."
                            }
                        }
                        button {
                            r#type: "button",
                            class: "bg-blue-600 hover:bg-blue-700 text-white px-6 py-3 rounded min-h-[44px]",
                            onclick: move |_| {
                                let on_changed = on_changed.clone();
                                let on_error = on_error.clone();
                                spawn(async move {
                                    let config = CONFIG.read().clone();
                                    match api::open_repayment_phase(&config, phase_id).await {
                                        // Pitfall #7 / Phase 8 CR-01: re-fetch via on_changed
                                        // statt Response-Body-Version zu verwenden
                                        // D-09: KEIN Auto-Tab-Switch — on_changed lädt nur die Phase neu
                                        Ok(_) => on_changed.call(()),
                                        Err(err) => on_error.call(err.message),
                                    }
                                });
                            },
                            "{i18n.t(Key::RepaymentPhaseOpen)}"
                        }
                    }
                }

                if should_show_close_button(phase_status) {
                    div { class: "rounded border border-gray-200 bg-red-50 p-4 flex items-center justify-between",
                        div {
                            p { class: "font-semibold text-red-900", "{i18n.t(Key::RepaymentPhaseClose)}" }
                            p { class: "text-sm text-red-700",
                                "Schließen geht nur, wenn alle Einträge ausbezahlt oder gelöscht sind."
                            }
                        }
                        button {
                            r#type: "button",
                            class: "bg-red-600 hover:bg-red-700 text-white px-6 py-3 rounded min-h-[44px]",
                            onclick: move |_| show_close_confirm.set(true),
                            "{i18n.t(Key::RepaymentPhaseClose)}"
                        }
                    }
                }
            }

            // D-07: Confirm-Modal vor Close-POST
            if *show_close_confirm.read() {
                Modal {
                    div { class: "flex flex-col gap-4",
                        h2 { class: "text-xl font-semibold", "{i18n.t(Key::RepaymentPhaseCloseConfirmTitle)}" }
                        p { class: "text-sm text-gray-700", "{i18n.t(Key::RepaymentPhaseCloseConfirmText)}" }
                        div { class: "flex gap-2 justify-end mt-2",
                            button {
                                r#type: "button",
                                class: "px-4 py-2 text-gray-700 hover:bg-gray-100 rounded min-h-[44px]",
                                onclick: move |_| show_close_confirm.set(false),
                                "{i18n.t(Key::Cancel)}"
                            }
                            button {
                                r#type: "button",
                                class: "bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded min-h-[44px]",
                                onclick: move |_| {
                                    show_close_confirm.set(false);
                                    let on_changed = on_changed.clone();
                                    let on_close_conflict = on_close_conflict.clone();
                                    let on_error = on_error.clone();
                                    spawn(async move {
                                        let config = CONFIG.read().clone();
                                        match api::close_repayment_phase(&config, phase_id).await {
                                            Ok(_) => on_changed.call(()),
                                            Err(err) => {
                                                // D-04: 409 mit CloseConflictResponse strukturiert anzeigen
                                                if let Some(cc) = parse_close_conflict(&err) {
                                                    on_close_conflict.call(cc);
                                                } else {
                                                    on_error.call(err.message);
                                                }
                                            }
                                        }
                                    });
                                },
                                "{i18n.t(Key::RepaymentPhaseClose)}"
                            }
                        }
                    }
                }
            }
        }
    }

    #[cfg(test)]
    mod tests {
        use super::*;

        #[test]
        fn open_button_only_in_preparation() {
            assert!(should_show_open_button(RepaymentPhaseStatusTO::Preparation));
            assert!(!should_show_open_button(RepaymentPhaseStatusTO::Open));
            assert!(!should_show_open_button(RepaymentPhaseStatusTO::Closed));
        }

        #[test]
        fn close_button_only_in_open() {
            assert!(!should_show_close_button(RepaymentPhaseStatusTO::Preparation));
            assert!(should_show_close_button(RepaymentPhaseStatusTO::Open));
            assert!(!should_show_close_button(RepaymentPhaseStatusTO::Closed));
        }

        #[test]
        fn share_value_readonly_in_closed() {
            assert!(is_share_value_editable(RepaymentPhaseStatusTO::Preparation));
            assert!(is_share_value_editable(RepaymentPhaseStatusTO::Open));
            assert!(!is_share_value_editable(RepaymentPhaseStatusTO::Closed));
        }

        #[test]
        fn parse_close_conflict_returns_none_on_non_409() {
            let err = AppError { status: Some(404), message: "Not found".into(), detail: None };
            assert!(parse_close_conflict(&err).is_none());
        }

        #[test]
        fn parse_close_conflict_returns_none_when_detail_missing() {
            let err = AppError { status: Some(409), message: "Conflict".into(), detail: None };
            assert!(parse_close_conflict(&err).is_none());
        }

        #[test]
        fn parse_close_conflict_returns_some_on_valid_body() {
            let body = r#"{"error":"pending entries","pending_count":2,"pending_member_numbers":["42","43"]}"#;
            let err = AppError {
                status: Some(409),
                message: "Conflict".into(),
                detail: Some(body.to_string()),
            };
            let cc = parse_close_conflict(&err).expect("should parse");
            assert_eq!(cc.pending_count, 2);
            assert_eq!(cc.pending_member_numbers, vec!["42".to_string(), "43".to_string()]);
        }

        #[test]
        fn parse_close_conflict_returns_none_on_garbled_body() {
            let err = AppError {
                status: Some(409),
                message: "Conflict".into(),
                detail: Some("not json".into()),
            };
            assert!(parse_close_conflict(&err).is_none());
        }
    }
    ```

    **Hinweise für den Executor:**

    - `phase.status` ist `Copy` (Enum mit `Copy` derived in Plan 12-01) — kein `.clone()` nötig.
    - `EventHandler::clone()` ist sicher (Dioxus 0.6).
    - `D-09 kein Auto-Tab-Switch` ist verteidigt durch `on_changed = || load_phase()` ohne `active_tab.set(...)`.
    - Component-First-Anker: keine inline-Status-Pills, alle Status via `RepaymentPhaseStatusBadge`.
    - D-01 Grep-Gate: 4-5 button-Tags hier — alle haben `r#type:`.

    **Optional-Verfeinerung (Plan-Discretion, nicht erforderlich für Acceptance):** `BasicsTab` könnte in eigene Datei `genossi-frontend/src/component/repayment_basics_tab.rs` extrahiert werden. Aktueller Inline-Stil ist akzeptabel — die Component ist relativ klein (~80 LOC), Page hat nur 1 Aufrufer. Plan 12-06 erweitert sie inline. Ist Component-First nicht verletzt, weil die Component im Page-File lokal definiert und nicht über Pages hinweg dupliziert wird.
  </action>
  <verify>
    <automated>cargo build -p genossi-frontend && cargo test -p genossi-frontend --lib page::repayment_phase_details::tests</automated>
  </verify>
  <done>
    - cargo build -p genossi-frontend exit 0
    - cargo test -p genossi-frontend --lib page::repayment_phase_details::tests zeigt 6 PASS (3 status + 3 parse)
    - `rg "TODO Plan 12-05" genossi-frontend/src/page/repayment_phase_details.rs` = 0
    - `rg "TabStrip \{|TabDef \{" genossi-frontend/src/page/repayment_phase_details.rs` ≥ 4 (TabStrip-Mount + 3 TabDefs)
    - `rg "api::get_repayment_phase|api::open_repayment_phase|api::close_repayment_phase" genossi-frontend/src/page/repayment_phase_details.rs` = 3 (alle 3 Phase-API-Calls verdrahtet)
    - `rg "RepaymentPhaseStatusBadge \{" genossi-frontend/src/page/repayment_phase_details.rs` ≥ 1 (Header-Badge)
    - `rg "parse_close_conflict" genossi-frontend/src/page/repayment_phase_details.rs` ≥ 2 (Definition + Aufruf)
    - `rg "show_close_confirm" genossi-frontend/src/page/repayment_phase_details.rs` ≥ 3 (Signal + 2-3 Verwendungen)
    - D-01 Grep-Gate für diese Datei: `rg -P '(?ms)button\s*\{(?:(?!\}).)*?\}' genossi-frontend/src/page/repayment_phase_details.rs | grep -v 'r#type:' | grep -c 'button {'` = 0
    - `rg "RequirePrivilege" genossi-frontend/src/page/repayment_phase_details.rs` ≥ 2
  </done>
</task>

</tasks>

<verification>
- `cargo build -p genossi-frontend` exit 0
- `cargo test -p genossi-frontend --lib page::repayment_phase_details::tests` 6 PASS
- D-01 Grep-Gate diese Datei = 0
- Component-First: `rg "RepaymentPhaseStatusBadge" genossi-frontend/src/page/repayment_phase_details.rs` ≥ 1
- Optimistic-Locking: nach close/open wird `on_changed` (load_phase) gerufen statt Response-Version zu verwenden
- D-09 kein Auto-Tab-Switch: `active_tab.set` wird im Open-Handler NICHT gerufen — Tab bleibt
</verification>

<success_criteria>
Vorstand klickt auf eine Phase in der Listen-Page, sieht 3-Tab-Layout, kann eine Phase in Vorbereitung öffnen (Open-Button im Stamm-Tab) und eine offene Phase schließen (mit Confirm-Modal). Bei 409 CloseConflictResponse erscheint ErrorAlert mit pending_count + Member-Liste (Detail-Expand).
</success_criteria>

<output>
After completion, create `.planning/phases/12-frontend-component-first/12-05-SUMMARY.md` documenting:
- Tab-Strip-Layout (D-06 kein dynamischer 4. Tab)
- BasicsTab als inline-Component vs. extrahierter Component (Discretion-Entscheidung)
- parse_close_conflict-Pattern für 409-Body-Deserialisierung (relevant für 12-10 BatchFailureResponse-Pattern!)
- Optimistic-Locking-Reload via on_changed-Callback statt Response-Version
- Stub-Marker für 12-08 + 12-14 (zukünftige Plans wissen wo sie ersetzen)
</output>
