---
phase: 12-frontend-component-first
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - genossi-frontend/src/api.rs
  - genossi-frontend/src/page/mail_page.rs
  - genossi-frontend/src/i18n/mod.rs
  - genossi-frontend/src/i18n/de.rs
  - genossi-frontend/src/i18n/en.rs
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04, UI-05, UI-06]
tags: [frontend, dioxus, api-extension, i18n]

must_haves:
  truths:
    - "12 neue async API-Funktionen sind in genossi-frontend/src/api.rs exportiert (Repayment-Phase-Lifecycle, RepaymentEntry-CRUD, batch-status, mark-paid-out)"
    - "Lokale TO-Strukturen RepaymentPhaseTO, RepaymentEntryTO, RepaymentPhaseStatusTO (Preparation/Open/Closed), RepaymentEntryStatusTO (Open/Contacted/PaidOut), CreateRepaymentPhaseRequest, UpdateRepaymentPhaseRequest, CreateRepaymentEntryRequest, UpdateRepaymentEntryRequest, BatchStatusRequest, CloseConflictResponse, BatchFailureResponse sind in api.rs deklariert (analog AssemblyTO Z.1516-1564)"
    - "send_bulk_mail-Signatur akzeptiert Optional template_id und repayment_phase_id (additiv, backward-compat via #[serde(default)] und skip_serializing_if)"
    - "i18n::Key enum hat ~30 neue Phase-12-Varianten (RepaymentPhases, RepaymentPhase*, RepaymentEntry*) und beide Locales (de.rs, en.rs) haben matching Match-Arms"
    - "cargo build -p genossi-frontend kompiliert ohne Fehler (D-26 Foundation steht)"
  artifacts:
    - path: "genossi-frontend/src/api.rs"
      provides: "12 Repayment-API-Funktionen, lokale TO-Strukturen, erweiterte send_bulk_mail-Signatur"
      contains: "list_repayment_phases"
    - path: "genossi-frontend/src/page/mail_page.rs"
      provides: "Rule-3-Auto-Fix: bestehende send_bulk_mail-Aufrufer um `None, None` (template_id, repayment_phase_id) erweitert; Plan 12-12 verdrahtet später die echten Werte"
      contains: "send_bulk_mail"
    - path: "genossi-frontend/src/i18n/mod.rs"
      provides: "Phase-12-Key-Enum-Varianten"
      contains: "RepaymentPhases"
    - path: "genossi-frontend/src/i18n/de.rs"
      provides: "Deutsche Übersetzungen für alle Phase-12-Keys"
      contains: "Anteils-Rückzahlung"
    - path: "genossi-frontend/src/i18n/en.rs"
      provides: "Englische Übersetzungen für alle Phase-12-Keys"
      contains: "Share Repayment"
  key_links:
    - from: "genossi-frontend/src/api.rs::list_repayment_phases"
      to: "/api/repayment-phase"
      via: "reqwest GET"
      pattern: "list_repayment_phases.*reqwest::get"
    - from: "genossi-frontend/src/api.rs::mark_repayment_entry_paid_out"
      to: "/api/repayment-entry/{id}/mark-paid-out"
      via: "reqwest POST"
      pattern: "mark_repayment_entry_paid_out.*\\.post"
    - from: "genossi-frontend/src/i18n/de.rs"
      to: "genossi-frontend/src/i18n/mod.rs::Key"
      via: "match arm coverage"
      pattern: "Key::RepaymentPhases"
    - from: "genossi-frontend/src/page/mail_page.rs::send_bulk_mail-Aufrufer"
      to: "api::send_bulk_mail (neue 8-Param-Signatur)"
      via: "Rule-3-Auto-Fix mit None, None"
      pattern: "send_bulk_mail\\(.*None,\\s*None"
---

<objective>
Foundation-Layer für alle nachfolgenden Phase-12-Plans: Frontend kennt das Backend-REST-API, hat lokale TO-Strukturen mit korrekten serde-Annotations, und alle i18n-Keys sind in beiden Locales gepflegt — bevor irgendein Component sie nutzt.

Purpose: Phase-12 Plans 02-15 hängen alle direkt oder indirekt an api.rs und/oder den i18n-Keys. Vorab-Aufbau verhindert Lock-Contention, da später ALLE Components parallel die selben Files referenzieren würden.

Output: api.rs mit 12 neuen Funktionen + lokal deklarierten TOs + 2 lokalen 409-Body-Strukturen; i18n/mod.rs Key-Enum erweitert; de.rs + en.rs Match-Arms ergänzt; mail_page.rs send_bulk_mail-Aufrufer mit `None, None` Auto-Fix erweitert (Plan 12-12 verdrahtet später die echten Werte).
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-frontend-component-first/12-CONTEXT.md
@.planning/phases/12-frontend-component-first/12-RESEARCH.md
@.planning/phases/12-frontend-component-first/12-PATTERNS.md
@genossi-frontend/CLAUDE.md

<interfaces>
<!-- Backend REST-Surface (verifiziert in 12-RESEARCH.md §"Backend REST Surface — VERIFIED") -->

GET    /api/repayment-phase                            → Vec<RepaymentPhaseTO>
POST   /api/repayment-phase  body=CreateRepaymentPhaseRequest{fiscal_year:i32, share_value:i64}  → RepaymentPhaseTO
GET    /api/repayment-phase/{id}                       → RepaymentPhaseTO
PUT    /api/repayment-phase/{id} body=UpdateRepaymentPhaseRequest{fiscal_year,share_value,version}  → RepaymentPhaseTO
POST   /api/repayment-phase/{id}/open                  → RepaymentPhaseTO
POST   /api/repayment-phase/{id}/close                 → RepaymentPhaseTO OR 409 + CloseConflictResponse

GET    /api/repayment-entry?phase_id=<uuid>            → Vec<RepaymentEntryTO>
POST   /api/repayment-entry  body=CreateRepaymentEntryRequest{phase_id, member_id, share_count_to_pay_out}  → RepaymentEntryTO
GET    /api/repayment-entry/{id}                       → RepaymentEntryTO
PUT    /api/repayment-entry/{id} body=UpdateRepaymentEntryRequest{share_count_to_pay_out:Option<i32>, status:Option<RepaymentEntryStatusTO>, version:Uuid}  → RepaymentEntryTO
DELETE /api/repayment-entry/{id}                       → 204 (soft-delete)
POST   /api/repayment-entry/batch-status body=BatchStatusRequest{entry_ids:Vec<Uuid>, target_status:RepaymentEntryStatusTO (PaidOut → 400)}  → 200 OR 404 OR 409+BatchFailureResponse
POST   /api/repayment-entry/{id}/mark-paid-out         → RepaymentEntryTO OR 400 (PAYO-03) OR 409 (PAYO-04 schon ausbezahlt)

GET    /api/repayment-phase/{phase_id}/export/pdf?include=open|all|paid  → PDF stream (Browser handelt; D-26 KEINE api.rs-Funktion)

POST   /api/mail/send-bulk body=SendBulkMailRequest{ ... + Option<String> template_id, Option<Uuid> repayment_phase_id }  (Phase 10 D-12, D-03)

<!-- TO-Strukturen (mirror der Backend-Definitionen aus genossi_rest_types/src/lib.rs:1186-1422) -->

pub enum RepaymentPhaseStatusTO { Preparation, Open, Closed }
pub struct RepaymentPhaseTO { id, fiscal_year:i32, share_value:i64, status, opened_at, closed_at, created, deleted, version }
pub enum RepaymentEntryStatusTO { Open, Contacted, PaidOut }
pub struct RepaymentEntryTO { id, member_id, phase_id, share_count_to_pay_out:i32, status, created, deleted, version }
pub struct CloseConflictResponse { error:String, pending_count:usize, pending_member_numbers:Vec<String> }
pub struct BatchFailureResponse { failure_index:usize, failure_id:String, failure_reason:String }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lokale Repayment-TO-Strukturen + 12 API-Funktionen in api.rs + send_bulk_mail-Aufrufer-Auto-Fix</name>
  <read_first>
    - genossi-frontend/src/api.rs (Z. 1-100 für AppError-/check_response-Pattern; Z. 1516-1722 für komplette Assembly-API als 1:1-Vorlage; Z. 818-892 für Mail-Request/SendBulkMailRequest)
    - genossi-frontend/rest-types/src/lib.rs (Liste der existierenden TOs — RepaymentPhaseTO/RepaymentEntryTO sind NICHT vorhanden, müssen lokal deklariert werden — analog AssemblyTO in api.rs:1516)
    - .planning/phases/12-frontend-component-first/12-RESEARCH.md §"Backend REST Surface — VERIFIED" + §"Reuse Asset Inventory"
    - .planning/phases/12-frontend-component-first/12-PATTERNS.md §8 "genossi-frontend/src/api.rs (api-extension)"
    - genossi_rest_types/src/lib.rs (Z. 1186-1422 für exakte Backend-TO-Felder; nur als Read-Reference, nicht importieren)
    - genossi-frontend/src/page/mail_page.rs (genau eine bestehende send_bulk_mail-Aufrufer-Call-Site; Rule-3-Auto-Fix scope — wird in dieser Task ebenfalls editiert, weshalb mail_page.rs in files_modified gelistet ist)
  </read_first>
  <behavior>
    - Test: cargo build -p genossi-frontend kompiliert ohne Fehler
    - Test: cargo test -p genossi-frontend --lib (alle bestehenden Tests bleiben grün)
    - Test: rg "pub async fn list_repayment_phases" genossi-frontend/src/api.rs findet 1 Treffer
    - Test: rg "pub async fn (create|get|update|open|close)_repayment_phase|pub async fn (list|create|get|update|delete|batch_toggle|mark)_repayment_entry|mark_repayment_entry_paid_out" genossi-frontend/src/api.rs findet exakt 12 Treffer
    - Test: rg "pub struct RepaymentPhaseTO|pub enum RepaymentPhaseStatusTO|pub struct RepaymentEntryTO|pub enum RepaymentEntryStatusTO|pub struct CreateRepaymentPhaseRequest|pub struct UpdateRepaymentPhaseRequest|pub struct CreateRepaymentEntryRequest|pub struct UpdateRepaymentEntryRequest|pub struct BatchStatusRequest|pub struct CloseConflictResponse|pub struct BatchFailureResponse" genossi-frontend/src/api.rs findet 11 Treffer
    - Test: send_bulk_mail-Signatur hat template_id:Option<&str>, repayment_phase_id:Option<Uuid> Parameter; SendBulkMailRequest hat #[serde(default, skip_serializing_if = "Option::is_none")] template_id + repayment_phase_id Felder
    - Test: mail_page.rs send_bulk_mail-Aufrufer ist mit `None, None` am Ende der Argument-Liste ergänzt (Auto-Fix). Plan 12-12 ersetzt diese Auto-Fix-Defaults durch echte Werte aus dem Repayment-Kontext.
  </behavior>
  <action>
    Erweitere `genossi-frontend/src/api.rs` am Ende der Datei (nach existierender Assembly-Section Z.1722) um einen neuen Phase-12-Block:

    1. **TO-Strukturen (analog AssemblyTO Z.1516-1564, ALLE Felder via #[serde(default)] für optional fields):**
       ```rust
       // ─── Phase 12 ─── RepaymentPhase / RepaymentEntry TOs ───────────────

       #[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
       pub enum RepaymentPhaseStatusTO {
           Preparation,
           Open,
           Closed,
       }

       #[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
       pub struct RepaymentPhaseTO {
           pub id: Uuid,
           pub fiscal_year: i32,
           pub share_value: i64,
           pub status: RepaymentPhaseStatusTO,
           #[serde(default)]
           pub opened_at: Option<String>,
           #[serde(default)]
           pub closed_at: Option<String>,
           #[serde(default)]
           pub created: Option<String>,
           #[serde(default)]
           pub deleted: Option<String>,
           #[serde(default)]
           pub version: Option<Uuid>,
       }

       #[derive(Debug, Clone, serde::Serialize)]
       pub struct CreateRepaymentPhaseRequest {
           pub fiscal_year: i32,
           pub share_value: i64,
       }

       #[derive(Debug, Clone, serde::Serialize)]
       pub struct UpdateRepaymentPhaseRequest {
           pub fiscal_year: i32,
           pub share_value: i64,
           pub version: Uuid,
       }

       #[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
       pub enum RepaymentEntryStatusTO {
           Open,
           Contacted,
           PaidOut,
       }

       #[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
       pub struct RepaymentEntryTO {
           pub id: Uuid,
           pub member_id: Uuid,
           pub phase_id: Uuid,
           pub share_count_to_pay_out: i32,
           pub status: RepaymentEntryStatusTO,
           #[serde(default)]
           pub created: Option<String>,
           #[serde(default)]
           pub deleted: Option<String>,
           #[serde(default)]
           pub version: Option<Uuid>,
       }

       #[derive(Debug, Clone, serde::Serialize)]
       pub struct CreateRepaymentEntryRequest {
           pub phase_id: Uuid,
           pub member_id: Uuid,
           pub share_count_to_pay_out: i32,
       }

       #[derive(Debug, Clone, serde::Serialize)]
       pub struct UpdateRepaymentEntryRequest {
           #[serde(skip_serializing_if = "Option::is_none")]
           pub share_count_to_pay_out: Option<i32>,
           #[serde(skip_serializing_if = "Option::is_none")]
           pub status: Option<RepaymentEntryStatusTO>,
           pub version: Uuid,
       }

       #[derive(Debug, Clone, serde::Serialize)]
       pub struct BatchStatusRequest {
           pub entry_ids: Vec<Uuid>,
           pub target_status: RepaymentEntryStatusTO,
       }

       /// 409-Body von POST /api/repayment-phase/{id}/close (Phase 8 D-15).
       /// Wird via serde_json::from_str::<CloseConflictResponse>(&app_error.detail.unwrap_or_default())
       /// im Caller deserialisiert (D-04).
       #[derive(Debug, Clone, PartialEq, serde::Deserialize)]
       pub struct CloseConflictResponse {
           pub error: String,
           pub pending_count: usize,
           pub pending_member_numbers: Vec<String>,
       }

       /// 409-Body von POST /api/repayment-entry/batch-status (Phase 8 Plan 09 CR-02).
       /// 404 bei missing entry_id; 409 mit BatchFailureResponse bei Domain-Conflict.
       #[derive(Debug, Clone, PartialEq, serde::Deserialize)]
       pub struct BatchFailureResponse {
           pub failure_index: usize,
           pub failure_id: String,
           pub failure_reason: String,
       }
       ```

    2. **12 API-Funktionen (Pattern 1:1 aus Assembly Z.1659-1722 + Soft-Delete Z.220-226). Pro D-26:**
       ```rust
       // ─── Phase 12 ─── RepaymentPhase endpoints ──────────────────────────

       pub async fn list_repayment_phases(config: &Config) -> Result<Vec<RepaymentPhaseTO>, AppError> {
           info!("Listing repayment phases");
           let url = format!("{}/api/repayment-phase", config.backend);
           let response = check_response(reqwest::get(url).await?).await?;
           Ok(response.json().await?)
       }

       pub async fn get_repayment_phase(config: &Config, id: Uuid) -> Result<RepaymentPhaseTO, AppError> {
           info!("Fetching repayment phase {id}");
           let url = format!("{}/api/repayment-phase/{id}", config.backend);
           let response = check_response(reqwest::get(url).await?).await?;
           Ok(response.json().await?)
       }

       pub async fn create_repayment_phase(
           config: &Config,
           req: &CreateRepaymentPhaseRequest,
       ) -> Result<RepaymentPhaseTO, AppError> {
           info!("Creating repayment phase");
           let url = format!("{}/api/repayment-phase", config.backend);
           let response = reqwest::Client::new().post(url).json(req).send().await?;
           let response = check_response(response).await?;
           Ok(response.json().await?)
       }

       pub async fn update_repayment_phase(
           config: &Config,
           id: Uuid,
           req: &UpdateRepaymentPhaseRequest,
       ) -> Result<RepaymentPhaseTO, AppError> {
           info!("Updating repayment phase {id}");
           let url = format!("{}/api/repayment-phase/{id}", config.backend);
           let response = reqwest::Client::new().put(url).json(req).send().await?;
           let response = check_response(response).await?;
           Ok(response.json().await?)
       }

       pub async fn open_repayment_phase(config: &Config, id: Uuid) -> Result<RepaymentPhaseTO, AppError> {
           info!("Opening repayment phase {id}");
           let url = format!("{}/api/repayment-phase/{id}/open", config.backend);
           let response = reqwest::Client::new().post(url).send().await?;
           let response = check_response(response).await?;
           Ok(response.json().await?)
       }

       pub async fn close_repayment_phase(config: &Config, id: Uuid) -> Result<RepaymentPhaseTO, AppError> {
           info!("Closing repayment phase {id}");
           let url = format!("{}/api/repayment-phase/{id}/close", config.backend);
           let response = reqwest::Client::new().post(url).send().await?;
           let response = check_response(response).await?;
           Ok(response.json().await?)
       }

       // ─── Phase 12 ─── RepaymentEntry endpoints ──────────────────────────

       pub async fn list_repayment_entries(config: &Config, phase_id: Uuid) -> Result<Vec<RepaymentEntryTO>, AppError> {
           info!("Listing repayment entries for phase {phase_id}");
           let url = format!("{}/api/repayment-entry?phase_id={phase_id}", config.backend);
           let response = check_response(reqwest::get(url).await?).await?;
           Ok(response.json().await?)
       }

       pub async fn get_repayment_entry(config: &Config, id: Uuid) -> Result<RepaymentEntryTO, AppError> {
           info!("Fetching repayment entry {id}");
           let url = format!("{}/api/repayment-entry/{id}", config.backend);
           let response = check_response(reqwest::get(url).await?).await?;
           Ok(response.json().await?)
       }

       pub async fn create_repayment_entry(
           config: &Config,
           req: &CreateRepaymentEntryRequest,
       ) -> Result<RepaymentEntryTO, AppError> {
           info!("Creating repayment entry");
           let url = format!("{}/api/repayment-entry", config.backend);
           let response = reqwest::Client::new().post(url).json(req).send().await?;
           let response = check_response(response).await?;
           Ok(response.json().await?)
       }

       pub async fn update_repayment_entry(
           config: &Config,
           id: Uuid,
           req: &UpdateRepaymentEntryRequest,
       ) -> Result<RepaymentEntryTO, AppError> {
           info!("Updating repayment entry {id}");
           let url = format!("{}/api/repayment-entry/{id}", config.backend);
           let response = reqwest::Client::new().put(url).json(req).send().await?;
           let response = check_response(response).await?;
           Ok(response.json().await?)
       }

       pub async fn delete_repayment_entry(config: &Config, id: Uuid) -> Result<(), AppError> {
           info!("Deleting repayment entry {id}");
           let url = format!("{}/api/repayment-entry/{id}", config.backend);
           let response = reqwest::Client::new().delete(url).send().await?;
           check_response(response).await?;
           Ok(())
       }

       pub async fn batch_toggle_repayment_status(
           config: &Config,
           req: &BatchStatusRequest,
       ) -> Result<(), AppError> {
           info!("Batch-toggling {} repayment entries", req.entry_ids.len());
           let url = format!("{}/api/repayment-entry/batch-status", config.backend);
           let response = reqwest::Client::new().post(url).json(req).send().await?;
           check_response(response).await?;
           Ok(())
       }

       pub async fn mark_repayment_entry_paid_out(
           config: &Config,
           id: Uuid,
       ) -> Result<RepaymentEntryTO, AppError> {
           info!("Marking repayment entry {id} paid-out");
           let url = format!("{}/api/repayment-entry/{id}/mark-paid-out", config.backend);
           let response = reqwest::Client::new().post(url).send().await?;
           let response = check_response(response).await?;
           Ok(response.json().await?)
       }
       ```

    3. **SendBulkMailRequest erweitern (Z.861-870):** Füge zwei optionale Felder ein (additiv, backward-compat):
       ```rust
       #[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
       pub struct SendBulkMailRequest {
           pub to_addresses: Vec<BulkRecipient>,
           pub subject: String,
           pub body: String,
           #[serde(default, skip_serializing_if = "Vec::is_empty")]
           pub attachment_ids: Vec<String>,
           #[serde(default, skip_serializing_if = "Vec::is_empty")]
           pub static_document_ids: Vec<String>,
           // ── Phase 12 (D-18 + Phase 10 D-03/D-12) ───────────────────────
           #[serde(default, skip_serializing_if = "Option::is_none")]
           pub template_id: Option<String>,
           #[serde(default, skip_serializing_if = "Option::is_none")]
           pub repayment_phase_id: Option<Uuid>,
       }
       ```

    4. **send_bulk_mail-Funktionssignatur erweitern (Z.872-892):** Backward-compatible — bestehende Callsites werden mit `None, None` explizit gerufen. Alternativ baut der Plan eine schmale Hilfs-Variante `send_bulk_mail_with_repayment(...)` und lässt die alte Signatur unverändert. **Plan-Discretion:** Bevorzugt: die `send_bulk_mail`-Signatur DIREKT erweitern und ALLE existierenden Aufrufer im Frontend (rg "send_bulk_mail" genossi-frontend/src/) updaten (Auto-Fix-Variante). Heutiger Aufrufer ist nur mail_page.rs — diese wird ohnehin in Plan 12-12 erweitert. Übergangslösung: bestehende Aufrufer mit `None, None` Auto-Fix:
       ```rust
       pub async fn send_bulk_mail(
           config: &Config,
           recipients: &[BulkRecipient],
           subject: &str,
           body: &str,
           attachment_ids: &[String],
           static_document_ids: &[String],
           template_id: Option<&str>,
           repayment_phase_id: Option<Uuid>,
       ) -> Result<MailJobTO, AppError> {
           info!("Sending bulk mail to {} recipients", recipients.len());
           let url = format!("{}/api/mail/send-bulk", config.backend);
           let req = SendBulkMailRequest {
               to_addresses: recipients.to_vec(),
               subject: subject.to_string(),
               body: body.to_string(),
               attachment_ids: attachment_ids.to_vec(),
               static_document_ids: static_document_ids.to_vec(),
               template_id: template_id.map(String::from),
               repayment_phase_id,
           };
           let response = reqwest::Client::new().post(url).json(&req).send().await?;
           let response = check_response(response).await?;
           Ok(response.json().await?)
       }
       ```

    5. **Rule-3-Auto-Fix in `genossi-frontend/src/page/mail_page.rs`:** Nach der Signatur-Änderung an `send_bulk_mail` triggert der Compiler einen Fehler an der einen bestehenden Call-Site in `mail_page.rs` (rg-verifiziert: nur ein Aufruf). Editiere die Call-Site und füge `None, None` als letzte zwei Argumente an die Argument-Liste an:

       ```rust
       // VORHER:
       match api::send_bulk_mail(&config, &recipients, &subj, &b, &att_ids, &static_ids).await { ... }

       // NACHHER (Auto-Fix):
       match api::send_bulk_mail(&config, &recipients, &subj, &b, &att_ids, &static_ids, None, None).await { ... }
       ```

       **Verifizierung der Call-Sites:**
       ```bash
       rg -n "send_bulk_mail\\(" genossi-frontend/src/
       ```
       Erwartet: genau 1 Treffer in `mail_page.rs` (zusätzlich zur Definition in `api.rs`). Falls weitere Treffer auftauchen (z.B. in einem Hilfsmodul) → alle ebenfalls mit `None, None` ergänzen. Bekanntes Pattern aus Plan 10-04, wo `genossi_bin/tests/e2e_tests.rs` 11 Call-Sites im selben GREEN-Commit erweitert wurden.

       **Wichtig:** Diese Auto-Fix-Stelle in `mail_page.rs` wird in Plan 12-12 erneut angefasst und auf echte Werte (`Some(template_id)`, `Some(phase_id)`) umgestellt. Plan 12-01 liefert nur die compile-able Foundation; Plan 12-12 verdrahtet die Logik.

    Alle Funktionen folgen exakt dem Assembly-Pattern. Keine eigene Error-Logik — `check_response` + `AppError`/`status_to_message` macht das deutsch-lokalisierte Mapping bereits (api.rs:73-87, 119-125). Strukturierte 409-Bodies werden im Caller via `serde_json::from_str::<CloseConflictResponse>(detail)` deserialisiert (per D-04 in Plan 12-05); api.rs deklariert die Strukturen nur, parst sie nicht selbst.

    **Wichtig (Pitfall #7 / Phase 8 CR-01):** Diese API-Funktionen geben die Backend-Response 1:1 zurück; Callers müssen nach jeder Mutation (PUT/POST) re-fetchen, statt die Response-Version direkt zu verwenden. Inline-Doc-Kommentar auf `update_repayment_phase` und `update_repayment_entry` ergänzen: `/// Caller MUST re-fetch via get_*(...) after this returns — Phase 8 CR-01 Pattern.`
  </action>
  <verify>
    <automated>cargo build -p genossi-frontend</automated>
  </verify>
  <done>
    - cargo build -p genossi-frontend exit 0
    - `rg "pub async fn (list|get|create|update|open|close)_repayment_phase|pub async fn (list|get|create|update|delete)_repayment_entry|batch_toggle_repayment_status|mark_repayment_entry_paid_out" genossi-frontend/src/api.rs` exakt 12 Treffer
    - SendBulkMailRequest hat `template_id` und `repayment_phase_id` Optional-Felder mit `#[serde(default, skip_serializing_if = "Option::is_none")]`
    - send_bulk_mail-Aufrufer in genossi-frontend/src/ alle mit `None, None` ergänzt (Rule-3-Auto-Fix in selbem Commit). `rg "send_bulk_mail\\(.*None,\\s*None" genossi-frontend/src/page/mail_page.rs` ≥ 1 Treffer.
    - Alle bestehenden Tests (`cargo test -p genossi-frontend --lib`) bleiben grün
  </done>
</task>

<task type="auto">
  <name>Task 2: ~30 i18n-Keys in mod.rs + matching Match-Arms in de.rs und en.rs</name>
  <read_first>
    - genossi-frontend/src/i18n/mod.rs (Z. 46-593 für Key-Enum-Block; Z. 633-720 für format_price-Pattern — nicht zu ändern, nur als Referenz)
    - genossi-frontend/src/i18n/de.rs (komplett für Match-Arm-Pattern und Enum-Block-Reihenfolge)
    - genossi-frontend/src/i18n/en.rs (komplett, gleiche Reihenfolge wie de.rs)
    - .planning/phases/12-frontend-component-first/12-PATTERNS.md §13 "i18n extension" (Key-Liste-Vorschlag)
    - .planning/phases/12-frontend-component-first/12-CONTEXT.md (D-14 Empty-State-Texte, D-16 Confirm-Modal-Inhalt, D-26 Wortlauten)
    - genossi-frontend/CLAUDE.md §i18n (autoritative Locale-Pflicht — beide de+en)
  </read_first>
  <action>
    Phase-12 braucht die exakt folgenden ~30 neuen i18n-Keys. Plan-Phase finalisiert die Liste — Plan-Discretion auf zusätzliche Keys, falls Components in späteren Plans welche brauchen (additiv ergänzbar pro Plan; aber: D-19 Wortlaut, D-04 Toast, D-14 Empty-States, D-16 3-Punkt-Warnung müssen jetzt schon da sein, damit Components sie direkt rendern können).

    **Schritt 1 — `genossi-frontend/src/i18n/mod.rs`** (existierender Key-Enum-Block; füge nach `AssemblyTabExport`-Block oder am Ende des Enums ein):
    ```rust
    pub enum Key {
        // ... existing
        // ─── Phase 12 ─── RepaymentPhase / RepaymentEntry ─────────────
        RepaymentPhases,                      // "Anteils-Rückzahlung" — Top-Bar-Nav, Page-Title
        RepaymentPhaseCreate,                 // "Neue Phase anlegen" — Listen-Page Header-Button
        RepaymentPhaseEmpty,                  // "Noch keine Rückzahlungsphasen angelegt"
        RepaymentPhaseEmptyHint,              // "Lege eine Phase für ein Geschäftsjahr an, um die Auszahlung zu verwalten."
        RepaymentPhaseFiscalYear,             // "Geschäftsjahr"
        RepaymentPhaseShareValue,             // "Anteilswert"
        RepaymentPhaseEntryCount,             // "Einträge"
        RepaymentPhaseStatusPreparation,      // "Vorbereitung"
        RepaymentPhaseStatusOpen,             // "Offen"
        RepaymentPhaseStatusClosed,           // "Abgeschlossen"
        RepaymentPhaseTabBasics,              // "Stammdaten"
        RepaymentPhaseTabEntries,             // "Einträge"
        RepaymentPhaseTabExport,              // "Export"
        RepaymentPhaseOpen,                   // "Phase öffnen"
        RepaymentPhaseClose,                  // "Phase abschließen"
        RepaymentPhaseCloseConfirmTitle,      // "Phase final abschließen?"
        RepaymentPhaseCloseConfirmText,       // "Alle Einträge sind ausbezahlt. Diese Aktion ist nicht rückgängig machbar."
        RepaymentPhaseCloseBlocked,           // "Schließen blockiert: {N} Einträge noch nicht ausbezahlt" — Plan 12-05 nutzt format!() mit pending_count
        RepaymentPhaseShareValueEditHint,     // "Korrektur wird auditiert" — Inline-Edit (D-05) im Offen-Status
        RepaymentEntries,                     // "Einträge"
        RepaymentEntryAdd,                    // "Eintrag manuell hinzufügen"
        RepaymentEntryDelete,                 // "Eintrag löschen"
        RepaymentEntryDeleteConfirm,          // "Eintrag löschen?"
        RepaymentEntryStatusOpen,             // "Offen"
        RepaymentEntryStatusContacted,        // "Angeschrieben"
        RepaymentEntryStatusPaidOut,          // "Ausbezahlt"
        RepaymentEntryMarkContacted,          // "Als angeschrieben markieren"
        RepaymentEntryMarkPaidOut,            // "Als ausbezahlt markieren"
        RepaymentEntryFilterAll,              // "Alle"
        RepaymentEntryColMemberNumber,        // "Mitgl.-Nr."
        RepaymentEntryColName,                // "Name"
        RepaymentEntryColShares,              // "Anteile"
        RepaymentEntryColAmount,              // "Betrag"
        RepaymentEntryColIban,                // "IBAN"
        RepaymentEntryColStatus,              // "Status"
        RepaymentEntryColActions,             // "Aktionen"
        RepaymentEntryEmptyAutoFill,          // "Keine Einträge — Vorjahres-Austritte fehlen. Eintrag manuell hinzufügen."
        RepaymentEntryEmptyFilter,            // "Keine Einträge mit diesem Status."
        RepaymentEntriesNotOpenYet,           // "Phase noch nicht geöffnet — Einträge erscheinen nach dem Öffnen."
        RepaymentExportNotOpenYet,            // "Phase noch nicht geöffnet — Export verfügbar ab Status 'Offen'."
        RepaymentEntryPaidOutConfirmTitle,    // "Auszahlung endgültig markieren?"
        RepaymentEntryPaidOutConfirmSum,      // "Summe:"
        RepaymentEntryPaidOutConfirmWarn1,    // "⚠ Diese Aktion ist final — kein Rückweg möglich."
        RepaymentEntryPaidOutConfirmWarn2,    // "⚠ Erzeugt einen Verkauf-Audit-Eintrag pro Mitglied."
        RepaymentEntryPaidOutConfirmWarn3,    // "⚠ Reduziert current_shares der betroffenen Mitglieder."
        RepaymentEntryPaidOutConfirmButton,   // "Endgültig markieren"
        RepaymentEntryBulkMailButton,         // "Mail an {N} ausgewählte" — Plan 12-13 nutzt format!()
        RepaymentExportInclude,               // "Welche Einträge einschließen?"
        RepaymentExportIncludeOpen,           // "Nur offene + angeschriebene (für Banking-Vorlage)"
        RepaymentExportIncludeAll,            // "Alle Einträge"
        RepaymentExportIncludePaid,           // "Nur ausbezahlte"
        RepaymentExportDownload,              // "PDF herunterladen"
        RepaymentTemplateVarPayoutAmount,     // "Auszahlbetrag" — Label des {{payout_amount}}-Buttons (D-19)
        RepaymentTemplateVarShareCount,       // "Anteile" — Label des {{share_count}}-Buttons (D-19)
        RepaymentTemplateVarFiscalYear,       // "Geschäftsjahr" — Label des {{fiscal_year}}-Buttons (D-19)
    }
    ```

    **Schritt 2 — `genossi-frontend/src/i18n/de.rs`** (analog existierender Match-Arm-Block; gleiche Reihenfolge wie im Enum für Code-Review-Diffability):
    ```rust
    pub fn translate(key: Key) -> Rc<str> {
        match key {
            // ... existing
            Key::RepaymentPhases => "Anteils-Rückzahlung".into(),
            Key::RepaymentPhaseCreate => "Neue Phase anlegen".into(),
            Key::RepaymentPhaseEmpty => "Noch keine Rückzahlungsphasen angelegt".into(),
            Key::RepaymentPhaseEmptyHint => "Lege eine Phase für ein Geschäftsjahr an, um die Auszahlung zu verwalten.".into(),
            Key::RepaymentPhaseFiscalYear => "Geschäftsjahr".into(),
            Key::RepaymentPhaseShareValue => "Anteilswert".into(),
            Key::RepaymentPhaseEntryCount => "Einträge".into(),
            Key::RepaymentPhaseStatusPreparation => "Vorbereitung".into(),
            Key::RepaymentPhaseStatusOpen => "Offen".into(),
            Key::RepaymentPhaseStatusClosed => "Abgeschlossen".into(),
            Key::RepaymentPhaseTabBasics => "Stammdaten".into(),
            Key::RepaymentPhaseTabEntries => "Einträge".into(),
            Key::RepaymentPhaseTabExport => "Export".into(),
            Key::RepaymentPhaseOpen => "Phase öffnen".into(),
            Key::RepaymentPhaseClose => "Phase abschließen".into(),
            Key::RepaymentPhaseCloseConfirmTitle => "Phase final abschließen?".into(),
            Key::RepaymentPhaseCloseConfirmText => "Alle Einträge sind ausbezahlt. Diese Aktion ist nicht rückgängig machbar.".into(),
            Key::RepaymentPhaseCloseBlocked => "Schließen blockiert: noch nicht alle Einträge ausbezahlt".into(),
            Key::RepaymentPhaseShareValueEditHint => "Korrektur wird auditiert".into(),
            Key::RepaymentEntries => "Einträge".into(),
            Key::RepaymentEntryAdd => "Eintrag manuell hinzufügen".into(),
            Key::RepaymentEntryDelete => "Eintrag löschen".into(),
            Key::RepaymentEntryDeleteConfirm => "Eintrag löschen?".into(),
            Key::RepaymentEntryStatusOpen => "Offen".into(),
            Key::RepaymentEntryStatusContacted => "Angeschrieben".into(),
            Key::RepaymentEntryStatusPaidOut => "Ausbezahlt".into(),
            Key::RepaymentEntryMarkContacted => "Als angeschrieben markieren".into(),
            Key::RepaymentEntryMarkPaidOut => "Als ausbezahlt markieren".into(),
            Key::RepaymentEntryFilterAll => "Alle".into(),
            Key::RepaymentEntryColMemberNumber => "Mitgl.-Nr.".into(),
            Key::RepaymentEntryColName => "Name".into(),
            Key::RepaymentEntryColShares => "Anteile".into(),
            Key::RepaymentEntryColAmount => "Betrag".into(),
            Key::RepaymentEntryColIban => "IBAN".into(),
            Key::RepaymentEntryColStatus => "Status".into(),
            Key::RepaymentEntryColActions => "Aktionen".into(),
            Key::RepaymentEntryEmptyAutoFill => "Keine Einträge — Vorjahres-Austritte fehlen. Eintrag manuell hinzufügen.".into(),
            Key::RepaymentEntryEmptyFilter => "Keine Einträge mit diesem Status.".into(),
            Key::RepaymentEntriesNotOpenYet => "Phase noch nicht geöffnet — Einträge erscheinen nach dem Öffnen.".into(),
            Key::RepaymentExportNotOpenYet => "Phase noch nicht geöffnet — Export verfügbar ab Status 'Offen'.".into(),
            Key::RepaymentEntryPaidOutConfirmTitle => "Auszahlung endgültig markieren?".into(),
            Key::RepaymentEntryPaidOutConfirmSum => "Summe:".into(),
            Key::RepaymentEntryPaidOutConfirmWarn1 => "⚠ Diese Aktion ist final — kein Rückweg möglich.".into(),
            Key::RepaymentEntryPaidOutConfirmWarn2 => "⚠ Erzeugt einen Verkauf-Audit-Eintrag pro Mitglied.".into(),
            Key::RepaymentEntryPaidOutConfirmWarn3 => "⚠ Reduziert current_shares der betroffenen Mitglieder.".into(),
            Key::RepaymentEntryPaidOutConfirmButton => "Endgültig markieren".into(),
            Key::RepaymentEntryBulkMailButton => "Mail an Ausgewählte".into(),
            Key::RepaymentExportInclude => "Welche Einträge einschließen?".into(),
            Key::RepaymentExportIncludeOpen => "Nur offene + angeschriebene (für Banking-Vorlage)".into(),
            Key::RepaymentExportIncludeAll => "Alle Einträge".into(),
            Key::RepaymentExportIncludePaid => "Nur ausbezahlte".into(),
            Key::RepaymentExportDownload => "PDF herunterladen".into(),
            Key::RepaymentTemplateVarPayoutAmount => "Auszahlbetrag".into(),
            Key::RepaymentTemplateVarShareCount => "Anteile".into(),
            Key::RepaymentTemplateVarFiscalYear => "Geschäftsjahr".into(),
        }
    }
    ```

    **Schritt 3 — `genossi-frontend/src/i18n/en.rs`** (gleiche Reihenfolge, englische Übersetzungen):
    ```rust
            Key::RepaymentPhases => "Share Repayment".into(),
            Key::RepaymentPhaseCreate => "Create new phase".into(),
            Key::RepaymentPhaseEmpty => "No repayment phases yet".into(),
            Key::RepaymentPhaseEmptyHint => "Create a phase for a fiscal year to manage payouts.".into(),
            Key::RepaymentPhaseFiscalYear => "Fiscal year".into(),
            Key::RepaymentPhaseShareValue => "Share value".into(),
            Key::RepaymentPhaseEntryCount => "Entries".into(),
            Key::RepaymentPhaseStatusPreparation => "Preparation".into(),
            Key::RepaymentPhaseStatusOpen => "Open".into(),
            Key::RepaymentPhaseStatusClosed => "Closed".into(),
            Key::RepaymentPhaseTabBasics => "Basics".into(),
            Key::RepaymentPhaseTabEntries => "Entries".into(),
            Key::RepaymentPhaseTabExport => "Export".into(),
            Key::RepaymentPhaseOpen => "Open phase".into(),
            Key::RepaymentPhaseClose => "Close phase".into(),
            Key::RepaymentPhaseCloseConfirmTitle => "Close phase irreversibly?".into(),
            Key::RepaymentPhaseCloseConfirmText => "All entries are paid out. This action cannot be undone.".into(),
            Key::RepaymentPhaseCloseBlocked => "Closing blocked: not all entries are paid out".into(),
            Key::RepaymentPhaseShareValueEditHint => "Change will be audited".into(),
            Key::RepaymentEntries => "Entries".into(),
            Key::RepaymentEntryAdd => "Add entry manually".into(),
            Key::RepaymentEntryDelete => "Delete entry".into(),
            Key::RepaymentEntryDeleteConfirm => "Delete entry?".into(),
            Key::RepaymentEntryStatusOpen => "Open".into(),
            Key::RepaymentEntryStatusContacted => "Contacted".into(),
            Key::RepaymentEntryStatusPaidOut => "Paid out".into(),
            Key::RepaymentEntryMarkContacted => "Mark as contacted".into(),
            Key::RepaymentEntryMarkPaidOut => "Mark as paid out".into(),
            Key::RepaymentEntryFilterAll => "All".into(),
            Key::RepaymentEntryColMemberNumber => "Member#".into(),
            Key::RepaymentEntryColName => "Name".into(),
            Key::RepaymentEntryColShares => "Shares".into(),
            Key::RepaymentEntryColAmount => "Amount".into(),
            Key::RepaymentEntryColIban => "IBAN".into(),
            Key::RepaymentEntryColStatus => "Status".into(),
            Key::RepaymentEntryColActions => "Actions".into(),
            Key::RepaymentEntryEmptyAutoFill => "No entries — no exits in this fiscal year. Add manually.".into(),
            Key::RepaymentEntryEmptyFilter => "No entries with this status.".into(),
            Key::RepaymentEntriesNotOpenYet => "Phase not opened — entries appear after opening.".into(),
            Key::RepaymentExportNotOpenYet => "Phase not opened — export available from status 'Open'.".into(),
            Key::RepaymentEntryPaidOutConfirmTitle => "Mark payout as final?".into(),
            Key::RepaymentEntryPaidOutConfirmSum => "Total:".into(),
            Key::RepaymentEntryPaidOutConfirmWarn1 => "⚠ This action is final — no way back.".into(),
            Key::RepaymentEntryPaidOutConfirmWarn2 => "⚠ Creates a sale audit entry per member.".into(),
            Key::RepaymentEntryPaidOutConfirmWarn3 => "⚠ Reduces current_shares of affected members.".into(),
            Key::RepaymentEntryPaidOutConfirmButton => "Finalize".into(),
            Key::RepaymentEntryBulkMailButton => "Mail to selected".into(),
            Key::RepaymentExportInclude => "Which entries to include?".into(),
            Key::RepaymentExportIncludeOpen => "Only open + contacted (banking template)".into(),
            Key::RepaymentExportIncludeAll => "All entries".into(),
            Key::RepaymentExportIncludePaid => "Only paid out".into(),
            Key::RepaymentExportDownload => "Download PDF".into(),
            Key::RepaymentTemplateVarPayoutAmount => "Payout amount".into(),
            Key::RepaymentTemplateVarShareCount => "Shares".into(),
            Key::RepaymentTemplateVarFiscalYear => "Fiscal year".into(),
    ```

    **Schritt 4 — Build-Verifikation:** `cargo build -p genossi-frontend` muss exit 0 — i18n-Match-Arms sind exhaustive (kein `_ => …` Wildcard, jeder neue Key MUSS in beiden Locales gepflegt sein, sonst E0004 non-exhaustive patterns).
  </action>
  <verify>
    <automated>cargo build -p genossi-frontend</automated>
  </verify>
  <done>
    - cargo build -p genossi-frontend exit 0
    - `rg -c "Key::RepaymentPhases\b" genossi-frontend/src/i18n/de.rs` = 1
    - `rg -c "Key::RepaymentPhases\b" genossi-frontend/src/i18n/en.rs` = 1
    - `rg -c "RepaymentPhases," genossi-frontend/src/i18n/mod.rs` = 1 (Enum-Variante)
    - Anzahl neuer Match-Arms in de.rs == Anzahl neuer Match-Arms in en.rs (`rg -c "Key::Repayment" genossi-frontend/src/i18n/de.rs` == `rg -c "Key::Repayment" genossi-frontend/src/i18n/en.rs`)
  </done>
</task>

</tasks>

<verification>
- `cargo build -p genossi-frontend` exit 0
- `cargo test -p genossi-frontend --lib` exit 0 (alle bestehenden Tests grün)
- `rg "pub async fn (list|get|create|update|open|close)_repayment_phase|pub async fn (list|get|create|update|delete)_repayment_entry|batch_toggle_repayment_status|mark_repayment_entry_paid_out" genossi-frontend/src/api.rs` exakt 12 Treffer
- `rg "pub struct RepaymentPhaseTO|pub enum RepaymentPhaseStatusTO|pub struct RepaymentEntryTO|pub enum RepaymentEntryStatusTO|pub struct CreateRepaymentPhaseRequest|pub struct UpdateRepaymentPhaseRequest|pub struct CreateRepaymentEntryRequest|pub struct UpdateRepaymentEntryRequest|pub struct BatchStatusRequest|pub struct CloseConflictResponse|pub struct BatchFailureResponse" genossi-frontend/src/api.rs` = 11 Treffer
- `rg -c "Key::Repayment" genossi-frontend/src/i18n/de.rs` == `rg -c "Key::Repayment" genossi-frontend/src/i18n/en.rs`
- SendBulkMailRequest hat exakt zwei neue Felder `template_id: Option<String>` und `repayment_phase_id: Option<Uuid>`, beide mit `#[serde(default, skip_serializing_if = "Option::is_none")]`
- `rg "send_bulk_mail\\(.*None,\\s*None" genossi-frontend/src/page/mail_page.rs` ≥ 1 Treffer (Auto-Fix-Default sichtbar; wird in Plan 12-12 durch echte Werte ersetzt)
</verification>

<success_criteria>
Foundation für Phase 12 steht: jede nachfolgende Plan-Iteration (12-02..12-15) kann `crate::api::list_repayment_phases`, `crate::api::RepaymentPhaseTO`, `crate::i18n::Key::RepaymentPhases` etc. ohne weitere Setups verwenden. Backward-Compat zur bestehenden Mail-Page ist erhalten — `send_bulk_mail` akzeptiert weiterhin alle existierenden Aufrufer (die in Plan 12-12 endgültig auf `repayment_phase_id` umgestellt werden).
</success_criteria>

<output>
After completion, create `.planning/phases/12-frontend-component-first/12-01-SUMMARY.md` documenting:
- Exakte 12 API-Funktionsnamen
- Liste der lokal in api.rs deklarierten TOs (inkl. der 2 lokalen 409-Body-Strukturen)
- Auswirkung auf bestehende send_bulk_mail-Aufrufer (welche Aufrufer wurden mit None, None ergänzt)
- Vollständige Liste der neuen i18n-Keys (gibt Pattern-Anker für Plan 12-02 ff., falls weitere Keys nachgezogen werden müssen)
</output>
