---
phase: 12-frontend-component-first
plan: 15
type: execute
wave: 11
depends_on: [02, 03, 04, 05, 06, 07, 08, 09, 10, 11, 12, 13, 14]
files_modified:
  - .planning/phases/12-frontend-component-first/12-UAT-CHECKLIST.md
autonomous: false
requirements: [UI-01, UI-02, UI-03, UI-04, UI-05, UI-06]
tags: [frontend, gate, verify, uat, button-pattern, component-first]

must_haves:
  truths:
    - "D-01/D-02 Grep-Gate-Verifizierung: jeder button-Tag in genossi-frontend/src/{component,page}/repayment_*.rs hat r#type:; multi-line PCRE-Pattern liefert 0 Treffer"
    - "Component-First-Anker-Verifizierung: keine inline-RSX-Duplikate fuer Status-Pills, Tab-Strips, Modals, Toasts in Phase-12-Dateien (alle nutzen Component-Reuse)"
    - "Reuse-Asset-Coverage-Pruefung: alle in CONTEXT.md/RESEARCH.md als 'verpflichtend reusen' markierten Components werden tatsaechlich verwendet"
    - "12-UAT-CHECKLIST.md ist erstellt mit ~60-80 manuellen Checkpoints analog Phase 4 (Lifecycle/Tabelle/Multi-Select/Inline-Edit/Add-Modal/PaidOut-Confirm/PDF-Download/Massenmail-Flow/Empty-States/Auth-Gate/Button-Reload)"
    - "Human-Checkpoint dokumentiert: Wave 11 ist non-autonomous; Vorstand klickt die UAT-Checkliste auf einer Staging-Instanz durch und vermerkt PASS/FAIL pro Item — KEINE PENDING-Items beim Signoff erlaubt"
    - "cargo test -p genossi-frontend --lib zeigt mindestens ~30+ Unit-Tests gruen (Summe aus Plans 12-01 bis 12-14)"
    - "cargo build -p genossi-frontend && cargo test -p genossi-frontend bleiben gruen"
    - "Resume-Signal 'approved' setzt voraus: 100% der UAT-Checklist-Items PASS-markiert. Jeder FAIL erzeugt entweder einen Defekt-Eintrag in der Defects-Tabelle MIT zugeordnetem Gap-Closure-Plan ODER wird vor Resume behoben — kein impliziter Toleranz-Anteil."
  artifacts:
    - path: ".planning/phases/12-frontend-component-first/12-UAT-CHECKLIST.md"
      provides: "Manueller UAT-Checklist mit allen Plan-12-Workflows"
      contains: "## Lifecycle"
      min_lines: 100
  key_links:
    - from: "Grep-Gate D-01"
      to: "Phase-12-Repayment-Files"
      via: "rg PCRE multi-line"
      pattern: "button.*r#type"
    - from: "Component-First-Anker"
      to: "Reuse-Asset-Inventory aus RESEARCH"
      via: "rg fuer Component-Imports"
      pattern: "use crate::component"
---

<objective>
Wave-11 Schluss: validiert dass alle Phase-12-Pflichten gehalten werden — D-01/D-02 Grep-Gate ueber alle Repayment-Files, Component-First-Anker (keine inline-Duplikate), Reuse-Coverage (alle 12 in RESEARCH dokumentierten Reuse-Assets werden tatsaechlich genutzt). Erstellt UAT-Checkliste fuer human-validated Verify-Phase analog Phase 4.

Purpose: Phase-12-Closure. Verhindert dass ein Wave-1-Pattern-Anker (z.B. Component-Reuse) im Wave-10-Detail vergessen wird. Sammelt alle non-automated Validierungs-Schritte in einer einzigen Checkliste fuer die Verify-Phase. **Zero-Tolerance-Gate**: Resume-Signal "approved" erfordert 100% PASS-Coverage — kein impliziter 90%-Toleranz-Anteil.

Output: 12-UAT-CHECKLIST.md + automatisierte Gate-Verifizierung als Bash-Skript-Auszuege im Plan.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-frontend-component-first/12-CONTEXT.md
@.planning/phases/12-frontend-component-first/12-RESEARCH.md
@.planning/phases/12-frontend-component-first/12-PATTERNS.md
@.planning/phases/12-frontend-component-first/12-01-PLAN.md
@.planning/phases/12-frontend-component-first/12-02-PLAN.md
@.planning/phases/12-frontend-component-first/12-03-PLAN.md
@.planning/phases/12-frontend-component-first/12-04-PLAN.md
@.planning/phases/12-frontend-component-first/12-05-PLAN.md
@.planning/phases/12-frontend-component-first/12-06-PLAN.md
@.planning/phases/12-frontend-component-first/12-07-PLAN.md
@.planning/phases/12-frontend-component-first/12-08-PLAN.md
@.planning/phases/12-frontend-component-first/12-09-PLAN.md
@.planning/phases/12-frontend-component-first/12-10-PLAN.md
@.planning/phases/12-frontend-component-first/12-11-PLAN.md
@.planning/phases/12-frontend-component-first/12-12-PLAN.md
@.planning/phases/12-frontend-component-first/12-13-PLAN.md
@.planning/phases/12-frontend-component-first/12-14-PLAN.md

<interfaces>
<!-- Reuse-Assets aus RESEARCH §"Reuse Asset Inventory" -->
- MemberSearch (Plan 12-09 nutzt es)
- Modal (Plans 12-04, 12-05, 12-09, 12-10)
- ErrorAlert (Plan 12-05)
- ToastContainer + show_toast (Plans 12-04, 12-05, 12-08)
- TabStrip + TabDef (Plan 12-05)
- AssemblyStatusBadge -> Vorlage fuer RepaymentPhaseStatusBadge + RepaymentEntryStatusBadge (Plan 12-02)
- NavGroup + NavItem (Plan 12-03)
- RequirePrivilege (Plans 12-04, 12-05)
- TemplateVarButtons (Plan 12-11 erweitert)
- MEMBERS Global-Signal + refresh_members (Plans 12-08, 12-10)
- AppError + status_to_message (Plan 12-01)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task: Phase-12 Closure Gate — automatisierte Grep-Gates + UAT-Checkliste + Vorstand-Walkthrough (Zero-Tolerance-PASS-Coverage)</name>
  <files>.planning/phases/12-frontend-component-first/12-UAT-CHECKLIST.md</files>
  <action>
    Fuehre die drei Validierungs-Schritte in der `<how-to-verify>`-Sektion aus (A: automatisierte Grep-Gates, B: UAT-Checkliste erstellen, C: human walkthrough mit 100%-PASS-Anforderung). Die UAT-Checkliste wird unter `.planning/phases/12-frontend-component-first/12-UAT-CHECKLIST.md` angelegt mit den 12 Sektionen A-L.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-frontend-component-first/12-UAT-CHECKLIST.md && cargo build -p genossi-frontend && cargo test -p genossi-frontend --lib</automated>
  </verify>
  <done>
    - 12-UAT-CHECKLIST.md exists
    - cargo build/test gruen
    - Schritt-A Grep-Gates exit 0 (Phase-12 buttons OHNE r#type: == 0, alle Pflicht-Reuse-Components werden genutzt, keine TODO-Stub-Marker mehr)
    - Vorstand-Signoff via "approved" mit nachweislich 100% PASS-Coverage ALLER Checklist-Items ODER explizit "uat-fails-found"+vollstaendige Defects-Liste (jeder FAIL mit Plan-Referenz und entweder behobenem Fix ODER zugeordnetem Gap-Closure-Plan)
  </done>
  <what-built>
    Phasen-12-Closure-Gate. Vor diesem Checkpoint wurden alle 14 Implementierungs-Plans vollstaendig ausgefuehrt (12-01 bis 12-14). Diese Wave bietet drei Validierungsschritte:
    1. Automatisierte Grep-Gates (D-01 Button-Pattern + Component-First-Anker + Reuse-Coverage)
    2. UAT-Checkliste erstellt fuer human-validierte Verify-Phase
    3. Human-Checkpoint: Vorstand exerziert die Workflows auf Staging durch und vermerkt PASS/FAIL je Item — keine PENDING-Items erlaubt beim Signoff
  </what-built>
  <how-to-verify>
    **Schritt A — Automatisierte Grep-Gates (vor dem Human-Checkpoint):**

    Fuehre die folgenden Bash-Commands aus dem Repository-Root aus und verifiziere die Erwartung:

    ```bash
    # ── D-01/D-02 Button-Pattern-Gate (multi-line PCRE) ─────────────────
    cd /home/neosam/programming/rust/projects/genossi3
    COUNT=$(rg -P '(?ms)button\s*\{(?:(?!\}).)*?\}' \
        genossi-frontend/src/component/repayment_*.rs \
        genossi-frontend/src/page/repayment_*.rs \
        2>/dev/null | grep -v 'r#type:' | grep -c 'button {' || true)
    echo "Phase-12 Buttons OHNE r#type: $COUNT"
    # Erwartet: 0

    # ── Negative-Control: Phase 4 Files muessen ebenfalls 0 liefern ─────
    COUNT_PHASE4=$(rg -P '(?ms)button\s*\{(?:(?!\}).)*?\}' \
        genossi-frontend/src/page/assembly_details.rs \
        genossi-frontend/src/page/assemblies.rs \
        2>/dev/null | grep -v 'r#type:' | grep -c 'button {' || true)
    echo "Phase-4 Buttons OHNE r#type (Negativ-Kontrolle, sollte auch 0): $COUNT_PHASE4"
    # Erwartet: 0 (Phase-4-Files sind auch konform; falls != 0, ist das Phase-4-Verschulden, kein Phase-12-Issue)

    # ── Component-First-Reuse-Coverage: alle Pflicht-Components werden genutzt ──
    echo "Component-Reuse-Coverage:"
    for COMP in TabStrip Modal ErrorAlert ToastContainer RequirePrivilege MemberSearch RepaymentPhaseStatusBadge RepaymentEntryStatusBadge EditableShareCountCell; do
        HITS=$(rg -l "\\b$COMP\\b" \
            genossi-frontend/src/page/repayment_*.rs \
            genossi-frontend/src/component/repayment_*.rs \
            2>/dev/null | wc -l || echo 0)
        echo "  $COMP: $HITS files use it"
    done
    # Erwartet: Jeder Eintrag hat HITS >= 1 (jeweils tatsaechlich genutzt)

    # ── Stub-Markers entfernt ─────────────────────────────────────────
    STUBS=$(rg "TODO Plan 12-" genossi-frontend/src/ 2>/dev/null | wc -l || echo 0)
    echo "Verbleibende TODO Plan 12-* Stub-Marker: $STUBS"
    # Erwartet: 0 (alle Stubs ersetzt)

    # ── Issue-#2-Check (Plan 12-12): kein hardcoded template_id: None ──
    HARDCODED_NONE=$(rg "template_id:\s*None|let template_id:\s*Option<&str>\s*=\s*None" \
        genossi-frontend/src/page/mail_page.rs 2>/dev/null | wc -l || echo 0)
    echo "Hardcoded template_id None in mail_page.rs: $HARDCODED_NONE"
    # Erwartet: 0 (Issue #2 BLOCKER-Fix gehalten)

    # ── UI-01 SC#1 Anzahl-Einträge-Spalte (Plan 12-04): per-Row use_resource ──
    UI01_HITS=$(rg "Anzahl Einträge|RepaymentPhaseEntryCount" \
        genossi-frontend/src/page/repayment_phases.rs 2>/dev/null | wc -l || echo 0)
    echo "UI-01 SC#1 Anzahl-Einträge-Anzeige: $UI01_HITS hits in repayment_phases.rs"
    # Erwartet: >= 1

    # ── Tests gruen ────────────────────────────────────────────────────
    cargo build -p genossi-frontend 2>&1 | tail -5
    cargo test -p genossi-frontend --lib 2>&1 | tail -10
    # Erwartet: kein Fehler; xx passed; 0 failed
    ```

    **Erfolgsbedingungen Schritt A:**
    - Phase-12-Buttons OHNE r#type: == 0
    - Component-Reuse-Coverage: jeder Eintrag >= 1 file
    - Verbleibende Stub-Marker: 0
    - Hardcoded template_id None in mail_page.rs: 0 (Issue #2 BLOCKER-Fix)
    - UI-01 SC#1 Anzahl-Einträge-Anzeige: >= 1 hit
    - cargo build + cargo test exit 0

    **Schritt B — UAT-Checkliste erstellen:**

    Erstelle `.planning/phases/12-frontend-component-first/12-UAT-CHECKLIST.md` mit folgender Struktur (analog Phase 4 04-UAT-CHECKLIST.md):

    ```markdown
    # Phase 12 — UAT-Checkliste (Frontend Component-First)

    **Tester:** Vorstand
    **Datum:** _________
    **Staging-URL:** _________
    **Backend-Version:** Phase 7-11 alle gruen
    **Test-Account:** mit OIDC-admin-Privilege

    > **Signoff-Regel (Plan 12-15):** "approved"-Signal erfordert 100% der Items PASS-markiert.
    > Jeder FAIL muss in der Defects-Tabelle stehen MIT einem zugeordneten Gap-Closure-Plan
    > (oder Inline-Fix VOR Resume). PENDING-Items beim Signoff sind nicht zulaessig — entweder
    > durchklicken, oder als FAIL+Defekt dokumentieren.

    ## Vorbereitung
    - [ ] dx serve laeuft; http://localhost:8080 erreichbar
    - [ ] Tailwind-Watch aktiv (npx tailwindcss in watch mode)
    - [ ] Backend laeuft auf http://localhost:3000 mit Phase-7-11-Migrations
    - [ ] Test-Member-Daten vorhanden (>= 5 Mitglieder mit unterschiedlichem current_shares + IBAN)
    - [ ] Optional: ein bestehender Test-Eintrag mit exit_date im Vorjahr (fuer Auto-Befuellung)

    ## A. Listen-Page /repayment-phases (UI-01)
    - [ ] Page-Mount: Top-Bar zeigt "Anteils-Rueckzahlung"-Nav-Item zwischen Assemblies und Mail (D-27)
    - [ ] Auth-Gate: Aufruf ohne admin-Privilege -> AccessDeniedPage
    - [ ] Empty-State: leere Liste zeigt Hinweis + 'Neue Phase anlegen'-CTA-Button
    - [ ] Create-Modal: Klick auf 'Neue Phase anlegen' -> Modal mit fiscal_year + share_value (EUR-Input)
    - [ ] Create-Modal Validation: Submit mit fiscal_year=0 oder share_value=0 -> Toast/Fehler
    - [ ] Create-Modal Submit: 'Speichern' bei "2026" + "60,00" -> Phase angelegt, Modal schliesst, Liste lasst
    - [ ] Liste-Sort: Mehrere Phasen vorhanden -> sortiert nach fiscal_year DESC, created DESC
    - [ ] Status-Badge sichtbar pro Row mit korrekter Farbe (Preparation=grau)
    - [ ] **UI-01 SC#1**: Spalte "Anzahl Einträge" sichtbar; bei Mount '…' Loading-Placeholder, danach exakter Count pro Phase (z.B. "5"); bei API-Error '?' als Fallback
    - [ ] Klick auf Phase-Row -> Navigation zu /repayment-phases/{id}

    ## B. Detail-Page Status Vorbereitung (UI-02)
    - [ ] Page-Header: Titel + RepaymentPhaseStatusBadge (grau "Vorbereitung")
    - [ ] D-06: 3 Tabs IMMER sichtbar (Stammdaten/Eintraege/Export)
    - [ ] Tab Stammdaten: fiscal_year + share_value als read-only Anzeige (+ "Bearbeiten"-Button neben share_value)
    - [ ] D-03 Lifecycle-Tile: "Phase oeffnen"-Action-Tile sichtbar (NICHT im Header)
    - [ ] Tab Eintraege: Hinweis-Box "Phase noch nicht geoeffnet"
    - [ ] Tab Export: Hinweis-Box "Phase noch nicht geoeffnet"
    - [ ] D-05 share_value-Inline-Edit: Klick "Bearbeiten" -> Input erscheint -> aendern -> Speichern -> aktualisiert
    - [ ] D-05 Audit-Hint: in Vorbereitung KEIN "Korrektur wird auditiert"-Hinweis sichtbar

    ## C. Phase oeffnen (Lifecycle)
    - [ ] Klick "Phase oeffnen" -> KEIN Confirm (D-07) -> Backend-POST -> Liste/Status reload
    - [ ] Nach Reload: Status-Badge zeigt "Offen" (blau)
    - [ ] D-09: Tab bleibt auf "Stammdaten" (KEIN Auto-Switch zum Einträge-Tab)
    - [ ] D-03 Lifecycle-Tile: jetzt "Phase abschliessen"-Action-Tile (rot)
    - [ ] Tab Eintraege: jetzt RepaymentEntryList sichtbar mit Auto-Befuellten Eintraegen (oder Empty-State falls keine Vorjahres-Austritte)
    - [ ] Tab Export: jetzt voll funktional (kein Hinweis mehr)

    ## D. RepaymentEntryList (UI-03)
    - [ ] Tabelle hat 7 Spalten (Checkbox + Mitgl.-Nr. + Name + Anteile + Betrag + IBAN + Status + Aktionen)
    - [ ] D-10 Betrag = share_count_to_pay_out * share_value, deutsch formatiert "X,XX €"
    - [ ] D-10 IBAN fehlt -> Spalte zeigt "—"
    - [ ] D-12 Status-Filter-Tab-Strip: 4 Tabs (Alle/Offen/Angeschrieben/Ausbezahlt) mit Counts
    - [ ] D-12 Klick auf Filter -> nur passende Eintraege sichtbar
    - [ ] D-11 Multi-Select: Header-Checkbox waehlt alle gefilterten aus; Per-Row-Checkbox toggle
    - [ ] D-11 Bulk-Buttons: bei 0 Selection disabled; bei >= 1 Selection aktiv mit Count-Badge
    - [ ] D-14 Default-Sort: Mitgliedsnummer ASC
    - [ ] D-13 Inline-Cell-Edit: Klick auf Anteile-Zelle -> Input + Save/Cancel; Save -> PUT mit version
    - [ ] D-13 Status=Ausbezahlt -> Cell ist read-only (kein Inline-Edit)
    - [ ] D-14 Soft-Delete: Trash-Icon nur sichtbar wenn !=Ausbezahlt; Klick -> Confirm-Modal -> Eintrag aus Liste raus
    - [ ] D-14 Empty-State: nach Filter ohne Treffer -> "Keine Eintraege mit diesem Status."
    - [ ] D-14 Empty-State (initial): 0 Auto-Befuellt -> "Keine Eintraege — Vorjahres-Austritte fehlen. Eintrag manuell hinzufuegen."

    ## E. Add-Entry-Modal (UI-04)
    - [ ] Klick "Eintrag manuell hinzufuegen" -> Modal mit MemberSearch + Anteile-Input
    - [ ] D-21 MemberSearch: Substring-Suche (Vorname/Nachname/Mitgl.-Nr.) funktioniert
    - [ ] D-22 Vorbefuellung: Member-Select -> share_count_to_pay_out wird mit current_shares befuellt
    - [ ] D-23 Validation: ohne Member ODER share_count<=0 -> Speichern-Button disabled
    - [ ] Submit -> Eintrag erscheint in Liste (Plan 12-09 entries_reload_trigger-Counter feuert)
    - [ ] **Plan 12-09**: Liste reloaded innerhalb 1-2 Sekunden ohne Page-Refresh (Counter-Pattern verbatim)

    ## F. Status-Toggle 'Als angeschrieben markieren' (D-20)
    - [ ] Multi-Select 2-3 Offene Eintraege
    - [ ] Klick "Als angeschrieben markieren (3)" -> Backend POST batch-status
    - [ ] Liste reloaded, Status-Badge jetzt blau "Angeschrieben"

    ## G. PaidOut-Confirm-Modal (UI-05)
    - [ ] Multi-Select 2 Angeschriebene
    - [ ] Klick "Als ausbezahlt markieren (2)" -> rotes Confirm-Modal
    - [ ] D-16: Modal zeigt Listentabelle + Gesamtsumme + 3 rote Warnzeilen + roter "Endgueltig markieren"-Button
    - [ ] D-15: Klick "Endgueltig markieren" -> Sequential-Loop (im Browser-DevTools sichtbar als 2 sequentielle POST mark-paid-out)
    - [ ] D-15 Summary-Toast: "2 Eintraege als ausbezahlt markiert."
    - [ ] D-17: bei PAYO-03-Validation-Fehler (Member.current_shares < share_count) -> per-Entry-Toast
    - [ ] Eintraege jetzt Status=Ausbezahlt; Inline-Cell-Edit fuer Anteile ist disabled
    - [ ] Member-Sidebar/andere Pages: current_shares ist aktualisiert (refresh_members nach Loop)
    - [ ] PAYO-04: Erneuter PaidOut-Versuch auf bereits-ausbezahlten Eintrag -> Backend 409 -> Toast

    ## H. Massenmail-Flow (UI-06)
    - [ ] Multi-Select 2-3 Eintraege
    - [ ] Klick "Mail an 3 ausgewaehlte" -> Browser navigiert zu /mail?from=repayment&phase_id=...&members=...
    - [ ] Mail-Page: Recipient-Picker zeigt die 3 Mitglieder bereits ausgewaehlt
    - [ ] D-19 TemplateVarButtons: 3 orange Buttons sichtbar ("Auszahlbetrag", "Anteile", "Geschaeftsjahr")
    - [ ] Click "Auszahlbetrag" -> `{{ payout_amount }}` ins Body eingefuegt
    - [ ] **Issue #2 (Plan 12-12)**: Template-Auswahl im TemplateSelector → selected_template_id-Signal wird gesetzt; im Browser-DevTools Network-Tab ist der send-bulk-Body mit `"template_id": "<echte-id>"` zu sehen (NICHT null/None)
    - [ ] Subject + Body schreiben mit `{{ payout_amount }}`-Substitution
    - [ ] Senden -> Mail-Job angelegt (sichtbar in Mail-Job-Liste)
    - [ ] Backend versendet personalisierte Mails (in Browser-DevTools: send-bulk Body enthaelt repayment_phase_id UND template_id)
    - [ ] Browser-Zurueck -> Detail-Page wieder; selected_ids ist (erwartet) leer; Vorstand muss manuell "Als angeschrieben markieren" klicken (D-20)

    ## I. Phase abschliessen (Lifecycle)
    - [ ] Klick "Phase abschliessen" -> Confirm-Modal (D-07) mit rotem Bestaetigungs-Button
    - [ ] Bei nicht-allen-ausbezahlten Eintraegen -> 409 CloseConflictResponse -> ErrorAlert mit pending_count + Details-Expand zeigt Member-Liste (D-04)
    - [ ] Bei allen ausbezahlt -> Confirm-Klick -> Backend POST close -> Status=Abgeschlossen (gruen)
    - [ ] D-08: Detail-Page jetzt komplett read-only: keine Inline-Edit, keine Trash-Icons, kein Add-Modal
    - [ ] Tab Export bleibt voll funktional

    ## J. PDF-Export (EXPO-01..03)
    - [ ] Tab Export: 3 Radio-Buttons (Open default ausgewaehlt) + grosser blauer "PDF herunterladen"-Anker
    - [ ] Klick "PDF herunterladen" mit include=open -> Browser oeffnet PDF im neuen Tab
    - [ ] PDF-Inhalt korrekt: Mitgliedsnummer + Name + IBAN + share_count + Betrag + Verwendungszweck (Backend Phase 11 EXPO-02)
    - [ ] Filter aendern auf "all"/"paid" -> Download liefert entsprechend gefilterte PDF
    - [ ] In Abgeschlossen-Status weiter funktional

    ## K. Button-Reload-Bug-Check (D-01)
    - [ ] In Detail-Page durch alle Tab-Klicks navigieren — keine URL-Query-Anhaengsel (kein Page-Reload)
    - [ ] In RepaymentEntryList alle Buttons klicken — kein Reload
    - [ ] In CreateRepaymentPhaseForm Submit klicken — Modal schliesst, kein Reload
    - [ ] In Add-Entry-Modal Submit klicken — Modal schliesst, kein Reload
    - [ ] In PaidOut-Confirm-Modal "Endgueltig markieren" — Modal schliesst, kein Reload
    - [ ] In Close-Confirm-Modal "Abschliessen" — Modal schliesst, kein Reload

    ## L. Auth-Gate (D-25)
    - [ ] Helper-Login (mock_auth oder echtes OIDC mit nicht-admin-Account) -> /repayment-phases zeigt AccessDeniedPage
    - [ ] /repayment-phases/{id} ebenfalls AccessDeniedPage
    - [ ] Top-Bar zeigt KEINEN "Anteils-Rueckzahlung"-NavItem (show_admin-Gate)

    ## Defects
    | # | Beschreibung | Plan-Referenz | Schwere | Gap-Closure-Plan oder Inline-Fix | Status |
    |---|---|---|---|---|---|
    |   |   |   |   |   |   |

    ## Zusammenfassung
    - Total Items: __
    - PASS: __
    - FAIL: __  (jeder FAIL hat einen Defekt-Eintrag oben mit Plan-Referenz)

    **Signoff-Regel:** Resume mit "approved" erfordert PASS == Total Items (oder FAIL+Defekt mit Plan-Closure-Pfad).
    PENDING-Items beim Signoff nicht zulaessig.

    **Tester-Signoff:** _________ Datum: _________
    ```

    **Schritt C — Human-Checkpoint (Zero-Tolerance-PASS-Coverage):**

    Nach Schritt B fuehrt der Vorstand die UAT-Checkliste auf Staging durch. Anforderungen:

    1. **JEDES** Item muss als PASS oder FAIL gewertet werden. KEINE PENDING-Items beim Signoff (vorher durchklicken).
    2. **JEDER FAIL** erzeugt einen Eintrag in der Defects-Tabelle MIT:
       - Beschreibung
       - Plan-Referenz (welcher 12-XX-Plan ist betroffen)
       - Schwere (blocker / major / minor)
       - **Eines der zwei:**
         (a) Gap-Closure-Plan-Pfad: der Vorstand triggert `/gsd-plan-phase 12 --gaps` zum Replan; ODER
         (b) Inline-Fix: Defekt wird vor "approved" behoben und das Item neu durchgeklickt (jetzt PASS)
    3. **Resume-Signal "approved"** = 100% der Items als PASS markiert (oder FAIL+behoben-und-PASS). Kein impliziter 90%-Toleranz-Anteil mehr.
    4. Alternative: Resume-Signal **"uat-fails-found"** + komplette Defects-Liste → Orchestrator startet `--gaps`-Plan-Run.

    Diese Regel ist striktere Reformulierung des frueheren "PASS-Rate >= 90%"-Gates (Warning #6/#10 aus der Revision).
  </how-to-verify>
  <resume-signal>
    Type "approved" wenn (a) Schritt A automatisierte Gates exit 0, (b) UAT-Checkliste angelegt UND vollstaendig durchgeklickt mit **100% PASS-Coverage** (oder dokumentierten FAIL→Inline-Fix→PASS-Aktualisierungen), und (c) Vorstand-Signoff in der Checkliste vermerkt. Jede dokumentierte FAIL ohne Inline-Fix darf NUR durch ein "uat-fails-found"-Signal weitergegeben werden — der Orchestrator startet dann `--gaps`-Plan-Run und Plan 12-15 wartet auf erneutes "approved" nach Gap-Closure.

    Smoke-Walkthrough allein reicht NICHT als "approved" — die vollstaendige Checkliste muss abgearbeitet sein.
  </resume-signal>
</task>

</tasks>

<verification>
- Schritt A automatisierte Gates: alle 6 Checks PASS (inkl. Issue-#2-Hardcoded-None-Check und UI-01-SC#1-Check)
- 12-UAT-CHECKLIST.md exists mit allen 12 Sektionen (A-L) UND der expliziten Signoff-Regel im Header
- cargo test/build gruen
- Human approval per "approved" Signal mit 100% PASS-Coverage
</verification>

<success_criteria>
Phase 12 ist closeable:
- Alle D-01..D-28 Decisions in mindestens einem Plan implementiert (Source-Audit-Coverage)
- Alle UI-01..UI-06 Requirements durch Plans abgedeckt
- D-01/D-02 Button-Pattern Grep-Gate haelt (kritischer Memory-Lock)
- Component-First-Anker haelt (alle Pflicht-Reuse-Assets tatsaechlich genutzt)
- UAT-Walkthrough vom Vorstand approved mit 100%-PASS-Coverage (KEIN impliziter 90%-Toleranz-Anteil)
- Resume-Signal "approved" erfordert vollstaendig abgearbeitete Checkliste (kein Smoke-Walkthrough-only)
</success_criteria>

<output>
After completion, create `.planning/phases/12-frontend-component-first/12-15-SUMMARY.md` documenting:
- Schritt-A-Output (Grep-Gate-Counts + Reuse-Coverage + Stub-Removal + Issue-#2-Check + UI-01-SC#1-Check)
- 12-UAT-CHECKLIST.md-Datei-Pfad + Total-Item-Count + Final PASS/FAIL-Count (FAILs nur mit Gap-Closure-Plan-Referenz oder Inline-Fix)
- Human-Signoff vom Vorstand (Datum + PASS == Total Items)
- Eventuelle Defects als Gap-Closure-Input (sofern weitergegeben mit "uat-fails-found")
</output>
