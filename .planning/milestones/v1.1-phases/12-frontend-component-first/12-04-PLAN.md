---
phase: 12-frontend-component-first
plan: 04
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified:
  - genossi-frontend/src/page/repayment_phases.rs
autonomous: true
requirements: [UI-01]
tags: [frontend, page, listing, modal]

must_haves:
  truths:
    - "Page /repayment-phases zeigt Liste aller Phasen mit Status (Badge), fiscal_year, share_value (formatted EUR), Anzahl Einträge (per-Row via use_resource auf api::list_repayment_entries(phase.id) — N+1 akzeptabel weil < 20 Phasen/Jahr per CONTEXT.md `<specifics>`)"
    - "Anzahl-Einträge-Spalte ist UI-01-SC#1 — Header-Spalte 'Anzahl Einträge' sichtbar; pro Row wird der Count via use_resource gerendert: 'Loading'-Placeholder '…' während fetch, danach exakter Count als Integer, bei API-Fehler '?' als Defensiv-Fallback"
    - "Default-Sort: fiscal_year DESC, created DESC (D-14 Claude's Discretion + Phase-7-D-08-Notiz) — Pure-Func sort_phases_default(phases) -> Vec<RepaymentPhaseTO> mit Unit-Test"
    - "Create-Modal (D-26): Form mit fiscal_year (i32-Input) + share_value (Euro-Input, intern via crate::component::repayment_format::parse_euro_to_cents in Cent umgerechnet — KEINE lokale Inline-Konvertierung) + Save/Cancel-Buttons; D-01 Button-Pattern verpflichtend"
    - "Empty-State analog assemblies.rs Z. 59-69: zentrierte Box + Hinweistext + Create-CTA"
    - "RequirePrivilege admin-Wrap (D-25); fallback AccessDeniedPage"
    - "ToastContainer für Errors (Pattern Z. 22-23, 91)"
    - "Stub-Page-Body aus 12-03 wird vollständig ersetzt — 'TODO Plan 12-04' Marker entfernt"
    - "Komponente-First: alle Statusanzeigen via RepaymentPhaseStatusBadge (aus Plan 12-02), keine inline Tailwind-Pill-Klassen"
  artifacts:
    - path: "genossi-frontend/src/page/repayment_phases.rs"
      provides: "#[component] pub fn RepaymentPhases() -> Element (volle Implementation der UI-01-Listen-Page inkl. Anzahl-Einträge-Spalte per-Row use_resource)"
      contains: "list_repayment_phases"
      min_lines: 150
  key_links:
    - from: "genossi-frontend/src/page/repayment_phases.rs"
      to: "/api/repayment-phase"
      via: "api::list_repayment_phases in spawn"
      pattern: "api::list_repayment_phases"
    - from: "genossi-frontend/src/page/repayment_phases.rs (per-Row Entry-Count)"
      to: "/api/repayment-entry?phase_id=..."
      via: "use_resource auf api::list_repayment_entries pro Row"
      pattern: "list_repayment_entries"
    - from: "genossi-frontend/src/page/repayment_phases.rs::CreateRepaymentPhaseForm"
      to: "/api/repayment-phase POST"
      via: "api::create_repayment_phase"
      pattern: "api::create_repayment_phase"
    - from: "Status-Anzeige"
      to: "RepaymentPhaseStatusBadge component"
      via: "component import"
      pattern: "RepaymentPhaseStatusBadge"
    - from: "Create-Modal share_value-Eingabe"
      to: "crate::component::repayment_format::parse_euro_to_cents"
      via: "Pure-Func-Reuse (kein lokales Inline-Parse)"
      pattern: "parse_euro_to_cents"
---

<objective>
UI-01: Listen-Page `/repayment-phases` ist voll funktional — Liste laden, sortieren, neue Phase per Modal anlegen, Anzahl-Einträge pro Row anzeigen (UI-01-SC#1). 1:1-Klon von `assemblies.rs` mit angepassten Feldern (fiscal_year + share_value + Anzahl-Einträge statt name + date + location). Component-First: nutzt `RepaymentPhaseStatusBadge` + `Modal` + `ToastContainer` + `RequirePrivilege` + Pure-Func `parse_euro_to_cents` aus Plan 12-02.

Purpose: Erste produktiv-ähnliche Page der Phase 12 — gibt dem Developer eine UX-Probe für die Plan-12-Patterns. Ab hier kann der Vorstand im Browser eine Phase anlegen UND auf einen Blick sehen, wie viele Einträge eine Phase hat (Banking-Workflow-Vorbereitung).

Output: Replace `repayment_phases.rs`-Stub aus Plan 12-03 mit voller Implementation.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-frontend-component-first/12-CONTEXT.md
@.planning/phases/12-frontend-component-first/12-RESEARCH.md
@.planning/phases/12-frontend-component-first/12-PATTERNS.md
@.planning/phases/12-frontend-component-first/12-01-PLAN.md
@.planning/phases/12-frontend-component-first/12-02-PLAN.md
@.planning/phases/12-frontend-component-first/12-03-PLAN.md

<interfaces>
<!-- Verwendete API (aus Plan 12-01) -->
pub async fn list_repayment_phases(config: &Config) -> Result<Vec<RepaymentPhaseTO>, AppError>;
pub async fn list_repayment_entries(config: &Config, phase_id: Uuid) -> Result<Vec<RepaymentEntryTO>, AppError>;
pub async fn create_repayment_phase(config: &Config, req: &CreateRepaymentPhaseRequest) -> Result<RepaymentPhaseTO, AppError>;
pub struct RepaymentPhaseTO { id, fiscal_year:i32, share_value:i64 (cents), status, opened_at, closed_at, created, deleted, version }
pub struct CreateRepaymentPhaseRequest { fiscal_year:i32, share_value:i64 }

<!-- Verwendete Components -->
crate::component::RepaymentPhaseStatusBadge { status: RepaymentPhaseStatusTO } // aus Plan 12-02
crate::component::repayment_format::format_payout_eur(share_count:i32, share_value_cents:i64) -> String // aus Plan 12-02
crate::component::repayment_format::parse_euro_to_cents(input:&str) -> Option<i64> // aus Plan 12-02 (kanonische Parse-Implementation)
crate::component::{Modal, ToastContainer, TopBar, show_toast} // existing

<!-- Verwendete i18n-Keys (aus Plan 12-01) -->
Key::RepaymentPhases, Key::RepaymentPhaseCreate, Key::RepaymentPhaseEmpty, Key::RepaymentPhaseEmptyHint,
Key::RepaymentPhaseFiscalYear, Key::RepaymentPhaseShareValue, Key::RepaymentPhaseEntryCount,
Key::Loading, Key::Save, Key::Cancel (last 3 existieren bereits)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure-Func sort_phases_default + Format-Helper für Share-Value-Display</name>
  <read_first>
    - genossi-frontend/src/component/member_search.rs (Z. 9-35 für pure-fn + #[cfg(test)] mod tests Pattern)
    - .planning/phases/12-frontend-component-first/12-RESEARCH.md §"Validation Architecture — Wave 0 Gaps" Bullet "sort_phases_default"
    - genossi-frontend/src/component/repayment_format.rs (aus Plan 12-02 — format_payout_eur ist die Vorlage; Share-Value-Display nutzt format_payout_eur(1, share_value_cents) — 1 Anteil-Äquivalent in EUR formatiert; parse_euro_to_cents wird im Create-Form genutzt)
  </read_first>
  <behavior>
    - Test: sort_phases_default mit Mix aus fiscal_year liefert höchsten fiscal_year zuerst
    - Test: bei gleichem fiscal_year wird neuester `created` zuerst sortiert (DESC)
    - Test: bei gleichem fiscal_year UND created bleibt Reihenfolge stabil
    - Test: leere Liste → leere Vec
    - Test: cargo test -p genossi-frontend --lib page::repayment_phases::tests::sort grün
  </behavior>
  <action>
    Bei Implementierung von `genossi-frontend/src/page/repayment_phases.rs` (Task 2) wird eine Pure-Funktion mit-extrahiert. Diese Task spezifiziert die Funktion + Tests, die in Task 2 zusammen mit der Page-Component im selben File landen:

    ```rust
    // genossi-frontend/src/page/repayment_phases.rs (Pure-Func-Sektion, oben in der Datei NACH den use-Imports)

    /// D-14 Claude's Discretion: Default-Sort `fiscal_year DESC, created DESC`
    /// (Phase-7 D-08-Notiz: "Frontend (Phase 12) sortiert per `fiscal_year DESC,
    /// created DESC` zur Auffindbarkeit").
    ///
    /// Stable bei `fiscal_year + created`-Ties (Rust's sort_by ist stable).
    fn sort_phases_default(phases: &[RepaymentPhaseTO]) -> Vec<RepaymentPhaseTO> {
        let mut result: Vec<RepaymentPhaseTO> = phases.to_vec();
        result.sort_by(|a, b| {
            // fiscal_year DESC
            b.fiscal_year.cmp(&a.fiscal_year)
                // created DESC (None sorts last; in practice all rows have a created timestamp)
                .then_with(|| b.created.cmp(&a.created))
        });
        result
    }

    #[cfg(test)]
    mod tests {
        use super::*;
        use crate::api::{RepaymentPhaseStatusTO, RepaymentPhaseTO};
        use uuid::Uuid;

        fn make_phase(year: i32, created_iso: &str) -> RepaymentPhaseTO {
            RepaymentPhaseTO {
                id: Uuid::new_v4(),
                fiscal_year: year,
                share_value: 10_000,
                status: RepaymentPhaseStatusTO::Preparation,
                opened_at: None,
                closed_at: None,
                created: Some(created_iso.into()),
                deleted: None,
                version: None,
            }
        }

        #[test]
        fn sort_by_fiscal_year_desc() {
            let phases = vec![
                make_phase(2023, "2023-01-01T00:00:00Z"),
                make_phase(2025, "2025-01-01T00:00:00Z"),
                make_phase(2024, "2024-01-01T00:00:00Z"),
            ];
            let sorted = sort_phases_default(&phases);
            assert_eq!(sorted[0].fiscal_year, 2025);
            assert_eq!(sorted[1].fiscal_year, 2024);
            assert_eq!(sorted[2].fiscal_year, 2023);
        }

        #[test]
        fn sort_by_created_desc_within_same_year() {
            let phases = vec![
                make_phase(2025, "2025-01-15T00:00:00Z"),
                make_phase(2025, "2025-06-01T00:00:00Z"),
                make_phase(2025, "2025-03-01T00:00:00Z"),
            ];
            let sorted = sort_phases_default(&phases);
            assert_eq!(sorted[0].created.as_deref(), Some("2025-06-01T00:00:00Z"));
            assert_eq!(sorted[1].created.as_deref(), Some("2025-03-01T00:00:00Z"));
            assert_eq!(sorted[2].created.as_deref(), Some("2025-01-15T00:00:00Z"));
        }

        #[test]
        fn sort_empty_returns_empty() {
            let sorted = sort_phases_default(&[]);
            assert_eq!(sorted.len(), 0);
        }
    }
    ```

    Test-Ziel: `cargo test -p genossi-frontend --lib page::repayment_phases::tests` zeigt 3 PASS.
  </action>
  <verify>
    <automated>cargo test -p genossi-frontend --lib page::repayment_phases::tests</automated>
  </verify>
  <done>
    - Pure-Funktion sort_phases_default lebt in repayment_phases.rs
    - 3 Unit-Tests PASS
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Listen-Page voll implementieren (1:1-Klon assemblies.rs + RepaymentPhase-spezifisch + per-Row Entry-Count)</name>
  <read_first>
    - genossi-frontend/src/page/assemblies.rs (KOMPLETT 1-185 — 1:1-Vorlage für Liste + Create-Modal + Empty-State + RequirePrivilege)
    - genossi-frontend/src/component/assembly_list_row.rs (Vorbild für Row-Rendering — kein Klon nötig, repayment-Row kann inline in der Page; Component-First: falls > 5 inline-Zeilen pro Row, extrahieren als `repayment_phase_list_row.rs` — Plan-Discretion. Acceptance-Test: rg Tab-Klassen-Duplikate)
    - genossi-frontend/src/api.rs (RepaymentPhaseTO, CreateRepaymentPhaseRequest, list_repayment_phases, list_repayment_entries, create_repayment_phase aus Plan 12-01)
    - genossi-frontend/src/component/mod.rs (verifiziere RepaymentPhaseStatusBadge ist exportiert — aus Plan 12-02)
    - genossi-frontend/src/component/repayment_format.rs (parse_euro_to_cents — aus Plan 12-02)
    - .planning/phases/12-frontend-component-first/12-PATTERNS.md §1 (Listen-Page) + §3 (Status-Badges-Reuse)
    - .planning/phases/12-frontend-component-first/12-RESEARCH.md "Code Examples — NavGroup-Extension" und Pattern §Listen-Page
    - .planning/phases/12-frontend-component-first/12-CONTEXT.md §`<specifics>` (N+1-Argument: < 20 Phasen/Jahr akzeptabel)
  </read_first>
  <action>
    **Ersetze den Stub-Inhalt von `genossi-frontend/src/page/repayment_phases.rs`** mit voller Implementation. Wichtig: der `RepaymentPhaseListRow`-Sub-Component kapselt das per-Row `use_resource` (jeder Row hat seinen eigenen Resource-Hook).

    ```rust
    //! Repayment phases list page (Phase 12 Plan 12-04, UI-01) — admin-only.
    //!
    //! Pattern aus genossi-frontend/src/page/assemblies.rs (1:1-Klon mit
    //! fiscal_year + share_value + Anzahl-Einträge statt name + date + location).
    //! Status-Badges via RepaymentPhaseStatusBadge (Plan 12-02).
    //! Default-Sort: fiscal_year DESC, created DESC (D-14).
    //! Anzahl-Einträge pro Row via use_resource auf list_repayment_entries (UI-01 SC#1; N+1 akzeptabel <20 Phasen).
    //! Euro-Parse via crate::component::repayment_format::parse_euro_to_cents (Plan 12-02 kanonisch).

    use dioxus::prelude::*;
    use dioxus_router::prelude::*;
    use uuid::Uuid;

    use crate::api::{
        self, CreateRepaymentPhaseRequest, RepaymentPhaseStatusTO, RepaymentPhaseTO,
    };
    use crate::auth::RequirePrivilege;
    use crate::component::repayment_format::{format_payout_eur, parse_euro_to_cents};
    use crate::component::{
        Modal, RepaymentPhaseStatusBadge, ToastContainer, TopBar, show_toast,
    };
    use crate::i18n::{use_i18n, Key};
    use crate::page::access_denied::AccessDeniedPage;
    use crate::router::Route;
    use crate::service::config::CONFIG;

    /// D-14: Default-Sort `fiscal_year DESC, created DESC` (Phase-7 D-08).
    fn sort_phases_default(phases: &[RepaymentPhaseTO]) -> Vec<RepaymentPhaseTO> {
        let mut result: Vec<RepaymentPhaseTO> = phases.to_vec();
        result.sort_by(|a, b| {
            b.fiscal_year.cmp(&a.fiscal_year)
                .then_with(|| b.created.cmp(&a.created))
        });
        result
    }

    #[component]
    pub fn RepaymentPhases() -> Element {
        let i18n = use_i18n();

        let mut phases = use_signal(Vec::<RepaymentPhaseTO>::new);
        let mut loading = use_signal(|| true);
        let mut show_create = use_signal(|| false);
        let mut toast_messages = use_signal(|| Vec::<(u64, String)>::new());
        let mut toast_counter = use_signal(|| 0u64);

        let load = move || {
            spawn(async move {
                loading.set(true);
                let config = CONFIG.read().clone();
                match api::list_repayment_phases(&config).await {
                    Ok(list) => phases.set(sort_phases_default(&list)),
                    Err(e) => show_toast(&mut toast_messages, &mut toast_counter, e.message),
                }
                loading.set(false);
            });
        };

        use_effect(move || {
            load();
        });

        rsx! {
            RequirePrivilege {
                privilege: "admin",
                fallback: rsx! { AccessDeniedPage { required_privilege: "admin".to_string() } },
                TopBar {}
                div { class: "container mx-auto px-4 py-6",
                    div { class: "flex justify-between items-start mb-4",
                        h1 { class: "text-2xl font-bold mb-1", "{i18n.t(Key::RepaymentPhases)}" }
                        button {
                            r#type: "button",
                            class: "bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded text-sm min-h-[44px]",
                            onclick: move |_| show_create.set(true),
                            "{i18n.t(Key::RepaymentPhaseCreate)}"
                        }
                    }

                    if *loading.read() {
                        p { class: "text-gray-500 text-center py-8", "{i18n.t(Key::Loading)}" }
                    } else if phases.read().is_empty() {
                        div { class: "text-center py-12",
                            p { class: "text-lg font-medium text-gray-700", "{i18n.t(Key::RepaymentPhaseEmpty)}" }
                            p { class: "text-sm text-gray-500 mt-2 mb-6", "{i18n.t(Key::RepaymentPhaseEmptyHint)}" }
                            button {
                                r#type: "button",
                                class: "bg-blue-600 hover:bg-blue-700 text-white px-6 py-3 rounded min-h-[44px]",
                                onclick: move |_| show_create.set(true),
                                "{i18n.t(Key::RepaymentPhaseCreate)}"
                            }
                        }
                    } else {
                        div { class: "flex flex-col gap-2",
                            // ── Header-Row mit Anzahl-Einträge-Spalte (UI-01 SC#1) ──
                            div { class: "grid grid-cols-12 gap-2 px-3 py-2 text-xs font-semibold text-gray-500 border-b",
                                div { class: "col-span-2", "{i18n.t(Key::RepaymentPhaseFiscalYear)}" }
                                div { class: "col-span-3", "{i18n.t(Key::RepaymentPhaseShareValue)}" }
                                div { class: "col-span-2", "Status" }
                                div { class: "col-span-2", "{i18n.t(Key::RepaymentPhaseEntryCount)}" }
                                div { class: "col-span-3", {""} }
                            }
                            for p in phases.read().iter() {
                                RepaymentPhaseListRow {
                                    key: "{p.id}",
                                    phase: p.clone(),
                                }
                            }
                        }
                    }

                    if *show_create.read() {
                        Modal {
                            CreateRepaymentPhaseForm {
                                on_close: move |_| show_create.set(false),
                                on_created: move |_| {
                                    show_create.set(false);
                                    load();
                                },
                                on_error: move |msg: String| show_toast(&mut toast_messages, &mut toast_counter, msg),
                            }
                        }
                    }
                }
                ToastContainer { messages: toast_messages }
            }
        }
    }

    /// Eine Row in der Listen-Page. Triggert per-Row ein use_resource auf
    /// list_repayment_entries(phase_id), um die Anzahl Einträge anzuzeigen.
    /// N+1-Queries akzeptabel laut CONTEXT.md `<specifics>` (< 20 Phasen/Jahr).
    /// Während fetch: '…' als Loading-Placeholder. Bei Fehler: '?' als Fallback.
    #[component]
    fn RepaymentPhaseListRow(phase: RepaymentPhaseTO) -> Element {
        let i18n = use_i18n();
        let phase_id = phase.id;

        // Per-Row Resource für Anzahl-Einträge (UI-01 SC#1)
        let entries_resource = use_resource(move || async move {
            let config = CONFIG.read().clone();
            api::list_repayment_entries(&config, phase_id).await
        });

        let entry_count_display: String = match &*entries_resource.read_unchecked() {
            None => "…".to_string(),                          // Loading
            Some(Ok(list)) => list.len().to_string(),         // Erfolg
            Some(Err(_)) => "?".to_string(),                  // Fehler — Defensiv-Fallback
        };

        rsx! {
            Link {
                to: Route::RepaymentPhaseDetails { id: phase.id.to_string() },
                class: "grid grid-cols-12 gap-2 px-3 py-3 items-center bg-white hover:bg-gray-50 border rounded",
                div { class: "col-span-2 font-medium", "{phase.fiscal_year}" }
                div { class: "col-span-3", "{format_payout_eur(1, phase.share_value)} / Anteil" }
                div { class: "col-span-2",
                    RepaymentPhaseStatusBadge { status: phase.status }
                }
                div { class: "col-span-2 text-right tabular-nums", "{entry_count_display}" }
                div { class: "col-span-3 text-xs text-gray-500 text-right",
                    if let Some(c) = phase.created.as_deref() { "{c}" }
                }
            }
        }
    }

    #[component]
    fn CreateRepaymentPhaseForm(
        on_close: EventHandler<()>,
        on_created: EventHandler<()>,
        on_error: EventHandler<String>,
    ) -> Element {
        let i18n = use_i18n();
        let mut fiscal_year = use_signal(|| 0_i32);
        // User entries Euro; persistent ist Cent (D-10 / Phase-7 D-12)
        let mut share_value_euro = use_signal(String::new);
        let mut submitting = use_signal(|| false);

        let invalid_msg = "Bitte gültiges Geschäftsjahr und Anteilswert > 0 angeben".to_string();

        rsx! {
            form {
                class: "flex flex-col gap-4",
                onsubmit: move |e| {
                    e.prevent_default();
                    let year = *fiscal_year.read();
                    // Plan 12-02 Kanonik: parse_euro_to_cents aus repayment_format
                    let share_value_cents = match parse_euro_to_cents(&share_value_euro.read()) {
                        Some(c) => c,
                        None => {
                            on_error.call(invalid_msg.clone());
                            return;
                        }
                    };
                    if year < 1900 || year > 9999 {
                        on_error.call(invalid_msg.clone());
                        return;
                    }
                    submitting.set(true);
                    let req = CreateRepaymentPhaseRequest {
                        fiscal_year: year,
                        share_value: share_value_cents,
                    };
                    spawn(async move {
                        let config = CONFIG.read().clone();
                        match api::create_repayment_phase(&config, &req).await {
                            Ok(_) => on_created.call(()),
                            Err(e) => on_error.call(e.message),
                        }
                        submitting.set(false);
                    });
                },
                h2 { class: "text-xl font-semibold", "{i18n.t(Key::RepaymentPhaseCreate)}" }
                label { class: "flex flex-col gap-1",
                    span { class: "text-sm text-gray-700", "{i18n.t(Key::RepaymentPhaseFiscalYear)}" }
                    input {
                        class: "border border-gray-300 rounded px-3 py-2",
                        r#type: "number",
                        value: "{fiscal_year}",
                        oninput: move |e| {
                            if let Ok(n) = e.value().parse::<i32>() {
                                fiscal_year.set(n);
                            }
                        },
                    }
                }
                label { class: "flex flex-col gap-1",
                    span { class: "text-sm text-gray-700", "{i18n.t(Key::RepaymentPhaseShareValue)} (EUR)" }
                    input {
                        class: "border border-gray-300 rounded px-3 py-2",
                        r#type: "text",
                        placeholder: "60,00",
                        value: "{share_value_euro}",
                        oninput: move |e| share_value_euro.set(e.value()),
                    }
                }
                div { class: "flex gap-2 justify-end mt-2",
                    button {
                        r#type: "button",
                        class: "px-4 py-2 text-gray-700 hover:bg-gray-100 rounded min-h-[44px]",
                        onclick: move |_| on_close.call(()),
                        "{i18n.t(Key::Cancel)}"
                    }
                    button {
                        r#type: "submit",
                        class: "bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded disabled:opacity-50 min-h-[44px]",
                        disabled: *submitting.read(),
                        "{i18n.t(Key::Save)}"
                    }
                }
            }
        }
    }

    #[cfg(test)]
    mod tests {
        use super::*;
        use crate::api::{RepaymentPhaseStatusTO, RepaymentPhaseTO};
        use uuid::Uuid;

        fn make_phase(year: i32, created_iso: &str) -> RepaymentPhaseTO {
            RepaymentPhaseTO {
                id: Uuid::new_v4(),
                fiscal_year: year,
                share_value: 10_000,
                status: RepaymentPhaseStatusTO::Preparation,
                opened_at: None,
                closed_at: None,
                created: Some(created_iso.into()),
                deleted: None,
                version: None,
            }
        }

        #[test]
        fn sort_by_fiscal_year_desc() {
            let phases = vec![
                make_phase(2023, "2023-01-01T00:00:00Z"),
                make_phase(2025, "2025-01-01T00:00:00Z"),
                make_phase(2024, "2024-01-01T00:00:00Z"),
            ];
            let sorted = sort_phases_default(&phases);
            assert_eq!(sorted[0].fiscal_year, 2025);
            assert_eq!(sorted[1].fiscal_year, 2024);
            assert_eq!(sorted[2].fiscal_year, 2023);
        }

        #[test]
        fn sort_by_created_desc_within_same_year() {
            let phases = vec![
                make_phase(2025, "2025-01-15T00:00:00Z"),
                make_phase(2025, "2025-06-01T00:00:00Z"),
                make_phase(2025, "2025-03-01T00:00:00Z"),
            ];
            let sorted = sort_phases_default(&phases);
            assert_eq!(sorted[0].created.as_deref(), Some("2025-06-01T00:00:00Z"));
            assert_eq!(sorted[1].created.as_deref(), Some("2025-03-01T00:00:00Z"));
            assert_eq!(sorted[2].created.as_deref(), Some("2025-01-15T00:00:00Z"));
        }

        #[test]
        fn sort_empty_returns_empty() {
            let sorted = sort_phases_default(&[]);
            assert_eq!(sorted.len(), 0);
        }
    }
    ```

    **Hinweis zur „Anzahl Einträge"-Spalte (UI-01-SC#1):** Der GET /api/repayment-phase-Endpoint liefert KEINEN aggregierten `entry_count`. Plan 12-04 implementiert pro Row ein eigenes `use_resource` auf `api::list_repayment_entries(phase.id)`. N+1-Queries pro Render — bei < 20 Phasen/Jahr (CONTEXT.md `<specifics>`) akzeptabel und im UI-01-SC#1-Kontext explizit erlaubt. UAT-Pflichtcheck Plan 12-15 verifiziert visuell.

    **Loading- und Error-Verhalten der Spalte:**
    - Während des Fetch: `"…"` als Placeholder (sichtbar = nutzerfreundliche Latenz-Indikation)
    - Bei Erfolg: `len().to_string()` (z.B. `"5"`)
    - Bei API-Fehler: `"?"` als Defensiv-Fallback (kein Crash, sichtbare Fehler-Spur)

    **D-01 Button-Gate:** Alle 4 button-Tags in der Page haben `r#type:`. Verifizier vor Commit:
    ```bash
    rg -P '(?ms)button\s*\{(?:(?!\}).)*?\}' genossi-frontend/src/page/repayment_phases.rs | grep -v 'r#type:' | grep -c 'button {'
    # Erwartet: 0
    ```

    **Component-First-Gate:** Status wird via `RepaymentPhaseStatusBadge { status: p.status }` gerendert — KEINE inline Tailwind-Pill-Klassen. Verifiziere:
    ```bash
    rg "RepaymentPhaseStatusBadge" genossi-frontend/src/page/repayment_phases.rs | wc -l
    # Erwartet: ≥ 2 (Import + Usage)
    ```

    **Parse-Reuse-Gate:** Kein lokales Euro→Cent-Parsing — nur Aufruf von `parse_euro_to_cents`:
    ```bash
    rg "parse_euro_to_cents" genossi-frontend/src/page/repayment_phases.rs
    # Erwartet: ≥ 2 Treffer (Import + Aufruf in CreateRepaymentPhaseForm)
    rg "\(\s*euros\s*\*\s*100\.0\s*\)\.round\(\)" genossi-frontend/src/page/repayment_phases.rs
    # Erwartet: 0 Treffer (kein Inline-Parse)
    ```

    **Anzahl-Einträge-Gate:**
    ```bash
    rg "Anzahl Einträge|RepaymentPhaseEntryCount" genossi-frontend/src/page/repayment_phases.rs
    # Erwartet: >= 1 (i18n-Key wird gerendert)
    rg "list_repayment_entries" genossi-frontend/src/page/repayment_phases.rs
    # Erwartet: 1 (per-Row use_resource)
    ```
  </action>
  <verify>
    <automated>cargo build -p genossi-frontend && cargo test -p genossi-frontend --lib page::repayment_phases::tests</automated>
  </verify>
  <done>
    - cargo build -p genossi-frontend exit 0
    - cargo test -p genossi-frontend --lib page::repayment_phases::tests zeigt 3 PASS
    - `rg "TODO Plan 12-04" genossi-frontend/src/page/repayment_phases.rs` = 0 (Stub-Marker entfernt)
    - `rg -P '(?ms)button\s*\{(?:(?!\}).)*?\}' genossi-frontend/src/page/repayment_phases.rs | grep -v 'r#type:' | grep -c 'button {'` = 0 (D-01 button-Gate hält)
    - `rg "RepaymentPhaseStatusBadge \{" genossi-frontend/src/page/repayment_phases.rs` ≥ 1 (Component-First via Badge)
    - `rg "api::list_repayment_phases|api::create_repayment_phase" genossi-frontend/src/page/repayment_phases.rs` ≥ 2 (beide API-Calls verdrahtet)
    - `rg "list_repayment_entries" genossi-frontend/src/page/repayment_phases.rs` = 1 (per-Row Entry-Count via use_resource)
    - `rg "Key::RepaymentPhaseEntryCount" genossi-frontend/src/page/repayment_phases.rs` = 1 (Spalten-Header gerendert)
    - `rg "parse_euro_to_cents" genossi-frontend/src/page/repayment_phases.rs` ≥ 2 (Import + Aufruf)
    - `rg "Modal \{" genossi-frontend/src/page/repayment_phases.rs` = 1 (Create-Modal verwendet existierende Modal-Component)
    - `rg "RequirePrivilege" genossi-frontend/src/page/repayment_phases.rs` ≥ 2 (Import + Wrapping)
  </done>
</task>

</tasks>

<verification>
- `cargo build -p genossi-frontend` exit 0
- `cargo test -p genossi-frontend --lib page::repayment_phases::tests` zeigt 3 PASS
- Manuelles Smoke: `dx serve` → http://localhost:8080/repayment-phases zeigt leere Liste + „Neue Phase anlegen"-Button (UAT-Pflichtcheck Plan 12-15)
- D-01 Grep-Gate für diese Datei hält
- Component-First-Anker via RepaymentPhaseStatusBadge gehalten
- UI-01 SC#1: Anzahl-Einträge-Spalte sichtbar und gerendert (mit '…'-Loading-Placeholder bei Mount, danach exakter Count)
- Parse-Reuse: KEINE lokale Euro→Cent-Konvertierung; nur parse_euro_to_cents aus Plan 12-02
</verification>

<success_criteria>
Vorstand öffnet `/repayment-phases`, sieht entweder leere Liste mit Empty-State + CTA ODER bestehende Phasen sortiert nach `fiscal_year DESC, created DESC`. Jede Row zeigt vier Daten-Spalten (Geschäftsjahr, Anteilswert, Status-Badge, Anzahl Einträge) — letztere mit „…" während des per-Row-Fetch und danach mit der echten Anzahl. Klick auf „Neue Phase anlegen" öffnet Modal mit fiscal_year + share_value (EUR-Eingabe, intern via `parse_euro_to_cents` in Cent). Submit ruft `POST /api/repayment-phase` und reloadet die Liste.

Klick auf eine Phase navigiert zu `/repayment-phases/{id}` (Detail-Page, in Plan 12-05 implementiert).
</success_criteria>

<output>
After completion, create `.planning/phases/12-frontend-component-first/12-04-SUMMARY.md` documenting:
- Listen-Page-Render-Pfad (use_effect → list_repayment_phases → sort_phases_default → render; pro Row use_resource → list_repayment_entries)
- Anzahl-Einträge-Spalten-Verhalten (Loading-`…`-Placeholder; Erfolg=Count; Fehler=`?`)
- Create-Modal: parse_euro_to_cents-Reuse (KEIN lokales Inline-Parse mehr); Sicherheits-Check via existierendem Test in Plan 12-02
- Verlinkung zur Detail-Page via dioxus_router `Link { to: Route::RepaymentPhaseDetails { id: p.id.to_string() } }`
</output>
