---
phase: 12-frontend-component-first
plan: 11
type: execute
wave: 8
depends_on: [01]
files_modified:
  - genossi-frontend/src/component/mail_compose/template_var_buttons.rs
autonomous: true
requirements: [UI-06]
tags: [frontend, component, mail, template, variables]

must_haves:
  truths:
    - "TemplateVarButtons Component bekommt einen zusaetzlichen Optional-Prop show_repayment_vars: bool (default=false) — backward-compatible fuer bestehende Aufrufer (mail_page.rs)"
    - "Wenn show_repayment_vars=true: drei zusaetzliche Buttons fuer {{ payout_amount }}, {{ share_count }}, {{ fiscal_year }} (D-19) werden zusaetzlich gerendert; Labels via i18n Key::RepaymentTemplateVar* aus Plan 12-01"
    - "Render-Reihenfolge: Repayment-Vars zwischen PRIMARY_VARS und Show-More-Button (sichtbar ohne 'Mehr'-Klick — wichtig fuer Mail-Compose-Workflow)"
    - "Visuelles Differential: Repayment-Buttons haben distinkten Style (bg-orange-100, text-orange-800) damit Nutzer den Kontext erkennt"
    - "Aufrufer in mail_page.rs wird in Plan 12-12 mit show_repayment_vars=repayment_phase_id.is_some() erweitert (additiv); diese Aenderung gehoert NICHT zu Plan 12-11, der Plan ist nur die Prop-Erweiterung selbst"
    - "Aufruf-Compat-Test: bestehender Aufruf `TemplateVarButtons { on_insert: ... }` in mail_page.rs Z. 375-379 bleibt funktional (show_repayment_vars defaultet zu false)"
    - "D-01 Button-Pattern haelt fuer alle button-Tags"
  artifacts:
    - path: "genossi-frontend/src/component/mail_compose/template_var_buttons.rs"
      provides: "TemplateVarButtons erweitert um show_repayment_vars Prop + REPAYMENT_VARS Konstante + bedingten Render-Block"
      contains: "REPAYMENT_VARS"
  key_links:
    - from: "TemplateVarButtons render"
      to: "show_repayment_vars Prop"
      via: "conditional rsx"
      pattern: "if show_repayment_vars"
---

<objective>
D-19: Mail-Compose-Page erweitert die Variable-Buttons-Liste um drei Repayment-spezifische Variablen — aber nur wenn der User aus dem Repayment-Workflow kam (sprich: `repayment_phase_id` Query-Param ist gesetzt). Plan 12-11 setzt die Prop-Erweiterung im TemplateVarButtons-Component, Plan 12-12 verdrahtet die Show-Bedingung in mail_page.rs.

Purpose: Vorstand sieht im Mail-Compose-Modus die Variablen `payout_amount` / `share_count` / `fiscal_year` als klickbare Buttons. Klick fuegt z.B. `{{ payout_amount }}` an die Cursor-Position im Mail-Body ein. Backend (Phase 10 D-05/D-13) loest die Variablen pro Empfaenger auf.

Output: Einzige Edit-Stelle im Component-File.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-frontend-component-first/12-CONTEXT.md
@.planning/phases/12-frontend-component-first/12-RESEARCH.md
@.planning/phases/12-frontend-component-first/12-PATTERNS.md
@.planning/phases/12-frontend-component-first/12-01-PLAN.md

<interfaces>
<!-- Existing Pattern (template_var_buttons.rs Z. 5-26, 28-90) -->
const PRIMARY_VARS: &[(&str, &str)] = &[ ... 6 entries ... ];
const SECONDARY_VARS: &[(&str, &str)] = &[ ... 11 entries ... ];
#[component] pub fn TemplateVarButtons(on_insert: EventHandler<String>) -> Element;

<!-- i18n-Keys aus Plan 12-01 -->
Key::RepaymentTemplateVarPayoutAmount  // "Auszahlbetrag" / "Payout amount"
Key::RepaymentTemplateVarShareCount    // "Anteile" / "Shares"
Key::RepaymentTemplateVarFiscalYear    // "Geschaeftsjahr" / "Fiscal year"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TemplateVarButtons mit show_repayment_vars Prop erweitern (D-19)</name>
  <read_first>
    - genossi-frontend/src/component/mail_compose/template_var_buttons.rs (KOMPLETT 1-91)
    - genossi-frontend/src/i18n/mod.rs (Verifizierung dass Key::RepaymentTemplateVar* aus Plan 12-01 existieren)
    - .planning/phases/12-frontend-component-first/12-PATTERNS.md §11 (Template-Var-Buttons-Erweiterung)
    - .planning/phases/12-frontend-component-first/12-CONTEXT.md (D-19)
  </read_first>
  <action>
    Erweitere `genossi-frontend/src/component/mail_compose/template_var_buttons.rs`:

    **Schritt 1 — Neue Konstante REPAYMENT_VARS** (nach SECONDARY_VARS, also nach Z. 26):
    ```rust
    /// Phase-12 (D-19) Repayment-spezifische Variablen. Werden nur bei
    /// `show_repayment_vars=true` gerendert. Aufgeloest backend-seitig in
    /// Phase 10 D-05/D-13 (minijinja Strict-Mode mit `{% if X is defined %}`).
    const REPAYMENT_VARS: &[(&str, &str)] = &[
        ("payout_amount", "Auszahlbetrag"),
        ("share_count", "Anteile"),
        ("fiscal_year", "Geschaeftsjahr"),
    ];
    ```

    **Schritt 2 — TemplateVarButtons-Signatur erweitern** (Z. 28-30):
    ```rust
    #[component]
    pub fn TemplateVarButtons(
        on_insert: EventHandler<String>,
        #[props(default)] show_repayment_vars: bool,
    ) -> Element {
        let i18n = use_i18n();
        let mut show_more = use_signal(|| false);
        ...
    ```

    **Schritt 3 — Repayment-Buttons-Block einfuegen** (innerhalb von `div { class: "flex flex-wrap gap-1", ... }`, NACH dem PRIMARY_VARS-Loop und VOR dem `if *show_more.read()`-Block):
    ```rust
                    // Phase-12 D-19: Repayment-Vars nur wenn show_repayment_vars=true
                    if show_repayment_vars {
                        for (var_name, _label_de) in REPAYMENT_VARS.iter() {
                            {
                                let vn = var_name.to_string();
                                let i18n_key = match *var_name {
                                    "payout_amount" => Key::RepaymentTemplateVarPayoutAmount,
                                    "share_count" => Key::RepaymentTemplateVarShareCount,
                                    "fiscal_year" => Key::RepaymentTemplateVarFiscalYear,
                                    _ => Key::RepaymentTemplateVarPayoutAmount, // unreachable defensive
                                };
                                let label = i18n.t(i18n_key).to_string();
                                rsx! {
                                    button {
                                        class: "bg-orange-100 hover:bg-orange-200 text-orange-800 px-2 py-1 rounded text-xs font-mono",
                                        r#type: "button",
                                        title: "{var_name}",
                                        onclick: move |_| {
                                            on_insert.call(format!("{{{{ {} }}}}", vn));
                                        },
                                        "{label}"
                                    }
                                }
                            }
                        }
                    }
    ```

    **Konkrete Edit-Position:** Direkt nach dem schliessenden `}` von `for (var_name, label) in PRIMARY_VARS.iter() { ... }` (Z. ~55) UND vor dem `if *show_more.read() {` (Z. ~56). So sieht der User PRIMARY_VARS, dann REPAYMENT_VARS (bei show_repayment_vars=true), dann optional SECONDARY_VARS (nach "Mehr"-Klick).

    **Visuelles Differential:** Repayment-Buttons haben `bg-orange-100 text-orange-800` (statt blue/gray) — User sieht visuell dass diese Variablen kontext-spezifisch sind.

    **Backward-Compat-Verifizierung:** Der bestehende Aufruf in `mail_page.rs:375-379` `TemplateVarButtons { on_insert: ... }` bleibt funktional. Plan 12-12 erweitert ihn um `show_repayment_vars: repayment_phase_id.read().is_some()`.

    **D-01 Check:** Drei neue button-Tags — alle mit `r#type:`. Grep-Gate:
    ```bash
    rg -P '(?ms)button\s*\{(?:(?!\}).)*?\}' genossi-frontend/src/component/mail_compose/template_var_buttons.rs | grep -v 'r#type:' | grep -c 'button {'
    # Erwartet: 0
    ```
  </action>
  <verify>
    <automated>cargo build -p genossi-frontend && cargo test -p genossi-frontend --lib</automated>
  </verify>
  <done>
    - cargo build -p genossi-frontend exit 0
    - cargo test -p genossi-frontend --lib bleibt grün (Phase-12-Tests + bestehende Tests)
    - `rg "REPAYMENT_VARS" genossi-frontend/src/component/mail_compose/template_var_buttons.rs` >= 2 (Definition + Verwendung)
    - `rg "show_repayment_vars" genossi-frontend/src/component/mail_compose/template_var_buttons.rs` >= 2 (Prop + If-Branch)
    - `rg "#\[props\(default\)\] show_repayment_vars: bool" genossi-frontend/src/component/mail_compose/template_var_buttons.rs` = 1
    - `rg "Key::RepaymentTemplateVarPayoutAmount|Key::RepaymentTemplateVarShareCount|Key::RepaymentTemplateVarFiscalYear" genossi-frontend/src/component/mail_compose/template_var_buttons.rs` = 3 (alle 3 Keys verwendet)
    - D-01 Grep-Gate fuer diese Datei = 0
    - Bestehender mail_page.rs-Aufruf kompiliert weiterhin (Backward-Compat via default-prop)
  </done>
</task>

</tasks>

<verification>
- `cargo build -p genossi-frontend` exit 0
- D-01 Grep-Gate haelt
- Backward-Compat: bestehende `TemplateVarButtons { on_insert: ... }`-Aufrufer bleiben funktional (default-prop)
- Visuelles Differential via bg-orange-100 sichtbar
</verification>

<success_criteria>
TemplateVarButtons hat einen neuen Optional-Prop `show_repayment_vars`. Wenn `true`: drei orange-getoente Buttons mit den Repayment-Variablen erscheinen zwischen PRIMARY_VARS und dem "Mehr"-Toggle. Bestehende Aufrufer ohne diesen Prop sind unveraendert.

Plan 12-12 verdrahtet die Show-Bedingung an `mail_page.rs::repayment_phase_id.read().is_some()`.
</success_criteria>

<output>
After completion, create `.planning/phases/12-frontend-component-first/12-11-SUMMARY.md` documenting:
- Edit-Position (Z. nach PRIMARY_VARS-Loop, vor Show-More-Block)
- Visuelles Differential (bg-orange-100 vs. bg-blue-100/bg-gray-100)
- Backward-Compat via #[props(default)] verteidigt
</output>
