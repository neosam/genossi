---
phase: 26-editor-formatierung-vervollstaendigen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - genossi_mail/src/sanitize.rs
  - genossi_bin/tests/e2e_tests.rs
autonomous: true
requirements:
  - EDIT-06
  - EDIT-07
  - EDIT-08
tags:
  - test-only
  - sanitize
  - ammonia
  - round-trip
  - e2e
must_haves:
  truths:
    - "sanitize_html preserves <ul><li> markup through ammonia default whitelist"
    - "sanitize_html preserves <ol><li> markup through ammonia default whitelist"
    - "sanitize_html preserves <h1>, <h2>, <h3> heading tags through ammonia default whitelist"
    - "POST /api/mail/templates with body_html containing lists+headings is stored and returned by GET with all tag tokens intact (D-04 backward-compat: sanitize.rs unchanged)"
  artifacts:
    - "genossi_mail/src/sanitize.rs::mod tests::sanitize_preserves_unordered_list"
    - "genossi_mail/src/sanitize.rs::mod tests::sanitize_preserves_ordered_list"
    - "genossi_mail/src/sanitize.rs::mod tests::sanitize_preserves_headings_h1_h2_h3"
    - "genossi_bin/tests/e2e_tests.rs::create_template_body_html_lists_and_headings_round_trip"
  key_links:
    - "Unit tests link ammonia default whitelist to the exact tag-set the Vorstand-Toolbar emits (per D-01: H1 kept)"
    - "E2E test links the full HTTP round-trip (POST → sanitize → SQLite → GET) so a future ammonia upgrade that regresses UL/OL/H1-H3 fails fast"
---

<objective>
Beweise via Unit-Tests (`genossi_mail`) und einem E2E-Round-Trip-Test (`genossi_bin`), dass die bestehende ammonia-Sanitize-Grenze (Phase 23) die Toolbar-Elemente `<ul><li>`, `<ol><li>` und `<h1><h2><h3>` unverändert durchreicht — sowohl im Isolations-Aufruf `sanitize_html(...)` als auch im vollen HTTP-Pfad POST → sanitize → SQLite → GET über `/api/mail/templates`.

Purpose: Deckt Success-Criteria 1, 2 (Editor-Round-Trip für Listen und Überschriften) sowie 5 (Backward-Compat via Nicht-Änderung von `sanitize.rs`, D-04) und erfüllt EDIT-06/07/08. `sanitize.rs` wird selbst NICHT verändert (D-04) — nur der `#[cfg(test)] mod tests`-Block wird um 3 Tests erweitert.

Output: `genossi_mail/src/sanitize.rs` mit 3 neuen Unit-Tests (Baseline 4 → 7 in diesem Modul; Package-Baseline 252 → 255) und `genossi_bin/tests/e2e_tests.rs` mit 1 neuem `#[tokio::test]` (Baseline 308 → 309, davon 1 pre-existing failure unverändert).
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-editor-formatierung-vervollstaendigen/26-CONTEXT.md
@.planning/phases/26-editor-formatierung-vervollstaendigen/26-RESEARCH.md
@genossi_mail/src/sanitize.rs
@genossi_bin/tests/e2e_tests.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drei ammonia-Round-Trip-Unit-Tests in sanitize.rs::mod tests anhängen (EDIT-06, EDIT-07, EDIT-08)</name>
  <files>genossi_mail/src/sanitize.rs</files>
  <read_first>
    - genossi_mail/src/sanitize.rs (kompletter Bestand — 4 existierende Tests als Muster; ANMERKUNG: `sanitize.rs` selbst — also der Wrapper `pub fn sanitize_html` und die `//!`-Doku — wird NICHT angefasst; nur `#[cfg(test)] mod tests { ... }` wird um 3 `#[test] fn ...`-Blöcke erweitert, siehe D-04)
    - .planning/phases/26-editor-formatierung-vervollstaendigen/26-RESEARCH.md §"Pattern 1: Ammonia Round-Trip Unit-Test" (exakter Test-Body als Muster)
    - .planning/phases/26-editor-formatierung-vervollstaendigen/26-CONTEXT.md D-01 (H1 bleibt) + D-03 (drei Test-Namen fixiert) + D-04 (sanitize.rs nicht ändern)
  </read_first>
  <behavior>
    - Test 1 `sanitize_preserves_unordered_list`: input `"<ul><li>a</li><li>b</li></ul>"` → output enthält `"<ul>"` UND `"<li>a</li>"` UND `"<li>b</li>"` (Substring-Assertions, kein byte-exakter Vergleich — Pitfall 4 Whitespace-Normalisierung).
    - Test 2 `sanitize_preserves_ordered_list`: input `"<ol><li>1</li><li>2</li></ol>"` → output enthält `"<ol>"` UND `"<li>1</li>"` UND `"<li>2</li>"`.
    - Test 3 `sanitize_preserves_headings_h1_h2_h3`: input `"<h1>A</h1><h2>B</h2><h3>C</h3>"` → output enthält alle sechs Tag-Tokens `<h1>`, `</h1>`, `<h2>`, `</h2>`, `<h3>`, `</h3>` (H1 mit-abgedeckt per D-01).
    - Fehler-Meldungen im Muster der bestehenden Tests: `"expected <tag> to survive, got: {output}"` — nutzt `output` via `{output}`-Format-Capture, wie in `sanitize_preserves_jinja_placeholder_in_text_content` (Zeile 100-106).
  </behavior>
  <action>
    Öffne `genossi_mail/src/sanitize.rs`, hänge im `#[cfg(test)] mod tests`-Block (nach `sanitize_preserves_jinja_placeholder_in_text_content`, Zeile ~108) drei neue `#[test] fn ...`-Blöcke an. Muster GENAU aus 26-RESEARCH.md §"Pattern 1" übernehmen: `let input = "..."; let output = sanitize_html(input); assert!(output.contains("..."), "expected ... to survive, got: {output}");`. Der Wrapper `pub fn sanitize_html` in Zeile 35-37 und die `//!`-Modul-Doku bleiben unangetastet (D-04 verbietet Änderungen an `sanitize.rs` außerhalb des Test-Moduls). H1 wird explizit mit-getestet, weil per D-01 der H1-Toolbar-Button in `wysiwyg_toolbar.rs` bleibt. Nach dem Anhängen: `cargo fmt -p genossi_mail`. Kommentare in den Tests dürfen keine `<script>`/`onclick`/`javascript:`/`data:`-Strings enthalten, damit die bestehenden Negativ-Grep-Assertions in `sanitize_strips_*`-Tests nicht durch Kommentar-Rauschen kollidieren (Comment-Text-Discipline).
  </action>
  <verify>
    <automated>cargo test -p genossi_mail --lib sanitize_preserves -- --exact</automated>
  </verify>
  <done>
    - `cargo test -p genossi_mail --lib` reportet 255 tests (252 Baseline + 3 neu), 0 failures.
    - `cargo test -p genossi_mail --lib sanitize_preserves_unordered_list -- --exact` grün.
    - `cargo test -p genossi_mail --lib sanitize_preserves_ordered_list -- --exact` grün.
    - `cargo test -p genossi_mail --lib sanitize_preserves_headings_h1_h2_h3 -- --exact` grün.
    - `sanitize.rs` Zeilen 1-37 (Doku + `pub fn sanitize_html`) sind byte-identisch zum Vor-Zustand (D-04 Backward-Compat).
    - `cargo fmt -- --check -p genossi_mail` grün.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: E2E-Round-Trip-Test für Template mit Listen+Headings anhängen (EDIT-06, EDIT-07, EDIT-08)</name>
  <files>genossi_bin/tests/e2e_tests.rs</files>
  <read_first>
    - genossi_bin/tests/e2e_tests.rs Zeile 14790-14842 (Vorbild `create_template_body_html_sanitized` — das strukturell nächstliegende Muster laut D-03; NICHT `bulk_mail_body_html_sanitized_and_persisted` bei ~14655, weil das einen MailJob-Umweg nimmt)
    - .planning/phases/26-editor-formatierung-vervollstaendigen/26-RESEARCH.md §"Pattern 2: E2E-Round-Trip via Template-Endpoint" (kompletter Test-Body als Vorlage)
    - .planning/phases/26-editor-formatierung-vervollstaendigen/26-CONTEXT.md D-03 (Test-Name + Ort fixiert) + D-04 (Toleranz: contains-Tokens, kein byte-exakter Vergleich — siehe Pitfall 4 in RESEARCH)
    - CLAUDE.md §"Test Commands" (E2E-Kommando `cargo test --test e2e_tests`)
    - .planning/STATE.md (dokumentiert Pre-existing failure `test_mail_preview_repayment_no_entries_does_not_default_to_one` aus Phase 22 — nicht Phase-26-Regression, siehe Pitfall 3 in RESEARCH)
  </read_first>
  <behavior>
    - Neuer `#[tokio::test] async fn create_template_body_html_lists_and_headings_round_trip()`.
    - Ort: an das Ende der `create_template_body_html_sanitized`-Nachbarschaft anhängen (nach Zeile 14842, direkt vor dem `// ── Phase 24 Plan 04 …`-Section-Kommentar bei Zeile 14844) — hält verwandte Template-Sanitize-Tests beisammen und erleichtert das spätere Auffinden.
    - Muster gespiegelt aus `create_template_body_html_sanitized` (14797): `let server = setup().await; let client = reqwest::Client::new(); ...POST /api/mail/templates mit body_html: "<h1>Titel</h1><h2>Untertitel</h2><ul><li>Punkt A</li><li>Punkt B</li></ul><ol><li>Schritt 1</li><li>Schritt 2</li></ol><h3>Sub</h3>" ... assert!(response.status(), StatusCode::CREATED); let created: MailTemplateTO = response.json().await.unwrap();`
    - Danach GET `/api/mail/templates/{created.id}` → `let fetched: MailTemplateTO = response.json().await.unwrap(); let stored = fetched.body_html.expect("body_html Some");`
    - Assertion-Loop über Token-Set: für jedes Element aus `["<h1>", "</h1>", "<h2>", "</h2>", "<h3>", "</h3>", "<ul>", "</ul>", "<ol>", "</ol>", "<li>", "</li>", "Titel", "Untertitel", "Punkt A", "Punkt B", "Schritt 1", "Schritt 2", "Sub"]` gilt `stored.contains(token)`.
    - Fehler-Meldung: `"round-trip lost token {token}, got: {stored}"` — analog zu bestehenden `stored_html.contains(...)`-Fehlerdrucks.
  </behavior>
  <action>
    Öffne `genossi_bin/tests/e2e_tests.rs`, hänge nach Zeile 14842 (Ende von `create_template_body_html_sanitized`, VOR dem Section-Kommentar `// ── Phase 24 Plan 04 …` bei Zeile 14844) den neuen Test aus 26-RESEARCH.md §"Pattern 2" ein. Doc-Kommentar: `/// Phase 26 EDIT-06/07/08 (D-03 b): POST + GET template with lists and headings — prove ammonia-default preserves <ul>, <ol>, <li>, <h1>, <h2>, <h3> through the Frontend→Backend→SQLite→Backend→Frontend round-trip.`. Nutze `setup()` und `server.url(...)` (bestehende Helpers, wie im Vorbild). Kein `sample_member()` nötig — Templates brauchen keinen Member. Der Test darf byte-exakte Assertions vermeiden (Pitfall 4), aber muss jeden Tag-Token und jedes Textfragment einzeln asserten. Nach dem Einfügen: `cargo fmt -p genossi_bin`. Kommentare im Test dürfen keine Payload-Strings enthalten, die andere Test-Assertions kaputt machen könnten (z. B. keine `<script>`/`onclick`-Beispiele im Doc-Kommentar).
  </action>
  <verify>
    <automated>cargo test -p genossi_bin --test e2e_tests create_template_body_html_lists_and_headings_round_trip -- --exact --nocapture</automated>
  </verify>
  <done>
    - `cargo test -p genossi_bin --test e2e_tests create_template_body_html_lists_and_headings_round_trip -- --exact` grün.
    - `cargo test -p genossi_bin --test e2e_tests` reportet 309 tests total, 308 passed, 1 pre-existing failure (`test_mail_preview_repayment_no_entries_does_not_default_to_one`) — Delta gegenüber Baseline: +1 grüner Test, unveränderte Failure-Zahl (Pitfall 3 in RESEARCH: dieser Failure ist Phase-22-Altlast, NICHT Phase-26-Regression).
    - Der neue Test steht als zusammenhängender Block direkt hinter `create_template_body_html_sanitized`.
    - `cargo fmt -- --check -p genossi_bin` grün.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vorstand-Browser → Backend REST | body_html reist als JSON-String vom Editor durch POST /api/mail/templates; wird sanitized bevor SQLite-Persistenz (bestehende Phase-23-Grenze) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-01 | Tampering | ammonia sanitize gate in genossi_mail::sanitize_html | low | accept | Bestehende Phase-23-Mitigation (ammonia::clean). Phase 26 ändert die Grenze NICHT (D-04) — die 3 neuen Unit-Tests + 1 E2E-Round-Trip erhöhen NUR die Test-Coverage der bestehenden Grenze für UL/OL/H1-H3. Sicherheitsprofil bleibt identisch zu Phase 25. Netto security-positiv (mehr Regressions-Detektion) ohne neue Angriffsfläche. |

Trivial threat model — Phase 26 fügt keinen neuen produktiven Code-Pfad hinzu (Zero-Production-Code-Phase); alle Änderungen sind Test-Assets die die bestehende sanitize gate härten. Keine neuen Endpoints, keine Auth-Änderung, keine neuen Trust-Boundaries. ASVS L1 V5 (Input Validation) bleibt via ammonia::clean() abgedeckt.
</threat_model>

<verification>
Backend-Test-Suite muss die folgenden Kommandos grün liefern:

```bash
cargo test -p genossi_mail --lib                                    # 255 passed (252 baseline + 3 neu), 0 failed
cargo test -p genossi_bin --test e2e_tests                          # 308 passed, 1 failed (pre-existing aus Phase 22, dokumentiert in STATE.md — kein Phase-26-Blocker)
cargo test -p genossi_mail --lib sanitize_preserves -- --exact      # 3 neue Tests einzeln grün
cargo test -p genossi_bin --test e2e_tests create_template_body_html_lists_and_headings_round_trip -- --exact
cargo fmt -- --check -p genossi_mail
cargo fmt -- --check -p genossi_bin
```

`sanitize.rs` Zeilen 1-37 (`pub fn sanitize_html` + `//!`-Doku) MÜSSEN byte-identisch zum Vor-Zustand sein (D-04). Prüfung via `git diff genossi_mail/src/sanitize.rs` bzw. `jj diff genossi_mail/src/sanitize.rs` — nur `mod tests { ... }` sollte diffen.
</verification>

<success_criteria>
- Plan-Abschluss meldet: 3 neue Unit-Tests in `genossi_mail`, 1 neuer E2E-Test in `genossi_bin`, alle vier grün.
- `sanitize.rs` Produktions-Code (Zeilen 1-37) unangetastet — D-04-Backward-Compat als mechanische Konsequenz.
- Die 4 neuen Tests bilden zusammen den Beweis für ROADMAP-Success-Criteria 1 (UL/OL Save→Reload), 2 (H2/H3 Save→Reload, plus H1 durch D-01) und 5 (Backward-Compat via Nicht-Änderung von sanitize.rs).
- Requirement-Coverage in diesem Plan: EDIT-06 (UL), EDIT-07 (OL), EDIT-08 (H1/H2/H3).
</success_criteria>

<output>
Create `.planning/phases/26-editor-formatierung-vervollstaendigen/26-01-SUMMARY.md` when done. Include: (a) test names + line numbers, (b) baseline→post test counts pro Package, (c) explizite Notiz dass `sanitize.rs` §"pub fn sanitize_html" unangetastet blieb (D-04 Beweis), (d) commit-hash(es) via `jj log -r @-` (Projekt nutzt jj — Memory `feedback_use_jj_not_git`).
</output>

## Artifacts this phase produces (Plan 26-01)

| Path | Kind | Purpose |
|------|------|---------|
| `genossi_mail/src/sanitize.rs` (mod tests +3 fns) | Rust unit tests | Beweis: ammonia-Default lässt UL/OL/H1-H3 durch |
| `genossi_bin/tests/e2e_tests.rs` (+1 fn) | Rust E2E test | Beweis: POST/GET-Round-Trip via `/api/mail/templates` verliert kein Tag-Token |
