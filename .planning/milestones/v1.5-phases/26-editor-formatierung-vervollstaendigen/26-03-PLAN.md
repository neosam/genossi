---
phase: 26-editor-formatierung-vervollstaendigen
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/26-editor-formatierung-vervollstaendigen/26-UAT-CHECKLIST.md
autonomous: true
requirements:
  - EDIT-10
tags:
  - uat
  - docs
  - deferred-verification
  - phase-24-carryover
must_haves:
  truths:
    - "26-UAT-CHECKLIST.md existiert und enthält 16 Verifikations-Steps (12 aus Phase 24 unverändert + 4 neue für UL/OL/H2/H3)"
    - "Die 3 HARD FAIL GATES aus Phase 24 (styleWithCSS-Bold, Paste-Plain, In-App-Modal-Link) sind in 26-UAT-CHECKLIST.md klar als solche markiert"
    - "Setup-Sektion referenziert den Skill run-rust-backend-and-frontend und ist auf 2026-07 aktualisiert"
    - "Ein Sign-Off-Feld deckt alle 16 Steps mit einem Vorstand-Termin ab (D-05); UAT ist Ship-Gate vor v1.5-Milestone-Close, kein Merge-Gate innerhalb Phase (D-06)"
  artifacts:
    - ".planning/phases/26-editor-formatierung-vervollstaendigen/26-UAT-CHECKLIST.md"
  key_links:
    - "Die Checklist verbindet die automatisierten Beweise aus 26-01 und 26-02 (Round-Trip + Grep-Gate) mit dem browser-basierten Verifikations-Schritt, den kein Automated-Test abdecken kann (execCommand-Ausgabe im echten Chromium/Firefox)"
    - "Die 4 neuen Steps 13-16 spiegeln 1:1 die drei Elemente-Familien aus EDIT-06/07/08 (UL, OL, H2, H3) — H1 wird nicht als eigener UAT-Step aufgeführt, weil D-01 den H1-Button unangetastet lässt (bereits in Phase-24-UAT über 'Toolbar zeigt 13 Buttons' abgedeckt)"
---

<objective>
Erzeuge `26-UAT-CHECKLIST.md` als Copy der Phase-24-UAT-Checkliste (`.planning/milestones/v1.4-phases/24-wysiwyg-frontend-editor/24-UAT-CHECKLIST.md`) mit vier neuen Verifikations-Steps 13-16 für UL/OL/H2/H3, aktualisierter Setup-Sektion (2026-07, Skill `run-rust-backend-and-frontend`) und einem konsolidierten Sign-Off-Feld für alle 16 Steps.

Purpose: Erfüllt EDIT-10 und deckt Success-Criterion 4 aus ROADMAP.md (v1.4-Phase-24-UAT-Nachhol + neue Formatierungen als kombinierter Vorstand-Smoke-Test). Löst die in STATE.md dokumentierte Deferred Verification für Phase 24 auf: alle 12 alten Steps + 4 neue in einer Session. Per D-06 ist der UAT-Smoke Ship-Gate vor `/gsd-complete-milestone` (v1.5-Milestone-Close), NICHT Merge-Gate innerhalb Phase 26 — die Phase gilt code-fertig, sobald 26-01 und 26-02 grün sind.

Output: `.planning/phases/26-editor-formatierung-vervollstaendigen/26-UAT-CHECKLIST.md` — neues Dokument, keine Änderung an Produktions-Code, keine Änderung an der Phase-24-Original-Checkliste.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-editor-formatierung-vervollstaendigen/26-CONTEXT.md
@.planning/phases/26-editor-formatierung-vervollstaendigen/26-RESEARCH.md
@.planning/milestones/v1.4-phases/24-wysiwyg-frontend-editor/24-UAT-CHECKLIST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 26-UAT-CHECKLIST.md aus Phase-24-Vorlage aufbauen + Steps 13-16 anhängen (EDIT-10)</name>
  <files>.planning/phases/26-editor-formatierung-vervollstaendigen/26-UAT-CHECKLIST.md</files>
  <read_first>
    - .planning/milestones/v1.4-phases/24-wysiwyg-frontend-editor/24-UAT-CHECKLIST.md (kompletter Bestand — Copy-Vorlage; Setup-Sektion Zeile 9-26, 12 Verifikations-Steps 1-12, Known Limitations, Regression Check, Sign-Off-Block Zeile 93-100)
    - .planning/phases/26-editor-formatierung-vervollstaendigen/26-RESEARCH.md §"Pattern 4: UAT-Checklist als Copy + Anhang" (Format-Vorschlag für Steps 13-16 und Sign-Off-Zeile)
    - .planning/phases/26-editor-formatierung-vervollstaendigen/26-CONTEXT.md D-05 (Copy von 24 + 4 Steps + ein Sign-Off) + D-06 (Ship-Gate, nicht Merge-Gate; jj-WIP-Kontext) + D-01 (H1 bleibt in Toolbar, wird über bestehenden „13 Buttons"-Text in Step 1 mit-abgedeckt, kein eigener H1-UAT-Step nötig)
    - Memory `reference_frontend_smoke_test_setup.md` (Backend :3000 mock_auth DEVUSER, Frontend dx :8080; Send-Button nicht klicken — echte Mitglieder-E-Mails in Dev-DB) — bereits im Phase-24-Setup-Block Zeile 26 als Warnung enthalten, in 2026-07-Fassung beibehalten
  </read_first>
  <behavior>
    - Datei-Header: „# Phase 26 UAT Checklist" (nicht „24"), „**Phase:** 26-editor-formatierung-vervollstaendigen", „**Coverage:** EDIT-06, EDIT-07, EDIT-08, EDIT-09, EDIT-10 plus alle Phase-24-UAT-Punkte (deferred aus v1.4)".
    - „**Companion automated tests:**" Zeile: `sanitize_preserves_unordered_list`, `sanitize_preserves_ordered_list`, `sanitize_preserves_headings_h1_h2_h3` (in `genossi_mail/src/sanitize.rs`), `create_template_body_html_lists_and_headings_round_trip` (in `genossi_bin/tests/e2e_tests.rs`), `style_with_css_false_guard_present` + `paste_handler_calls_prevent_default_before_read` (in `genossi-frontend/src/component/mail_compose/wysiwyg_editor.rs`), plus die Phase-24-Companions `preview_body_html_round_trips_to_response` + `inbox_reply_body_html_sanitized_and_persisted`.
    - Einleitungs-Paragraph erklärt: „This checklist walks a Vorstand-facing UAT run through the three compose flows (Massenmail, Inbox-Reply, Mail-Template editor) to prove the WYSIWYG editor behaves correctly in a real browser. Steps 3, 4, 5 are hard-fail gates (Phase-24-Invarianten). Steps 13-16 verify the Phase-26 additions: UL, OL, H2, H3 round-trip through Save + Reload. Per D-06 this checklist is the SHIP-Gate before `/gsd-complete-milestone` (v1.5-milestone-close), NOT a merge-gate inside Phase 26 (jj-WIP-Changes make classic PR-gating irrelevant)."
    - Setup-Sektion: identischer Inhalt wie in 24-UAT-CHECKLIST.md Zeile 9-26 (Backend mit `--features mock_auth`, Frontend `dx serve`, Skill `run-rust-backend-and-frontend`, Warnung „Send NICHT klicken — Dev-DB hat echte E-Mails"). Datums-Marker im Header „Updated 2026-07-17" damit klar ist, dass für v1.5 die Setup-Anweisungen aktuell sind (Assumption A6 aus RESEARCH.md hat inhaltlich keine Änderung ergeben — Ports/Skill unverändert).
    - Steps 1-12: WORTWÖRTLICHE COPY aus 24-UAT-CHECKLIST.md (Zeile 32-54), inklusive der drei HARD FAIL GATE-Markierungen bei Steps 3, 4, 5.
    - Neue Steps 13-16 (mit `[EDIT-XX]`-Requirement-Tags im Titel, analog zum Phase-24-Muster):
      - **13. Unordered List Toolbar-Button erzeugt `<ul><li>` [EDIT-06].** Navigate to Massenmail-Compose. Type „Erstens" ↵ „Zweitens" ↵ „Drittens". Select alle drei Zeilen. Click UL-Button (`•`) in der Toolbar. Open DevTools Elements panel, inspect innerHTML des `<div id="wysiwyg-editor">`. Expected: `<ul><li>Erstens</li><li>Zweitens</li><li>Drittens</li></ul>` — leere `<br>`-Filler in `<li>` sind akzeptiert (execCommand-Cross-Browser-Toleranz, Pitfall 5 in 26-RESEARCH.md). Save-as-Template → Templates-Page → Reload → Template öffnen → innerHTML enthält weiterhin `<ul>` und `<li>`-Tags mit den drei Texten. **FAIL:** UL-Struktur ist verloren oder als `<div>`/`<p>`-Fallback vorhanden — Fix: `wysiwyg_toolbar.rs` UL-Button-Handler prüfen.
      - **14. Ordered List Toolbar-Button erzeugt `<ol><li>` [EDIT-07].** Wie Step 13, aber mit dem OL-Button (`1.`). Expected: `<ol><li>Erstens</li><li>Zweitens</li><li>Drittens</li></ol>`. Save → Reload → OL-Struktur intakt. **FAIL:** OL fehlt oder wird zu UL. Fix: `wysiwyg_toolbar.rs` OL-Button-Handler prüfen.
      - **15. H2 Toolbar-Button erzeugt `<h2>` und überlebt Reload [EDIT-08].** Type „Kapitel-Titel", select all, click H2-Button (`H2`). Expected: innerHTML enthält `<h2>Kapitel-Titel</h2>`. Save-as-Template → Reload → Template neu öffnen → innerHTML enthält weiterhin `<h2>`. **FAIL:** `<div>`/`<p>` statt `<h2>` oder `<h2>`-Tag wird beim Reload zu Plain-Text. Fix: `wysiwyg_toolbar.rs` H2-Button-Handler (`formatBlock <h2>`) + ammonia-Whitelist prüfen.
      - **16. H3 Toolbar-Button erzeugt `<h3>` und überlebt Reload [EDIT-08].** Wie Step 15, aber mit H3-Button (`H3`) und Text „Sub-Titel". Expected: `<h3>Sub-Titel</h3>` überlebt Save → Reload byte-identisch (modulo ammonia-Whitespace-Normalisierung, Pitfall 4).
    - „Known limitations"-Sektion aus Phase 24 (Zeile 56-64) übernehmen und um zwei neue Bullets erweitern:
      - „**execCommand emitiert manchmal leere `<br>`-Filler in `<li>`.** Chromium fügt `<br>` in leere neue Listeneinträge ein, Firefox nicht (Pitfall 5 in 26-RESEARCH.md). Der Round-Trip-Test in `e2e_tests.rs` und die ammonia-Grenze lassen `<br>` beide durch — daher ist der Filler kein UAT-Fail, auch wenn er im innerHTML sichtbar ist."
      - „**H1-Button bleibt in der Toolbar (D-01).** Auch wenn EDIT-08 nur H2/H3 fordert, wird H1 NICHT entfernt — die 13-Buttons-Ziffer in Step 1 zählt H1 mit. Ein Round-Trip-Test in `genossi_mail/src/sanitize.rs::sanitize_preserves_headings_h1_h2_h3` deckt H1 zusätzlich automatisiert ab."
    - „Regression check"-Sektion aus Phase 24 (Zeile 68-91) übernehmen, aber die Baseline-Zahlen aktualisieren: `genossi_mail --lib` von „252+" auf „255+" (nach Plan 26-01), `genossi_bin --test e2e_tests` von „306 tests" auf „309 tests (davon 1 pre-existing failure aus Phase 22 — dokumentiert in STATE.md, NICHT Phase-26-Regression)", `genossi-frontend` von „284+" auf „286+" (nach Plan 26-02). Frontend-Kommando bleibt `cargo test --bin genossi-frontend` bzw. `cargo test -p genossi-frontend`.
    - „Sign-off"-Sektion: EIN Sign-Off-Feld für alle 16 Steps (D-05 „ein Sign-Off-Termin"):
      ```
      - **Vorstand smoke tester:** _______________
      - **Date:** _______________
      - **All 16 steps checked:** ☐ Yes  ☐ No — see notes
      - **Hard-fail gates (3, 4, 5) passed:** ☐ Yes  ☐ No — MUST fix before v1.5 milestone close
      - **New Phase-26 steps (13, 14, 15, 16) passed:** ☐ Yes  ☐ No — MUST fix before v1.5 milestone close
      ```
    - Abschluss-Paragraph (D-06 explizit): „If any hard-fail gate (3, 4, 5) or new Phase-26 step (13, 14, 15, 16) failed, block v1.5-milestone-close (`/gsd-complete-milestone`) and file a bug ticket referencing the specific EDIT-XX requirement plus DevTools evidence. Non-critical failures on Phase-24 steps (11, 12) may be deferred — but Phase-24 was already deferred once; the milestone-audit skill checks that this checklist is signed off before v1.5 archives."
  </behavior>
  <action>
    Erstelle `.planning/phases/26-editor-formatierung-vervollstaendigen/26-UAT-CHECKLIST.md`. Nutze die exakte Datei-Struktur von `.planning/milestones/v1.4-phases/24-wysiwyg-frontend-editor/24-UAT-CHECKLIST.md` (Sektionen: Header, Companion tests, Setup, Verification Steps, Known limitations, Regression check, Sign-off, Deferred/Close-Note) als Skelett. Steps 1-12 wortwörtlich kopieren; die drei HARD FAIL GATE-Markierungen bei Steps 3/4/5 mit dem gleichen `⚠️ **HARD FAIL GATE.**`-Marker beibehalten. Steps 13-16 aus dem `<behavior>`-Block einfügen. Setup-Datum mit „Updated 2026-07-17" markieren. Sign-Off auf 16 Steps + neue Zeile für Steps 13-16 erweitern. Companion-tests-Zeile um die 4 neuen automatisierten Tests aus Plan 26-01 und 26-02 erweitern. Baseline-Zahlen im Regression-Check-Block aktualisieren (255 / 309 mit 1 Pre-existing failure / 286+). D-01, D-05, D-06 explizit im Text zitieren, wo sie eine Design-Entscheidung erklären (H1-Beibehaltung, ein Sign-Off, Ship-Gate-Semantik).
  </action>
  <verify>
    <automated>test -f .planning/phases/26-editor-formatierung-vervollstaendigen/26-UAT-CHECKLIST.md &amp;&amp; grep -c '^- \[ \] \*\*' .planning/phases/26-editor-formatierung-vervollstaendigen/26-UAT-CHECKLIST.md | grep -q '^16$' &amp;&amp; grep -qE 'HARD FAIL GATE' .planning/phases/26-editor-formatierung-vervollstaendigen/26-UAT-CHECKLIST.md &amp;&amp; grep -qE '\[EDIT-06\]' .planning/phases/26-editor-formatierung-vervollstaendigen/26-UAT-CHECKLIST.md &amp;&amp; grep -qE '\[EDIT-07\]' .planning/phases/26-editor-formatierung-vervollstaendigen/26-UAT-CHECKLIST.md &amp;&amp; grep -qE '\[EDIT-08\]' .planning/phases/26-editor-formatierung-vervollstaendigen/26-UAT-CHECKLIST.md &amp;&amp; grep -qE 'All 16 steps checked' .planning/phases/26-editor-formatierung-vervollstaendigen/26-UAT-CHECKLIST.md &amp;&amp; grep -qE 'run-rust-backend-and-frontend' .planning/phases/26-editor-formatierung-vervollstaendigen/26-UAT-CHECKLIST.md &amp;&amp; grep -qE '255' .planning/phases/26-editor-formatierung-vervollstaendigen/26-UAT-CHECKLIST.md &amp;&amp; echo "PLAN-26-03 VERIFY PASS"</automated>
  </verify>
  <done>
    - Datei existiert unter `.planning/phases/26-editor-formatierung-vervollstaendigen/26-UAT-CHECKLIST.md`.
    - Enthält genau 16 Verifikations-Checkbox-Steps (Zählung via `grep -c '^- \[ \] \*\*'`).
    - Alle drei Requirement-Tags `[EDIT-06]`, `[EDIT-07]`, `[EDIT-08]` sind in Steps 13-16 explizit gesetzt (EDIT-09 wird über Companion-tests-Zeile abgedeckt, EDIT-10 ist die Meta-Erfüllung durch Existenz der Datei selbst).
    - Setup-Sektion referenziert `run-rust-backend-and-frontend`, Backend `--features mock_auth`, Frontend `dx serve` :8080, Warnung „Send NICHT klicken".
    - HARD FAIL GATE-Marker sind bei Steps 3, 4, 5 vorhanden (`⚠️ **HARD FAIL GATE.**`).
    - Sign-Off-Block enthält „All 16 steps checked" und getrennte Bestätigung für „Hard-fail gates (3, 4, 5)" UND „New Phase-26 steps (13, 14, 15, 16)".
    - Regression-Check-Block nennt aktualisierte Baselines: 255 (`genossi_mail --lib`), 309 mit 1 pre-existing failure (`genossi_bin --test e2e_tests`), 286+ (`genossi-frontend`).
    - Abschluss-Paragraph erklärt Ship-Gate-Semantik (D-06) und dass v1.5-milestone-close blockiert wird, wenn Hard-fail oder neue Steps 13-16 fehlschlagen.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human Vorstand-Verifier → Software Behavior | Der UAT-Smoke ist die einzige Verifikation, die execCommand-Cross-Browser-Verhalten, DevTools-innerHTML-Beobachtung und die Wahrnehmung „sieht aus wie normale Text-Verarbeitung" abdeckt — kein Automated-Test kann das ersetzen. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-03 | Repudiation | UAT sign-off | low | mitigate | Sign-Off-Feld mit Vorstand-Name + Date + Checkbox pro Kategorie (Hard-fail-gates, neue Phase-26-Steps) erzeugt einen nachvollziehbaren Verifikations-Nachweis. Der Milestone-Audit-Skill kann die Datei später verifizieren. Datei ist committed via jj → jj-Log führt Buch. |

Kein technischer Angriffs-Vektor eingeführt — die Datei ist Dokumentation. Sicherheitswert liegt in der prozessualen Kontrolle: v1.5 archiviert nicht ohne diese Signatur.
</threat_model>

<verification>
```bash
# Automated integrity check (auch aus <verify> im Task)
test -f .planning/phases/26-editor-formatierung-vervollstaendigen/26-UAT-CHECKLIST.md
grep -c '^- \[ \] \*\*' .planning/phases/26-editor-formatierung-vervollstaendigen/26-UAT-CHECKLIST.md
# → 16

# Manuelle Verifikation (Reviewer):
# - Öffne die Datei, lies Steps 1-12, vergleiche mit .planning/milestones/v1.4-phases/24-wysiwyg-frontend-editor/24-UAT-CHECKLIST.md Steps 1-12 (müssen wortidentisch sein).
# - Lies Steps 13-16, prüfe dass jeder einen Toolbar-Klick, DevTools-innerHTML-Check und Save→Reload-Verifikation beschreibt.
# - Prüfe dass Setup-Sektion Backend :3000 mock_auth + Frontend dx serve :8080 + „Send nicht klicken" enthält.
```

Das eigentliche UAT-Ausführen ist NICHT Teil dieser Phase (D-06 Ship-Gate) — die Checkliste wird für Vorstand bereitgestellt und vor v1.5-Milestone-Close abgehakt.
</verification>

<success_criteria>
- Plan-Abschluss meldet: `26-UAT-CHECKLIST.md` mit 16 Steps existiert, Zählung stimmt via grep.
- Requirement-Coverage in diesem Plan: EDIT-10 (UAT-Checkliste-Nachhol).
- Der Milestone-Audit-Skill (bei `/gsd-complete-milestone` für v1.5) findet die Datei und kann den Sign-Off-Status prüfen.
</success_criteria>

<output>
Create `.planning/phases/26-editor-formatierung-vervollstaendigen/26-03-SUMMARY.md` when done. Include: (a) Pfad + Größe der neuen Datei, (b) Diff-Zusammenfassung gegen 24-UAT-CHECKLIST.md (welche Zeilen aktualisiert wurden für 2026-07, welche Baseline-Zahlen), (c) Text der 4 neuen Steps 13-16 (zum schnellen Review-Copy), (d) commit-hash via `jj log -r @-`.
</output>

## Artifacts this phase produces (Plan 26-03)

| Path | Kind | Purpose |
|------|------|---------|
| `.planning/phases/26-editor-formatierung-vervollstaendigen/26-UAT-CHECKLIST.md` | Markdown UAT checklist | 16-Punkte-Vorstand-Sign-Off — Ship-Gate vor v1.5-Milestone-Close (D-05, D-06) |
