---
phase: 28-desktop-mobile-vorschau
plan: 05
type: execute
wave: 4
depends_on: ["28-04"]
files_modified:
  - .planning/phases/28-desktop-mobile-vorschau/28-UAT-CHECKLIST.md
autonomous: false
requirements: [PREV-01, PREV-02, PREV-03, PREV-04, PREV-05]

must_haves:
  truths:
    - "Es existiert eine ausführbare UAT-Checkliste, die genau die Punkte abdeckt, die nicht automatisierbar sind"
    - "Jeder der vier RESEARCH-Annahmen mit UAT-Bedarf (Cookie im Sandbox-iframe, srcdoc-Reaktivität, Re-Render ohne Remount, Zeichenkodierung) ist ein eigener, prüfbarer Schritt"
    - "Die Checkliste warnt an prominenter Stelle davor, in der Dev-Umgebung den Versand auszulösen"
    - "Die CSS-Bleed-Gegenprobe ist als konkreter DevTools-Handgriff beschrieben, nicht als Absichtserklärung"
  artifacts:
    - ".planning/phases/28-desktop-mobile-vorschau/28-UAT-CHECKLIST.md"
  key_links:
    - "Erfolgskriterium 4 (kein CSS-Bleed in beide Richtungen) → UAT-Schritt mit Konflikt-Selektor"
    - "Erfolgskriterium 5 (v1.4-Templates ohne Bilder) → UAT-Schritt mit Alt-Template"
    - "RESEARCH-Annahme A2 (SameSite=Strict-Cookie im Sandbox-iframe) → UAT-Schritt mit Network-Tab"
    - "Pitfall 1 end-to-end → UAT-Schritt über den Test-Empfänger-Pfad des TemplateTester"
---

<objective>
Die nicht automatisierbaren Anteile der Phase in eine ausführbare Vorstands-Checkliste gießen
und den Abnahme-Checkpoint setzen.

Purpose: Vier Dinge lassen sich weder nativ noch am HTTP-Endpoint prüfen und brauchen einen
Browser: die visuelle Abgrenzung des Device-Rahmens (PREV-04), das tatsächliche Laden der
Bilder im sandboxed iframe samt Cookie-Verhalten (PREV-03), die CSS-Bleed-Gegenprobe in beide
Richtungen (PREV-05, Erfolgskriterium 4) und die Darstellung bestehender v1.4-Templates ohne
Bilder (Erfolgskriterium 5). Dazu kommen vier im Research protokollierte Annahmen, die
Browser-Verhalten betreffen und deshalb empirisch bestätigt werden müssen.

Output: `28-UAT-CHECKLIST.md` plus ein Abnahme-Checkpoint. Die Checkliste folgt der Form der
Phase-26-Checkliste und ist wie dort das Ship-Gate vor dem Milestone-Abschluss, nicht ein
Merge-Gate innerhalb der Phase — das Repo arbeitet mit jj-WIP-Changes, klassisches PR-Gating
greift hier nicht.

Kein Code in diesem Plan. Keine Änderung an Quelldateien.
</objective>

<execution_context>
@/home/neosam/programming/projects/genossi/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/projects/genossi/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-desktop-mobile-vorschau/28-CONTEXT.md
@.planning/phases/28-desktop-mobile-vorschau/28-RESEARCH.md
@.planning/phases/28-desktop-mobile-vorschau/28-01-SUMMARY.md
@.planning/phases/28-desktop-mobile-vorschau/28-02-SUMMARY.md
@.planning/phases/28-desktop-mobile-vorschau/28-03-SUMMARY.md
@.planning/phases/28-desktop-mobile-vorschau/28-04-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this plan produces

| Art | Pfad | Neu / geändert |
|---|---|---|
| UAT-Checkliste | `.planning/phases/28-desktop-mobile-vorschau/28-UAT-CHECKLIST.md` | neu |

Keine Quelldateien. Keine neuen Symbole, keine neuen i18n-Keys, keine Tests.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: 28-UAT-CHECKLIST.md schreiben</name>

  <files>.planning/phases/28-desktop-mobile-vorschau/28-UAT-CHECKLIST.md</files>

  <read_first>
    - `.planning/phases/26-editor-formatierung-vervollstaendigen/26-UAT-CHECKLIST.md` — die vollständige Formvorlage: Kopfblock mit `**Phase:**`, `**Coverage:**` und `**Companion automated tests:**`, danach ein Setup-Abschnitt mit den beiden Startbefehlen und dem Testdaten-Hinweis, danach nummerierte Checkbox-Schritte. Jeder Schritt nennt die Requirement-Id in eckigen Klammern, beschreibt den Handgriff, das erwartete Ergebnis und bei Fehlschlag den Fix-Ort. Hard-Fail-Gates sind mit einem Warnzeichen markiert.
    - `.planning/phases/28-desktop-mobile-vorschau/28-RESEARCH.md` § Test-Strategie, Absatz „Nicht automatisierbar" — die vier Punkte, die zwingend in die Checkliste gehören.
    - `.planning/phases/28-desktop-mobile-vorschau/28-RESEARCH.md` § Assumptions Log — die sechs Annahmen; A2, A3 und A4 tragen ausdrücklich den Vermerk, dass ein UAT-Schritt einzuplanen ist. A1 (Zeichenkodierung) ist zwar mitigiert, wird aber mitgeprüft, weil deutsche Umlaute in jeder realen Mail vorkommen.
    - `.planning/phases/28-desktop-mobile-vorschau/28-RESEARCH.md` § CSS-Isolation, Punkt 3 — der beschriebene 30-Sekunden-DevTools-Check für die Gegenprobe.
    - `.planning/phases/28-desktop-mobile-vorschau/28-RESEARCH.md` § Pitfall 1 und § Pitfall 6 — die beiden Verhaltensweisen, die im Browser bestätigt werden müssen.
    - `.planning/phases/28-desktop-mobile-vorschau/28-CONTEXT.md` § Integration Points — die UAT-Setup-Warnung: die Dev-DB enthält echte Mitglieder-E-Mail-Adressen.
    - Alle vier SUMMARY-Dateien der Pläne 28-01 bis 28-04 — damit die Checkliste die tatsächlich gebauten Symbole, Testnamen und Fix-Orte benennt und nicht die geplanten.
    - `.claude/skills/run-rust-backend-and-frontend/SKILL.md` — das Projekt-Skill für das Hochfahren beider Server; die Checkliste verweist darauf statt die Befehle zu duplizieren, nennt sie aber zusätzlich wörtlich als Fallback.
  </read_first>

  <action>
Schreibe `.planning/phases/28-desktop-mobile-vorschau/28-UAT-CHECKLIST.md` in der Form der
Phase-26-Checkliste. Sprache Deutsch; Code-Identifier, Dateipfade und Befehle im Original.

**Kopfblock.** `**Phase:** 28-desktop-mobile-vorschau`, `**Coverage:** PREV-01..PREV-05` sowie
`**Companion automated tests:**` mit den tatsächlich existierenden Testnamen aus den vier
SUMMARYs — mindestens `preview_body_html_is_sanitized_before_render`,
`preview_body_html_img_keeps_asset_id_strips_src` (beide in `genossi_bin/tests/e2e_tests.rs`),
`preview_frame_never_allows_scripts`, `srcdoc_is_self_contained_no_external_css`,
`inject_asset_src_rejects_quote_injection_payload` (alle in
`genossi-frontend/src/component/mail_compose/mail_preview_frame.rs`),
`editor_is_hidden_offscreen_not_display_none`,
`preview_mode_switch_syncs_dom_before_switching` (beide in
`genossi-frontend/src/component/mail_compose/wysiwyg_editor.rs`) und
`test_mail_recipient_comes_from_test_address_only` (in
`genossi-frontend/src/component/mail_compose/template_tester.rs`).

Ein Einleitungsabsatz hält fest, dass diese Checkliste analog zur Phase-26-Regelung das
Ship-Gate vor dem v1.5-Milestone-Abschluss ist und kein Merge-Gate innerhalb der Phase.

**Setup-Abschnitt.** Verweis auf das Projekt-Skill `run-rust-backend-and-frontend`, dazu die
Befehle wörtlich als Fallback: Backend aus dem Repo-Root mit `cargo run --features mock_auth
--bin genossi` auf Port 3000, Frontend aus `genossi-frontend/` mit dem Tailwind-Watcher
(`npx tailwindcss -i ./input.css -o ./assets/tailwind.css --watch`) und `dx serve` auf Port 8080.

Direkt darunter, optisch hervorgehoben, die Datenschutz-Warnung: die Dev-Datenbank enthält
echte Mitglieder-E-Mail-Adressen. In dieser Checkliste wird der Versand-Button auf der
Massenmail-Seite und im Reply-Formular NICHT geklickt. Der einzige zugelassene Versandpfad
ist der Test-Empfänger im `TemplateTester`, weil dieser eine ausdrücklich eingetippte
Test-Adresse verwendet und niemals die Adresse des gewählten Mitglieds.

**Testdaten-Vorbereitung.** Drei Vorbereitungspunkte, jeweils als Checkbox:
ein Mitglied mit Umlauten in Vor- oder Nachnamen; ein bestehendes v1.4-Template ohne Bilder
(oder eines neu anlegen, das nur Text und Fettung enthält); ein v1.5-Template mit mindestens
einer Liste, einer Überschrift und einem per Toolbar oder Drag-and-Drop eingefügten Bild.

**Verifikationsschritte.** Nummerierte Checkboxen, jeder Schritt mit Requirement-Id in eckigen
Klammern, Handgriff, erwartetem Ergebnis und Fix-Ort bei Fehlschlag. Mindestens diese
sechzehn Schritte, in dieser Reihenfolge:

  1. **Drei Modus-Buttons sichtbar und beschriftet [PREV-01].** Massenmail-Compose öffnen.
     Über der Toolbar steht eine Button-Gruppe mit drei Schaltflächen. Der aktive Modus ist
     farblich hervorgehoben. Konsole zeigt keine roten Fehler.
  2. **Umschalten blendet die Toolbar aus [PREV-04, D-14].** Auf Desktop-Vorschau schalten.
     Die Formatier-Toolbar verschwindet vollständig, nicht ausgegraut. Zurück auf Bearbeiten:
     Toolbar ist wieder da und die Buttons funktionieren.
  3. **Hinweiszeile ohne gewähltes Mitglied [PREV-02, D-03].** Ohne Mitgliedsauswahl in einen
     Vorschau-Modus schalten. Erwartet: eine Hinweiszeile, kein leerer Rahmen. Im Network-Tab
     wurde KEIN Request an `/api/mail/preview` gestellt.
  4. **Device-Rahmen und Label [PREV-04, D-15].** ⚠️ Hard-Fail-Gate. Mitglied wählen,
     Desktop-Vorschau. Erwartet: grauer Hintergrund, zentrierter Rahmen, darüber ein Label,
     das die Breite in Klammern nennt. Auf Mobile umschalten: der Rahmen wird sichtbar
     schmaler, das Label ändert sich entsprechend. Fehlschlag heißt, dass ein versehentliches
     Tippen im Vorschau-Modus nicht offensichtlich folgenlos wirkt.
  5. **Genau ein Request beim Wechsel in die Vorschau [D-05].** Network-Tab filtern auf
     `preview`. Von Bearbeiten auf Desktop: genau ein Request. Von Desktop auf Mobile: KEIN
     weiterer Request, die Breite ändert sich trotzdem. Von Mobile zurück auf Bearbeiten und
     wieder auf Mobile: genau ein weiterer Request.
  6. **Vorschau zeigt die sanitisierte Fassung [PREV-02].** ⚠️ Hard-Fail-Gate. Im Editor per
     DevTools-Elements-Panel ein Attribut in den contenteditable-Inhalt einschleusen, das die
     ammonia-Allowlist nicht passiert, danach in die Vorschau schalten. Erwartet: die Vorschau
     zeigt den Inhalt ohne dieses Attribut. Zum Gegenbeweis das iframe-Dokument im
     Elements-Panel aufklappen. Fehlschlag heißt, dass die Vorschau das rohe Editor-DOM zeigt
     und der Phasenzweck verfehlt ist.
  7. **Bilder laden im iframe [PREV-03, Annahme A2].** ⚠️ Hard-Fail-Gate. Das v1.5-Template
     mit Bild in den Editor laden, Mitglied wählen, in die Desktop-Vorschau schalten. Erwartet:
     das Bild wird angezeigt. Im Network-Tab erscheint ein Request auf die
     Asset-Bytes-Route mit Status 200, und in dessen Request-Headern ist das Session-Cookie
     enthalten. Fehlschlag mit Status 401 oder ohne Cookie bedeutet, dass die
     Same-Origin-Erlaubnis der Sandbox nicht greift; Fix-Ort ist das Sandbox-Attribut in
     `mail_preview_frame.rs`.
  8. **Kaputtes Bild bleibt beim Browser-Default [D-08].** Im DevTools-Elements-Panel die
     Asset-Id im Editor-Inhalt auf eine gültige, aber unbekannte UUID ändern, Vorschau neu
     laden. Erwartet: das Standard-Symbol des Browsers für ein nicht ladbares Bild, kein
     eigener Platzhalter, kein Absturz der Vorschau.
  9. **v1.4-Template ohne Bilder rendert unverändert [Erfolgskriterium 5].** Das
     Alt-Template laden, beide Vorschau-Modi durchschalten. Erwartet: Text, Absätze und
     Fettung erscheinen korrekt; keine Fehlermeldung, kein leerer Rahmen.
 10. **v1.5-Template mit Listen, Überschriften und Bild [Erfolgskriterium 5].** Erwartet:
     Aufzählungszeichen bzw. Nummern sind sichtbar, Überschriften sind größer als der Fließtext,
     das Bild ist auf die Rahmenbreite begrenzt und läuft nicht über.
 11. **Umlaute erscheinen korrekt [Annahme A1, Pitfall 8].** Ein Text mit `Grüße` und
     `Mitgliedschaftserklärung` sowie ein Mitglied mit Umlaut im Namen. Erwartet: in beiden
     Vorschau-Modi keine Ersatzzeichen und keine Mojibake.
 12. **CSS-Bleed-Gegenprobe in beide Richtungen [PREV-05, Erfolgskriterium 4].** ⚠️
     Hard-Fail-Gate. Richtung eins: im DevTools-Styles-Panel temporär eine globale Regel auf
     `body` setzen, die die Schriftart auffällig ändert (etwa auf eine Schreibschrift). Erwartet:
     die App ändert sich, der Inhalt IM Rahmen bleibt unverändert. Richtung zwei: im
     Elements-Panel in das iframe-Dokument hineingehen und dort eine auffällige Regel setzen.
     Erwartet: nur der Rahmeninhalt ändert sich, die App außen bleibt unverändert. Beide
     Änderungen danach verwerfen. Fehlschlag bedeutet, dass die Isolation selbst unterlaufen
     wurde — Fix-Ort ist der Dokument-Aufbau in `mail_preview_frame.rs`.
 13. **Links in der Vorschau sind nicht klickbar [PREV-04, D-07].** Ein Template mit einem
     Link laden, in die Vorschau schalten, auf den Link klicken. Erwartet: nichts passiert,
     kein neuer Tab, keine Navigation, keine Konsolenmeldung.
 14. **Zweimaliges Hin- und Herschalten zeigt aktuellen Inhalt [Annahme A3].** In die
     Vorschau schalten, zurück auf Bearbeiten, den Text sichtbar ändern, wieder in die
     Vorschau. Erwartet: die Vorschau zeigt die geänderte Fassung, nicht die alte.
 15. **Der Editor-Inhalt überlebt einen Mitglieds-Wechsel [Annahme A4].** Text tippen, ohne
     das Template zu wechseln ein anderes Mitglied auswählen. Erwartet: der getippte Text
     steht unverändert im Editor. Zusatzprüfung für Pitfall 6: ein anderes Template auswählen.
     Erwartet und ausdrücklich KEIN Fehler: der Modus springt auf Bearbeiten zurück und der
     Editor zeigt das neue Template.
 16. **Zeilenumbrüche überleben den Vorschau-Ausflug [Pitfall 1, PREV-01].** ⚠️
     Hard-Fail-Gate. Auf der Mail-Template-Seite ein Template mit mehreren Absätzen und einer
     Liste anlegen. In die Desktop-Vorschau und zurück auf Bearbeiten schalten. Danach im
     `TemplateTester` ein Mitglied wählen, eine eigene Test-Adresse eintragen und die Test-Mail
     senden. Erwartet: der `text/plain`-Teil der empfangenen Mail hat die Absatz- und
     Listenumbrüche. Fehlschlag mit einer einzigen Zeile Fließtext bedeutet, dass der
     contenteditable-Container im Vorschau-Modus aus dem Rendering genommen wurde; Fix-Ort ist
     `editor_container_style` in `wysiwyg_editor.rs`.

**Zusatzabschnitt „Nebeneffekt aus D-01 gegenprüfen".** Ein eigener Checkbox-Punkt: die
bestehende `TemplatePreview` zeigt seit Plan 28-01 ebenfalls die sanitisierte Fassung. Prüfen,
dass sie dadurch nicht unerwartet anders aussieht als vor Phase 28, und einen etwaigen
Unterschied im UAT-Protokoll notieren statt ihn als Bug zu melden — er ist die Behebung eines
vorbestehenden Defekts.

**Abschlussabschnitt.** Eine Tabelle mit den drei Wiederholungsflächen (Massenmail-Compose,
Mail-Template-Editor, Inbox-Reply) und den Schrittnummern, die in jeder Fläche zu wiederholen
sind: die Schritte 1 bis 5 gelten in allen dreien, weil der `WysiwygEditor` überall dieselbe
Component ist. Für das Reply-Formular zusätzlich der Hinweis, dass dort per Ausstiegsklausel
keine Mitgliedsauswahl existiert und das zugeordnete Mitglied implizit gilt — Schritt 3 ist
dort nur ohne zugeordnetes Mitglied auslösbar.

Am Ende ein Protokollblock: Datum, ausführende Person, Ergebnis pro Hard-Fail-Gate, und für
jeden Fehlschlag die drei geforderten Angaben (getroffene Requirement-Id, DevTools-Beleg,
vermuteter Fix-Ort).
  </action>

  <verify>
    <automated>test -f .planning/phases/28-desktop-mobile-vorschau/28-UAT-CHECKLIST.md &amp;&amp; grep -c '^- \[ \]' .planning/phases/28-desktop-mobile-vorschau/28-UAT-CHECKLIST.md</automated>
  </verify>

  <acceptance_criteria>
    - Die Datei existiert: `test -f .planning/phases/28-desktop-mobile-vorschau/28-UAT-CHECKLIST.md`.
    - `grep -c '^- \[ \]' .planning/phases/28-desktop-mobile-vorschau/28-UAT-CHECKLIST.md` ist >= 20 (16 Verifikationsschritte, 3 Vorbereitungspunkte, 1 Nebeneffekt-Punkt).
    - Jede der fünf Requirement-Ids kommt vor: `for r in PREV-01 PREV-02 PREV-03 PREV-04 PREV-05; do grep -c "$r" .planning/phases/28-desktop-mobile-vorschau/28-UAT-CHECKLIST.md; done` — alle fünf Werte sind >= 1.
    - Die Datenschutz-Warnung steht drin: `grep -c 'echte Mitglieder' .planning/phases/28-desktop-mobile-vorschau/28-UAT-CHECKLIST.md` ist >= 1.
    - Die Setup-Befehle stehen wörtlich drin: `grep -c 'mock_auth' …/28-UAT-CHECKLIST.md` >= 1 und `grep -c 'dx serve' …/28-UAT-CHECKLIST.md` >= 1.
    - Mindestens fünf Schritte sind als Hard-Fail-Gate markiert: `grep -c '⚠️' …/28-UAT-CHECKLIST.md` ist >= 5.
    - Die vier RESEARCH-Annahmen mit UAT-Bedarf sind namentlich referenziert: `grep -c 'A1\|A2\|A3\|A4' …/28-UAT-CHECKLIST.md` ist >= 4.
    - Die Companion-Testnamen sind echt: jeder im Kopfblock genannte Testname existiert im Repo, geprüft mit `grep -rn '<testname>' genossi-frontend/src genossi_bin/tests genossi_mail/src` — jeder Name liefert mindestens einen Treffer.
    - `git diff --name-only` listet ausschließlich die neue Checklisten-Datei.
  </acceptance_criteria>

  <done>
`28-UAT-CHECKLIST.md` existiert mit mindestens zwanzig Checkboxen, deckt alle fünf
Requirements sowie die vier UAT-pflichtigen Research-Annahmen ab, trägt die Datenschutz-Warnung
und nennt ausschließlich Testnamen, die tatsächlich im Repo existieren.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Vorstands-Smoke-Abnahme der Device-Vorschau</name>

  <files>.planning/phases/28-desktop-mobile-vorschau/28-UAT-CHECKLIST.md</files>

  <read_first>
    - `.planning/phases/28-desktop-mobile-vorschau/28-UAT-CHECKLIST.md` — die in Task 1 erstellte Checkliste, vollständig.
    - `.planning/phases/28-desktop-mobile-vorschau/28-01-SUMMARY.md` bis `28-04-SUMMARY.md` — was tatsächlich gebaut wurde, inklusive der beiden geführten Negativ-Nachweise.
  </read_first>

  <action>
Führe die in Task 1 erstellte Checkliste in einer echten Browser-Session durch und protokolliere
das Ergebnis. Fahre dazu beide Server hoch (Projekt-Skill `run-rust-backend-and-frontend`, sonst
`cargo run --features mock_auth --bin genossi` aus dem Repo-Root und Tailwind-Watcher plus
`dx serve` aus `genossi-frontend/`), arbeite die Testdaten-Vorbereitung und danach die sechzehn
Verifikationsschritte der Reihe nach ab und trage das Ergebnis in den Protokollblock am Ende
der Checkliste ein.

Der Versand-Button auf der Massenmail-Seite und im Reply-Formular wird NICHT geklickt — die
Dev-Datenbank enthält echte Mitglieder-E-Mail-Adressen. Der einzige zugelassene Versandpfad ist
Schritt 16 über den Test-Empfänger im `TemplateTester` mit einer selbst eingetippten Adresse.

Kann die Vorstands-Session jetzt nicht stattfinden, protokolliere die Abnahme ausdrücklich als
aufgeschoben und ergänze in `.planning/STATE.md` unter „Deferred Verification" eine Zeile für
Phase 28 mit dem Zustand `verification_deferred_human` und dem Resume-Hinweis auf
`28-UAT-CHECKLIST.md` — analog zu den bestehenden Einträgen für Phase 24 und Phase 26. Ein
stillschweigendes Überspringen ist nicht zulässig.
  </action>

  <what-built>
Plan 28-01 hat `POST /api/mail/preview` um den ammonia-Schritt vor dem Jinja-Rendering
erweitert. Plan 28-02 hat `mail_preview_frame.rs` mit `PreviewMode`, `inject_asset_src`,
`preview_srcdoc`, dem Baseline-Stylesheet und der `MailPreviewFrame`-Component samt vier
Grep-Gates angelegt. Plan 28-03 hat den Drei-Modi-Umschalter, die Toolbar-Ausblendung, die
Off-Screen-Hide-Strategie und den Preview-Fetch in den `WysiwygEditor` verdrahtet. Plan 28-04
hat die Mitglieds-Auswahl auf Page-Ebene gehoben und alle drei Call-Sites verkabelt.

Automatisiert abgesichert: `cd genossi-frontend && cargo test` (mindestens 326 Tests),
`cargo test -p genossi_mail`, `cargo test -p genossi_bin --test e2e_tests preview`. Zwei
Negativ-Nachweise wurden einmalig geführt und protokolliert: der Sandbox-Grep-Gate schlägt
bei hinzugefügter Script-Erlaubnis fehl, und der Hide-Strategie-Test schlägt bei
Rendering-Unterdrückung fehl.

Nicht automatisierbar und daher Gegenstand dieser Abnahme: die visuelle Abgrenzung des
Device-Rahmens, das tatsächliche Laden der Bilder im iframe samt Cookie-Verhalten, die
CSS-Bleed-Gegenprobe in beide Richtungen und die Darstellung bestehender v1.4-Templates.
  </what-built>

  <how-to-verify>
1. Server hochfahren — entweder über das Projekt-Skill `run-rust-backend-and-frontend` oder
   manuell: aus dem Repo-Root `cargo run --features mock_auth --bin genossi` (Port 3000), aus
   `genossi-frontend/` der Tailwind-Watcher plus `dx serve` (Port 8080).
2. `http://localhost:8080` im Browser öffnen, DevTools mit den Tabs Console, Network und
   Elements bereithalten.
3. `.planning/phases/28-desktop-mobile-vorschau/28-UAT-CHECKLIST.md` Schritt für Schritt
   abarbeiten, beginnend mit der Testdaten-Vorbereitung.
4. WARNUNG: den Versand-Button auf der Massenmail-Seite und im Reply-Formular NICHT klicken.
   Die Dev-Datenbank enthält echte Mitglieder-E-Mail-Adressen. Der einzige zugelassene
   Versandpfad ist Schritt 16 über den Test-Empfänger im `TemplateTester`, wo eine selbst
   eingetippte Test-Adresse verwendet wird.
5. Besondere Aufmerksamkeit auf die fünf Hard-Fail-Gates: Device-Rahmen (Schritt 4),
   sanitisierte Fassung in der Vorschau (Schritt 6), Bilder laden mit Cookie (Schritt 7),
   CSS-Bleed-Gegenprobe (Schritt 12) und Zeilenumbrüche nach dem Vorschau-Ausflug (Schritt 16).
6. Ergebnis im Protokollblock am Ende der Checkliste eintragen. Für jeden Fehlschlag: die
   getroffene Requirement-Id, ein DevTools-Beleg und der vermutete Fix-Ort.

Hinweis zur Einordnung: Diese Abnahme ist analog zur Phase-26-Regelung das Ship-Gate vor dem
v1.5-Milestone-Abschluss, kein Merge-Gate innerhalb der Phase. Kann die Vorstands-Session
jetzt nicht stattfinden, wird die Abnahme als aufgeschoben protokolliert und in STATE.md unter
den aufgeschobenen Verifikationen eingetragen — zusammen mit den bereits offenen Posten aus
Phase 24 und Phase 26.
  </how-to-verify>

  <acceptance_criteria>
    - Entweder: alle sechzehn Verifikationsschritte sind abgehakt und die fünf Hard-Fail-Gates sind bestanden; das Ergebnis steht im Protokollblock der Checkliste mit Datum und ausführender Person.
    - Oder: die Abnahme ist ausdrücklich aufgeschoben; in diesem Fall ist in `.planning/STATE.md` unter „Deferred Verification" eine Zeile für Phase 28 mit dem Zustand `verification_deferred_human` und dem Resume-Hinweis auf `28-UAT-CHECKLIST.md` ergänzt.
    - In beiden Fällen: das Ergebnis ist im SUMMARY dieses Plans festgehalten, inklusive der Liste etwaiger Fehlschläge mit Requirement-Id und vermutetem Fix-Ort.
  </acceptance_criteria>

  <resume-signal>Schreibe „approved", wenn alle Schritte bestanden sind; „deferred", wenn die Vorstands-Session später stattfindet; andernfalls beschreibe die Abweichungen mit Schrittnummer.</resume-signal>

  <verify>
    <automated>grep -qE 'approved|deferred|verification_deferred_human' .planning/phases/28-desktop-mobile-vorschau/28-UAT-CHECKLIST.md .planning/STATE.md</automated>
  </verify>

  <done>
Die Abnahme ist entweder vollständig durchgeführt (alle sechzehn Schritte abgehakt, fünf
Hard-Fail-Gates bestanden, Protokollblock mit Datum und ausführender Person gefüllt) oder
ausdrücklich aufgeschoben und in `.planning/STATE.md` unter „Deferred Verification" mit dem
Zustand `verification_deferred_human` eingetragen. In beiden Fällen steht das Ergebnis im
SUMMARY dieses Plans.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev-Umgebung → echte Mitglieder-E-Mail-Adressen | Die Entwicklungsdatenbank enthält produktive Adressen; ein versehentlicher Versand verlässt die Testumgebung |
| Browser-Session → Asset-Bytes-Route | Der UAT prüft, ob der Sandbox-iframe das Session-Cookie mitschickt — also genau die AuthZ-Wirkung des Phase-27-Endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-25 | Information Disclosure | UAT-Durchführung auf der Dev-Datenbank | high | mitigate | Die Datenschutz-Warnung steht unmittelbar unter dem Setup-Abschnitt und verbietet den Versand-Button auf der Massenmail-Seite und im Reply-Formular. Der einzige zugelassene Versandpfad ist der Test-Empfänger im `TemplateTester`, dessen Adresse der Prüfer selbst eintippt. Als Kriterium in Task 1 gegen die Datei geprüft |
| T-28-26 | Elevation of Privilege | Bild-Requests aus dem Sandbox-iframe | medium | mitigate | Schritt 7 der Checkliste prüft empirisch, dass die Asset-Bytes-Route mit Status 200 und mitgesendetem Session-Cookie antwortet. Damit wird die im Research als Annahme A2 protokollierte Browser-Verhaltensvermutung zur belegten Tatsache — sie ist Browser-Verhalten und nicht aus dem Repo-Code ableitbar |
| T-28-27 | Tampering | CSS-Isolation in der Praxis | high | mitigate | Schritt 12 prüft beide Richtungen mit einem bewusst gesetzten Konflikt-Selektor. Der automatisierte Teil beweist nur, dass wir die Browser-Garantie nicht selbst unterlaufen; erst dieser Schritt bestätigt sie am laufenden Objekt |
| T-28-28 | Tampering | Zeilenumbrüche im versendeten `text/plain`-Teil | high | mitigate | Schritt 16 fährt Pitfall 1 end-to-end über den sicheren Test-Empfänger-Pfad: Vorschau, zurück, senden, empfangene Mail prüfen. Das ist der einzige Weg, die Wirkung der Off-Screen-Strategie auf die tatsächlich versendete Mail zu belegen |
| T-28-29 | Repudiation | unprotokollierte oder stillschweigend übersprungene Abnahme | medium | mitigate | Der Checkpoint verlangt entweder ein vollständiges Protokoll oder einen ausdrücklichen Eintrag unter „Deferred Verification" in `.planning/STATE.md`. Ein stillschweigendes Überspringen ist durch die Abnahmekriterien ausgeschlossen |
| T-28-SC | Tampering | npm/pip/cargo installs | high | mitigate | Dieser Plan installiert kein Paket und ändert keine Quelldatei; er schreibt genau eine Markdown-Datei. Verifizierbar über das `git diff --name-only`-Kriterium in Task 1 |
</threat_model>

<verification>
1. `.planning/phases/28-desktop-mobile-vorschau/28-UAT-CHECKLIST.md` existiert und enthält
   mindestens zwanzig Checkboxen.
2. Alle fünf Requirement-Ids PREV-01 bis PREV-05 kommen in der Checkliste vor.
3. Mindestens fünf Schritte sind als Hard-Fail-Gate markiert.
4. Jeder im Kopfblock genannte Companion-Testname existiert tatsächlich im Repo.
5. Die Datenschutz-Warnung steht in der Checkliste und ist als solche erkennbar.
6. Der Checkpoint ist entweder mit vollständigem Protokoll bestanden oder ausdrücklich
   aufgeschoben und in `.planning/STATE.md` unter „Deferred Verification" vermerkt.
7. `git diff --name-only` listet ausschließlich die Checklisten-Datei (und bei aufgeschobener
   Abnahme zusätzlich `.planning/STATE.md`).
</verification>

<success_criteria>
- Die nicht automatisierbaren Anteile aller fünf Requirements sind als konkrete,
  nachvollziehbare Handgriffe beschrieben — keine Absichtserklärungen.
- Die vier UAT-pflichtigen Research-Annahmen haben je einen eigenen Schritt.
- Die Datenschutz-Warnung ist prominent und der einzige zugelassene Versandpfad ist benannt.
- Die Abnahme ist entweder durchgeführt und protokolliert oder ausdrücklich aufgeschoben und
  in STATE.md eingetragen.
</success_criteria>

<output>
Create `.planning/phases/28-desktop-mobile-vorschau/28-05-SUMMARY.md` when done.
Halte darin fest: (a) den Zustand der Abnahme (durchgeführt oder aufgeschoben), (b) bei
Durchführung das Ergebnis je Hard-Fail-Gate, (c) bei Aufschub den in STATE.md ergänzten
Eintrag im Wortlaut, (d) eine Liste der Punkte, die in eine spätere Phase gehören
(etwa Auto-Höhe des Rahmens, falls die feste Höhe im UAT als zu klein empfunden wird).
</output>
