---
phase: 01-assembly-aggregat-audit-hardening
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - genossi_service/src/assembly.rs
  - genossi_service/src/lib.rs
  - genossi_service_impl/src/assembly.rs
  - genossi_service_impl/src/lib.rs
autonomous: true
requirements: [ASSY-01, ASSY-02, ASSY-03, ASSY-05, ASSY-07]
requirements_addressed: [ASSY-01, ASSY-02, ASSY-03, ASSY-05, ASSY-07]
must_haves:
  truths:
    - "AssemblyService Trait definiert 6 Methoden: create_assembly, update_assembly, open_assembly, close_assembly, get_assembly, get_all_assemblies (D-13, RESEARCH §6)"
    - "create_assembly setzt Status auf Preparation, opened_at=None, closed_at=None und ruft audited_create! mit process \"assembly.create\" (D-11)"
    - "open_assembly prueft entity.status == Preparation; wenn nicht: ServiceError::Conflict (D-07, Pitfall 3)"
    - "open_assembly setzt status=Open und opened_at=now und ruft audited_update! mit process \"assembly.open\" (D-08, D-11)"
    - "open_assembly befuellt assembly_member_snapshot mit count_active-Filter aus member_dao (deleted IS NULL + status.is_normal + exit_date > opened_date) ATOMAR in derselben Transaction (D-02, D-08, RESEARCH §10)"
    - "close_assembly prueft entity.status == Open; wenn nicht: ServiceError::Conflict (Pitfall 3)"
    - "close_assembly setzt status=Closed und closed_at=now und ruft audited_update! mit process \"assembly.close\" (D-09, D-11)"
    - "close_assembly enthaelt KEINEN HelperSession-Cascade-Code (Phase 3, D-09)"
    - "update_assembly prueft entity.status == Preparation; sonst Conflict (D-07)"
    - "update_assembly prueft entity.version == request.version; sonst Conflict (Optimistic-Locking)"
    - "Alle 6 Service-Methoden rufen permission_service.check_permission(\"admin\", context) auf (D-14)"
    - "open_assembly verwendet eine einzige use_transaction(None), tx.clone() fuer alle Sub-Calls, EIN commit am Ende (Pitfall 2)"
    - "Snapshot-DAO-Aufrufe verwenden NICHT audited_create! (Pitfall 1)"
  artifacts:
    - path: genossi_service/src/assembly.rs
      provides: "Assembly DTO, AssemblySubmission, AssemblyUpdate, AssemblyDetail, AssemblyService trait"
      exports: ["Assembly", "AssemblySubmission", "AssemblyUpdate", "AssemblyDetail", "AssemblyService", "MockAssemblyService"]
    - path: genossi_service_impl/src/assembly.rs
      provides: "AssemblyServiceImpl mit gen_service_impl!, AssemblyServiceDeps, Lifecycle-Methoden"
      exports: ["AssemblyServiceImpl", "AssemblyServiceDeps"]
  key_links:
    - from: "AssemblyServiceImpl::open_assembly"
      to: "atomare Tx + audited_update! + Snapshot-Befuellung"
      via: "use_transaction(None) -> audited_update! -> member_dao.all -> snapshot_dao.create_batch -> commit"
      pattern: 'transaction_dao\.use_transaction\(None\)'
    - from: "AssemblyServiceImpl::open_assembly"
      to: "count_active filter"
      via: "filter is_normal + exit_date > opened_date"
      pattern: 'is_normal\(\).*exit_date'
    - from: "AssemblyServiceImpl::create_assembly"
      to: "audit-Hashchain"
      via: 'audited_create!(self, self.assembly_dao, &entity, "assembly.create", &user_id, tx)'
      pattern: 'audited_create!.*"assembly\.create"'
---

<objective>
Service-Schicht fuer das Assembly-Aggregat mit Lifecycle-Logik (Preparation→Open→Closed), atomarer Snapshot-Befuellung beim Open und Audit-Macro-Integration. Wave 2, depends on Plan 01 (DAO-Schicht).

Purpose: Domain-Logik konzentriert in AssemblyServiceImpl. State-Transition-Guards (Pitfall 3) und atomare Tx (Pitfall 2) sind die zwei kritischen Punkte. Permission-Checks und Audit-Macros sind Replikation existierender Patterns.
Output: Trait-Definition + Impl mit gen_service_impl!. ≥ 4 Unit-Tests mit Mocks. Phase-Plan 04 (REST) konsumiert die Trait.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-assembly-aggregat-audit-hardening/01-CONTEXT.md
@.planning/phases/01-assembly-aggregat-audit-hardening/01-RESEARCH.md
@.planning/phases/01-assembly-aggregat-audit-hardening/01-PATTERNS.md
@.planning/phases/01-assembly-aggregat-audit-hardening/01-01-PLAN.md
@.planning/phases/01-assembly-aggregat-audit-hardening/01-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@genossi_service/src/application.rs
@genossi_service_impl/src/application.rs
@genossi_service_impl/src/audit_macros.rs
@genossi_service_impl/src/macros.rs
@genossi_service/src/permission.rs

<interfaces>
<!-- Aus Plan 01 verfuegbar: -->
```rust
// genossi_dao::assembly
pub enum AssemblyStatus { Preparation, Open, Closed }
pub struct AssemblyEntity { id, name, date, location, status, opened_at, closed_at, created, deleted, version }
#[automock] pub trait AssemblyDao { type Transaction; dump_all, create, update, all, find_by_id }

// genossi_dao::assembly_member_snapshot
pub struct AssemblyMemberSnapshotEntity { assembly_id, member_id, captured_at }
#[automock] pub trait AssemblyMemberSnapshotDao { type Transaction; create, create_batch, find_by_assembly_id, count_by_assembly_id }
```

<!-- Audit-Macros (genossi_service_impl/src/audit_macros.rs) — Signaturen: -->
```rust
// audited_create!($self, $dao, $entity, $process, $user_id, $tx)
//   - committet NICHT
//   - $self braucht Felder: audit_log_dao, uuid_service
//   - Macro ruft $dao.create(...) und schreibt audit-entries fuer alle Some-Felder

// audited_update!($self, $dao, $entity_id, $new_entity, $process, $user_id, $tx)
//   - laedt old via $dao.find_by_id($entity_id, $tx.clone())
//   - committet NICHT
//   - schreibt audit-entries NUR fuer geaenderte Felder
```

<!-- Aus genossi_service_impl/src/macros.rs (gen_service_impl!): -->
```rust
gen_service_impl! {
    struct ServiceImpl: ServiceTrait = ServiceDeps {
        DepName: TraitName<Transaction = Self::Transaction> = field_name,
        ...
    }
}
// Generiert: ServiceDeps trait + ServiceImpl<Deps> struct mit Arc-gewrappten Feldern + new()-Konstruktor
```

<!-- Existing Service-Pattern (Vorlage): -->
```rust
// genossi_service/src/permission.rs
pub trait PermissionService {
    type Context;
    async fn check_permission(&self, privilege: &str, auth: Authentication<Self::Context>) -> Result<(), ServiceError>;
    async fn current_user_id(&self, auth: Authentication<Self::Context>) -> Result<Option<String>, ServiceError>;
}
pub struct Authentication<C> { /* ... */ }

// genossi_dao::TransactionDao
pub trait TransactionDao {
    type Transaction;
    async fn use_transaction(&self, tx: Option<Self::Transaction>) -> Result<Self::Transaction, DaoError>;
    async fn commit(&self, tx: Self::Transaction) -> Result<(), DaoError>;
}

// genossi_dao::member::MemberDao
pub trait MemberDao {
    async fn all(&self, tx: Self::Transaction) -> Result<Arc<[MemberEntity]>, DaoError>;
}
// MemberEntity hat: id, status: MemberStatus (mit is_normal()), exit_date: Option<Date>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AssemblyService-Trait + DTOs in genossi_service</name>
  <files>
    genossi_service/src/assembly.rs,
    genossi_service/src/lib.rs
  </files>
  <read_first>
    - genossi_service/src/application.rs (gesamte Datei — Vorlage fuer DTO-Struct, Submission/Update-DTOs, Service-Trait mit #[automock])
    - genossi_service/src/permission.rs (Authentication-Type, PermissionService-Trait)
    - genossi_service/src/lib.rs (mod-Reihenfolge, ServiceError-Re-Exports)
    - genossi_dao/src/assembly.rs (Plan 01 — AssemblyEntity, AssemblyStatus)
    - .planning/phases/01-assembly-aggregat-audit-hardening/01-RESEARCH.md (§6 Service-Shape)
  </read_first>
  <behavior>
    - `Assembly` Domain-DTO ist konstruierbar via `Assembly::from(&AssemblyEntity)` (alle Felder uebernommen, Arc<str> wird als Arc<str> gehalten)
    - `From<&Assembly> for AssemblyEntity` fuer Plan-04-Update-Pfad existiert
    - `AssemblyService` Trait ist via #[automock] mockbar
    - `AssemblyService` hat genau 6 async-Methoden mit Signaturen aus RESEARCH §6
    - `AssemblyDetail` enthaelt `assembly: Assembly` und `snapshot_member_count: u64`
    - Compile-Check: `MockAssemblyService` ist verfuegbar
  </behavior>
  <action>
    Erstelle `genossi_service/src/assembly.rs` und modifiziere `genossi_service/src/lib.rs`.

    **Datei 1: `genossi_service/src/assembly.rs`** — folge `genossi_service/src/application.rs` 1:1:

    ```rust
    use async_trait::async_trait;
    use genossi_dao::assembly::{AssemblyEntity, AssemblyStatus};
    use mockall::automock;
    use std::fmt::Debug;
    use std::sync::Arc;
    use uuid::Uuid;

    use crate::permission::Authentication;
    use crate::ServiceError;

    #[derive(Clone, Debug, PartialEq, Eq)]
    pub struct Assembly {
        pub id: Uuid,
        pub name: Arc<str>,
        pub date: time::PrimitiveDateTime,
        pub location: Option<Arc<str>>,
        pub status: AssemblyStatus,
        pub opened_at: Option<time::PrimitiveDateTime>,
        pub closed_at: Option<time::PrimitiveDateTime>,
        pub created: time::PrimitiveDateTime,
        pub deleted: Option<time::PrimitiveDateTime>,
        pub version: Uuid,
    }

    impl From<&AssemblyEntity> for Assembly {
        fn from(e: &AssemblyEntity) -> Self {
            Self {
                id: e.id,
                name: e.name.clone(),
                date: e.date,
                location: e.location.clone(),
                status: e.status.clone(),
                opened_at: e.opened_at,
                closed_at: e.closed_at,
                created: e.created,
                deleted: e.deleted,
                version: e.version,
            }
        }
    }

    impl From<&Assembly> for AssemblyEntity {
        fn from(a: &Assembly) -> Self {
            Self {
                id: a.id,
                name: a.name.clone(),
                date: a.date,
                location: a.location.clone(),
                status: a.status.clone(),
                opened_at: a.opened_at,
                closed_at: a.closed_at,
                created: a.created,
                deleted: a.deleted,
                version: a.version,
            }
        }
    }

    #[derive(Clone, Debug)]
    pub struct AssemblySubmission {
        pub name: Arc<str>,
        pub date: time::PrimitiveDateTime,
        pub location: Option<Arc<str>>,
    }

    #[derive(Clone, Debug)]
    pub struct AssemblyUpdate {
        pub name: Arc<str>,
        pub date: time::PrimitiveDateTime,
        pub location: Option<Arc<str>>,
        pub version: Uuid,
    }

    #[derive(Clone, Debug)]
    pub struct AssemblyDetail {
        pub assembly: Assembly,
        pub snapshot_member_count: u64,
    }

    #[automock(type Context=(); type Transaction = genossi_dao::MockTransaction;)]
    #[async_trait]
    pub trait AssemblyService {
        type Context: Clone + Debug + PartialEq + Eq + Send + Sync + 'static;
        type Transaction: genossi_dao::Transaction;

        async fn create_assembly(
            &self,
            submission: &AssemblySubmission,
            context: Authentication<Self::Context>,
        ) -> Result<Assembly, ServiceError>;

        async fn update_assembly(
            &self,
            id: Uuid,
            update: &AssemblyUpdate,
            context: Authentication<Self::Context>,
        ) -> Result<Assembly, ServiceError>;

        async fn open_assembly(
            &self,
            id: Uuid,
            context: Authentication<Self::Context>,
        ) -> Result<Assembly, ServiceError>;

        async fn close_assembly(
            &self,
            id: Uuid,
            context: Authentication<Self::Context>,
        ) -> Result<Assembly, ServiceError>;

        async fn get_assembly(
            &self,
            id: Uuid,
            context: Authentication<Self::Context>,
        ) -> Result<AssemblyDetail, ServiceError>;

        async fn get_all_assemblies(
            &self,
            context: Authentication<Self::Context>,
        ) -> Result<Arc<[Assembly]>, ServiceError>;
    }
    ```

    Tests in `#[cfg(test)] mod tests`:
    1. `test_assembly_from_entity_roundtrip`: erzeuge AssemblyEntity, konvertiere zu Assembly, dann zurueck zu AssemblyEntity, asserte Felder gleich
    2. `test_mock_assembly_service_compiles`: `let _: MockAssemblyService = MockAssemblyService::new();` (compile-only)

    **Datei 2: `genossi_service/src/lib.rs`** — fuege `pub mod assembly;` alphabetisch zwischen `application` und `audit_log` ein.
  </action>
  <verify>
    <automated>cargo test -p genossi_service assembly 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p genossi_service` exit 0
    - `cargo test -p genossi_service assembly` exit 0 mit ≥ 2 Tests
    - `grep -c "pub trait AssemblyService" genossi_service/src/assembly.rs` == 1
    - `grep -c "async fn create_assembly" genossi_service/src/assembly.rs` ≥ 1
    - `grep -c "async fn open_assembly" genossi_service/src/assembly.rs` ≥ 1
    - `grep -c "async fn close_assembly" genossi_service/src/assembly.rs` ≥ 1
    - `grep -c "async fn update_assembly" genossi_service/src/assembly.rs` ≥ 1
    - `grep -c "async fn get_assembly" genossi_service/src/assembly.rs` ≥ 1
    - `grep -c "async fn get_all_assemblies" genossi_service/src/assembly.rs` ≥ 1
    - `grep -c "pub struct AssemblyDetail" genossi_service/src/assembly.rs` == 1
    - `grep -c "pub struct AssemblySubmission" genossi_service/src/assembly.rs` == 1
    - `grep -c "pub struct AssemblyUpdate" genossi_service/src/assembly.rs` == 1
    - `grep -c "pub mod assembly" genossi_service/src/lib.rs` == 1
    - `grep -c "snapshot_member_count: u64" genossi_service/src/assembly.rs` == 1
  </acceptance_criteria>
  <done>
    Trait + DTOs definiert, `MockAssemblyService` automatisch generiert, alle From-Impls bidirektional, lib.rs aktualisiert.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AssemblyServiceImpl mit Lifecycle-Logik, atomarer Open-Tx, Audit-Macros</name>
  <files>
    genossi_service_impl/src/assembly.rs,
    genossi_service_impl/src/lib.rs
  </files>
  <read_first>
    - genossi_service_impl/src/application.rs (gesamte Datei — Vorlage fuer gen_service_impl!, audit_macros, permission_check, Status-Guard, transaction_dao-Pattern)
    - genossi_service_impl/src/audit_macros.rs (audited_create! / audited_update! Signatur)
    - genossi_service_impl/src/macros.rs (gen_service_impl! Macro)
    - genossi_service/src/assembly.rs (Task 1, fuer Trait + DTOs)
    - genossi_dao/src/assembly.rs (AssemblyEntity)
    - genossi_dao/src/assembly_member_snapshot.rs (AssemblyMemberSnapshotEntity)
    - genossi_dao/src/member.rs:170-185 (count_active filter — exakte Logik fuer D-02 Snapshot-Befuellung)
    - .planning/phases/01-assembly-aggregat-audit-hardening/01-RESEARCH.md (§6 gen_service_impl!-Block, §10 Open-Atomicity, §8 State-Transition-Guards)
    - .planning/phases/01-assembly-aggregat-audit-hardening/01-PATTERNS.md (Pattern fuer service_impl/assembly.rs, Pitfall 1+2+3+6)
    - .planning/phases/01-assembly-aggregat-audit-hardening/01-CONTEXT.md (D-02, D-07, D-08, D-09, D-11, D-14)
  </read_first>
  <behavior>
    - `AssemblyServiceImpl` ist via `gen_service_impl!` generiert mit Deps: AssemblyDao, AssemblyMemberSnapshotDao, MemberDao, AuditLogDao, PermissionService, UuidService, TransactionDao
    - `create_assembly`: oeffnet Tx, prueft permission "admin", erzeugt AssemblyEntity mit status=Preparation, opened_at=None, closed_at=None, version=new_uuid, ruft `audited_create!(self, self.assembly_dao, &entity, "assembly.create", &user_id, tx)`, committet, returnt Assembly. Mock-Test: ein audited_create-Aufruf, ein commit
    - `open_assembly`: oeffnet Tx, prueft permission, laedt entity, Conflict wenn status != Preparation; setzt status=Open, opened_at=now; ruft audited_update! mit "assembly.open"; ruft member_dao.all(tx.clone()), filtert nach is_normal + exit_date, baut AssemblyMemberSnapshotEntity-Vec, ruft snapshot_dao.create_batch(...) ODER eine Schleife mit snapshot_dao.create; committet EINMAL am Ende
    - `close_assembly`: oeffnet Tx, prueft permission, laedt entity, Conflict wenn status != Open; setzt status=Closed, closed_at=now; ruft audited_update! mit "assembly.close"; committet
    - `close_assembly` ENTHAELT KEINEN HelperSession-Cascade-Code (D-09; Phase 3 wird das ergaenzen)
    - `update_assembly`: prueft Conflict wenn status != Preparation ODER entity.version != update.version; setzt name/date/location, ruft audited_update! mit "assembly.update"
    - `get_assembly`: laedt assembly, laedt snapshot_count via count_by_assembly_id, returnt AssemblyDetail
    - `get_all_assemblies`: laedt all() (filtert deleted IS NULL automatisch), returnt Arc<[Assembly]>
    - Alle 6 Methoden rufen `permission_service.check_permission("admin", context).await?` (D-14)
  </behavior>
  <action>
    Erstelle `genossi_service_impl/src/assembly.rs` und modifiziere `genossi_service_impl/src/lib.rs`.

    **Datei 1: `genossi_service_impl/src/assembly.rs`**:

    Header mit Imports und Process-Konstanten:
    ```rust
    use async_trait::async_trait;
    use genossi_dao::assembly::{AssemblyDao, AssemblyEntity, AssemblyStatus};
    use genossi_dao::assembly_member_snapshot::{AssemblyMemberSnapshotDao, AssemblyMemberSnapshotEntity};
    use genossi_dao::audit_log::AuditLogDao;
    use genossi_dao::member::MemberDao;
    use genossi_dao::TransactionDao;
    use genossi_service::assembly::{
        Assembly, AssemblyDetail, AssemblyService, AssemblySubmission, AssemblyUpdate,
    };
    use genossi_service::permission::{Authentication, PermissionService};
    use genossi_service::uuid_service::UuidService;
    use genossi_service::ServiceError;
    use std::sync::Arc;
    use uuid::Uuid;

    use crate::gen_service_impl;

    const ASSEMBLY_PROCESS_CREATE: &str = "assembly.create";
    const ASSEMBLY_PROCESS_OPEN: &str = "assembly.open";
    const ASSEMBLY_PROCESS_CLOSE: &str = "assembly.close";
    const ASSEMBLY_PROCESS_UPDATE: &str = "assembly.update";
    const ADMIN_PRIVILEGE: &str = "admin";
    ```

    `gen_service_impl!`-Block:
    ```rust
    gen_service_impl! {
        struct AssemblyServiceImpl: AssemblyService = AssemblyServiceDeps {
            AssemblyDao: AssemblyDao<Transaction = Self::Transaction> = assembly_dao,
            AssemblyMemberSnapshotDao: AssemblyMemberSnapshotDao<Transaction = Self::Transaction> = assembly_member_snapshot_dao,
            MemberDao: MemberDao<Transaction = Self::Transaction> = member_dao,
            AuditLogDao: AuditLogDao<Transaction = Self::Transaction> = audit_log_dao,
            PermissionService: PermissionService<Context = Self::Context> = permission_service,
            UuidService: UuidService = uuid_service,
            TransactionDao: TransactionDao<Transaction = Self::Transaction> = transaction_dao,
        }
    }
    ```

    `impl<Deps: AssemblyServiceDeps> AssemblyService for AssemblyServiceImpl<Deps>` mit allen 6 Methoden:

    1. **`create_assembly`** (per ASSY-01, D-11):
    ```rust
    async fn create_assembly(
        &self,
        submission: &AssemblySubmission,
        context: Authentication<Self::Context>,
    ) -> Result<Assembly, ServiceError> {
        let tx = self.transaction_dao.use_transaction(None).await?;

        let user_id = self.permission_service
            .current_user_id(context.clone())
            .await?
            .unwrap_or_else(|| "SYSTEM".to_string());
        self.permission_service.check_permission(ADMIN_PRIVILEGE, context).await?;

        let now = time::OffsetDateTime::now_utc();
        let created = time::PrimitiveDateTime::new(now.date(), now.time());

        let entity = AssemblyEntity {
            id: self.uuid_service.new_v4().await,
            name: submission.name.clone(),
            date: submission.date,
            location: submission.location.clone(),
            status: AssemblyStatus::Preparation,
            opened_at: None,
            closed_at: None,
            created,
            deleted: None,
            version: self.uuid_service.new_v4().await,
        };

        crate::audited_create!(self, self.assembly_dao, &entity, ASSEMBLY_PROCESS_CREATE, &user_id, tx);

        self.transaction_dao.commit(tx).await?;
        Ok(Assembly::from(&entity))
    }
    ```

    2. **`open_assembly`** (per ASSY-02, D-08, D-11, RESEARCH §10, Pitfall 2 — kritischer Punkt):
    ```rust
    async fn open_assembly(
        &self,
        id: Uuid,
        context: Authentication<Self::Context>,
    ) -> Result<Assembly, ServiceError> {
        let tx = self.transaction_dao.use_transaction(None).await?;

        let user_id = self.permission_service
            .current_user_id(context.clone())
            .await?
            .unwrap_or_else(|| "SYSTEM".to_string());
        self.permission_service.check_permission(ADMIN_PRIVILEGE, context).await?;

        let mut entity = self.assembly_dao.find_by_id(id, tx.clone()).await?
            .ok_or(ServiceError::EntityNotFound(id))?;
        if entity.status != AssemblyStatus::Preparation {
            return Err(ServiceError::Conflict(Arc::from(format!(
                "Cannot open assembly: status is '{}', expected 'Preparation'",
                entity.status.as_str()
            ))));
        }

        let now_offset = time::OffsetDateTime::now_utc();
        let now_pdt = time::PrimitiveDateTime::new(now_offset.date(), now_offset.time());
        let opened_date = now_offset.date();
        entity.status = AssemblyStatus::Open;
        entity.opened_at = Some(now_pdt);

        crate::audited_update!(
            self, self.assembly_dao, id, &entity,
            ASSEMBLY_PROCESS_OPEN, &user_id, tx
        );

        // count_active-Filter inline (D-02): identisch zu genossi_dao/src/member.rs:172-185
        // member_dao.all() filtert bereits deleted IS NULL.
        let all_members = self.member_dao.all(tx.clone()).await?;
        let snapshot_entities: Vec<AssemblyMemberSnapshotEntity> = all_members
            .iter()
            .filter(|m| m.status.is_normal())
            .filter(|m| m.exit_date.map_or(true, |d| d > opened_date))
            .map(|m| AssemblyMemberSnapshotEntity {
                assembly_id: id,
                member_id: m.id,
                captured_at: now_pdt,
            })
            .collect();

        // Snapshot-Inserts in derselben Tx (Pitfall 1: KEIN audited_create!).
        self.assembly_member_snapshot_dao
            .create_batch(&snapshot_entities, ASSEMBLY_PROCESS_OPEN, tx.clone())
            .await?;

        self.transaction_dao.commit(tx).await?;
        Ok(Assembly::from(&entity))
    }
    ```

    3. **`close_assembly`** (per ASSY-03, D-09, D-11):
    ```rust
    async fn close_assembly(
        &self,
        id: Uuid,
        context: Authentication<Self::Context>,
    ) -> Result<Assembly, ServiceError> {
        let tx = self.transaction_dao.use_transaction(None).await?;

        let user_id = self.permission_service
            .current_user_id(context.clone())
            .await?
            .unwrap_or_else(|| "SYSTEM".to_string());
        self.permission_service.check_permission(ADMIN_PRIVILEGE, context).await?;

        let mut entity = self.assembly_dao.find_by_id(id, tx.clone()).await?
            .ok_or(ServiceError::EntityNotFound(id))?;
        if entity.status != AssemblyStatus::Open {
            return Err(ServiceError::Conflict(Arc::from(format!(
                "Cannot close assembly: status is '{}', expected 'Open'",
                entity.status.as_str()
            ))));
        }

        let now_offset = time::OffsetDateTime::now_utc();
        let now_pdt = time::PrimitiveDateTime::new(now_offset.date(), now_offset.time());
        entity.status = AssemblyStatus::Closed;
        entity.closed_at = Some(now_pdt);

        crate::audited_update!(
            self, self.assembly_dao, id, &entity,
            ASSEMBLY_PROCESS_CLOSE, &user_id, tx
        );

        // KEINE Cascade-Invalidation (D-09; Phase 3 erweitert das).

        self.transaction_dao.commit(tx).await?;
        Ok(Assembly::from(&entity))
    }
    ```

    4. **`update_assembly`** (per D-07 status-Guard + Optimistic-Locking):
    ```rust
    async fn update_assembly(
        &self,
        id: Uuid,
        update: &AssemblyUpdate,
        context: Authentication<Self::Context>,
    ) -> Result<Assembly, ServiceError> {
        let tx = self.transaction_dao.use_transaction(None).await?;

        let user_id = self.permission_service
            .current_user_id(context.clone())
            .await?
            .unwrap_or_else(|| "SYSTEM".to_string());
        self.permission_service.check_permission(ADMIN_PRIVILEGE, context).await?;

        let mut entity = self.assembly_dao.find_by_id(id, tx.clone()).await?
            .ok_or(ServiceError::EntityNotFound(id))?;
        if entity.status != AssemblyStatus::Preparation {
            return Err(ServiceError::Conflict(Arc::from(format!(
                "Cannot update assembly: status is '{}', expected 'Preparation' (D-07)",
                entity.status.as_str()
            ))));
        }
        if entity.version != update.version {
            return Err(ServiceError::Conflict(Arc::from("Version mismatch")));
        }

        entity.name = update.name.clone();
        entity.date = update.date;
        entity.location = update.location.clone();

        crate::audited_update!(
            self, self.assembly_dao, id, &entity,
            ASSEMBLY_PROCESS_UPDATE, &user_id, tx
        );

        self.transaction_dao.commit(tx).await?;
        Ok(Assembly::from(&entity))
    }
    ```

    5. **`get_assembly`** (returnt AssemblyDetail mit snapshot_member_count):
    ```rust
    async fn get_assembly(
        &self,
        id: Uuid,
        context: Authentication<Self::Context>,
    ) -> Result<AssemblyDetail, ServiceError> {
        let tx = self.transaction_dao.use_transaction(None).await?;
        self.permission_service.check_permission(ADMIN_PRIVILEGE, context).await?;

        let entity = self.assembly_dao.find_by_id(id, tx.clone()).await?
            .ok_or(ServiceError::EntityNotFound(id))?;
        let snapshot_member_count = self.assembly_member_snapshot_dao
            .count_by_assembly_id(id, tx.clone())
            .await?;

        self.transaction_dao.commit(tx).await?;
        Ok(AssemblyDetail {
            assembly: Assembly::from(&entity),
            snapshot_member_count,
        })
    }
    ```

    6. **`get_all_assemblies`**:
    ```rust
    async fn get_all_assemblies(
        &self,
        context: Authentication<Self::Context>,
    ) -> Result<Arc<[Assembly]>, ServiceError> {
        let tx = self.transaction_dao.use_transaction(None).await?;
        self.permission_service.check_permission(ADMIN_PRIVILEGE, context).await?;

        let entities = self.assembly_dao.all(tx.clone()).await?;
        let assemblies: Arc<[Assembly]> = entities.iter().map(Assembly::from).collect();

        self.transaction_dao.commit(tx).await?;
        Ok(assemblies)
    }
    ```

    Tests in `#[cfg(test)] mod tests` mit Mocks (Mockall #[automock] der Plan-01-Traits + bestehender Permission/Transaction-Mocks):

    Vorlage: `genossi_service_impl/src/application.rs` letzter test-Block.

    Mindestens 5 Tests:
    1. `test_create_assembly_success`: MockAssemblyDao.expect_create() once, MockAuditLogDao.expect_get_latest_hash, expect_create_entries; MockPermissionService.expect_check_permission ok; MockTransactionDao.expect_use_transaction + expect_commit; assert returnt Assembly mit status==Preparation
    2. `test_open_assembly_from_preparation_succeeds_atomic`: setze Mock-AssemblyEntity mit status=Preparation; expect_update once, expect MemberDao.all returns 2 active members, expect snapshot_dao.create_batch once with 2 entities; expect commit once. Assert returnt Assembly mit status=Open, opened_at.is_some()
    3. `test_open_assembly_from_closed_returns_conflict`: setze MockAssemblyDao.expect_find_by_id mit Entity.status=Closed; assert ServiceError::Conflict
    4. `test_close_assembly_from_preparation_returns_conflict`: setze Entity.status=Preparation; assert ServiceError::Conflict
    5. `test_update_assembly_version_mismatch_returns_conflict`: Entity.version != update.version; assert ServiceError::Conflict mit "Version mismatch"
    6. (Bonus) `test_open_assembly_filters_inactive_members`: MockMemberDao.all returnt 3 Members — 1 Normal+aktive, 1 FehlerhaftErfasst, 1 Normal mit exit_date in der Vergangenheit; expect snapshot_dao.create_batch called with EXAKT 1 entity (Pitfall 6)

    **Datei 2: `genossi_service_impl/src/lib.rs`** — fuege `pub mod assembly;` alphabetisch zwischen `application` und `audit_log` ein (oder analog zur bestehenden mod-Reihenfolge).
  </action>
  <verify>
    <automated>cargo test -p genossi_service_impl assembly 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p genossi_service_impl` exit 0
    - `cargo test -p genossi_service_impl assembly` exit 0 mit ≥ 5 passing tests
    - `grep -c "ASSEMBLY_PROCESS_CREATE.*\"assembly.create\"" genossi_service_impl/src/assembly.rs` == 1
    - `grep -c "ASSEMBLY_PROCESS_OPEN.*\"assembly.open\"" genossi_service_impl/src/assembly.rs` == 1
    - `grep -c "ASSEMBLY_PROCESS_CLOSE.*\"assembly.close\"" genossi_service_impl/src/assembly.rs` == 1
    - `grep -c "ADMIN_PRIVILEGE.*\"admin\"" genossi_service_impl/src/assembly.rs` == 1
    - `grep -c "audited_create!" genossi_service_impl/src/assembly.rs` == 1 (nur in create_assembly)
    - `grep -c "audited_update!" genossi_service_impl/src/assembly.rs` == 3 (open, close, update)
    - `grep -c "use_transaction(None)" genossi_service_impl/src/assembly.rs` == 6 (eine pro Methode)
    - `grep -c "transaction_dao.commit" genossi_service_impl/src/assembly.rs` == 6 (eine pro Methode; in open EINE einzige)
    - In open_assembly-Block (zwischen `async fn open_assembly` und naechster `async fn`): `grep -c "use_transaction"` == 1 UND `grep -c "transaction_dao.commit"` == 1 (Pitfall 2 — atomare Tx, ein Commit)
    - `grep -c "AssemblyStatus::Preparation" genossi_service_impl/src/assembly.rs` ≥ 4 (init + 2 guards + tests)
    - `grep -c "ServiceError::Conflict" genossi_service_impl/src/assembly.rs` ≥ 4
    - `grep -c "is_normal()" genossi_service_impl/src/assembly.rs` ≥ 1 (D-02 filter)
    - `grep -c "exit_date.map_or" genossi_service_impl/src/assembly.rs` ≥ 1 (D-02 filter)
    - `grep -c "snapshot_member_count" genossi_service_impl/src/assembly.rs` ≥ 1
    - `grep -c "assembly_member_snapshot_dao.*audited_" genossi_service_impl/src/assembly.rs` == 0 (Pitfall 1: Snapshot wird NICHT audited)
    - `grep -c "HelperSession\|helper_session" genossi_service_impl/src/assembly.rs` == 0 (D-09: Phase 3)
    - `grep -c "pub mod assembly" genossi_service_impl/src/lib.rs` == 1
  </acceptance_criteria>
  <done>
    AssemblyServiceImpl ist voll funktionsfaehig. Alle 6 Methoden compilieren, Lifecycle-Guards sind Test-belegt, Snapshot-Befuellung ist atomar in derselben Tx (Pitfall 2), count_active-Filter ist 1:1 aus member.rs uebernommen (Pitfall 6), Snapshot wird NICHT auditiert (Pitfall 1).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REST→Service | Authentication<Context> wird vom REST-Handler injiziert; Service prueft `permission_service.check_permission("admin", auth)` (D-14) |
| Service→Audit | Service ruft `audited_create!`/`audited_update!`-Macros; Macros bauen Audit-Entries und schreiben sie atomar mit den DAO-Mutationen in derselben Tx |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Elevation of Privilege | AssemblyService Methoden | mitigate | Jede der 6 Methoden ruft `permission_service.check_permission("admin", context).await?` (D-14); Test-Coverage indirekt via REST-Plan-04 |
| T-01-03-02 | Tampering | open_assembly Atomicity | mitigate | Single use_transaction(None) am Top, single commit am Ende, tx.clone() fuer alle Sub-Calls (Pitfall 2); verifiziert via test_open_assembly_from_preparation_succeeds_atomic |
| T-01-03-03 | Tampering | State-Transition-Bypass | mitigate | Status-Guard in open/close/update (Pitfall 3); ServiceError::Conflict bei illegalen Uebergaengen; verifiziert via test_*_returns_conflict |
| T-01-03-04 | Tampering | Optimistic-Locking auf update_assembly | mitigate | `if entity.version != update.version { return Conflict("Version mismatch") }`; verifiziert via test_update_assembly_version_mismatch_returns_conflict |
| T-01-03-05 | Repudiation | Audit-Hashchain-Bruch | mitigate | Audit-Macros (Pitfall 1); Snapshot-Inserts NICHT auditieren (kein Lifecycle-Event); E2E-Verify-Test in Plan 05 deckt das ab |
| T-01-03-06 | Information Disclosure | Snapshot-Filter Pitfall 6 | mitigate | count_active-Filter exakt aus member.rs:172-185 (deleted IS NULL via member_dao.all + is_normal + exit_date > opened_date); test_open_assembly_filters_inactive_members verifiziert die drei Filter-Komponenten |
</threat_model>

<verification>
- `cargo build --workspace` gruen
- `cargo test -p genossi_service assembly` gruen
- `cargo test -p genossi_service_impl assembly` gruen mit ≥ 5 Tests (incl. Lifecycle-Conflict-Tests)
- `cargo clippy --all-targets --all-features` ohne neue Warnings
- `cargo fmt --check` gruen
- Keine Regressionen in bestehenden Service-Tests (Member, Application, MemberAction)
</verification>

<success_criteria>
- AssemblyService Trait + DTOs in genossi_service::assembly verfuegbar
- AssemblyServiceImpl mit allen 6 Methoden in genossi_service_impl::assembly
- Lifecycle-Guards (D-07) verifizieren die linearen Uebergaenge Preparation→Open→Closed
- Optimistic-Locking auf update_assembly (RESEARCH Open Q2)
- Atomare Open-Tx mit Snapshot-Befuellung in derselben Transaction (D-08, Pitfall 2)
- Audit-Macros mit korrekten Process-Identifiern: "assembly.create", "assembly.open", "assembly.close", "assembly.update" (D-11)
- Snapshot-DAO-Aufrufe verwenden NICHT audited_create! (Pitfall 1)
- KEIN HelperSession-Code in close_assembly (D-09)
- Permission-Check "admin" in jeder Methode (D-14)
- ≥ 5 Mock-basierte Unit-Tests gruen
</success_criteria>

<output>
Erstelle nach Abschluss `.planning/phases/01-assembly-aggregat-audit-hardening/01-03-SUMMARY.md` mit Service-Methoden-Liste, Test-Coverage und Hinweis fuer Plan 04 (REST-Konsumenten).
</output>
