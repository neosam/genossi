---
phase: 01-assembly-aggregat-audit-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - genossi_rest_types/src/lib.rs
autonomous: true
requirements: [ASSY-01, ASSY-02, ASSY-03, ASSY-05]
requirements_addressed: [ASSY-01, ASSY-02, ASSY-03, ASSY-05]
must_haves:
  truths:
    - "AssemblyStatusTO Enum mit Varianten Preparation, Open, Closed (D-17, englische Varianten)"
    - "AssemblyTO mit allen Feldern aus AssemblyEntity, Datetime-Felder via iso8601_datetime serde-Modul (D-05)"
    - "AssemblyDetailTO enthaelt assembly: AssemblyTO + snapshot_member_count: u64 (D-04 ad-hoc COUNT, RESEARCH §6 Open Q1)"
    - "CreateAssemblyRequest hat name, date, location (per AssemblySubmission)"
    - "UpdateAssemblyRequest hat name, date, location, version (Optimistic-Locking, RESEARCH Open Q2)"
    - "Bidirektionale From-Impls zwischen AssemblyStatus (genossi_dao) <-> AssemblyStatusTO"
    - "From<&genossi_service::assembly::Assembly> for AssemblyTO existiert"
    - "Alle TOs haben #[derive(ToSchema)] fuer OpenAPI"
  artifacts:
    - path: genossi_rest_types/src/lib.rs
      provides: "AssemblyStatusTO, AssemblyTO, AssemblyDetailTO, CreateAssemblyRequest, UpdateAssemblyRequest mit ToSchema + Serde"
      exports: ["AssemblyStatusTO", "AssemblyTO", "AssemblyDetailTO", "CreateAssemblyRequest", "UpdateAssemblyRequest"]
  key_links:
    - from: "AssemblyTO"
      to: "iso8601_datetime serde-Modul"
      via: "#[serde(serialize_with/deserialize_with)] auf date/opened_at/closed_at/created/deleted"
      pattern: 'serialize_with = "iso8601_datetime::serialize"'
    - from: "AssemblyStatus (genossi_dao)"
      to: "AssemblyStatusTO"
      via: "From-Impl in beide Richtungen"
      pattern: "impl From<&AssemblyStatus> for AssemblyStatusTO"
---

<objective>
REST-Types fuer das Assembly-Aggregat: Status-TO, Entity-TO, Detail-TO, Request-DTOs. Wird in Wave 2 von REST-Handlern (Plan 04) und Service-DTOs (indirekt ueber Plan 03) konsumiert.

Purpose: Klar getrennte Wire-Format-Schicht mit OpenAPI-Schema-Generierung; ISO8601-Datetime-Roundtrip; bidirektionale Domain-TO-Mappings.
Output: Erweiterte `genossi_rest_types/src/lib.rs` mit fuenf neuen Public-Types und allen From-Impls. Plan ist parallel zu Plan 01 ausfuehrbar (kein gemeinsames File).
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-assembly-aggregat-audit-hardening/01-CONTEXT.md
@.planning/phases/01-assembly-aggregat-audit-hardening/01-RESEARCH.md
@.planning/phases/01-assembly-aggregat-audit-hardening/01-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@genossi_rest_types/src/lib.rs

<interfaces>
<!-- Vorlage: ApplicationStatusTO + ApplicationTO Block in genossi_rest_types/src/lib.rs (Zeilen ~804-1000). -->
<!-- WICHTIG: Plan 02 erwartet, dass `genossi_dao::assembly::AssemblyStatus` aus Plan 01 verfuegbar ist, ABER Plan 02 darf parallel zu Plan 01 ausgefuehrt werden, weil files_modified disjunkt sind. -->
<!-- Falls Plan 01 zum Build-Zeitpunkt noch nicht in den DAO-Crate gemerged ist, muss der Executor warten / koordinieren. Wave-1-Ausfuehrung garantiert beides parallel. -->

From genossi_rest_types/src/lib.rs (existing pattern):
```rust
pub mod iso8601_datetime {
    pub fn serialize<S: serde::Serializer>(...) -> Result<S::Ok, S::Error>;
    pub fn deserialize<'de, D: serde::Deserializer<'de>>(...) -> Result<Option<time::PrimitiveDateTime>, D::Error>;
}

#[derive(Clone, Debug, PartialEq, Eq, Serialize, Deserialize, ToSchema)]
pub enum ApplicationStatusTO { Offen, Bestaetigt, Abgelehnt }
// Vorlage fuer AssemblyStatusTO

#[derive(Clone, Debug, Serialize, Deserialize, ToSchema)]
pub struct ApplicationTO {
    pub id: Uuid,
    pub first_name: String,
    /* ... */
    #[serde(
        serialize_with = "iso8601_datetime::serialize",
        deserialize_with = "iso8601_datetime::deserialize",
        default
    )]
    pub created: Option<time::PrimitiveDateTime>,
}
// Vorlage fuer AssemblyTO
```

Erwartete Schnittstelle aus Plan 01 (genossi_dao):
```rust
// genossi_dao::assembly
pub enum AssemblyStatus { Preparation, Open, Closed }
pub struct AssemblyEntity {
    pub id: Uuid,
    pub name: Arc<str>,
    pub date: time::PrimitiveDateTime,
    pub location: Option<Arc<str>>,
    pub status: AssemblyStatus,
    pub opened_at: Option<time::PrimitiveDateTime>,
    pub closed_at: Option<time::PrimitiveDateTime>,
    pub created: time::PrimitiveDateTime,
    pub deleted: Option<time::PrimitiveDateTime>,
    pub version: Uuid,
}
```

Erwartete Schnittstelle aus Plan 03 (genossi_service):
```rust
// genossi_service::assembly
pub struct Assembly { /* gleiche Felder wie AssemblyEntity */ }
pub struct AssemblyDetail { pub assembly: Assembly, pub snapshot_member_count: u64 }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AssemblyStatusTO + AssemblyTO + AssemblyDetailTO mit ISO8601-Serde</name>
  <files>genossi_rest_types/src/lib.rs</files>
  <read_first>
    - genossi_rest_types/src/lib.rs (gesamte Datei zwischen Zeilen ~804-1000 — ApplicationStatusTO + ApplicationTO Block ist 1:1 die Vorlage; iso8601_datetime-Modul oben)
    - .planning/phases/01-assembly-aggregat-audit-hardening/01-RESEARCH.md (§2 Status-Enum, §6 Service-Shape, RESEARCH Open Q1 Snapshot-Count statt Liste)
    - .planning/phases/01-assembly-aggregat-audit-hardening/01-PATTERNS.md (Pattern fuer rest_types/src/lib.rs)
    - .planning/phases/01-assembly-aggregat-audit-hardening/01-CONTEXT.md (D-04, D-05, D-13, D-17)
  </read_first>
  <behavior>
    - `AssemblyStatusTO` hat exakt 3 Varianten: Preparation, Open, Closed
    - `AssemblyStatusTO` ist Serialize+Deserialize+ToSchema; serialisiert zu JSON-Strings "Preparation"/"Open"/"Closed"
    - `From<&AssemblyStatus>` fuer AssemblyStatusTO und Reverse-Direction kompilieren und liefern erwartete Mapping
    - `AssemblyTO` hat genau 10 oeffentliche Felder: id, name, date, location, status, opened_at, closed_at, created, deleted, version
    - `AssemblyTO` deserialisiert ohne Fehler, wenn `date/opened_at/closed_at/created/deleted` im JSON fehlen (Optional + #[serde(default)])
    - `AssemblyDetailTO` enthaelt `assembly: AssemblyTO` und `snapshot_member_count: u64`
    - `From<&genossi_service::assembly::Assembly>` fuer AssemblyTO uebernimmt alle Felder mit korrektem Wrap (Some(...) fuer date/created)
  </behavior>
  <action>
    Modifiziere `genossi_rest_types/src/lib.rs`. Fuege nach dem ApplicationTO-Block (oder thematisch passend, alphabetisch nach `Application*` sortiert) folgende Public-Types hinzu — alle in der Krate-Toplevel und alle mit `#[derive(ToSchema)]`:

    ```rust
    #[derive(Clone, Debug, PartialEq, Eq, Serialize, Deserialize, ToSchema)]
    pub enum AssemblyStatusTO {
        Preparation,
        Open,
        Closed,
    }

    impl From<&genossi_dao::assembly::AssemblyStatus> for AssemblyStatusTO {
        fn from(s: &genossi_dao::assembly::AssemblyStatus) -> Self {
            use genossi_dao::assembly::AssemblyStatus;
            match s {
                AssemblyStatus::Preparation => AssemblyStatusTO::Preparation,
                AssemblyStatus::Open => AssemblyStatusTO::Open,
                AssemblyStatus::Closed => AssemblyStatusTO::Closed,
            }
        }
    }

    impl From<&AssemblyStatusTO> for genossi_dao::assembly::AssemblyStatus {
        fn from(s: &AssemblyStatusTO) -> Self {
            use genossi_dao::assembly::AssemblyStatus;
            match s {
                AssemblyStatusTO::Preparation => AssemblyStatus::Preparation,
                AssemblyStatusTO::Open => AssemblyStatus::Open,
                AssemblyStatusTO::Closed => AssemblyStatus::Closed,
            }
        }
    }

    #[derive(Clone, Debug, Serialize, Deserialize, ToSchema)]
    pub struct AssemblyTO {
        #[schema(example = "123e4567-e89b-12d3-a456-426614174000")]
        pub id: Uuid,
        #[schema(example = "GV 2026")]
        pub name: String,
        #[serde(
            serialize_with = "iso8601_datetime::serialize",
            deserialize_with = "iso8601_datetime::deserialize",
            default
        )]
        pub date: Option<time::PrimitiveDateTime>,
        pub location: Option<String>,
        pub status: AssemblyStatusTO,
        #[serde(
            serialize_with = "iso8601_datetime::serialize",
            deserialize_with = "iso8601_datetime::deserialize",
            default
        )]
        pub opened_at: Option<time::PrimitiveDateTime>,
        #[serde(
            serialize_with = "iso8601_datetime::serialize",
            deserialize_with = "iso8601_datetime::deserialize",
            default
        )]
        pub closed_at: Option<time::PrimitiveDateTime>,
        #[serde(
            serialize_with = "iso8601_datetime::serialize",
            deserialize_with = "iso8601_datetime::deserialize",
            default
        )]
        pub created: Option<time::PrimitiveDateTime>,
        #[serde(
            serialize_with = "iso8601_datetime::serialize",
            deserialize_with = "iso8601_datetime::deserialize",
            default
        )]
        pub deleted: Option<time::PrimitiveDateTime>,
        pub version: Option<Uuid>,
    }

    impl From<&genossi_service::assembly::Assembly> for AssemblyTO {
        fn from(a: &genossi_service::assembly::Assembly) -> Self {
            Self {
                id: a.id,
                name: a.name.to_string(),
                date: Some(a.date),
                location: a.location.as_ref().map(|s| s.to_string()),
                status: AssemblyStatusTO::from(&a.status),
                opened_at: a.opened_at,
                closed_at: a.closed_at,
                created: Some(a.created),
                deleted: a.deleted,
                version: Some(a.version),
            }
        }
    }

    #[derive(Clone, Debug, Serialize, Deserialize, ToSchema)]
    pub struct AssemblyDetailTO {
        pub assembly: AssemblyTO,
        pub snapshot_member_count: u64,
    }

    impl From<&genossi_service::assembly::AssemblyDetail> for AssemblyDetailTO {
        fn from(d: &genossi_service::assembly::AssemblyDetail) -> Self {
            Self {
                assembly: AssemblyTO::from(&d.assembly),
                snapshot_member_count: d.snapshot_member_count,
            }
        }
    }
    ```

    Tests in `#[cfg(test)] mod tests` (am Datei-Ende oder im bestehenden Block):
    1. `test_assembly_status_to_serialize`: `serde_json::to_string(&AssemblyStatusTO::Preparation).unwrap() == "\"Preparation\""` (genauso fuer Open, Closed)
    2. `test_assembly_status_to_deserialize`: `serde_json::from_str::<AssemblyStatusTO>("\"Open\"").unwrap() == AssemblyStatusTO::Open`
    3. `test_assembly_to_optional_dates_default`: JSON `{"id":"...", "name":"GV", "status":"Preparation"}` deserialisiert zu AssemblyTO mit date=None, opened_at=None, closed_at=None, created=None, deleted=None, version=None
    4. `test_status_bidirectional_mapping`: `AssemblyStatus::from(&AssemblyStatusTO::from(&AssemblyStatus::Open)) == AssemblyStatus::Open` fuer alle drei Varianten

    Hinweis: Falls die genossi_rest_types-Crate bislang keine Dependency auf `genossi_service` hat, MUSS sie ergaenzt werden, weil `From<&Assembly>` darauf zugreift. Pruefe `genossi_rest_types/Cargo.toml` und ergaenze `genossi_service = { path = "../genossi_service" }` plus `genossi_dao = { path = "../genossi_dao" }`, falls die Dependencies fehlen — analog zur ApplicationTO-Vorlage.
  </action>
  <verify>
    <automated>cargo test -p genossi_rest_types assembly 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p genossi_rest_types` exit 0
    - `cargo test -p genossi_rest_types assembly` exit 0 mit ≥ 4 passing tests
    - `grep -c "pub enum AssemblyStatusTO" genossi_rest_types/src/lib.rs` == 1
    - `grep -c "Preparation" genossi_rest_types/src/lib.rs | grep -E '^[1-9]'` (≥ 4 Vorkommen: enum-Variante, From-Impl x2, Tests)
    - `grep -c "pub struct AssemblyTO" genossi_rest_types/src/lib.rs` == 1
    - `grep -c "pub struct AssemblyDetailTO" genossi_rest_types/src/lib.rs` == 1
    - `grep -c "snapshot_member_count: u64" genossi_rest_types/src/lib.rs` == 1
    - `grep -c "impl From<&genossi_dao::assembly::AssemblyStatus> for AssemblyStatusTO" genossi_rest_types/src/lib.rs` == 1
    - `grep -c "impl From<&AssemblyStatusTO> for genossi_dao::assembly::AssemblyStatus" genossi_rest_types/src/lib.rs` == 1
    - `grep -c "impl From<&genossi_service::assembly::Assembly> for AssemblyTO" genossi_rest_types/src/lib.rs` == 1
    - Anzahl `iso8601_datetime::serialize` Direktiven im AssemblyTO-Block: ≥ 5 (date, opened_at, closed_at, created, deleted)
  </acceptance_criteria>
  <done>
    Alle 5 neuen Types existieren mit ToSchema-Derive. Beide From-Richtungen Status zwischen DAO und TO kompilieren. AssemblyTO und AssemblyDetailTO haben From-Impls von Service-Domain-Typen. Alle 4 Unit-Tests gruen.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CreateAssemblyRequest und UpdateAssemblyRequest</name>
  <files>genossi_rest_types/src/lib.rs</files>
  <read_first>
    - genossi_rest_types/src/lib.rs (Zeilen mit `AdminCreateApplicationRequest` und `UpdateApplicationRequest` als Vorlagen)
    - .planning/phases/01-assembly-aggregat-audit-hardening/01-RESEARCH.md (§6 AssemblySubmission/AssemblyUpdate, Open Q2 Optimistic-Locking auf Update)
    - .planning/phases/01-assembly-aggregat-audit-hardening/01-CONTEXT.md (D-05, D-07)
  </read_first>
  <behavior>
    - `CreateAssemblyRequest` hat genau 3 Felder: name (String), date (Option<PrimitiveDateTime> via iso8601_datetime), location (Option<String>)
    - `UpdateAssemblyRequest` hat genau 4 Felder: name, date, location, version (Uuid; per RESEARCH Open Q2 — Optimistic-Locking Pflicht)
    - Beide sind Deserialize+ToSchema; serialisieren ist nicht strikt notwendig, aber Serialize wird mit dem Pattern uebernommen
    - JSON-Roundtrip liefert dieselben Werte zurueck
  </behavior>
  <action>
    Fuege im selben File `genossi_rest_types/src/lib.rs` (am AssemblyTO-Block angrenzend) folgende DTOs hinzu:

    ```rust
    #[derive(Clone, Debug, Serialize, Deserialize, ToSchema)]
    pub struct CreateAssemblyRequest {
        #[schema(example = "GV 2026")]
        pub name: String,
        #[serde(
            serialize_with = "iso8601_datetime::serialize",
            deserialize_with = "iso8601_datetime::deserialize",
            default
        )]
        pub date: Option<time::PrimitiveDateTime>,
        pub location: Option<String>,
    }

    #[derive(Clone, Debug, Serialize, Deserialize, ToSchema)]
    pub struct UpdateAssemblyRequest {
        #[schema(example = "GV 2026")]
        pub name: String,
        #[serde(
            serialize_with = "iso8601_datetime::serialize",
            deserialize_with = "iso8601_datetime::deserialize",
            default
        )]
        pub date: Option<time::PrimitiveDateTime>,
        pub location: Option<String>,
        pub version: Uuid,
    }
    ```

    Tests in `#[cfg(test)] mod tests`:
    1. `test_create_assembly_request_minimal_json`: `serde_json::from_str::<CreateAssemblyRequest>("{\"name\": \"GV 2026\"}").unwrap()` ergibt name="GV 2026", date=None, location=None
    2. `test_create_assembly_request_full_json`: parse JSON `{"name":"GV","date":"2026-06-15T18:00:00.000000000","location":"Vereinsheim"}` ohne Fehler
    3. `test_update_assembly_request_requires_version`: parse JSON ohne `version`-Feld -> Err (serde-deserialization-Fehler, weil version: Uuid keine #[serde(default)])
  </action>
  <verify>
    <automated>cargo test -p genossi_rest_types -- assembly_request 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub struct CreateAssemblyRequest" genossi_rest_types/src/lib.rs` == 1
    - `grep -c "pub struct UpdateAssemblyRequest" genossi_rest_types/src/lib.rs` == 1
    - `grep -c "pub version: Uuid" genossi_rest_types/src/lib.rs | grep -E '^[1-9]'` (≥ 1 Vorkommen — UpdateAssemblyRequest)
    - `cargo build -p genossi_rest_types` exit 0
    - `cargo test -p genossi_rest_types assembly_request` exit 0 mit ≥ 3 Tests
    - Alle Datetime-Felder in Create/Update haben das `iso8601_datetime`-Serde-Attribut: `grep -B1 "pub date: Option<time::PrimitiveDateTime>" genossi_rest_types/src/lib.rs` zeigt `iso8601_datetime`
  </acceptance_criteria>
  <done>
    Beide Request-DTOs existieren, kompilieren mit ToSchema, Tests bestaetigen JSON-Roundtrip-Verhalten. `UpdateAssemblyRequest` enforced Version-Feld via fehlendem #[serde(default)].
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→REST→TO | TOs sind die Wire-Format-Schicht; serde-Validation findet beim Parsen statt. UpdateAssemblyRequest erzwingt `version` (Uuid), CreateAssemblyRequest erzwingt `name` (String — nicht-leer-Check ist im Service-Layer). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | UpdateAssemblyRequest::version | mitigate | `version: Uuid` ohne #[serde(default)]; fehlendes Feld ⇒ serde-Deserialize-Fehler ⇒ HTTP 422 |
| T-01-02-02 | Information Disclosure | AssemblyTO Wire-Format | accept | TO enthaelt nur Lifecycle-Daten (name, date, location, status, timestamps); keine PII |
| T-01-02-03 | Information Disclosure | AssemblyDetailTO snapshot_member_count | accept | Nur die Zahl Y wird exponiert, nicht die member_ids — kein Datenschutz-Issue |
</threat_model>

<verification>
- `cargo build -p genossi_rest_types` gruen
- `cargo test -p genossi_rest_types` gruen mit allen neuen + bestehenden Tests
- `cargo clippy --all-targets --all-features` ohne neue Warnings im rest_types-Crate
- `cargo fmt --check` gruen
- OpenAPI-Schema-Generierung in spaeterem Plan 04 wird die ToSchema-Derives konsumieren
</verification>

<success_criteria>
- 5 neue Public-Types in `genossi_rest_types/src/lib.rs`: AssemblyStatusTO, AssemblyTO, AssemblyDetailTO, CreateAssemblyRequest, UpdateAssemblyRequest
- Bidirektionale From-Impls zwischen `genossi_dao::assembly::AssemblyStatus` und `AssemblyStatusTO`
- `From<&genossi_service::assembly::Assembly> for AssemblyTO` und `From<&AssemblyDetail> for AssemblyDetailTO` kompilieren
- ISO8601-Datetime-Serde auf allen Datetime-Feldern (date, opened_at, closed_at, created, deleted)
- ToSchema-Derive auf allen 5 Types
- ≥ 7 Unit-Tests gruen (Status-Roundtrip, Status-Mapping, optional dates, Create-minimal, Create-full, Update-requires-version)
</success_criteria>

<output>
Erstelle nach Abschluss `.planning/phases/01-assembly-aggregat-audit-hardening/01-02-SUMMARY.md` mit den realisierten Types und Test-Resultaten.
</output>
