---
phase: 02-helfer-token-session-authcontext-helper
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - genossi_service_impl/Cargo.toml
autonomous: true
requirements: [HLPR-01]
must_haves:
  truths:
    - "qrcode 0.14 ist als Workspace-Dependency in Cargo.toml registriert (D-13)"
    - "rand 0.8 mit features [std, std_rng, getrandom] ist als Workspace-Dependency registriert (D-10, RESEARCH §Standard Stack)"
    - "genossi_service_impl/Cargo.toml deklariert qrcode + rand via { workspace = true }"
    - "sha2 0.10 ist bereits in genossi_service_impl/Cargo.toml:26 (no-op, RESEARCH bestätigt: schon im Stack)"
    - "cargo build --workspace exit 0 nach Dependency-Install"
    - "cargo metadata zeigt qrcode-Version 0.14.x und rand-Version 0.8.x"
  artifacts:
    - path: Cargo.toml
      provides: "Workspace-Dependency-Eintraege für qrcode und rand (D-13)"
    - path: genossi_service_impl/Cargo.toml
      provides: "Verbrauch der neuen Dependencies via { workspace = true } für die Service-Layer-Verwendung in Plan 05"
  key_links:
    - from: "Cargo.toml [workspace.dependencies]"
      to: "qrcode 0.14"
      via: "qrcode = \"0.14\""
      pattern: 'qrcode = "0\.14"'
    - from: "Cargo.toml [workspace.dependencies]"
      to: "rand 0.8 mit getrandom"
      via: "rand = { version = \"0.8\", default-features = false, features = [\"std\", \"std_rng\", \"getrandom\"] }"
      pattern: 'rand = \{ version = "0\.8"'
---

<objective>
Workspace-Dependency-Setup für Phase 2. Zwei neue Crates (`qrcode` 0.14 für SVG-QR-Render, `rand` 0.8 für `OsRng` Crockford-Base32-Generierung) werden zentral in `Cargo.toml` deklariert und in `genossi_service_impl/Cargo.toml` konsumiert. Plan 05 (Service-Impl) verbraucht beide.

Purpose: `cargo build --workspace` muss nach Plan 05 grün sein; ohne diese Deps schlägt Plan 05 fehl. Da Plan 03 keine semantische Logik einführt (nur Dependency-Wiring), kann er parallel zu Plan 01 + 02 in Wave 1 laufen.

Output: Beide Cargo-Files aktualisiert, `Cargo.lock` regeneriert, Build grün.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-helfer-token-session-authcontext-helper/02-CONTEXT.md
@.planning/phases/02-helfer-token-session-authcontext-helper/02-RESEARCH.md
@.planning/phases/02-helfer-token-session-authcontext-helper/02-PATTERNS.md
@CLAUDE.md
@Cargo.toml
@genossi_service_impl/Cargo.toml

<interfaces>
<!-- Aktueller Cargo.toml-Stand (Zeilen 23-48): -->
```toml
[workspace.dependencies]
async-trait = "0.1"
axum = { version = "0.8.3", features = ["multipart"] }
mockall = "0.13"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sqlx = { version = "0.8", features = ["runtime-tokio", "sqlite", "time"] }
time = { version = "0.3", features = ["serde", "formatting", "parsing"] }
tokio = { version = "1.35", features = ["full"] }
# ... weitere bestehende Deps
```

<!-- Aktueller genossi_service_impl/Cargo.toml-Stand (Zeile 26): -->
```toml
sha2 = "0.10"   # bereits vorhanden — D-11 nutzt es ohne Änderung
```

<!-- Phase-2-Anforderung (RESEARCH §Standard Stack — Supporting): -->
- qrcode 0.14.1 — pure-Rust QR-Encoder mit `render::svg`-Modul; MIT/Apache; verifiziert via docs.rs/qrcode/0.14.1
- rand 0.8 — bereits transitiv im Lockfile (0.8.5 + 0.9.4) via ring/uuid; direkter Workspace-Eintrag verhindert 0.9-Duplikat im Workspace-Graph
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Workspace + Service-Impl Cargo.toml Dependency-Wiring</name>
  <files>
    Cargo.toml,
    genossi_service_impl/Cargo.toml
  </files>
  <read_first>
    - Cargo.toml (gesamte Datei — 48 Zeilen, [workspace.dependencies] block 23-48)
    - genossi_service_impl/Cargo.toml (gesamte Datei — bestätigt sha2 ist bereits in Zeile 26)
    - .planning/phases/02-helfer-token-session-authcontext-helper/02-CONTEXT.md (D-10, D-13)
    - .planning/phases/02-helfer-token-session-authcontext-helper/02-RESEARCH.md (§Standard Stack: qrcode 0.14.1 verified, rand 0.8 default-features = false mit ["std", "std_rng", "getrandom"])
  </read_first>
  <behavior>
    - `[workspace.dependencies]` Block in Root-Cargo.toml hat zwei neue Einträge: `qrcode = "0.14"` und `rand = { version = "0.8", default-features = false, features = ["std", "std_rng", "getrandom"] }`
    - `genossi_service_impl/Cargo.toml` deklariert `qrcode = { workspace = true }` und `rand = { workspace = true }` im `[dependencies]` Block
    - `cargo build --workspace` exit 0
    - `cargo tree -p qrcode -p rand 2>&1` zeigt qrcode 0.14.x und rand 0.8.x als direkte Workspace-Deps
    - `Cargo.lock` enthält neue Einträge für qrcode und rand-direkt-Workspace
  </behavior>
  <action>
    Modifiziere die zwei Cargo-Files:

    **Datei 1: `Cargo.toml`** — füge im `[workspace.dependencies]`-Block (Zeilen 23-48) zwei neue Zeilen ein, alphabetisch sortiert. Konkret nach `mockall = "0.13"` (Zeile 26):

    ```toml
    [workspace.dependencies]
    async-trait = "0.1"
    axum = { version = "0.8.3", features = ["multipart"] }
    mockall = "0.13"
    qrcode = "0.14"                                           # NEU — Phase 2 D-13
    rand = { version = "0.8", default-features = false, features = ["std", "std_rng", "getrandom"] }   # NEU — Phase 2 D-10
    serde = { version = "1.0", features = ["derive"] }
    serde_json = "1.0"
    # ... rest unverändert
    ```

    Hinweis Sortier-Order: alphabetisch — `mockall` < `qrcode` < `rand` < `serde`. Falls die existierende Sortierung nicht streng alphabetisch ist (etwa weil `axum` schon nicht-alphabetisch positioniert ist), füge die zwei Zeilen nach `mockall` und vor `serde` ein, das ist die sicherste Position.

    **Datei 2: `genossi_service_impl/Cargo.toml`** — füge im `[dependencies]`-Block zwei neue Zeilen ein. Position: nach `sha2 = "0.10"` (Zeile 26) oder analog der bestehenden Sortierung:

    ```toml
    [dependencies]
    # ... existing deps ...
    sha2 = "0.10"
    qrcode = { workspace = true }     # NEU — Phase 2 D-13
    rand = { workspace = true }       # NEU — Phase 2 D-10
    # ... rest unverändert
    ```

    Hinweis: Wenn der existierende `[dependencies]`-Block einen anderen Sortierstil verwendet (z.B. nach Nutzungs-Kategorie), füge die zwei Einträge in der pragmatisch sinnvollsten Position ein — wichtig ist, dass beide Einträge mit `{ workspace = true }` syntax verwenden, damit die Version aus der Root-Cargo.toml gezogen wird.

    KEINE Änderung an `genossi_service_impl/Cargo.toml` an `sha2 = "0.10"` (D-11 nutzt es unverändert, RESEARCH bestätigt).

    Nach den Edits: `cargo build --workspace` ausführen — Cargo wird `Cargo.lock` automatisch regenerieren mit den neuen Crate-Versionen. Falls `cargo build` einen anderen Fehler wirft (z.B. wegen Plan 01/02-Änderungen, die noch nicht gemerged sind), ist das okay — der Wave-1-parallele-Build wird erst nach allen drei Plans grün.
  </action>
  <verify>
    <automated>cargo metadata --format-version 1 2>/dev/null | python3 -c "import json,sys; d=json.load(sys.stdin); names={p['name'] for p in d['packages']}; print('qrcode',  'qrcode' in names); print('rand', 'rand' in names)" 2>&1 | tail -10 && cargo build --workspace 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^qrcode = "0\.14"' Cargo.toml` == 1
    - `grep -c '^rand = { version = "0\.8"' Cargo.toml` == 1
    - `grep -c 'features = \["std", "std_rng", "getrandom"\]' Cargo.toml` ≥ 1
    - `grep -c 'qrcode = { workspace = true }' genossi_service_impl/Cargo.toml` == 1
    - `grep -c 'rand = { workspace = true }' genossi_service_impl/Cargo.toml` == 1
    - `grep -c 'sha2 = "0\.10"' genossi_service_impl/Cargo.toml` == 1 (unverändert)
    - `cargo metadata --format-version 1 | grep -c '"name":"qrcode"'` ≥ 1
    - `cargo metadata --format-version 1 | grep -c '"name":"rand"'` ≥ 1
    - `cargo build --workspace 2>&1 | tail -1 | grep -c "error"` == 0 (Build muss erfolgreich sein, nachdem alle Wave-1-Plans gemerged sind)
    - `Cargo.lock` enthält Eintrag für `qrcode` (Version 0.14.x): `grep -A 1 'name = "qrcode"' Cargo.lock | grep -c 'version = "0.14'` ≥ 1
  </acceptance_criteria>
  <done>
    Beide Cargo-Files aktualisiert, Cargo.lock regeneriert, `cargo build --workspace` grün. Plan 05 kann die neuen Crates in `genossi_service_impl/src/helper_token.rs` importieren ohne weitere Cargo-Arbeit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workspace→External Crate | Externe Crates (qrcode 0.14, rand 0.8) werden in den Build-Graph eingebracht. Beide sind etablierte pure-Rust-Crates mit MIT/Apache-Lizenz; Audit-Trail über docs.rs/crates.io. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | qrcode crate supply-chain | accept | qrcode 0.14.1 ist de-facto-Standard im Rust-Ökosystem; Cargo-Lockfile pin verhindert silent-upgrades. cargo audit (manual oder CI) deckt CVEs zukünftig ab. |
| T-02-03-02 | Tampering | rand crate supply-chain | accept | rand 0.8 ist LTS und bereits transitiv im Lockfile via ring/uuid. Direkter Workspace-Eintrag stabilisiert nur den Graph, ändert nichts am Code. |
| T-02-03-03 | Information Disclosure | rand-OsRng leak via debug logs | mitigate | OsRng-Konsumption in Plan 05 darf den generierten Klartext NICHT loggen (siehe Plan 05 Anti-Pattern: kein tracing::debug auf code) |
</threat_model>

<verification>
- `cargo build --workspace` grün (vorausgesetzt Plan 01 + 02 sind merged — Wave-1-parallel)
- `cargo metadata` zeigt qrcode 0.14.x und rand 0.8.x als Workspace-Members
- `cargo clippy --workspace --all-targets` ohne neue Warnings
- `cargo fmt --check` grün
- Optionale Doku: `cargo tree -p genossi_service_impl 2>&1 | grep -E "qrcode|rand "` zeigt beide Crates als direkte Deps
</verification>

<success_criteria>
- `qrcode = "0.14"` in `Cargo.toml [workspace.dependencies]`
- `rand = { version = "0.8", default-features = false, features = ["std", "std_rng", "getrandom"] }` in `Cargo.toml [workspace.dependencies]`
- `qrcode = { workspace = true }` und `rand = { workspace = true }` in `genossi_service_impl/Cargo.toml [dependencies]`
- `Cargo.lock` aktualisiert, beide Crates in der Lock-Datei
- `cargo build --workspace` grün
- Plan 05 kann ohne weitere Cargo-Arbeit `use qrcode::{...}` und `use rand::{rngs::OsRng, RngCore}` verwenden
</success_criteria>

<output>
Erstelle nach Abschluss `.planning/phases/02-helfer-token-session-authcontext-helper/02-03-SUMMARY.md` mit:
- Konkrete neue Crate-Versionen (laut Cargo.lock)
- Bestätigung, dass `cargo build --workspace` grün ist
- Hinweis für Plan 05 zur Importierbarkeit der Crates
</output>
