---
phase: 06-teilnehmerlisten-export-f-r-generalversammlungen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - genossi_service_impl/Cargo.toml
  - genossi_bin/Cargo.toml
  - templates/teilnehmerliste.typ
autonomous: true
requirements:
  - D-02
  - D-04
  - D-08
  - D-10
tags:
  - export
  - typst
  - dependencies

must_haves:
  truths:
    - "rust_xlsxwriter ist als regulaere [dependencies] im genossi_service_impl Crate verfuegbar (D-02)"
    - "csv ist als regulaere [dependencies] im genossi_service_impl Crate verfuegbar (D-01/D-03 Vorbereitung)"
    - "PDF-Template `templates/teilnehmerliste.typ` existiert und nutzt _layout.typ als Wrapper (D-04)"
    - "Template-Header rendert X von Y bei `total != none`, sonst nur X anwesend (D-08, D-10)"
    - "Tabelle nutzt `table.header(repeat: true)` und 6 Spalten (Nr, Nachname, Vorname, Anrede, Titel, anwesend) (D-04, D-06)"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace dependency declarations for rust_xlsxwriter und csv"
      contains: "[workspace.dependencies]"
    - path: "genossi_service_impl/Cargo.toml"
      provides: "rust_xlsxwriter + csv als regulaere deps"
      contains: "rust_xlsxwriter"
    - path: "templates/teilnehmerliste.typ"
      provides: "Typst-Template fuer Teilnehmerliste mit Kopfblock + Tabelle"
      min_lines: 35
  key_links:
    - from: "templates/teilnehmerliste.typ"
      to: "templates/_layout.typ"
      via: "Typst-Import"
      pattern: "#import \"_layout.typ\": letter"
    - from: "genossi_service_impl/Cargo.toml"
      to: "Cargo.toml workspace.dependencies"
      via: "workspace = true"
      pattern: "rust_xlsxwriter = \\{ workspace = true \\}"
---

<objective>
Lege die zwei Build-Fundamente fuer Phase 6 — die Workspace-Dependency-Promotions
fuer `rust_xlsxwriter` und `csv` sowie das neue Typst-Template `teilnehmerliste.typ`
— ohne Code-Aenderungen in anderen Crates. Beide Outputs sind unabhaengig voneinander,
sodass Plan 02 (Service-Impl) anschliessend in einem Aufwasch beides nutzen kann.

Purpose: Plan 02 implementiert XLSX/CSV/PDF-Writer; ohne diese Vorarbeit waere jeder
Writer ein Bauch-Block bestehend aus Dependency-Diskussion + Code. Splitten reduziert
Plan-02-Kontext um ~10%.

Output:
- `Cargo.toml` mit neuen `[workspace.dependencies]` Eintraegen
- `genossi_service_impl/Cargo.toml` referenziert die neuen Workspace-Deps
- `genossi_bin/Cargo.toml` ist auf `{ workspace = true }` migriert (vorher hardcoded `"0.82"` und `"1.3"`)
- `templates/teilnehmerliste.typ` — produktives Typst-Template fuer Phase 2-Step
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-teilnehmerlisten-export-f-r-generalversammlungen/06-CONTEXT.md
@.planning/phases/06-teilnehmerlisten-export-f-r-generalversammlungen/06-RESEARCH.md
@.planning/phases/06-teilnehmerlisten-export-f-r-generalversammlungen/06-PATTERNS.md

<interfaces>
<!-- Existing layout helper that the new template MUST import -->

From templates/_layout.typ:
```typst
#let letter(
  title: none,
  date: none,
  content,
) = {
  set page(paper: "a4", margin: (top: 3cm, bottom: 2.5cm, left: 2.5cm, right: 2cm))
  set text(font: "Liberation Sans", size: 11pt, lang: "de")
  set par(leading: 0.8em, justify: true)
  if date != none { align(right)[#date]; v(1cm) }
  if title != none { text(size: 14pt, weight: "bold")[#title]; v(0.5cm) }
  content
}
```

<!-- Existing per-crate Cargo.toml state (verified via grep) -->

genossi_bin/Cargo.toml:58: `rust_xlsxwriter = "0.82"`  (in [dev-dependencies])
genossi_bin/Cargo.toml:60: `csv = "1.3"`  (in [dev-dependencies])
genossi_rest/Cargo.toml:30: `csv = "1.3"`  (in [dependencies])

The workspace root Cargo.toml does NOT yet declare either crate in
`[workspace.dependencies]` — this plan adds them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote `rust_xlsxwriter` and `csv` to workspace dependencies and consume them in genossi_service_impl</name>
  <files>Cargo.toml, genossi_service_impl/Cargo.toml, genossi_bin/Cargo.toml, genossi_rest/Cargo.toml</files>
  <read_first>
    - Cargo.toml (the workspace root — locate the `[workspace.dependencies]` block)
    - genossi_service_impl/Cargo.toml (target consumer — see what other workspace = true deps look like)
    - genossi_bin/Cargo.toml (lines 56-62 — the current dev-dep declarations to migrate)
    - genossi_rest/Cargo.toml (line 30 — the current direct csv dep to migrate)
    - .planning/phases/06-teilnehmerlisten-export-f-r-generalversammlungen/06-RESEARCH.md §"Standard Stack" (rust_xlsxwriter 0.82 ist im Lock; csv 1.3)
  </read_first>
  <action>
    Schritt 1 — Workspace-Root `Cargo.toml`: Im Block `[workspace.dependencies]`
    nach den bestehenden Eintraegen (alphabetisch eingeordnet) zwei neue
    Zeilen hinzufuegen, genau wie folgt:

    ```toml
    csv = "1.3"
    rust_xlsxwriter = "0.82"
    ```

    Versionswahl: Pin auf `"0.82"` (in Cargo.lock bereits gebaut, kein Bump-Risiko)
    — per RESEARCH §"Open Question 1" Empfehlung. Kein Bump auf 0.94/0.95.

    Schritt 2 — `genossi_service_impl/Cargo.toml`: Im `[dependencies]`-Block,
    alphabetisch passend zwischen `cms`/`const-oid` einerseits und `der`/`flate2`
    andererseits, die zwei neuen Zeilen einfuegen:

    ```toml
    csv = { workspace = true }
    rust_xlsxwriter = { workspace = true }
    ```

    Schritt 3 — `genossi_bin/Cargo.toml`: Den existierenden Hardcode-Eintrag
    `rust_xlsxwriter = "0.82"` (Zeile 58, im `[dev-dependencies]`-Block)
    auf `rust_xlsxwriter = { workspace = true }` umstellen.
    Den existierenden Hardcode-Eintrag `csv = "1.3"` (Zeile 60, ebenfalls
    `[dev-dependencies]`) auf `csv = { workspace = true }` umstellen.

    Schritt 4 — `genossi_rest/Cargo.toml`: Den existierenden Eintrag
    `csv = "1.3"` (Zeile 30, im `[dependencies]`-Block) auf
    `csv = { workspace = true }` umstellen.

    Schritt 5 — Build verifizieren:
    `cargo build -p genossi_service_impl` muss gruen sein.
    `cargo build --workspace` muss gruen sein (kein Re-Lock noetig — beide Versionen
    sind bereits im Cargo.lock).

    NICHTS anderes anfassen: keine Code-Imports, kein neuer Service. Nur deps.
  </action>
  <verify>
    <automated>cargo build --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "rust_xlsxwriter" Cargo.toml` zeigt `rust_xlsxwriter = "0.82"` im workspace-Block.
    - `grep -n "^csv" Cargo.toml` zeigt `csv = "1.3"` im workspace-Block.
    - `grep -n "rust_xlsxwriter" genossi_service_impl/Cargo.toml` zeigt `rust_xlsxwriter = { workspace = true }`.
    - `grep -n "^csv = " genossi_service_impl/Cargo.toml` zeigt `csv = { workspace = true }`.
    - `grep -n "rust_xlsxwriter" genossi_bin/Cargo.toml` zeigt `rust_xlsxwriter = { workspace = true }` (KEIN `"0.82"` mehr direkt).
    - `grep -n "^csv = " genossi_bin/Cargo.toml` zeigt `csv = { workspace = true }`.
    - `grep -n "^csv = " genossi_rest/Cargo.toml` zeigt `csv = { workspace = true }` (KEIN `"1.3"` mehr direkt).
    - `cargo build --workspace` Exit-Code 0.
    - `cargo metadata --format-version 1 --no-deps | grep -c rust_xlsxwriter` >= 1 (Workspace kennt die Dep).
  </acceptance_criteria>
  <done>
    Alle vier Cargo.toml sind aktualisiert, `cargo build --workspace` ist gruen.
    Plan 02 kann `use rust_xlsxwriter::Workbook;` und `use csv::WriterBuilder;`
    in `genossi_service_impl` ohne weitere Dependency-Aenderungen verwenden.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Typst template `templates/teilnehmerliste.typ` with X-von-Y header and 6-column repeating-header table</name>
  <files>templates/teilnehmerliste.typ</files>
  <read_first>
    - templates/_layout.typ (full — to understand the `letter()` helper signature: takes `title`, `date`, `content`)
    - templates/join_confirmation.typ (full — to see the established `sys.inputs.at("...")` + `json.decode` pattern)
    - .planning/phases/06-teilnehmerlisten-export-f-r-generalversammlungen/06-RESEARCH.md §"Pattern 2: Typst-Template mit `table.header(repeat: true)`" (the verbatim template-body source — lines 295-345)
    - .planning/phases/06-teilnehmerlisten-export-f-r-generalversammlungen/06-PATTERNS.md §"templates/teilnehmerliste.typ"
  </read_first>
  <action>
    Erstelle die Datei `templates/teilnehmerliste.typ` exakt mit folgendem Inhalt
    (uebernommen aus RESEARCH.md §"Pattern 2", mit kleinen Konsistenz-Anpassungen):

    ```typst
    // Teilnehmerliste fuer Generalversammlung — Phase 6 (D-04, D-08, D-10)
    // Inputs via sys.inputs:
    //   meta (JSON string): {"title": str, "date": str, "present": int, "total": int|null}
    //   rows (JSON string): [{"member_number": int, "first_name": str, "last_name": str,
    //                         "salutation": str|null, "title": str|null, "is_present": bool}, ...]

    #import "_layout.typ": letter

    #let meta = json.decode(sys.inputs.at("meta"))
    #let rows = json.decode(sys.inputs.at("rows"))

    #show: letter.with(
      title: meta.title,
      date: meta.date,
    )

    #text(size: 12pt)[
      #if meta.at("total", default: none) != none [
        *#meta.present von #meta.total anwesend*
      ] else [
        *#meta.present anwesend*
      ]
    ]

    #v(0.5cm)

    #table(
      columns: (auto, 1fr, 1fr, auto, auto, auto),
      align: (right, left, left, left, left, center),
      stroke: 0.5pt,
      table.header(
        repeat: true,
        [*Nr.*], [*Nachname*], [*Vorname*], [*Anrede*], [*Titel*], [*anwesend*],
      ),
      ..rows.map(r => (
        [#r.member_number],
        [#r.last_name],
        [#r.first_name],
        [#r.at("salutation", default: "")],
        [#r.at("title", default: "")],
        if r.is_present [✓] else [],
      )).flatten()
    )
    ```

    Wichtig zur Datei-Position: Datei liegt im Top-Level `templates/`, NICHT in
    `templates/defaults/`. RESEARCH-Pitfall 3 weist auf den `template_storage().base_path()`-
    Mechanismus hin — der existierende `pdf_generation.rs` nimmt `template_base: &Path`
    als Parameter, und Plan 02 ruft das mit dem `templates/`-Root auf, der bereits
    `_layout.typ` enthaelt (`#import "_layout.typ"` resolved gegen `template_base`).

    Format-Details (per D-08 + D-10):
    - Kopfblock: `*X von Y anwesend*` wenn `total != none`, sonst `*X anwesend*`.
    - X = `meta.present` (Anzahl anwesender Mitglieder, vom Service vorberechnet).
    - Y = `meta.total` (Snapshot-Gesamtgroesse, NUR bei `include=all` gesetzt; bei
      `include=present` ist `total` JSON-`null` -> in Typst `none`).
    - Glyph fuer Anwesenheit im PDF: `✓` (per Planner-Entscheidung aus
      CONTEXT §"Claude's Discretion").

    KEIN Logo, KEINE Unterschriftenspalte, KEINE Fusszeile (per D-08).

    Verifikation, dass Typst die Datei syntaktisch akzeptiert: `cargo build` muss
    durchlaufen (build verifiziert nichts, aber kompiliert kein Typst). Echter
    Render-Test passiert in Plan 03 (E2E mit `%PDF-` Magic-Bytes). Fuer dieses
    Plan reicht die Datei-Existenz + Inhaltspruefung.
  </action>
  <verify>
    <automated>test -f templates/teilnehmerliste.typ && grep -q '#import "_layout.typ": letter' templates/teilnehmerliste.typ && grep -q 'table.header' templates/teilnehmerliste.typ && grep -q 'meta.present' templates/teilnehmerliste.typ</automated>
  </verify>
  <acceptance_criteria>
    - `test -f templates/teilnehmerliste.typ` succeeds.
    - `grep -c '#import "_layout.typ": letter' templates/teilnehmerliste.typ` == 1.
    - `grep -c 'json.decode(sys.inputs.at(' templates/teilnehmerliste.typ` >= 2 (meta + rows).
    - `grep -c 'table.header' templates/teilnehmerliste.typ` == 1.
    - `grep -c 'repeat: true' templates/teilnehmerliste.typ` == 1.
    - `grep -c 'meta.present' templates/teilnehmerliste.typ` >= 2 (Header beide Branches).
    - `grep -c 'meta.total' templates/teilnehmerliste.typ` >= 1.
    - File contains the 6 column headers: `Nr.`, `Nachname`, `Vorname`, `Anrede`, `Titel`, `anwesend` (grep all six).
    - File contains the `✓` glyph for is_present: `grep -c '✓' templates/teilnehmerliste.typ` == 1.
    - File does NOT contain `Logo` or `Unterschrift` or `Fusszeile`: `grep -ciE 'logo|unterschrift|fusszeile' templates/teilnehmerliste.typ` == 0.
    - `wc -l templates/teilnehmerliste.typ` >= 35 (Sanity: Datei nicht leer).
  </acceptance_criteria>
  <done>
    Template `templates/teilnehmerliste.typ` existiert, importiert `_layout.typ`,
    dekodiert `meta` und `rows` aus `sys.inputs`, rendert konditionalen X-von-Y-Header
    und eine 6-spaltige Tabelle mit `table.header(repeat: true)`. Plan 02 kann
    `inputs.insert(Str::from("meta"), ...)` und `inputs.insert(Str::from("rows"), ...)`
    verwenden, um das Template zu fuettern.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-Toolchain → Crates.io | rust_xlsxwriter 0.82 + csv 1.3 als Dependency (supply-chain) |
| Typst-Template-Engine → Filesystem | _layout.typ wird via `#import` aufgeloest (templates dir is read-only at runtime) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T (Tampering) | rust_xlsxwriter supply-chain | accept | Crate ist gepinnt auf "0.82" (bereits in Cargo.lock mit Checksum). Maintainer `jmcnamara` mit langer Domain-Historie. Kein Bump in diesem Plan -> unveraendertes Risiko vs. Status quo. |
| T-06-02 | I (Information Disclosure) | Typst-Template enthaelt Mitgliederdaten | mitigate | Template selbst enthaelt KEINE eingebauten PII — alle Daten kommen via `sys.inputs` zur Render-Zeit. Datei ist Code, kein Datenspeicher. |
| T-06-03 | T (Tampering) | `_layout.typ`-Import-Pfad | accept | Pfad ist relativ zum `template_base`-Argument, das Plan 03 (DI-Wiring) aus `rest_state.template_storage().base_path()` bezieht — kontrolliert vom Server-Bin, kein User-Input. Path-Traversal-Risiko ist 0. |

No HIGH-severity findings. Plan 1 ist reine Build-Konfig + statisches Asset.
</threat_model>

<verification>
- `cargo build --workspace` ist gruen
- `cargo metadata --format-version 1 --no-deps` zeigt `rust_xlsxwriter` und `csv` als Workspace-Deps
- `templates/teilnehmerliste.typ` existiert mit allen geforderten Strukturelementen (siehe acceptance_criteria von Task 2)
- KEIN Test laeuft fehl: `cargo test --workspace --no-run` baut erfolgreich (kein Code-Aenderung -> keine neuen Tests)
</verification>

<success_criteria>
- Plan 02 kann ohne weitere Cargo.toml-Aenderungen `use rust_xlsxwriter::Workbook;` und `use csv::WriterBuilder;` in `genossi_service_impl/src/attendance_export.rs` schreiben.
- Plan 02/03 kann das neue Template via `PdfGenerator::render_attendance_list(...)` (neue Methode, Plan 02) gegen `templates/teilnehmerliste.typ` aufrufen.
- D-02 erfuellt (rust_xlsxwriter Workspace-Dep).
- D-04 vorbereitet (Template existiert und passt zum bestehenden _layout.typ Pattern).
- D-08 + D-10 im Template kodiert (konditionale X-von-Y/X-Header).
</success_criteria>

<output>
After completion, create `.planning/phases/06-teilnehmerlisten-export-f-r-generalversammlungen/06-01-SUMMARY.md`
</output>
