---
phase: 06-teilnehmerlisten-export-f-r-generalversammlungen
plan: 04
type: execute
wave: 4
depends_on: ["06-03"]
files_modified:
  - genossi-frontend/src/api.rs
  - genossi-frontend/src/page/assembly_details.rs
  - genossi-frontend/src/i18n/mod.rs
  - genossi-frontend/src/i18n/de.rs
  - genossi-frontend/src/i18n/en.rs
autonomous: false
requirements:
  - D-09
  - D-15
  - D-19
  - D-20
tags:
  - export
  - frontend
  - dioxus
  - ui

must_haves:
  truths:
    - "Vorstand sieht im Detail-View einer GESCHLOSSENEN GV einen 4. Tab `Export` (D-19, D-20)"
    - "Vorstand sieht im Detail-View einer NICHT-geschlossenen GV KEINEN `Export`-Tab (D-11/D-19)"
    - "Im Export-Tab kann Vorstand ein Format (PDF/CSV/XLSX) und include-Option (Alle/Nur Anwesende) waehlen (D-01, D-09)"
    - "Default-Auswahl ist Format=PDF + include=all (per UI-SPEC + D-09 Recommendation)"
    - "Filename-Preview reagiert auf Format-Wechsel und zeigt `gv-{YYYY-MM-DD}-teilnehmer.{ext}` (D-15)"
    - "Klick auf `Herunterladen` triggert Browser-Download via Blob-URL + programmatischen Anchor-Click; Button bleibt waehrend `await` disabled (UI-SPEC Loading-State)"
    - "Fehlerpfade (403/409/500) zeigen Toast mit DE+EN-uebersetzten Strings"
    - "Component ist inline in `assembly_details.rs` (kein neuer File unter `src/component/`) — D-20 Wiederverwendung nicht absehbar"
    - "i18n-Keys liegen in beiden Locales (de + en) (Genossi-Konvention)"
  artifacts:
    - path: "genossi-frontend/src/api.rs"
      provides: "Neue Funktion export_attendance_url(config, aid, format, include) -> Result<String, AppError>"
      contains: "pub async fn export_attendance_url"
    - path: "genossi-frontend/src/page/assembly_details.rs"
      provides: "ExportTab Component + erweiterte tab_defs (Closed-Status)"
      contains: "ExportTab"
      min_lines_added: 80
    - path: "genossi-frontend/src/i18n/mod.rs"
      provides: "Mindestens 11 neue Key-Varianten fuer Export-Block (siehe UI-SPEC §Copywriting Contract)"
      contains: "AssemblyTabExport"
    - path: "genossi-frontend/src/i18n/de.rs"
      provides: "DE-Strings fuer alle neuen Keys"
      contains: "Teilnehmerliste exportieren"
    - path: "genossi-frontend/src/i18n/en.rs"
      provides: "EN-Strings fuer alle neuen Keys"
      contains: "Export attendance list"
  key_links:
    - from: "genossi-frontend/src/page/assembly_details.rs::ExportTab"
      to: "genossi-frontend/src/api.rs::export_attendance_url"
      via: "spawn(async move { ... api::export_attendance_url(...) ... })"
      pattern: "export_attendance_url"
    - from: "genossi-frontend/src/page/assembly_details.rs"
      to: "GET /api/assembly/{aid}/attendance-export/{format}?include={include}"
      via: "fetch + .blob() + anchor.click() pattern"
      pattern: "attendance-export"
    - from: "ExportTab visibility"
      to: "assembly.status == AssemblyStatusTO::Closed"
      via: "tab_defs.push(...) only if Closed"
      pattern: "AssemblyStatusTO::Closed"
---

<objective>
Bringe das Frontend-UI fuer den Phase-6-Export online: einen neuen 4. Tab
„Export" in der Assembly-Detail-Page, der NUR bei `assembly.status == Closed`
erscheint, eine API-Funktion fuer den Blob-Download und die i18n-Keys in beiden
Locales.

Purpose: D-19 + D-20 + die Frontend-Haelfte von D-09 + D-15 sichtbar machen.
Plan 03 hat das HTTP-Backend, dieser Plan macht es klickbar fuer den Vorstand.

Output:
- `genossi-frontend/src/api.rs` — neue async-fn `export_attendance_url`
- `genossi-frontend/src/page/assembly_details.rs` — `ExportTab` inline-Component + status-bedingte tab_defs
- `genossi-frontend/src/i18n/{mod,de,en}.rs` — 11+ neue Translation-Keys

CHECKPOINT: Dieser Plan enthaelt eine `checkpoint:human-verify`-Task, damit der
User den UI-Flow auf einer realen Closed-GV optisch verifizieren kann (laut
Genossi-Pattern: GV-Flows werden mit Browser-Klick getestet, nicht nur mit Unit-
Tests, weil Layout/Reaktivitaet von der Tailwind-Compile-Zeit abhaengen).
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-teilnehmerlisten-export-f-r-generalversammlungen/06-CONTEXT.md
@.planning/phases/06-teilnehmerlisten-export-f-r-generalversammlungen/06-RESEARCH.md
@.planning/phases/06-teilnehmerlisten-export-f-r-generalversammlungen/06-PATTERNS.md
@.planning/phases/06-teilnehmerlisten-export-f-r-generalversammlungen/06-UI-SPEC.md
@.planning/phases/06-teilnehmerlisten-export-f-r-generalversammlungen/06-03-SUMMARY.md
@genossi-frontend/CLAUDE.md

<interfaces>
<!-- Existing API function — exact analog to copy -->

From genossi-frontend/src/api.rs (lines 506-548, verified):
```rust
pub async fn render_template_pdf(
    config: &Config,
    path: &str,
    member_id: Uuid,
) -> Result<String, AppError> {
    use wasm_bindgen::JsCast;
    use wasm_bindgen_futures::JsFuture;

    let url = template_render_url(config, path, member_id);
    info!("Rendering template PDF: {url}");
    let mut opts = web_sys::RequestInit::new();
    opts.set_method("POST");

    let request = web_sys::Request::new_with_str_and_init(&url, &opts)
        .map_err(|e| AppError::new(None, "Verbindungsfehler", Some(format!("{:?}", e))))?;
    let window = web_sys::window()
        .ok_or_else(|| AppError::new(None, "Verbindungsfehler", None))?;
    let resp_value = JsFuture::from(window.fetch_with_request(&request))
        .await
        .map_err(|e| AppError::new(None, "Verbindungsfehler", Some(format!("{:?}", e))))?;
    let resp: web_sys::Response = resp_value.dyn_into()
        .map_err(|_| AppError::new(None, "Verbindungsfehler", None))?;
    if !resp.ok() { return Err(map_web_response_error(&resp).await); }
    let blob = JsFuture::from(resp.blob().unwrap())
        .await
        .map_err(|e| AppError::new(None, "Verbindungsfehler", Some(format!("{:?}", e))))?;
    let blob: web_sys::Blob = blob.dyn_into()
        .map_err(|_| AppError::new(None, "Verbindungsfehler", None))?;
    let blob_url = web_sys::Url::create_object_url_with_blob(&blob)
        .map_err(|e| AppError::new(None, "Verbindungsfehler", Some(format!("{:?}", e))))?;
    Ok(blob_url)
}
```

<!-- Existing page component — exact analog for ExportTab -->

From genossi-frontend/src/page/assembly_details.rs (verified):
- Line 25 imports include `TabDef, TabStrip, Modal, ToastContainer, CreateTokenForm` etc.
- Lines 82-92 — existing `let tab_defs = vec![ TabDef { key: "basics", ...}, ...]` (3 entries).
- TokensTab and other tabs are inline `#[component] fn ...(...) -> Element` blocks at the bottom of the file.

From genossi-frontend/src/i18n/mod.rs (existing — verified):
- `pub enum Key { ... AssemblyTabBasics, AssemblyTabTokens, AssemblyTabAttendance, ... }`
- Both de.rs and en.rs MUST have entries for every Key variant — compile-time enforced.

<!-- UI-SPEC contract — bindend -->

From .planning/phases/06-teilnehmerlisten-export-f-r-generalversammlungen/06-UI-SPEC.md:
- §"Copywriting Contract" — exact DE + EN strings (verbindlich)
- §"Component Inventory" — exact Tailwind class lists per element (verbindlich)
- §"State Machine" — Tab-Sichtbarkeit + Block-States
- §"Layout Decision" — Option A (neuer 4. Tab) wurde gewaehlt
- §"Component Decision (D-20)" — inline in assembly_details.rs, kein src/component/-File
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add i18n keys + DE + EN translations</name>
  <files>genossi-frontend/src/i18n/mod.rs, genossi-frontend/src/i18n/de.rs, genossi-frontend/src/i18n/en.rs</files>
  <read_first>
    - genossi-frontend/src/i18n/mod.rs (full — to see existing Key enum + Locale pattern)
    - genossi-frontend/src/i18n/de.rs (search for `AssemblyTab` to find the analog block to extend)
    - genossi-frontend/src/i18n/en.rs (same — to mirror the analog block)
    - genossi-frontend/CLAUDE.md (i18n contract — both locales MUST have every Key)
    - .planning/phases/06-teilnehmerlisten-export-f-r-generalversammlungen/06-UI-SPEC.md §"Copywriting Contract" (lines 99-138 — the verbatim DE+EN strings)
  </read_first>
  <behavior>
    - Test 1: `Key::AssemblyTabExport` existiert und ist in beiden Locales (de + en) gemappt — compile-time enforced via match-exhaustivity.
    - Test 2: Mindestens 11 neue Keys vorhanden (gemaess UI-SPEC Copywriting-Contract).
    - Test 3: `cargo build -p genossi-frontend` (oder `cd genossi-frontend && cargo check`) compiliert ohne `non-exhaustive match`-Errors.
  </behavior>
  <action>
    Schritt A — `genossi-frontend/src/i18n/mod.rs`: Im `pub enum Key { ... }`
    direkt nach `AssemblyTabAttendance` (alphabetisch passend) folgende neue
    Varianten hinzufuegen:

    ```rust
    AssemblyTabExport,
    AttendanceExportHeading,
    AttendanceExportSubheading,
    AttendanceExportFormatLabel,
    AttendanceExportFormatPdfTitle,
    AttendanceExportFormatPdfHint,
    AttendanceExportFormatCsvTitle,
    AttendanceExportFormatCsvHint,
    AttendanceExportFormatXlsxTitle,
    AttendanceExportFormatXlsxHint,
    AttendanceExportIncludeLabel,
    AttendanceExportIncludeAll,
    AttendanceExportIncludePresent,
    AttendanceExportFilenameLabel,
    AttendanceExportSubmit,
    AttendanceExportSubmitLoading,
    AttendanceExportClosedGateHeading,
    AttendanceExportClosedGateBody,
    AttendanceExportError409,
    AttendanceExportError403,
    AttendanceExportErrorNetwork,
    ```

    Schritt B — `genossi-frontend/src/i18n/de.rs`: An gleicher Stelle (nach dem
    AssemblyTab* Block) folgende DE-Strings hinzufuegen, exakt aus UI-SPEC §
    Copywriting Contract:

    ```rust
    Key::AssemblyTabExport => "Export",
    Key::AttendanceExportHeading => "Teilnehmerliste exportieren",
    Key::AttendanceExportSubheading => "Erzeugt eine Datei zur Anlage an das GV-Protokoll.",
    Key::AttendanceExportFormatLabel => "Format",
    Key::AttendanceExportFormatPdfTitle => "PDF (für Protokoll)",
    Key::AttendanceExportFormatPdfHint => "Empfohlen für Protokoll-Anlage",
    Key::AttendanceExportFormatCsvTitle => "CSV (Semikolon, Excel-kompatibel)",
    Key::AttendanceExportFormatCsvHint => "Für Verband oder Weiterverarbeitung",
    Key::AttendanceExportFormatXlsxTitle => "Excel (XLSX)",
    Key::AttendanceExportFormatXlsxHint => "Excel-natives Format",
    Key::AttendanceExportIncludeLabel => "Welche Mitglieder einbeziehen?",
    Key::AttendanceExportIncludeAll => "Alle Mitglieder (mit Anwesenheits-Spalte)",
    Key::AttendanceExportIncludePresent => "Nur Anwesende",
    Key::AttendanceExportFilenameLabel => "Dateiname",
    Key::AttendanceExportSubmit => "Herunterladen",
    Key::AttendanceExportSubmitLoading => "Wird erzeugt …",
    Key::AttendanceExportClosedGateHeading => "Export verfügbar nach GV-Schluss",
    Key::AttendanceExportClosedGateBody => "Die Teilnehmerliste kann erst exportiert werden, sobald diese Generalversammlung geschlossen ist.",
    Key::AttendanceExportError409 => "Export ist nur für geschlossene Generalversammlungen möglich.",
    Key::AttendanceExportError403 => "Keine Berechtigung zum Export.",
    Key::AttendanceExportErrorNetwork => "Export fehlgeschlagen. Bitte erneut versuchen.",
    ```

    Schritt C — `genossi-frontend/src/i18n/en.rs`: Spiegel-Strings:

    ```rust
    Key::AssemblyTabExport => "Export",
    Key::AttendanceExportHeading => "Export attendance list",
    Key::AttendanceExportSubheading => "Generates a file to attach to the assembly minutes.",
    Key::AttendanceExportFormatLabel => "Format",
    Key::AttendanceExportFormatPdfTitle => "PDF (for minutes)",
    Key::AttendanceExportFormatPdfHint => "Recommended for minutes attachment",
    Key::AttendanceExportFormatCsvTitle => "CSV (semicolon, Excel-compatible)",
    Key::AttendanceExportFormatCsvHint => "For associations or further processing",
    Key::AttendanceExportFormatXlsxTitle => "Excel (XLSX)",
    Key::AttendanceExportFormatXlsxHint => "Excel-native format",
    Key::AttendanceExportIncludeLabel => "Which members to include?",
    Key::AttendanceExportIncludeAll => "All members (with attendance column)",
    Key::AttendanceExportIncludePresent => "Only attendees",
    Key::AttendanceExportFilenameLabel => "Filename",
    Key::AttendanceExportSubmit => "Download",
    Key::AttendanceExportSubmitLoading => "Generating …",
    Key::AttendanceExportClosedGateHeading => "Export available after the assembly is closed",
    Key::AttendanceExportClosedGateBody => "The attendance list can only be exported once this assembly is closed.",
    Key::AttendanceExportError409 => "Export is only available for closed assemblies.",
    Key::AttendanceExportError403 => "You don't have permission to export.",
    Key::AttendanceExportErrorNetwork => "Export failed. Please try again.",
    ```

    Verifikation: `cd genossi-frontend && cargo check` (oder `dx check`) muss
    OHNE „non-exhaustive match patterns" Errors compilieren.
  </action>
  <verify>
    <automated>cd genossi-frontend && cargo check 2>&1 | tee /tmp/i18n-check.log; ! grep -E 'non-exhaustive|missing arm' /tmp/i18n-check.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AssemblyTabExport" genossi-frontend/src/i18n/mod.rs` == 1.
    - `grep -c "AttendanceExportHeading" genossi-frontend/src/i18n/mod.rs` == 1.
    - `grep -c "AttendanceExport" genossi-frontend/src/i18n/mod.rs` >= 18 (enthaelt alle 18 neuen `AttendanceExport*` Keys).
    - `grep -c "Teilnehmerliste exportieren" genossi-frontend/src/i18n/de.rs` == 1.
    - `grep -c "Export attendance list" genossi-frontend/src/i18n/en.rs` == 1.
    - `grep -c "AttendanceExport" genossi-frontend/src/i18n/de.rs` >= 18.
    - `grep -c "AttendanceExport" genossi-frontend/src/i18n/en.rs` >= 18.
    - `cd genossi-frontend && cargo check` Exit-Code 0 — KEIN „non-exhaustive match patterns"-Fehler.
  </acceptance_criteria>
  <done>
    Alle 21 neuen Keys sind in beiden Locales registriert; das Frontend
    compiliert ohne Match-Exhaustivity-Fehler. Task 2 kann `i18n.t(Key::AttendanceExportHeading)`
    direkt in RSX schreiben.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add `export_attendance_url` in api.rs + `ExportTab` inline component in assembly_details.rs</name>
  <files>genossi-frontend/src/api.rs, genossi-frontend/src/page/assembly_details.rs</files>
  <read_first>
    - genossi-frontend/src/api.rs (lines 497-548 — the verbatim `render_template_pdf` analog)
    - genossi-frontend/src/api.rs (lines 1639-1655 + 1803-1862 — existing attendance-related API block; ExportFunktion gehoert in der Naehe)
    - genossi-frontend/src/page/assembly_details.rs (full — to see the existing 3 tabs + the TokensTab component pattern that ExportTab mirrors)
    - genossi-frontend/src/page/assembly_details.rs (lines 82-92 — existing tab_defs construction; lines 206-303 — TokensTab pattern)
    - .planning/phases/06-teilnehmerlisten-export-f-r-generalversammlungen/06-UI-SPEC.md §"Component Inventory" (lines 214-238 — verbindliche Tailwind-Klassen)
    - .planning/phases/06-teilnehmerlisten-export-f-r-generalversammlungen/06-UI-SPEC.md §"Interaction Contract" (lines 256-294 — Default/Loading/Success/Error State)
    - .planning/phases/06-teilnehmerlisten-export-f-r-generalversammlungen/06-PATTERNS.md §"genossi-frontend/src/page/assembly_details.rs"
    - genossi-frontend/CLAUDE.md (Component-First Konvention)
  </read_first>
  <behavior>
    - Test 1: `api::export_attendance_url(config, aid, "pdf", "all").await` baut die korrekte URL `{backend}/api/assembly/{aid}/attendance-export/pdf?include=all`.
    - Test 2: ExportTab rendert mit Default-Format "pdf" und Default-Include "all".
    - Test 3: Filename-Preview reagiert auf Format-Wechsel — bei Format-Klick `csv` wird `gv-{date}-teilnehmer.csv` angezeigt; bei `xlsx` -> `.xlsx`.
    - Test 4 (visual via checkpoint): tab_defs enthaelt 4 Eintraege NUR wenn assembly.status == Closed; sonst 3.
  </behavior>
  <action>
    Schritt A — `genossi-frontend/src/api.rs`: Neue Funktion `export_attendance_url`
    nach `get_assembly_stats` (etwa Zeile 1862 — pruefe genaue Position) einfuegen:

    ```rust
    /// Phase 6 (D-14, D-15, D-16): Triggers attendance-list export and returns
    /// a blob URL that the caller wraps in `<a download="...">`.
    ///
    /// `format`: must be "csv" | "pdf" | "xlsx" — caller validates.
    /// `include`: must be "all" | "present".
    pub async fn export_attendance_url(
        config: &Config,
        assembly_id: Uuid,
        format: &str,
        include: &str,
    ) -> Result<String, AppError> {
        use wasm_bindgen::JsCast;
        use wasm_bindgen_futures::JsFuture;

        let url = format!(
            "{}/api/assembly/{}/attendance-export/{}?include={}",
            config.backend, assembly_id, format, include
        );
        log::info!("Exporting attendance list: {url}");

        let mut opts = web_sys::RequestInit::new();
        opts.set_method("GET");

        let request = web_sys::Request::new_with_str_and_init(&url, &opts)
            .map_err(|e| AppError::new(None, "Verbindungsfehler", Some(format!("{:?}", e))))?;
        let window = web_sys::window()
            .ok_or_else(|| AppError::new(None, "Verbindungsfehler", None))?;
        let resp_value = JsFuture::from(window.fetch_with_request(&request))
            .await
            .map_err(|e| AppError::new(None, "Verbindungsfehler", Some(format!("{:?}", e))))?;
        let resp: web_sys::Response = resp_value
            .dyn_into()
            .map_err(|_| AppError::new(None, "Verbindungsfehler", None))?;
        if !resp.ok() {
            return Err(map_web_response_error(&resp).await);
        }

        let blob = JsFuture::from(resp.blob().unwrap())
            .await
            .map_err(|e| AppError::new(None, "Verbindungsfehler", Some(format!("{:?}", e))))?;
        let blob: web_sys::Blob = blob
            .dyn_into()
            .map_err(|_| AppError::new(None, "Verbindungsfehler", None))?;
        let blob_url = web_sys::Url::create_object_url_with_blob(&blob)
            .map_err(|e| AppError::new(None, "Verbindungsfehler", Some(format!("{:?}", e))))?;
        Ok(blob_url)
    }
    ```

    HINWEIS: Der `log::info!` macro-Pfad muss in api.rs schon importiert sein
    (siehe wie `render_template_pdf` es nutzt). Pruefe das vor dem Edit.
    `info!` vs `log::info!` — pruefe Konvention in api.rs.

    Schritt B — `genossi-frontend/src/page/assembly_details.rs` editieren.

    Teil B1 — `tab_defs` (Zeilen 82-92) erweitern um den 4. Tab NUR bei Closed:

    ```rust
    // Vorher:
    let tab_defs = vec![
        TabDef { key: "basics", label: i18n.t(Key::AssemblyTabBasics).to_string() },
        TabDef { key: "tokens", label: i18n.t(Key::AssemblyTabTokens).to_string() },
        TabDef { key: "attendance", label: i18n.t(Key::AssemblyTabAttendance).to_string() },
    ];

    // Nachher (D-19: Tab nur bei Closed sichtbar):
    let mut tab_defs = vec![
        TabDef { key: "basics", label: i18n.t(Key::AssemblyTabBasics).to_string() },
        TabDef { key: "tokens", label: i18n.t(Key::AssemblyTabTokens).to_string() },
        TabDef { key: "attendance", label: i18n.t(Key::AssemblyTabAttendance).to_string() },
    ];
    if matches!(a.status, AssemblyStatusTO::Closed) {
        tab_defs.push(TabDef {
            key: "export",
            label: i18n.t(Key::AssemblyTabExport).to_string(),
        });
    }
    ```

    Teil B2 — Im Match auf `active_tab` (oder `selected_tab` — pruefe den exakten
    Variant-Namen in der Datei) den vierten Arm fuer "export" einfuegen, der
    `ExportTab` rendert. Pattern: identisch zu wie "tokens" -> TokensTab gemappt
    ist. Beispiel:

    ```rust
    "export" => rsx! { ExportTab { assembly: a.clone(), on_error: handle_error } },
    ```

    Teil B3 — Am Ende der Datei (nach den anderen `#[component] fn ...`) das
    neue `ExportTab` Component einfuegen, basierend auf UI-SPEC §"Component
    Inventory" + §"Interaction Contract":

    ```rust
    #[component]
    fn ExportTab(assembly: AssemblyTO, on_error: EventHandler<String>) -> Element {
        let i18n = use_i18n();
        let mut selected_format = use_signal(|| "pdf".to_string());
        let mut selected_include = use_signal(|| "all".to_string());
        let mut submitting = use_signal(|| false);

        // D-15 Filename-Preview: gv-{YYYY-MM-DD}-teilnehmer.{ext}
        // assembly.date ist ein DateTime — formatieren auf YYYY-MM-DD.
        // Falls assembly.date None oder parse-Fehler, Fallback "gv-teilnehmer.{ext}".
        let date_str = format_assembly_date_yyyy_mm_dd(&assembly.date)
            .unwrap_or_else(|| "".to_string());
        let filename = if date_str.is_empty() {
            format!("gv-teilnehmer.{}", selected_format.read())
        } else {
            format!("gv-{}-teilnehmer.{}", date_str, selected_format.read())
        };

        let assembly_id = assembly.id;
        let on_submit = move |evt: FormEvent| {
            evt.prevent_default();
            let fmt = selected_format.read().clone();
            let inc = selected_include.read().clone();
            let on_error = on_error.clone();
            submitting.set(true);
            let date_for_dl = date_str.clone();
            spawn(async move {
                let cfg = CONFIG.read().clone();
                match api::export_attendance_url(&cfg, assembly_id, &fmt, &inc).await {
                    Ok(blob_url) => {
                        // Programmatic <a download> click — see UI-SPEC §"Success Path"
                        let document = web_sys::window().unwrap().document().unwrap();
                        let a: web_sys::HtmlAnchorElement = document
                            .create_element("a").unwrap()
                            .dyn_into().unwrap();
                        a.set_href(&blob_url);
                        a.set_download(&format!("gv-{}-teilnehmer.{}", date_for_dl, fmt));
                        a.click();
                        let _ = web_sys::Url::revoke_object_url(&blob_url);
                    }
                    Err(e) => {
                        // Map HTTP status -> localized error string.
                        // AppError.status is `Option<u16>` (api.rs:16) — match directly on the field,
                        // there is NO `.status_code()` method.
                        let key = match e.status {
                            Some(409) => Key::AttendanceExportError409,
                            Some(403) => Key::AttendanceExportError403,
                            _ => Key::AttendanceExportErrorNetwork,
                        };
                        on_error.call(i18n.t(key).to_string());
                    }
                }
                submitting.set(false);
            });
        };

        rsx! {
            div { class: "bg-white p-4 sm:p-6 rounded-lg border border-gray-200",
                h2 { class: "text-xl font-semibold mb-2", "{i18n.t(Key::AttendanceExportHeading)}" }
                p  { class: "text-sm text-gray-600 mb-6", "{i18n.t(Key::AttendanceExportSubheading)}" }

                form { class: "flex flex-col gap-6", onsubmit: on_submit,

                    // ===== Format Radio-Cards =====
                    div {
                        span { class: "text-sm text-gray-700 mb-2 block", "{i18n.t(Key::AttendanceExportFormatLabel)}" }
                        div { class: "grid grid-cols-1 sm:grid-cols-3 gap-3",
                            for &(fmt_key, title_key, hint_key) in &[
                                ("pdf",  Key::AttendanceExportFormatPdfTitle,  Key::AttendanceExportFormatPdfHint),
                                ("csv",  Key::AttendanceExportFormatCsvTitle,  Key::AttendanceExportFormatCsvHint),
                                ("xlsx", Key::AttendanceExportFormatXlsxTitle, Key::AttendanceExportFormatXlsxHint),
                            ] {
                                {
                                    // fmt_key: &str, title_key/hint_key: Key (destructured via `&(...)`).
                                    let is_selected = selected_format.read().as_str() == fmt_key;
                                    let card_class = if is_selected {
                                        "border-2 border-blue-500 bg-blue-50 px-4 py-3 rounded cursor-pointer flex flex-col gap-1 min-h-[44px]"
                                    } else {
                                        "border-2 border-gray-200 hover:border-gray-300 bg-white px-4 py-3 rounded cursor-pointer flex flex-col gap-1 min-h-[44px]"
                                    };
                                    let value_for_click = fmt_key.to_string();
                                    rsx! {
                                        label {
                                            class: "{card_class}",
                                            onclick: move |_| selected_format.set(value_for_click.clone()),
                                            input { r#type: "radio", name: "export_format", value: "{fmt_key}",
                                                    class: "sr-only", checked: is_selected }
                                            span { class: "text-base font-semibold", "{i18n.t(title_key)}" }
                                            span { class: "text-xs text-gray-500", "{i18n.t(hint_key)}" }
                                        }
                                    }
                                }
                            }
                        }
                    }

                    // ===== Include Radio-Group =====
                    div {
                        span { class: "text-sm text-gray-700 mb-2 block", "{i18n.t(Key::AttendanceExportIncludeLabel)}" }
                        div { class: "flex flex-col gap-2",
                            for &(inc_key, label_key) in &[
                                ("all",     Key::AttendanceExportIncludeAll),
                                ("present", Key::AttendanceExportIncludePresent),
                            ] {
                                {
                                    // inc_key: &str, label_key: Key (destructured via `&(...)`).
                                    let val = inc_key.to_string();
                                    let is_sel = selected_include.read().as_str() == inc_key;
                                    rsx! {
                                        label { class: "flex items-center gap-2 cursor-pointer min-h-[44px] px-2",
                                            input {
                                                r#type: "radio",
                                                name: "export_include",
                                                value: "{inc_key}",
                                                checked: is_sel,
                                                onclick: move |_| selected_include.set(val.clone()),
                                            }
                                            span { class: "text-base", "{i18n.t(label_key)}" }
                                        }
                                    }
                                }
                            }
                        }
                    }

                    // ===== Filename Preview =====
                    div { class: "flex items-baseline gap-3 text-sm",
                        span { class: "text-gray-700", "{i18n.t(Key::AttendanceExportFilenameLabel)}" }
                        code { class: "text-xs text-gray-500 font-mono", "{filename}" }
                    }

                    // ===== Submit Button =====
                    div { class: "flex justify-end",
                        button {
                            r#type: "submit",
                            class: "bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded min-h-[44px] disabled:opacity-50",
                            disabled: *submitting.read(),
                            if *submitting.read() {
                                "{i18n.t(Key::AttendanceExportSubmitLoading)}"
                            } else {
                                "{i18n.t(Key::AttendanceExportSubmit)}"
                            }
                        }
                    }
                }
            }
        }
    }

    // Helper — pure-logic, testable independently.
    fn format_assembly_date_yyyy_mm_dd(date: &Option<String>) -> Option<String> {
        // assembly.date kommt als ISO-8601-String aus dem Backend.
        // Plan-Impl: split off the "T..." Komponente — kein chrono noetig
        let s = date.as_ref()?;
        let date_part = s.split('T').next()?;
        // Validate basic shape: 10 chars, mit "-" an Position 4 und 7
        if date_part.len() != 10 || date_part.chars().nth(4) != Some('-') || date_part.chars().nth(7) != Some('-') {
            return None;
        }
        Some(date_part.to_string())
    }

    #[cfg(test)]
    mod export_tab_tests {
        use super::*;
        #[test]
        fn format_date_extracts_yyyy_mm_dd_from_iso8601() {
            assert_eq!(
                format_assembly_date_yyyy_mm_dd(&Some("2026-05-15T19:00:00Z".to_string())),
                Some("2026-05-15".to_string())
            );
        }
        #[test]
        fn format_date_returns_none_for_invalid_input() {
            assert_eq!(format_assembly_date_yyyy_mm_dd(&None), None);
            assert_eq!(format_assembly_date_yyyy_mm_dd(&Some("invalid".to_string())), None);
            assert_eq!(format_assembly_date_yyyy_mm_dd(&Some("2026/05/15".to_string())), None);
        }
    }
    ```

    WICHTIGE Hinweise zur Umsetzung:
    - `AppError.status` ist `Option<u16>` (verifiziert api.rs:16). Match direkt auf `e.status`, NICHT auf `e.status_code()` (existiert nicht). Der `map_web_response_error`-Helper in `api.rs` setzt `status: Some(code)` schon korrekt, bevor er den `AppError` zurueckgibt.
    - Falls `CONFIG` der globale Signal-Name ist (siehe TokensTab analog), bleibt der Aufruf wie oben. Falls anders benannt: anpassen.
    - Falls `use_i18n()` nicht im Scope ist: pruefe wie TokensTab es importiert (Zeile 212 etwa). Gleiche Imports uebernehmen.
    - Falls die Tab-Match-Branch-Syntax sich vom Beispiel unterscheidet (z.B. `selected_tab.read().as_str()` statt `active_tab`), passe an die existierende Konvention in der Datei an.

    Schritt C — Falls `assembly_details.rs` import-Block den `dyn_into` und
    `web_sys::HtmlAnchorElement` noch nicht hat, hinzufuegen (am Top der Datei):

    ```rust
    use wasm_bindgen::JsCast;
    ```

    (`web_sys` und `wasm_bindgen_futures` sollten bereits importiert sein
    via Phase-4-Code — sonst aus `api.rs` analog uebernehmen.)
  </action>
  <verify>
    <automated>cd genossi-frontend && cargo check 2>&1 | tail -50 && cargo test --lib export_tab_tests -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub async fn export_attendance_url" genossi-frontend/src/api.rs` == 1.
    - `grep -c "attendance-export" genossi-frontend/src/api.rs` >= 1.
    - `grep -c "fn ExportTab" genossi-frontend/src/page/assembly_details.rs` == 1.
    - `grep -c "format_assembly_date_yyyy_mm_dd" genossi-frontend/src/page/assembly_details.rs` >= 2 (Definition + Verwendung).
    - `grep -c "AttendanceExportHeading" genossi-frontend/src/page/assembly_details.rs` >= 1.
    - `grep -c "AssemblyStatusTO::Closed" genossi-frontend/src/page/assembly_details.rs` >= 1 (Tab-Sichtbarkeits-Gate D-19).
    - `grep -c "\"export\"" genossi-frontend/src/page/assembly_details.rs` >= 2 (TabDef.key + match-arm).
    - `grep -c "create_object_url_with_blob" genossi-frontend/src/api.rs` >= 2 (existing render_template_pdf + neu export_attendance_url).
    - `cd genossi-frontend && cargo check` Exit-Code 0.
    - `cd genossi-frontend && cargo test --lib export_tab_tests` — 2 Pure-Logic-Tests gruen.
  </acceptance_criteria>
  <done>
    `ExportTab` Component existiert inline in `assembly_details.rs` (D-20),
    erscheint NUR bei Closed-GVs (D-19), bietet Format- + Include-Auswahl
    + Filename-Preview + Download-Button. `api.rs` hat die Blob-Download-Funktion
    fuer `/api/assembly/{aid}/attendance-export/{format}?include={include}`.
    Pure-Logic-Tests fuer die Datum-Extraktion sind gruen.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of Closed-GV export flow in browser</name>
  <files>(no file changes — checkpoint verifies the artifacts from Task 1 + Task 2)</files>
  <action>Run the verification steps in `<how-to-verify>` below — this is a checkpoint task, not a code-modifying task. Do not edit files; instead, pause and wait for the `resume-signal`.</action>
  <verify>
    <automated>echo "checkpoint:human-verify — manual sign-off required"</automated>
  </verify>
  <done>User has responded `approved` to the resume-signal, OR the user has reported a concrete issue and that issue has been triaged (either fixed inline or filed for a follow-up plan).</done>
  <what-built>
    Plan 04 ist fertig: i18n-Keys gesetzt (DE+EN), api.rs hat
    `export_attendance_url`, assembly_details.rs hat den `ExportTab` der nur
    bei Closed-Status erscheint, plus Format-Cards (PDF/CSV/XLSX), Include-
    Radio (Alle/Nur Anwesende), reaktive Filename-Preview und Submit-Button
    mit Loading-State.

    Die unit/lib-Tests sind alle gruen, aber die Tailwind-Compile-Pipeline +
    Browser-Click + nativer Download-Dialog laesst sich nur visuell verifizieren.
  </what-built>
  <how-to-verify>
    1. Backend starten:
       ```bash
       cargo run --bin genossi
       ```
    2. Frontend starten (separates Terminal):
       ```bash
       cd genossi-frontend && dx serve
       ```
    3. Im Browser auf das Vorstand-Frontend gehen (URL aus der `dx serve`-Ausgabe).
    4. Eine NICHT-geschlossene GV oeffnen (Status Vorbereitung oder Geoeffnet).
       VERIFIZIEREN: TabStrip zeigt drei Tabs (`Basics`, `Tokens`, `Anwesenheit`).
       **KEIN** `Export`-Tab sichtbar.
    5. Eine geschlossene GV oeffnen (per Test-Setup oder echter Closed-GV).
       VERIFIZIEREN: TabStrip zeigt jetzt VIER Tabs — der vierte heisst `Export`
       (DE) bzw. `Export` (EN — gleicher String).
    6. Klick auf `Export`-Tab.
       VERIFIZIEREN:
       - Section-Heading „Teilnehmerliste exportieren"
       - Drei Format-Karten (PDF, CSV, XLSX). PDF ist by-default selektiert
         (blauer Rand + blau-50 Hintergrund).
       - Zwei Radio-Optionen: „Alle Mitglieder (mit Anwesenheits-Spalte)" (selected)
         und „Nur Anwesende".
       - Filename-Preview unten zeigt z.B. `gv-2026-05-15-teilnehmer.pdf`.
    7. Klick auf `CSV`-Format-Card.
       VERIFIZIEREN: Filename-Preview wechselt zu `gv-2026-05-15-teilnehmer.csv`,
       blauer Rand wandert von PDF zu CSV.
    8. Klick auf `Herunterladen`-Button.
       VERIFIZIEREN:
       - Button-Label wechselt kurz zu „Wird erzeugt …"
       - Browser oeffnet nativen Download-Dialog ODER laedt die Datei direkt
         in den Downloads-Ordner.
       - Datei ist `gv-2026-05-15-teilnehmer.csv`, kann mit Excel/LibreOffice
         geoeffnet werden, zeigt Mitgliedsnummer/Nachname/Vorname/.../anwesend
         mit Semikolon-Trennung.
    9. Wiederholen mit PDF: Klick `PDF` -> `Herunterladen` -> PDF oeffnet sich
       im Viewer mit Kopf „X von Y anwesend" und der Tabelle. ✓-Glyphe in der
       letzten Spalte bei anwesenden Mitgliedern, leer sonst.
    10. Wiederholen mit XLSX: Datei in Excel/LibreOffice oeffnen — keine
        Excel-Warnung „File needs to be recovered".
    11. Wiederholen mit `include=Nur Anwesende` ausgewaehlt: CSV oder PDF
        enthaelt nur die anwesenden Mitglieder (weniger Zeilen).
    12. Sprachumschaltung auf EN (falls Locale-Switcher verfuegbar): Tab-Label
        bleibt „Export", aber Heading wechselt zu „Export attendance list",
        Buttons zu „Download" / „Generating …".
  </how-to-verify>
  <resume-signal>Antworte mit "approved" oder beschreibe Probleme</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → REST | OIDC-Cookie wird via `fetch` mitgesendet (Same-Origin) — Server prueft Permission |
| Browser memory → Filesystem | Blob-URL wird in Anchor gehaengt + clickt; Browser handhabt den Download |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-15 | I (Information Disclosure) | XSS via Filename in der UI-Preview | mitigate | Filename wird im Frontend aus statischen Strings + parsed ISO-Datum zusammengebaut; kein User-Input fliesst in den Preview. `assembly.date` ist DB-validated, kein Skript-Pfad. Render erfolgt in `code`-Element mit textContent, nicht HTML-injection. |
| T-06-16 | I (Information Disclosure) | Blob-URL-Leak bei vielen Klicks | mitigate | Nach `.click()` wird `web_sys::Url::revoke_object_url(&blob_url)` aufgerufen (siehe Action-Block). Memory-Footprint stabil. |
| T-06-17 | T (Tampering) | User-modifiziertes Frontend ruft Export auf Open-GV | accept | UI-Sichtbarkeit ist nur UX-Lack. Backend-Status-Gate (Plan 02, D-11) ist die echte Sicherheit. Auf modifiziertes Frontend antwortet der Server mit 409 — Vorstand sieht den Toast. Keine Frontend-Mitigation noetig. |
| T-06-18 | T (Tampering) | Status-Discriminator im Frontend manipulierbar | accept | Selbes Argument: Backend ist Autoritaet. Frontend zeigt nur, was Backend zulaesst. |

No HIGH-severity findings. Frontend ist UX-Schicht; Sicherheits-Garantien liegen in Plan 02 + Plan 03.
</threat_model>

<verification>
- `cd genossi-frontend && cargo check` ist gruen.
- `cd genossi-frontend && cargo test --lib export_tab_tests` ist gruen.
- `cargo build -p genossi_bin && cargo build --release -p genossi-frontend` (optional, je nach Build-Pipeline) — beide gruen.
- Manuelle Browser-Verifikation (Task 3) ist approved.
</verification>

<success_criteria>
- D-19 sichtbar — Vorstand findet den Download-Knopf im Closed-Detail-View.
- D-20 erfuellt — Component ist inline, kein neuer File unter src/component/.
- D-09 Frontend — Include-Toggle funktioniert; Default „Alle".
- D-15 Frontend-Preview — Filename-Schema ist sichtbar und reaktiv.
- DE+EN beide locales decken alle UI-Strings ab.
- Browser-Download-Flow funktioniert mit allen drei Formaten (per Checkpoint).
</success_criteria>

<output>
After completion, create `.planning/phases/06-teilnehmerlisten-export-f-r-generalversammlungen/06-04-SUMMARY.md`
</output>
