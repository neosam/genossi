---
phase: 03-attendance-aggregat-cascade-invalidation
plan: 05
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03", "03-04"]
files_modified:
  - genossi_service_impl/src/attendance.rs
  - genossi_service_impl/src/lib.rs
  - genossi_service_impl/src/assembly.rs
autonomous: true
requirements: [ASSY-04, ASSY-06, ATTN-01, ATTN-02, ATTN-03, ATTN-04, ATTN-05, ATTN-06, SYNC-02]
tags: [service-impl, permission-funnel, cascade, attendance, helper-discrimination]

threat_model:
  trust_boundaries:
    - "REST-Handler → Service: REST-Handler liefert via Authentication<Self::Context> die bereits aus Cookie/JWT extrahierte Identität. Service traut dem Auth-Layer; Plan 05 prüft NUR Authorisierung (assembly_id-Match, Status, admin-Privilege)."
    - "Service → DAO: Plan-05-Service garantiert dass DAO nur mit (a) bestehender assembly_id, (b) snapshot-membership-validierter member_id (D-27) aufgerufen wird."
    - "Cascade in close_assembly: Status-Update läuft in tx; PermissionDao::delete_session läuft pool-based AUSSERHALB der tx (Pool-vs-TX-Caveat — Conflict 2)."
  threats:
    - id: "T-03-05-01"
      category: "Elevation of Privilege"
      component: "AttendanceServiceImpl::check_assembly_access (Permission-Funnel)"
      disposition: "mitigate"
      mitigation: "ALLE 4 Endpoint-Methods (list_members, mark_present, mark_absent, stats) rufen check_assembly_access als ERSTEN Schritt nach use_transaction. Helper-Branch via ClaimContext::as_helper (Plan 03) — nur bei kind=='helper' UND helper_aid==endpoint_aid UND assembly.status==Open. Vorstand-Branch via permission_service.check_permission('admin'). Default-Drop: alle anderen Cases liefern PermissionDenied (failure-closed). Doc-Comment auf check_assembly_access verbietet Endpoint-Bypass-Aufrufe."
    - id: "T-03-05-02"
      category: "Elevation of Privilege"
      component: "Helper-cross-assembly attack"
      disposition: "mitigate"
      mitigation: "D-18-Branch: helper_aid != endpoint_aid → PermissionDenied. Helfer mit Token für GV1 kann GV2-Endpoints nicht aufrufen. Test in Plan 06 (E2E) verifiziert."
    - id: "T-03-05-03"
      category: "Tampering"
      component: "Snapshot-Membership-Check (D-27)"
      disposition: "mitigate"
      mitigation: "mark_present und mark_absent rufen attendance_dao.is_in_snapshot VOR dem UPSERT/UPDATE. Nicht-Snapshot-Member → ServiceError::EntityNotFound (404). Verhindert dass Helfer/Vorstand Anwesenheit für nicht-GV-Mitglieder erzeugen."
    - id: "T-03-05-04"
      category: "Repudiation"
      component: "Attendance ohne Audit (D-08)"
      disposition: "accept"
      mitigation: "Per User-Decision (D-08, ATTN-05) explizit ausgeschlossen — Verband fordert nur Anzahl, nicht den Vorgang. Vorstand kann via marked_by_user_id (im DAO-Row, NICHT im TO) sehen, wer den letzten Toggle-On gemacht hat. Audit-Logging für post-close-edit (ASSY-06) ist deferred (CONTEXT §Deferred)."
    - id: "T-03-05-05"
      category: "Denial of Service"
      component: "close_assembly Cascade-Loop"
      disposition: "mitigate"
      mitigation: "Cascade-Loop ist Continue-on-Error mit tracing::warn! pro Fail (Conflict 2 Resolution). Defense-in-Depth: Phase-2-D-18-Status-Check im verify_user_session (D-14 Phase 3) deckt jeden Fail-Forward-Edge-Case ab — fehlgeschlagene Session-DELETEs werden vom nächsten Helfer-Request über Status=Closed-Check abgewiesen. Status-Close (audited_update!) ist bereits committed BEVOR die Cascade läuft, also kein partial state."
    - id: "T-03-05-06"
      category: "Information Disclosure"
      component: "list_members SELECT-Whitelist"
      disposition: "mitigate"
      mitigation: "Service ruft attendance_dao.list_members_for_assembly (Plan 01) — DAO selektiert nur die 7 erlaubten Spalten via expliziter SELECT-Liste (KEIN m.*). Service mappt 1:1 in Arc<[AttendanceMemberRow]>. Plan 06 wandelt in AttendanceMemberTO mit der gleichen Whitelist."

must_haves:
  truths:
    - "D-22, D-23: AttendanceServiceImpl existiert mit Deps: AttendanceDao, AssemblyDao, MemberDao, AssemblyMemberSnapshotDao, PermissionService, PermissionDao, TransactionDao (KEIN UuidService, KEIN AuditLogDao)"
    - "D-17, D-18: AttendanceServiceImpl::check_assembly_access lebt im AttendanceServiceImpl (NICHT im PermissionService) und führt 3-Branch-Logic aus: Authentication::Full → Ok; ctx.as_helper Some(helper_aid) → Match aid + Status=Open Check; sonst → permission_service.check_permission(admin)"
    - "AttendanceServiceImpl::list_members → check_assembly_access → attendance_dao.list_members_for_assembly → Ok"
    - "D-27: AttendanceServiceImpl::mark_present → check_assembly_access → is_in_snapshot (404 sonst) → upsert_present mit marked_by_user_id aus permission_service.current_user_id"
    - "D-27: AttendanceServiceImpl::mark_absent → check_assembly_access → is_in_snapshot (404 sonst) → soft_delete (idempotent, kein Error bei 0-rows)"
    - "AttendanceServiceImpl::stats → check_assembly_access → count_present_by_assembly + assembly_member_snapshot_dao.count_by_assembly_id → AttendanceStats { present, total }"
    - "D-25: AttendanceServiceImpl::list_members reicht den optionalen search-String 1:1 an attendance_dao.list_members_for_assembly weiter — KEIN Memory-Filter im Service (Substring-Suche bleibt im DAO)"
    - "D-19, D-20: Vorstand-Branch (admin-Privilege) reicht ohne Status-Check; Vorstand kann Post-Close-Edit auf attendance ausführen, GV bleibt Closed"
    - "D-08: KEINE audited_*!-Macros in AttendanceServiceImpl — auch nicht für Vorstand-Post-Close-Edit"
    - "D-16: AssemblyServiceImpl bekommt 2 neue Deps: HelperTokenDao (für list_session_ids_for_assembly) + PermissionDao (für delete_session)"
    - "D-11, D-13, D-15: AssemblyServiceImpl::close_assembly cascade-erweitert — nach audited_update! → list_session_ids_for_assembly (in tx) → COMMIT tx → for sid in ids: permission_dao.delete_session(sid) (pool, continue-on-error mit tracing::warn! per RESEARCH-Conflict-2-Resolution)"
    - "D-14: Phase-2-Status-Check im verify_user_session bleibt als Defense-in-Depth bestehen"
    - "D-11: close_assembly bekommt aktive Cascade-Erweiterung — alle Helfer-Sessions dieser GV werden nach dem audited_update! invalidiert (erfüllt ROADMAP-Phase-3-SC#8)"
    - "D-13: KEIN HelperSessionService-Wrapper — AssemblyServiceImpl orchestriert direkt list_session_ids_for_assembly + permission_dao.delete_session-Loop"
    - "D-17: check_assembly_access lebt EXKLUSIV im AttendanceServiceImpl, NICHT im PermissionService — vermeidet Aggregat-Dependencies im PermissionService"
    - "D-19: admin-Privilege reicht für Vorstand-Branch — KEINE neue Privilege-Konstante (kein attendance.access)"
    - "D-22: AttendanceService-Trait hat genau 4 Methods (list_members, mark_present, mark_absent, stats) und folgt dem Genossi-Service-Pattern"
    - "Bestehende Phase-1-close_assembly-Tests (test_close_assembly_from_preparation_returns_conflict) bleiben grün"
  artifacts:
    - path: "genossi_service_impl/src/attendance.rs"
      provides: "AttendanceServiceImpl + check_assembly_access + 4 Methods + Tests"
      exports: ["AttendanceServiceImpl", "AttendanceServiceDeps"]
    - path: "genossi_service_impl/src/lib.rs"
      provides: "pub mod attendance"
      contains: "pub mod attendance"
    - path: "genossi_service_impl/src/assembly.rs"
      provides: "AssemblyServiceImpl mit Cascade-Erweiterung in close_assembly + 2 neue Deps"
      contains: "list_session_ids_for_assembly"
  key_links:
    - from: "AttendanceServiceImpl::check_assembly_access"
      to: "ClaimContext::as_helper"
      via: "ctx.as_helper() für Helper-Discrimination"
      pattern: "as_helper"
    - from: "AttendanceServiceImpl::mark_present"
      to: "AttendanceDao::is_in_snapshot + upsert_present"
      via: "Snapshot-Check (D-27) + UPSERT (D-05)"
      pattern: "is_in_snapshot.*upsert_present"
    - from: "AssemblyServiceImpl::close_assembly"
      to: "HelperTokenDao::list_session_ids_for_assembly + PermissionDao::delete_session"
      via: "Cascade-Loop nach Commit"
      pattern: "list_session_ids_for_assembly.*for.*delete_session"
---

<objective>
Plan 05 ist die Service-Logik-Schwerpunkt-Plan dieser Phase. Er liefert AttendanceServiceImpl (alle 4 Endpoint-Methods + zentrale Permission-Funnel `check_assembly_access`) UND erweitert AssemblyServiceImpl::close_assembly um die Cascade-Invalidation (D-11 bis D-15, mit Conflict-2-Resolution: Commit BEFORE pool-based delete_session). Beide Verantwortlichkeiten landen im selben Plan, weil die neue HelperTokenDao-Dep auf AssemblyServiceImpl und die neue ClaimContext::as_helper-Dependency auf AttendanceServiceImpl beide aus Wave 1 (Plan 02 + 03) kommen, und beide Service-Implementierungen DenWandelMatch-Tests-Refaktoring im selben `assembly.rs::tests`-Modul brauchen (handgeschriebene mocks für TestHelperTokenDao + TestPermissionDao — Pitfall 4).

Implementiert die User-Decisions D-08 (kein Audit), D-11 (Cascade in close_assembly), D-13 (kein HelperSessionService-Wrapper, AssemblyServiceImpl orchestriert direkt), D-14 (Defense-in-Depth Status-Check bleibt), D-15 (tx-Reihenfolge mit RESEARCH-Conflict-2 commit-vor-pool-loop continue-on-error), D-16 (HelperTokenDao + PermissionDao als neue AssemblyServiceDeps), D-17 (check_assembly_access lebt im AttendanceServiceImpl), D-18 (Helper-Branch Implementation: aid-Match + Status=Open), D-19 (admin-Privilege reicht), D-20 (Post-Close-Edit für admin, kein Status-Check), D-22 (4 AttendanceService-Methods), D-23 (AttendanceServiceImpl-Deps via gen_service_impl!), D-25 (Substring-Search wird vom Service 1:1 ans DAO weitergereicht), D-26 (ServiceError-Mapping zur HTTP-Status-Code-Brücke), D-27 (snapshot-membership-check vor jedem mark_*-DAO-Aufruf).

Purpose: Schließt 8 von 9 Phase-Requirements (alle ATTN-* + ASSY-04 + ASSY-06 + SYNC-02 — letztere nur auf Service-Ebene; der eigentliche Race-Test landet als E2E in Plan 06). Der einzige nicht abgedeckte Requirement ist ASSY-03 (das ist Phase-1-Cascade-Trigger im close_assembly, hier durch Plan 02 + 05 zusammen geliefert).
Output: Eine neue Service-Impl-Datei + Erweiterung der bestehenden assembly.rs-Datei + Modul-Tests (Service-Layer-Unit-Tests; E2E-Tests landen in Plan 06).
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-attendance-aggregat-cascade-invalidation/03-CONTEXT.md
@.planning/phases/03-attendance-aggregat-cascade-invalidation/03-RESEARCH.md
@.planning/phases/03-attendance-aggregat-cascade-invalidation/03-PATTERNS.md
@.planning/phases/03-attendance-aggregat-cascade-invalidation/03-01-SUMMARY.md
@.planning/phases/03-attendance-aggregat-cascade-invalidation/03-02-SUMMARY.md
@.planning/phases/03-attendance-aggregat-cascade-invalidation/03-03-SUMMARY.md
@.planning/phases/03-attendance-aggregat-cascade-invalidation/03-04-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- AssemblyServiceImpl gen_service_impl-Block (Bestand, Phase 1): -->
From genossi_service_impl/src/assembly.rs:50-60:
```rust
gen_service_impl! {
    struct AssemblyServiceImpl: AssemblyService = AssemblyServiceDeps {
        AssemblyDao: AssemblyDao<Transaction = Self::Transaction> = assembly_dao,
        AssemblyMemberSnapshotDao: AssemblyMemberSnapshotDao<Transaction = Self::Transaction> = assembly_member_snapshot_dao,
        MemberDao: MemberDao<Transaction = Self::Transaction> = member_dao,
        AuditLogDao: AuditLogDao<Transaction = Self::Transaction> = audit_log_dao,
        PermissionService: PermissionService<Context = Self::Context> = permission_service,
        UuidService: UuidService = uuid_service,
        TransactionDao: TransactionDao<Transaction = Self::Transaction> = transaction_dao,
    }
}
```

<!-- close_assembly bestehender Body (Phase 1): -->
genossi_service_impl/src/assembly.rs::close_assembly (lines ~254-304) nutzt bereits:
- self.transaction_dao.use_transaction(None)
- self.permission_service.current_user_id(context.clone())
- self.permission_service.check_permission(ADMIN_PRIVILEGE, context)
- self.assembly_dao.find_by_id(id, tx.clone())
- Status-Check `entity.status != AssemblyStatus::Open`
- crate::audited_update!(self, self.assembly_dao, id, &entity, ASSEMBLY_PROCESS_CLOSE, &user_id, tx);
- self.transaction_dao.commit(tx)

<!-- ClaimContext::as_helper aus Plan 03: -->
fn as_helper(&self) -> Option<Uuid> { None }  // default
// AuthenticatedContext-Override liefert Some(assembly_id) bei kind=='helper'-claims.
// HINWEIS: Phase-2-Claims-Schema (genossi_service_impl/src/session.rs:17-30) ist
// `{"kind":"helper","assembly_id":"<uuid>"}` — KEIN session_id-Feld. Die session_id
// ist die SessionEntity-row-id und wird hier NICHT benötigt; check_assembly_access
// braucht nur die assembly_id für aid-Match + Status-Check. Cascade-side session
// enumeration in close_assembly läuft über HelperTokenDao::list_session_ids_for_assembly.

<!-- AttendanceDao Methods aus Plan 01: -->
- upsert_present(aid, mid, marked_at, marked_by_user_id, tx) -> Result<(), DaoError>
- soft_delete(aid, mid, deleted_at, tx) -> Result<(), DaoError>
- list_members_for_assembly(aid, search: Option<&str>, tx) -> Result<Arc<[AttendanceMemberRow]>, DaoError>
- count_present_by_assembly(aid, tx) -> Result<u64, DaoError>
- is_in_snapshot(aid, mid, tx) -> Result<bool, DaoError>

<!-- HelperTokenDao::list_session_ids_for_assembly aus Plan 02: -->
async fn list_session_ids_for_assembly(&self, assembly_id: Uuid, tx: Self::Transaction) -> Result<Vec<Arc<str>>, DaoError>;

<!-- PermissionDao::delete_session aus genossi_dao/src/permission.rs:90: -->
async fn delete_session(&self, session_id: &str) -> Result<(), DaoError>;
// !!! KEIN tx-Argument — pool-basiert, MUSS außerhalb der tx aufgerufen werden (Conflict 2).

<!-- AssemblyMemberSnapshotDao::count_by_assembly_id (Phase 1): -->
async fn count_by_assembly_id(&self, assembly_id: Uuid, tx: Self::Transaction) -> Result<u64, DaoError>;

<!-- AttendanceService Trait + AttendanceStats aus Plan 04: -->
async fn list_members(&self, assembly_id, search: Option<String>, ctx) -> Result<Arc<[AttendanceMemberRow]>, ServiceError>;
async fn mark_present(&self, assembly_id, member_id, ctx) -> Result<(), ServiceError>;
async fn mark_absent(&self, assembly_id, member_id, ctx) -> Result<(), ServiceError>;
async fn stats(&self, assembly_id, ctx) -> Result<AttendanceStats, ServiceError>;

<!-- gen_service_impl-Macro aus genossi_service_impl/src/macros.rs: -->
// Generiert struct AttendanceServiceImpl<Deps> mit Dep-Felder + AttendanceServiceDeps-Trait.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AttendanceServiceImpl + check_assembly_access + 4 Methods + Unit-Tests</name>
  <files>
    genossi_service_impl/src/attendance.rs,
    genossi_service_impl/src/lib.rs
  </files>
  <read_first>
    - genossi_service_impl/src/assembly.rs (komplette Datei, insbesondere lines 1-110 für gen_service_impl + create_assembly-Body, lines 254-304 für close_assembly-Body, lines 350-700 für mock!-Pattern in Tests)
    - genossi_service_impl/src/macros.rs (gen_service_impl + audited_*! Macros — wir nutzen GenServiceImpl, KEIN audited_*!)
    - genossi_service_impl/src/permission.rs (lines 1-60 — check_permission Match-Shape; Vorbild für check_assembly_access-Branching)
    - genossi_service/src/attendance.rs (Plan 04 — Trait-Signaturen)
    - genossi_service/src/claim_context.rs (Plan 03 — as_helper-Method)
    - genossi_service/src/auth_types.rs (Authentication-Enum)
    - genossi_dao/src/attendance.rs (Plan 01 — DAO-Trait + AttendanceMemberRow)
    - .planning/phases/03-attendance-aggregat-cascade-invalidation/03-CONTEXT.md (D-17, D-18, D-19, D-20, D-22, D-23, D-26, D-27)
    - .planning/phases/03-attendance-aggregat-cascade-invalidation/03-RESEARCH.md §Konkrete Code-Recommendations §AttendanceServiceImpl::mark_present + mark_absent (lines 1056-1136); §Pattern 6 (Permission-Funnel); §Pitfall 4 (handgeschriebene mocks)
    - .planning/phases/03-attendance-aggregat-cascade-invalidation/03-PATTERNS.md §"genossi_service_impl/src/attendance.rs" (lines 270-378)
  </read_first>
  <behavior>
    - Test 1 `test_check_assembly_access_full_authentication_returns_ok`: ctx = Authentication::Full → check_assembly_access(any_aid, ctx) liefert Ok(_assembly).
    - Test 2 `test_check_assembly_access_helper_matching_aid_open_returns_ok`: ctx = Authentication::Context(TestContext mit user_id "helper:abc") UND TestContext::as_helper liefert Some(aid) UND assembly.status == Open → liefert Ok(assembly).
    - Test 3 `test_check_assembly_access_helper_wrong_aid_returns_permission_denied`: helper_aid != endpoint_aid → ServiceError::PermissionDenied.
    - Test 4 `test_check_assembly_access_helper_assembly_closed_returns_permission_denied`: helper_aid==endpoint_aid aber assembly.status==Closed → ServiceError::PermissionDenied.
    - Test 5 `test_check_assembly_access_admin_pass_through_no_status_check`: ctx via permission_service.check_permission("admin")=Ok, assembly.status==Closed → liefert Ok (D-20: admin Post-Close-Edit erlaubt).
    - Test 6 `test_check_assembly_access_admin_denied_returns_permission_denied`: ctx, permission_service.check_permission("admin")=Err(PermissionDenied) → ServiceError::PermissionDenied.
    - Test 7 `test_check_assembly_access_unknown_assembly_returns_entity_not_found`: assembly_dao.find_by_id liefert None → ServiceError::EntityNotFound.
    - Test 8 `test_mark_present_idempotent_calls_upsert_with_synthetic_user_id`: Setup-Mock erwartet `attendance_dao.is_in_snapshot=true` und `attendance_dao.upsert_present(_, _, _, "helper:abc-token", _)` 1×; Result Ok(()).
    - Test 9 `test_mark_present_member_not_in_snapshot_returns_404`: is_in_snapshot=false → ServiceError::EntityNotFound(member_id); upsert_present darf NICHT aufgerufen werden.
    - Test 10 `test_mark_absent_idempotent_no_error_on_no_op`: is_in_snapshot=true, soft_delete=Ok(()) → Result Ok(()).
    - Test 11 `test_mark_absent_member_not_in_snapshot_returns_404`: is_in_snapshot=false → EntityNotFound.
    - Test 12 `test_list_members_returns_dao_result_unmodified`: list_members_for_assembly liefert Arc-of-3-Rows → Service liefert exakt diese Arc weiter.
    - Test 13 `test_list_members_passes_search_string_to_dao`: ctx mit search=Some("Müller") → mock erwartet `list_members_for_assembly(_, Some("Müller"), _)` aufgerufen.
    - Test 14 `test_stats_combines_present_and_total_counts`: count_present_by_assembly=3, count_by_assembly_id (snapshot)=10 → AttendanceStats { present: 3, total: 10 }.
  </behavior>
  <action>
**Schritt 1 — Datei `genossi_service_impl/src/attendance.rs` anlegen.**

Module-Header:

```rust
use std::sync::Arc;

use async_trait::async_trait;
use uuid::Uuid;

use genossi_dao::assembly::{AssemblyEntity, AssemblyStatus};
use genossi_dao::attendance::{AttendanceDao, AttendanceMemberRow};
use genossi_dao::assembly::AssemblyDao;
use genossi_dao::assembly_member_snapshot::AssemblyMemberSnapshotDao;
use genossi_dao::member::MemberDao;
use genossi_dao::TransactionDao;

use genossi_service::attendance::{AttendanceService, AttendanceStats};
use genossi_service::claim_context::ClaimContext;
use genossi_service::permission::{Authentication, PermissionService};
use genossi_service::ServiceError;

use crate::gen_service_impl;

const ADMIN_PRIVILEGE: &str = "admin";
```

**Schritt 2 — gen_service_impl-Skelett** (D-23: 6 Deps, KEIN UuidService, KEIN AuditLogDao):

```rust
gen_service_impl! {
    struct AttendanceServiceImpl: AttendanceService = AttendanceServiceDeps {
        AttendanceDao: AttendanceDao<Transaction = Self::Transaction> = attendance_dao,
        AssemblyDao: AssemblyDao<Transaction = Self::Transaction> = assembly_dao,
        MemberDao: MemberDao<Transaction = Self::Transaction> = member_dao,
        AssemblyMemberSnapshotDao: AssemblyMemberSnapshotDao<Transaction = Self::Transaction> = assembly_member_snapshot_dao,
        PermissionService: PermissionService<Context = Self::Context> = permission_service,
        TransactionDao: TransactionDao<Transaction = Self::Transaction> = transaction_dao,
    }
}
```

**Schritt 3 — `impl AttendanceService for AttendanceServiceImpl<Deps>`** mit allen 4 Methods + check_assembly_access:

**check_assembly_access** (D-17, D-18 — zentraler Permission-Funnel; gibt Assembly zurück damit Caller nicht erneut laden müssen):

```rust
impl<Deps: AttendanceServiceDeps> AttendanceServiceImpl<Deps>
where
    Deps::Context: ClaimContext,
{
    /// Permission funnel for ALL 4 attendance endpoints (D-17/D-18).
    ///
    /// Returns the loaded AssemblyEntity so the caller doesn't re-load it.
    /// Errors:
    ///   ServiceError::EntityNotFound(assembly_id) — assembly not found.
    ///   ServiceError::PermissionDenied — helper-aid mismatch / status != Open
    ///                                    / non-admin without helper-claim.
    async fn check_assembly_access(
        &self,
        assembly_id: Uuid,
        context: Authentication<Deps::Context>,
        tx: Deps::Transaction,
    ) -> Result<AssemblyEntity, ServiceError> {
        // Always load the assembly first — needed for both Helper-status-check
        // and EntityNotFound-mapping.
        let assembly = self
            .assembly_dao
            .find_by_id(assembly_id, tx.clone())
            .await?
            .ok_or(ServiceError::EntityNotFound(assembly_id))?;

        match &context {
            Authentication::Full => Ok(assembly),
            Authentication::Context(ctx) => {
                // Helper discrimination via ClaimContext::as_helper (Plan 03).
                if let Some(helper_aid) = ctx.as_helper() {
                    if helper_aid != assembly_id {
                        return Err(ServiceError::PermissionDenied);
                    }
                    if assembly.status != AssemblyStatus::Open {
                        return Err(ServiceError::PermissionDenied);
                    }
                    return Ok(assembly);
                }
                // Vorstand-branch: admin privilege via PermissionService.
                // D-20: KEIN Status-Check — admin darf Post-Close-Edit (ASSY-06).
                self.permission_service
                    .check_permission(ADMIN_PRIVILEGE, context)
                    .await?;
                Ok(assembly)
            }
        }
    }
}
```

**Service-Impl-Block** mit den 4 Methods. **Wichtig: Trait-Bound `Deps::Context: ClaimContext` muss auf den `impl AttendanceService`-Block ebenfalls** (siehe AssemblyService-Pattern):

```rust
#[async_trait]
impl<Deps: AttendanceServiceDeps> AttendanceService for AttendanceServiceImpl<Deps>
where
    Deps::Context: ClaimContext,
{
    type Context = Deps::Context;
    type Transaction = Deps::Transaction;

    async fn list_members(
        &self,
        assembly_id: Uuid,
        search: Option<String>,
        context: Authentication<Self::Context>,
    ) -> Result<Arc<[AttendanceMemberRow]>, ServiceError> {
        let tx = self.transaction_dao.use_transaction(None).await?;
        let _assembly = self.check_assembly_access(assembly_id, context, tx.clone()).await?;

        let rows = self
            .attendance_dao
            .list_members_for_assembly(assembly_id, search.as_deref(), tx.clone())
            .await?;

        self.transaction_dao.commit(tx).await?;
        Ok(rows)
    }

    async fn mark_present(
        &self,
        assembly_id: Uuid,
        member_id: Uuid,
        context: Authentication<Self::Context>,
    ) -> Result<(), ServiceError> {
        let tx = self.transaction_dao.use_transaction(None).await?;
        let _assembly = self.check_assembly_access(assembly_id, context.clone(), tx.clone()).await?;

        // D-27: snapshot membership check.
        if !self
            .attendance_dao
            .is_in_snapshot(assembly_id, member_id, tx.clone())
            .await?
        {
            return Err(ServiceError::EntityNotFound(member_id));
        }

        // marked_by_user_id: helper:<token_id> ODER OIDC-User-ID (Phase 2 D-17).
        let user_id = self
            .permission_service
            .current_user_id(context)
            .await?
            .unwrap_or_else(|| "SYSTEM".to_string());

        let now_offset = time::OffsetDateTime::now_utc();
        let now = time::PrimitiveDateTime::new(now_offset.date(), now_offset.time());

        self.attendance_dao
            .upsert_present(assembly_id, member_id, now, &user_id, tx.clone())
            .await?;

        self.transaction_dao.commit(tx).await?;
        Ok(())
    }

    async fn mark_absent(
        &self,
        assembly_id: Uuid,
        member_id: Uuid,
        context: Authentication<Self::Context>,
    ) -> Result<(), ServiceError> {
        let tx = self.transaction_dao.use_transaction(None).await?;
        let _assembly = self.check_assembly_access(assembly_id, context, tx.clone()).await?;

        if !self
            .attendance_dao
            .is_in_snapshot(assembly_id, member_id, tx.clone())
            .await?
        {
            return Err(ServiceError::EntityNotFound(member_id));
        }

        let now_offset = time::OffsetDateTime::now_utc();
        let now = time::PrimitiveDateTime::new(now_offset.date(), now_offset.time());

        self.attendance_dao
            .soft_delete(assembly_id, member_id, now, tx.clone())
            .await?;

        self.transaction_dao.commit(tx).await?;
        Ok(())
    }

    async fn stats(
        &self,
        assembly_id: Uuid,
        context: Authentication<Self::Context>,
    ) -> Result<AttendanceStats, ServiceError> {
        let tx = self.transaction_dao.use_transaction(None).await?;
        let _assembly = self.check_assembly_access(assembly_id, context, tx.clone()).await?;

        let present = self
            .attendance_dao
            .count_present_by_assembly(assembly_id, tx.clone())
            .await?;
        let total = self
            .assembly_member_snapshot_dao
            .count_by_assembly_id(assembly_id, tx.clone())
            .await?;

        self.transaction_dao.commit(tx).await?;
        Ok(AttendanceStats { present, total })
    }
}
```

**Schritt 4 — Test-Modul** am Ende der Datei. Reuse das Pattern aus `genossi_service_impl/src/assembly.rs::tests` (lines 350-700+). Definitionen, die du brauchst:

- `TestTransaction` (reuse falls möglich aus assembly.rs::tests via gemeinsamem `pub(crate)`-Helper-Modul; sonst dupliziere die Definition).
- `TestContext` (struct mit user_id + claims + ClaimContext-impl). Wichtig: `as_helper`-Override für Test 2 muss konfigurierbar sein. Empfohlen: `TestContext { user_id: Arc<str>, helper_claim: Option<Uuid> }` + `impl ClaimContext for TestContext { fn has_claims(&self) -> bool { false } fn as_helper(&self) -> Option<Uuid> { self.helper_claim } }`. (Hinweis: Plan-03-Trait-Signatur ist `Option<Uuid>` — die session_id ist NICHT Teil des Claims-Schemas, siehe Plan 03 + genossi_service_impl/src/session.rs:17-30.)
- `mock!{}`-Blocks für `TestAttendanceDao`, `TestAssemblyDao` (kann von assembly.rs reused werden falls man ein gemeinsames test_helpers-Modul macht — sonst duplizieren), `TestMemberDao`, `TestAssemblyMemberSnapshotDao`, `TestPermissionService`, `TestTxDao`. Alle gegen TestTransaction.
- `TestDeps`-Struct + `impl AttendanceServiceDeps for TestDeps`.

Schreibe die 14 Tests aus `<behavior>` als `#[tokio::test]`-Funktionen mit Mock-Setups. Beispiel für Test 8:

```rust
#[tokio::test]
async fn test_mark_present_idempotent_calls_upsert_with_synthetic_user_id() {
    let aid = Uuid::new_v4();
    let mid = Uuid::new_v4();

    let mut tx_dao = MockTestTxDao::new();
    tx_dao.expect_use_transaction().returning(|_| Ok(TestTransaction));
    tx_dao.expect_commit().returning(|_| Ok(()));

    let mut assembly_dao = MockTestAssemblyDao::new();
    let assembly = AssemblyEntity {
        id: aid,
        // ... build via existing helper from assembly.rs::tests
        status: AssemblyStatus::Open,
        // ...
    };
    let assembly_clone = assembly.clone();
    assembly_dao.expect_find_by_id()
        .returning(move |_id, _tx| Ok(Some(assembly_clone.clone())));

    let mut perm = MockTestPermissionService::new();
    perm.expect_current_user_id()
        .returning(|_| Ok(Some("helper:abc-token".to_string())));
    // For Helper-branch — check_permission should NOT be called when ctx.as_helper() = Some.
    // Mockall default: any unexpected expect_check_permission call is a panic; if our
    // helper-branch is correct, we don't set it — Test fails if helper-discrimination broken.

    let mut attendance_dao = MockTestAttendanceDao::new();
    attendance_dao.expect_is_in_snapshot()
        .returning(|_aid, _mid, _tx| Ok(true));
    attendance_dao.expect_upsert_present()
        .withf(|_aid, _mid, _t, by, _tx| by == "helper:abc-token")
        .times(1)
        .returning(|_, _, _, _, _| Ok(()));

    let svc = build_test_service(attendance_dao, assembly_dao, /* others */, perm, tx_dao);
    let ctx = Authentication::Context(TestContext {
        user_id: Arc::from("helper:abc-token"),
        helper_claim: Some(aid),
    });
    let result = svc.mark_present(aid, mid, ctx).await;
    assert!(result.is_ok(), "expected Ok(()), got {:?}", result);
}
```

**Konkretes Mock-Setup für Test 4 (helper-aid match aber assembly.status=Closed → PermissionDenied)** — W-2-Fix: subtile Permutation gegenüber Test 2/8, weil der Helper-Branch bis zum Status-Check kommt und DANN ablehnt:

```rust
#[tokio::test]
async fn test_check_assembly_access_helper_assembly_closed_returns_permission_denied() {
    let aid = Uuid::new_v4();

    let mut tx_dao = MockTestTxDao::new();
    tx_dao.expect_use_transaction().returning(|_| Ok(TestTransaction));
    // KEIN expect_commit() — die Methode bricht VOR dem commit ab (PermissionDenied
    // bubblet aus check_assembly_access via list_members/mark_*/stats und der Caller
    // committed nicht). Falls dein Helper-Pattern den commit immer aufruft, dann
    // .expect_commit().times(0..=1) — siehe assembly.rs::tests Konvention.

    let mut assembly_dao = MockTestAssemblyDao::new();
    let assembly = AssemblyEntity {
        id: aid,
        status: AssemblyStatus::Closed,  // <-- Schlüssel-Permutation
        // ... weitere Felder via build_test_assembly(aid, AssemblyStatus::Closed)
    };
    let assembly_clone = assembly.clone();
    assembly_dao.expect_find_by_id()
        .with(eq(aid), always())
        .times(1)
        .returning(move |_, _| Ok(Some(assembly_clone.clone())));

    // PermissionService darf NICHT aufgerufen werden — der Helper-Branch greift
    // bevor der admin-Fallback erreicht wird. MockTestPermissionService::new()
    // ohne expect_check_permission → bei unerwartetem Aufruf panic'd mockall.
    let perm = MockTestPermissionService::new();

    // attendance_dao wird in check_assembly_access nicht touched.
    let attendance_dao = MockTestAttendanceDao::new();

    // Wir testen check_assembly_access am einfachsten indirekt via list_members
    // (rufst die kürzeste Endpoint-Method, weil check_assembly_access privat ist).
    let svc = build_test_service(attendance_dao, assembly_dao, /* member_dao */ MockTestMemberDao::new(),
        /* snapshot_dao */ MockTestSnapshotDao::new(), perm, tx_dao);

    let ctx = Authentication::Context(TestContext {
        user_id: Arc::from("helper:abc-token"),
        helper_claim: Some(aid),  // helper_aid == endpoint_aid → kommt zum Status-Check
    });

    let result = svc.list_members(aid, None, ctx).await;
    assert!(matches!(result, Err(ServiceError::PermissionDenied)),
        "expected PermissionDenied (helper-aid match aber Status=Closed), got {:?}", result);
}
```

**Konkretes Mock-Setup für Test 5 (admin pass-through mit assembly.status=Closed → Ok, KEIN Status-Check für admin — D-20)** — W-2-Fix: muss explizit zeigen dass kein Status-Check stattfindet:

```rust
#[tokio::test]
async fn test_check_assembly_access_admin_pass_through_no_status_check() {
    let aid = Uuid::new_v4();

    let mut tx_dao = MockTestTxDao::new();
    tx_dao.expect_use_transaction().returning(|_| Ok(TestTransaction));
    tx_dao.expect_commit().returning(|_| Ok(()));

    let mut assembly_dao = MockTestAssemblyDao::new();
    let assembly = AssemblyEntity {
        id: aid,
        status: AssemblyStatus::Closed,  // <-- D-20: admin darf trotzdem
        // ... weitere Felder
    };
    let assembly_clone = assembly.clone();
    assembly_dao.expect_find_by_id()
        .with(eq(aid), always())
        .times(1)
        .returning(move |_, _| Ok(Some(assembly_clone.clone())));

    let mut perm = MockTestPermissionService::new();
    // ADMIN-Branch wird erreicht (helper_claim=None → kein Helper-Branch).
    // Wichtig: D-20 — KEIN Status-Check für admin, also passiert die Method
    // OBWOHL status=Closed.
    perm.expect_check_permission()
        .withf(|priv_str, _ctx| priv_str == "admin")
        .times(1)
        .returning(|_, _| Ok(()));

    // list_members ruft list_members_for_assembly nach erfolgreichem check_assembly_access
    let mut attendance_dao = MockTestAttendanceDao::new();
    attendance_dao.expect_list_members_for_assembly()
        .returning(|_, _, _| Ok(Arc::from(vec![]) as Arc<[AttendanceMemberRow]>));

    let svc = build_test_service(attendance_dao, assembly_dao, MockTestMemberDao::new(),
        MockTestSnapshotDao::new(), perm, tx_dao);

    let ctx = Authentication::Context(TestContext {
        user_id: Arc::from("admin-user"),
        helper_claim: None,  // <-- KEIN Helper-Claim → admin-Branch
    });

    let result = svc.list_members(aid, None, ctx).await;
    assert!(result.is_ok(),
        "D-20: admin muss list_members auch bei Status=Closed bekommen, got {:?}", result);
}
```

(Die übrigen Tests 1, 3, 6, 7, 9-14 folgen demselben Muster: Mock-Setup gemäß `<behavior>`-Beschreibung, Aufruf der entsprechenden Endpoint-Method, Assertion auf Ok/Err. Für Tests die check_assembly_access indirekt testen — also alle ausser den reinen list/mark/stats-Tests — wähle list_members als Aufrufpfad, weil es die wenigsten DAO-Mocks braucht.)

**Schritt 5 — `genossi_service_impl/src/lib.rs` modifizieren**: füge `pub mod attendance;` hinzu — alphabetisch nach `pub mod assembly;` einsortieren. Falls das Modul `pub use ...`-Re-Exports macht (siehe `pub use session::{SessionServiceImpl, MockSessionServiceImpl};`), füge analog hinzu: `pub use attendance::{AttendanceServiceImpl, AttendanceServiceDeps};`.

**Was NICHT machen:**
- KEINE audited_*!-Macros in irgendeiner Method dieser Datei (D-08, ATTN-05).
- KEIN Audit-Process-Konstante (kein `ATTENDANCE_PROCESS_*: &str`).
- KEINE Seitenefekte BEFORE check_assembly_access — der Funnel ist immer der erste DAO-touchende Schritt nach use_transaction (verhindert Information-Disclosure-Bypass).
- KEIN `permission_service.check_permission` für die Helper-Branch — Helper-Discrimination passiert NUR über `ctx.as_helper()`, nicht über privilege-strings.
- KEIN Status-Check für die admin-Branch (D-20).
- KEIN bypass von `is_in_snapshot` in mark_absent — auch wenn idempotent, ist der Snapshot-Check Teil der Permission-Surface (D-27 + D-26 fordert 404 für nicht-snapshot-mid).
  </action>
  <verify>
    <automated>cargo build -p genossi_service_impl 2>&amp;1 | tail -20 &amp;&amp; cargo test -p genossi_service_impl attendance --no-fail-fast 2>&amp;1 | tail -60</automated>
  </verify>
  <done>
    - `cargo build -p genossi_service_impl` exits 0.
    - `cargo test -p genossi_service_impl attendance` exits 0; mindestens 14 Tests laufen.
    - `grep -c 'pub mod attendance' genossi_service_impl/src/lib.rs` = 1.
    - `grep -c 'fn check_assembly_access' genossi_service_impl/src/attendance.rs` = 1.
    - `grep -c 'as_helper' genossi_service_impl/src/attendance.rs` ≥ 1.
    - `grep -c 'AttendanceStatus::Open\|AssemblyStatus::Open' genossi_service_impl/src/attendance.rs` ≥ 1 (Helper-Branch Status-Check).
    - `grep -c 'is_in_snapshot' genossi_service_impl/src/attendance.rs` ≥ 2 (mark_present + mark_absent).
    - `grep -c 'audited_create\|audited_update\|audited_delete\|Auditable' genossi_service_impl/src/attendance.rs` = 0 (D-08).
    - `grep -c 'check_permission(ADMIN_PRIVILEGE\|check_permission("admin"' genossi_service_impl/src/attendance.rs` ≥ 1 (admin-Branch).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AssemblyServiceImpl Cascade-Erweiterung in close_assembly + 2 neue Deps</name>
  <files>
    genossi_service_impl/src/assembly.rs
  </files>
  <read_first>
    - genossi_service_impl/src/assembly.rs (komplette Datei — gen_service_impl-Block lines 50-60, close_assembly-Body lines ~254-304, Test-Mock-Pattern lines 350-700+ inklusive `mock! { TestAssemblyDao }`-Blöcke und TestDeps-Wiring)
    - genossi_service_impl/src/helper_token.rs (lines 316-325 — Pool-vs-TX-Caveat-Comment-Pattern; Vorbild für die Continue-on-Error tracing::warn!-Strategie)
    - genossi_dao/src/helper_token.rs (Plan 02 — list_session_ids_for_assembly-Trait-Method)
    - genossi_dao/src/permission.rs (lines 87-95 — delete_session-Signatur ohne tx)
    - .planning/phases/03-attendance-aggregat-cascade-invalidation/03-CONTEXT.md (D-11 bis D-16)
    - .planning/phases/03-attendance-aggregat-cascade-invalidation/03-RESEARCH.md §DECISION CONFLICT 2 (lines 547-580); §Pattern 5 (lines 367-385); §Konkrete Code-Recommendations §AssemblyServiceImpl::close_assembly Cascade (lines 1138-1213); §Pitfall 4 (handgeschriebene mocks lines 1299-1340)
    - .planning/phases/03-attendance-aggregat-cascade-invalidation/03-PATTERNS.md §"genossi_service_impl/src/assembly.rs (MODIFY)" (lines 600-651)
  </read_first>
  <behavior>
    - Test 1 `test_close_assembly_cascades_to_all_helper_sessions`: Setup mit Status=Open + 3 helper-Sessions ["s1", "s2", "s3"]; mock list_session_ids_for_assembly liefert diese 3; mock delete_session erwartet 3× Aufruf jeweils mit "s1"/"s2"/"s3"; Result Ok(_assembly) und assembly.status == Closed.
    - Test 2 `test_close_assembly_continues_on_delete_session_error`: list_session_ids = ["s1", "s2"]; delete_session("s1") liefert Err(DaoError::DatabaseError("simulated")), delete_session("s2") liefert Ok(()); Result Ok(_) (kein Error gepropagiert — Continue-on-Error per Conflict 2 Resolution); audited_update! wurde 1× aufgerufen (close-Audit-Entry persistent).
    - Test 3 `test_close_assembly_empty_session_list_succeeds`: list_session_ids = []; delete_session wird 0× aufgerufen; Result Ok(_).
    - Test 4 `test_close_assembly_from_preparation_returns_conflict_unchanged`: bestehender Phase-1-Test (assembly.status=Preparation) bleibt grün — der neue Cascade-Code wird short-circuited (vor list_session_ids).
    - Test 5 `test_close_assembly_audited_update_runs_before_cascade_discovery`: Reihenfolge per `mock.expect_xxx().times(1).in_sequence(...)`: Status-Update + AuditLogDao::create THEN list_session_ids_for_assembly THEN commit THEN delete_session.
  </behavior>
  <action>
**Schritt 1 — `gen_service_impl!`-Block erweitern** (lines 50-60). Erweitere die bestehenden 7 Deps um 2 neue Einträge:

```rust
gen_service_impl! {
    struct AssemblyServiceImpl: AssemblyService = AssemblyServiceDeps {
        AssemblyDao: AssemblyDao<Transaction = Self::Transaction> = assembly_dao,
        AssemblyMemberSnapshotDao: AssemblyMemberSnapshotDao<Transaction = Self::Transaction> = assembly_member_snapshot_dao,
        MemberDao: MemberDao<Transaction = Self::Transaction> = member_dao,
        AuditLogDao: AuditLogDao<Transaction = Self::Transaction> = audit_log_dao,
        PermissionService: PermissionService<Context = Self::Context> = permission_service,
        UuidService: UuidService = uuid_service,
        TransactionDao: TransactionDao<Transaction = Self::Transaction> = transaction_dao,
        // NEU Phase 3 (D-12, D-16):
        HelperTokenDao: HelperTokenDao<Transaction = Self::Transaction> = helper_token_dao,
        // NEU Phase 3 (Cascade calls delete_session, on PermissionDao):
        PermissionDao: PermissionDao = permission_dao,
    }
}
```

**Wichtig:** PermissionDao hat ein Self::Transaction associated type, wird hier aber **ohne** `<Transaction = Self::Transaction>`-Bound verwendet, weil delete_session pool-basiert ist (kein tx-Argument). Falls gen_service_impl! das Bound erzwingt, behalte `PermissionDao: PermissionDao<Transaction = Self::Transaction> = permission_dao,` — die Method delete_session ignoriert tx einfach.

**Schritt 2 — Use-Statement-Erweiterungen** am Datei-Anfang:

```rust
use genossi_dao::helper_token::HelperTokenDao;
use genossi_dao::permission::PermissionDao;
```

**Schritt 3 — `close_assembly`-Body modifizieren.** Suche im bestehenden Body die Zeile mit `crate::audited_update!(self, self.assembly_dao, id, &entity, ASSEMBLY_PROCESS_CLOSE, &user_id, tx);`. **Direkt danach** (vor `self.transaction_dao.commit(tx).await?` und `Ok(Assembly::from(&entity))`) einfügen — Conflict-2-Resolution-Strategie:

```rust
        // D-11/D-12 — Phase 3 cascade extension.
        // Discover all bound helper-session ids INSIDE the still-open tx so
        // we read the same snapshot as the audited_update above.
        let session_ids = self
            .helper_token_dao
            .list_session_ids_for_assembly(id, tx.clone())
            .await?;

        // CONFLICT 2 (RESEARCH.md §DECISION CONFLICTS): commit BEFORE pool-based
        // delete_session calls. PermissionDao::delete_session takes no `tx` argument
        // (genossi_dao/src/permission.rs:90); it acquires its own pool connection.
        // The same caveat is documented in helper_token.rs:316-325 — keeping an open
        // BEGIN while a parallel pool acquire is requested deadlocks the sqlite-pool.
        self.transaction_dao.commit(tx).await?;

        // D-13/D-14: Continue-on-Error. The status=Closed audit-entry is already
        // committed; failed session-DELETEs are caught by the Phase-2-D-18
        // verify_user_session status-check (defense-in-depth). Each failure logs
        // a WARN line for operator visibility.
        for sid in session_ids.iter() {
            if let Err(e) = self.permission_dao.delete_session(sid.as_ref()).await {
                tracing::warn!(
                    error = ?e,
                    session_id = %sid.as_ref(),
                    assembly_id = %id,
                    "cascade delete_session failed; defense-in-depth via verify_user_session-Status-Check active"
                );
            }
        }

        Ok(Assembly::from(&entity))
```

**WICHTIG: Wenn das bestehende `close_assembly` schon ein `self.transaction_dao.commit(tx).await?;` und `Ok(Assembly::from(&entity))` am Ende hat: ENTFERNE die alte commit-Zeile** (sie wird von der neuen ersetzt — die Cascade läuft NACH dem commit). Lese `close_assembly` Zeile-für-Zeile durch und stelle sicher dass am Ende exakt EIN commit-Aufruf steht (vor der for-Loop) und EIN return Ok(...) steht (nach der for-Loop).

**Schritt 4 — Tests im bestehenden `tests`-Modul am Ende von `assembly.rs` modifizieren:**

**4a — Handgeschriebene Mocks erweitern** (Pitfall 4 — RESEARCH §1311-1340):

Füge **direkt unter dem bestehenden `mock! { pub TestAssemblyDao { ... } }`-Block** zwei neue mock!-Blöcke ein:

```rust
mock! {
    pub TestHelperTokenDao {}
    #[async_trait]
    impl HelperTokenDao for TestHelperTokenDao {
        type Transaction = TestTransaction;
        async fn dump_all(&self, tx: TestTransaction) -> Result<Arc<[HelperTokenEntity]>, DaoError>;
        async fn create(&self, entity: &HelperTokenEntity, process: &str, tx: TestTransaction) -> Result<(), DaoError>;
        async fn update(&self, entity: &HelperTokenEntity, process: &str, tx: TestTransaction) -> Result<(), DaoError>;
        async fn atomic_redeem(&self, token_hash: &str, used_at: time::PrimitiveDateTime, tx: TestTransaction) -> Result<Option<(Uuid, Uuid)>, DaoError>;
        async fn set_session_id(&self, token_id: Uuid, session_id: &str, tx: TestTransaction) -> Result<(), DaoError>;
        async fn lookup_status(&self, token_hash: &str, tx: TestTransaction) -> Result<Option<(Option<time::PrimitiveDateTime>, Option<time::PrimitiveDateTime>)>, DaoError>;
        async fn all_for_assembly(&self, assembly_id: Uuid, tx: TestTransaction) -> Result<Arc<[HelperTokenEntity]>, DaoError>;
        async fn list_session_ids_for_assembly(&self, assembly_id: Uuid, tx: TestTransaction) -> Result<Vec<Arc<str>>, DaoError>;
    }
}

mock! {
    pub TestPermissionDao {}
    #[async_trait]
    impl PermissionDao for TestPermissionDao {
        type Transaction = TestTransaction;
        async fn has_privilege(&self, user: &str, privilege: &str) -> Result<bool, DaoError>;
        async fn all_users(&self) -> Result<Arc<[UserEntity]>, DaoError>;
        async fn get_user(&self, name: &str) -> Result<Option<UserEntity>, DaoError>;
        async fn create_user(&self, user: &UserEntity, process: &str) -> Result<(), DaoError>;
        async fn delete_user(&self, username: &str) -> Result<(), DaoError>;
        async fn all_roles(&self) -> Result<Arc<[RoleEntity]>, DaoError>;
        async fn get_role(&self, name: &str) -> Result<Option<RoleEntity>, DaoError>;
        async fn create_role(&self, role: &RoleEntity, process: &str) -> Result<(), DaoError>;
        async fn delete_role(&self, role_name: &str) -> Result<(), DaoError>;
        async fn all_privileges(&self) -> Result<Arc<[PrivilegeEntity]>, DaoError>;
        async fn get_privilege(&self, name: &str) -> Result<Option<PrivilegeEntity>, DaoError>;
        async fn create_privilege(&self, privilege: &PrivilegeEntity, process: &str) -> Result<(), DaoError>;
        async fn delete_privilege(&self, privilege_name: &str) -> Result<(), DaoError>;
        async fn add_user_role(&self, username: &str, role: &str, process: &str) -> Result<(), DaoError>;
        async fn remove_user_role(&self, username: &str, role: &str) -> Result<(), DaoError>;
        async fn get_user_roles(&self, username: &str) -> Result<Arc<[RoleEntity]>, DaoError>;
        async fn add_role_privilege(&self, role_name: &str, privilege_name: &str, process: &str) -> Result<(), DaoError>;
        async fn remove_role_privilege(&self, role_name: &str, privilege_name: &str) -> Result<(), DaoError>;
        async fn get_role_privileges(&self, role_name: &str) -> Result<Arc<[PrivilegeEntity]>, DaoError>;
        async fn get_user_privileges(&self, username: &str) -> Result<Arc<[PrivilegeEntity]>, DaoError>;
        async fn create_session(&self, session: &SessionEntity) -> Result<(), DaoError>;
        async fn get_session(&self, session_id: &str) -> Result<Option<SessionEntity>, DaoError>;
        async fn delete_session(&self, session_id: &str) -> Result<(), DaoError>;
        async fn cleanup_expired_sessions(&self, before_timestamp: i64) -> Result<(), DaoError>;
        async fn touch_session(&self, session_id: &str, now: i64) -> Result<(), DaoError>;
        async fn delete_sessions_for_user(&self, user_id: &str) -> Result<u64, DaoError>;
    }
}
```

(Die genaue Method-Liste muss exakt mit `genossi_dao/src/permission.rs::PermissionDao` matchen — falls Signaturen leicht abweichen weil das Trait noch eine `ensure_user_exists`-Default-Method hat, kannst du die Default-Method weglassen oder als `// inherits default` markieren — mockall braucht alle non-default-Methods explizit.)

**4b — `TestDeps`-Struct + `impl AssemblyServiceDeps for TestDeps`** erweitern um die 2 neuen associated types und Field-Slots:

```rust
impl AssemblyServiceDeps for TestDeps {
    type Context = TestContext;
    type Transaction = TestTransaction;
    type AssemblyDao = MockTestAssemblyDao;
    type AssemblyMemberSnapshotDao = MockTestSnapshotDao;
    type MemberDao = MockTestMemberDao;
    type AuditLogDao = MockTestAuditLogDao;
    type PermissionService = MockTestPermissionService;
    type UuidService = MockTestUuidService;
    type TransactionDao = MockTestTxDao;
    // NEU Phase 3:
    type HelperTokenDao = MockTestHelperTokenDao;
    type PermissionDao = MockTestPermissionDao;
}
```

Der `setup_service` / `build_test_service`-Helper am Test-Modul-Ende (assembly.rs lines ~711-720) muss ebenfalls die 2 neuen Deps in der `AssemblyServiceImpl { ... }`-Construction Field-Liste annehmen und einsetzen:

```rust
fn build_test_service(
    assembly_dao: MockTestAssemblyDao,
    snapshot_dao: MockTestSnapshotDao,
    member_dao: MockTestMemberDao,
    audit_log_dao: MockTestAuditLogDao,
    permission_service: MockTestPermissionService,
    uuid_service: MockTestUuidService,
    tx_dao: MockTestTxDao,
    // NEU:
    helper_token_dao: MockTestHelperTokenDao,
    permission_dao: MockTestPermissionDao,
) -> AssemblyServiceImpl<TestDeps> {
    AssemblyServiceImpl {
        assembly_dao: Arc::new(assembly_dao),
        assembly_member_snapshot_dao: Arc::new(snapshot_dao),
        member_dao: Arc::new(member_dao),
        audit_log_dao: Arc::new(audit_log_dao),
        permission_service: Arc::new(permission_service),
        uuid_service: Arc::new(uuid_service),
        transaction_dao: Arc::new(tx_dao),
        helper_token_dao: Arc::new(helper_token_dao),
        permission_dao: Arc::new(permission_dao),
        _phantom: std::marker::PhantomData,
    }
}
```

**Bestehende Tests, die `build_test_service(...)` aufrufen, müssen angepasst werden** — sie geben jetzt 2 neue Mock-Args mit:
- Für Tests, die NICHT zu close_assembly gehören: erzeuge `MockTestHelperTokenDao::new()` + `MockTestPermissionDao::new()` ohne expect_*-Aufrufe (sie werden nie touched, also sind unerwartete Aufrufe Test-Failures was korrekt ist).
- Für `test_close_assembly_from_preparation_returns_conflict` (Phase-1-Test): die helper_token_dao + permission_dao werden NICHT touched, weil der Status-Check vor list_session_ids_for_assembly short-circuited. Also auch nur `Mock...::new()`.

**4c — 5 neue Tests gemäß `<behavior>`** im Test-Modul:

Test 1 (cascade-loops-through-session-ids):
```rust
#[tokio::test]
async fn test_close_assembly_cascades_to_all_helper_sessions() {
    let aid = Uuid::new_v4();
    let assembly = build_test_assembly(aid, AssemblyStatus::Open);

    let mut tx_dao = MockTestTxDao::new();
    tx_dao.expect_use_transaction().returning(|_| Ok(TestTransaction));
    tx_dao.expect_commit().times(1).returning(|_| Ok(()));

    let mut assembly_dao = MockTestAssemblyDao::new();
    let assembly_clone = assembly.clone();
    assembly_dao.expect_find_by_id().returning(move |_, _| Ok(Some(assembly_clone.clone())));
    assembly_dao.expect_update().times(1).returning(|_, _, _| Ok(()));

    let mut helper_token_dao = MockTestHelperTokenDao::new();
    helper_token_dao.expect_list_session_ids_for_assembly()
        .with(eq(aid), always())
        .times(1)
        .returning(|_, _| Ok(vec![Arc::from("s1"), Arc::from("s2"), Arc::from("s3")]));

    let mut permission_dao = MockTestPermissionDao::new();
    permission_dao.expect_delete_session().with(eq("s1")).times(1).returning(|_| Ok(()));
    permission_dao.expect_delete_session().with(eq("s2")).times(1).returning(|_| Ok(()));
    permission_dao.expect_delete_session().with(eq("s3")).times(1).returning(|_| Ok(()));

    // Audit-Log + permission_service mocks (admin context, current_user_id liefert "admin"):
    let mut audit = MockTestAuditLogDao::new();
    audit.expect_create_batch().returning(|_, _| Ok(()));   // oder analog je nach audited_update! macro
    let mut perm = MockTestPermissionService::new();
    perm.expect_current_user_id().returning(|_| Ok(Some("admin".into())));
    perm.expect_check_permission().returning(|_, _| Ok(()));
    let mut uuid_svc = MockTestUuidService::new();
    uuid_svc.expect_new_v4().returning(Uuid::new_v4);

    let svc = build_test_service(assembly_dao, MockTestSnapshotDao::new(),
        MockTestMemberDao::new(), audit, perm, uuid_svc, tx_dao,
        helper_token_dao, permission_dao);

    let result = svc.close_assembly(aid, Authentication::Full).await;
    assert!(result.is_ok());
    assert_eq!(result.unwrap().status, AssemblyStatus::Closed);
}
```

Tests 2–5 analog mit angepassten Mock-Erwartungen.

**Was NICHT machen:**
- KEINE Modifikation der `close_assembly`-Method-Signatur (gleiche Parameter wie Phase 1).
- KEIN Cascade VOR dem audited_update! (D-15: Status-Update zuerst, damit der Audit-Eintrag persistent ist auch wenn cascade fehlschlägt).
- KEIN Cascade WÄHREND der tx (Conflict 2 — würde deadlocken).
- KEIN Error-Propagation aus dem cascade-Loop (Continue-on-Error per Resolution).
- KEIN automock-regenerieren — die handgeschriebenen `mock! { TestXxx }`-Blöcke sind Pflicht weil mockall-automock Transaction = MockTransaction hardcoded (siehe Pitfall 4 §1340).
  </action>
  <verify>
    <automated>cargo build -p genossi_service_impl 2>&amp;1 | tail -20 &amp;&amp; cargo test -p genossi_service_impl assembly --no-fail-fast 2>&amp;1 | tail -60</automated>
  </verify>
  <done>
    - `cargo build -p genossi_service_impl` exits 0.
    - `cargo test -p genossi_service_impl assembly` exits 0; **alle bestehenden Tests bleiben grün** + mindestens 5 neue Tests.
    - `grep -c 'list_session_ids_for_assembly' genossi_service_impl/src/assembly.rs` ≥ 2 (1× call in close_assembly + ≥ 1× in mock!-Block).
    - `grep -c 'permission_dao.delete_session' genossi_service_impl/src/assembly.rs` = 1 (cascade-loop call).
    - `grep -c 'tracing::warn' genossi_service_impl/src/assembly.rs` ≥ 1 (continue-on-error).
    - `grep -c 'HelperTokenDao' genossi_service_impl/src/assembly.rs` ≥ 2 (gen_service_impl + mock + use-statement).
    - `grep -c 'cascade delete_session failed' genossi_service_impl/src/assembly.rs` = 1.
    - Reihenfolge im close_assembly: `audited_update` line < `list_session_ids_for_assembly` line < `transaction_dao.commit` line < `for sid in` line. Verify mit `awk` oder manuelle Inspektion.
  </done>
</task>

</tasks>

<threat_model>
Siehe Frontmatter `threat_model:` — sechs Threats. Wichtigste:
- **T-03-05-01** Permission-Funnel: alle 4 Endpoints rufen check_assembly_access als ersten Schritt.
- **T-03-05-02** Cross-Assembly-Helper-Attack: D-18-Branch (helper_aid != endpoint_aid → PermissionDenied).
- **T-03-05-03** Snapshot-Membership-Bypass: D-27-Check vor jedem mark_*-DAO-Aufruf.
- **T-03-05-05** Cascade-DoS: Continue-on-Error + Defense-in-Depth via Phase-2-D-18.
</threat_model>

<verification>
- `cargo build -p genossi_service_impl` exits 0.
- `cargo build -p genossi_service_impl --features oidc` exits 0.
- `cargo test -p genossi_service_impl attendance assembly` exits 0; alle Tests grün.
- `cargo fmt -- --check` exits 0.
- `cargo clippy -p genossi_service_impl --no-deps -- -D warnings` exits 0.
</verification>

<success_criteria>
- AttendanceServiceImpl implementiert AttendanceService-Trait mit zentraler Permission-Funnel.
- check_assembly_access deckt 3 Branches korrekt ab (Full, Helper-mit-aid-und-Status-Open, admin).
- close_assembly cascade-erweitert mit Conflict-2-Resolution (Commit BEFORE pool-Loop).
- Bestehende Phase-1-close_assembly-Tests bleiben grün (no-regression).
- 14 + 5 = 19 neue Service-Layer-Tests verifizieren die Logik.
- E2E-Tests (Race, Cascade-DB, PII-Leak, Hash-Chain-Stability, Post-Close-Edit, Helfer-vs-Vorstand) folgen in Plan 06.
- 8 Phase-Requirements (ASSY-04, ASSY-06, ATTN-01..06, SYNC-02) auf Service-Ebene erfüllt; ASSY-03-Cascade-Trigger ist Plan-02+Plan-05-Komposition.
</success_criteria>

<output>
Nach Abschluss: erstelle `.planning/phases/03-attendance-aggregat-cascade-invalidation/03-05-SUMMARY.md` mit:
- Liste der erstellten/modifizierten Files
- check_assembly_access-Branching (verbatim)
- Cascade-Reihenfolge in close_assembly (verbatim)
- Test-Liste + Status (alle grün, inkl. Phase-1-Regressionstest)
- Mock-Erweiterungen (TestHelperTokenDao + TestPermissionDao) für Plan 06 dokumentieren
- Hinweis auf Plan-06-Konsument (REST-Handler + DI-Wiring + E2E-Tests)
</output>
