---
phase: 04-frontend-component-first-mit-qr-scanner-und-manual-code-fall
plan: 09
type: execute
wave: 4
depends_on: [01, 03, 04, 05, 06b, 07]
files_modified:
  - genossi-frontend/src/page/helper_login.rs
  - genossi-frontend/src/page/helper_attendance.rs
autonomous: true
requirements: [HLPR-03, SYNC-01]
must_haves:
  truths:
    - "Helfer-Login-Page (`/helper`) zeigt BEIDE Pfade gleichzeitig sichtbar: QR-Code-Scan-Button UND Manual-Code-Input (HLPR-03)"
    - "Camera-Permission wird ERST nach Klick auf 'QR-Code scannen' angefragt (D-03 — iOS-User-Gesture-Compliance)"
    - "Nach erfolgreichem Scan/Submit: POST /api/helper/redeem; bei 200 Navigate zu /helper/attendance"
    - "Auto-Redirect: beim Mount von /helper prüft die Page GET /api/helper/session — wenn 200, navigate zu /helper/attendance (D-06)"
    - "Helfer-Attendance-Page (`/helper/attendance`) lädt assembly_name via /api/helper/session und rendert <HelperShell> mit GV-Name + Logout"
    - "Anwesenheits-Body nutzt EXAKT dieselben 4 Components wie assembly_details Anwesenheits-Tab (LiveCounter + AttendanceSearch + AttendanceList + ConnectionBanner) — ATTN-06"
    - "Logout-Button POST /api/helper/logout + Navigate zu /helper"
    - "Bei 401 von Backend (Session abgelaufen): Navigate zu /helper + Toast 'Session abgelaufen'"
    - "Helfer-Login-Errors mit i18n-Override pro HTTP-Status (404→NotFound, 410→AlreadyUsed, 403→AssemblyClosed, 400→InvalidFormat, 429→RateLimit) — Inline unter Manual-Input, NICHT als Toast"
  artifacts:
    - path: "genossi-frontend/src/page/helper_login.rs"
      provides: "HelperLogin Page mit QrScanner-Modal + ManualCodeInput parallel + Auto-Redirect"
      contains: "pub fn HelperLogin, QrScanner, ManualCodeInput"
      min_lines: 100
    - path: "genossi-frontend/src/page/helper_attendance.rs"
      provides: "HelperAttendance Page mit HelperShell + 4 Anwesenheits-Components"
      contains: "pub fn HelperAttendance, HelperShell, LiveCounter, AttendanceList"
      min_lines: 80
  key_links:
    - from: "helper_login.rs Submit-Handler"
      to: "api::redeem_helper_token + Navigate(Route::HelperAttendance{})"
      via: "spawn-async"
      pattern: "redeem_helper_token.*HelperAttendance"
    - from: "helper_attendance.rs Composition"
      to: "AttendanceList + AttendanceSearch + LiveCounter + ConnectionBanner (Plan 04 Components)"
      via: "Component-First reuse — same 4 Components as assembly_details Plan 08"
      pattern: "AttendanceList.*AttendanceSearch.*LiveCounter"
    - from: "helper_attendance.rs Logout-Action"
      to: "api::helper_logout + Navigate(Route::HelperLogin{})"
      via: "HelperShell on_logout EventHandler"
      pattern: "helper_logout.*HelperLogin"
---

<objective>
Beide Helfer-Pages ausimplementieren. `/helper` zeigt QR-Scan + Manual-Code parallel (HLPR-03), Auto-Redirect bei vorhandener Session, Inline-Error-Mapping für Redeem-Statuscodes. `/helper/attendance` rendert die 4 shared Anwesenheits-Components in HelperShell-Layout (no TopBar/Footer); Logout via /api/helper/logout.

Purpose: HLPR-03 acceptance (Manual-Code-Fallback funktioniert ohne Camera) + SYNC-01 acceptance (5s-Polling, Refresh-on-Search, Refresh-on-Toggle); ATTN-06 Component-Reuse-Beweis (Diff vs assembly_details Anwesenheits-Tab zeigt nur Wrapper-Unterschied).
Output: 2 Page-Files vollständig ausimplementiert.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/neosam/programming/rust/projects/genossi3/.planning/phases/04-frontend-component-first-mit-qr-scanner-und-manual-code-fall/04-CONTEXT.md
@/home/neosam/programming/rust/projects/genossi3/.planning/phases/04-frontend-component-first-mit-qr-scanner-und-manual-code-fall/04-PATTERNS.md
@/home/neosam/programming/rust/projects/genossi3/.planning/phases/04-frontend-component-first-mit-qr-scanner-und-manual-code-fall/04-RESEARCH.md
@/home/neosam/programming/rust/projects/genossi3/.planning/phases/04-frontend-component-first-mit-qr-scanner-und-manual-code-fall/04-UI-SPEC.md
@/home/neosam/programming/rust/projects/genossi3/genossi-frontend/CLAUDE.md
@/home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/api.rs
@/home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/home.rs

<interfaces>
**Imports für HelperLogin:**
```rust
use dioxus::prelude::*;
use crate::api::{self, AppError, HelperSessionTO};
use crate::component::{HelperShell, ManualCodeInput, QrScanner};
use crate::i18n::{use_i18n, Key};
use crate::router::Route;
use crate::service::config::CONFIG;
```

**Imports für HelperAttendance:**
```rust
use dioxus::prelude::*;
use crate::api::{self, HelperSessionTO};
use crate::component::{
    AttendanceList, AttendanceSearch, ConnectionBanner, ConnState,
    HelperShell, LiveCounter, ToastContainer, show_toast,
};
use crate::i18n::{use_i18n, Key};
use crate::router::Route;
use crate::service::config::CONFIG;
```

**Pattern-Anker:**
- `genossi-frontend/src/page/home.rs:1-15` — Auto-Redirect via `use_effect` + `nav.replace`
- `genossi-frontend/src/page/applications_page.rs:115-150` — Composition-Pattern

**Login-Flow (UI-SPEC §`/helper`):**
1. Mount → spawn `api::get_helper_session(&config)` → 200: nav.replace(Route::HelperAttendance{}); 401: render Login-UI
2. UI zeigt nebeneinander: Button "QR-Code scannen" (öffnet QrScanner als Modal-Overlay) + ManualCodeInput
3. QR-Submit oder Manual-Submit → spawn `api::redeem_helper_token(&config, code)` → 200: nav.replace(Route::HelperAttendance{}); error: Inline-Message via Status-Code-Mapping

**Status-Code-Mapping (UI-SPEC §"Error state — Redeem"):**
| status | i18n key |
|--------|----------|
| 400 | `HelperLoginInvalidFormat` |
| 403 | `HelperLoginErrorAssemblyClosed` |
| 404 | `HelperLoginErrorNotFound` |
| 410 | `HelperLoginErrorAlreadyUsed` |
| 429 | `HelperLoginErrorRateLimit` |
| _ | generic via AppError.message |

**Attendance-Flow (UI-SPEC §`/helper/attendance`):**
1. Mount → spawn `api::get_helper_session(&config)` → 200: store assembly_id + assembly_name; 401: nav zu /helper + toast
2. Wrap in `<HelperShell assembly_name=Some(...) on_logout=...>`
3. Body: `<ConnectionBanner>` + `<LiveCounter>` + `<AttendanceSearch>` + `<AttendanceList read_only=false>` + `<ToastContainer>`
4. on_logout: spawn `api::helper_logout(&config)` → nav zu /helper
</interfaces>
</context>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-35 | E (Elevation of Privilege) | Helfer-Page accessible ohne Helper-Cookie | mitigate | helper_attendance.rs prüft `/api/helper/session` beim Mount; bei 401 nav.replace zu /helper + Toast. Backend bleibt authoritative (alle Helfer-API-Calls 401en ohne Cookie). |
| T-04-36 | I (Information Disclosure) | Manual-Code wird im Fehlerfall in URL/Logs landen | mitigate | api::redeem_helper_token loggt nicht den Code (Plan 03); Frontend speichert Code nicht im URL — POST-Body. |
| T-04-37 | T (Tampering) | XSS via redeem-error-message | mitigate | Inline-Error rendert i18n-String + AppError.message — Dioxus escapes; KEIN dangerous_inner_html |
| T-04-38 | I (Information Disclosure) | Logout schlägt fehl, Cookie bleibt aktiv | mitigate | Bei helper_logout-Fehler: trotzdem nav zu /helper + Toast (UX-Recovery); Backend cleart Session bei nächstem Cookie-Check spätestens beim GV-Schluss (Phase 2 D-18) |
| T-04-39 | E (Elevation of Privilege) | Helfer kann assembly_id manipulieren | accept | assembly_id stammt EXKLUSIV aus /api/helper/session-Response (Backend authoritative); Frontend speichert es lokal aber API-Calls validieren via Cookie+assembly_id |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: HelperLogin Page (HLPR-03 — QR + Manual parallel + Auto-Redirect)</name>
  <files>
    genossi-frontend/src/page/helper_login.rs
  </files>
  <read_first>
    - /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/home.rs (Auto-Redirect-Pattern)
    - /home/neosam/programming/rust/projects/genossi3/.planning/phases/04-frontend-component-first-mit-qr-scanner-und-manual-code-fall/04-UI-SPEC.md:585-597 (HelperLogin page Layout)
    - /home/neosam/programming/rust/projects/genossi3/.planning/phases/04-frontend-component-first-mit-qr-scanner-und-manual-code-fall/04-PATTERNS.md:493-520 (Pattern für helper_login.rs)
  </read_first>
  <action>
    **Komplett ersetzen** des Stubs in `genossi-frontend/src/page/helper_login.rs`:
    ```rust
    //! HelperLogin Page (Phase 4 Plan 09) — /helper route, public.
    //!
    //! HLPR-03: zeigt QR-Code-Scan-Button UND ManualCodeInput parallel.
    //! D-03: Camera-Permission erst nach Klick.
    //! D-06: Auto-Redirect bei vorhandener Session.
    //! Status-Code-Mapping für Redeem-Errors (UI-SPEC §"Error state — Redeem").

    use dioxus::prelude::*;

    use crate::api::{self, AppError};
    use crate::component::{HelperShell, ManualCodeInput, QrScanner};
    use crate::i18n::{use_i18n, Key};
    use crate::router::Route;
    use crate::service::config::CONFIG;

    fn map_redeem_error(i18n: &crate::i18n::I18n, err: &AppError) -> String {
        match err.status {
            Some(400) => i18n.t(Key::HelperLoginInvalidFormat).to_string(),
            Some(403) => i18n.t(Key::HelperLoginErrorAssemblyClosed).to_string(),
            Some(404) => i18n.t(Key::HelperLoginErrorNotFound).to_string(),
            Some(410) => i18n.t(Key::HelperLoginErrorAlreadyUsed).to_string(),
            Some(429) => i18n.t(Key::HelperLoginErrorRateLimit).to_string(),
            _ => err.message.clone(),
        }
    }

    // W-05: Delayed loading skeleton — zeigt für die ersten 200ms NICHTS,
    // erst danach einen zentrierten Skeleton-Box. Vermeidet Flash-of-Loading
    // bei schnellen /api/helper/session-Probes (RESEARCH §UI-SPEC Loading-Pattern).
    #[component]
    fn DelayedLoadingSkeleton() -> Element {
        let mut visible = use_signal(|| false);
        use_effect(move || {
            spawn(async move {
                gloo_timers::future::TimeoutFuture::new(200).await;
                visible.set(true);
            });
        });
        if !*visible.read() {
            return rsx! { div { class: "h-1" } }; // 1px placeholder, no flash
        }
        rsx! {
            div { class: "flex items-center justify-center py-12",
                div { class: "animate-pulse flex flex-col gap-2 w-64",
                    div { class: "h-4 bg-gray-200 rounded w-3/4 mx-auto" }
                    div { class: "h-4 bg-gray-200 rounded w-1/2 mx-auto" }
                }
            }
        }
    }

    #[component]
    pub fn HelperLogin() -> Element {
        let i18n = use_i18n();
        let nav = navigator();

        let mut show_scanner = use_signal(|| false);
        let mut submitting = use_signal(|| false);
        let mut error_msg = use_signal(|| Option::<String>::None);
        let mut redirect_check_done = use_signal(|| false);

        // Auto-Redirect (D-06): probe /api/helper/session on mount.
        use_effect(move || {
            let nav = nav;
            spawn(async move {
                let config = CONFIG.read().clone();
                if let Ok(_) = api::get_helper_session(&config).await {
                    // Already have a valid session — navigate to attendance.
                    nav.replace(Route::HelperAttendance {});
                    return;
                }
                redirect_check_done.set(true);
            });
        });

        let do_redeem = move |code: String| {
            let nav = nav;
            let i18n_for_err = i18n.clone();
            spawn(async move {
                submitting.set(true);
                error_msg.set(None);
                let config = CONFIG.read().clone();
                match api::redeem_helper_token(&config, code).await {
                    Ok(_) => {
                        nav.replace(Route::HelperAttendance {});
                    }
                    Err(e) => {
                        error_msg.set(Some(map_redeem_error(&i18n_for_err, &e)));
                    }
                }
                submitting.set(false);
            });
        };

        rsx! {
            HelperShell {
                assembly_name: None,
                on_logout: move |_| {}, // logout meaningless on login page; no-op
                if !*redirect_check_done.read() {
                    // W-05: kein Spinner unter ~200ms (delayed via gloo_timers).
                    // Falls die Probe > 200ms braucht, zeigen wir einen zentrierten Skeleton-Box
                    // (UI-SPEC-konform — print:hidden, mobile-friendly).
                    DelayedLoadingSkeleton {}
                } else {
                    div { class: "py-8",
                        h1 { class: "text-2xl font-bold mb-2", "{i18n.t(Key::HelperLoginTitle)}" }
                        p { class: "text-gray-600 mb-8", "{i18n.t(Key::HelperLoginSubtitle)}" }

                        div { class: "flex flex-col md:flex-row gap-6 md:gap-12 items-stretch",
                            // Left path: QR-Code-Scan
                            div { class: "flex-1 flex flex-col gap-3",
                                button {
                                    class: "bg-blue-600 hover:bg-blue-700 text-white font-medium px-6 py-4 rounded-lg w-full text-lg min-h-[44px]",
                                    onclick: move |_| show_scanner.set(true),
                                    span { class: "mr-2", "\u{1F4F7}" } // camera glyph
                                    "{i18n.t(Key::HelperLoginScanQR)}"
                                }
                            }
                            // Divider (visible on desktop)
                            div { class: "text-center text-gray-400 text-sm md:flex md:items-center md:px-4",
                                "oder"
                            }
                            // Right path: Manual-Code
                            div { class: "flex-1",
                                ManualCodeInput {
                                    on_submit: move |code: String| do_redeem(code),
                                    submitting: *submitting.read(),
                                    error: error_msg.read().clone(),
                                }
                            }
                        }

                        if *show_scanner.read() {
                            QrScanner {
                                on_scan: move |code: String| {
                                    show_scanner.set(false);
                                    do_redeem(code);
                                },
                                on_error: move |msg: String| {
                                    show_scanner.set(false);
                                    error_msg.set(Some(msg));
                                },
                                on_cancel: move |_| show_scanner.set(false),
                            }
                        }
                    }
                }
            }
        }
    }
    ```

    Commit: `feat(04-09): HelperLogin page (HLPR-03 — QR + Manual parallel + auto-redirect)`
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/genossi3/genossi-frontend && cargo build 2>&1 | tail -3 && grep -q "QrScanner" src/page/helper_login.rs && grep -q "ManualCodeInput" src/page/helper_login.rs && grep -q "redeem_helper_token" src/page/helper_login.rs && grep -q "get_helper_session" src/page/helper_login.rs && grep -q "Route::HelperAttendance" src/page/helper_login.rs && grep -qE "HelperLoginErrorAlreadyUsed|HelperLoginErrorNotFound" src/page/helper_login.rs && grep -q "show_scanner" src/page/helper_login.rs && grep -q "HelperShell" src/page/helper_login.rs && grep -q "DelayedLoadingSkeleton\|TimeoutFuture::new(200)" src/page/helper_login.rs && grep -q "animate-pulse" src/page/helper_login.rs</automated>
  </verify>
  <done>HelperLogin compiliert; rendert QrScanner + ManualCodeInput parallel; Auto-Redirect bei vorhandener Session via get_helper_session; Status-Code-Mapping in map_redeem_error; HLPR-03 acceptance (Manual-Pfad funktioniert auch ohne Camera); W-05 acceptance: DelayedLoadingSkeleton zeigt für die ersten 200ms NICHTS und erst dann einen zentrierten animate-pulse-Skeleton.</done>
</task>

<task type="auto">
  <name>Task 2: HelperAttendance Page (4 shared Components + Logout)</name>
  <files>
    genossi-frontend/src/page/helper_attendance.rs
  </files>
  <read_first>
    - /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/applications_page.rs:115-150 (Composition-Pattern)
    - /home/neosam/programming/rust/projects/genossi3/.planning/phases/04-frontend-component-first-mit-qr-scanner-und-manual-code-fall/04-UI-SPEC.md:599-608 (helper_attendance Page Layout)
    - /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/assembly_details.rs (Plan 08 — gleicher Components-Block für ATTN-06 Reuse-Diff)
  </read_first>
  <action>
    **Komplett ersetzen** des Stubs in `genossi-frontend/src/page/helper_attendance.rs`:
    ```rust
    //! HelperAttendance Page (Phase 4 Plan 09) — /helper/attendance route.
    //!
    //! Wraps in HelperShell (no TopBar/Footer). Body uses the SAME 4 components
    //! as the Vorstand Anwesenheits-Tab (assembly_details.rs) — ATTN-06 Component-Reuse.

    use dioxus::prelude::*;

    use crate::api::{self, HelperSessionTO};
    use crate::component::{
        AttendanceList, AttendanceSearch, ConnState, ConnectionBanner, HelperShell,
        LiveCounter, ToastContainer, show_toast,
    };
    use crate::i18n::{use_i18n, Key};
    use crate::router::Route;
    use crate::service::config::CONFIG;

    #[component]
    pub fn HelperAttendance() -> Element {
        let i18n = use_i18n();
        let nav = navigator();

        let mut session = use_signal(|| Option::<HelperSessionTO>::None);
        let mut loading = use_signal(|| true);
        let mut conn_lost = use_signal(|| false);
        let mut search_query = use_signal(String::new);
        let mut refresh_signal = use_signal(|| 0u64);
        let mut toast_messages = use_signal(|| Vec::<(u64, String)>::new());
        let mut toast_counter = use_signal(|| 0u64);

        // Mount: probe session. 401 → nav back to /helper.
        use_effect(move || {
            let nav = nav;
            spawn(async move {
                let config = CONFIG.read().clone();
                match api::get_helper_session(&config).await {
                    Ok(s) => {
                        session.set(Some(s));
                    }
                    Err(_) => {
                        nav.replace(Route::HelperLogin {});
                        return;
                    }
                }
                loading.set(false);
            });
        });

        let on_logout = move |_| {
            let nav = nav;
            spawn(async move {
                let config = CONFIG.read().clone();
                let _ = api::helper_logout(&config).await; // best effort; nav anyway
                nav.replace(Route::HelperLogin {});
            });
        };

        let session_data = session.read().clone();
        let assembly_name = session_data.as_ref().map(|s| s.assembly_name.clone());
        let assembly_id_opt = session_data.as_ref().map(|s| s.assembly_id);

        rsx! {
            HelperShell {
                assembly_name: assembly_name,
                on_logout: on_logout,
                if *loading.read() {
                    div { class: "p-4", "{i18n.t(Key::Loading)}" }
                } else if let Some(assembly_id) = assembly_id_opt {
                    ConnectionBanner { visible: *conn_lost.read() }
                    LiveCounter {
                        assembly_id: assembly_id,
                        polling_enabled: true,
                        on_connection_state: move |s: ConnState| {
                            conn_lost.set(matches!(s, ConnState::Lost));
                        },
                    }
                    AttendanceSearch {
                        on_change: move |q: String| search_query.set(q),
                    }
                    AttendanceList {
                        assembly_id: assembly_id,
                        search_query: search_query.into(),
                        read_only: false,
                        refresh_signal: refresh_signal.into(),
                        on_toggle_success: move |_| {
                            refresh_signal.with_mut(|n| *n += 1);
                        },
                    }
                }
            }
            ToastContainer { messages: toast_messages.into() }
        }
    }
    ```

    Commit: `feat(04-09): HelperAttendance page reuses 4 shared components (ATTN-06, SYNC-01)`
  </action>
  <verify>
    <automated>cargo build -p genossi-frontend 2>&1 | tail -3 && grep -q "HelperShell" /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/helper_attendance.rs && grep -q "AttendanceList" /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/helper_attendance.rs && grep -q "AttendanceSearch" /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/helper_attendance.rs && grep -q "LiveCounter" /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/helper_attendance.rs && grep -q "ConnectionBanner" /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/helper_attendance.rs && grep -q "helper_logout" /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/helper_attendance.rs && grep -q "get_helper_session" /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/helper_attendance.rs && grep -q "Route::HelperLogin" /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/helper_attendance.rs && ! grep -E "TopBar|Footer" /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/helper_attendance.rs</automated>
  </verify>
  <done>HelperAttendance compiliert; nutzt HelperShell + die 4 shared Components; KEIN TopBar/Footer in Page; Logout via helper_logout; 401-Fallback nav zu /helper; Component-Anker für ATTN-06-Diff zu assembly_details.rs.</done>
</task>

</tasks>

<verification>
```bash
cargo build -p genossi-frontend 2>&1 | tail -5

# HLPR-03 acceptance: ManualCodeInput muss in helper_login.rs verwendet werden
grep -q "ManualCodeInput" /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/helper_login.rs

# Auto-Redirect (D-06): get_helper_session call beim Mount
grep -q "get_helper_session" /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/helper_login.rs

# Logout-Endpoint
grep -q "helper_logout" /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/helper_attendance.rs

# ATTN-06 Component-Reuse-Diff: helper_attendance UND assembly_details nutzen die SELBEN 4 Components
echo "── Components in helper_attendance ──"
grep -E "AttendanceList|AttendanceSearch|LiveCounter|ConnectionBanner" /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/helper_attendance.rs
echo "── Components in assembly_details (Plan 08) ──"
grep -E "AttendanceList|AttendanceSearch|LiveCounter|ConnectionBanner" /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/assembly_details.rs

# Datenschutz: kein TopBar/Footer in Helfer-Pages
! grep -E "TopBar|Footer" /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/helper_login.rs
! grep -E "TopBar|Footer" /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/helper_attendance.rs
```

</verification>

<success_criteria>
- Beide Pages compile-grün
- helper_login.rs zeigt QR + Manual parallel (HLPR-03), Auto-Redirect bei vorhandener Session, map_redeem_error covered alle 5 Status-Codes (400/403/404/410/429)
- helper_attendance.rs nutzt HelperShell + 4 shared Components + helper_logout
- ATTN-06 acceptance: helper_attendance.rs UND assembly_details.rs Anwesenheits-Tab nutzen EXAKT dieselben 4 Components (Component-First)
- KEIN TopBar/Footer in Helfer-Pages (Datenschutz)
- W-05 acceptance: helper_login.rs nutzt DelayedLoadingSkeleton (200ms-Verzögerung + animate-pulse-Skeleton)
- 401-Fallback nav zu /helper bei Session-Verlust
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-component-first-mit-qr-scanner-und-manual-code-fall/04-09-SUMMARY.md`.
</output>
