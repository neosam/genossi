---
phase: 04-frontend-component-first-mit-qr-scanner-und-manual-code-fall
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - genossi-frontend/Cargo.toml
  - genossi-frontend/src/js.rs
  - genossi-frontend/src/lib.rs
  - genossi-frontend/src/helper_code.rs
  - genossi-frontend/input.css
  - genossi-frontend/tailwind.config.js
  - genossi-frontend/assets/zxing.umd.min.js
  - genossi-frontend/assets/zxing.umd.min.js.sha256
  - genossi-frontend/assets/README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Cargo.toml hat alle Phase-4-web-sys-Features (MediaDevices, MediaStream*, MediaTrack*, HtmlVideoElement) — KEIN BarcodeDetector-Feature (RESEARCH Pitfall 1)"
    - "js.rs hat einen wasm-bindgen-extern-Block für BarcodeDetector + has_barcode_detector()-Feature-Detection + ZXING_POLYFILL Asset-Konstante"
    - "ZXing-JS-Polyfill ist lokal vendored als assets/zxing.umd.min.js + .sha256 (Apache-2.0, v0.21.3)"
    - "input.css hat den @media print-Block aus UI-SPEC §QrCard print contract — `.qr-card` wird zentriert auf A4 gedruckt"
    - "tailwind.config.js safelist enthält qr-card + amber-Klassen (RESEARCH Pitfall 6 — präventiv, Plan 10 verifiziert post-build)"
    - "Pure-Logic CROCKFORD_ALPHABET-Konstante + is_valid_helper_code(s) Cargo-testbar — single source of truth (RESEARCH Pitfall 9)"
  artifacts:
    - path: "genossi-frontend/Cargo.toml"
      provides: "web-sys feature additions"
      contains: "MediaDevices"
    - path: "genossi-frontend/src/js.rs"
      provides: "BarcodeDetector wasm-bindgen extern + has_barcode_detector + ZXING_POLYFILL Asset"
      contains: "pub fn has_barcode_detector"
    - path: "genossi-frontend/src/helper_code.rs"
      provides: "CROCKFORD_ALPHABET + is_valid_helper_code + sanitize_helper_code_input (Pure Functions, Cargo-testbar)"
      contains: "pub const CROCKFORD_ALPHABET"
    - path: "genossi-frontend/src/lib.rs"
      provides: "Modul-Registrierung helper_code"
      contains: "pub mod helper_code"
    - path: "genossi-frontend/input.css"
      provides: "@media print-Block für .qr-card"
      contains: "@media print"
    - path: "genossi-frontend/tailwind.config.js"
      provides: "Safelist-Ergänzungen für qr-card und amber-Banner"
      contains: "qr-card"
    - path: "genossi-frontend/assets/zxing.umd.min.js"
      provides: "lokal vendored ZXing-JS-Polyfill v0.21.3"
      contains: ""
    - path: "genossi-frontend/assets/zxing.umd.min.js.sha256"
      provides: "SHA256-Pin für vendored asset"
      contains: ""
  key_links:
    - from: "genossi-frontend/src/js.rs::has_barcode_detector"
      to: "window.BarcodeDetector"
      via: "js_sys::Reflect::has"
      pattern: "Reflect::has.*BarcodeDetector"
    - from: "input.css `.qr-card` rule"
      to: "RSX `class: \"qr-card ...\"` (Plan 05 in qr_card.rs)"
      via: "Tailwind utility-Mix + custom-class"
      pattern: "qr-card"
    - from: "helper_code::CROCKFORD_ALPHABET"
      to: "Phase 2 Backend Crockford-Generator"
      via: "Cargo-Test-Verifikation der Identität"
      pattern: "0123456789ABCDEFGHJKMNPQRSTVWXYZ"
---

<objective>
Frontend-Foundation für Phase 4 anlegen: web-sys Camera-Features, ZXing-JS-Polyfill lokal vendoren, JS-Bridge für BarcodeDetector, Print-CSS für QR-Cards, Tailwind-Safelist-Schutz, Crockford-Validation als testbare Pure Function. Alle nachfolgenden Plans (04-04 bis 04-09) hängen von dieser Foundation ab.

Purpose: Build-Infrastruktur und shared Pure-Logic ohne UI-Dependencies. Single source of truth für Crockford-Alphabet (RESEARCH Open Question 2 + Pitfall 9).
Output: 9 Files modifiziert/neu, ein neues Modul `helper_code` mit cargo-testbaren Funktionen (9 Tests via `cd genossi-frontend && cargo test helper_code` — verified pattern: `cargo test member_search` läuft im Repo 8/8 grün), ein vendor-Asset für offline-tauglichen Polyfill.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/neosam/programming/rust/projects/genossi3/.planning/phases/04-frontend-component-first-mit-qr-scanner-und-manual-code-fall/04-CONTEXT.md
@/home/neosam/programming/rust/projects/genossi3/.planning/phases/04-frontend-component-first-mit-qr-scanner-und-manual-code-fall/04-RESEARCH.md
@/home/neosam/programming/rust/projects/genossi3/.planning/phases/04-frontend-component-first-mit-qr-scanner-und-manual-code-fall/04-PATTERNS.md
@/home/neosam/programming/rust/projects/genossi3/.planning/phases/04-frontend-component-first-mit-qr-scanner-und-manual-code-fall/04-UI-SPEC.md
@/home/neosam/programming/rust/projects/genossi3/genossi-frontend/CLAUDE.md
@/home/neosam/programming/rust/projects/genossi3/genossi-frontend/Cargo.toml
@/home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/js.rs
@/home/neosam/programming/rust/projects/genossi3/genossi-frontend/input.css
@/home/neosam/programming/rust/projects/genossi3/genossi-frontend/tailwind.config.js

**Pattern-Anker (file:line):**
- `genossi-frontend/Cargo.toml:39-63` — bestehende web-sys-Features-Liste (Phase 4 ergänzt 6 Einträge ans Ende)
- `genossi-frontend/src/js.rs:1-22` — bestehender wasm-bindgen-extern-Block für CodeMirror; Phase 4 appended BarcodeDetector + ZXING_POLYFILL-Asset im gleichen Stil
- `genossi-frontend/src/component/member_search.rs:1-30` — Pattern für Pure Function + `#[cfg(test)] mod tests` (Vorlage für `helper_code` Tests)
- `genossi-frontend/input.css:1-25` — bestehender `@layer utilities`-Block; Phase 4 ergänzt `@media print` darunter
- `genossi-frontend/tailwind.config.js:20-28` — bestehende Safelist mit print-Klassen; Phase 4 ergänzt `qr-card` + `bg-amber-100`/`text-amber-900`/`border-amber-400` + `animate-spin`/`animate-pulse`
- `genossi-frontend/src/page/not_authenticated.rs:18` — Beispiel `asset!("/assets/shifty.webp")` (manganis-Asset-Pattern)

**RESEARCH Pitfalls (Plan 02-spezifisch):**
- Pitfall 1: NICHT `BarcodeDetector` als web-sys-Feature einsetzen — feature existiert nicht in web-sys 0.3.97. Nur via wasm-bindgen extern.
- Pitfall 6: Tailwind-Purge entfernt `.qr-card` falls nicht im Source erkannt → safelist erweitern (präventiv hier; Plan 10 verifiziert post-build).
- Pitfall 9: Crockford-Alphabet hat I, L, O, U **excluded** (nur 32 chars: `0123456789ABCDEFGHJKMNPQRSTVWXYZ`). UI-SPEC-Range-Notation `0-9A-HJ-NP-Z` würde `L` und `U` einschließen — daher in Code KEINE Range-Regex, sondern explizite Whitelist.
- Pitfall 14: `print:`-Tailwind-Variant ist konfiguriert (`tailwind.config.js:9-11` zeigt `screens: { print: { raw: 'print' } }`) — nichts zu ändern.

**ZXing-JS Vendoring Procedure** (UI-SPEC §"ZXing-JS Vendoring Procedure"):
1. `mkdir -p genossi-frontend/assets/`
2. `curl -sL https://unpkg.com/@zxing/library@0.21.3/umd/index.min.js -o genossi-frontend/assets/zxing.umd.min.js`
3. `cd genossi-frontend && sha256sum assets/zxing.umd.min.js > assets/zxing.umd.min.js.sha256`
4. README für Reviewer-Verifikation
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time → vendored asset | Apache-2.0 vetted JS-Lib (UI-SPEC §Registry Safety); SHA256-pinned, kein CDN-Runtime-Fetch |
| Browser → BarcodeDetector | Browser-API; reines Frame-Decoding, keine Netzwerk-Egress |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | T (Tampering) | Vendored ZXing-JS-Asset | mitigate | SHA256-Pin als `.sha256`-File neben dem JS — Reviewer kann mit `sha256sum -c` verifizieren; keine Auto-Update via npm/CDN |
| T-04-07 | I (Information Disclosure) | BarcodeDetector-Feature-Detection-Probe | accept | `js_sys::Reflect::has(window, "BarcodeDetector")` ist read-only, keine User-Daten betroffen |
| T-04-08 | E (Elevation of Privilege) | helper_code Validation als Security-Boundary | mitigate | Modul-Doccomment: "UX-only validation — backend D-24 ist authoritative; Frontend-Filter NUR Convenience" — explizit dokumentiert |
| T-04-09 | T (Tampering) | Tailwind-Purge entfernt amber-/qr-card-Klassen | mitigate | safelist-Ergänzung erzwingt Erhalt; Plan 10 verifiziert per `grep qr-card dist/assets/tailwind.css` post-build |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Cargo.toml web-sys features + ZXing-JS vendoring + assets/README.md</name>
  <files>
    genossi-frontend/Cargo.toml,
    genossi-frontend/assets/zxing.umd.min.js,
    genossi-frontend/assets/zxing.umd.min.js.sha256,
    genossi-frontend/assets/README.md
  </files>
  <read_first>
    - /home/neosam/programming/rust/projects/genossi3/genossi-frontend/Cargo.toml:39-63 (existing web-sys features-Liste)
    - /home/neosam/programming/rust/projects/genossi3/.planning/phases/04-frontend-component-first-mit-qr-scanner-und-manual-code-fall/04-UI-SPEC.md:670-696 (ZXing-JS Vendoring Procedure)
  </read_first>
  <action>
    1. **Cargo.toml ergänzen** (`genossi-frontend/Cargo.toml`) — am Ende des `[dependencies.web-sys] features = [...]` Arrays vor dem schließenden `]`:
       ```toml
       "MediaDevices",
       "MediaStream",
       "MediaStreamTrack",
       "MediaStreamConstraints",
       "MediaTrackConstraints",
       "HtmlVideoElement",
       ```
       **NICHT hinzufügen:** `"BarcodeDetector"` — RESEARCH Pitfall 1; existiert nicht in web-sys 0.3.97. Cargo-Build würde failen mit "unknown feature".
       **NICHT hinzufügen:** `"Navigator"`, `"Window"` — bereits vorhanden (Cargo.toml Zeile 42-43, verifiziert).

    2. **ZXing-JS lokal vendoren** (UI-SPEC §"ZXing-JS Vendoring Procedure"):
       ```bash
       mkdir -p /home/neosam/programming/rust/projects/genossi3/genossi-frontend/assets
       curl -sL https://unpkg.com/@zxing/library@0.21.3/umd/index.min.js \
           -o /home/neosam/programming/rust/projects/genossi3/genossi-frontend/assets/zxing.umd.min.js
       cd /home/neosam/programming/rust/projects/genossi3/genossi-frontend && \
           sha256sum assets/zxing.umd.min.js > assets/zxing.umd.min.js.sha256
       ```
       Verify download: `wc -c genossi-frontend/assets/zxing.umd.min.js` muss > 100000 (~200KB erwartet).
       Verify pin: `cd genossi-frontend && sha256sum -c assets/zxing.umd.min.js.sha256` → "OK".

    3. **assets/README.md anlegen** (`genossi-frontend/assets/README.md`):
       ```markdown
       # Phase 4 Vendored Assets

       ## zxing.umd.min.js

       - **Why:** Phase 4 D-02 Polyfill für QR-Scanning auf Browsern ohne native BarcodeDetector
         (de facto: alle iOS Safari < 26.5 — siehe RESEARCH §Browser-Support-Realität).
         Lokal vendored statt CDN, damit Vereinsheim-WiFi-Aussetzer (Phase-5-Risiko) das
         QR-Scanning nicht blockieren.
       - **Pinned version:** 0.21.3 — siehe `04-UI-SPEC.md` §"ZXing-JS Vetting" (vetted 2026-05-04).
       - **License:** Apache-2.0 (kompatibel mit Genossi-Commercial-Use).
       - **Source:** https://unpkg.com/@zxing/library@0.21.3/umd/index.min.js
       - **SHA256-Verifikation:**
         ```bash
         cd genossi-frontend && sha256sum -c assets/zxing.umd.min.js.sha256
         ```
       - **Update-Procedure:** wenn upgraden auf eine neuere Version, neue SHA256 berechnen
         und REVISION-Eintrag hier ergänzen. NIEMALS `@latest` — bleeding-edge-Releases müssen
         geprüft werden.
       ```

    4. Commit: `chore(04-02): add web-sys camera features + vendor ZXing-JS 0.21.3`
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/genossi3/genossi-frontend && cargo build 2>&1 | tail -3 && grep -q '"MediaDevices"' Cargo.toml && grep -q '"HtmlVideoElement"' Cargo.toml && ! grep -q '"BarcodeDetector"' Cargo.toml && test -f assets/zxing.umd.min.js && test -f assets/zxing.umd.min.js.sha256 && test -f assets/README.md && (cd assets && sha256sum -c zxing.umd.min.js.sha256)</automated>
  </verify>
  <done>Cargo build grün; web-sys-Features ergänzt OHNE BarcodeDetector; assets/zxing.umd.min.js + .sha256 + README.md committed; sha256sum -c verifiziert OK.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: js.rs BarcodeDetector-Bridge + helper_code Pure-Logic-Modul + Cargo-Tests</name>
  <files>
    genossi-frontend/src/js.rs,
    genossi-frontend/src/helper_code.rs,
    genossi-frontend/src/lib.rs
  </files>
  <read_first>
    - /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/js.rs:1-30 (existing CodeMirror-Bridge-Pattern als Vorlage)
    - /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/lib.rs (Modulregistrierung)
    - /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/component/member_search.rs:1-247 (`#[cfg(test)] mod tests` Pattern für Pure Functions — verified-anchor: `cd genossi-frontend && cargo test member_search` läuft 8/8 grün im Repo, gleicher Befehl funktioniert für `helper_code`)
  </read_first>
  <behavior>
    - **`is_valid_helper_code(s: &str) -> bool`**:
      - True wenn s.chars().count() == 10 UND alle Zeichen in CROCKFORD_ALPHABET
      - False für leeren String, < 10, > 10, Lowercase, Zeichen aus {I, L, O, U}
    - **`sanitize_helper_code_input(raw: &str) -> String`**:
      - Uppercase first
      - Filter on alphabet (drops {I, L, O, U}, dashes, spaces, special chars)
      - Truncate to 10 Crockford-chars
    - **9 Cargo-Unit-Tests** in `helper_code::tests`:
      - alphabet_has_exactly_32_chars
      - alphabet_excludes_i_l_o_u
      - alphabet_includes_all_digits
      - is_valid_helper_code_accepts_10_char_uppercase
      - is_valid_helper_code_rejects_wrong_length (empty, 6, 11)
      - is_valid_helper_code_rejects_excluded_letters (I, L, O, U)
      - is_valid_helper_code_rejects_lowercase
      - sanitize_uppercases_and_filters
      - sanitize_truncates_to_10_chars
  </behavior>
  <action>
    1. **`genossi-frontend/src/js.rs` ergänzen** (Append nach den existierenden CodeMirror-externs am Datei-Ende):
       ```rust
       // ─── Phase 4 Plan 02 ─── BarcodeDetector + ZXing-Polyfill bridge ─────────────

       /// Browser-native Barcode/QR detection.
       /// NOT available in iOS Safari (any version through 26.5) — see RESEARCH.md §Browser-Support-Realität.
       /// `has_barcode_detector()` MUST be called first; if false, fall back to ZXing-JS polyfill.
       #[wasm_bindgen]
       extern "C" {
           pub type BarcodeDetector;

           #[wasm_bindgen(constructor)]
           pub fn new(options: &JsValue) -> BarcodeDetector;

           #[wasm_bindgen(method)]
           pub fn detect(this: &BarcodeDetector, source: &JsValue) -> js_sys::Promise;
       }

       /// Feature detection: `'BarcodeDetector' in window`.
       /// Source: RESEARCH.md §"BarcodeDetector via wasm-bindgen extern".
       pub fn has_barcode_detector() -> bool {
           let window = match web_sys::window() {
               Some(w) => w,
               None => return false,
           };
           js_sys::Reflect::has(&window, &JsValue::from_str("BarcodeDetector"))
               .unwrap_or(false)
       }

       /// Asset path for the lazy-loaded ZXing-JS polyfill (used by qr_scanner.rs in Plan 05
       /// when has_barcode_detector() == false).
       /// Manganis fingerprints the path at build time — see Pitfall 7 in RESEARCH.md.
       pub const ZXING_POLYFILL: manganis::Asset = manganis::asset!("/assets/zxing.umd.min.js");
       ```

    2. **Neues Modul `genossi-frontend/src/helper_code.rs` anlegen** (Pure Logic, single source of truth):
       ```rust
       //! Helper-Code-Validierung (Phase 4 Plan 02).
       //!
       //! UX-Validation für `ManualCodeInput` (Plan 05) — schnelles Feedback ohne Roundtrip.
       //! KEINE Security-Boundary: Backend D-24 (Phase 2 Plan 02-05) ist authoritative.
       //! Diese Konstante MUSS deckungsgleich mit Phase-2-Backend-Crockford-Alphabet sein
       //! (siehe RESEARCH.md §"Pitfall 9 — Crockford-Alphabet-Discrepancy").

       /// Crockford Base32 alphabet — 32 characters.
       /// Excludes I, L, O, U (per Phase 2 D-09).
       pub const CROCKFORD_ALPHABET: &str = "0123456789ABCDEFGHJKMNPQRSTVWXYZ";

       /// True iff `s` has exactly 10 chars, all from `CROCKFORD_ALPHABET`.
       pub fn is_valid_helper_code(s: &str) -> bool {
           s.chars().count() == 10 && s.chars().all(|c| CROCKFORD_ALPHABET.contains(c))
       }

       /// Filters input to Crockford-Base32, uppercases, truncates to 10 chars.
       pub fn sanitize_helper_code_input(raw: &str) -> String {
           raw.to_uppercase()
               .chars()
               .filter(|c| CROCKFORD_ALPHABET.contains(*c))
               .take(10)
               .collect()
       }

       #[cfg(test)]
       mod tests {
           use super::*;

           #[test]
           fn alphabet_has_exactly_32_chars() {
               assert_eq!(CROCKFORD_ALPHABET.chars().count(), 32);
           }

           #[test]
           fn alphabet_excludes_i_l_o_u() {
               assert!(!CROCKFORD_ALPHABET.contains('I'));
               assert!(!CROCKFORD_ALPHABET.contains('L'));
               assert!(!CROCKFORD_ALPHABET.contains('O'));
               assert!(!CROCKFORD_ALPHABET.contains('U'));
           }

           #[test]
           fn alphabet_includes_all_digits() {
               for c in '0'..='9' {
                   assert!(CROCKFORD_ALPHABET.contains(c), "missing digit {c}");
               }
           }

           #[test]
           fn is_valid_helper_code_accepts_10_char_uppercase() {
               assert!(is_valid_helper_code("ABC1234567"));
               assert!(is_valid_helper_code("0123456789"));
               assert!(is_valid_helper_code("ZYXWVTSRQP"));
           }

           #[test]
           fn is_valid_helper_code_rejects_wrong_length() {
               assert!(!is_valid_helper_code(""));
               assert!(!is_valid_helper_code("ABC123"));
               assert!(!is_valid_helper_code("ABC12345678")); // 11 chars
           }

           #[test]
           fn is_valid_helper_code_rejects_excluded_letters() {
               assert!(!is_valid_helper_code("ABCIDEFGHJ")); // I
               assert!(!is_valid_helper_code("ABCLDEFGHJ")); // L
               assert!(!is_valid_helper_code("ABCODEFGHJ")); // O
               assert!(!is_valid_helper_code("ABCUDEFGHJ")); // U
           }

           #[test]
           fn is_valid_helper_code_rejects_lowercase() {
               assert!(!is_valid_helper_code("abc1234567"));
           }

           #[test]
           fn sanitize_uppercases_and_filters() {
               assert_eq!(sanitize_helper_code_input("abc123!@#"), "ABC123");
           }

           #[test]
           fn sanitize_truncates_to_10_chars() {
               // Input upper: ABCDEFGHIJKLMNOP. Filter drops I, L, O. Take 10:
               // A B C D E F G H J K (skipped I, then J K from the rest)
               assert_eq!(sanitize_helper_code_input("ABCDEFGHIJKLMNOP"), "ABCDEFGHJK");
           }
       }
       ```

    3. **`genossi-frontend/src/lib.rs` erweitern** — `pub mod helper_code;` ergänzen (zwischen den existierenden `pub mod`-Deklarationen).

    4. **Cargo-Tests laufen** (Pure Logic — KEIN WASM nötig — auf native Target). **Wichtig:** Das Frontend-Crate ist ein Binary (`cargo run --bin genossi-frontend`), daher schlägt `cargo test -p genossi-frontend` fehl mit "package ID specification ... did not match". Stattdessen aus dem Verzeichnis testen (Pattern verifiziert via existing `member_search`-Tests, 8/8 passed):
       ```bash
       cd /home/neosam/programming/rust/projects/genossi3/genossi-frontend && cargo test helper_code 2>&1 | tail -10
       ```
       9 Tests grün.

    5. Commit: `feat(04-02): js bridge for BarcodeDetector + Crockford helper_code module with 9 unit tests`
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/genossi3/genossi-frontend && cargo test helper_code 2>&1 | tee /tmp/04-02-t2.log | grep -E 'test result.*9 passed.*0 failed' && cargo build 2>&1 | tail -3 && grep -q "pub mod helper_code" src/lib.rs && grep -q "pub fn has_barcode_detector" src/js.rs && grep -q "pub const ZXING_POLYFILL" src/js.rs</automated>
  </verify>
  <done>9 Cargo-Tests in `helper_code::tests` grün; js.rs hat `has_barcode_detector` + `ZXING_POLYFILL`; `pub mod helper_code` in lib.rs; Build grün.</done>
</task>

<task type="auto">
  <name>Task 3: input.css print rules + tailwind safelist erweitern</name>
  <files>
    genossi-frontend/input.css,
    genossi-frontend/tailwind.config.js
  </files>
  <read_first>
    - /home/neosam/programming/rust/projects/genossi3/genossi-frontend/input.css:1-25 (existing @layer utilities; Phase 4 ergänzt darunter @media print)
    - /home/neosam/programming/rust/projects/genossi3/genossi-frontend/tailwind.config.js:20-28 (existing safelist)
    - /home/neosam/programming/rust/projects/genossi3/.planning/phases/04-frontend-component-first-mit-qr-scanner-und-manual-code-fall/04-UI-SPEC.md:373-410 (QrCard print contract — exakter CSS-Block)
  </read_first>
  <action>
    1. **`genossi-frontend/input.css`** — am Datei-Ende den Print-Block ergänzen (nach dem schließenden `}` des `@layer utilities`):
       ```css

       /* ─── Phase 4 Plan 02 ─── Print rules for QrCard (UI-SPEC §QrCard print contract) */
       @media print {
           /* Hide everything except the .qr-card being printed. */
           body * {
               visibility: hidden;
           }
           .qr-card,
           .qr-card * {
               visibility: visible;
           }
           .qr-card {
               position: absolute;
               left: 50%;
               top: 50%;
               transform: translate(-50%, -50%);
               box-shadow: none;
               border: none;
               max-width: 80mm;
           }
           .qr-card .w-64 {
               /* QR SVG fills printable area */
               width: 60mm;
               height: 60mm;
           }
           .qr-card .font-mono {
               font-size: 16pt;
               letter-spacing: 0.15em;
           }
           @page {
               size: A4 portrait;
               margin: 16mm;
           }
       }

       /* Ensure background colors print (Chrome default = off) */
       body {
           -webkit-print-color-adjust: exact;
           print-color-adjust: exact;
       }
       ```

    2. **`genossi-frontend/tailwind.config.js` safelist-Array erweitern** — die existing Einträge UNVERÄNDERT lassen, ergänzen:
       ```js
         safelist: [
           "bg-red-200",
           "print:bg-white",
           "cursor-not-allowed",
           "text-green-800",
           "text-red-800",
           "bg-missingColor",
           "bg-blockedColor",
           // ─── Phase 4 Plan 02 ─── (RESEARCH Pitfall 6 — präventive Erhaltung)
           "qr-card",
           "bg-amber-100",
           "text-amber-900",
           "border-amber-400",
           "border-b-2",
           "animate-spin",
           "animate-pulse",
           "print:hidden",
         ]
       ```

    3. Commit: `style(04-02): print CSS for QrCard + tailwind safelist for amber + qr-card`
  </action>
  <verify>
    <automated>grep -q "@media print" /home/neosam/programming/rust/projects/genossi3/genossi-frontend/input.css && grep -q ".qr-card" /home/neosam/programming/rust/projects/genossi3/genossi-frontend/input.css && grep -q "print-color-adjust" /home/neosam/programming/rust/projects/genossi3/genossi-frontend/input.css && grep -q '"qr-card"' /home/neosam/programming/rust/projects/genossi3/genossi-frontend/tailwind.config.js && grep -q '"bg-amber-100"' /home/neosam/programming/rust/projects/genossi3/genossi-frontend/tailwind.config.js && grep -q '"animate-spin"' /home/neosam/programming/rust/projects/genossi3/genossi-frontend/tailwind.config.js</automated>
  </verify>
  <done>input.css hat den @media-print-Block exakt aus UI-SPEC; Tailwind-Safelist enthält qr-card + amber-/animate-Klassen für Phase-4-Components.</done>
</task>

</tasks>

<verification>
```bash
# Build the frontend (compile-time validation)
cd /home/neosam/programming/rust/projects/genossi3 && cargo build -p genossi-frontend 2>&1 | tail -3

# Pure-Logic-Tests — Frontend-Crate ist Binary, daher `cd` und ohne -p
cd /home/neosam/programming/rust/projects/genossi3/genossi-frontend && cargo test helper_code 2>&1 | grep -E 'test result.*9 passed'

# SHA256-Pin-Verifikation
cd /home/neosam/programming/rust/projects/genossi3/genossi-frontend && sha256sum -c assets/zxing.umd.min.js.sha256

# BarcodeDetector NICHT als Cargo-Feature (Pitfall 1)
! grep -q '"BarcodeDetector"' /home/neosam/programming/rust/projects/genossi3/genossi-frontend/Cargo.toml && echo "OK: no BarcodeDetector feature"

# Anchor for downstream plans
grep -q "pub fn has_barcode_detector" /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/js.rs && echo "OK: has_barcode_detector exposed"
grep -q "pub const ZXING_POLYFILL" /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/js.rs && echo "OK: ZXING_POLYFILL exposed"
grep -q "pub const CROCKFORD_ALPHABET" /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/helper_code.rs && echo "OK: CROCKFORD_ALPHABET exposed"
```

</verification>

<success_criteria>
- `cargo build -p genossi-frontend` grün — keine "unknown feature" Errors
- 9 helper_code-Cargo-Tests grün
- `assets/zxing.umd.min.js` (~200KB) + `.sha256` + README.md vorhanden, SHA-Pin-Verifikation OK
- `js.rs` hat `pub fn has_barcode_detector`, `pub const ZXING_POLYFILL`, BarcodeDetector-extern-Block
- `helper_code.rs` hat CROCKFORD_ALPHABET (32 chars), is_valid_helper_code, sanitize_helper_code_input
- `input.css` enthält @media print mit `.qr-card` Block (UI-SPEC-konform)
- Tailwind-Safelist enthält `qr-card`, `bg-amber-100`, `text-amber-900`, `animate-spin`, `animate-pulse`
- KEIN `"BarcodeDetector"`-Feature in Cargo.toml (Pitfall 1)
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-component-first-mit-qr-scanner-und-manual-code-fall/04-02-SUMMARY.md`.
</output>
