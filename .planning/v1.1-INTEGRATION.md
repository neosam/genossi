---
milestone: v1.1
milestone_name: Anteile-Rückzahlungsphase
audited: 2026-06-02
auditor: integration-checker
status: passed
scope:
  phases: [07, 08, 09, 10, 11, 12, 13]
  requirements_total: 30
  requirements_complete_or_in_progress: 30
  requirements_deferred: 1 (EXPO-04 → v2)
summary:
  cross_phase_wirings_verified: 9 of 9
  api_routes_with_consumer: 11 of 11
  e2e_flows_complete: 7 of 7 (5 production + 2 UI)
  orphaned_exports: 0
  missing_connections: 0
  unprotected_pages: 0
  pending_human_verification: 1 (UI-06 Auth-Gate UAT — non-blocker)
---

# Milestone v1.1 — Cross-Phase Integration Audit

**Verdict: PASSED**

Alle 7 Phasen (07–13) sind end-to-end verdrahtet. Keine BLOCKER, keine fehlenden Connections. Ein WARNING (Phase 12 UI-06 UAT-Auth-Gate-Checkpoints) bleibt offen, ist aber kein Integrations-Defekt — die Code-Pfade existieren und sind grep-verifiziert (`RequirePrivilege` auf allen 3 Repayment-Pages).

## Wiring-Summary

| Layer-Übergang | Connected | Orphaned | Missing |
|----------------|-----------|----------|---------|
| DAO → Service-Impl | 9 (RepaymentPhaseDao, RepaymentEntryDao, MemberActionDao, MemberDocumentDao, MailJobDao, MailTemplateDao, AuditLogDao, MemberDao, TransactionDao) | 0 | 0 |
| Service-Impl → REST-Handler | 11 (4 phase + 6 entry + 1 mark-paid-out incl. + 1 mail/send-bulk + 1 export + 1 letter) | 0 | 0 |
| REST-Handler → Frontend-API-Client | 11 (12 Endpoints zuzgl. Mail-Send-Bulk; alle 1:1 in genossi-frontend/src/api.rs) | 0 | 0 |
| Frontend-API → Page/Component | 11 (RepaymentPhases, RepaymentPhaseDetails, RepaymentEntryList, ExportTab, MailPage, BulkLetter-Button) | 0 | 0 |
| Worker → Audit-Pipeline (Mail) | 1 (start_mail_worker → 14-arg-Spawn mit gemeinsamem audit_log_dao via Arc::clone — single hash chain) | 0 | 0 |

## API-Routen + Consumer-Mapping

| REST-Endpoint | Backend-Handler | Frontend-Caller | Status |
|---------------|-----------------|-----------------|--------|
| `POST/GET/PUT/DELETE /api/repayment-phase{/...}` | `genossi_rest/src/repayment_phase.rs` | `api.rs:2150-2207` | WIRED |
| `POST /api/repayment-phase/{id}/open` | `repayment_phase.rs` | `api.rs:2196` | WIRED |
| `POST /api/repayment-phase/{id}/close` | `repayment_phase.rs` | `api.rs:2207` | WIRED |
| `GET/POST/PUT/DELETE /api/repayment-entry{/...}` | `genossi_rest/src/repayment_entry.rs` | `api.rs:2221-2266` | WIRED |
| `POST /api/repayment-entry/batch-status` | `repayment_entry.rs:268` | `api.rs:2280` | WIRED |
| `POST /api/repayment-entry/{id}/mark-paid-out` | `repayment_entry.rs:332` | `api.rs:2292` | WIRED |
| `POST /api/mail/send-bulk` (+ template_id, repayment_phase_id Body-Fields) | `genossi_mail/src/rest.rs:326` | `api.rs:890` | WIRED |
| `GET /api/repayment-phase/{id}/export/{format}` | `genossi_rest/src/repayment_export.rs:116` | `page/repayment_phase_details.rs:103` (build_export_url) | WIRED |
| `POST /api/repayment-phase/{phase_id}/letters/generate` | `genossi_rest/src/repayment_letter.rs:102` | `api.rs:1970-1979` (generate_repayment_letters) | WIRED |

## E2E-Flow-Verifikation

### Flow A — Anteils-Rückzahlung Lifecycle (Vorstand) — PASSED

| Schritt | REQ | Code-Pfad | Status |
|---------|-----|-----------|--------|
| 1. RepaymentPhase create + open → Auto-Fill | PHAS-01, PHAS-02, ENTR-01 | `genossi_service_impl/src/repayment_phase.rs::open_repayment_phase` (267) iteriert Members + erzeugt Entries; verifiziert in `repayment_phase.rs:1648 test_open_phase_auto_fill_creates_entries_for_matching_members` + E2E `test_repayment_phase_lifecycle_audit_chain_intact` | WIRED |
| 2. Entry-Status toggeln + Manual-Entry | ENTR-02..06 | `repayment_entry.rs::create/update/delete/batch_toggle_status` + Re-Read Pattern (CR-01-Fix); REST: `/api/repayment-entry`, `/batch-status` | WIRED |
| 3. Bulk-Massenmail mit Repayment-Context | MAIL-01..04 | `genossi_mail/src/rest.rs:326 send_bulk_mail` → routet auf `validate_template_with_repayment` wenn `repayment_phase_id` gesetzt → worker (`worker.rs:269 if let Some(phase_id) = job.repayment_phase_id`) merged Context via `merge_repayment_context` → audited MemberDocument-create pro Empfänger | WIRED |
| 4. Bulk-Anschreiben PDF (für Members ohne Email) | BRIEF-01 | `repayment_letter::generate_letters` → Bundle-PDF + audited `MemberDocument`-create pro Member (`repayment_letter.rs:391 audited_create!`) → Direct-Download mit `X-Document-Count`-Header | WIRED |
| 5. PDF-Export Auszahlungsliste | EXPO-01,02,03,05 | `repayment_export.rs:116 export_repayment` → Format-Whitelist (`pdf` only, csv/xlsx → 400); E2E `test_export_repayment_pdf_open_happy_path` etc.; **EXPO-05**: Compile-Gate `no_audit_macros_used` in `repayment_export.rs:679` schließt Audit-Schreiben aus | WIRED |
| 6. Mark-paid-out Cascade | PAYO-01..04 | `repayment_entry.rs:517 mark_paid_out` — 12-Schritt-Cascade: 1× audited_create! (MemberAction::Verkauf, shares_change=-N) + 2× audited_update! (Member.current_shares -= N + action_count +1, Entry.status=PaidOut), alle 3 mit `REPAYMENT_ENTRY_PROCESS_MARK_PAID_OUT`; PAYO-03/04 als Pre-Checks; E2E `test_mark_paid_out_happy_path_cascade` verifiziert Audit-Verify | WIRED |
| 7. Phase Close (audited) | PHAS-03, PHAS-04, PHAS-05 | `repayment_phase.rs::close_repayment_phase` — Pending-Entry-Validation (CloseConflictResponse mit pending_member_numbers bei 409) + audited_update mit `repayment-phase.close` | WIRED |

### Flow B — Frontend Read-Path (Vorstand UI) — PASSED

| Schritt | REQ | Code-Pfad | Status |
|---------|-----|-----------|--------|
| 1. Phase-Liste + Detail-Page | UI-01, UI-02 | `genossi-frontend/src/page/repayment_phases.rs` + `repayment_phase_details.rs`; Router: `router.rs:42,44` (`/repayment-phases`, `/repayment-phases/:id`) | WIRED |
| 2. Entry-Tabelle Sort/Filter (Shared Component) | UI-03 | `genossi-frontend/src/component/repayment_entry_list.rs` (re-exported via `component/mod.rs:106`) — Multi-Select, Status-Filter, Sortierung | WIRED |
| 3. Bulk-Actions Bar (Mail + Letter) | UI-04, UI-06, BRIEF-01 | `repayment_entry_list.rs:140 on_mail_request` + `144 on_letter_request` Props; Detail-Page wired beide Closures (`repayment_phase_details.rs:247 mail-redirect via window.location`; `262 letter-call via api::generate_repayment_letters`) | WIRED |
| 4. Mail-Send-Dialog mit Repayment-Vars | UI-06, MAIL-01..04 | `page/mail_page.rs::parse_mail_query` (7 Unit-Tests) parst `?from=repayment&phase_id=&members=` aus Query → pre-selected Empfänger + TemplateVarButtons (`show_repayment_vars`-Prop) → `selected_template_id`-Signal → send_bulk_mail mit beiden UUIDs | WIRED |
| 5. Auth-Gate (Non-Admin → AccessDeniedPage) | UI-06 (Auth-Pfad) | 3 Pages haben `RequirePrivilege { fallback: AccessDeniedPage }`: `repayment_phases.rs:69`, `repayment_phase_details.rs:164`, `mail_page.rs:216` | WIRED (code-path; UAT pending) |

## DI-Wiring (genossi_bin/src/lib.rs)

| Service | Field | Construction | RestState-Impl |
|---------|-------|--------------|----------------|
| RepaymentPhaseService | 594 | 862 | 1587 (Phase 7) |
| RepaymentEntryService | 596 | 879 | 1597 (Phase 8) |
| RepaymentExportService | 606 | 933 | 1633 (Phase 11) |
| RepaymentLetterService | 616 | 965 | 1640-1644 (Phase 13) |
| MailWorker spawn | n/a | `start_mail_worker:1325` mit 14 Args | gemeinsamer `audit_log_dao` (Arc) → single hash chain — Phase 10 Plan 07 verifiziert |

**Arc-Sharing verifiziert:** `repayment_phase_dao` und `repayment_entry_dao` werden GENAU EINMAL gebaut (`Arc::new(...)`) und via `.clone()` an mehrere Services geteilt (RepaymentPhaseService + RepaymentEntryService + RepaymentLetterService + RepaymentExportService). Audit-Hash-Chain bleibt single-source — `audit_log_dao.clone()` aus dem zentralen Konstruktionspunkt.

## Requirements Integration Map

| REQ | Integration-Pfad | Status | Issue |
|-----|------------------|--------|-------|
| PHAS-01 | DAO (Phase 7-01) → Service (07-03 `create_repayment_phase`) → REST `POST /api/repayment-phase` (07-04) → Frontend `api::create_repayment_phase` (api.rs:2170) → `RepaymentPhases`-Page | WIRED | — |
| PHAS-02 | Service (07-03 skeleton + 08-04 auto-fill) → `open_repayment_phase` ruft `RepaymentEntryDao.create()` für jeden Member im fiscal_year via Arc-shared DAO | WIRED | — |
| PHAS-03 | Service (07-03 skeleton + 08-04 pending-validation) → `close_repayment_phase` zählt Pending-Entries; CloseConflictResponse-Struct gemeinsam mit Frontend `api.rs:2127` (parse_close_conflict in Page) | WIRED | — |
| PHAS-04 | Service `update_repayment_phase` (Edit-Matrix D-04, D-07) → REST `PUT /api/repayment-phase/{id}` → Frontend Inline-Edit in BasicsTab | WIRED | — |
| PHAS-05 | `audited_create!`/`audited_update!`/`audited_delete!` mit Process-Konstanten `repayment-phase.{create,update,open,close,delete}` — Audit-Disziplin-Grep-Gate in Plan 07-03 = 0 direkte DAO-Schreibvorgänge | WIRED | — |
| ENTR-01 | Auto-Fill in `open_repayment_phase` (Phase 8-04) → iteriert via MemberDao alle Members mit exit_date im fiscal_year → erzeugt RepaymentEntry mit `share_count_to_pay_out = Member.current_shares` | WIRED | — |
| ENTR-02 | Service `create_repayment_entry` → REST `POST /api/repayment-entry` → Frontend `AddEntryModal` (Plan 12-09) mit MemberSearch | WIRED | — |
| ENTR-03 | DAO ohne Composite-PK auf (member_id, phase_id) — verifiziert in Migration `20260530xxx_create_repayment_entry_table.sql` | WIRED | — |
| ENTR-04 | Service `update_repayment_entry` (Edit-Matrix: PaidOut → 409) → REST `PUT /api/repayment-entry/{id}` → Frontend `EditableShareCountCell` | WIRED | — |
| ENTR-05 | Service `delete_repayment_entry` (Status-Guard: PaidOut → 409) → REST `DELETE /api/repayment-entry/{id}` → Frontend Soft-Delete-Button | WIRED | — |
| ENTR-06 | Service `batch_toggle_status` mit BatchFailureResponse (404 für unknown IDs, CR-02-Fix) → REST `POST /batch-status` → Frontend Bulk-Action-Bar | WIRED | — |
| PAYO-01..04 | `mark_paid_out` 12-Schritt-Cascade in einer SQLite-Tx (1× audited_create! MemberAction::Verkauf + 2× audited_update! Member+Entry) → REST `POST /{id}/mark-paid-out` (Phase 9-02) → Frontend `PayoutConfirmModal` (UI-05) → E2E-verifiziert (audit-chain valid + Race-Defense) | WIRED | — |
| MAIL-01 | `send_bulk_mail` REST mit Multi-Recipient-Array → Worker iteriert + persistiert MailJob → Worker sendet pro Empfänger | WIRED | — |
| MAIL-02 | Template-Variable `{{ payout_amount }}` → `merge_repayment_context` in Worker (`worker.rs:355`) berechnet `share_count_to_pay_out × phase.share_value` zur Sendezeit | WIRED | — |
| MAIL-03 | Template-Variablen `{{ share_count }}`, `{{ fiscal_year }}` → `merge_repayment_context` (selber Pfad wie MAIL-02) | WIRED | — |
| MAIL-04 | Worker erzeugt MemberDocument per `try_create_member_document_audited` (worker.rs:546) via `audited_create!` mit Template-Referenz; Single-Hash-Chain via gemeinsam-genutzter `audit_log_dao` | WIRED | — |
| EXPO-01 | `export_repayment` akzeptiert Open UND Closed Phases (`check_admin_and_phase_status` mit Open+Closed-Matcher in service_impl::repayment_export.rs) | WIRED | — |
| EXPO-02 | PDF-Renderer in `repayment_export.rs` (PDF-Generation via typst) generiert pro Eintrag: Mitgliedsnummer, Name, IBAN, share_count, Auszahlungsbetrag, Verwendungszweck; sortiert nach Mitgliedsnummer | WIRED | — |
| EXPO-03 | Query-Param `?include=open\|all\|paid` via `ExportIncludeQuery` (`repayment_export.rs:185 default=Open`) → Frontend `ExportInclude`-Enum mit Radio-Filter (Plan 12-14) | WIRED | — |
| EXPO-04 | — | DEFERRED (v2, D-12 Phase-11-Discuss) | Out of scope für v1.1 |
| EXPO-05 | Compile-Gate Test `no_audit_macros_used` in `genossi_service_impl/src/repayment_export.rs:679` verifiziert via grep dass keine audited_create!/audited_update!/audited_delete! im Modul vorkommen | WIRED | — |
| UI-01 | `page/repayment_phases.rs` mit Listing (Status, fiscal_year, share_value, Anzahl Einträge); Router `/repayment-phases` | WIRED | — |
| UI-02 | `page/repayment_phase_details.rs` mit 3-Tab-Layout (Basics, Entries, Export); Router `/repayment-phases/:id` | WIRED | — |
| UI-03 | Shared Component `component/repayment_entry_list.rs` mit 7 Spalten, Multi-Select, Status-Filter, Sortierung | WIRED | — |
| UI-04 | `AddEntryModal` inline in `repayment_phase_details.rs` mit MemberSearch + Vorbefüllung via Member.current_shares | WIRED | — |
| UI-05 | `PaidOutConfirmModal` inline in `repayment_phase_details.rs` (D-15 Sequential-Loop, D-16 Warntext: irreversibel + auditiert + reduziert current_shares) | WIRED | — |
| UI-06 | `repayment_entry_list.rs:280 on_letter_request` + `page/repayment_phase_details.rs:247 on_mail_request → build_mail_redirect_url → window.location.set_href("/mail?from=repayment&phase_id=&members=")`; `mail_page.rs::parse_mail_query` parst Query-Params; `selected_template_id`-Signal liefert template_id (Issue #2 BLOCKER-Fix) | WIRED (code) | WARNING: 3 UAT-Checkpoints in 12-HUMAN-UAT.md noch nicht durch Vorstand verifiziert (non-admin Login-Session erforderlich) |
| BRIEF-01 | `repayment_letter::generate_letters` REST (Phase 13-05) → Service erzeugt Bundle-PDF + audited MemberDocument-create pro Member → Frontend `api::generate_repayment_letters` (api.rs:1970) → Bulk-Letter-Button (Plan 13-06) mit `<a download>`-Trick + revoke_object_url | WIRED | — |

## Requirements mit hauptsächlich Single-Phase-Scope

Keine. Alle 30 Requirements (29 implementiert + 1 deferred) haben mindestens einen Cross-Phase-Touchpoint via DAO→Service→REST→Frontend. EXPO-04 ist explizit deferred (D-12) und benötigt keine Wiring-Validierung.

## Audit-Chain-Integrität

Single-Hash-Chain-Invariante verifiziert:
- `audit_log_dao: Arc<AuditLogDao>` wird EINMAL in `RestStateImpl::new()` konstruiert und via `.clone()` an alle audited Services UND den Mail-Worker geteilt
- Phase 10 Plan 07 dokumentiert die Persistierung von 6 DAOs (member_document, repayment_phase, repayment_entry, mail_template, transaction, audit_log) als RestStateImpl-Fields
- E2E-Tests `test_mark_paid_out_happy_path_cascade` und `test_bulk_repayment_mail_audit_chain_remains_valid` assertieren `/api/audit/verify == valid: true` nach allen Cascade-Operationen
- Export-Endpoint (Phase 11) ist read-only — compile-time-gated via `no_audit_macros_used` Test

## Findings

### BLOCKER
None.

### WARNING (1)

**W1** — UI-06 UAT-Checkpoints offen (non-blocker)
- **Path:** `.planning/phases/12-frontend-component-first/12-HUMAN-UAT.md` Sektion L (Auth-Gate)
- **Issue:** 3 Checklisten-Items zur Verifikation des Non-Admin → AccessDeniedPage-Pfads benötigen eine echte non-admin Login-Session (mock_auth-Test-Harness injiziert immer admin; OIDC-Tests wären erforderlich)
- **Affected REQ:** UI-06 (Auth-Pfad-Verifikation; Code-Pfade selbst sind WIRED)
- **Code-Beweis vorhanden:** 3 `RequirePrivilege { fallback: AccessDeniedPage }`-Blöcke in `repayment_phases.rs:69`, `repayment_phase_details.rs:164`, `mail_page.rs:216`
- **Service-/REST-Layer-Gate:** vollständig unit-getestet (test_generate_permission_denied_returns_403 + test_map_letter_error_permission_denied_to_403)
- **Empfehlung:** UAT-Walkthrough mit DEVUSER-Override auf staging oder Annahme als Tech-Debt mit Hinweis auf Test-Coverage in den unteren Layern

### Aggregierte Tabelle

| Kategorie | Anzahl | Davon WIRED | Davon BROKEN | Notes |
|-----------|--------|-------------|--------------|-------|
| Cross-Phase-Wiring-Punkte | 9 | 9 | 0 | DAO↔Service, Service↔REST, REST↔Frontend, Worker↔Audit |
| REST-Endpunkte mit Frontend-Consumer | 11 | 11 | 0 | Alle Backend-Routes haben passenden Frontend-Aufruf |
| E2E-Flows | 7 | 7 | 0 | 5 Backend (Flow A) + 2 Frontend (Flow B) |
| Audit-Macros-Konsistenz | 3 audited Services + Worker | 4 | 0 | RepaymentPhase, RepaymentEntry, RepaymentLetter, MailWorker — alle teilen `audit_log_dao` |
| Permission-Gates (Vorstand-only) | 11 REST-Endpoints | 11 | 0 | Globaler OIDC + per-Handler `check_permission("admin")` |
| Orphan-Exports | 0 | n/a | n/a | Alle Module sind aus mindestens einem Konsumenten heraus referenziert |
| Unprotected sensitive Pages | 0 | n/a | n/a | Alle 3 Repayment-/Mail-Pages haben RequirePrivilege-Gate |

## Conclusion

**Status: passed**

Phasen 07–13 sind end-to-end verdrahtet. Der gesamte Flow A (Anteils-Rückzahlung-Lebenszyklus) ist sowohl im Backend als auch im Frontend funktional, und Flow B (Frontend Read-Path) ist über sämtliche Pages und Components abgedeckt. Die Audit-Chain bleibt durch das `audit_log_dao.clone()`-Pattern über alle 4 audited Schreib-Vorgänge (RepaymentPhase, RepaymentEntry/Cascade, RepaymentLetter, Mail-Worker MemberDocument) konsistent. Die einzige offene Position ist die UAT-Verifikation der Auth-Gate-Code-Pfade (UI-06), die im Code grep-verifiziert sind und auf Service-/REST-Layer-Ebene durch Unit-Tests abgesichert werden — die Verifikation der Frontend-Fallback-Anzeige hängt am externen Vorstands-Walkthrough mit non-admin OIDC-Session.

Empfehlung: **passed** for `update_roadmap` und Milestone-Archivierung; UAT-Restpunkte als minor Tech-Debt in `RETROSPECTIVE.md` festhalten, falls kein Walkthrough binnen 7 Tagen erfolgt.

---
*Audit completed: 2026-06-02*
*Auditor: cross-phase integration-checker*
