---
phase: quick-260614-b1t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/sqlite/20260614010000_mail_recipient_rendered_reconstructed.sql
  - genossi_mail/src/dao.rs
  - genossi_mail/src/dao_sqlite.rs
  - genossi_mail/src/template.rs
  - genossi_mail/src/worker.rs
  - genossi_mail/src/backfill.rs
  - genossi_mail/src/lib.rs
  - genossi_mail/src/service.rs
  - genossi_mail/src/inbox.rs
  - genossi_mail/src/rest.rs
  - genossi_bin/src/lib.rs
  - genossi_bin/src/main.rs
  - genossi-frontend/src/api.rs
  - genossi-frontend/src/component/mail_recipient_rendered_content.rs
  - genossi-frontend/src/i18n/mod.rs
  - genossi-frontend/src/i18n/de.rs
  - genossi-frontend/src/i18n/en.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "Beim Server-Start werden alle mail_recipients mit NULL rendered_subject UND rendered_body rückwirkend gerendert und befüllt"
    - "Rückwirkend befüllte Zeilen tragen rendered_reconstructed=true; vom Worker live gesendete Zeilen tragen rendered_reconstructed=false"
    - "Der Backfill ist idempotent: bereits gefüllte Zeilen werden nicht angefasst, fehlender Member/Render-Fehler überspringt die Zeile (bleibt NULL)"
    - "Die Render-Logik existiert nur einmal (eine Funktion), genutzt von Worker UND Backfill"
    - "Das Frontend zeigt ein deutlich sichtbares Badge 'nachträglich rekonstruiert' wenn rendered_reconstructed=true"
  artifacts:
    - path: "migrations/sqlite/20260614010000_mail_recipient_rendered_reconstructed.sql"
      provides: "rendered_reconstructed Spalte (NOT NULL DEFAULT 0) auf mail_recipients"
      contains: "rendered_reconstructed"
    - path: "genossi_mail/src/backfill.rs"
      provides: "run_rendered_backfill one-shot idempotenter Job"
      contains: "rendered_reconstructed"
    - path: "genossi_mail/src/template.rs"
      provides: "extrahierte wiederverwendbare Render-Funktion (Worker + Backfill)"
    - path: "genossi-frontend/src/component/mail_recipient_rendered_content.rs"
      provides: "Badge 'nachträglich rekonstruiert' bei rendered_reconstructed=true"
  key_links:
    - from: "genossi_bin/src/main.rs"
      to: "genossi_mail::backfill::run_rendered_backfill"
      via: "rest_state.start_rendered_backfill_worker() nach migrate, in Startup-Sequenz"
      pattern: "start_rendered_backfill"
    - from: "genossi_mail/src/worker.rs"
      to: "genossi_mail::template (extrahierte Render-Funktion)"
      via: "Aufruf statt inline-Render"
      pattern: "render_recipient_content|resolve_rendered"
    - from: "genossi_mail/src/backfill.rs"
      to: "genossi_mail::template (extrahierte Render-Funktion)"
      via: "Aufruf der gleichen Funktion"
      pattern: "render_recipient_content|resolve_rendered"
---

<objective>
Backfill: rendered_subject/rendered_body für bestehende mail_recipients-Zeilen rückwirkend rekonstruieren, mit Kennzeichnung `rendered_reconstructed`. Baut auf Quick-Task 260614-9zf auf (nullable Spalten + Worker-Persistenz, bereits committet).

Purpose: Vorstand sieht auch für historisch versandte Mails (vor 9zf) den tatsächlich gesendeten Betreff/Text — sichtbar markiert, dass es sich um eine nachträgliche Rekonstruktion handelt (nicht das byte-genaue Original aus dem Versand-Moment).

Output: Migration für `rendered_reconstructed`, extrahierte DRY-Render-Funktion (Worker + Backfill teilen sie), idempotenter one-shot Backfill-Job beim Server-Start, REST- und Frontend-Durchstich mit Badge.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260614-9zf-mail-versand-gerenderten-betreff-und-tex/260614-9zf-SUMMARY.md
@./CLAUDE.md
@./genossi-frontend/CLAUDE.md

<interfaces>
<!-- Aus der Codebase extrahiert — Executor nutzt diese direkt, keine Exploration nötig. -->

genossi_mail/src/dao.rs — MailRecipient (Quick-9zf-Stand, rendered_subject/rendered_body bereits vorhanden):
```rust
pub struct MailRecipient {
    pub id: Uuid,
    pub created: time::PrimitiveDateTime,
    pub deleted: Option<time::PrimitiveDateTime>,
    pub version: Uuid,
    pub mail_job_id: Uuid,
    pub to_address: Arc<str>,
    pub member_id: Option<Uuid>,
    pub status: Arc<str>,
    pub error: Option<Arc<str>>,
    pub sent_at: Option<time::PrimitiveDateTime>,
    pub message_id: Option<Arc<str>>,
    pub rendered_subject: Option<Arc<str>>,
    pub rendered_body: Option<Arc<str>>,
    // NEU in diesem Task:
    // pub rendered_reconstructed: bool,
}

pub trait MailRecipientDao: Send + Sync + 'static {
    async fn create(&self, recipient: &MailRecipient) -> Result<(), MailDaoError>;
    async fn find_by_job_id(&self, job_id: Uuid) -> Result<Arc<[MailRecipient]>, MailDaoError>;
    async fn next_pending(&self) -> Result<Option<MailRecipient>, MailDaoError>;
    async fn update(&self, recipient: &MailRecipient) -> Result<(), MailDaoError>;
    async fn find_sent_member_ids_by_job_id(&self, job_id: Uuid) -> Result<Arc<[Uuid]>, MailDaoError>;
    // NEU: async fn find_recipients_without_rendered(&self) -> Result<Arc<[MailRecipient]>, MailDaoError>;
}

pub trait MailJobDao: Send + Sync + 'static {
    async fn find_by_id(&self, id: Uuid) -> Result<MailJob, MailDaoError>;
    // ...
}
```

genossi_mail/src/template.rs — vorhandene Render-Bausteine (alle pub):
```rust
pub trait MemberResolver: Send + Sync + 'static {
    async fn find_member_by_id(&self, id: Uuid) -> Result<Option<MemberEntity>, MailServiceError>;
}
pub fn member_to_template_context(entity: &MemberEntity) -> minijinja::Value;
pub fn merge_repayment_context(base: Value, payout_amount: &str, share_count: i32, share_value: &str, fiscal_year: i32) -> Value;
pub fn render_template(template_str: &str, context: &Value) -> Result<String, TemplateError>;
```

genossi_mail/src/worker.rs — inline Render (Zeilen ~378-577) den es zu extrahieren gilt:
- Wenn `recipient.member_id == Some(id)`: member laden via MemberResolver; bei None/Err → Fehler.
  - `ctx = member_to_template_context(&member)`
  - Falls `job.repayment_phase_id == Some(phase_id)`: tx öffnen, RepaymentPhaseDao::find_by_id, RepaymentEntryDao::find_by_phase_id, `share_value_str = format!("{},{:02}", phase.share_value/100, phase.share_value%100)`, `RepaymentContextResolver::aggregate(&phase, &entries, member.id)`:
    - Ok(rc) → `ctx = merge_repayment_context(ctx, &rc.payout_amount, rc.share_count, &share_value_str, rc.fiscal_year)`
    - Err(ServiceError::EntityNotFound(_)) → ctx unverändert lassen (strict-env fail downstream ist gewollt)
    - Err(other) → Render-Fehler
  - `subject = render_template(&job.subject, &ctx)?`, `body = render_template(&job.body, &ctx)?`
- Wenn `member_id == None`: `(job.subject.to_string(), job.body.to_string())` (plain passthrough).

genossi_bin/src/lib.rs — Wiring-Bausteine (PoolMemberResolver + worker deps existieren bereits):
```rust
pub struct PoolMemberResolver { pool: Arc<SqlitePool> } // impl genossi_mail::template::MemberResolver
// In RestStateImpl bereits als Felder vorhanden (siehe start_mail_worker, Zeile ~1456):
//   worker_job_dao, worker_recipient_dao, repayment_entry_dao, repayment_phase_dao,
//   transaction_dao, repayment_context_resolver, pool
```

genossi_bin/src/main.rs — Startup-Sequenz (Backfill nach migrate, neben anderen Workern wiren):
```rust
sqlx::migrate!("../migrations/sqlite").run(&*pool).await.expect(...);
let rest_state = RestStateImpl::new(pool);
// ... initialize_audit_snapshot, provision_defaults ...
rest_state.start_mail_worker();
rest_state.start_attachment_backfill_worker();   // existierendes one-shot Backfill-Precedent
// NEU: rest_state.start_rendered_backfill_worker();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend — Migration, DAO-Flag, DRY-Render-Extraktion, Worker-Flag, Backfill-Job, Startup-Wiring</name>
  <files>migrations/sqlite/20260614010000_mail_recipient_rendered_reconstructed.sql, genossi_mail/src/dao.rs, genossi_mail/src/dao_sqlite.rs, genossi_mail/src/template.rs, genossi_mail/src/worker.rs, genossi_mail/src/backfill.rs, genossi_mail/src/lib.rs, genossi_mail/src/service.rs, genossi_mail/src/inbox.rs, genossi_bin/src/lib.rs, genossi_bin/src/main.rs</files>
  <behavior>
    - Render-Extraktion (template.rs): `resolve_rendered_content(recipient, job, member_resolver, repayment_phase_dao, repayment_entry_dao, transaction_dao, repayment_context_resolver) -> Result<(String, String), RenderOutcome>` (oder äquivalente Signatur) liefert für member_id=Some den gemergten Member+Repayment-Render, für member_id=None den plain `(job.subject, job.body)`. Test: gleicher Input → identisches Ergebnis wie alter Worker-Pfad (member-only, repayment-merge, plain-passthrough, fehlender Member → Fehler-Variante).
    - DAO-Roundtrip: neuer MailRecipient mit rendered_reconstructed wird via create+find_by_job_id korrekt zurückgelesen; update persistiert das Flag (true und false). next_pending mapped das Flag.
    - find_recipients_without_rendered: liefert nur Zeilen wo rendered_subject IS NULL AND rendered_body IS NULL AND deleted IS NULL (auch status='failed'); Zeilen mit gesetztem rendered_* werden NICHT geliefert.
    - Backfill (run_rendered_backfill): befüllt NULL-Zeilen mit gerendertem subject/body + rendered_reconstructed=true; bereits gefüllte Zeilen bleiben unangetastet (Idempotenz, zweiter Lauf no-op); fehlender Member ODER Render-Fehler → Zeile bleibt NULL (übersprungen, kein Flag).
  </behavior>
  <action>
**1. Migration** `migrations/sqlite/20260614010000_mail_recipient_rendered_reconstructed.sql`:
`ALTER TABLE mail_recipients ADD COLUMN rendered_reconstructed INTEGER NOT NULL DEFAULT 0;`
Kommentar-Header wie in 20260614000000_*.sql (kein down-migration, SQLite-Konvention). Nummerierung 20260614010000 liegt nach der 9zf-Migration 20260614000000.

**2. DAO** (`genossi_mail/src/dao.rs`): Feld `pub rendered_reconstructed: bool` an `MailRecipient` anhängen (nach rendered_body). Neue Trait-Methode an `MailRecipientDao`: `async fn find_recipients_without_rendered(&self) -> Result<Arc<[MailRecipient]>, MailDaoError>;` — `#[automock]` generiert das Mock automatisch mit.

**3. DAO-SQLite** (`genossi_mail/src/dao_sqlite.rs`):
- `MailRecipientDb` struct: Feld `rendered_reconstructed: i64` ergänzen; im `TryFrom` `rendered_reconstructed: db.rendered_reconstructed != 0`.
- `create`: Spalte + `?`-Bind in INSERT ergänzen (binde `if recipient.rendered_reconstructed {1i64} else {0i64}` statt der bisherigen NULL-Default-Stelle — beachte: aktuelles INSERT hat rendered_subject/body als literale NULL und KEIN sent_at-Wert nach message_id; passe Spaltenliste + VALUES + bind-Reihenfolge sauber an, ergänze rendered_reconstructed).
- `find_by_job_id`, `next_pending` (inkl. `r.`-Prefix-Variante): `rendered_reconstructed` zur SELECT-Spaltenliste hinzufügen.
- `update`: `rendered_reconstructed = ?` in SET-Klausel + Bind (`if recipient.rendered_reconstructed {1i64} else {0i64}`) ergänzen.
- Neue Impl `find_recipients_without_rendered`: `SELECT ... rendered_reconstructed FROM mail_recipients WHERE rendered_subject IS NULL AND rendered_body IS NULL AND deleted IS NULL ORDER BY created ASC` → mappen wie find_by_job_id.
- Test-Schema `setup_db` (CREATE TABLE mail_recipients): Spalte `rendered_reconstructed INTEGER NOT NULL DEFAULT 0` ergänzen.
- `sample_recipient`: `rendered_reconstructed: false` ergänzen.
- Neue Tests: `test_recipient_roundtrip_rendered_reconstructed_flag` (create mit false → find liefert false; update auf true → find liefert true), `test_find_recipients_without_rendered_filters_filled_rows` (zwei Zeilen: eine NULL, eine mit gesetztem rendered_* → nur die NULL-Zeile kommt zurück).

**4. Render-Extraktion** (`genossi_mail/src/template.rs`): Extrahiere die inline Render-Logik aus worker.rs (~378-577) in EINE wiederverwendbare async-Funktion, z.B. `resolve_rendered_content`. Inputs: `&MailRecipient`, `&MailJob`, `&dyn`/generics für `MemberResolver`, `RepaymentPhaseDao`, `RepaymentEntryDao`, `TransactionDao`, `RepaymentContextResolver` (gleiche Trait-Bounds wie worker.rs `start_mail_worker`, Transaction=MD::Transaction-Pattern → hier generisch über gemeinsamen Transaction-Typ). Output: `Result<(String, String), RenderFailure>` wobei RenderFailure die Fehlerursache als String trägt (Worker mappt sie weiter auf `mark_recipient_failed`). WICHTIG: byte-für-byte identisches Verhalten zum alten Pfad (member-only render, repayment-merge inkl. EntityNotFound→ctx-unverändert, plain-passthrough bei member_id=None, share_value_str-Format). NICHT die `tokio::time::sleep`/`mark_recipient_failed`-Schleifensteuerung mit extrahieren — nur die reine Rendering-Pipeline; Fehler werden als Err zurückgegeben.
Falls die volle generische Signatur in template.rs den Import-Graph aufbläht: Funktion alternativ in neues Modul `genossi_mail/src/render.rs` legen (in lib.rs registrieren). Entscheidung dem Executor überlassen; Bedingung: NUR EINE Definition, von Worker UND Backfill genutzt.
Tests: `test_resolve_rendered_content_member_only`, `_repayment_merge`, `_plain_passthrough_no_member`, `_missing_member_returns_err`.

**5. Worker** (`genossi_mail/src/worker.rs`): Ersetze den inline-Render-Block (~378-577) durch Aufruf der extrahierten Funktion. Bei Err → `mark_recipient_failed(...)` + `continue` mit der zurückgegebenen Fehlermeldung (gleiche tracing/interval-Semantik wie bisher). Zusätzlich: im `updated_recipient` nach Versand `updated_recipient.rendered_reconstructed = false;` setzen (explizit Original-Kennzeichnung, neben den bereits gesetzten rendered_subject/rendered_body Zeilen ~622-623).

**6. Backfill** (`genossi_mail/src/backfill.rs`, neu; in `genossi_mail/src/lib.rs` als `pub mod backfill;` registrieren): `pub async fn run_rendered_backfill<...>(recipient_dao, job_dao, member_resolver, repayment_phase_dao, repayment_entry_dao, transaction_dao, repayment_context_resolver)` — one-shot (kein loop):
  - `recipient_dao.find_recipients_without_rendered()` holen.
  - Pro Zeile: `job = job_dao.find_by_id(r.mail_job_id)` (bei Err → skip + tracing::warn, Zeile bleibt NULL).
  - `resolve_rendered_content(&r, &job, ...)` aufrufen:
    - Ok((subject, body)) → `updated = r.clone(); updated.version = Uuid::new_v4(); updated.rendered_subject = Some(Arc::from(subject)); updated.rendered_body = Some(Arc::from(body)); updated.rendered_reconstructed = true;` → `recipient_dao.update(&updated)`.
    - Err(_) → skip (tracing::warn, Zeile bleibt NULL → nächster Start versucht erneut).
  - tracing::info am Ende mit Zähler (z.B. "rendered backfill: X von Y befüllt, Z übersprungen"). Idempotenz folgt automatisch, weil befüllte Zeilen beim nächsten Lauf nicht mehr von find_recipients_without_rendered geliefert werden.
  Tests (in backfill.rs, mit Mock-DAOs via mockall + MockMemberResolver): `test_backfill_fills_null_rows_and_sets_flag_true`, `test_backfill_skips_missing_member_leaves_null` (MemberResolver liefert Ok(None) → update wird NICHT aufgerufen für diese Zeile), `test_backfill_idempotent_does_not_touch_filled_rows` (find_recipients_without_rendered liefert leere Liste → keine update-Aufrufe). Nutze `MockMailRecipientDao`/`MockMailJobDao` mit `.expect_update()` Erwartungen zum Asserten.

**7. Konstruktoren** (`genossi_mail/src/service.rs` Zeilen ~335/725/740, `genossi_mail/src/inbox.rs` ~693): jede `MailRecipient { ... }`-Konstruktion um `rendered_reconstructed: false` ergänzen.

**8. Startup-Wiring** (`genossi_bin/src/lib.rs`): neue Methode `start_rendered_backfill_worker(&self)` analog `start_attachment_backfill_worker` — clont die bereits vorhandenen Felder (`worker_recipient_dao` bzw. neuer Arc, `worker_job_dao`, `PoolMemberResolver::new(self.pool.clone())`, `repayment_phase_dao`, `repayment_entry_dao`, `transaction_dao`, `repayment_context_resolver`), `tokio::spawn` → `genossi_mail::backfill::run_rendered_backfill(...)`. Falls die `worker_*`-DAO-Felder schon vom mail_worker konsumiert/moved sind: eigene `Arc::new(MailRecipientDao::new(self.pool.clone()))` / `MailJobDao::new(...)` erzeugen (Pattern wie inbox_*/worker_* Felder zeigen).
`genossi_bin/src/main.rs`: nach `start_mail_worker()`/neben `start_attachment_backfill_worker()` aufrufen: `rest_state.start_rendered_backfill_worker(); tracing::info!("Rendered backfill worker spawned");`. Reihenfolge: nach `sqlx::migrate!` (LOCKED Entscheidung 1).
  </action>
  <verify>
    <automated>cargo test -p genossi_mail</automated>
    <automated>cargo build -p genossi_bin</automated>
  </verify>
  <done>
- Migration 20260614010000 existiert, fügt rendered_reconstructed (NOT NULL DEFAULT 0) hinzu.
- `cargo test -p genossi_mail` grün inkl. neuer Render-Extraktion-, DAO-Roundtrip-, find_recipients_without_rendered- und Backfill-Tests (fill+flag, skip-missing-member, idempotent).
- Render-Logik existiert nur EINMAL, Worker UND Backfill rufen sie auf (kein dupliziertes member_to_template_context/merge/render_template außerhalb der extrahierten Funktion).
- Worker setzt rendered_reconstructed=false beim Live-Versand.
- `cargo build -p genossi_bin` grün; start_rendered_backfill_worker() in main.rs nach migrate gewired.
- Bestehende Mail-Tests brechen nicht.
  </done>
</task>

<task type="auto">
  <name>Task 2: REST — MailRecipientTO um rendered_reconstructed erweitern</name>
  <files>genossi_mail/src/rest.rs</files>
  <action>
`MailRecipientTO` (struct ~Zeile 106): Feld `pub rendered_reconstructed: bool` ergänzen (nach rendered_body). KEIN skip_serializing_if — das Flag immer serialisieren (default-false bei alten Zeilen ist über die DB-Migration NOT NULL DEFAULT 0 garantiert). `#[serde(default)]` für Eingangs-Robustheit setzen.
`impl From<&MailRecipient> for MailRecipientTO` (~Zeile 284): `rendered_reconstructed: r.rendered_reconstructed,` ergänzen.
Falls ein bestehender Konstruktions- oder Test-Fall im rest.rs ein MailRecipientTO literal baut: Feld ergänzen.
  </action>
  <verify>
    <automated>cargo test -p genossi_mail</automated>
  </verify>
  <done>
- MailRecipientTO trägt rendered_reconstructed: bool; From<&MailRecipient> mappt es.
- `cargo test -p genossi_mail` grün.
  </done>
</task>

<task type="auto">
  <name>Task 3: Frontend — api-Feld + Badge "nachträglich rekonstruiert" in MailRecipientRenderedContent</name>
  <files>genossi-frontend/src/api.rs, genossi-frontend/src/component/mail_recipient_rendered_content.rs, genossi-frontend/src/i18n/mod.rs, genossi-frontend/src/i18n/de.rs, genossi-frontend/src/i18n/en.rs, genossi-frontend/src/page/mail_page.rs</files>
  <action>
**1. api.rs** (`MailRecipientTO` ~Zeile 840): Feld `#[serde(default)] pub rendered_reconstructed: bool` ergänzen (default=false für Backward-Compat mit alten Responses). Bestehende serde-default-Tests (~2511) brechen nicht (default greift).

**2. i18n**: neuer Key `MailRenderedReconstructed` in `mod.rs` (enum `Key`, neben MailRenderedContent). de.rs: `Key::MailRenderedReconstructed => "Nachträglich rekonstruiert".into(),`. en.rs: `Key::MailRenderedReconstructed => "Reconstructed afterwards".into(),`. Beide Locales PFLICHT.

**3. Component** (`mail_recipient_rendered_content.rs`): Prop `rendered_reconstructed: bool` an `MailRecipientRenderedContent` ergänzen. Wenn `true` UND (subject ODER body vorhanden): ein deutlich sichtbares Badge im Heading-Bereich rendern, z.B. amber: `span { class: "ml-2 inline-flex items-center rounded bg-amber-100 px-2 py-0.5 text-xs font-medium text-amber-800", "{i18n.t(Key::MailRenderedReconstructed)}" }`. Component-First beibehalten (nur diese Komponente anfassen, keine inline-Duplikate in mail_page.rs). Early-return (beide None) bleibt unverändert.

**4. mail_page.rs**: an BEIDEN Aufrufstellen von `MailRecipientRenderedContent` (~Zeile 757 und ~936) `rendered_reconstructed: r.rendered_reconstructed,` als Prop ergänzen.
  </action>
  <verify>
    <automated>cargo test --manifest-path genossi-frontend/Cargo.toml</automated>
    <automated>cargo check --manifest-path genossi-frontend/Cargo.toml</automated>
  </verify>
  <done>
- api::MailRecipientTO trägt rendered_reconstructed (serde default=false).
- MailRenderedReconstructed in mod.rs + de.rs + en.rs vorhanden.
- Badge wird bei rendered_reconstructed=true sichtbar gerendert; beide Aufrufstellen übergeben das Prop.
- `cargo check`/`cargo test` der Frontend-Manifest grün; bestehende serde-default-Tests passieren.
  </done>
</task>

</tasks>

<verification>
- `cargo test -p genossi_mail` grün (Backend inkl. neuer Tests).
- `cargo build -p genossi_bin` grün (Startup-Wiring kompiliert).
- `cargo check --manifest-path genossi-frontend/Cargo.toml` grün.
- Render-Logik nur einmal definiert (grep: kein zweiter `merge_repayment_context`/`render_template`-Aggregationsblock außerhalb der extrahierten Funktion in worker.rs).
- Backfill-Idempotenz + skip-missing-member durch Unit-Tests bewiesen.
</verification>

<success_criteria>
- Server-Start löst nach Migrations einen one-shot Backfill aus, der NULL-rendered_*-Zeilen rückwirkend rendert und rendered_reconstructed=true setzt.
- Worker setzt beim Live-Versand rendered_reconstructed=false.
- Backfill ist idempotent und überspringt Zeilen ohne ladbaren Member oder mit Render-Fehler (bleiben NULL).
- Eine einzige Render-Funktion, geteilt von Worker und Backfill (DRY).
- REST + Frontend führen das Flag durch; Frontend zeigt ein sichtbares Badge bei rekonstruierten Zeilen.
- Alle bestehenden Mail- und Frontend-Tests bleiben grün.
</success_criteria>

<output>
After completion, create `.planning/quick/260614-b1t-backfill-rendered-subject-rendered-body-/260614-b1t-SUMMARY.md`
</output>
