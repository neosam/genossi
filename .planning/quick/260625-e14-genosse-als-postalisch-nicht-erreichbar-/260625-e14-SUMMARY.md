---
phase: quick-260625-e14
plan: 01
subsystem: database
tags: [member, postal-status, enum, audit, sqlite, dioxus, i18n]

requires:
  - phase: quick-260607-mw9
    provides: "account_holder field + APPENDED-AT-END audit_fields pattern (Vorbild für append-Disziplin)"
provides:
  - "PostalStatus enum (Erreichbar default / Unzustellbar) auf MemberEntity, durch alle Layer gespiegelt"
  - "postal_status als letztes audit_fields()-Element (Hashchain-stabil)"
  - "Migration 20260625000000_add_postal_status_to_member.sql (TEXT DEFAULT 'Erreichbar')"
  - "PostalStatusTO in genossi_rest_types und genossi-frontend/rest-types"
  - "Wiederverwendbarer MemberPostalStatusSelect-Component + Member-Detail-Wiring + i18n de/en"
affects: [versand-flows, repayment-letter, gv-einladungen, member-detail-ui]

tech-stack:
  added: []
  patterns:
    - "Enum-statt-Bool für erweiterbare Status-Felder (PostalStatus spiegelt MemberStatus 1:1)"
    - "Neue audit_fields() IMMER am Ende anhängen (Hashchain-Stabilität)"
    - "Component-First Frontend-Select statt inline-RSX-Duplikat"

key-files:
  created:
    - migrations/sqlite/20260625000000_add_postal_status_to_member.sql
    - genossi-frontend/src/component/member_postal_status_select.rs
  modified:
    - genossi_dao/src/member.rs
    - genossi_dao_impl_sqlite/src/member.rs
    - genossi_service/src/member.rs
    - genossi_rest_types/src/lib.rs
    - genossi-frontend/rest-types/src/lib.rs
    - genossi-frontend/src/page/member_details.rs
    - genossi-frontend/src/i18n/{mod,de,en}.rs

key-decisions:
  - "PostalStatus als erweiterbares Enum (nicht Bool), Default Erreichbar — spätere Gründe (Umgezogen/Verstorben) ohne erneute Migration ergänzbar"
  - "postal_status als letztes audit_fields()-Element; account_holder rückt auf vorletzte Position — bestehende Audit-Hashes bleiben gültig"
  - "Versand-Flow-Integration ausgeklammert (separater Folge-Task); repayment_*/mail/assembly nur um Default-Feld ergänzt, keine Filter-/Send-Logik"

patterns-established:
  - "Append-only audit_fields(): test_auditable_postal_status_appended_at_end ersetzt den account_holder-Last-Test"
  - "PostalStatusTO mit all()/as_str()/from_str()/display_name()/Default analog MemberStatusTO in beiden rest-types Crates"

requirements-completed: [QUICK-260625-e14]

duration: 35min
completed: 2026-06-25
---

# Quick 260625-e14: Member postalischer Status Summary

**Erweiterbares PostalStatus-Enum (Erreichbar/Unzustellbar) auf Member durch alle Layer (DAO → SQLite → Service → REST-Types → Frontend), mit Audit-Hashchain-Integration, Migration und wiederverwendbarem Dioxus-Select-Component.**

## Performance

- **Duration:** ~35 min
- **Tasks:** 3
- **Files modified:** 37 (+2 created: Migration, Component)

## Accomplishments
- `PostalStatus`-Enum (Default `Erreichbar`, Variante `Unzustellbar`) auf `MemberEntity`, vollständig durch alle Layer gespiegelt
- `postal_status` als **letztes** `audit_fields()`-Element → Statuswechsel landen automatisch via bestehendem `audited_update!`-Pfad in der Hashchain, ohne bestehende Hashes zu invalidieren
- SQLite create/read/update-Mapping (positionale FromRow-Spalte nach `account_holder`) + Migration `TEXT DEFAULT 'Erreichbar'`
- `PostalStatusTO` in `genossi_rest_types` (mit From-Impls, OpenAPI-Schema) und `genossi-frontend/rest-types` (mit `all/as_str/from_str/display_name/Default`)
- Component-First: neuer `MemberPostalStatusSelect` auf der Member-Detail-Seite, schreibt in `member.postal_status`; i18n-Keys in de + en
- Neue Tests grün: DAO-Roundtrip/audit (len==23, last==postal_status), Service Member↔Entity-Roundtrip, REST-Types MemberTO↔Member-Roundtrip + serde-default, Frontend rest-types PostalStatusTO-Roundtrip

## Task Commits

VCS-Hinweis: Dieses Projekt ist ein jj-Repo. Per Auftrag wurden KEINE Commits durch den Executor gemacht — das Committen übernimmt der Orchestrator via jj. Logisch atomare Tasks:

1. **Task 1: Backend-Datenmodell (Enum, DAO-Feld, SQLite-Mapping, Migration, Audit)** — `feat` (uncommitted)
2. **Task 2: Service + REST-Types durchschleifen, alle literalen Konstruktionen reparieren, Roundtrip-Tests** — `feat` (uncommitted)
3. **Task 3: Frontend PostalStatusSelect-Component, member_details-Wiring, i18n, rest-types-Tests** — `feat` (uncommitted)

## Files Created/Modified
- `migrations/sqlite/20260625000000_add_postal_status_to_member.sql` — neue Spalte `postal_status TEXT DEFAULT 'Erreichbar'`
- `genossi_dao/src/member.rs` — `PostalStatus`-Enum, Feld auf `MemberEntity`, audit_fields-Eintrag (last), Tests
- `genossi_dao_impl_sqlite/src/member.rs` — MemberDb-Spalte, TryFrom, dump_all/create/update Spalten-Mapping
- `genossi_service/src/member.rs` — Feld auf `Member`, beide From-Impls, Roundtrip-Tests
- `genossi_rest_types/src/lib.rs` — `PostalStatusTO`, From-Impls, MemberTO-Feld, MemberTO↔Member-Maps + Tests
- `genossi_rest/src/member.rs` — `PostalStatusTO` in OpenAPI `components(schemas(...))`
- `genossi-frontend/rest-types/src/lib.rs` — Frontend `PostalStatusTO`, MemberTO-Feld + Tests
- `genossi-frontend/src/component/member_postal_status_select.rs` — neuer wiederverwendbarer Select
- `genossi-frontend/src/component/mod.rs` — Modul-Registrierung + Re-Export
- `genossi-frontend/src/page/member_details.rs` — Import, Default-Feld, Select-Wiring
- `genossi-frontend/src/i18n/{mod,de,en}.rs` — `MemberPostalStatus*`-Keys
- Default-Feld-Ergänzung (KEINE Logik): `genossi_service_impl/{application,member,member_import,membership_adjust,assembly,member_action,pdf_generation,repayment_entry,repayment_export,repayment_letter,repayment_phase,validation}.rs`, `genossi_mail/{backfill,render,rest,template}.rs`, `genossi_rest/src/dev.rs`, `genossi_bin/tests/{e2e_tests,membership_adjust_e2e,repayment_letter_e2e,transfer_recipients_e2e}.rs`, Frontend `component/{member_search,membership_adjust_modal,repayment_entry_list}.rs`

## Decisions Made
- Enum statt Bool (erweiterbar), Default `Erreichbar` — wie in CONTEXT/Plan festgelegt
- `postal_status` ans Ende von `audit_fields()`; alter Test `test_auditable_account_holder_appended_at_end` durch `test_auditable_postal_status_appended_at_end` ersetzt (prüft zusätzlich account_holder auf vorletzter Position)
- `#[serde(default)]` auf `postal_status` in MemberTO (beide Crates) für Abwärtskompatibilität älterer Clients

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] OpenAPI-Schema-Registrierung für PostalStatusTO**
- **Found during:** Task 2
- **Issue:** Plan nannte die Prüfung optional; `MemberStatusTO` ist in `genossi_rest/src/member.rs` `components(schemas(...))` registriert — ohne `PostalStatusTO` wäre das Schema in Swagger inkonsistent (MemberTO referenziert es).
- **Fix:** `genossi_rest_types::PostalStatusTO` neben `MemberStatusTO` in die schemas-Liste ergänzt.
- **Files modified:** genossi_rest/src/member.rs
- **Verification:** `cargo build` grün.

**2. [Rule 3 - Blocking] Mehr literale Konstruktions-Sites als im Plan gelistet**
- **Found during:** Task 2 & 3
- **Issue:** Über die im Plan genannten Dateien hinaus erzwang der Compiler `postal_status` u.a. in `genossi_rest/src/dev.rs`, `genossi_service_impl/{member_action,pdf_generation,repayment_export}.rs`, `genossi_bin/tests/{e2e_tests,repayment_letter_e2e,transfer_recipients_e2e}.rs`, Frontend `member_search.rs` und einer zweiten MemberTO-Test-Site in `genossi-frontend/rest-types/src/lib.rs`.
- **Fix:** In jeder Site das Default-Feld (`PostalStatus::Erreichbar` / `PostalStatusTO::Erreichbar`) ergänzt — KEINE Logikänderung. Import `PostalStatusTO` in `membership_adjust_e2e.rs` ergänzt.
- **Verification:** `cargo build --all-targets` grün; Frontend `cargo check --all-targets` grün.

---

**Total deviations:** 2 auto-fixed (beide Rule 3 - Blocking/Compile).
**Impact on plan:** Reine Compile-Notwendigkeiten, keine Logik- oder Scope-Erweiterung. Versand-Flow-Integration blieb wie gefordert ausgeklammert.

## Issues Encountered

**Pre-existing e2e-Failure (out of scope, NICHT durch diese Task verursacht):**
`genossi_bin::e2e_tests::test_mail_preview_repayment_no_entries_does_not_default_to_one` schlägt fehl
(`json["errors"].as_array()` panic — `errors` wird bei leerer Liste aus der PreviewResponse weggelassen).
`git diff HEAD genossi_mail/src/rest.rs` zeigt: einzige Änderung ist eine Zeile in einem `#[cfg(test)]`-Modul;
der Produktions-`/api/mail/preview`-Handler ist byte-identisch zu HEAD → Verhalten unverändert → Failure existiert
bereits auf HEAD. Dokumentiert in `deferred-items.md`. Alle übrigen 298 e2e-Tests grün.

## Test Results
- `cargo test -p genossi_dao member`: 42 passed (inkl. postal_status-Roundtrip, audit len==23, last==postal_status)
- `cargo test -p genossi_service -p genossi_service_impl -p genossi_rest_types -p genossi_dao_impl_sqlite`: alle grün (77/63/44/93/416)
- `cargo build --all-targets`: grün
- Frontend (separates Workspace): `cargo test -p rest-types` 22 passed; `cargo check --all-targets` grün (nur pre-existing unused-import Warnings)
- sqlx: DAO nutzt `sqlx::query`/`query_as` (runtime, keine compile-checked Makros) → `cargo sqlx prepare` nicht erforderlich (Build bestätigt)

## Known Stubs
None.

## Next Phase Readiness
- Datenmodell + Audit + UI für postalischen Status produktionsbereit.
- **Folge-Task (ausgeklammert):** Berücksichtigung in Versand-Flows (RepaymentLetter, Brief-Bulk-Mail, GV-Einladungen) als Pre-Flight-Warnung (nicht hart filtern), gemäß CONTEXT-Richtungsentscheidung.

## Self-Check: PASSED
- Created files verified on disk: migration, MemberPostalStatusSelect component, SUMMARY.
- No VCS commits performed (jj repo — orchestrator commits via jj per task instructions).

---
*Phase: quick-260625-e14*
*Completed: 2026-06-25*
