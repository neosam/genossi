---
phase: quick-260625-e14
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/sqlite/20260625000000_add_postal_status_to_member.sql
  - genossi_dao/src/member.rs
  - genossi_dao_impl_sqlite/src/member.rs
  - genossi_service/src/member.rs
  - genossi_service_impl/src/member.rs
  - genossi_service_impl/src/membership_adjust.rs
  - genossi_service_impl/src/assembly.rs
  - genossi_service_impl/src/repayment_entry.rs
  - genossi_service_impl/src/repayment_letter.rs
  - genossi_service_impl/src/repayment_phase.rs
  - genossi_mail/src/backfill.rs
  - genossi_mail/src/render.rs
  - genossi_mail/src/rest.rs
  - genossi_rest_types/src/lib.rs
  - genossi_bin/tests/membership_adjust_e2e.rs
  - genossi-frontend/rest-types/src/lib.rs
  - genossi-frontend/src/component/member_postal_status_select.rs
  - genossi-frontend/src/component/mod.rs
  - genossi-frontend/src/page/member_details.rs
  - genossi-frontend/src/component/membership_adjust_modal.rs
  - genossi-frontend/src/component/repayment_entry_list.rs
  - genossi-frontend/src/i18n/mod.rs
  - genossi-frontend/src/i18n/de.rs
  - genossi-frontend/src/i18n/en.rs
autonomous: true
requirements: [QUICK-260625-e14]
security_enforcement: false

must_haves:
  truths:
    - "Ein Member kann den postalischen Status Erreichbar oder Unzustellbar tragen"
    - "Bestandsdaten ohne Wert gelten als Erreichbar (Default)"
    - "Eine Statusänderung wird über audited_update! in der Audit-Hashchain protokolliert"
    - "Der postalische Status überlebt einen DAO-Roundtrip (write -> read)"
    - "Auf der Member-Detail-Seite kann der postalische Status per Select gewählt und gespeichert werden"
  artifacts:
    - path: "migrations/sqlite/20260625000000_add_postal_status_to_member.sql"
      provides: "Neue Spalte postal_status TEXT DEFAULT 'Erreichbar'"
      contains: "ALTER TABLE member ADD COLUMN postal_status"
    - path: "genossi_dao/src/member.rs"
      provides: "PostalStatus enum + postal_status Feld auf MemberEntity + audit_fields-Eintrag"
      contains: "enum PostalStatus"
    - path: "genossi_rest_types/src/lib.rs"
      provides: "PostalStatusTO + postal_status Feld auf MemberTO + From-Impls"
      contains: "PostalStatusTO"
    - path: "genossi-frontend/rest-types/src/lib.rs"
      provides: "Frontend PostalStatusTO mit all/as_str/from_str/display_name + postal_status auf MemberTO"
      contains: "PostalStatusTO"
    - path: "genossi-frontend/src/component/member_postal_status_select.rs"
      provides: "Wiederverwendbarer Select-Component für den postalischen Status"
      contains: "MemberPostalStatusSelect"
  key_links:
    - from: "genossi_service_impl/src/member.rs update()"
      to: "audited_update! / MemberEntity::audit_fields()"
      via: "item.into() -> MemberEntity, postal_status fließt in audit_fields"
      pattern: "audited_update!"
    - from: "genossi-frontend/src/page/member_details.rs"
      to: "MemberPostalStatusSelect -> member.write().postal_status"
      via: "Component onchange schreibt in das member-Signal"
      pattern: "MemberPostalStatusSelect"
    - from: "genossi_dao_impl_sqlite/src/member.rs"
      to: "member.postal_status Spalte"
      via: "SELECT/INSERT/UPDATE Spalten-Mapping"
      pattern: "postal_status"
---

<objective>
Mitglieder können als postalisch nicht erreichbar markiert werden. Neues erweiterbares
Status-Enum `PostalStatus` (Varianten `Erreichbar` als Default, `Unzustellbar`) auf der
Member-Entität, vollständig durch alle Layer gespiegelt (DAO, DAO-SQLite, Service,
REST-Types, Frontend-REST-Types, Frontend-UI, i18n), inklusive Audit-Integration und
Migration.

Purpose: Vorbereitung der papierlosen, fehlerarmen Versand-Workflows — markierte Mitglieder
können später (separater Folge-Task) in Versand-Flows gewarnt/gekennzeichnet werden. Diese
Task liefert NUR Datenmodell + Audit + Frontend.

Output: Migration, Backend-Enum + Feld durch alle Layer, Audit-Eintrag, REST-Serialisierung,
wiederverwendbarer Frontend-Select-Component, i18n-Keys, Tests.

AUSGEKLAMMERT (LOCKED CONTEXT, NICHT planen/umsetzen): Berücksichtigung in Versand-Flows
(RepaymentLetter, Brief-Bulk-Mail, GV-Einladungen). Das ist ein separater Folge-Task. Die in
diesem Plan gelisteten repayment_*/mail/assembly-Dateien werden NUR berührt, um neue
`postal_status:`-Felder in literalen Struct-Konstruktionen zu ergänzen, damit der Workspace
kompiliert — KEINE Versand-Logik anfassen.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/quick/260625-e14-genosse-als-postalisch-nicht-erreichbar-/260625-e14-CONTEXT.md

<interfaces>
<!-- Vorbild-Pattern: PostalStatus spiegelt EXAKT MemberStatus. Extrahiert aus der Codebase, -->
<!-- direkt verwendbar — keine weitere Codebase-Exploration nötig.                            -->

DAO-Enum-Vorbild (genossi_dao/src/member.rs:37-66) — PostalStatus muss diesem Muster folgen:
```rust
#[derive(Clone, Debug, PartialEq, Eq, Default)]
pub enum MemberStatus {
    #[default]
    Normal,
    FehlerhaftErfasst,
}
impl MemberStatus {
    pub fn as_str(&self) -> &'static str { /* match -> &'static str */ }
    pub fn from_str(s: &str) -> Result<Self, DaoError> { /* match, sonst DaoError::ParseError */ }
}
```

MemberEntity (genossi_dao/src/member.rs:69-100): Feld `status: MemberStatus` existiert.
HINWEIS Audit-Hashchain-Stabilität: Im account_holder-Kommentar (Zeile 92-96, 249-254) ist
dokumentiert, dass NEUE Felder am ENDE von `audit_fields()` angehängt werden müssen.
`account_holder` ist aktuell der letzte Eintrag. `postal_status` MUSS NACH `account_holder`
als allerletzter Eintrag in `audit_fields()` ergänzt werden — sonst werden existierende
Audit-Log-Hashes ungültig.

DAO-SQLite Spalten-Mapping (genossi_dao_impl_sqlite/src/member.rs):
- struct MemberDb: `status: Option<String>` (Z.62), `account_holder: Option<String>` (Z.65) — positionale sqlx::FromRow, Reihenfolge = SELECT-Spaltenreihenfolge
- TryFrom<&MemberDb> (Z.101-108): status via MemberStatus::from_str(...).transpose()?.unwrap_or_default()
- dump_all SELECT (Z.131-135): Spaltenliste, ...status, account_holder, created...
- create INSERT (Z.179-209): Spaltenliste + VALUES (?...) + .bind(status).bind(account_holder)
- update UPDATE (Z.268-300): SET ...status = ?, account_holder = ?... + .bind(status).bind(account_holder)

REST-Types (genossi_rest_types/src/lib.rs):
- `pub enum MemberStatusTO` (Z.134) mit From<&MemberStatus> (Z.141) und From<&MemberStatusTO> (Z.150)
- MemberTO.status (Z.220), MemberTO::from(&Member) status-Map (Z.264), From<&MemberTO> status-Map (Z.335)

Frontend-REST-Types (genossi-frontend/rest-types/src/lib.rs):
- `pub enum MemberStatusTO` (Z.144) mit `all()`, `as_str()`, `from_str()`, `display_name()`, `Default`
- MemberTO.status (Z.230)

Frontend Status-Select (genossi-frontend/src/page/member_details.rs:588-610): bestehender INLINE
RSX-Select für MemberStatus. Component-First-Pflicht (CLAUDE.md): den neuen postalischen Select
NICHT erneut inline duplizieren, sondern als wiederverwendbaren Component extrahieren.

i18n (genossi-frontend/src/i18n/): Key-Enum in mod.rs (`MemberStatus`, `MemberStatusNormal`,
`MemberStatusFehlerhaftErfasst` ~Z.129-131), Übersetzungen in de.rs/en.rs (~Z.80-82).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend-Datenmodell — PostalStatus Enum, DAO-Feld, SQLite-Mapping, Migration, Audit</name>
  <files>migrations/sqlite/20260625000000_add_postal_status_to_member.sql, genossi_dao/src/member.rs, genossi_dao_impl_sqlite/src/member.rs</files>
  <behavior>
    - PostalStatus::default() == PostalStatus::Erreichbar
    - PostalStatus roundtrip: für jede Variante gilt from_str(as_str()) == Variante (Erreichbar, Unzustellbar)
    - PostalStatus::from_str("Unbekannt") ist Err
    - MemberEntity::audit_fields() enthält ("postal_status", Some("Erreichbar")) und postal_status ist der LETZTE Eintrag (NACH account_holder)
    - audit_fields().len() == 23 (vorher 22; +1 postal_status)
    - DAO-Roundtrip (in genossi_dao_impl_sqlite member-Tests, falls vorhanden — sonst neuer Test in member.rs des DAO-Impl): create+read eines Members mit postal_status=Unzustellbar liest Unzustellbar zurück; create ohne Wert (DEFAULT) liest Erreichbar
  </behavior>
  <action>
1. Migration `migrations/sqlite/20260625000000_add_postal_status_to_member.sql` anlegen (Vorbild 20260411000002_add_status_to_member.sql, EXAKT eine Zeile):
   `ALTER TABLE member ADD COLUMN postal_status TEXT DEFAULT 'Erreichbar';`

2. `genossi_dao/src/member.rs`:
   - Neues Enum `PostalStatus` direkt nach `MemberStatus` (Z.66), Pattern 1:1 gespiegelt:
     `#[derive(Clone, Debug, PartialEq, Eq, Default)] pub enum PostalStatus { #[default] Erreichbar, Unzustellbar }`
     mit `as_str()` ("Erreichbar"/"Unzustellbar") und `from_str()` (sonst `DaoError::ParseError`). Optional `is_reachable()` analog `is_normal()`.
   - Feld auf `MemberEntity` ergänzen: `pub postal_status: PostalStatus,` — ANHÄNGEN NACH `account_holder` (Z.96), VOR `created`. Kommentar: "Quick 260625-e14: postalischer Status. APPENDED AT END für Hashchain-Stabilität."
   - `audit_fields()`: NEUEN Eintrag `("postal_status", Some(self.postal_status.as_str().to_string()))` als ALLERLETZTEN Eintrag im vec! ergänzen — NACH dem account_holder-Eintrag (Z.251-254). NICHT umsortieren.
   - In `make_entity_with_exit` (Test-Helper Z.275-302) `postal_status: PostalStatus::Erreichbar,` ergänzen.
   - Test `test_auditable_fields_count`: 22 -> 23 anpassen, `assert!(field_names.contains(&"postal_status"))` ergänzen.
   - Test `test_auditable_account_holder_appended_at_end`: dieser prüft account_holder als letztes — er ist jetzt FALSCH. Entweder umbenennen/anpassen auf `test_auditable_postal_status_appended_at_end` (prüft `fields.last() == Some("postal_status")`) ODER einen zusätzlichen Test ergänzen und den alten so anpassen, dass account_holder die VORLETZTE Position prüft. Empfehlung: alten Test in postal_status-Variante umwandeln (last() == "postal_status").
   - Neue Enum-Tests ergänzen analog test_member_status_* (roundtrip, as_str, invalid_value, default).

3. `genossi_dao_impl_sqlite/src/member.rs`:
   - Import `PostalStatus` aus genossi_dao::member ergänzen (Z.2).
   - struct `MemberDb`: `postal_status: Option<String>,` ANHÄNGEN NACH `account_holder` (Z.65), VOR `created`. (positionale FromRow → muss in SELECT-Reihenfolge stehen).
   - `TryFrom<&MemberDb>`: `postal_status: db.postal_status.as_deref().map(PostalStatus::from_str).transpose()?.unwrap_or_default(),` ANHÄNGEN NACH account_holder (Z.108).
   - `dump_all` SELECT-Spaltenliste (Z.134): `... bank_account, status, account_holder, postal_status, created, deleted, version` — postal_status NACH account_holder, VOR created.
   - `create`: lokale Variable `let postal_status = entity.postal_status.as_str().to_string();` (analog status); INSERT Spaltenliste + VALUES um eine Spalte/ein `?` erweitern (postal_status NACH account_holder, VOR created); `.bind(postal_status)` NACH `.bind(account_holder)` (Z.207), VOR `.bind(created)`.
   - `update`: `let postal_status = entity.postal_status.as_str().to_string();`; SET-Klausel `... status = ?, account_holder = ?, postal_status = ?, deleted = ?, version = ?`; `.bind(postal_status)` NACH `.bind(account_holder)` (Z.296), VOR `.bind(deleted)`.
   - WICHTIG: Reihenfolge MemberDb-Feld / SELECT-Spalte / FromRow muss konsistent sein (positional). postal_status überall an derselben relativen Position (nach account_holder, vor created/deleted).
   - DAO-Roundtrip-Test (falls in dieser Datei ein Test-Modul mit echtem In-Memory-Pool existiert, dort ergänzen; sonst keinen erzwingen — der Roundtrip wird durch den e2e-Test in Task 2 abgedeckt). Falls kein Test-Modul existiert: Verifikation primär über cargo build/test des Pakets.

4. WICHTIG (CLAUDE.md global rule): Tests vorhanden. Wenn sqlx offline-Daten verwendet werden, ggf. `DATABASE_URL=sqlite:genossi.db cargo sqlx prepare` ausführen, falls Compile-Fehler wegen query!-Makros auftreten (hier werden jedoch `sqlx::query`/`query_as` ohne Compile-Check verwendet → vermutlich nicht nötig).
  </action>
  <verify>
    <automated>cargo test -p genossi_dao member 2>&1 | tail -20</automated>
    <automated>cargo build -p genossi_dao_impl_sqlite 2>&1 | tail -15</automated>
  </verify>
  <done>PostalStatus-Enum existiert mit Default Erreichbar; MemberEntity hat postal_status als letztes audit_fields-Element; SQLite create/read/update mappen die Spalte; Migration vorhanden; genossi_dao-Tests grün (inkl. audit_fields.len()==23 und postal_status-als-letztes).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Service + REST-Types durchschleifen, alle literalen Konstruktionen reparieren, Audit-Roundtrip-Test</name>
  <files>genossi_service/src/member.rs, genossi_service_impl/src/member.rs, genossi_rest_types/src/lib.rs, genossi_service_impl/src/membership_adjust.rs, genossi_service_impl/src/assembly.rs, genossi_service_impl/src/repayment_entry.rs, genossi_service_impl/src/repayment_letter.rs, genossi_service_impl/src/repayment_phase.rs, genossi_mail/src/backfill.rs, genossi_mail/src/render.rs, genossi_mail/src/rest.rs, genossi_bin/tests/membership_adjust_e2e.rs</files>
  <behavior>
    - Service-Roundtrip: Member { postal_status: Unzustellbar, .. } -> MemberEntity -> Member behält Unzustellbar (analog test_member_to_entity_preserves_account_holder)
    - MemberTO (genossi_rest_types) -> Member -> MemberTO behält postal_status
    - e2e (optional, falls schnell ergänzbar): Member anlegen, postal_status auf Unzustellbar updaten, Audit-Log enthält Feld postal_status mit neuem Wert
  </behavior>
  <action>
1. `genossi_service/src/member.rs`:
   - Import `PostalStatus` aus genossi_dao::member ergänzen (Z.2).
   - Struct `Member`: `pub postal_status: PostalStatus,` ANHÄNGEN NACH `account_holder` (Z.39), VOR `created`. Kommentar "Quick 260625-e14: postalischer Status, spiegelt MemberEntity."
   - `From<&MemberEntity> for Member` (Z.70): `postal_status: entity.postal_status.clone(),` ergänzen.
   - `From<&Member> for MemberEntity` (Z.103): `postal_status: member.postal_status.clone(),` ergänzen.
   - Falls in dieser Datei ein Test-Helper `make_member()`/`default member` existiert, postal_status ergänzen.

2. `genossi_rest_types/src/lib.rs`:
   - Import `PostalStatus` ergänzen (Z.2 use genossi_dao::member::{...}).
   - Neues `PostalStatusTO` Enum analog `MemberStatusTO` (Z.134): Varianten `Erreichbar` (#[default]), `Unzustellbar`, Derives `Clone, Debug, PartialEq, Eq, Serialize, Deserialize, ToSchema, Default`.
   - `From<&PostalStatus> for PostalStatusTO` und `From<&PostalStatusTO> for PostalStatus` (analog Z.141 / Z.150).
   - `MemberTO`: Feld `#[serde(default)] pub postal_status: PostalStatusTO,` ANHÄNGEN NACH `status` (Z.220) ODER nach account_holder — konsistent platzieren, VOR created. (Default sorgt für Abwärtskompatibilität älterer Clients.)
   - `MemberTO::from(&Member)` (Z.264): `postal_status: PostalStatusTO::from(&m.postal_status),`.
   - `From<&MemberTO> for Member` (Z.335): `postal_status: PostalStatus::from(&to.postal_status),`.
   - MemberSlimTO NICHT anfassen (PII-Whitelist, postal_status ist kein Slim-Feld).
   - In ToSchema/OpenApi-Registrierung: prüfen, ob `PostalStatusTO` als Schema registriert werden muss (analog MemberStatusTO). Falls MemberStatusTO irgendwo in einer components(schemas(...))-Liste steht, PostalStatusTO daneben ergänzen.

3. Literale Konstruktions-Sites reparieren (Compiler erzwingt das, da postal_status kein Option ist). In JEDER Datei, die eine `Member { .. }`, `MemberEntity { .. }` oder `MemberTO { .. }` mit explizitem `status:`-Feld baut, `postal_status: <Default-Variante>` ergänzen:
   - MemberEntity/Member: `postal_status: PostalStatus::Erreichbar,` (bzw. `genossi_dao::member::PostalStatus::Erreichbar`)
   - genossi_rest_types MemberTO: `postal_status: PostalStatusTO::Erreichbar,` (bzw. `..Default::default()` falls die Site das nutzt)
   Betroffene Dateien (Liste aus grep `status: MemberStatus`/`status: MemberStatusTO`): genossi_service_impl/src/member.rs (Test-Helper Z.~194), membership_adjust.rs, assembly.rs, repayment_entry.rs, repayment_letter.rs, repayment_phase.rs, genossi_mail/src/backfill.rs, render.rs, rest.rs, genossi_bin/tests/membership_adjust_e2e.rs.
   Strategie: `cargo build` laufen lassen, der Compiler listet jede fehlende `postal_status`-Site mit Datei+Zeile auf — exakt dort ergänzen. KEINE Logik ändern, nur das neue Default-Feld einsetzen.
   AUSGEKLAMMERT-Hinweis: repayment_*/mail/assembly NUR um das Default-Feld ergänzen — KEINE Versand-/Filter-Logik mit postal_status hinzufügen.

4. Audit-/Service-Roundtrip-Test (CLAUDE.md global rule):
   - In genossi_service_impl/src/member.rs (Test-Modul mit test_member_to_entity_preserves_account_holder, Z.~233): analogen Test `test_member_to_entity_preserves_postal_status` ergänzen (Unzustellbar überlebt Member->Entity->Member).
   - In genossi_rest_types Tests (falls vorhanden) MemberTO<->Member Roundtrip für postal_status; sonst im member.rs-Service-Test abgedeckt.
  </action>
  <verify>
    <automated>cargo build 2>&1 | tail -20</automated>
    <automated>cargo test -p genossi_service_impl member 2>&1 | tail -20</automated>
    <automated>cargo test -p genossi_rest_types 2>&1 | tail -10</automated>
  </verify>
  <done>Gesamter Workspace kompiliert; postal_status fließt Member<->Entity<->MemberTO ohne Verlust; Roundtrip-Test grün; update()-Pfad nutzt unverändert audited_update! (postal_status landet automatisch in audit_fields).</done>
</task>

<task type="auto">
  <name>Task 3: Frontend — wiederverwendbarer Postal-Status-Select, member_details-Wiring, i18n</name>
  <files>genossi-frontend/rest-types/src/lib.rs, genossi-frontend/src/component/member_postal_status_select.rs, genossi-frontend/src/component/mod.rs, genossi-frontend/src/page/member_details.rs, genossi-frontend/src/component/membership_adjust_modal.rs, genossi-frontend/src/component/repayment_entry_list.rs, genossi-frontend/src/i18n/mod.rs, genossi-frontend/src/i18n/de.rs, genossi-frontend/src/i18n/en.rs</files>
  <action>
1. `genossi-frontend/rest-types/src/lib.rs`:
   - Neues `PostalStatusTO` Enum analog `MemberStatusTO` (Z.144-186): Varianten `Erreichbar`, `Unzustellbar`; `all()` -> [Erreichbar, Unzustellbar]; `as_str()` ("Erreichbar"/"Unzustellbar"); `from_str()` (Option); `display_name()` ("Erreichbar"/"Unzustellbar"); `impl Default` -> Erreichbar; Derives Clone, Debug, Serialize, Deserialize, PartialEq, Eq.
   - `MemberTO`: `#[serde(default)] pub postal_status: PostalStatusTO,` ANHÄNGEN NACH `status` (Z.230), VOR created.
   - Falls in dieser Datei MemberTO-Literale gebaut werden (member_search/Default-MemberTO): postal_status ergänzen (Compiler erzwingt).

2. Component-First (CLAUDE.md Pflicht — kein inline-RSX-Duplikat): Neuen wiederverwendbaren Component `genossi-frontend/src/component/member_postal_status_select.rs` anlegen. Props: `value: PostalStatusTO`, `onchange: EventHandler<PostalStatusTO>`, optional `label: String`. RSX spiegelt den bestehenden Status-Select (member_details.rs:588-610): `<select>` mit `for s in PostalStatusTO::all()` → `<option value=s.as_str() selected=value==*s>{i18n-übersetzter Name}</option>`, onchange ruft `PostalStatusTO::from_str` und feuert den EventHandler. Übersetzte Anzeige über i18n-Keys (siehe Schritt 4) statt display_name(), damit en/de funktioniert.
   - In `genossi-frontend/src/component/mod.rs` das neue Modul `pub mod member_postal_status_select;` registrieren und ggf. re-exportieren (Muster der anderen Components folgen).

3. `genossi-frontend/src/page/member_details.rs`:
   - Import `MemberPostalStatusSelect` (component) und `PostalStatusTO`.
   - Direkt NACH dem bestehenden Status-Block (nach Z.610) einen neuen Block einfügen, der `MemberPostalStatusSelect` rendert:
     `MemberPostalStatusSelect { value: member.read().postal_status.clone(), onchange: move |s| { member.write().postal_status = s; } }`
   - Den bestehenden Default-Member-Konstruktor (Z.~121 `status: MemberStatusTO::Normal,`) um `postal_status: PostalStatusTO::Erreichbar,` ergänzen.
   - HINWEIS (optional, saubere Lösung): Der bestehende inline MemberStatus-Select (Z.588-610) DARF in dieser Task unverändert bleiben — er ist kein Duplikat des Postal-Selects. Den NEUEN Postal-Select aber NICHT inline duplizieren, sondern den Component nutzen.

4. i18n (genossi-frontend/src/i18n/):
   - mod.rs: Key-Enum um `MemberPostalStatus`, `MemberPostalStatusErreichbar`, `MemberPostalStatusUnzustellbar` ergänzen (nach den MemberStatus*-Keys ~Z.131).
   - de.rs: `Key::MemberPostalStatus => "Postalischer Status".into(), Key::MemberPostalStatusErreichbar => "Erreichbar".into(), Key::MemberPostalStatusUnzustellbar => "Unzustellbar".into(),`
   - en.rs: `Key::MemberPostalStatus => "Postal Status".into(), Key::MemberPostalStatusErreichbar => "Reachable".into(), Key::MemberPostalStatusUnzustellbar => "Undeliverable".into(),`
   - Der Component nutzt diese Keys für die Option-Beschriftung und das Label (i18n.t(...)).

5. Literale MemberTO-Konstruktionen in Frontend-Components reparieren (membership_adjust_modal.rs, repayment_entry_list.rs bauen MemberTO mit status:) → `postal_status: PostalStatusTO::Erreichbar,` (oder `..Default::default()`) ergänzen, damit das Frontend-Crate kompiliert. KEINE Logik ändern.

6. Tests (CLAUDE.md global rule): In genossi-frontend/rest-types einen Roundtrip-Test ergänzen analog vorhandener MemberStatusTO-Tests, falls vorhanden — sonst neues #[cfg(test)] mod: für jede PostalStatusTO::all()-Variante `from_str(as_str()) == Some(variante)`, und `from_str("X") == None`, `default == Erreichbar`. (Component-RSX selbst wird nicht unit-getestet — Verifikation über cargo check des Frontend-Crates.)
  </action>
  <verify>
    <automated>cargo test -p rest-types 2>&1 | tail -15 || cargo test -p genossi-frontend-rest-types 2>&1 | tail -15</automated>
    <automated>cargo check -p genossi-frontend 2>&1 | tail -20</automated>
  </verify>
  <done>Frontend-PostalStatusTO mit all/as_str/from_str/display_name + Default; wiederverwendbarer MemberPostalStatusSelect-Component existiert und ist auf der Member-Detail-Seite eingebunden und schreibt in member.postal_status; i18n-Keys in de+en vorhanden; Frontend-Crate kompiliert; rest-types-Roundtrip-Test grün.</done>
</task>

</tasks>

<verification>
- `cargo build` (gesamter Workspace) kompiliert ohne Fehler.
- `cargo test -p genossi_dao member` grün: PostalStatus-Roundtrip, audit_fields.len()==23, postal_status als letzter audit_fields-Eintrag.
- `cargo test -p genossi_service_impl member` grün: Member<->Entity-Roundtrip behält postal_status.
- `cargo check -p genossi-frontend` ohne Fehler; MemberPostalStatusSelect eingebunden.
- Migrations-Datei `20260625000000_add_postal_status_to_member.sql` vorhanden, Spalte `postal_status TEXT DEFAULT 'Erreichbar'`.
- `cargo clippy --all-targets` ohne neue Warnungen (best effort).
- AUSGEKLAMMERT verifiziert: kein neuer Code wertet postal_status in Versand-Flows aus (kein `postal_status` in Filter-/Send-Logik von repayment_*/mail/assembly — nur Default-Feld-Ergänzung).
</verification>

<success_criteria>
- Ein Member trägt einen erweiterbaren postalischen Status (Erreichbar default / Unzustellbar).
- Bestandsdaten ohne Wert sind Erreichbar (DB DEFAULT + unwrap_or_default).
- Statusänderung läuft über den bestehenden audited_update!-Pfad und landet in der Audit-Hashchain (postal_status als letztes audit_fields-Feld → bestehende Hashes bleiben gültig).
- Postal-Status überlebt DAO- und REST-Roundtrip.
- Member-Detail-Seite bietet einen Component-basierten Select (kein inline-Duplikat); i18n in de+en.
- Versand-Flow-Integration NICHT enthalten (separater Folge-Task).
</success_criteria>

<output>
After completion, create `.planning/quick/260625-e14-genosse-als-postalisch-nicht-erreichbar-/260625-e14-SUMMARY.md`
</output>
