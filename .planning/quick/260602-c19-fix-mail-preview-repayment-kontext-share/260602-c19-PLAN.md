---
phase: 260602-c19
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - genossi_mail/src/rest.rs
  - genossi_bin/src/lib.rs
  - genossi_bin/tests/e2e_tests.rs
autonomous: true
requirements:
  - "BUG-c19: Mail-Preview share_count zeigt echte Per-Member-Aggregation (nicht hartkodierte 1)"

must_haves:
  truths:
    - "POST /api/mail/preview mit body.repayment_phase_id liefert share_count == Summe der Open/Contacted RepaymentEntries des selektierten Members"
    - "POST /api/mail/preview liefert payout_amount im Format 'X,YZ' aus share_count * phase.share_value (Cents)"
    - "POST /api/mail/preview liefert fiscal_year == phase.fiscal_year"
    - "Wenn Member keine Open/Contacted-Entries in der Phase hat, bleibt base_ctx unverändert (kein 0/1-Default-Merge)"
    - "Pure-Member-Preview-Pfad (ohne repayment_phase_id) bleibt unverändert"
    - "Send-Pfad (worker.rs:332-361) bleibt unverändert — Aggregations-Logik wird dupliziert, nicht refaktoriert (Scope-Disziplin)"
  artifacts:
    - path: "genossi_mail/src/rest.rs"
      provides: "MailRestState::resolve_repayment_context Trait-Methode + preview_mail Handler nutzt sie"
      contains: "fn resolve_repayment_context"
    - path: "genossi_bin/src/lib.rs"
      provides: "RestStateImpl::resolve_repayment_context-Implementierung mit echter DAO-Aggregation"
      contains: "fn resolve_repayment_context"
    - path: "genossi_bin/tests/e2e_tests.rs"
      provides: "E2E-Regression-Test: Preview share_count == 3 für Member mit 3 Anteilen"
      contains: "test_mail_preview_repayment_share_count"
  key_links:
    - from: "genossi_mail/src/rest.rs::preview_mail"
      to: "MailRestState::resolve_repayment_context"
      via: "state.resolve_repayment_context(phase_id, member_id).await"
      pattern: "state\\.resolve_repayment_context\\("
    - from: "genossi_bin/src/lib.rs::resolve_repayment_context"
      to: "self.repayment_phase_dao / self.repayment_entry_dao"
      via: "find_by_id + find_by_phase_id + filter+sum"
      pattern: "find_by_phase_id"
---

<objective>
Bug-Fix: Mail-Preview-Endpoint (`POST /api/mail/preview`) ruft im Repayment-Kontext
`merge_repayment_context` mit hartkodierten Dummies `("60,00", 1, 2026)` auf.
Folge: Die Vorschau zeigt für jedes Mitglied `share_count = 1`, unabhängig vom
DB-Stand. Send-Pfad (Worker) funktioniert korrekt — Bug ist isoliert auf Preview.

Purpose: Anwender wählen im Repayment-Flow Mitglieder aus, klicken "Mail senden"
und sehen in der Vorschau falsche Anteils-Zahlen. Das untergräbt das Vertrauen
in den Versand und führt zu mentaler Doppelarbeit (gegenchecken). Fix angleicht
Preview an Worker-Logik, damit was-du-siehst == was-du-sendest gilt.

Output:
- `MailRestState`-Trait um `resolve_repayment_context` erweitert.
- `preview_mail`-Handler nutzt echte Aggregation statt Dummies.
- `RestStateImpl` implementiert die Methode analog `worker.rs:332-361`.
- E2E-Test sichert das Verhalten als Regression dauerhaft ab.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/debug/rueckzahlung-mail-anteile-1.md
@./CLAUDE.md

# Debug-Session enthält vollständige Root-Cause-Analyse mit Code-Snippets (Option A).
# Worker-Logik (worker.rs:332-361) ist die Single Source of Truth für die
# Aggregations-Regel — Preview MUSS identisch rechnen.

@/home/neosam/programming/rust/projects/genossi3/genossi_mail/src/rest.rs
@/home/neosam/programming/rust/projects/genossi3/genossi_mail/src/worker.rs
@/home/neosam/programming/rust/projects/genossi3/genossi_bin/src/lib.rs
@/home/neosam/programming/rust/projects/genossi3/genossi_mail/src/template.rs
@/home/neosam/programming/rust/projects/genossi3/genossi_dao/src/repayment_entry.rs
@/home/neosam/programming/rust/projects/genossi3/genossi_dao/src/repayment_phase.rs

<interfaces>
<!-- Worker-Aggregations-Logik (genossi_mail/src/worker.rs:332-361) — Single Source of Truth.
     Preview MUSS identisch rechnen. Filter, Status-Set, Format-Convention kopieren. -->

Worker-Logik (worker.rs:332-361, normalisiert):
```rust
// D-06 filter: deleted IS NULL AND status IN (Open, Contacted).
// PaidOut and Declined explicitly excluded (semantically already paid out / refused).
let relevant: Vec<_> = entries.iter()
    .filter(|e| {
        e.deleted.is_none()
            && e.member_id == member.id
            && matches!(
                e.status,
                genossi_dao::repayment_entry::RepaymentEntryStatus::Open
                | genossi_dao::repayment_entry::RepaymentEntryStatus::Contacted
            )
    })
    .collect();

// D-05: only merge if at least one relevant entry exists.
if !relevant.is_empty() {
    let share_count: i32 =
        relevant.iter().map(|e| e.share_count_to_pay_out).sum();
    let cents: i64 = (share_count as i64) * (phase.share_value);
    // German locale "X,YZ" (Plan 10.05-aligned formatting).
    let payout_amount = format!("{},{:02}", cents / 100, cents % 100);

    ctx = merge_repayment_context(
        ctx, &payout_amount, share_count, phase.fiscal_year,
    );
}
```

MailRestState-Trait (genossi_mail/src/rest.rs:25-48), existierendes Pattern:
```rust
pub trait MailRestState: Clone + Send + Sync + 'static {
    type MailService: MailService;
    fn mail_service(&self) -> Arc<Self::MailService>;
    fn resolve_document(&self, document_id: uuid::Uuid)
        -> std::pin::Pin<Box<dyn std::future::Future<Output = Option<ResolvedDocument>> + Send + '_>>;
    fn get_recipient_attachments(&self, recipient_id: uuid::Uuid)
        -> std::pin::Pin<Box<dyn std::future::Future<Output = Vec<MailAttachmentTO>> + Send + '_>>;
    fn resolve_member(&self, member_id: uuid::Uuid)
        -> std::pin::Pin<Box<dyn std::future::Future<Output = Option<MemberEntity>> + Send + '_>>;
    fn resolve_members(&self, member_ids: &[uuid::Uuid])
        -> std::pin::Pin<Box<dyn std::future::Future<Output = Vec<MemberEntity>> + Send + '_>>;
}
```

Existierender Preview-Handler (genossi_mail/src/rest.rs:494-508) — zu ersetzen:
```rust
let base_ctx = member_to_template_context(&member);
let ctx = if body.repayment_phase_id.as_deref()
    .map(|s| !s.is_empty()).unwrap_or(false)
{
    crate::template::merge_repayment_context(base_ctx, "60,00", 1, 2026) // <-- BUG
} else {
    base_ctx
};
```

RestStateImpl-DAO-Felder (genossi_bin/src/lib.rs:623-624) — bereits vorhanden:
- `repayment_phase_dao: Arc<RepaymentPhaseDao>` (= RepaymentPhaseDaoImpl)
- `repayment_entry_dao: Arc<RepaymentEntryDao>` (= RepaymentEntryDaoImpl)
- `transaction_dao: Arc<TransactionDao>` (= TransactionDaoImpl)
- pool ist `self.pool: Arc<SqlitePool>`

RepaymentEntryDao::find_by_phase_id (genossi_dao/src/repayment_entry.rs:140):
```rust
async fn find_by_phase_id(
    &self, phase_id: Uuid, tx: Self::Transaction,
) -> Result<Arc<[RepaymentEntryEntity]>, DaoError>;
// Default-Impl filtert bereits deleted.is_none(), wir filtern hier additiv auf
// member_id + status. Robust gegen Default-Impl-Änderungen, deshalb expliziter
// `e.deleted.is_none()`-Filter bleibt erhalten.
```

RepaymentEntryEntity-Felder (genossi_dao/src/repayment_entry.rs:55-65):
- `id: Uuid`, `member_id: Uuid`, `phase_id: Uuid`
- `share_count_to_pay_out: i32`
- `status: RepaymentEntryStatus` (Open | Contacted | PaidOut | Declined)
- `deleted: Option<PrimitiveDateTime>`

RepaymentPhaseEntity-Felder (genossi_dao/src/repayment_phase.rs:41-52):
- `id: Uuid`, `fiscal_year: i32`, `share_value: i64` (Cents, D-12 i64-Konvention)
- `status: RepaymentPhaseStatus`

Existierendes `resolve_member`-Impl-Pattern (genossi_bin/src/lib.rs:1404-1421)
ist die Vorlage für `resolve_repayment_context`: pool clonen, neue
TransactionDaoImpl lokal bauen, tx eröffnen, DAO-Methoden aufrufen, Option zurückgeben.
NOTE: Pattern nutzt frisch konstruierte DAOs statt der RestStateImpl-Felder —
das ist Konvention im File und wird hier beibehalten (kein Refactor in dieser
Quick-Session; Tech-Debt für später).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trait-Methode + Preview-Handler-Fix + RestStateImpl-Implementierung</name>
  <files>genossi_mail/src/rest.rs, genossi_bin/src/lib.rs</files>
  <action>
**Schritt A — `MailRestState`-Trait erweitern (genossi_mail/src/rest.rs:25-48):**

Neue Methode in den Trait einfügen, Signatur-Stil 1:1 wie bestehende
`resolve_member`-Methode (kein async_trait, sondern handgeschriebenes Pin/Box-Future):

```rust
/// Resolve the per-member repayment context for `/preview` rendering.
///
/// Mirrors the aggregation logic in `genossi_mail/src/worker.rs:332-361`
/// (filter `deleted IS NULL` + `status IN (Open, Contacted)`, sum
/// `share_count_to_pay_out`, format payout in German locale `X,YZ`).
///
/// Returns `None` when the phase does not exist OR the member has no
/// Open/Contacted entries in the phase (D-05 symmetry with the worker).
///
/// Tuple shape: `(payout_amount, share_count, fiscal_year)`.
fn resolve_repayment_context(
    &self,
    phase_id: uuid::Uuid,
    member_id: uuid::Uuid,
) -> std::pin::Pin<
    Box<dyn std::future::Future<Output = Option<(String, i32, i32)>> + Send + '_>,
>;
```

**Schritt B — Preview-Handler patchen (genossi_mail/src/rest.rs:494-508):**

Hartkodiertes `merge_repayment_context(base_ctx, "60,00", 1, 2026)` durch
echten Call ersetzen. Replace existing block:

```rust
let base_ctx = member_to_template_context(&member);
let ctx = match body.repayment_phase_id.as_deref() {
    Some(s) if !s.is_empty() => {
        let phase_id = uuid::Uuid::parse_str(s).map_err(|_| {
            MailServiceError::BadRequest(Arc::from("Invalid repayment_phase_id"))
        })?;
        match state.resolve_repayment_context(phase_id, member_id).await {
            Some((payout, share_count, fiscal_year)) => {
                crate::template::merge_repayment_context(
                    base_ctx, &payout, share_count, fiscal_year,
                )
            }
            // D-05 symmetry with worker.rs:347-361: no relevant entries → no merge.
            // Template must guard with `{% if share_count is defined %}` (Plan 10.05).
            None => base_ctx,
        }
    }
    _ => base_ctx,
};
```

Den falschen UAT-Defekt-#6-Kommentar (rest.rs:494-497) komplett entfernen, durch
einen 1-Zeilen-Kommentar ersetzen, der auf worker.rs:332-361 als Source of
Truth verweist. Doc-String an `PreviewRequest.repayment_phase_id`
(genossi_mail/src/rest.rs:149-153) anpassen: NICHT mehr "gemergte Dummy-Werte",
sondern: "Wenn gesetzt, werden `payout_amount`/`share_count`/`fiscal_year` aus
den Open/Contacted-RepaymentEntries des Members aggregiert (gleiche Logik wie
der Send-Worker)".

**Schritt C — `RestStateImpl::resolve_repayment_context` implementieren
(genossi_bin/src/lib.rs, innerhalb `impl MailRestState for RestStateImpl`,
direkt nach `resolve_member` Z. 1404-1421 einfügen):**

Folge dem `resolve_member`-Stilmuster (pool clonen, lokale DAOs konstruieren,
tx eröffnen, ok()? für Fehler-Swallowing). KEINE neuen Felder an RestStateImpl
hinzufügen — bewusste Pattern-Konsistenz mit `resolve_member`.

```rust
fn resolve_repayment_context(
    &self,
    phase_id: UuidType,
    member_id: UuidType,
) -> std::pin::Pin<
    Box<dyn std::future::Future<Output = Option<(String, i32, i32)>> + Send + '_>,
> {
    let pool = self.pool.clone();
    Box::pin(async move {
        use genossi_dao::repayment_entry::{RepaymentEntryDao as _, RepaymentEntryStatus};
        use genossi_dao::repayment_phase::RepaymentPhaseDao as _;
        use genossi_dao::TransactionDao as _;

        let transaction_dao = TransactionDaoImpl::new(pool.clone());
        let phase_dao =
            genossi_dao_impl_sqlite::repayment_phase::RepaymentPhaseDaoImpl::new(pool.clone());
        let entry_dao =
            genossi_dao_impl_sqlite::repayment_entry::RepaymentEntryDaoImpl::new(pool);

        let tx = transaction_dao.transaction().await.ok()?;
        // Reuse tx for both DAO calls (find_by_id consumes Self::Transaction).
        // tx is Cloneable (Arc-backed) per existing resolve_member pattern; if
        // not, split into two transactions — verify against the local DAO type
        // signatures during implementation.
        let phase = phase_dao.find_by_id(phase_id, tx.clone()).await.ok()??;
        let entries = entry_dao.find_by_phase_id(phase_id, tx).await.ok()?;

        // Mirror worker.rs:332-361 EXACTLY — D-06 filter + D-05 emptiness check.
        let share_count: i32 = entries
            .iter()
            .filter(|e| {
                e.deleted.is_none()
                    && e.member_id == member_id
                    && matches!(
                        e.status,
                        RepaymentEntryStatus::Open | RepaymentEntryStatus::Contacted
                    )
            })
            .map(|e| e.share_count_to_pay_out)
            .sum();

        if share_count == 0 {
            return None;
        }

        let cents: i64 = (share_count as i64) * phase.share_value;
        let payout_amount = format!("{},{:02}", cents / 100, cents % 100);
        Some((payout_amount, share_count, phase.fiscal_year))
    })
}
```

**Implementierungs-Notiz (txn-Cloneability):** `MockTransaction` und
`TransactionImpl` in der Codebase sind durchweg `Clone` (siehe vergleichbare
Worker-Aggregation in worker.rs:332-361, die `entry_dao.find_by_phase_id(phase_id, tx)`
nach `phase_dao.find_by_id(phase_id, tx_clone)` aufruft). Falls beim Kompilieren
`Transaction: !Clone` aufschlägt, ist der Fallback: zwei separate
`transaction_dao.transaction().await`-Calls (eine pro DAO-Call) — semantisch
identisch, weil es read-only Lookups sind und die Phase zwischen den beiden
Lookups praktisch nicht verschwinden kann.

**Verify-Hinweis Schritt B:** Der bestehende Kommentar in
`PreviewRequest.repayment_phase_id` (Z. 149-159 ca.) erwähnt die alten
Dummy-Werte explizit — wenn die alte Formulierung stehen bleibt, wäre der Code
nicht mehr selbst-konsistent. Doc-Update ist Teil dieses Tasks.

**Wichtig (CLAUDE.md-Konformität):**
- KEIN Audit-Macro (Preview ist read-only, kein Schreibpfad).
- KEINE Migration (kein Schema-Touch).
- ISO8601-Datetime ist hier nicht involviert (keine PrimitiveDateTime-API-Felder).
- Layered DAO/Service/REST: Trait-Erweiterung respektiert die existierende
  `MailRestState`-Konvention (genossi_bin macht das DI-Wiring; genossi_mail
  bleibt frei von genossi_dao_impl_sqlite-Imports).
  </action>
  <verify>
    <automated>cargo build -p genossi_mail -p genossi_bin 2>&1 | tail -20</automated>
  </verify>
  <done>
- `cargo build -p genossi_mail -p genossi_bin` compiliert ohne Fehler.
- `grep -n "resolve_repayment_context" genossi_mail/src/rest.rs` zeigt exakt 2 Treffer (Trait-Decl + Handler-Call).
- `grep -n "resolve_repayment_context" genossi_bin/src/lib.rs` zeigt exakt 1 Treffer (Impl).
- `grep -nE '"60,00"|merge_repayment_context\(.*,\s*1\s*,\s*2026\)' genossi_mail/src/rest.rs` returnt 0 Treffer (Dummies entfernt; Kommentare zählen via `grep -v '^//'` ausgeschlossen).
- `grep -c "Filter.*Open.*Contacted\|RepaymentEntryStatus::Open" genossi_bin/src/lib.rs` ≥ 1 (Filter dupliziert für Preview).
- Doc-Comment an `PreviewRequest.repayment_phase_id` erwähnt NICHT mehr "Dummy" / "60,00" / "1".
  </done>
</task>

<task type="auto">
  <name>Task 2: E2E-Regression-Test — Preview liefert echte share_count-Aggregation</name>
  <files>genossi_bin/tests/e2e_tests.rs</files>
  <action>
Einen einzelnen E2E-Regression-Test ans Ende der Repayment-Test-Sektion in
`genossi_bin/tests/e2e_tests.rs` anhängen. Nutzt das existierende
`create_open_repayment_phase`-Helper-Pattern (siehe Z. 11109-11131) und folgt
dem etablierten Setup-Order (Member ZUERST, Phase DANACH — sonst greift
Auto-Fill nicht; siehe STATE.md-Eintrag "Plan 09-04: Setup-Reihenfolge bei
Auto-Fill-Phases").

```rust
/// Quick-c19 Regression — Mail-Preview rendert echte share_count-Aggregation
/// statt hartkodierter "1". Verifiziert dass POST /api/mail/preview die
/// Worker-Logik (worker.rs:332-361) für RepaymentContext spiegelt.
///
/// Setup: 1 Member mit current_shares=3 + exit_date in FY → Auto-Fill erzeugt
/// 1 Entry mit share_count_to_pay_out=3 (Default beim Open der Phase).
/// Body-Template: "Anteile: {{ share_count }}, Auszahlung: {{ payout_amount }} EUR, Jahr: {{ fiscal_year }}"
/// Erwartung: rendered_body enthält "3" (nicht "1") + "360,00" + "2026".
#[tokio::test]
async fn test_mail_preview_repayment_share_count_aggregates_real_value() {
    let server = setup().await;
    let client = reqwest::Client::new();
    let fiscal_year = 2026;

    // Member ZUERST anlegen — Auto-Fill braucht ihn beim Open der Phase.
    let member = create_member_with_exit_date(&client, &server, 1, fiscal_year, 3).await;

    // share_value = 12000 Cents = 120,00 EUR pro Anteil → 3 * 120,00 = 360,00.
    let phase = create_open_repayment_phase(&client, &server, fiscal_year, 12000).await;

    let preview_body = serde_json::json!({
        "subject": "Test {{ share_count }}",
        "body": "Anteile: {{ share_count }}, Auszahlung: {{ payout_amount }} EUR, Jahr: {{ fiscal_year }}",
        "member_id": member.id.to_string(),
        "repayment_phase_id": phase.id.to_string(),
    });

    let resp = client
        .post(server.url("/api/mail/preview"))
        .json(&preview_body)
        .send()
        .await
        .expect("preview POST failed");
    assert_eq!(resp.status(), reqwest::StatusCode::OK);

    let json: serde_json::Value = resp.json().await.expect("decode preview response");
    let rendered_body = json["body"].as_str().expect("body must be string");
    let rendered_subject = json["subject"].as_str().expect("subject must be string");

    // Regression core: NOT "Anteile: 1".
    assert!(
        !rendered_body.contains("Anteile: 1,"),
        "BUG c19: preview must NOT render hardcoded share_count=1; got body={:?}",
        rendered_body
    );
    // Positive assertions: actual aggregated values present.
    assert!(
        rendered_body.contains("Anteile: 3,"),
        "preview must render real share_count=3 from RepaymentEntry; got body={:?}",
        rendered_body
    );
    assert!(
        rendered_body.contains("360,00"),
        "preview must render payout_amount=360,00 (= 3 * 120,00 EUR); got body={:?}",
        rendered_body
    );
    assert!(
        rendered_body.contains("2026"),
        "preview must render fiscal_year=2026; got body={:?}",
        rendered_body
    );
    assert!(
        rendered_subject.contains("3"),
        "subject template must also resolve share_count; got subject={:?}",
        rendered_subject
    );
}

/// Quick-c19 Regression — D-05 Symmetrie: Member ohne Open/Contacted-Entries
/// in der Phase → kein Merge → Template ohne `{% if defined %}`-Guard auf
/// share_count muss bei minijinja strict-env einen Render-Error in `errors`
/// liefern (statt fälschlich `1` zu rendern).
#[tokio::test]
async fn test_mail_preview_repayment_no_entries_does_not_default_to_one() {
    let server = setup().await;
    let client = reqwest::Client::new();
    let fiscal_year = 2026;

    // Member OHNE exit_date → kein Auto-Fill-Entry erzeugt.
    let member = create_member(&client, &server, sample_member(1)).await;
    let phase = create_open_repayment_phase(&client, &server, fiscal_year, 12000).await;

    let preview_body = serde_json::json!({
        "subject": "Plain",
        // Bewusst KEIN {% if defined %}-Guard, damit ein versehentliches Merge mit
        // Dummy-Werten als Render-Success durchkäme.
        "body": "Anteile: {{ share_count }}",
        "member_id": member.id.to_string(),
        "repayment_phase_id": phase.id.to_string(),
    });

    let resp = client
        .post(server.url("/api/mail/preview"))
        .json(&preview_body)
        .send()
        .await
        .expect("preview POST failed");
    assert_eq!(resp.status(), reqwest::StatusCode::OK);

    let json: serde_json::Value = resp.json().await.expect("decode");
    let errors = json["errors"].as_array().expect("errors must be array");
    let body_text = json["body"].as_str().unwrap_or("");

    // Either errors list is non-empty (strict-env complained) OR rendered body
    // contains an empty/placeholder — but NEVER "Anteile: 1" (= alter Dummy-Pfad).
    assert!(
        !body_text.contains("Anteile: 1"),
        "BUG c19: with no entries, preview must NOT fall back to share_count=1; got body={:?}, errors={:?}",
        body_text, errors
    );
    assert!(
        !errors.is_empty() || body_text.is_empty(),
        "with no entries and unguarded template, expected render error OR empty body; got body={:?}, errors={:?}",
        body_text, errors
    );
}
```

**Wenn `create_member` ohne exit_date / `sample_member`-Helper-Signaturen
abweichen:** Adapt an die bestehenden Helper im selben File (siehe Z. 11140+
Pattern). Es geht NUR um die Assertions — die Helper-Konstruktion ist Mittel
zum Zweck.

**Skip-Fallback (falls Setup-Helper nicht in diesem Modul vorhanden):** Wenn
`create_member_with_exit_date` nicht aufrufbar ist, prüfe mit `grep -n "fn
create_member_with_exit_date\|fn sample_member\|fn create_member" genossi_bin/tests/e2e_tests.rs`
und passe die Calls an die existierenden Signaturen an — KEINE neuen Helper
einführen.

**Wichtig:** Tests werden als ein einzelner `test`-Commit additiv hinzugefügt
(NICHT TDD-RED→GREEN, Fix ist in Task 1 bereits in-tree). Pattern-Anker:
STATE.md "Plan 08-10: 5 E2E-Regression-Tests werden als ein einzelner
`test`-Commit additiv hinzugefügt".
  </action>
  <verify>
    <automated>cargo test -p genossi_bin --test e2e_tests test_mail_preview_repayment 2>&1 | tail -25</automated>
  </verify>
  <done>
- Beide Tests laufen grün (`test_mail_preview_repayment_share_count_aggregates_real_value` + `test_mail_preview_repayment_no_entries_does_not_default_to_one`).
- Tests asserten explizit auf NICHT-"1" (Regression-Anker gegen genau dieses Bug-Symptom).
- Vollständiger `cargo test -p genossi_bin` läuft grün (keine Kollateral-Schäden in den anderen E2E-Tests).
- `cargo fmt -- --check` ist clean für die zwei modifizierten Files.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `/api/mail/preview` | Authenticated Vorstands-Request, übergibt member_id + repayment_phase_id als UUIDs |
| Preview-Handler → SQLite (read) | Read-only DAO-Lookups (RepaymentPhaseDao, RepaymentEntryDao), kein write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-c19-01 | Tampering | Preview-Handler share_count-Berechnung | mitigate | Aggregation 1:1 zur Worker-Logik (worker.rs:332-361) duplizieren — gleiche Filter (`deleted IS NULL` + `status IN Open/Contacted`), gleicher Summen-Code, gleiche Format-String-Konvention. Test 1 verifiziert, dass Preview-Werte mit DB-Wahrheit übereinstimmen. |
| T-c19-02 | Information Disclosure | repayment_phase_id ist client-controlled UUID | accept | Preview gibt nur aggregierte Zahlen für den (vom selben Request mitgelieferten) member_id zurück. Kein Cross-Member-Leak: filter auf `e.member_id == member_id` ist Pflicht. Vorhandene Vorstands-Auth via Middleware schützt den Endpoint. |
| T-c19-03 | Repudiation | Preview ist Read-only | accept | Kein Audit-Log nötig (CLAUDE.md: Audit nur für Member/MemberAction/MemberDocument/Application Write-Pfade). |
| T-c19-04 | Denial of Service | Aggregation lädt alle Entries einer Phase | accept | Identisches Risiko wie Worker (gleicher Code-Pfad, gleiche Last). Phase-Größe ist durch Mitgliederzahl der Genossenschaft begrenzt (< 10k); kein neuer Lastvektor. |
| T-c19-05 | Spoofing | UUID-Parse-Error → BadRequest | mitigate | `uuid::Uuid::parse_str(...).map_err(|_| MailServiceError::BadRequest(Arc::from("Invalid repayment_phase_id")))` — gleicher Pattern wie für `member_id` (rest.rs:486-487). Mappt auf HTTP 400 via existierendem `error_handler`. |

</threat_model>

<verification>
Nach Abschluss beider Tasks:

1. `cargo build` (Workspace) compiliert.
2. `cargo test -p genossi_bin --test e2e_tests` läuft komplett grün.
3. `cargo test -p genossi_mail` läuft grün (keine Template-Tests gebrochen — wir touchen template.rs NICHT).
4. `cargo fmt -- --check` clean.
5. `cargo clippy --all-targets -- -D warnings` clean für die zwei modifizierten Files (best-effort; pre-existing workspace warnings nicht in Scope).
6. Manueller Smoke-Test (optional, nicht blocking): Live-Server gegen Phase mit Test-Mitglied (3 Anteile) → Preview rendert "3" statt "1".

**Pattern-Konsistenz-Check (Worker ↔ Preview-Parity):**
```bash
# Beide Aufrufstellen verwenden identischen Filter
grep -nE "RepaymentEntryStatus::(Open|Contacted)" genossi_mail/src/worker.rs genossi_bin/src/lib.rs
# Beide nutzen identische Cents-zu-EUR-Formatierung
grep -nE 'format!\("\{\},\{:02\}"' genossi_mail/src/worker.rs genossi_bin/src/lib.rs
```
Beide grep-Gates müssen mindestens 1 Treffer in jedem File liefern.
</verification>

<success_criteria>
- [ ] `MailRestState`-Trait hat neue Methode `resolve_repayment_context`.
- [ ] `preview_mail`-Handler ruft `state.resolve_repayment_context(phase_id, member_id).await` auf, statt Dummies zu mergen.
- [ ] `RestStateImpl::resolve_repayment_context` aggregiert Open/Contacted-RepaymentEntries des Members, summiert `share_count_to_pay_out`, berechnet payout_amount im Format `X,YZ`, liefert `(payout, count, fiscal_year)`.
- [ ] D-05-Symmetrie zum Worker: `None` wenn keine relevanten Entries existieren — kein Fallback auf 0/1/Dummy.
- [ ] E2E-Test `test_mail_preview_repayment_share_count_aggregates_real_value` asserted `Anteile: 3` (NICHT `Anteile: 1`) im rendered_body.
- [ ] E2E-Test `test_mail_preview_repayment_no_entries_does_not_default_to_one` asserted NICHT-Fallback auf `Anteile: 1` bei fehlenden Entries.
- [ ] Worker-Send-Pfad bleibt unverändert (kein Refactor in worker.rs in dieser Session).
- [ ] Pure-Member-Preview-Pfad (ohne repayment_phase_id) ist unverändert.
- [ ] Doc-Comment an `PreviewRequest.repayment_phase_id` reflektiert das neue Verhalten (keine "Dummy"-Erwähnung mehr).
- [ ] `cargo test -p genossi_bin --test e2e_tests` läuft komplett grün.
</success_criteria>

<output>
After completion, create `.planning/quick/260602-c19-fix-mail-preview-repayment-kontext-share/260602-c19-01-SUMMARY.md`
documenting:
- Root cause (Preview-Handler hartkodierte Dummies)
- Fix-Strategie (Option A: Aggregation an Worker-Logik angleichen)
- Geänderte Dateien (rest.rs, lib.rs, e2e_tests.rs)
- Tech-Debt-Notiz: Worker und Preview duplizieren die Aggregations-Logik — ein
  späterer Refactor könnte einen gemeinsamen Helper (z.B. `template::compute_repayment_share_count(entries, member_id, phase) -> Option<(String, i32, i32)>`)
  extrahieren. Out-of-scope für Quick-c19, weil das Worker-Touch erfordert
  und damit den klar isolierten Bug-Fix-Scope verlässt.
</output>
