---
phase: quick-260614-9zf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/sqlite/20260614000000_mail_recipient_rendered_subject_body.sql
  - genossi_mail/src/dao.rs
  - genossi_mail/src/dao_sqlite.rs
  - genossi_mail/src/worker.rs
  - genossi_mail/src/rest.rs
  - genossi-frontend/src/api.rs
  - genossi-frontend/src/page/mail_page.rs
  - genossi-frontend/src/component/mail_recipient_rendered_content.rs
  - genossi-frontend/src/component/mod.rs
  - genossi-frontend/src/i18n/mod.rs
  - genossi-frontend/src/i18n/de.rs
  - genossi-frontend/src/i18n/en.rs
autonomous: true
requirements: [QUICK-260614-9zf]
must_haves:
  truths:
    - "Nach erfolgreichem Versand sind rendered_subject + rendered_body pro MailRecipient in SQLite persistiert"
    - "DAO create/update/find Roundtrip liefert die neuen Felder verlustfrei zurück"
    - "Die Recipient-REST-Antwort gibt rendered_subject + rendered_body aus (wenn vorhanden)"
    - "Im Frontend wird der gespeicherte gerenderte Betreff + Text in der Empfänger-Detailansicht angezeigt"
    - "Altdaten und nicht-gesendete/fehlgeschlagene Empfänger zeigen keine gerenderten Felder (NULL/None)"
  artifacts:
    - path: "migrations/sqlite/20260614000000_mail_recipient_rendered_subject_body.sql"
      provides: "Nullable Spalten rendered_subject + rendered_body auf mail_recipients"
      contains: "ALTER TABLE mail_recipients ADD COLUMN rendered_subject"
    - path: "genossi_mail/src/dao.rs"
      provides: "MailRecipient mit rendered_subject/rendered_body: Option<Arc<str>>"
      contains: "rendered_subject"
    - path: "genossi-frontend/src/component/mail_recipient_rendered_content.rs"
      provides: "Wiederverwendbare Anzeige-Komponente für gerenderten Betreff + Text (Component-First)"
  key_links:
    - from: "genossi_mail/src/worker.rs"
      to: "updated_recipient.rendered_subject / rendered_body"
      via: "Zuweisung vor recipient_dao.update()"
      pattern: "updated_recipient.rendered_(subject|body)"
    - from: "genossi_mail/src/dao_sqlite.rs"
      to: "mail_recipients UPDATE"
      via: "SET ... rendered_subject = ?, rendered_body = ?"
      pattern: "rendered_subject = \\?"
    - from: "genossi-frontend/src/page/mail_page.rs"
      to: "MailRecipientRenderedContent"
      via: "Komponente in Recipient-Detail-Zeile"
      pattern: "MailRecipientRenderedContent"
---

<objective>
Pro Empfänger den tatsächlich generierten (gerenderten) Betreff UND Text persistieren und bis ins Frontend anzeigen. Heute rendert der Worker `rendered_subject`/`rendered_body` pro Empfänger nur für den Versand und verwirft sie. Diese Quick-Task speichert sie am `MailRecipient` und zeigt sie an.

Purpose: Nachvollziehbarkeit — Vorstand kann sehen, was ein konkreter Empfänger tatsächlich erhalten hat (Audit-/Support-Wert), nicht nur das Template mit Platzhaltern.
Output: Migration + DAO-Persistenz + Worker-Schreiben + REST-Ausgabe + Frontend-Anzeige + Tests.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@./genossi-frontend/CLAUDE.md

@genossi_mail/src/dao.rs
@genossi_mail/src/dao_sqlite.rs
@genossi_mail/src/worker.rs
@genossi_mail/src/rest.rs
@genossi-frontend/src/page/mail_page.rs

<interfaces>
<!-- Aus der Codebase extrahiert — Executor nutzt diese direkt, keine Exploration nötig. -->

DAO-Entity (genossi_mail/src/dao.rs, ca. Z. 51-64) — aktuell:
```rust
pub struct MailRecipient {
    pub id: Uuid,
    pub created: time::PrimitiveDateTime,
    pub deleted: Option<time::PrimitiveDateTime>,
    pub version: Uuid,
    pub mail_job_id: Uuid,
    pub to_address: Arc<str>,
    pub member_id: Option<Uuid>,
    pub status: Arc<str>,
    pub error: Option<Arc<str>>,
    pub sent_at: Option<time::PrimitiveDateTime>,
    pub message_id: Option<Arc<str>>,
    // NEU: rendered_subject: Option<Arc<str>>, rendered_body: Option<Arc<str>>
}
```

DAO-Impl (genossi_mail/src/dao_sqlite.rs):
- Query-Stil ist RUNTIME (`sqlx::query` / `sqlx::query_as::<_, MailRecipientDb>`), NICHT die compile-checked Makros `query!`. => KEIN `cargo sqlx prepare` / DATABASE_URL nötig.
- `MailRecipientDb` FromRow-Struct (Z. 203-216): Felder als `Option<String>` ergänzen.
- `impl TryFrom<&MailRecipientDb> for MailRecipient` (Z. 218-237): mappen via `db.rendered_subject.as_deref().map(Arc::from)`.
- INSERT in `create` (Z. 258-273): aktuell setzt rendered_* NICHT — Spalten als NULL einfügen (frische Recipients haben noch keinen Render).
- SELECT-Spaltenliste in `find_by_job_id` (Z. 281) und `next_pending` (Z. 297, mit `r.`-Präfix) erweitern.
- UPDATE in `update` (Z. 323-331): aktuell `SET status = ?, error = ?, sent_at = ?, message_id = ?, version = ?` — `rendered_subject = ?, rendered_body = ?` ergänzen + binden.
- NICHT anfassen: `find_sent_member_ids_by_job_id` (selektiert nur member_id) und die Communication-UNION-Query (selektiert `j.subject`, nicht recipient-rendered).
- In-Memory-Test-Schema `setup_db()` (Z. 1238-1255, `CREATE TABLE mail_recipients`): neue Spalten `rendered_subject TEXT`, `rendered_body TEXT` ergänzen, sonst brechen DAO-Tests.
- `sample_recipient()` (Z. 1364-1378): neue Felder mit `None` initialisieren.

Worker (genossi_mail/src/worker.rs):
- `rendered_subject`/`rendered_body` sind `String` (Z. 545 für Template-Fall `(subject, body)`, Z. 576 für Passthrough `(job.subject.to_string(), job.body.to_string())`), gebunden in `let (rendered_subject, rendered_body) = ...` (Z. 379 / else Z. 574-577).
- `updated_recipient` wird Z. 615 als Clone von `next` erzeugt, danach Status/sent_at/message_id/error gesetzt (Z. 627-647), dann `recipient_dao.update(&updated_recipient)` Z. 650.
- Genau VOR dem `recipient_dao.update`-Aufruf (oder direkt nach Z. 616) beide gerenderten Werte zuweisen:
  `updated_recipient.rendered_subject = Some(Arc::from(rendered_subject.as_str()));`
  `updated_recipient.rendered_body = Some(Arc::from(rendered_body.as_str()));`
  Das gilt für BEIDE Pfade (Ok=sent und Err=failed) — `rendered_subject`/`rendered_body` existieren in beiden Fällen, da der Render vor dem Send passiert ist. Bei Render-/Member-Fehlern wird stattdessen `mark_recipient_failed` (eigene Funktion, ab Z. 159) per `continue` aufgerufen, bevor gerendert wurde — dort bleiben rendered_* unverändert None (korrekt).

REST (genossi_mail/src/rest.rs):
- `MailRecipientTO` (Z. 105-120): zwei Felder ergänzen:
  `#[serde(skip_serializing_if = "Option::is_none")] pub rendered_subject: Option<String>,`
  `#[serde(skip_serializing_if = "Option::is_none")] pub rendered_body: Option<String>,`
- `impl From<&MailRecipient> for MailRecipientTO` (Z. 279-291): `rendered_subject: r.rendered_subject.as_deref().map(String::from), rendered_body: ...`.

Frontend API-Modell (genossi-frontend/src/api.rs, `MailRecipientTO` ca. Z. 840-847):
```rust
pub struct MailRecipientTO {
    pub id: String,
    pub to_address: String,
    pub member_id: Option<String>, // (prüfen ob vorhanden)
    pub status: String,
    pub error: Option<String>,
    pub sent_at: Option<String>,
    // NEU: pub rendered_subject: Option<String>, pub rendered_body: Option<String>,
}
```
=> Beide als `#[serde(default)] Option<String>` ergänzen (default, damit alte/None-Antworten ohne die Felder weiterhin deserialisieren).

Frontend-Anzeige (genossi-frontend/src/page/mail_page.rs):
- Recipient-Tabellen an ZWEI Stellen: ausklappbare Job-Liste (ca. Z. 699-754) und `MailJobDetail`-Seite (ca. Z. 862-922).
- i18n-Keys: bestehende `Key::MailSubject` ("Betreff"), `Key::MailBody` ("Nachricht") in mod.rs Z. 237-238 / de.rs Z. 182-183 vorhanden — für die Labels wiederverwendbar. NEU benötigt: `Key::MailRenderedContent` (Überschrift, z.B. "Gesendeter Inhalt" / "Sent content") in mod.rs + de.rs + en.rs.

Component-First: genossi-frontend/CLAUDE.md verlangt wiederverwendbare Komponenten. Die Anzeige erscheint an zwei Stellen => EIGENE Komponente `MailRecipientRenderedContent` in `genossi-frontend/src/component/`, registriert in `component/mod.rs`. Vorbild für Struktur/Stil: bestehende Komponente `mail_recipient_status_badge.rs`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend-Persistenz — Migration, DAO-Entity, DAO-Impl, Worker + DAO-Roundtrip-Tests</name>
  <files>migrations/sqlite/20260614000000_mail_recipient_rendered_subject_body.sql, genossi_mail/src/dao.rs, genossi_mail/src/dao_sqlite.rs, genossi_mail/src/worker.rs</files>
  <behavior>
    - DAO-Roundtrip create: frisch erzeugter Recipient (rendered_* = None) → find_by_job_id liefert rendered_subject == None, rendered_body == None.
    - DAO-Roundtrip update: Recipient mit rendered_subject = Some("Hallo Max"), rendered_body = Some("Text für Max") → update → find_by_job_id liefert exakt diese Werte zurück.
    - DAO-Roundtrip update behält bestehende Felder (status/sent_at/message_id) korrekt bei, während rendered_* gesetzt werden (Erweiterung von test_recipient_update bzw. neuer Test).
    - next_pending mappt die neuen Spalten ebenfalls (keine Panic / korrektes None für pending Recipient).
  </behavior>
  <action>
1. **Migration** `migrations/sqlite/20260614000000_mail_recipient_rendered_subject_body.sql` (fortlaufende Nummerierung nach 20260608000000): zwei `ALTER TABLE mail_recipients ADD COLUMN rendered_subject TEXT;` und `... ADD COLUMN rendered_body TEXT;`. Beide nullable (kein NOT NULL, kein DEFAULT) — Begründung als Kommentar: Altdaten und noch-nicht-gesendete/fehlgeschlagene Empfänger haben keinen gerenderten Text. Kein Down-Migration (SQLite < 3.35 kann Spalten nicht entfernen) — Konvention wie in 20260603100000_mail_job_attach_repayment_letter.sql.

2. **DAO-Entity** (`genossi_mail/src/dao.rs`): `MailRecipient` um `pub rendered_subject: Option<Arc<str>>` und `pub rendered_body: Option<Arc<str>>` ergänzen.

3. **DAO-Impl** (`genossi_mail/src/dao_sqlite.rs`):
   - `MailRecipientDb`: `rendered_subject: Option<String>`, `rendered_body: Option<String>`.
   - `TryFrom<&MailRecipientDb> for MailRecipient`: `rendered_subject: db.rendered_subject.as_deref().map(Arc::from)`, analog body.
   - `create` INSERT: Spalten + Werte ergänzen; frische Recipients → NULL einfügen (`Option::<String>::None`).
   - `find_by_job_id` SELECT-Spaltenliste: `rendered_subject, rendered_body` ergänzen.
   - `next_pending` SELECT: `r.rendered_subject, r.rendered_body` ergänzen.
   - `update` UPDATE: `SET ... rendered_subject = ?, rendered_body = ?, version = ? ...` und Werte binden via `recipient.rendered_subject.as_deref()`.
   - `setup_db()` Test-Schema (`CREATE TABLE mail_recipients`): `rendered_subject TEXT, rendered_body TEXT` ergänzen.
   - `sample_recipient()`: neue Felder mit `None` initialisieren.
   - Query-Stil ist runtime (`sqlx::query`/`query_as`), KEIN `cargo sqlx prepare` nötig.

4. **Worker** (`genossi_mail/src/worker.rs`): Direkt nach `let mut updated_recipient = next.clone();` (Z. 615) bzw. spätestens vor `recipient_dao.update(&updated_recipient)` (Z. 650) beide gerenderten Werte zuweisen:
   `updated_recipient.rendered_subject = Some(Arc::from(rendered_subject.as_str()));`
   `updated_recipient.rendered_body = Some(Arc::from(rendered_body.as_str()));`
   Gilt für Erfolgs- UND Fehlversand-Pfad (beide nach dem Render erreicht). Render-/Member-Fehler nutzen `mark_recipient_failed` + `continue` vor dem Render → dort bleiben rendered_* None (kein Code-Change nötig). Sicherstellen, dass `Arc` importiert ist (ist es bereits via `std::sync::Arc` im Worker — sonst ergänzen).

5. **Tests** (PFLICHT, im bestehenden `#[cfg(test)] mod tests` in dao_sqlite.rs):
   - Bestehenden `test_recipient_create_and_find_by_job_id` NICHT brechen (rendered_* erwartet None).
   - NEUER Test `test_recipient_update_persists_rendered_subject_body`: create → update mit rendered_subject/rendered_body Some(...) → find_by_job_id → assert exakte Werte; zusätzlich assert dass ein frischer (nicht upgedateter) Recipient None liefert.
   - Sicherstellen dass `cargo test -p genossi_mail` grün ist (alle bestehenden Mail-Tests inkl. attach_repayment_letter-Roundtrips).
  </action>
  <verify>
    <automated>cargo test -p genossi_mail dao_sqlite 2>&1 | tail -20 && cargo build -p genossi_mail 2>&1 | tail -5</automated>
  </verify>
  <done>Migration angelegt; MailRecipient hat rendered_subject/rendered_body; DAO create/find/update + next_pending mappen sie; Worker schreibt sie vor update; neuer Roundtrip-Test grün; alle bestehenden genossi_mail-Tests grün.</done>
</task>

<task type="auto">
  <name>Task 2: REST-Ausgabe — MailRecipientTO um rendered_subject/rendered_body erweitern</name>
  <files>genossi_mail/src/rest.rs</files>
  <action>
1. `MailRecipientTO` (genossi_mail/src/rest.rs, ca. Z. 105-120): zwei Felder ergänzen:
   `#[serde(skip_serializing_if = "Option::is_none")] pub rendered_subject: Option<String>,`
   `#[serde(skip_serializing_if = "Option::is_none")] pub rendered_body: Option<String>,`
2. `impl From<&MailRecipient> for MailRecipientTO` (ca. Z. 279-291): `rendered_subject: r.rendered_subject.as_deref().map(String::from),` und analog `rendered_body`.
3. Falls ein Test/Beispiel `MailRecipientTO { ... }` direkt konstruiert, neue Felder ergänzen (Build-Check zeigt es).
4. Keine OpenAPI-Schema-Anpassung über die Felder hinaus nötig (utoipa leitet aus ToSchema ab).
  </action>
  <verify>
    <automated>cargo build -p genossi_mail 2>&1 | tail -5 && cargo test -p genossi_mail rest 2>&1 | tail -15</automated>
  </verify>
  <done>MailRecipientTO trägt rendered_subject/rendered_body; From-Impl mappt sie; genossi_mail kompiliert + REST-Tests grün.</done>
</task>

<task type="auto">
  <name>Task 3: Frontend-Anzeige — API-Modell, wiederverwendbare Komponente, Einbindung, i18n</name>
  <files>genossi-frontend/src/api.rs, genossi-frontend/src/component/mail_recipient_rendered_content.rs, genossi-frontend/src/component/mod.rs, genossi-frontend/src/page/mail_page.rs, genossi-frontend/src/i18n/mod.rs, genossi-frontend/src/i18n/de.rs, genossi-frontend/src/i18n/en.rs</files>
  <action>
1. **API-Modell** (genossi-frontend/src/api.rs, `MailRecipientTO` ca. Z. 840-847): `#[serde(default)] pub rendered_subject: Option<String>,` und `#[serde(default)] pub rendered_body: Option<String>,` ergänzen. `default` ist wichtig, damit Antworten ohne die Felder (None/Altdaten) weiterhin deserialisieren. Falls ein Test ein `MailRecipientTO { ... }` direkt baut, Felder ergänzen.

2. **i18n** (Component-First-Vorbedingung): neuen Key `MailRenderedContent` in `genossi-frontend/src/i18n/mod.rs` (enum `Key`) ergänzen; Übersetzungen in de.rs ("Gesendeter Inhalt") und en.rs ("Sent content"). Bestehende `Key::MailSubject` / `Key::MailBody` für die Feld-Labels wiederverwenden (keine neuen Keys dafür).

3. **Komponente** (Component-First, PFLICHT laut genossi-frontend/CLAUDE.md): `genossi-frontend/src/component/mail_recipient_rendered_content.rs` neu anlegen — `#[component] pub fn MailRecipientRenderedContent(rendered_subject: Option<String>, rendered_body: Option<String>) -> Element`. Verhalten: wenn beide None → nichts rendern (return `rsx!{}`); sonst kompakter Block mit Überschrift `Key::MailRenderedContent`, darunter Betreff (Label `Key::MailSubject` + Wert) und Text (Label `Key::MailBody` + Wert, Zeilenumbrüche via `whitespace-pre-wrap`). Stil/Struktur an `genossi-frontend/src/component/mail_recipient_status_badge.rs` orientieren (use_i18n, Tailwind-Klassen konsistent mit den Tabellen). Komponente in `genossi-frontend/src/component/mod.rs` registrieren (`mod ...; pub use ...::MailRecipientRenderedContent;` analog zu vorhandenen Einträgen).

4. **Einbindung** (genossi-frontend/src/page/mail_page.rs): An den ZWEI Recipient-Tabellen den gerenderten Inhalt anzeigen, ohne die bestehende NoRepaymentLetterAction-Spalte zu brechen. Sauberster Weg: pro Recipient eine zusätzliche `tr` (Detail-Zeile) unter der Hauptzeile mit einer über alle Spalten gehenden `td { colspan: 4, MailRecipientRenderedContent { rendered_subject: r.rendered_subject.clone(), rendered_body: r.rendered_body.clone() } }`. Da die Komponente bei None/None nichts rendert, bleiben Zeilen für nicht-gesendete Empfänger leer/unauffällig. Beide Stellen umsetzen (ausklappbare Job-Liste ~Z. 699-754 und `MailJobDetail` ~Z. 862-922). Component-First strikt: KEIN inline-RSX-Duplikat des Inhalts — beide Stellen nutzen `MailRecipientRenderedContent`.

5. Build/Check: `cargo check -p genossi-frontend` (bzw. `cargo check` im Frontend-Crate) muss grün sein; bestehende Frontend-Tests nicht brechen.
  </action>
  <verify>
    <automated>cargo check -p genossi-frontend 2>&1 | tail -15</automated>
  </verify>
  <done>api::MailRecipientTO trägt rendered_subject/rendered_body (serde default); neue wiederverwendbare Komponente MailRecipientRenderedContent existiert + in mod.rs registriert; beide Recipient-Tabellen binden sie ein (kein Inline-Duplikat); i18n-Key in allen drei Dateien; Frontend kompiliert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker → DB | Worker schreibt gerenderten Text in mail_recipients (interner Trust, keine externe Eingabe an dieser Stelle) |
| REST → Frontend | Recipient-Detail-API gibt gerenderten Text aus; nur authentifizierte Vorstands-Clients (bestehender Auth-Layer) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9zf-01 | Information Disclosure | rendered_body enthält ggf. personenbezogene Daten (Name etc. aus Template-Render) in der Recipient-API | accept | Endpoint liegt hinter dem bestehenden Auth-Middleware der Mail-Routen (gleiche Schutzklasse wie subject/to_address/error, die bereits ausgegeben werden); keine neue Exposition gegenüber Helfer-/QR-Pfad (der greift nicht auf mail_recipients zu) |
| T-9zf-02 | Tampering | Frontend rendert rendered_body als Text | mitigate | Dioxus RSX escapt Text-Interpolation `{...}` standardmäßig (kein dangerous_inner_html); whitespace-pre-wrap nur CSS, kein HTML-Injection-Pfad |
</threat_model>

<verification>
- `cargo test -p genossi_mail` grün (DAO-Roundtrip neu + alle bestehenden Mail-Tests).
- `cargo build -p genossi_mail` grün (DAO/Worker/REST).
- `cargo check -p genossi-frontend` grün.
- Migration-Datei folgt Konvention (Zeitstempel-Präfix, ALTER TABLE, nullable, kein down).
- Manuell-optional: Server starten, Mail-Job an Empfänger mit member_id senden, Recipient-Detail im Frontend öffnet → gerenderter Betreff/Text sichtbar; nicht-gesendeter Empfänger zeigt nichts.
</verification>

<success_criteria>
- rendered_subject + rendered_body werden pro MailRecipient nach Versand persistiert (sent UND failed Pfad).
- DAO create/update/find Roundtrip verlustfrei; neuer Test grün; bestehende Tests grün.
- Recipient-REST-Antwort gibt die Felder aus (skip wenn None).
- Frontend zeigt den gespeicherten gerenderten Betreff + Text über eine wiederverwendbare Komponente (Component-First), an beiden Recipient-Tabellen.
- Altdaten/None brechen weder Deserialisierung noch UI.
</success_criteria>

<output>
After completion, create `.planning/quick/260614-9zf-mail-versand-gerenderten-betreff-und-tex/260614-9zf-SUMMARY.md`
</output>
