---
phase: 260603-m48-frontend-template-editor-repayment-varia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - genossi-frontend/src/page/mail_templates.rs
autonomous: true
requirements:
  - QUICK-M48-01
must_haves:
  truths:
    - "Template-Editor zeigt die Repayment-Variablen-Buttons (payout_amount, share_count, fiscal_year) sichtbar an"
    - "Mail-Sendeseite (mail_page.rs) zeigt die Buttons weiterhin nur kontextabhängig (wenn repayment_phase_id gesetzt ist)"
    - "Inbox-Reply-Formular (reply_form.rs) zeigt die Buttons weiterhin NICHT"
    - "Das Frontend kompiliert ohne Fehler"
  artifacts:
    - path: "genossi-frontend/src/page/mail_templates.rs"
      provides: "TemplateVarButtons-Aufruf mit show_repayment_vars: true"
      contains: "show_repayment_vars: true"
  key_links:
    - from: "genossi-frontend/src/page/mail_templates.rs"
      to: "genossi-frontend/src/component/mail_compose/template_var_buttons.rs"
      via: "TemplateVarButtons prop show_repayment_vars: true"
      pattern: "show_repayment_vars:\\s*true"
---

<objective>
Im Template-Editor (`mail_templates.rs`) die `TemplateVarButtons`-Komponente so aufrufen, dass die Repayment-Variablen-Buttons (payout_amount, share_count, fiscal_year) sichtbar sind.

Purpose: Autoren, die Repayment-Templates schreiben, brauchen die Variablen-Buttons unabhängig vom Laufzeit-Kontext. Backend-seitig sind die Variablen via `{% if X is defined %}` abgesichert (Phase 10 D-05), so dass nicht aufgelöste Variablen in Nicht-Repayment-Kontexten kein Problem darstellen.

Output: Eine einzige geänderte Zeile in `mail_templates.rs`, die `show_repayment_vars: true` an `TemplateVarButtons` übergibt.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@genossi-frontend/CLAUDE.md
@genossi-frontend/src/page/mail_templates.rs
@genossi-frontend/src/component/mail_compose/template_var_buttons.rs

<interfaces>
<!-- Component API – already implemented, ready to consume. -->

From genossi-frontend/src/component/mail_compose/template_var_buttons.rs:
```rust
pub fn TemplateVarButtons(
    on_insert: EventHandler<String>,
    #[props(default)] show_repayment_vars: bool,
) -> Element
```

The component already defines REPAYMENT_VARS = [payout_amount, share_count, fiscal_year]
and conditionally renders them based on `show_repayment_vars`. No component changes needed.
</interfaces>

<current_call_site>
genossi-frontend/src/page/mail_templates.rs:237 currently:
```rust
TemplateVarButtons {
    on_insert: move |var_text: String| {
        edit_body.write().push_str(&var_text);
    },
}
```

Target:
```rust
TemplateVarButtons {
    on_insert: move |var_text: String| {
        edit_body.write().push_str(&var_text);
    },
    show_repayment_vars: true,
}
```
</current_call_site>

<do_not_modify>
- genossi-frontend/src/page/mail_page.rs:421 — sets `show_repayment_vars: repayment_phase_id.read().is_some()` context-aware. Leave unchanged.
- genossi-frontend/src/component/inbox/reply_form.rs:56 — does not pass the prop (defaults to false). Leave unchanged.
- genossi-frontend/src/component/mail_compose/template_var_buttons.rs — API ready, no changes needed.
</do_not_modify>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TemplateVarButtons im Editor mit show_repayment_vars: true aufrufen</name>
  <files>genossi-frontend/src/page/mail_templates.rs</files>
  <action>
Edit `genossi-frontend/src/page/mail_templates.rs` at the `TemplateVarButtons { ... }` invocation (around line 237, inside the edit form section right above the body textarea).

Add the prop `show_repayment_vars: true,` after the existing `on_insert` closure so the call becomes:

```rust
TemplateVarButtons {
    on_insert: move |var_text: String| {
        edit_body.write().push_str(&var_text);
    },
    show_repayment_vars: true,
}
```

There is exactly ONE call site to `TemplateVarButtons` in this file — modify only that one. Do NOT touch `mail_page.rs` or `reply_form.rs`. Do NOT change the component itself. Do NOT add new components — the existing component already supports this prop (Component-First Principle: just toggle existing prop).

Rationale: In the template editor, the author writes templates that may be intended for repayment campaigns. The buttons must be visible regardless of any runtime context. Variables that don't resolve in non-repayment send contexts are guarded backend-side via Jinja `{% if X is defined %}` blocks (per Phase 10 D-05).
  </action>
  <verify>
    <automated>cargo check -p genossi-frontend 2>&amp;1 | tail -20 &amp;&amp; grep -n "show_repayment_vars: true" genossi-frontend/src/page/mail_templates.rs &amp;&amp; test "$(grep -c 'show_repayment_vars' genossi-frontend/src/page/mail_templates.rs)" = "1" &amp;&amp; ! grep -q "show_repayment_vars" genossi-frontend/src/component/inbox/reply_form.rs &amp;&amp; grep -q "show_repayment_vars: repayment_phase_id" genossi-frontend/src/page/mail_page.rs</automated>
  </verify>
  <done>
- `cargo check -p genossi-frontend` succeeds without errors
- `genossi-frontend/src/page/mail_templates.rs` contains exactly one occurrence of `show_repayment_vars: true` at the `TemplateVarButtons` call site
- `genossi-frontend/src/page/mail_page.rs` still uses the context-aware expression `show_repayment_vars: repayment_phase_id...` (unchanged)
- `genossi-frontend/src/component/inbox/reply_form.rs` still does not pass `show_repayment_vars` (unchanged)
- `genossi-frontend/src/component/mail_compose/template_var_buttons.rs` unchanged
  </done>
</task>

</tasks>

<verification>
1. `cargo check -p genossi-frontend` exits 0
2. `grep -n "show_repayment_vars: true" genossi-frontend/src/page/mail_templates.rs` returns exactly one line near the edit form's TemplateVarButtons block
3. `git diff --stat` (or `jj diff --stat`) shows only `genossi-frontend/src/page/mail_templates.rs` modified — no other files touched
4. The component `genossi-frontend/src/component/mail_compose/template_var_buttons.rs` is byte-identical (no diff)
5. `genossi-frontend/src/page/mail_page.rs` and `genossi-frontend/src/component/inbox/reply_form.rs` are byte-identical (no diff)
</verification>

<success_criteria>
- One single, minimal change in `mail_templates.rs` that adds `show_repayment_vars: true,` to the `TemplateVarButtons` call
- Frontend compiles cleanly
- Editor now exposes payout_amount / share_count / fiscal_year buttons to template authors
- Send-page context-awareness and inbox-reply behaviour remain untouched
</success_criteria>

<output>
After completion, create `.planning/quick/260603-m48-frontend-template-editor-repayment-varia/260603-m48-SUMMARY.md` documenting:
- The single line added
- Confirmation that mail_page.rs and reply_form.rs were not touched
- `cargo check -p genossi-frontend` result
</output>
