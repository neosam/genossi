---
phase: 260603-m48-frontend-template-editor-repayment-varia
plan: 01
status: complete
one_liner: "Template-Editor (mail_templates.rs) zeigt jetzt die Repayment-Variablen-Buttons (payout_amount/share_count/fiscal_year) via show_repayment_vars: true an"
subsystem: frontend
tags: [frontend, mail-template, repayment, component-first, ui]
requirements:
  - QUICK-M48-01
dependency_graph:
  requires:
    - genossi-frontend/src/component/mail_compose/template_var_buttons.rs (TemplateVarButtons component with show_repayment_vars prop, already deployed)
  provides:
    - Repayment-Variablen sichtbar im Template-Editor unabhängig vom Laufzeit-Kontext
  affects:
    - genossi-frontend/src/page/mail_templates.rs (call site)
tech_stack:
  added: []
  patterns:
    - Component-First: Bestehender Prop wurde nur am Call-Site umgeschaltet, keine Komponente dupliziert
key_files:
  created: []
  modified:
    - genossi-frontend/src/page/mail_templates.rs
decisions:
  - "Editor-Modus zeigt Repayment-Vars immer (show_repayment_vars: true), während Mail-Sendeseite weiterhin kontextabhängig (repayment_phase_id.is_some()) bleibt — Editor-Autor:innen müssen die Variablen sehen, ohne dass eine Phase aktiv ist."
metrics:
  duration_min: 3
  completed: 2026-06-03
commits:
  - 0a58da9
---

# Quick Task 260603-m48: Template-Editor zeigt Repayment-Variablen

## Objective

Im Template-Editor (`mail_templates.rs`) die `TemplateVarButtons`-Komponente so aufrufen, dass die Repayment-Variablen-Buttons (`payout_amount`, `share_count`, `fiscal_year`) sichtbar sind, unabhängig vom Laufzeit-Kontext.

## Change

Eine einzige Zeile in `genossi-frontend/src/page/mail_templates.rs` hinzugefügt — der bestehende `show_repayment_vars: bool` Prop der `TemplateVarButtons`-Komponente wird am Editor-Call-Site auf `true` gesetzt.

### Diff

```diff
diff --git a/genossi-frontend/src/page/mail_templates.rs b/genossi-frontend/src/page/mail_templates.rs
index b008c73..d766398 100644
--- a/genossi-frontend/src/page/mail_templates.rs
+++ b/genossi-frontend/src/page/mail_templates.rs
@@ -238,6 +238,7 @@ pub fn MailTemplatesPage() -> Element {
                                             on_insert: move |var_text: String| {
                                                 edit_body.write().push_str(&var_text);
                                             },
+                                            show_repayment_vars: true,
                                         }

                                         // Body
```

Die Komponente unterstützt diesen Prop bereits seit Phase 10; es war keine Komponenten-Änderung nötig (Component-First-Prinzip eingehalten).

## Verification

### `cargo check -p genossi-frontend`

Erfolgreich (nur `dead_code`-Warnungen, keine Errors). Letzte 5 Zeilen der Ausgabe:

```
    |
    = note: `Key` has derived impls for the traits `Debug` and `Clone`, but these are intentionally ignored during dead code analysis

warning: `genossi-frontend` (bin "genossi-frontend") generated 27 warnings (run `cargo fix --bin "genossi-frontend"` to apply 16 suggestions)
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.15s
```

### Grep-Gates

- `grep -n "show_repayment_vars: true" genossi-frontend/src/page/mail_templates.rs` → `241:                                            show_repayment_vars: true,`
- `grep -c "show_repayment_vars" genossi-frontend/src/page/mail_templates.rs` → `1` (exakt eine Occurrence)
- `grep -n "show_repayment_vars: repayment_phase_id" genossi-frontend/src/page/mail_page.rs` → `421:                                show_repayment_vars: repayment_phase_id.read().is_some(),` (kontext-aware, unverändert)
- `grep -n "show_repayment_vars" genossi-frontend/src/component/inbox/reply_form.rs` → keine Match (exit 1, korrekt)

### Forbidden files unverändert

`git diff --name-only HEAD~1 HEAD -- genossi-frontend/src/page/mail_page.rs genossi-frontend/src/component/inbox/reply_form.rs genossi-frontend/src/component/mail_compose/template_var_buttons.rs` liefert leere Ausgabe (exit 0) — alle drei "do-not-modify"-Dateien sind byte-identisch zu HEAD~1.

### Commit-Statistik

```
 genossi-frontend/src/page/mail_templates.rs | 1 +
 1 file changed, 1 insertion(+)
```

Genau eine Datei im Commit `0a58da9` modifiziert; nur eine Insertion, keine Deletions.

## Deviations from Plan

Keine — Plan wurde exakt wie geschrieben ausgeführt. Die Verification-Adjustment des Orchestrators (separates Targeted-Diff für die drei forbidden files, statt allgemeines `git diff --stat`) war wegen der pre-existierenden unrelated Modifications im Working Tree nötig, aber konzeptuell identisch zur Plan-Verification.

## Commits

- `0a58da9` — `feat(quick-260603-m48): show repayment vars in template editor`

## Self-Check: PASSED

- FOUND: `genossi-frontend/src/page/mail_templates.rs` (modifiziert, eine neue Zeile mit `show_repayment_vars: true,` an Position 241)
- FOUND: Commit `0a58da9` in `git log --oneline -5`
- FOUND: `genossi-frontend/src/page/mail_page.rs` unverändert (Zeile 421 weiterhin context-aware)
- FOUND: `genossi-frontend/src/component/inbox/reply_form.rs` unverändert (kein `show_repayment_vars`)
- FOUND: `genossi-frontend/src/component/mail_compose/template_var_buttons.rs` unverändert
