---
phase: 24-wysiwyg-frontend-editor
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - genossi-frontend/src/js.rs
  - genossi-frontend/src/component/mail_compose/wysiwyg_editor.rs
  - genossi-frontend/src/component/mail_compose/wysiwyg_toolbar.rs
  - genossi-frontend/src/component/mail_compose/wysiwyg_link_dialog.rs
  - genossi-frontend/src/component/mail_compose/mod.rs
autonomous: true
requirements: [EDIT-01, EDIT-02, EDIT-03, EDIT-04]
must_haves:
  truths:
    - "A WysiwygEditor Dioxus component exists in genossi-frontend/src/component/mail_compose/wysiwyg_editor.rs and exposes `on_change: EventHandler<(String, String)>` where the tuple is (plain: innerText, html: innerHTML)"
    - "On mount, the editor calls document.execCommand(styleWithCSS, false, false) exactly once so bold/italic produce semantic <b>/<i> not span[style] (per D-05 + RESEARCH Pitfall 1)"
    - "Toolbar buttons emit only ammonia-safe tags: b, i, u, s, ol/ul/li, h1/h2/h3, p, blockquote, a[href], and unlink"
    - "Every button element in editor/toolbar/dialog uses r#type: button + prevent_default in onclick (Dioxus button-reload-bug workaround)"
    - "onpaste handler calls preventDefault(), reads ClipboardData::get_data(text/plain), and inserts via execCommand(insertText, …) so only plain text lands in the DOM"
    - "Toolbar-button click triggers execCommand AND immediately syncs innerHTML+innerText back to on_change (fixes DOM-sync-race Pitfall 5)"
    - "Link dialog uses the shared Modal component (component/modal.rs) — no window.prompt() anywhere in the new code"
  artifacts:
    - "genossi-frontend/src/component/mail_compose/wysiwyg_editor.rs (new — hosts contenteditable div + Toolbar + LinkDialog wiring)"
    - "genossi-frontend/src/component/mail_compose/wysiwyg_toolbar.rs (new — button row with execCommand dispatches)"
    - "genossi-frontend/src/component/mail_compose/wysiwyg_link_dialog.rs (new — URL/text form inside Modal)"
    - "genossi-frontend/src/js.rs: exec_command_bool, exec_command_str, exec_command_simple helpers"
    - "genossi-frontend/src/component/mail_compose/mod.rs: pub mod wysiwyg_editor + pub use WysiwygEditor"
  key_links:
    - "wysiwyg_toolbar.rs → js::exec_command_* (execCommand fassade)"
    - "wysiwyg_link_dialog.rs → component::modal::Modal (in-app dialog per D-06)"
    - "wysiwyg_editor.rs → i18n::Key::MailEditor* (all 19 keys from Plan 24-01)"
---

<objective>
Build the new reusable WysiwygEditor Dioxus component set — the single shared contenteditable+toolbar+link-dialog trio that Plan 24-03 will drop into all three MailBodyEditor call sites. No callers migrate in this plan; the goal is a compilable, correctly-wired component with all Dioxus / web-sys / execCommand plumbing in place so the migration in Plan 24-03 is purely rewiring.

Purpose: Isolating the new component in its own plan keeps the context budget clean (heavy web-sys / js-sys / Dioxus event-loop work in one place) and lets the migration in Plan 24-03 stay a pure edit-3-sites-mechanically operation.

Output: Three new files under genossi-frontend/src/component/mail_compose/ (wysiwyg_editor.rs, wysiwyg_toolbar.rs, wysiwyg_link_dialog.rs), three new exec_command_* helpers in js.rs, mod.rs re-exporting `WysiwygEditor`. The component compiles under `cargo check -p genossi-frontend` and has unit tests for pure helpers (URL validation).
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-wysiwyg-frontend-editor/24-CONTEXT.md
@.planning/phases/24-wysiwyg-frontend-editor/24-RESEARCH.md
@.planning/phases/24-wysiwyg-frontend-editor/24-01-PLAN.md
@genossi-frontend/CLAUDE.md
@genossi-frontend/src/js.rs
@genossi-frontend/src/component/modal.rs
@genossi-frontend/src/component/mail_compose/body_editor.rs
@genossi-frontend/src/component/mail_compose/mod.rs
@genossi-frontend/src/component/mail_compose/template_tester.rs
@genossi-frontend/src/page/templates.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add exec_command_* helpers to js.rs (execCommand facade)</name>
  <files>genossi-frontend/src/js.rs</files>
  <read_first>
    - genossi-frontend/src/js.rs lines 100-155 (existing `copy_with_exec_command` — canonical js_sys::Reflect + web_sys::Document pattern; new helpers mirror this shape)
    - .planning/phases/24-wysiwyg-frontend-editor/24-RESEARCH.md §"Pattern 2: execCommand via js_sys::Reflect (no bundle needed)" — exact three helper signatures to build
  </read_first>
  <action>
    Append three new `pub fn` helpers to genossi-frontend/src/js.rs after `copy_with_exec_command` (currently ends at line 155): `pub fn exec_command_bool(doc: &web_sys::Document, cmd: &str, arg: bool) -> Result<bool, wasm_bindgen::JsValue>`, `pub fn exec_command_str(doc: &web_sys::Document, cmd: &str, arg: &str) -> Result<bool, wasm_bindgen::JsValue>`, and `pub fn exec_command_simple(doc: &web_sys::Document, cmd: &str) -> Result<bool, wasm_bindgen::JsValue>`. All three obtain the `execCommand` function via `js_sys::Reflect::get(doc, &JsValue::from_str("execCommand"))?.dyn_ref::<js_sys::Function>()`, call it with `call3` (bool/str variants pass `showUI=false` as arg 2 and the actual arg as arg 3) or `call1` (simple variant) and return `Ok(returned.as_bool().unwrap_or(false))`. Add a top-of-file doc comment on the trio explaining they are the contenteditable execCommand facade for the WYSIWYG editor and cite Pitfall 1 of 24-RESEARCH.md (`styleWithCSS=false` MUST be called once at mount). Keep each helper `#[allow(dead_code)]` if the compiler complains about unused variants — the callers land in Task 2 within the same commit boundary. Do NOT add a new `extern "C"` bundle binding; the direct Reflect path (analog to copy_with_exec_command) is preferred per EDIT-02 (no new JS bundle). Do NOT touch the existing `create_typst_editor` extern block.
  </action>
  <verify>
    <automated>grep -c 'pub fn exec_command_' genossi-frontend/src/js.rs; cargo check -p genossi-frontend 2>&1 | tail -10</automated>
  </verify>
  <done>
    All three helpers exist (grep count = 3) and `cargo check -p genossi-frontend` passes without warnings other than possibly `dead_code` on the not-yet-called helpers.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create wysiwyg_link_dialog.rs (in-app link URL modal, per D-06)</name>
  <files>genossi-frontend/src/component/mail_compose/wysiwyg_link_dialog.rs</files>
  <read_first>
    - genossi-frontend/src/component/modal.rs (the shared Modal component — takes `children: Element`, no props beyond that)
    - genossi-frontend/src/component/mail_compose/template_tester.rs lines 39-42, 103-150 (pattern for a small input + validation helper + button using r#type="button" + prevent_default)
    - .planning/phases/24-wysiwyg-frontend-editor/24-RESEARCH.md §"Pattern 3: In-App Link Dialog via modal.rs" (selection-preservation caveat) and §"i18n Keys Required" for MailEditorLinkDialogTitle / Url / TextLabel / Insert / Cancel keys
  </read_first>
  <action>
    Create genossi-frontend/src/component/mail_compose/wysiwyg_link_dialog.rs. Define `pub fn is_valid_link_url(url: &str) -> bool` (pure helper, gated on `!url.trim().is_empty()` AND `url.trim().starts_with("http://")` OR `url.trim().starts_with("https://")` — reject `javascript:`, `data:`, and bare relative URLs; ammonia would strip them anyway but UX rejects them upfront). Define `#[component] pub fn WysiwygLinkDialog(open: Signal<bool>, on_insert: EventHandler<(String, String)>) -> Element` where the tuple is `(url, display_text)`. The component renders NOTHING when `*open.read() == false`. When `open` is `true`, wrap the form in `Modal { ... }` with three inputs (URL required, display-text optional) and two buttons Insert + Cancel — both `r#type: "button"` with `evt.prevent_default()` in onclick (Dioxus button-reload-bug workaround, per 24-RESEARCH.md Pitfall 2 and project memory feedback_dioxus_button_type.md). Insert is disabled while `is_valid_link_url(url)` returns false. On Insert click, `on_insert.call((url.trim().to_string(), display_text.trim().to_string()))` then `open.set(false)`. On Cancel click, `open.set(false)` and reset local input signals. Use `Key::MailEditorLinkDialogTitle`, `MailEditorLinkUrlLabel`, `MailEditorLinkTextLabel`, `MailEditorLinkInsert`, `MailEditorLinkCancel` via `use_i18n()`. Add a `#[cfg(test)] mod tests` block with three tests: `is_valid_link_url_accepts_http_and_https`, `is_valid_link_url_rejects_javascript_and_data_scheme`, `is_valid_link_url_rejects_empty_and_whitespace` (mirrors the test pattern established by template_tester.rs::is_valid_test_address at lines 170-197). Do NOT call `window.prompt()` anywhere. Do NOT read from or write to `document.getSelection()` here — selection preservation is the caller's responsibility (handled in Task 4 wysiwyg_editor.rs where the toolbar Link button captures the range before opening the dialog).
  </action>
  <verify>
    <automated>cargo test -p genossi-frontend --lib is_valid_link_url 2>&1 | tail -15; cargo check -p genossi-frontend 2>&1 | tail -5</automated>
  </verify>
  <done>
    All three is_valid_link_url_* tests pass; wysiwyg_link_dialog.rs compiles; no usage of `window.prompt` in the new file.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create wysiwyg_toolbar.rs (button row with execCommand dispatches, ammonia-safe tag set)</name>
  <files>genossi-frontend/src/component/mail_compose/wysiwyg_toolbar.rs</files>
  <read_first>
    - genossi-frontend/src/component/mail_compose/template_tester.rs lines 103-150 (r#type="button" + prevent_default onclick pattern to mirror)
    - .planning/phases/24-wysiwyg-frontend-editor/24-RESEARCH.md §"execCommand — full commands the toolbar will use" (the exact table: bold, italic, underline, strikeThrough, insertUnorderedList, insertOrderedList, formatBlock=h1/h2/h3/p/blockquote, createLink, unlink) and §"i18n Keys Required" (13 toolbar-relevant MailEditor* keys)
    - genossi-frontend/src/js.rs (the exec_command_* helpers from Task 1)
  </read_first>
  <action>
    Create genossi-frontend/src/component/mail_compose/wysiwyg_toolbar.rs. Define `#[component] pub fn WysiwygToolbar(editor_id: String, on_command: EventHandler<()>, on_link_click: EventHandler<()>) -> Element`. Props explained: `editor_id` is the HTML `id` of the sibling contenteditable div so this component can focus it before every execCommand; `on_command` is fired AFTER every non-link execCommand so the parent (wysiwyg_editor) can re-read innerHTML/innerText to sync signals (Pitfall 5); `on_link_click` is fired instead of calling execCommand("createLink") directly — the parent opens the link dialog and preserves selection. Render one `<div class="flex flex-wrap gap-1 border-b px-2 py-1 bg-gray-50">` holding 13 buttons: Bold (execCommand bold), Italic (italic), Underline (underline), Strikethrough (strikeThrough), UL (insertUnorderedList), OL (insertOrderedList), H1/H2/H3 (formatBlock with "<h1>" / "<h2>" / "<h3>"), Paragraph (formatBlock "<p>"), Blockquote (formatBlock "<blockquote>"), Link (fires on_link_click), Unlink (execCommand unlink). Every button uses `r#type: "button"`, `title: i18n.t(Key::MailEditor…)` for accessibility, and its onclick handler MUST: (a) call `evt.prevent_default()` first, (b) grab `web_sys::window().and_then(|w| w.document())`, (c) call the editor element via `doc.get_element_by_id(&editor_id).and_then(|el| el.dyn_ref::<web_sys::HtmlElement>().map(|h| h.focus()))` before the command (so the selection is inside the editor), (d) call the appropriate `crate::js::exec_command_simple` or `exec_command_str` helper, (e) fire `on_command.call(())`. The Link button skips execCommand and just fires `on_link_click.call(())` — the parent handles range preservation. Use exact i18n keys from Plan 24-01: MailEditorBold, MailEditorItalic, MailEditorUnderline, MailEditorStrike, MailEditorUnorderedList, MailEditorOrderedList, MailEditorHeading1, MailEditorHeading2, MailEditorHeading3, MailEditorParagraph, MailEditorBlockquote, MailEditorLink, MailEditorUnlink. Button labels are short glyphs (B, I, U, S, ≡, 1., H1, H2, H3, ¶, "❝", 🔗, unlink glyph) with the i18n key as `title` attribute for the tooltip (aria-label doubles). Do NOT invoke execCommand for tags outside the ammonia-safe set (no `foreColor`, `hiliteColor`, `insertImage`, `insertHorizontalRule`, `justifyLeft` — none of these produce ammonia-safe output). Do NOT wrap the toolbar in a `form`.
  </action>
  <verify>
    <automated>grep -c 'r#type: "button"' genossi-frontend/src/component/mail_compose/wysiwyg_toolbar.rs; grep -cE 'exec_command_(simple|str)' genossi-frontend/src/component/mail_compose/wysiwyg_toolbar.rs; cargo check -p genossi-frontend 2>&1 | tail -5</automated>
  </verify>
  <done>
    All 13 buttons carry `r#type: "button"` (grep count = 13); at least 12 exec_command_* dispatches present (all non-Link buttons); `cargo check -p genossi-frontend` clean.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create wysiwyg_editor.rs (contenteditable host + on_change tuple + paste handler + toolbar/dialog wiring)</name>
  <files>genossi-frontend/src/component/mail_compose/wysiwyg_editor.rs, genossi-frontend/Cargo.toml</files>
  <read_first>
    - genossi-frontend/src/component/mail_compose/body_editor.rs (the old MailBodyEditor — the prop shape `value: String, on_change: EventHandler<String>` is the DEPRECATED contract; the new component EXTENDS it to `(plain, html)` — per D-01)
    - genossi-frontend/src/page/templates.rs line 484 (existing `onmounted` usage on a `<div>` in Dioxus 0.6.3 — proof the API surface is available)
    - .planning/phases/24-wysiwyg-frontend-editor/24-RESEARCH.md §"Pattern 1: Dioxus contenteditable + onmounted / oninput / onpaste" (full skeleton), §"Pitfall 1" (styleWithCSS false on mount), §"Pitfall 5" (DOM-sync-race — sync from DOM after every toolbar click), §"Pitfall 6" (selection preservation for link dialog), §"Pitfall 3" (paste order: preventDefault first, getData second)
  </read_first>
  <action>
    Create genossi-frontend/src/component/mail_compose/wysiwyg_editor.rs. Public API: `#[component] pub fn WysiwygEditor(value: String, on_change: EventHandler<(String, String)>) -> Element` where the tuple is `(plain: innerText, html: innerHTML)`. Import `WysiwygToolbar` from `wysiwyg_toolbar` and `WysiwygLinkDialog` from `wysiwyg_link_dialog` (both from the same mod, direct `use` paths). Layout: wrap everything in a `<div class="border rounded">` containing (top) a `WysiwygToolbar` and (bottom) the contenteditable div. Give the contenteditable div a stable id `"wysiwyg-editor"` (constant string — MUST match the id passed to WysiwygToolbar so its focus-before-command wiring hits the right node), classes `"w-full px-3 py-2 min-h-40 focus:outline-none prose prose-sm max-w-none"`, and the attributes `contenteditable: "true"` and `role: "textbox"`. On the contenteditable div: `onmounted` handler that (a) calls `crate::js::exec_command_bool(&doc, "styleWithCSS", false)` exactly once (Pitfall 1 — persists for the document lifetime, cite in an inline `// Pitfall 1` comment), (b) sets the initial content by grabbing the mounted element via `web_sys::window().and_then(|w| w.document()).and_then(|d| d.get_element_by_id("wysiwyg-editor"))` and calling `.set_inner_html(&value)`. On the contenteditable div: `oninput` handler that reads the same element, extracts `.inner_html()` (String) and casts to `HtmlElement` for `.inner_text()` (String — per D-02, innerText not textContent so line breaks survive), then calls `on_change.call((plain, html))`. On the contenteditable div: `onpaste` handler — see next sentence for exact ordering. The paste handler MUST call `evt.prevent_default()` FIRST (per Pitfall 3), then attempt to read the plain text out of the ClipboardData: prefer the Dioxus `Event<ClipboardData>` route by calling `evt.data().get_data("text/plain")` if the Dioxus API supports it in 0.6.3; the researcher's Open Question §3 flags this as `[ASSUMED]` — the executor MUST verify this signature exists before commit by writing a 5-line probe (e.g. `let _: String = evt.data().get_data("text/plain").unwrap_or_default();` compiles cleanly), and if the exact `ClipboardData::get_data` isn't there, fall back to reading via `web_sys::ClipboardEvent` cast: `evt.web_event().dyn_into::<web_sys::ClipboardEvent>().ok().and_then(|ce| ce.clipboard_data()).and_then(|dt| dt.get_data("text/plain").ok())`. Once the plain-text string is in hand, call `crate::js::exec_command_str(&doc, "insertText", &text)` — this inserts at the caret and lets the browser fold the change into the undo stack. Link-Dialog wiring: hold two local signals `let mut link_dialog_open = use_signal(|| false);` and `let mut saved_range = use_signal(|| None::<web_sys::Range>);` (add `Range` to Cargo.toml web-sys features if the compiler flags it missing — do this before commit; else add via `Selection` if Range isn't accessible from that layer). Pass `on_link_click: move |_| { /* Pitfall 6: capture the current Selection Range before opening the modal */ if let Some(win) = web_sys::window() { if let Some(sel) = win.get_selection().ok().flatten() { if sel.range_count() > 0 { if let Ok(r) = sel.get_range_at(0) { saved_range.set(Some(r)); } } } } link_dialog_open.set(true); }` to WysiwygToolbar. Render `WysiwygLinkDialog { open: link_dialog_open, on_insert: move |(url, text): (String, String)| { /* focus editor, restore range, run createLink */ if let Some(win) = web_sys::window() { if let Some(doc) = win.document() { if let Some(el) = doc.get_element_by_id("wysiwyg-editor") { if let Some(html_el) = el.dyn_ref::<web_sys::HtmlElement>() { html_el.focus().ok(); } } if let (Some(sel), Some(range)) = (win.get_selection().ok().flatten(), saved_range.read().clone()) { sel.remove_all_ranges().ok(); sel.add_range(&range).ok(); } let _ = crate::js::exec_command_str(&doc, "createLink", &url); /* sync signals after DOM mutation */ if let Some(el) = doc.get_element_by_id("wysiwyg-editor") { let h = el.inner_html(); let plain = el.dyn_ref::<web_sys::HtmlElement>().map(|he| he.inner_text()).unwrap_or_default(); on_change.call((plain, h)); } } } /* Optional: rewrite the anchor text if the user supplied a distinct display_text — omit for now, users can type it themselves before opening the dialog */ } }` (this is prose describing the closure; the executor writes the actual Rust). Also pass an `on_command` handler to WysiwygToolbar that runs the same "read innerHTML+innerText and call on_change" sync (extract into a private helper `fn sync_from_dom(on_change: &EventHandler<(String, String)>)` at file scope). This handles Pitfall 5. If Cargo.toml web-sys is missing `Selection` or `Range` features, add both — this is legitimate scope for this task since the editor cannot preserve selection without them.
  </action>
  <verify>
    <automated>grep -c 'contenteditable' genossi-frontend/src/component/mail_compose/wysiwyg_editor.rs; grep -c 'styleWithCSS' genossi-frontend/src/component/mail_compose/wysiwyg_editor.rs; grep -c 'prevent_default' genossi-frontend/src/component/mail_compose/wysiwyg_editor.rs; grep -c 'window.prompt' genossi-frontend/src/component/mail_compose/wysiwyg_editor.rs; cargo check -p genossi-frontend 2>&1 | tail -10</automated>
  </verify>
  <done>
    `cargo check -p genossi-frontend` clean; wysiwyg_editor.rs contains exactly 1 `contenteditable`, at least 1 `styleWithCSS`, at least 1 `prevent_default`, 0 `window.prompt` usages; if Selection/Range web-sys features were required, they are present in Cargo.toml.
  </done>
</task>

<task type="auto">
  <name>Task 5: Export WysiwygEditor from mail_compose/mod.rs</name>
  <files>genossi-frontend/src/component/mail_compose/mod.rs</files>
  <read_first>
    - genossi-frontend/src/component/mail_compose/mod.rs (7 existing `pub mod` and `pub use` statements)
  </read_first>
  <action>
    Add three new `pub mod` lines to genossi-frontend/src/component/mail_compose/mod.rs after the existing `pub mod template_var_buttons;`: `pub mod wysiwyg_editor;`, `pub mod wysiwyg_toolbar;`, `pub mod wysiwyg_link_dialog;`. Add ONE new `pub use` line at the bottom (after the existing `pub use template_var_buttons::TemplateVarButtons;`): `pub use wysiwyg_editor::WysiwygEditor;`. Do NOT re-export WysiwygToolbar or WysiwygLinkDialog — they are internal sub-components accessed via the editor. Do NOT delete `pub mod body_editor;` or `pub use body_editor::MailBodyEditor;` — Plan 24-03 removes those after the migration is verified. `cargo check -p genossi-frontend` must remain clean.
  </action>
  <verify>
    <automated>grep -c 'pub mod wysiwyg' genossi-frontend/src/component/mail_compose/mod.rs; grep -c 'pub use wysiwyg_editor::WysiwygEditor' genossi-frontend/src/component/mail_compose/mod.rs; cargo check -p genossi-frontend 2>&1 | tail -5</automated>
  </verify>
  <done>
    3 `pub mod wysiwyg*` lines and 1 `pub use wysiwyg_editor::WysiwygEditor` line present; body_editor exports remain unchanged; `cargo check -p genossi-frontend` clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → browser DOM | User keystrokes / pastes populate the contenteditable innerHTML; this string later crosses the client→API boundary in Plan 24-03 |
| browser DOM → Dioxus signal | The oninput/on_command handlers read innerHTML+innerText and push into the parent's on_change tuple |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-04 | Tampering | wysiwyg_editor.rs paste handler | low | mitigate | Plain-text-only paste via preventDefault + insertText (per D-07). Not a security boundary — ammonia at store boundary in Phase 23 D-03 is the security gate. UX-clean paste avoids leaking Word/Office/browser markup into the wire body_html. |
| T-24-05 | Tampering | wysiwyg_link_dialog.rs URL validation | low | mitigate | `is_valid_link_url` rejects everything but http:// and https:// before enabling Insert. Ammonia at server side additionally strips javascript:/data: even if a savvy caller manages to inject them via DOM. |
| T-24-06 | Information Disclosure | Selection Range signal | low | accept | The saved Range lives in a Dioxus signal for the component lifetime; it does not leak beyond the component and is dropped on unmount. |
</threat_model>

<verification>
Overall plan-02 checks after all 5 tasks complete:

- `cargo check -p genossi-frontend` — no errors, no warnings other than potential `dead_code` on WysiwygEditor and helpers (used from Plan 24-03).
- `cargo test -p genossi-frontend --lib is_valid_link_url` — 3 tests pass.
- File-existence check: all three of wysiwyg_editor.rs, wysiwyg_toolbar.rs, wysiwyg_link_dialog.rs exist.
- Grep hygiene: `grep -rn 'window.prompt' genossi-frontend/src/component/mail_compose/wysiwyg_*.rs` returns nothing (D-06).
- Grep hygiene: `grep -rn 'onsubmit' genossi-frontend/src/component/mail_compose/wysiwyg_*.rs` returns nothing (Dioxus button-reload-bug protection).
- Grep hygiene: `grep -c 'r#type: "button"' genossi-frontend/src/component/mail_compose/wysiwyg_toolbar.rs` >= 13.
</verification>

<success_criteria>
- WysiwygEditor Dioxus component exists with `on_change: EventHandler<(String, String)>` producing (plain, html).
- On mount: styleWithCSS=false is called exactly once — B/I produce semantic tags not span[style].
- Toolbar has exactly 13 ammonia-safe buttons (B, I, U, S, UL, OL, H1, H2, H3, P, Blockquote, Link, Unlink), all r#type=button.
- Paste handler enforces plain-text-only via preventDefault + insertText.
- Link dialog is an in-app Modal (D-06) — no window.prompt.
- Selection Range is captured before the Link modal opens and restored before createLink is called (Pitfall 6).
- Every non-Link toolbar command triggers a signal-sync from DOM (Pitfall 5).
- js.rs gains exec_command_bool / exec_command_str / exec_command_simple helpers.
- mod.rs exports WysiwygEditor; body_editor.rs remains untouched (removed in Plan 24-03).
</success_criteria>

<output>
Create `.planning/phases/24-wysiwyg-frontend-editor/24-02-SUMMARY.md` when done using the summary.md template. Capture: three new component files, three exec_command_* helpers in js.rs, one new mod.rs entry, and confirmation that the styleWithCSS-false-on-mount + button-reload-bug + Selection-preservation invariants are pinned in code. Note that Plan 24-03 depends on this SUMMARY landing and will remove body_editor.rs after the migration is verified.
</output>