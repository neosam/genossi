---
phase: 16-service-rest-teil-rueckgabe-auto-anlegen-phase
plan: 05
type: execute
mode: gap_closure
wave: 1
depends_on: []
files_modified:
  - genossi_service_impl/src/membership_adjust.rs
  - genossi_bin/tests/membership_adjust_e2e.rs
autonomous: true
requirements: [PART-04]
must_haves:
  truths:
    - "partial_repayment in genossi_service_impl/src/membership_adjust.rs rejects a Closed RepaymentPhase with ServiceError::Conflict mapping to HTTP 409 (CR-01, REVIEW.md). Guard sits between the phase lookup (membership_adjust.rs:344-348) and the auto-create match-block (membership_adjust.rs:353); it MUST run only when target_phase_existing is Some(_) so the auto-create branch is unaffected"
    - "Guard logic: only RepaymentPhaseStatus::Closed triggers 409. Preparation and Open both pass through (D-11.1 Lifecycle Closed = no writes; Preparation reuse intentional — established via Phase 14 prep workflow and the lookup currently reuses it; D-16-01 Variante B unaffected)"
    - "Conflict message contains the literal substring 'closed' (lowercase) and the fiscal_year (e.g. format!(\"Phase for fiscal_year {} is closed (D-11.1)\", effective.fiscal_year)) so E2E and forensics can grep deterministically"
    - "1 new unit test in service_tests mod: test_partial_repayment_rejects_closed_phase. Uses MockTestRepaymentPhaseDao::expect_all returning a Closed phase for the target fiscal_year, MockTestRepaymentPhaseDao::expect_create().times(0) (Closed must never trigger auto-create), MockTestRepaymentEntryDao::expect_create().times(0) (no entry write), and asserts the result is Err(ServiceError::Conflict(_))"
    - "1 new E2E test in genossi_bin/tests/membership_adjust_e2e.rs: test_partial_repayment_closed_phase_returns_409. Uses existing helpers create_repayment_phase / create_active_member / put_member_current_shares / partial_repayment_body / setup. Sequence: create phase (status=Preparation) → POST /open → POST /close → POST /api/members/{id}/partial-repayment with H1 date for that fiscal_year → expect HTTP 409 Conflict with body substring 'closed'"
    - "All 10 existing service unit tests under test_partial_repayment_* in genossi_service_impl/src/membership_adjust.rs still pass (cargo test -p genossi_service_impl --lib test_partial_repayment passes 11/11 after the change)"
    - "All 19 existing E2E tests in genossi_bin/tests/membership_adjust_e2e.rs (8 partial_repayment + 5 cancel_membership + 4 increase_shares + 2 cancel_membership-date) still pass; cargo test --test membership_adjust_e2e --features mock_auth reports 20 passed (19 alt + 1 neu), 0 failed, 0 ignored after the change"
    - "cargo build --workspace --features mock_auth exits 0; cargo clippy -p genossi_service_impl -p genossi_bin --features mock_auth exits 0"
    - "Audit-chain stays valid (`GET /api/audit/verify` returns valid=true) — the new guard is a pre-write rejection and writes no audit entries"
  artifacts:
    - path: "genossi_service_impl/src/membership_adjust.rs"
      provides: "Closed-phase status guard in partial_repayment + 1 new unit test test_partial_repayment_rejects_closed_phase"
      contains: "RepaymentPhaseStatus::Closed"
    - path: "genossi_bin/tests/membership_adjust_e2e.rs"
      provides: "1 new E2E test test_partial_repayment_closed_phase_returns_409 against POST /api/members/{id}/partial-repayment with a Closed phase"
      contains: "test_partial_repayment_closed_phase_returns_409"
  key_links:
    - from: "genossi_service_impl/src/membership_adjust.rs::partial_repayment"
      to: "ServiceError::Conflict (HTTP 409)"
      via: "if target.status == RepaymentPhaseStatus::Closed -> return Err(ServiceError::Conflict(...))"
      pattern: "RepaymentPhaseStatus::Closed"
    - from: "genossi_bin/tests/membership_adjust_e2e.rs::test_partial_repayment_closed_phase_returns_409"
      to: "POST /api/repayment-phase/{id}/close + POST /api/members/{id}/partial-repayment"
      via: "reqwest sequence: create_repayment_phase -> /open -> /close -> partial-repayment"
      pattern: "test_partial_repayment_closed_phase_returns_409"
---

<objective>
Schließe Gap aus 16-VERIFICATION.md (CR-01 / Closed-Phase-Status-Guard) durch Einbau eines
Status-Filters in `partial_repayment`. Ziel: Eine geschlossene `RepaymentPhase` darf keinen
weiteren `RepaymentEntry` aufnehmen — sie liefert stattdessen HTTP 409 Conflict.

**Hintergrund:** `membership_adjust.rs:344-348` looktet die Zielphase ausschließlich nach
`fiscal_year`. Existiert eine Phase, wird sie unabhängig vom Status wiederverwendet — auch
wenn sie bereits `Closed` ist. Das umgeht D-11.1 Lifecycle (Closed = read-only). Die
aktuelle E2E-Suite stimuliert diesen Pfad nicht; der Audit-Log könnte einen Entry in einer
abgeschlossenen Auszahlungsphase aufnehmen, was die Verbands-Konformität verletzt.

**Domain-Entscheidung (HUMAN-UAT.md Option A, 2026-06-05):** Nur `Closed` blockiert. Eine
existierende `Preparation`-Phase wird absichtlich wiederverwendet (Phase-14-Pre-Workflow),
eine `Open`-Phase ist der Standardfall. Auto-Create-Branch bleibt unangetastet.

Output: Status-Guard + 1 Unit-Test + 1 E2E-Test. Verification von 16-VERIFICATION.md
"human_needed" → "pass" möglich.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-service-rest-teil-rueckgabe-auto-anlegen-phase/16-CONTEXT.md
@.planning/phases/16-service-rest-teil-rueckgabe-auto-anlegen-phase/16-VERIFICATION.md
@.planning/phases/16-service-rest-teil-rueckgabe-auto-anlegen-phase/16-REVIEW.md
@.planning/phases/16-service-rest-teil-rueckgabe-auto-anlegen-phase/16-HUMAN-UAT.md
@.planning/phases/16-service-rest-teil-rueckgabe-auto-anlegen-phase/16-02-SUMMARY.md
@.planning/phases/16-service-rest-teil-rueckgabe-auto-anlegen-phase/16-04-SUMMARY.md

@genossi_service_impl/src/membership_adjust.rs
@genossi_bin/tests/membership_adjust_e2e.rs

<interfaces>
<!-- All contracts the executor needs. NO codebase exploration required. -->

# Aktueller Code an der Bug-Stelle (genossi_service_impl/src/membership_adjust.rs:340-353 — verbatim)

```rust
        // Step 9: ensure_repayment_phase — find existing or auto-create (D-16-01 Variante B).
        // `all()` (Default-Impl) filtert soft-deleted aus. SQLite-Impl von dump_all ordnet
        // `ORDER BY fiscal_year DESC, created DESC` (RESEARCH §"Don't Hand-Roll"), also ist
        // `first()` die juengste Phase fuer den share_value-Fallback (D-16-05).
        let all_phases = self.repayment_phase_dao.all(tx.clone()).await?;
        let target_phase_existing = all_phases
            .iter()
            .find(|p| p.fiscal_year == effective.fiscal_year)
            .cloned();

        let now_offset = time::OffsetDateTime::now_utc();
        let now_pdt = time::PrimitiveDateTime::new(now_offset.date(), now_offset.time());

        let (target_phase, was_created): (RepaymentPhaseEntity, bool) = match target_phase_existing
        {
```

# RepaymentPhaseStatus Enum (verbatim aus genossi_dao/src/repayment_phase.rs:10-22)

```rust
pub enum RepaymentPhaseStatus {
    Preparation,
    Open,
    Closed,
}

impl RepaymentPhaseStatus {
    pub fn as_str(&self) -> &'static str {
        match self {
            RepaymentPhaseStatus::Preparation => "Preparation",
            RepaymentPhaseStatus::Open => "Open",
            RepaymentPhaseStatus::Closed => "Closed",
        }
    }
```

# ServiceError::Conflict Variante (verbatim aus genossi_service/src/lib.rs — bereits in Step 5 verwendet, genossi_service_impl/src/membership_adjust.rs:319-322)

```rust
return Err(ServiceError::Conflict(Arc::from(format!(
    "Cannot start partial repayment for cancelled member (exit_date={:?})",
    member_entity.exit_date
))));
// -> mappt im REST-Layer auf HTTP 409 (siehe genossi_rest/src/lib.rs RestError::Conflict)
```

# Bestehende Unit-Test-Fixture für Closed-Phase (genossi_service_impl/src/membership_adjust.rs:1004-1019 — verbatim, fiscal_year + share_value parametrisierbar; default-status ist Open)

```rust
fn sample_repayment_phase(fiscal_year: i32, share_value: i64) -> RepaymentPhaseEntity {
    let date = time::Date::from_calendar_date(2020, time::Month::January, 1).unwrap();
    let datetime = time::PrimitiveDateTime::new(date, time::Time::MIDNIGHT);
    RepaymentPhaseEntity {
        id: Uuid::new_v4(),
        fiscal_year,
        share_value,
        status: RepaymentPhaseStatus::Open,        // <-- HARDCODED Open
        opened_at: Some(datetime),
        closed_at: None,
        created: datetime,
        deleted: None,
        version: Uuid::new_v4(),
    }
}
```

→ Für den neuen Test KEINE Mutation der Helper-Funktion. Stattdessen die Phase im Test
inline mutieren:
```rust
let mut closed_phase = sample_repayment_phase(target_fy, 10000);
closed_phase.status = RepaymentPhaseStatus::Closed;
closed_phase.closed_at = Some(datetime);   // optional, nur für Authentizität
```

# Bestehender E2E-Test-Aufbau (Vorlage für Test_partial_repayment_sum_check_block_400 — genossi_bin/tests/membership_adjust_e2e.rs:609-651, verbatim Beispielsequenz)

```rust
#[tokio::test]
async fn test_partial_repayment_sum_check_block_400() {
    let server = setup().await;
    let client = reqwest::Client::new();
    let m = create_active_member(&client, &server, 1102, "PartSum").await;
    let m = put_member_current_shares(&client, &server, &m, 3).await;
    let member_id = m.id.expect("id");

    let h1_date = today_march_15();
    let target_fy = h1_date.year();
    let _phase = create_repayment_phase(&client, &server, target_fy, 10000).await;
    /* ... */
}
```

→ Helper-Verfügbarkeit (verifiziert gegen genossi_bin/tests/membership_adjust_e2e.rs):
- `setup() -> TestServer` (asynchron)
- `create_active_member(&reqwest::Client, &TestServer, member_number: i64, first_name: &str) -> MemberTO` (Z. 89-94)
- `put_member_current_shares(&reqwest::Client, &TestServer, &MemberTO, target_shares: i32) -> MemberTO` (Z. 487-492)
- `create_repayment_phase(&reqwest::Client, &TestServer, fiscal_year: i32, share_value: i64) -> serde_json::Value` (Z. 517-540 — Status `Preparation`)
- `partial_repayment_body(date: &str, shares: i32) -> serde_json::Value`
- `today_march_15() -> time::Date`

Hinweis: `MemberTO` hat `id: Option<Uuid>` — `.expect("id")` ist das idiomatische Pattern, das auch in den bestehenden 8 partial_repayment-E2E-Tests verwendet wird.

# REST-Routes für Phase-Lifecycle (verbatim aus genossi_rest/src/repayment_phase.rs:275, 284, 351)

```rust
// path = "/{id}/open"  → POST /api/repayment-phase/{id}/open
// path = "/{id}/close" → POST /api/repayment-phase/{id}/close
//   pub async fn close_repayment_phase<RestState>(...)
//   route: .route("/{id}/close", post(close_repayment_phase::<RestState>))
```

→ E2E-Sequenz: create_repayment_phase liefert Preparation. `POST /open` aktiviert.
`POST /close` schließt. Anschließend `POST /api/members/{id}/partial-repayment`.

</interfaces>

<existing_summaries_to_respect>
- Plan 16-02 (Service-Impl): Hat das 12-Step-State-Machine + Auto-Create-Branch + 10
  Unit-Tests + Audit-Wiring eingebaut. **NICHT entfernen oder umstrukturieren** — nur
  zwischen Step 9 (Phase-Lookup) und dem `match target_phase_existing { ... }`-Block
  einen Guard ergänzen, der bei Closed-Phase abbricht. `was_created`-Variable und der
  Auto-Create-Branch bleiben unverändert.
- Plan 16-04 (REST + E2E): Hat 8 E2E-Tests + REST-Handler + DI-Wiring geliefert.
  **NICHT umarbeiten** — nur einen 9. E2E-Test in der gleichen Datei am Ende
  anhängen (vor potentiellen Helper-Mods).
- Audit-Process-Strings (`PARTIAL_REPAYMENT_PROCESS`, `REPAYMENT_PHASE_CREATE_PROCESS`)
  bleiben unverändert; der Guard schreibt keinen Audit-Eintrag.
</existing_summaries_to_respect>

</context>

<tasks>

<task id="1" type="execute">
<title>Add Closed-Phase status guard in partial_repayment</title>

<read_first>
- `genossi_service_impl/src/membership_adjust.rs` (lines 340-393 — Step 9 + Auto-Create-Branch im Ist-Zustand verstehen)
- `genossi_dao/src/repayment_phase.rs` (lines 10-22 — RepaymentPhaseStatus enum)
- `.planning/phases/16-service-rest-teil-rueckgabe-auto-anlegen-phase/16-VERIFICATION.md` (CR-01 Empfehlung)
</read_first>

<action>
Editiere `genossi_service_impl/src/membership_adjust.rs`. Füge **direkt nach** Zeile 348
(d.h. nach der `let target_phase_existing = all_phases.iter().find(...).cloned();`-Zuweisung
und **vor** dem `let now_offset = ...`-Block) folgenden Guard ein:

```rust
        // Phase 16.05 / CR-01 — D-11.1-Status-Guard: Eine geschlossene Phase darf
        // keinen neuen Entry aufnehmen. Preparation und Open passieren (Preparation =
        // Phase-14-Pre-Workflow-Reuse, Open = Standardfall, Auto-Create unten erzeugt
        // ohnehin Open). Closed → HTTP 409 Conflict.
        if let Some(ref existing) = target_phase_existing {
            if existing.status == RepaymentPhaseStatus::Closed {
                return Err(ServiceError::Conflict(Arc::from(format!(
                    "Phase for fiscal_year {} is closed (D-11.1)",
                    effective.fiscal_year
                ))));
            }
        }
```

Verifiziere mit `cargo build --workspace --features mock_auth`, dass kein Import fehlt
(`RepaymentPhaseStatus` ist bereits genutzt — z. B. in Step 9-Kommentar/Auto-Create:
`status: RepaymentPhaseStatus::Open`. Import-Pfad: schon vorhanden über
`use genossi_dao::repayment_phase::{RepaymentPhaseEntity, RepaymentPhaseStatus};` o.ä.).
Falls der Import fehlt — was unwahrscheinlich ist, weil der Auto-Create-Branch
`RepaymentPhaseStatus::Open` bereits referenziert — ergänze ihn am Datei-Kopf zur
bestehenden `use`-Liste, nicht als neue Zeile.
</action>

<acceptance_criteria>
- `grep -n "RepaymentPhaseStatus::Closed" genossi_service_impl/src/membership_adjust.rs` zeigt
  mindestens **2 Treffer** nach der Änderung (1 im Service-Body, 1 im neuen Unit-Test aus Task 2)
- `grep -c "is closed (D-11.1)" genossi_service_impl/src/membership_adjust.rs` ≥ 1
- `cargo build --workspace --features mock_auth` exit 0
- `cargo clippy -p genossi_service_impl --features mock_auth -- -D warnings` exit 0
- Der Guard steht zwischen Zeile 348 (Lookup) und Zeile 350 des Originalzustands. Er darf NIE im
  `None`-Branch des Auto-Create-Match laufen. Verifiziere durch Read:
  `awk '/let target_phase_existing/,/let now_offset/' genossi_service_impl/src/membership_adjust.rs`
  enthält den neuen `if let Some(ref existing) = target_phase_existing {`-Block.
- Bestehende 10 partial_repayment-Unit-Tests bestehen unverändert:
  `cargo test -p genossi_service_impl --lib test_partial_repayment` → 10 passed (vor Task 2)
</acceptance_criteria>
</task>

<task id="2" type="execute">
<title>Add unit test test_partial_repayment_rejects_closed_phase (mockall)</title>

<read_first>
- `genossi_service_impl/src/membership_adjust.rs` (lines 732-1100 — service_tests mod-Header, Mocks, Fixtures, build_service_part)
- `genossi_service_impl/src/membership_adjust.rs` (lines 1856-1917 — test_partial_repayment_auto_create_uses_previous_share_value als naheste Vorlage)
- `genossi_service_impl/src/membership_adjust.rs` (lines 1004-1019 — sample_repayment_phase Helper)
</read_first>

<action>
Hänge im `mod service_tests`-Block (nach `test_partial_repayment_paid_out_entries_excluded_from_sum`
am Datei-Ende des Mod, vor `}` der Mod-Schließung) folgenden Unit-Test an:

```rust
    // Phase 16.05 / CR-01 — Closed phase MUST be rejected with ServiceError::Conflict.
    // Service must not call repayment_phase_dao.create (no auto-create on Closed) and not
    // call repayment_entry_dao.create (no entry write).
    #[tokio::test]
    async fn test_partial_repayment_rejects_closed_phase() {
        let member_id = Uuid::new_v4();
        let willensbekundung = h1_test_date();
        let target_fy = h1_target_fy();

        let mut member_dao = MockTestMemberDao::new();
        member_dao
            .expect_find_by_id()
            .times(1)
            .returning(move |_, _| Ok(Some(sample_member_entity_with_shares(member_id, 3, None))));

        // Existing phase for target_fy in status Closed (Phase 14 prep workflow finished).
        let mut closed_phase = sample_repayment_phase(target_fy, 10000);
        closed_phase.status = RepaymentPhaseStatus::Closed;
        let phases: Arc<[RepaymentPhaseEntity]> = Arc::from(vec![closed_phase]);

        let mut repayment_phase_dao = MockTestRepaymentPhaseDao::new();
        repayment_phase_dao
            .expect_all()
            .returning(move |_| Ok(phases.clone()));
        // Guard short-circuits BEFORE auto-create.
        repayment_phase_dao.expect_create().times(0);

        let mut repayment_entry_dao = MockTestRepaymentEntryDao::new();
        // Guard short-circuits BEFORE sum-check + entry write.
        repayment_entry_dao.expect_find_by_member_and_phase().times(0);
        repayment_entry_dao.expect_create().times(0);

        let service = build_service_part(
            member_dao,
            MockTestMemberActionDao::new(),
            allow_audit_log(),
            allow_admin_perms(),
            setup_tx_dao(),
            repayment_phase_dao,
            repayment_entry_dao,
        );

        let result = service
            .partial_repayment(member_id, 1, willensbekundung, Authentication::Full, None)
            .await;

        match result {
            Err(ServiceError::Conflict(msg)) => {
                let text = msg.to_string();
                assert!(
                    text.contains("closed"),
                    "expected message to mention 'closed', got: {}",
                    text
                );
                assert!(
                    text.contains(&target_fy.to_string()),
                    "expected message to mention fiscal_year {}, got: {}",
                    target_fy,
                    text
                );
            }
            Err(other) => panic!("expected ServiceError::Conflict, got {:?}", other),
            Ok(_) => panic!("expected ServiceError::Conflict, got Ok(_)"),
        }
    }
```

Nur diesen einen Test hinzufügen. Keine existierenden Tests/Helpers modifizieren.
</action>

<acceptance_criteria>
- `grep -c "fn test_partial_repayment_rejects_closed_phase" genossi_service_impl/src/membership_adjust.rs` = 1
- `cargo test -p genossi_service_impl --lib test_partial_repayment_rejects_closed_phase --features mock_auth` → 1 passed
- `cargo test -p genossi_service_impl --lib test_partial_repayment --features mock_auth` → 11 passed (10 alt + 1 neu), 0 failed
- `cargo test -p genossi_service_impl --lib --features mock_auth` → 0 failed
- Test enthält genau diese Expectations: `expect_create().times(0)` auf phase_dao UND entry_dao,
  `expect_find_by_member_and_phase().times(0)` auf entry_dao. Verifiziere (Gesamt-Datei-Counts steigen nach Insertion):
  ```bash
  grep -cE "expect_create\\(\\)\\.times\\(0\\)" genossi_service_impl/src/membership_adjust.rs   # ≥ 2 nach Insert (1× phase_dao + 1× entry_dao im neuen Test)
  grep -cE "expect_find_by_member_and_phase\\(\\)\\.times\\(0\\)" genossi_service_impl/src/membership_adjust.rs  # ≥ 1 nach Insert (entry_dao im neuen Test)
  ```
</acceptance_criteria>
</task>

<task id="3" type="execute">
<title>Add E2E test test_partial_repayment_closed_phase_returns_409</title>

<read_first>
- `genossi_bin/tests/membership_adjust_e2e.rs` (lines 500-545 — create_repayment_phase Helper inspizieren)
- `genossi_bin/tests/membership_adjust_e2e.rs` (lines 609-715 — sum-check + auto-fill-skip Tests als Vorlage für /open- und phase_id-Extraktion)
- `genossi_rest/src/repayment_phase.rs` (lines 270-360 — /open- und /close-Routes bestätigen)
</read_first>

<action>
Hänge im `genossi_bin/tests/membership_adjust_e2e.rs` am Datei-Ende (NACH dem letzten
existierenden `#[tokio::test]`-Block; vor evtl. `#[ignore]`-Helpers) folgenden Test an:

```rust
/// Phase 16.05 / CR-01 — Closed phase darf KEINEN partial_repayment-Entry aufnehmen.
/// Sequenz: Phase create (Preparation) → POST /open → POST /close → POST partial-repayment
/// → erwartet HTTP 409 Conflict mit Body-Substring "closed".
#[tokio::test]
async fn test_partial_repayment_closed_phase_returns_409() {
    let server = setup().await;
    let client = reqwest::Client::new();
    let m = create_active_member(&client, &server, 1108, "PartClosed").await;
    let m = put_member_current_shares(&client, &server, &m, 3).await;
    let member_id = m.id.expect("id");

    let h1_date = today_march_15();
    let target_fy = h1_date.year();

    // 1) Phase fuer target_fy anlegen (Preparation).
    let phase = create_repayment_phase(&client, &server, target_fy, 10000).await;
    let phase_id = phase["id"].as_str().expect("phase.id");

    // 2) Phase oeffnen.
    let r_open = client
        .post(server.url(&format!("/api/repayment-phase/{}/open", phase_id)))
        .send()
        .await
        .expect("open phase");
    assert_eq!(
        r_open.status(),
        StatusCode::OK,
        "open phase: {}",
        r_open.text().await.unwrap_or_default()
    );

    // 3) Phase schliessen.
    let r_close = client
        .post(server.url(&format!("/api/repayment-phase/{}/close", phase_id)))
        .send()
        .await
        .expect("close phase");
    assert_eq!(
        r_close.status(),
        StatusCode::OK,
        "close phase: {}",
        r_close.text().await.unwrap_or_default()
    );

    // 4) partial-repayment auf geschlossene Phase → 409 Conflict (D-11.1 Guard).
    let resp = client
        .post(server.url(&format!("/api/members/{}/partial-repayment", member_id)))
        .json(&partial_repayment_body(&h1_date.to_string(), 1))
        .send()
        .await
        .expect("POST partial-repayment");
    let status = resp.status();
    let body_text = resp.text().await.unwrap_or_default();
    assert_eq!(
        status,
        StatusCode::CONFLICT,
        "expected 409 Conflict for closed phase, got {}: {}",
        status,
        body_text
    );
    assert!(
        body_text.contains("closed"),
        "expected body to mention 'closed', got: {}",
        body_text
    );
    assert!(
        body_text.contains(&target_fy.to_string()),
        "expected body to mention fiscal_year {}, got: {}",
        target_fy,
        body_text
    );
}
```

Nur diesen einen Test hinzufügen. Helpers wiederverwenden, keine neuen Helpers anlegen.
Falls `StatusCode::CONFLICT` nicht bereits importiert ist: der bestehende Test
`test_partial_repayment_cancelled_member_block_409` nutzt es schon — Import ist da.
</action>

<acceptance_criteria>
- `grep -c "fn test_partial_repayment_closed_phase_returns_409" genossi_bin/tests/membership_adjust_e2e.rs` = 1
- `cargo test --test membership_adjust_e2e --features mock_auth test_partial_repayment_closed_phase_returns_409` → 1 passed
- `cargo test --test membership_adjust_e2e --features mock_auth test_partial_repayment` → 9 passed (8 alt + 1 neu), 0 failed (Filter auf partial_repayment-Familie)
- `cargo test --test membership_adjust_e2e --features mock_auth` → 20 passed (19 alt + 1 neu), 0 failed, 0 ignored (gesamte Datei)
- `cargo build --tests --features mock_auth` exit 0
- Test ruft `StatusCode::CONFLICT` und `body_text.contains("closed")` auf — verifiziere mit
  `grep -A 60 "fn test_partial_repayment_closed_phase_returns_409" genossi_bin/tests/membership_adjust_e2e.rs | grep -E "StatusCode::CONFLICT|contains\\(\"closed\"\\)"`
</acceptance_criteria>
</task>

<task id="4" type="execute">
<title>Full-stack verification + audit-chain check</title>

<read_first>
- `genossi_service_impl/src/membership_adjust.rs` (full file — confirm no other test was broken)
- `genossi_bin/tests/membership_adjust_e2e.rs` (full file — confirm no other test was broken)
</read_first>

<action>
Führe folgende Verifikationsbefehle aus und erfasse Output für SUMMARY.md:

```bash
cargo build --workspace --features mock_auth
cargo clippy -p genossi_service_impl -p genossi_bin --features mock_auth -- -D warnings
cargo test -p genossi_service_impl --lib test_partial_repayment --features mock_auth
cargo test --test membership_adjust_e2e --features mock_auth
cargo test --workspace --features mock_auth
```

Falls `cargo test --workspace` Tests in **anderen** Crates fehlschlagen, die NICHT
membership_adjust betreffen: dokumentiere als Pre-existing Failure in SUMMARY und prüfe
gegen Phase-15- und Phase-16-Vor-Summaries, ob sie schon vor diesem Plan rot waren.
Pre-existing-Failures blockieren diesen Gap-Closure nicht.

Audit-Chain-Smoke (manuell, nur dokumentieren — wird durch E2E indirekt abgedeckt, da
der Guard pre-write rejected):
- Bestätige durch Code-Read: zwischen dem neuen Guard (`return Err(ServiceError::Conflict(...))`)
  und der Datei-Position `audited_create!` (Step 12) liegt **kein** weiterer Audit-Write. Der
  Guard kann den Audit-Log nicht korrumpieren.
</action>

<acceptance_criteria>
- `cargo build --workspace --features mock_auth` exit 0
- `cargo clippy -p genossi_service_impl -p genossi_bin --features mock_auth -- -D warnings` exit 0
- `cargo test -p genossi_service_impl --lib test_partial_repayment --features mock_auth` → 11 passed, 0 failed
- `cargo test --test membership_adjust_e2e --features mock_auth test_partial_repayment` → 9 passed, 0 failed (Filter)
- `cargo test --test membership_adjust_e2e --features mock_auth` → 20 passed, 0 failed, 0 ignored (gesamte Datei)
- Falls `cargo test --workspace` Failures außerhalb von partial_repayment/membership_adjust zeigt:
  in SUMMARY.md unter "Pre-existing failures (out of scope)" listen mit Plan-/Commit-Referenz aus
  früheren Phasen.
- Verifiziere durch Read, dass der Guard tatsächlich an der richtigen Stelle steht:
  ```bash
  awk '/let all_phases = self.repayment_phase_dao.all/,/let \(target_phase, was_created\)/' \
      genossi_service_impl/src/membership_adjust.rs
  ```
  zeigt nacheinander: `let all_phases ...` → `let target_phase_existing = ...` → der neue
  `if let Some(ref existing) = target_phase_existing { ... RepaymentPhaseStatus::Closed ... }`
  → `let now_offset = ...` → `let now_pdt = ...` → `let (target_phase, was_created) = match ...`
</acceptance_criteria>
</task>

</tasks>

<verification>
After all four tasks complete, the gap-closure is verified when:

1. `genossi_service_impl/src/membership_adjust.rs::partial_repayment` rejects a Closed
   `RepaymentPhase` with `ServiceError::Conflict` before any auto-create or sum-check runs.
2. `cargo test -p genossi_service_impl --lib test_partial_repayment` reports 11 passed (10
   pre-existing + `test_partial_repayment_rejects_closed_phase`).
3. `cargo test --test membership_adjust_e2e --features mock_auth` reports 20 passed (19
   pre-existing + `test_partial_repayment_closed_phase_returns_409`), 0 ignored. Davon 9
   `test_partial_repayment_*` (8 alt + 1 neu).
4. `cargo build --workspace --features mock_auth` and
   `cargo clippy -p genossi_service_impl -p genossi_bin --features mock_auth -- -D warnings`
   both exit 0.
5. The 9 existing VERIFICATION.md Truths remain VERIFIED — re-running `/gsd-verify-work 16`
   should flip the verification status from `human_needed` / `gaps_found` to `passed`
   because CR-01 is now resolved with a deterministic test.
6. Re-running `/gsd-code-review` on the changed range surfaces 0 BLOCKERs for the
   Closed-phase code path (CR-01 closed).

**Phase 16 Must-Have (PART-04) impact:** PART-04 ("Sum-Check + Auto-Fill-Skip verhindert
Duplikate") wird nicht verändert, aber durch den neuen Closed-Guard zusätzlich gehärtet
(eine Closed-Phase ist jetzt ein definiertes Ablehnungsverhalten, kein Silent-Reuse mehr).
</verification>

<out_of_scope>
- Refactor / Umverteilung der Step-9-Logik (Lookup-Strategie bleibt `all_phases.iter().find(...)`)
- Änderungen am Auto-Create-Branch (D-16-01 Variante B bleibt: Open-Status für Auto-Created)
- Behandlung des Preparation-Pfads (bewusst unverändert — Phase-14-Pre-Workflow-Reuse)
- Neue DAO-Methoden (find_by_fiscal_year_and_status o.ä. — `all` + Status-Check im Service reicht)
- REST-Layer-Anpassungen (Error-Mapping `ServiceError::Conflict → HTTP 409` ist bereits in
  `genossi_rest/src/lib.rs` implementiert)
- Permission-Reordering (CR-02 aus REVIEW.md — separates Gap, nicht Teil von CR-01)
- PII in Conflict-Message (WR-02 aus REVIEW.md — separates Gap; neue Message enthält nur
  `fiscal_year`, keine PII)
- Frontend-Anpassungen (genossi-frontend) — REST-Verhalten ändert sich nur um eine
  zusätzliche 409-Antwortklasse, die der Vorstand-UI über die bestehende Error-Pipeline anzeigt
</out_of_scope>

<dependencies>
- Plan 16-02 (committed `83c2d3a` / earlier) — liefert `partial_repayment` Service-Impl
  und die 10 Unit-Test-Mocks. **Muss vor Wave 1 dieses Plans verifiziert sein.**
- Plan 16-04 (committed) — liefert REST-Handler + 8 E2E-Tests + `create_repayment_phase`-Helper.
  **Muss vor Wave 1 dieses Plans verifiziert sein.**
- Phase 14 (committed) — liefert `RepaymentPhase`-Lifecycle (Preparation/Open/Closed) +
  `/api/repayment-phase/{id}/close`-Endpoint. **Out-of-scope, aber required.**
- Kein neuer DAO. Kein neuer Service. Kein neuer REST-Handler.
</dependencies>
</content>
</invoke>