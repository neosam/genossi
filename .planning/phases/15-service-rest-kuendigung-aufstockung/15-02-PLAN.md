---
phase: 15-service-rest-kuendigung-aufstockung
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - genossi_service_impl/src/membership_adjust.rs
autonomous: true
requirements: [CANC-01, CANC-03, CANC-04, CANC-05, PERM-01, PERM-02, AUDT-01]
tags: [rust, service-impl, audit-macro, transaction, cancel-membership]

must_haves:
  truths:
    - "MembershipAdjustServiceImpl ist via gen_service_impl! mit 6 Deps (MemberActionDao, MemberDao, AuditLogDao, PermissionService, UuidService, TransactionDao) deklariert"
    - "cancel_membership() pruefen ADMIN_PRIVILEGE per check_permission, validiert das Datum via validate_willensbekundung_date, prueft EntityNotFound, prueft Already-Cancelled (HTTP 409), erzeugt MemberAction::Austritt via audited_create! mit Process-String 'member-adjust.cancel', ruft recalc_dates Free-Function auf und gibt (MemberAction, Member) zurueck"
    - "MemberAction::Austritt hat shares_change=0, transfer_member_id=None, effective_date=Some(compute_effective_date(...).effective_date), date=willensbekundung_date (CANC-03)"
    - "Kein direkter `member_action_dao.create(...)` / `member_dao.update(...)` ausserhalb der `audited_*!`-Macros (AUDT-01 Grep-Gate)"
    - "Keine MemberAction::Verkauf und kein RepaymentEntry werden direkt erzeugt (CANC-05)"
    - "Keine direkte Member.exit_date-Mutation — alleinig via recalc_dates-Hook (CANC-04)"
    - "Mindestens 4 Service-Unit-Tests mit MockDaos sind gruen (Happy-Path-H1, Happy-Path-H2, Permission-Denied, Already-Cancelled)"
    - "HTTP 401 fuer PermissionDenied (NICHT 403): Per genossi_rest/src/lib.rs:115 globalem Mapping (`ServiceError::PermissionDenied -> RestError::Unauthorized -> 401`). D-15-12-Enumeration ('403 (No Admin Privilege)') widerspricht der eigenen Why-Klausel ('konsistent mit existing RestError-Mapping') und der ROADMAP-Success-Criteria ('401'). Plaene folgen Why-Klausel + ROADMAP. CONTEXT.md wird post-phase korrigiert (403-Eintrag entfernen)."
  artifacts:
    - path: "genossi_service_impl/src/membership_adjust.rs"
      provides: "MembershipAdjustServiceImpl mit cancel_membership() + Service-Unit-Tests"
      contains: "impl<Deps: MembershipAdjustServiceDeps> MembershipAdjustService for MembershipAdjustServiceImpl"
  key_links:
    - from: "MembershipAdjustServiceImpl::cancel_membership"
      to: "audited_create! macro auf member_action_dao"
      via: "crate::audited_create!(self, self.member_action_dao, &action_entity, CANCEL_PROCESS, &user_id, tx)"
      pattern: "audited_create!\\([\\s\\S]*?CANCEL_PROCESS"
    - from: "MembershipAdjustServiceImpl::cancel_membership"
      to: "recalc_dates Free-Function (aus Plan 01)"
      via: "crate::member_action::recalc_dates(...)"
      pattern: "crate::member_action::recalc_dates\\("
    - from: "MembershipAdjustServiceImpl::cancel_membership"
      to: "ADMIN_PRIVILEGE permission"
      via: "self.permission_service.check_permission(ADMIN_PRIVILEGE, ...)"
      pattern: "check_permission\\(genossi_service::permission::ADMIN_PRIVILEGE"
---

<objective>
`MembershipAdjustService::cancel_membership` implementieren — Single-Action-Operation, die das v1.2-Audit-Pattern fuer ganze Phase 15-17 etabliert.

Purpose:
- Implementiert CANC-01..05 + PERM-01 + AUDT-01 in einer atomaren Service-Methode mit Transaction-Lifecycle.
- Etabliert die `ADMIN_PRIVILEGE`-Permission-Funnel + `audited_create!`-Macro-Compliance fuer den v1.2-Namespace.
- Konsumiert Phase-14-`compute_effective_date` (H1/H2-Stichtag) + Plan-01-`validate_willensbekundung_date` + Plan-01-`recalc_dates`-Free-Function.

Output:
- Erweitertes `genossi_service_impl/src/membership_adjust.rs` mit `MembershipAdjustServiceImpl<Deps>` + `cancel_membership`-Impl + 4 Service-Unit-Tests.
- Process-Konstante `CANCEL_PROCESS = "member-adjust.cancel"` (D-15-02).
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md

VCS: this project uses jj (Jujutsu). The executor should NOT run git commands directly; rely on GSD's commit step which already wraps jj-aware commits.
</execution_context>

<context>
@.planning/phases/15-service-rest-kuendigung-aufstockung/15-CONTEXT.md
@.planning/phases/15-service-rest-kuendigung-aufstockung/15-PATTERNS.md
@.planning/phases/15-service-rest-kuendigung-aufstockung/15-01-PLAN.md
@.planning/phases/15-service-rest-kuendigung-aufstockung/15-01-SUMMARY.md
@CLAUDE.md

<interfaces>
From `genossi_service_impl/src/member_action.rs:21-30` (gen_service_impl-Vorbild):
```rust
gen_service_impl! {
    struct MemberActionServiceImpl: MemberActionService = MemberActionServiceDeps {
        MemberActionDao: MemberActionDao<Transaction = Self::Transaction> = member_action_dao,
        MemberDao: MemberDao<Transaction = Self::Transaction> = member_dao,
        AuditLogDao: AuditLogDao<Transaction = Self::Transaction> = audit_log_dao,
        PermissionService: PermissionService<Context = Self::Context> = permission_service,
        UuidService: UuidService = uuid_service,
        TransactionDao: TransactionDao<Transaction = Self::Transaction> = transaction_dao,
    }
}
```

From `genossi_service_impl/src/member_action.rs:289-356` (Service-Method-Sequence — Vorlage fuer cancel_membership):
```rust
async fn create(&self, item: &MemberAction, context, tx) -> Result<MemberAction, ServiceError> {
    let tx = self.transaction_dao.use_transaction(tx).await?;
    let user_id = self.permission_service.current_user_id(context.clone()).await?
        .unwrap_or_else(|| "SYSTEM".to_string());
    self.permission_service.check_permission(MANAGE_MEMBERS_PRIVILEGE, context).await?;
    // validate_action(...)
    // member existence check
    let now = time::OffsetDateTime::now_utc();
    let new_action = MemberAction { id: self.uuid_service.new_v4().await, ... };
    let action_entity: MemberActionEntity = (&new_action).into();
    crate::audited_create!(self, self.member_action_dao, &action_entity, MEMBER_ACTION_SERVICE_PROCESS, &user_id, tx);
    self.recalc_dates(new_action.member_id, tx.clone()).await?;
    self.recalc_migrated(new_action.member_id, tx.clone()).await?;
    self.transaction_dao.commit(tx).await?;
    Ok(new_action)
}
```

From `genossi_service_impl/src/audit_macros.rs:6-36` (audited_create!-Signatur):
```
audited_create!($self:expr, $dao:expr, $entity:expr, $process:expr, $user_id:expr, $tx:expr)
```

From `genossi_service/src/permission.rs:28`:
```rust
pub const ADMIN_PRIVILEGE: &str = "admin";
```

From `genossi_dao/src/member_action.rs` (ActionType-Variante):
```rust
ActionType::Austritt
```

From `genossi_rest/src/lib.rs:104-120` (global ServiceError -> RestError mapping):
```rust
impl From<genossi_service::ServiceError> for RestError {
    fn from(e: genossi_service::ServiceError) -> Self {
        match e {
            ServiceError::EntityNotFound(_) => RestError::NotFound,           // 404
            ServiceError::ValidationError(items) => RestError::BadRequest(_), // 400
            ServiceError::PermissionDenied => RestError::Unauthorized,        // 401 (NOT 403)
            ServiceError::Conflict(msg) => RestError::Conflict(_),            // 409
            _ => RestError::InternalError(_),                                 // 500
        }
    }
}
```

WICHTIG (D-15-12-Konflikt-Resolution): CONTEXT.md D-15-12 listet "403 (No Admin Privilege)" auf, aber die Why-Klausel sagt "Konsistent mit existing RestError-Mapping". Der existing Code-Mapping ist 401. Die ROADMAP-Success-Criteria sagt ebenfalls 401. Plaene folgen Why-Klausel + ROADMAP (= 401). Die "403"-Erwaehnung in D-15-12 ist ein Transkriptionsfehler in CONTEXT.md.

**TestDeps-Pattern (KEIN existing Analog in member_action.rs — siehe member.rs:413-712 fuer das aktuelle Vorbild)**:

`genossi_service_impl/src/member_action.rs` enthaelt KEINE `TestDeps`/`MockTest*`-Struktur (grep ergibt 0 Treffer). Das relevante Vorbild ist `genossi_service_impl/src/member.rs:413-712` (transfer-recipients-Tests). Plan 02 baut die Test-Infrastruktur basierend auf diesem Vorbild auf — siehe Task 2 inline.

Per-File Mock-Pattern (`mockall::mock!`) statt globaler `#[automock]`, weil:
- Mock-Trait-Signaturen mit `TestTransaction` (Debug + Clone) noetig (genossi_dao::MockTransaction hat KEIN Debug — siehe member.rs:430).
- `gen_service_impl!` verlangt `Debug` auf Transaction-Type.

From Plan 01 (Wave 1 outputs):
- `MembershipAdjustService` trait in `genossi_service/src/membership_adjust.rs` mit `cancel_membership(member_id, willensbekundung_date, context, tx) -> Result<(MemberAction, Member), ServiceError>`
- `validate_willensbekundung_date(date, today) -> Vec<ValidationFailureItem>` in `genossi_service_impl/src/membership_adjust.rs`
- `pub(crate) async fn recalc_dates<Md, Mad, Tx>(...)` Free-Function in `genossi_service_impl/src/member_action.rs`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MembershipAdjustServiceImpl + Process-Const + Imports</name>
  <files>genossi_service_impl/src/membership_adjust.rs</files>
  <read_first>
    - genossi_service_impl/src/membership_adjust.rs (Plan 01 hat es um validate_willensbekundung_date erweitert; Datei pre-existing seit Phase 14)
    - genossi_service_impl/src/member_action.rs (Z. 1-30 als Vorbild — Imports + gen_service_impl)
    - genossi_service_impl/src/macros.rs (gen_service_impl Macro-Definition)
    - .planning/phases/15-service-rest-kuendigung-aufstockung/15-PATTERNS.md Section Pattern 3a + 3b
    - .planning/phases/15-service-rest-kuendigung-aufstockung/15-CONTEXT.md D-15-01, D-15-02, D-15-16
  </read_first>
  <behavior>
    - Test 1: `cargo build -p genossi_service_impl` kompiliert clean nach Hinzufuegen des gen_service_impl-Blocks
    - Test 2: `MembershipAdjustServiceImpl<Deps>` ist Type-Konstruierbar mit den 6 Deps
    - Test 3: Process-Konstanten `CANCEL_PROCESS` und `UPGRADE_PROCESS` sind verfuegbar (UPGRADE_PROCESS wird in Plan 03 genutzt, hier mit angelegt)
  </behavior>
  <action>
    1. Imports am Anfang von `genossi_service_impl/src/membership_adjust.rs` hinzufuegen (nach den existing time-Imports):

```rust
use std::sync::Arc;
use uuid::Uuid;

use async_trait::async_trait;
use genossi_dao::audit_log::AuditLogDao;
use genossi_dao::member::{MemberDao, MemberEntity};
use genossi_dao::member_action::{ActionType, MemberActionDao, MemberActionEntity};
use genossi_dao::TransactionDao;

use genossi_service::member::Member;
use genossi_service::member_action::MemberAction;
use genossi_service::membership_adjust::MembershipAdjustService;
use genossi_service::permission::{Authentication, PermissionService, ADMIN_PRIVILEGE};
use genossi_service::uuid_service::UuidService;
use genossi_service::{ServiceError, ValidationFailureItem};

use crate::gen_service_impl;
```

    2. Process-Konstanten oben im Modul deklarieren (D-15-02):

```rust
const CANCEL_PROCESS: &str = "member-adjust.cancel";
const UPGRADE_PROCESS: &str = "member-adjust.upgrade";
```

    3. `gen_service_impl!`-Block einfuegen (analog `member_action.rs:21-30`, D-15-16):

```rust
gen_service_impl! {
    struct MembershipAdjustServiceImpl: MembershipAdjustService = MembershipAdjustServiceDeps {
        MemberActionDao: MemberActionDao<Transaction = Self::Transaction> = member_action_dao,
        MemberDao: MemberDao<Transaction = Self::Transaction> = member_dao,
        AuditLogDao: AuditLogDao<Transaction = Self::Transaction> = audit_log_dao,
        PermissionService: PermissionService<Context = Self::Context> = permission_service,
        UuidService: UuidService = uuid_service,
        TransactionDao: TransactionDao<Transaction = Self::Transaction> = transaction_dao,
    }
}
```

    4. Skeleton-`async_trait`-impl mit `cancel_membership` als Stub (wird in Task 2 vervollstaendigt), aber `increase_shares` muss auch als Stub erfuellt sein, damit das Trait kompiliert. Andernfalls Compile-Fehler weil Plan 03 noch nicht da ist.

```rust
#[async_trait]
impl<Deps: MembershipAdjustServiceDeps> MembershipAdjustService for MembershipAdjustServiceImpl<Deps> {
    type Context = <Deps as MembershipAdjustServiceDeps>::Context;
    type Transaction = <Deps as MembershipAdjustServiceDeps>::Transaction;

    async fn cancel_membership(
        &self,
        _member_id: Uuid,
        _willensbekundung_date: time::Date,
        _context: Authentication<Self::Context>,
        _tx: Option<Self::Transaction>,
    ) -> Result<(MemberAction, Member), ServiceError> {
        Err(ServiceError::InternalError(Arc::from("cancel_membership not yet implemented — Task 2")))
    }

    async fn increase_shares(
        &self,
        _member_id: Uuid,
        _shares: i32,
        _willensbekundung_date: time::Date,
        _context: Authentication<Self::Context>,
        _tx: Option<Self::Transaction>,
    ) -> Result<(MemberAction, Member), ServiceError> {
        // Wird in Plan 03 ueberschrieben
        Err(ServiceError::InternalError(Arc::from("increase_shares — Plan 03")))
    }
}
```

    Wichtig: Die Stub-Returns werden in Task 2 (cancel_membership) und Plan 03 (increase_shares) ersetzt. Sie sind nur ein temporaerer Compile-Trick, damit das Trait waehrend Wave 2 kompiliert.
  </action>
  <verify>
    <automated>cargo build -p genossi_service_impl 2>&amp;1 | tail -10 &amp;&amp; grep -n "const CANCEL_PROCESS" /home/neosam/programming/rust/projects/genossi3/genossi_service_impl/src/membership_adjust.rs &amp;&amp; grep -n "gen_service_impl" /home/neosam/programming/rust/projects/genossi3/genossi_service_impl/src/membership_adjust.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p genossi_service_impl` exit code 0
    - `grep -c 'const CANCEL_PROCESS: &str = "member-adjust.cancel";' genossi_service_impl/src/membership_adjust.rs` returns 1
    - `grep -c 'const UPGRADE_PROCESS: &str = "member-adjust.upgrade";' genossi_service_impl/src/membership_adjust.rs` returns 1
    - `grep -c 'struct MembershipAdjustServiceImpl: MembershipAdjustService' genossi_service_impl/src/membership_adjust.rs` returns 1
    - `grep -c 'MembershipAdjustServiceDeps' genossi_service_impl/src/membership_adjust.rs` >= 2 (Trait + impl-Bound)
    - `grep -c 'use genossi_service::permission::.*ADMIN_PRIVILEGE' genossi_service_impl/src/membership_adjust.rs` returns 1
  </acceptance_criteria>
  <done>
    Struct + Deps + Process-Konstanten + Stub-Impl kompiliert. Task 2 kann den Body von `cancel_membership` mit Domain-Logik fuellen.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cancel_membership-Body + 4 vollstaendige Service-Unit-Tests</name>
  <files>genossi_service_impl/src/membership_adjust.rs</files>
  <read_first>
    - genossi_service_impl/src/member_action.rs (Z. 289-356 — vollstaendige Service-Method-Sequence als Vorlage; KEIN TestDeps-Pattern darin)
    - genossi_service_impl/src/member.rs (Z. 412-712 — Tests-Modul mit Per-File `mockall::mock!`-Pattern + `TestTransaction`-Struct + `TestDeps`-Struct als VOLLSTAENDIGES Vorbild — verbatim spiegeln)
    - genossi_service_impl/src/audit_macros.rs (Z. 5-36 — audited_create!-Macro-Signatur)
    - genossi_service_impl/src/membership_adjust.rs (eigene Stub-Methode aus Task 1)
    - genossi_service/src/member.rs (Member Domain-Typ Z. 12-38 — `pub current_shares: i32`, `pub exit_date: Option<time::Date>` direkter Feld-Zugriff)
    - genossi_service/src/member_action.rs (MemberAction-Typ + From<&MemberAction> for MemberActionEntity-Impl)
    - .planning/phases/15-service-rest-kuendigung-aufstockung/15-PATTERNS.md Section Pattern 3c + Pattern 5 + Pattern 9
    - .planning/phases/15-service-rest-kuendigung-aufstockung/15-CONTEXT.md D-15-01..08, §specifics (Test-Naming)
  </read_first>
  <behavior>
    Phase-15 cancel_membership Sequence (analog `MemberActionService::create`):
    1. `tx = use_transaction(tx).await?` (use_transaction setzt Tx auf, falls None)
    2. `user_id = current_user_id(context.clone()).await?.unwrap_or("SYSTEM")`
    3. `check_permission(ADMIN_PRIVILEGE, context).await?` (PERM-01)
    4. `today = OffsetDateTime::now_utc().date()` und `validate_willensbekundung_date(willensbekundung_date, today)` -> leerer Vec or ValidationError (PERM-02)
    5. `member_entity = member_dao.find_by_id(member_id, tx.clone()).await?.ok_or(EntityNotFound)?` (CANC-01)
    6. Already-Cancelled-Check: `if member_entity.exit_date.is_some() { return Err(ServiceError::Conflict(Arc::from("member already cancelled"))) }` (D-15-12, HTTP 409)
    7. `effective = compute_effective_date(willensbekundung_date)` (Pure-Function aus Phase 14)
    8. `new_action = MemberAction { id: uuid_service.new_v4().await, member_id, action_type: ActionType::Austritt, date: willensbekundung_date, shares_change: 0, transfer_member_id: None, effective_date: Some(effective.effective_date), comment: None, created: now, deleted: None, version: uuid_service.new_v4().await }` (CANC-03)
    9. `action_entity: MemberActionEntity = (&new_action).into(); audited_create!(self, self.member_action_dao, &action_entity, CANCEL_PROCESS, &user_id, tx)` (AUDT-01, D-15-02)
    10. `crate::member_action::recalc_dates(&*self.member_dao, &*self.member_action_dao, member_id, tx.clone()).await?` (CANC-04, Plan-01-Free-Function)
    11. `updated_entity = member_dao.find_by_id(member_id, tx.clone()).await?.ok_or(EntityNotFound)?` (re-read fuer Response, da exit_date sich geaendert hat)
    12. `transaction_dao.commit(tx).await?`
    13. `Ok((new_action, Member::from(&updated_entity)))`

    Unit-Tests (alle 4 sind vollstaendig auszuformulieren — KEINE Platzhalter-Kommentare):
    - `test_cancel_membership_happy_path_h1`: willensbekundung = `today.replace_month(March)?.replace_day(15)?` (im today.year(), H1-Bereich) -> action.effective_date = `today.year()-12-31`
    - `test_cancel_membership_happy_path_h2`: willensbekundung = `today.replace_month(August)?.replace_day(15)?` (im today.year(), H2-Bereich) -> action.effective_date = `(today.year()+1)-12-31`
    - `test_cancel_membership_permission_denied`: MockPermissionService gibt PermissionDenied zurueck -> Service-Methode propagiert
    - `test_cancel_membership_already_cancelled`: Mock-Member hat exit_date=Some(...) -> Conflict
  </behavior>
  <action>
    1. Bisherigen `cancel_membership`-Stub durch die volle Implementation ersetzen:

```rust
async fn cancel_membership(
    &self,
    member_id: Uuid,
    willensbekundung_date: time::Date,
    context: Authentication<Self::Context>,
    tx: Option<Self::Transaction>,
) -> Result<(MemberAction, Member), ServiceError> {
    let tx = self.transaction_dao.use_transaction(tx).await?;

    let user_id = self
        .permission_service
        .current_user_id(context.clone())
        .await?
        .unwrap_or_else(|| "SYSTEM".to_string());

    self.permission_service
        .check_permission(ADMIN_PRIVILEGE, context)
        .await?;

    // PERM-02: Datum-Bounds-Validierung (D-15-05..08)
    let today = time::OffsetDateTime::now_utc().date();
    let validation_errors = validate_willensbekundung_date(willensbekundung_date, today);
    if !validation_errors.is_empty() {
        return Err(ServiceError::ValidationError(validation_errors));
    }

    // CANC-01: Member existence
    let member_entity = self
        .member_dao
        .find_by_id(member_id, tx.clone())
        .await?
        .ok_or(ServiceError::EntityNotFound(member_id))?;

    // D-15-12 / Roadmap: Already-Cancelled -> HTTP 409
    if member_entity.exit_date.is_some() {
        return Err(ServiceError::Conflict(Arc::from("member already cancelled")));
    }

    // CANC-02 / D-14-04..07: H1/H2-Stichtag berechnen (Phase-14-Pure-Function)
    let effective = compute_effective_date(willensbekundung_date);

    let now = time::OffsetDateTime::now_utc();
    let new_action = MemberAction {
        id: self.uuid_service.new_v4().await,
        member_id,
        action_type: ActionType::Austritt,
        date: willensbekundung_date,
        shares_change: 0, // CANC-03
        transfer_member_id: None,
        effective_date: Some(effective.effective_date),
        comment: None,
        created: time::PrimitiveDateTime::new(now.date(), now.time()),
        deleted: None,
        version: self.uuid_service.new_v4().await,
    };

    // AUDT-01 / D-15-01 / D-15-02: audited_create! statt direkter DAO-Call
    let action_entity: MemberActionEntity = (&new_action).into();
    crate::audited_create!(
        self,
        self.member_action_dao,
        &action_entity,
        CANCEL_PROCESS,
        &user_id,
        tx
    );

    // CANC-04: exit_date via recalc_dates (KEINE direkte Member.exit_date-Mutation)
    crate::member_action::recalc_dates(
        &*self.member_dao,
        &*self.member_action_dao,
        member_id,
        tx.clone(),
    )
    .await?;

    // Re-Read fuer Response — recalc_dates hat exit_date gesetzt
    let updated_entity = self
        .member_dao
        .find_by_id(member_id, tx.clone())
        .await?
        .ok_or(ServiceError::EntityNotFound(member_id))?;

    self.transaction_dao.commit(tx).await?;

    Ok((new_action, Member::from(&updated_entity)))
}
```

    2. Tests-Modul `#[cfg(test)] mod service_tests { ... }` am Ende der Datei hinzufuegen.

    **WICHTIG**: Das Pattern aus `genossi_service_impl/src/member.rs:412-712` ist das verbindliche Vorbild — KEINE globalen `MockMemberDao` etc. verwenden, sondern Per-File `mockall::mock!` mit `TestTransaction`-Struct.

    Vollstaendiges Test-Modul (copy-paste-executable, mit allen 4 Tests):

```rust
#[cfg(test)]
mod service_tests {
    //! Service-Tests fuer cancel_membership (Plan 15-02).
    //!
    //! Per-File-Mock-Pattern statt globalem `#[automock]`, weil `gen_service_impl!`
    //! `Debug` auf Transaction-Type verlangt; genossi_dao::MockTransaction hat KEIN
    //! Debug-Derive. Vorbild: genossi_service_impl/src/member.rs:412-712.

    use super::*;
    use async_trait::async_trait;
    use genossi_dao::audit_log::{AuditLogDao, AuditLogEntry, AuditQueryFilter};
    use genossi_dao::member::{MemberDao, MemberEntity, MemberStatus, Salutation};
    use genossi_dao::member_action::{MemberActionDao, MemberActionEntity};
    use genossi_dao::{DaoError, Transaction, TransactionDao};
    use genossi_service::permission::{MockContext, PermissionService};
    use mockall::mock;

    /// Test-local Transaction with Debug — MockTransaction in genossi_dao hat kein Debug,
    /// aber gen_service_impl! verlangt es.
    #[derive(Clone, Debug)]
    pub struct TestTransaction;

    #[async_trait]
    impl Transaction for TestTransaction {
        async fn begin(&mut self) -> Result<(), DaoError> { Ok(()) }
        async fn commit(self) -> Result<(), DaoError> { Ok(()) }
        async fn rollback(self) -> Result<(), DaoError> { Ok(()) }
    }

    mock! {
        pub TestTxDao {}
        #[async_trait]
        impl TransactionDao for TestTxDao {
            type Transaction = TestTransaction;
            async fn transaction(&self) -> Result<TestTransaction, DaoError>;
            async fn use_transaction(&self, tx: Option<TestTransaction>) -> Result<TestTransaction, DaoError>;
            async fn commit(&self, tx: TestTransaction) -> Result<(), DaoError>;
        }
    }

    mock! {
        pub TestMemberDao {}
        #[async_trait]
        impl MemberDao for TestMemberDao {
            type Transaction = TestTransaction;
            async fn dump_all(&self, tx: TestTransaction) -> Result<Arc<[MemberEntity]>, DaoError>;
            async fn create(&self, entity: &MemberEntity, process: &str, tx: TestTransaction) -> Result<(), DaoError>;
            async fn update(&self, entity: &MemberEntity, process: &str, tx: TestTransaction) -> Result<(), DaoError>;
            async fn all(&self, tx: TestTransaction) -> Result<Arc<[MemberEntity]>, DaoError>;
            async fn find_by_id(&self, id: Uuid, tx: TestTransaction) -> Result<Option<MemberEntity>, DaoError>;
            async fn update_migrated(&self, id: Uuid, migrated: bool, tx: TestTransaction) -> Result<(), DaoError>;
            async fn update_dates(&self, id: Uuid, join_date: time::Date, exit_date: Option<time::Date>, tx: TestTransaction) -> Result<(), DaoError>;
            async fn find_by_member_number(&self, member_number: i64, tx: TestTransaction) -> Result<Option<MemberEntity>, DaoError>;
            async fn count_active(&self, today: time::Date, tx: TestTransaction) -> Result<u64, DaoError>;
            async fn next_member_number(&self, tx: TestTransaction) -> Result<i64, DaoError>;
        }
    }

    mock! {
        pub TestMemberActionDao {}
        #[async_trait]
        impl MemberActionDao for TestMemberActionDao {
            type Transaction = TestTransaction;
            async fn dump_all(&self, tx: TestTransaction) -> Result<Arc<[MemberActionEntity]>, DaoError>;
            async fn create(&self, entity: &MemberActionEntity, process: &str, tx: TestTransaction) -> Result<(), DaoError>;
            async fn update(&self, entity: &MemberActionEntity, process: &str, tx: TestTransaction) -> Result<(), DaoError>;
            async fn all(&self, tx: TestTransaction) -> Result<Arc<[MemberActionEntity]>, DaoError>;
            async fn find_by_id(&self, id: Uuid, tx: TestTransaction) -> Result<Option<MemberActionEntity>, DaoError>;
            async fn find_by_member_id(&self, member_id: Uuid, tx: TestTransaction) -> Result<Arc<[MemberActionEntity]>, DaoError>;
        }
    }

    mock! {
        pub TestAuditLogDao {}
        #[async_trait]
        impl AuditLogDao for TestAuditLogDao {
            type Transaction = TestTransaction;
            async fn create_entries(&self, entries: &[AuditLogEntry], tx: TestTransaction) -> Result<(), DaoError>;
            async fn get_latest_hash(&self, tx: TestTransaction) -> Result<Option<String>, DaoError>;
            async fn get_by_entity(&self, entity_type: &str, entity_id: Uuid, tx: TestTransaction) -> Result<Arc<[AuditLogEntry]>, DaoError>;
            async fn get_all_ordered(&self, tx: TestTransaction) -> Result<Arc<[AuditLogEntry]>, DaoError>;
            async fn query(&self, filter: AuditQueryFilter, limit: i64, offset: i64, tx: TestTransaction) -> Result<Arc<[AuditLogEntry]>, DaoError>;
            async fn count(&self, filter: AuditQueryFilter, tx: TestTransaction) -> Result<i64, DaoError>;
        }
    }

    mock! {
        pub TestPermissionService {}
        #[async_trait]
        impl PermissionService for TestPermissionService {
            type Context = MockContext;
            async fn check_permission(&self, privilege: &str, context: Authentication<MockContext>) -> Result<(), ServiceError>;
            async fn current_user_id(&self, context: Authentication<MockContext>) -> Result<Option<String>, ServiceError>;
            async fn get_all_users(&self, context: Authentication<MockContext>) -> Result<Arc<[genossi_service::auth_types::UserResponseTO]>, ServiceError>;
            async fn create_user(&self, user: genossi_service::auth_types::UserTO, context: Authentication<MockContext>) -> Result<(), ServiceError>;
            async fn delete_user(&self, username: String, context: Authentication<MockContext>) -> Result<(), ServiceError>;
            async fn get_all_roles(&self, context: Authentication<MockContext>) -> Result<Arc<[genossi_service::auth_types::RoleResponseTO]>, ServiceError>;
            async fn create_role(&self, role: genossi_service::auth_types::RoleTO, context: Authentication<MockContext>) -> Result<(), ServiceError>;
            async fn delete_role(&self, role_name: String, context: Authentication<MockContext>) -> Result<(), ServiceError>;
            async fn get_all_privileges(&self, context: Authentication<MockContext>) -> Result<Arc<[genossi_service::auth_types::PrivilegeResponseTO]>, ServiceError>;
            async fn create_privilege(&self, privilege: genossi_service::auth_types::PrivilegeTO, context: Authentication<MockContext>) -> Result<(), ServiceError>;
            async fn delete_privilege(&self, privilege_name: String, context: Authentication<MockContext>) -> Result<(), ServiceError>;
            async fn assign_user_role(&self, user_role: genossi_service::auth_types::UserRole, context: Authentication<MockContext>) -> Result<(), ServiceError>;
            async fn remove_user_role(&self, user_role: genossi_service::auth_types::UserRole, context: Authentication<MockContext>) -> Result<(), ServiceError>;
            async fn get_user_roles(&self, username: String, context: Authentication<MockContext>) -> Result<Arc<[genossi_service::auth_types::RoleResponseTO]>, ServiceError>;
            async fn assign_role_privilege(&self, role_privilege: genossi_service::auth_types::RolePrivilege, context: Authentication<MockContext>) -> Result<(), ServiceError>;
            async fn remove_role_privilege(&self, role_privilege: genossi_service::auth_types::RolePrivilege, context: Authentication<MockContext>) -> Result<(), ServiceError>;
            async fn get_role_privileges(&self, role_name: String, context: Authentication<MockContext>) -> Result<Arc<[genossi_service::auth_types::PrivilegeResponseTO]>, ServiceError>;
            async fn get_user_privileges(&self, username: String, context: Authentication<MockContext>) -> Result<Arc<[genossi_service::auth_types::PrivilegeResponseTO]>, ServiceError>;
            async fn has_claims(&self, context: &MockContext) -> Result<bool, ServiceError>;
        }
    }

    /// Static UUID-Service fuer Tests — neue UUIDs jedes Mal.
    #[derive(Clone)]
    struct StaticUuidService;
    #[async_trait]
    impl UuidService for StaticUuidService {
        async fn new_v4(&self) -> Uuid { Uuid::new_v4() }
    }

    /// TestDeps wires the local mocks as associated types.
    pub struct TestDeps;
    impl MembershipAdjustServiceDeps for TestDeps {
        type Context = MockContext;
        type Transaction = TestTransaction;
        type MemberActionDao = MockTestMemberActionDao;
        type MemberDao = MockTestMemberDao;
        type AuditLogDao = MockTestAuditLogDao;
        type PermissionService = MockTestPermissionService;
        type UuidService = StaticUuidService;
        type TransactionDao = MockTestTxDao;
    }

    fn sample_member_entity(id: Uuid, exit_date: Option<time::Date>) -> MemberEntity {
        let join = time::Date::from_calendar_date(2020, time::Month::January, 1).unwrap();
        MemberEntity {
            id,
            member_number: 1001,
            first_name: Arc::from("Klaus"),
            last_name: Arc::from("Kuendigung"),
            salutation: Some(Salutation::Herr),
            title: None, email: None, company: None, comment: None,
            street: None, house_number: None, postal_code: None, city: None,
            join_date: join,
            shares_at_joining: 1, current_shares: 1, current_balance: 0, action_count: 0,
            migrated: false, exit_date,
            bank_account: None, status: MemberStatus::Normal,
            created: time::PrimitiveDateTime::new(join, time::Time::MIDNIGHT),
            deleted: None,
            version: Uuid::new_v4(),
        }
    }

    fn setup_tx_dao() -> MockTestTxDao {
        let mut tx_dao = MockTestTxDao::new();
        tx_dao.expect_use_transaction().returning(|_| Ok(TestTransaction));
        tx_dao.expect_commit().returning(|_| Ok(()));
        tx_dao
    }

    fn build_service(
        member_dao: MockTestMemberDao,
        member_action_dao: MockTestMemberActionDao,
        audit_log_dao: MockTestAuditLogDao,
        permission_service: MockTestPermissionService,
        tx_dao: MockTestTxDao,
    ) -> MembershipAdjustServiceImpl<TestDeps> {
        MembershipAdjustServiceImpl {
            member_action_dao: Arc::new(member_action_dao),
            member_dao: Arc::new(member_dao),
            audit_log_dao: Arc::new(audit_log_dao),
            permission_service: Arc::new(permission_service),
            uuid_service: Arc::new(StaticUuidService),
            transaction_dao: Arc::new(tx_dao),
        }
    }

    // ---------- Test 1: Happy-Path H1 ----------
    #[tokio::test]
    async fn test_cancel_membership_happy_path_h1() {
        // Datum-Fragility-Fix (BLOCKER 1): leite das Test-Datum relativ zu today() ab,
        // damit der Test nicht beim Year-Rollover bricht. H1 = Januar..Juni desselben Jahres.
        let today = time::OffsetDateTime::now_utc().date();
        let willensbekundung = today
            .replace_month(time::Month::March).expect("March in today.year() exists")
            .replace_day(15).expect("Day 15 always valid");
        let expected_effective = time::Date::from_calendar_date(
            today.year(), time::Month::December, 31
        ).expect("year-12-31 always valid");

        let member_id = Uuid::new_v4();

        let mut member_dao = MockTestMemberDao::new();
        let mid = member_id;
        // find_by_id wird 2x aufgerufen: 1x fuer existence-check, 1x fuer re-read nach recalc_dates.
        member_dao.expect_find_by_id()
            .times(2)
            .returning(move |_, _| {
                let mut m = sample_member_entity(mid, None);
                // 2. Aufruf: exit_date wurde von recalc_dates gesetzt (wir simulieren das,
                // weil unsere Mock-update_dates KEIN State haelt; assertion fuer member.exit_date
                // greift hier nicht — Stack-Konsistenz wird in E2E (Plan 04) verifiziert).
                m.exit_date = None; // Mock-only: re-read returns same state, OK fuer Action-Assertions.
                Ok(Some(m))
            });
        // recalc_dates ruft update_dates auf
        member_dao.expect_update_dates()
            .returning(|_, _, _, _| Ok(()));

        let mut member_action_dao = MockTestMemberActionDao::new();
        // recalc_dates ruft find_by_member_id auf
        member_action_dao.expect_find_by_member_id()
            .returning(|_, _| Ok(Arc::from(Vec::<MemberActionEntity>::new())));
        // audited_create! ruft create() auf mit Process="member-adjust.cancel"
        member_action_dao.expect_create()
            .withf(|_entity, process, _tx| process == "member-adjust.cancel")
            .times(1)
            .returning(|_, _, _| Ok(()));

        let mut audit_log_dao = MockTestAuditLogDao::new();
        audit_log_dao.expect_get_latest_hash().returning(|_| Ok(None));
        audit_log_dao.expect_create_entries().returning(|_, _| Ok(()));

        let mut permission_service = MockTestPermissionService::new();
        permission_service.expect_current_user_id()
            .returning(|_| Ok(Some("admin".to_string())));
        permission_service.expect_check_permission()
            .withf(|priv_, _| priv_ == "admin")
            .returning(|_, _| Ok(()));

        let service = build_service(
            member_dao, member_action_dao, audit_log_dao,
            permission_service, setup_tx_dao(),
        );

        let result = service.cancel_membership(
            member_id, willensbekundung, Authentication::Full, None
        ).await;

        let (action, _member) = result.expect("cancel_membership should succeed");
        assert_eq!(action.action_type, ActionType::Austritt);
        assert_eq!(action.shares_change, 0);
        assert_eq!(action.date, willensbekundung);
        assert_eq!(action.effective_date, Some(expected_effective));
        assert_eq!(action.transfer_member_id, None);
    }

    // ---------- Test 2: Happy-Path H2 ----------
    #[tokio::test]
    async fn test_cancel_membership_happy_path_h2() {
        let today = time::OffsetDateTime::now_utc().date();
        // H2 = August desselben Jahres -> effective = next-year-12-31
        let willensbekundung = today
            .replace_month(time::Month::August).expect("August in today.year() exists")
            .replace_day(15).expect("Day 15 always valid");
        let expected_effective = time::Date::from_calendar_date(
            today.year() + 1, time::Month::December, 31
        ).expect("(year+1)-12-31 always valid");

        let member_id = Uuid::new_v4();

        let mut member_dao = MockTestMemberDao::new();
        let mid = member_id;
        member_dao.expect_find_by_id()
            .times(2)
            .returning(move |_, _| Ok(Some(sample_member_entity(mid, None))));
        member_dao.expect_update_dates().returning(|_, _, _, _| Ok(()));

        let mut member_action_dao = MockTestMemberActionDao::new();
        member_action_dao.expect_find_by_member_id()
            .returning(|_, _| Ok(Arc::from(Vec::<MemberActionEntity>::new())));
        member_action_dao.expect_create()
            .withf(|_, process, _| process == "member-adjust.cancel")
            .times(1)
            .returning(|_, _, _| Ok(()));

        let mut audit_log_dao = MockTestAuditLogDao::new();
        audit_log_dao.expect_get_latest_hash().returning(|_| Ok(None));
        audit_log_dao.expect_create_entries().returning(|_, _| Ok(()));

        let mut permission_service = MockTestPermissionService::new();
        permission_service.expect_current_user_id()
            .returning(|_| Ok(Some("admin".to_string())));
        permission_service.expect_check_permission()
            .returning(|_, _| Ok(()));

        let service = build_service(
            member_dao, member_action_dao, audit_log_dao,
            permission_service, setup_tx_dao(),
        );

        let result = service.cancel_membership(
            member_id, willensbekundung, Authentication::Full, None
        ).await;

        let (action, _member) = result.expect("cancel_membership H2 should succeed");
        assert_eq!(action.action_type, ActionType::Austritt);
        assert_eq!(action.effective_date, Some(expected_effective));
    }

    // ---------- Test 3: Permission denied ----------
    #[tokio::test]
    async fn test_cancel_membership_permission_denied() {
        let today = time::OffsetDateTime::now_utc().date();
        let willensbekundung = today
            .replace_month(time::Month::March).unwrap()
            .replace_day(15).unwrap();

        // Keine DAO-Calls erwartet — Permission-Denied bricht VOR DAO-Touches ab.
        let member_dao = MockTestMemberDao::new();
        let member_action_dao = MockTestMemberActionDao::new();
        let audit_log_dao = MockTestAuditLogDao::new();

        let mut permission_service = MockTestPermissionService::new();
        permission_service.expect_current_user_id()
            .returning(|_| Ok(Some("user".to_string())));
        permission_service.expect_check_permission()
            .returning(|_, _| Err(ServiceError::PermissionDenied));

        let service = build_service(
            member_dao, member_action_dao, audit_log_dao,
            permission_service, setup_tx_dao(),
        );

        let result = service.cancel_membership(
            Uuid::new_v4(), willensbekundung, Authentication::Full, None
        ).await;

        assert!(
            matches!(result, Err(ServiceError::PermissionDenied)),
            "expected PermissionDenied (mapped to HTTP 401 via genossi_rest/src/lib.rs:115), got {:?}",
            result
        );
    }

    // ---------- Test 4: Already-Cancelled ----------
    #[tokio::test]
    async fn test_cancel_membership_already_cancelled() {
        let today = time::OffsetDateTime::now_utc().date();
        let willensbekundung = today
            .replace_month(time::Month::March).unwrap()
            .replace_day(15).unwrap();
        let already_exit = time::Date::from_calendar_date(
            today.year(), time::Month::December, 31
        ).unwrap();

        let member_id = Uuid::new_v4();

        let mut member_dao = MockTestMemberDao::new();
        let mid = member_id;
        member_dao.expect_find_by_id()
            .times(1)  // nur 1x — Conflict bricht vor recalc_dates ab
            .returning(move |_, _| Ok(Some(sample_member_entity(mid, Some(already_exit)))));

        // KEINE audit/create-Calls erwartet
        let member_action_dao = MockTestMemberActionDao::new();
        let audit_log_dao = MockTestAuditLogDao::new();

        let mut permission_service = MockTestPermissionService::new();
        permission_service.expect_current_user_id()
            .returning(|_| Ok(Some("admin".to_string())));
        permission_service.expect_check_permission()
            .returning(|_, _| Ok(()));

        let service = build_service(
            member_dao, member_action_dao, audit_log_dao,
            permission_service, setup_tx_dao(),
        );

        let result = service.cancel_membership(
            member_id, willensbekundung, Authentication::Full, None
        ).await;

        match result {
            Err(ServiceError::Conflict(msg)) => {
                assert!(msg.contains("already cancelled"), "unexpected msg: {}", msg);
            }
            other => panic!("Expected Conflict, got {:?}", other),
        }
    }
}
```

    Notiz fuer den Executor:
    - `mockall::mock!`-Bloecke benoetigen die exakte Trait-Signatur. Falls eine Method-Signatur in einem DAO-Trait sich aendert, muss der mock!-Block angepasst werden.
    - Test 1 setzt im Mock `m.exit_date = None` auf der 2. find_by_id-Antwort, weil unsere MockMemberDao keinen State zwischen Calls haelt. Die Konsistenz (exit_date wird tatsaechlich gesetzt) wird in Plan 04 E2E mit real-Datenbank verifiziert.
    - StaticUuidService gibt frische UUIDs zurueck — die Tests pruefen Action-Type/Date/Shares, nicht UUID-Werte.
    - Alle Tests folgen der HTTP-401-Konvention fuer PermissionDenied (siehe must_haves).
  </action>
  <verify>
    <automated>cargo test -p genossi_service_impl --lib membership_adjust::service_tests 2>&amp;1 | tail -25 &amp;&amp; grep -cE 'audited_create!\s*\([\s\S]{0,200}CANCEL_PROCESS' /home/neosam/programming/rust/projects/genossi3/genossi_service_impl/src/membership_adjust.rs &amp;&amp; bash -c "grep -v '^//' /home/neosam/programming/rust/projects/genossi3/genossi_service_impl/src/membership_adjust.rs | grep -v '^ \\*' | grep -cE 'self\\.member_action_dao\\.create\\('"</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p genossi_service_impl --lib membership_adjust::service_tests::test_cancel_membership_happy_path_h1` exits 0
    - `cargo test -p genossi_service_impl --lib membership_adjust::service_tests::test_cancel_membership_happy_path_h2` exits 0
    - `cargo test -p genossi_service_impl --lib membership_adjust::service_tests::test_cancel_membership_permission_denied` exits 0
    - `cargo test -p genossi_service_impl --lib membership_adjust::service_tests::test_cancel_membership_already_cancelled` exits 0
    - `grep -c 'ActionType::Austritt' genossi_service_impl/src/membership_adjust.rs` >= 1 (Austritts-Action im Service-Body)
    - `grep -c 'shares_change: 0' genossi_service_impl/src/membership_adjust.rs` >= 1 (CANC-03)
    - `grep -c 'audited_create!' genossi_service_impl/src/membership_adjust.rs` >= 1 (AUDT-01)
    - AUDT-01-Grep-Gate: `bash -c "grep -v '^//' genossi_service_impl/src/membership_adjust.rs | grep -v '^ \\*' | grep -E 'self\\.member_action_dao\\.create\\(' | wc -l"` == 0 (kein direkter DAO-create-Call ausserhalb der Macros)
    - AUDT-01-Grep-Gate: `bash -c "grep -v '^//' genossi_service_impl/src/membership_adjust.rs | grep -v '^ \\*' | grep -E 'self\\.member_dao\\.update\\(' | wc -l"` == 0 (kein direkter member_dao.update-Call; nur ueber audited_update! erlaubt — relevant fuer Plan 03)
    - CANC-05-Grep-Gate: `grep -c 'ActionType::Verkauf\|RepaymentEntry' genossi_service_impl/src/membership_adjust.rs` returns 0 (KEINE Verkauf-Action / RepaymentEntry direkt in Phase 15)
    - CANC-04-Grep-Gate: `grep -cE 'member_dao\\.update_dates|member_dao\\.update_migrated' genossi_service_impl/src/membership_adjust.rs` returns 0 (KEINE direkte exit_date-Mutation im Service-Body; recalc_dates-Free-Function-Aufruf ueber `crate::member_action::recalc_dates` ruft `member_dao.update_dates` INTERN auf — das ist OK)
    - `grep -c 'crate::member_action::recalc_dates' genossi_service_impl/src/membership_adjust.rs` >= 1 (CANC-04 via Plan-01-Free-Function)
  </acceptance_criteria>
  <done>
    `cancel_membership` ist atomar (use_transaction -> commit), audit-compliant, permission-protected. 4 Service-Unit-Tests gruen (alle 4 vollstaendig copy-paste-executable, keine Platzhalter). HTTP-401 fuer PermissionDenied dokumentiert in must_haves. Wave 3 (Plan 03 increase_shares) kann die `gen_service_impl!`-Struct + Process-Konstanten weiter nutzen.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REST -> Service (cancel) | Member-ID + willensbekundung_date kommen aus HTTP-Layer als untrusted Input |
| Service -> DAO | atomare Transaction (alle Writes via audited_*! Macros, kein Direct-DAO-Bypass) |
| Service -> Audit-Log | Process-String `member-adjust.cancel` ist constant; user_id stammt aus PermissionService.current_user_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-01 | Spoofing | check_permission(ADMIN_PRIVILEGE) | mitigate | PERM-01: Erste DAO-touchende Operation ist `check_permission(ADMIN_PRIVILEGE, context).await?` — Test `test_cancel_membership_permission_denied` verifiziert dass Fehler propagiert (HTTP 401 via globalem Mapping in genossi_rest/src/lib.rs:115). |
| T-15-02-02 | Tampering | audited_create! bypass | mitigate | AUDT-01-Grep-Gate als Acceptance-Kriterium: `grep -E 'self\.member_action_dao\.create\('` muss 0 sein. Macro hat hardkodiertes `$dao.create(...)` -> kein Bypass moeglich. |
| T-15-02-03 | Repudiation | hash-chain consistency | mitigate | `audited_create!` lese `get_latest_hash` und schreibe `build_create_entries` mit `prev_hash` als Link. Plan-04-E2E `/api/audit/verify` verifiziert end-to-end. |
| T-15-02-04 | Information Disclosure | error message leak | accept | `ServiceError::Conflict(Arc::from("member already cancelled"))` enthaelt keinen Member-ID oder Audit-Hash. Generischer Text fuer Frontend. |
| T-15-02-05 | Denial of Service | recalc_dates loop | mitigate | recalc_dates (Plan-01-Free-Function) ist O(n) auf MemberActions-Liste. Pro Mitglied max. < 100 Actions im Praxis-Use-Case (DAO `find_by_member_id` ist indiziert). Akzeptable Komplexitaet. |
| T-15-02-06 | Elevation of Privilege | already-cancelled re-cancel | mitigate | Member.exit_date IS NOT NULL -> `ServiceError::Conflict` vor audited_create!. Kein doppelter Austritts-Audit-Eintrag -> Hash-Chain bleibt konsistent. |
| T-15-02-07 | Tampering | direct Member.exit_date mutation | mitigate | CANC-04-Grep-Gate als Acceptance: `member_dao.update_dates`/`member_dao.update_migrated` darf NICHT im Service-Body `membership_adjust.rs` aufgerufen werden — nur via `recalc_dates`-Free-Function in `member_action.rs`. |
| T-15-02-08 | Tampering | direct Verkauf-Action / RepaymentEntry creation | mitigate | CANC-05-Grep-Gate: `ActionType::Verkauf` und `RepaymentEntry` duerfen NICHT in `membership_adjust.rs` vorkommen. v1.1-PaidOut-Cascade ist Single-Source-of-Truth. |
| T-15-02-09 | Information Disclosure | SQL injection via UUID | accept | sqlx-Parameterized-Queries (DAO-Layer); Uuid-Typ ist nicht string-konkateniert. Kein neuer SQL-Pfad in Phase 15. |
</threat_model>

<verification>
- `cargo build -p genossi_service_impl` exits 0
- `cargo test -p genossi_service_impl --lib membership_adjust::service_tests` exits 0 (alle 4 Service-Tests gruen)
- AUDT-01-Grep-Gate (direkt-create/update outside Macros): 0 Hits in `genossi_service_impl/src/membership_adjust.rs`
- CANC-04/CANC-05-Grep-Gates erfuellt (siehe acceptance_criteria)
- Re-Read von Member nach recalc_dates ist im Code (Response zeigt aktuelles exit_date)
- HTTP 401 fuer PermissionDenied per global Mapping (genossi_rest/src/lib.rs:115); Test 3 verifiziert Service-Layer-Propagation
</verification>

<success_criteria>
- 4 Service-Unit-Tests gruen (alle 4 vollstaendig ausformuliert, keine Platzhalter-Kommentare)
- Phase-14-`compute_effective_date` wird verbatim aus dem Service aufgerufen — H1/H2-Korrektheit fliesst durch
- Plan-01-`validate_willensbekundung_date` wird verbatim aus dem Service aufgerufen — PERM-02-Korrektheit fliesst durch
- Plan-01-`recalc_dates`-Free-Function wird verbatim aus dem Service aufgerufen — CANC-04-Konsistenz fliesst durch
- Test-Daten sind year-rollover-safe (relativ zu `OffsetDateTime::now_utc().date()`)
- `gen_service_impl!`-Block kompiliert; Wave 3 (Plan 03) muss nur `increase_shares` ueberschreiben + 4 Tests ergaenzen
</success_criteria>

<output>
After completion, create `.planning/phases/15-service-rest-kuendigung-aufstockung/15-02-SUMMARY.md` per template at `.claude/get-shit-done/templates/summary.md`.
</output>
