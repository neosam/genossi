---
phase: 15-service-rest-kuendigung-aufstockung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - genossi_service/src/membership_adjust.rs
  - genossi_service/src/lib.rs
  - genossi_service_impl/src/membership_adjust.rs
  - genossi_service_impl/src/member_action.rs
autonomous: true
requirements: [PERM-02]
tags: [rust, axum, service-trait, validation, refactor, pure-function]

must_haves:
  truths:
    - "MembershipAdjustService trait existiert in genossi_service/src/membership_adjust.rs mit cancel_membership + increase_shares (D-15-13, D-15-14, D-15-15)"
    - "validate_willensbekundung_date Pure-Function ist als pub(crate) Free-Function in genossi_service_impl/src/membership_adjust.rs definiert (D-15-05, D-15-07)"
    - "recalc_dates ist zu pub(crate) async Free-Function in genossi_service_impl/src/member_action.rs refaktoriert; bestehende Methode delegiert (D-15-04)"
    - "Mindestens 6 Edge-Case-Unit-Tests fuer validate_willensbekundung_date sind gruen (Vorjahr, aktuelles Jahr, naechstes Jahr, uebernaechstes Jahr, Schaltjahr, today=31.12.)"
    - "cargo build -p genossi_service && cargo build -p genossi_service_impl kompiliert clean"
    - "Bestehende MemberActionService-Tests (recalc_dates-Konsumenten) bleiben gruen — kein Behavior-Change"
  artifacts:
    - path: "genossi_service/src/membership_adjust.rs"
      provides: "MembershipAdjustService trait + Mock"
      contains: "pub trait MembershipAdjustService"
    - path: "genossi_service/src/lib.rs"
      provides: "Modul-Registrierung membership_adjust"
      contains: "pub mod membership_adjust;"
    - path: "genossi_service_impl/src/membership_adjust.rs"
      provides: "validate_willensbekundung_date Pure-Function + 6 Edge-Case-Tests"
      contains: "pub(crate) fn validate_willensbekundung_date"
    - path: "genossi_service_impl/src/member_action.rs"
      provides: "recalc_dates als Free-Function + Delegations-Wrapper"
      contains: "pub(crate) async fn recalc_dates"
  key_links:
    - from: "genossi_service_impl/src/membership_adjust.rs"
      to: "genossi_service/src/membership_adjust.rs::MembershipAdjustService"
      via: "use im Service-Impl (Wave 2/3)"
      pattern: "use genossi_service::membership_adjust::MembershipAdjustService"
    - from: "genossi_service_impl/src/member_action.rs"
      to: "neue Free-Function recalc_dates"
      via: "delegierender Wrapper auf MemberActionServiceImpl"
      pattern: "recalc_dates\\(&\\*self\\.member_dao"
---

<objective>
Foundation fuer Phase 15 schaffen — bevor `cancel_membership` und `increase_shares` implementiert werden koennen.

Purpose:
- Definiert das Trait `MembershipAdjustService` mit den zwei Phase-15-Methoden (Phase 16/17 ergaenzen Methoden inkrementell, D-15-13).
- Etabliert die Pure-Function `validate_willensbekundung_date` als Server-Layer-Datum-Validierung (PERM-02).
- Refaktoriert `recalc_dates` zur Free-Function, damit `MembershipAdjustServiceImpl::cancel_membership` (Plan 02) sie ohne `MemberActionServiceImpl`-Coupling aufrufen kann (D-15-04).

Output:
- `genossi_service/src/membership_adjust.rs` (neue Datei)
- Aktualisiertes `genossi_service/src/lib.rs` (Modul-Registrierung)
- Erweitertes `genossi_service_impl/src/membership_adjust.rs` (Pure-Function + Tests)
- Refaktoriertes `genossi_service_impl/src/member_action.rs` (Free-Function `recalc_dates` + Wrapper)
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md

VCS: this project uses jj (Jujutsu). The executor should NOT run git commands directly; rely on GSD's commit step which already wraps jj-aware commits.
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-service-rest-kuendigung-aufstockung/15-CONTEXT.md
@.planning/phases/15-service-rest-kuendigung-aufstockung/15-PATTERNS.md
@CLAUDE.md

<interfaces>
Existing Code Contracts the executor needs verbatim — extract from codebase.

From `genossi_service/src/permission.rs:14-46`:
```rust
pub enum Authentication<Context> { Full, Context(Context) }
pub const ADMIN_PRIVILEGE: &str = "admin";

#[automock(type Context=();)]
#[async_trait]
pub trait PermissionService {
    type Context: Clone + Debug + Send + Sync + ClaimContext + 'static;
    async fn check_permission(&self, privilege: &str, context: Authentication<Self::Context>) -> Result<(), ServiceError>;
    async fn current_user_id(&self, context: Authentication<Self::Context>) -> Result<Option<String>, ServiceError>;
}
```

From `genossi_service/src/member_action.rs:13-26` (Domain-Typ):
```rust
pub struct MemberAction {
    pub id: Uuid,
    pub member_id: Uuid,
    pub action_type: ActionType,
    pub date: time::Date,
    pub shares_change: i32,
    pub transfer_member_id: Option<Uuid>,
    pub effective_date: Option<time::Date>,
    pub comment: Option<Arc<str>>,
    pub created: time::PrimitiveDateTime,
    pub deleted: Option<time::PrimitiveDateTime>,
    pub version: Uuid,
}
```

From `genossi_dao/src/member_action.rs` (ActionType-Enum):
```rust
pub enum ActionType {
    Eintritt, Austritt, Todesfall, Aufstockung,
    Verkauf, UebertragungEmpfang, UebertragungAbgabe, Note,
}
```

From `genossi_dao/src/member.rs:69-95` (MemberEntity-Auszug):
```rust
pub struct MemberEntity {
    pub id: Uuid,
    pub current_shares: i32,
    pub join_date: time::Date,
    pub exit_date: Option<time::Date>,
    pub version: Uuid,
    // ... weitere Felder
}
```

From `genossi_service/src/lib.rs:11-15` (aktuelles Modul-Layout):
```rust
pub mod member;
pub mod member_action;
pub mod member_document;
pub mod member_import;
pub mod permission;
```

From `genossi_service_impl/src/member_action.rs:155-177` (Pure-Function compute_dates — Vorbild):
```rust
pub(crate) fn compute_dates(
    member: &genossi_dao::member::MemberEntity,
    actions: &[genossi_dao::member_action::MemberActionEntity],
) -> (time::Date, Option<time::Date>) {
    let join_date = actions.iter()
        .find(|a| a.action_type == ActionType::Eintritt)
        .map(|a| a.date).unwrap_or(member.join_date);
    let exit_date = actions.iter()
        .find(|a| a.action_type == ActionType::Austritt)
        .map(|a| a.effective_date.unwrap_or(a.date))
        .or_else(|| actions.iter().find(|a| a.action_type == ActionType::Todesfall).map(|a| a.date));
    (join_date, exit_date)
}
```

From `genossi_service_impl/src/member_action.rs:180-203` (aktuelle recalc_dates-Methode — zu refaktorieren):
```rust
impl<Deps: MemberActionServiceDeps> MemberActionServiceImpl<Deps> {
    async fn recalc_dates(&self, member_id: Uuid, tx: Deps::Transaction) -> Result<(), ServiceError> {
        let member = self.member_dao.find_by_id(member_id, tx.clone()).await?
            .ok_or(ServiceError::EntityNotFound(member_id))?;
        let actions = self.member_action_dao.find_by_member_id(member_id, tx.clone()).await?;
        let (join_date, exit_date) = compute_dates(&member, &actions);
        self.member_dao.update_dates(member_id, join_date, exit_date, tx).await?;
        Ok(())
    }
}
```

From `genossi_service_impl/src/membership_adjust.rs` (vorhanden seit Phase 14):
- Pure-Function `compute_effective_date(willensbekundung: Date) -> EffectiveDate { fiscal_year, effective_date }` (pub(crate))
- 6 Edge-Case-Tests bereits vorhanden (compute_effective_date)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Trait MembershipAdjustService definieren + Modul registrieren</name>
  <files>genossi_service/src/membership_adjust.rs, genossi_service/src/lib.rs</files>
  <read_first>
    - genossi_service/src/membership_adjust.rs (Datei existiert noch NICHT — wird angelegt)
    - genossi_service/src/lib.rs (Zeilen 11-15 — Modul-Registrierungs-Block)
    - genossi_service/src/member_action.rs (Z. 1-9 Imports, Z. 80-105 Trait-Skeleton — verbatim als Vorlage uebernehmen)
    - .planning/phases/15-service-rest-kuendigung-aufstockung/15-PATTERNS.md Section Pattern 1 + Pattern 2
    - .planning/phases/15-service-rest-kuendigung-aufstockung/15-CONTEXT.md D-15-13, D-15-14, D-15-15
  </read_first>
  <behavior>
    - Test 1: `cargo build -p genossi_service` kompiliert clean (Trait + Mock per `#[automock]` + Re-Export)
    - Test 2: `MockMembershipAdjustService` ist verfuegbar (mockall-Builder-API funktioniert in Konsumenten-Crates)
  </behavior>
  <action>
    1. Neue Datei `genossi_service/src/membership_adjust.rs` anlegen (per D-15-13, D-15-14, D-15-15):

```rust
//! v1.2 Mitgliedschaft-Anpassungen — Service-Trait (Foundation fuer Phase 15-17).
//!
//! Phase 15 definiert `cancel_membership` + `increase_shares`. Phase 16 ergaenzt
//! `partial_repayment`, Phase 17 ergaenzt `transfer_shares` (D-15-13 inkrementelles Wachsen).

use async_trait::async_trait;
use mockall::automock;
use std::fmt::Debug;
use uuid::Uuid;

use crate::member::Member;
use crate::member_action::MemberAction;
use crate::permission::Authentication;
use crate::ServiceError;

/// Service-Trait fuer v1.2-Mitgliedschaft-Anpassungen (PERM-01: admin-only).
///
/// Beide Phase-15-Methoden geben `(MemberAction, Member)` zurueck, damit das Frontend
/// nach dem Commit ohne zusaetzlichen GET-Round-Trip rendern kann (D-15-11).
#[automock(type Context=(); type Transaction = genossi_dao::MockTransaction;)]
#[async_trait]
pub trait MembershipAdjustService {
    type Context: Clone + Debug + PartialEq + Eq + Send + Sync + 'static;
    type Transaction: genossi_dao::Transaction;

    /// Kuendigt eine Mitgliedschaft via `MemberAction::Austritt` (CANC-01..05).
    async fn cancel_membership(
        &self,
        member_id: Uuid,
        willensbekundung_date: time::Date,
        context: Authentication<Self::Context>,
        tx: Option<Self::Transaction>,
    ) -> Result<(MemberAction, Member), ServiceError>;

    /// Stockt die Anteile eines aktiven Mitglieds atomar auf (UPGD-01..04).
    async fn increase_shares(
        &self,
        member_id: Uuid,
        shares: i32,
        willensbekundung_date: time::Date,
        context: Authentication<Self::Context>,
        tx: Option<Self::Transaction>,
    ) -> Result<(MemberAction, Member), ServiceError>;
}
```

    2. Modul in `genossi_service/src/lib.rs` registrieren — alphabetisch zwischen `member_import` und `permission` (Z. 11-15):

```rust
pub mod member;
pub mod member_action;
pub mod member_document;
pub mod member_import;
pub mod membership_adjust;
pub mod permission;
```

    Hinweis: `Member` ist der Domain-Typ aus `genossi_service::member::Member` (existing seit v1.0). `MemberAction` aus `genossi_service::member_action::MemberAction`.
  </action>
  <verify>
    <automated>cargo build -p genossi_service 2>&amp;1 | tail -5 &amp;&amp; grep -n "pub mod membership_adjust" /home/neosam/programming/rust/projects/genossi3/genossi_service/src/lib.rs &amp;&amp; grep -n "pub trait MembershipAdjustService" /home/neosam/programming/rust/projects/genossi3/genossi_service/src/membership_adjust.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p genossi_service` exit code 0
    - `grep -n 'pub trait MembershipAdjustService' genossi_service/src/membership_adjust.rs` returns 1 line
    - `grep -n 'pub mod membership_adjust;' genossi_service/src/lib.rs` returns 1 line
    - `grep -c 'async fn cancel_membership' genossi_service/src/membership_adjust.rs` >= 1
    - `grep -c 'async fn increase_shares' genossi_service/src/membership_adjust.rs` >= 1
    - `grep -n '#\[automock' genossi_service/src/membership_adjust.rs` returns 1 line (Mock vorhanden)
  </acceptance_criteria>
  <done>
    Trait + Mock + Modul-Registrierung clean kompiliert; bereit fuer Wave 2+3 (Service-Impl + REST).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure-Function validate_willensbekundung_date + 6 Edge-Case-Tests</name>
  <files>genossi_service_impl/src/membership_adjust.rs</files>
  <read_first>
    - genossi_service_impl/src/membership_adjust.rs (Z. 1-115 — bestehende compute_effective_date + Tests)
    - genossi_service_impl/src/member_action.rs (Z. 76-153 — validate_action als Vorbild fuer ValidationFailureItem)
    - genossi_service/src/lib.rs (sicher dass ValidationFailureItem re-exportiert/importierbar ist: `genossi_service::ValidationFailureItem`)
    - .planning/phases/15-service-rest-kuendigung-aufstockung/15-PATTERNS.md Section Pattern 3d
    - .planning/phases/15-service-rest-kuendigung-aufstockung/15-CONTEXT.md D-15-05, D-15-06, D-15-07, D-15-08
  </read_first>
  <behavior>
    - Test 1: date=2026-06-15, today=2026-03-15 → empty errors
    - Test 2: date=2027-06-15, today=2026-03-15 → empty errors
    - Test 3: date=2025-12-31, today=2026-03-15 → 1 error field="willensbekundung_date", message enthaelt "2026" + "2027"
    - Test 4: date=2028-01-01, today=2026-03-15 → 1 error
    - Test 5: date=2027-12-31, today=2026-12-31 → empty errors
    - Test 6: date=2024-02-29, today=2024-01-15 → empty errors
  </behavior>
  <action>
    1. Funktion in `genossi_service_impl/src/membership_adjust.rs` ergaenzen — nach der existing `EffectiveDate`-Struct (Z. 44) einfuegen:

```rust
use genossi_service::ValidationFailureItem;
use std::sync::Arc;

/// Validiert das Willensbekundungs-Datum gegen die Kalender-Jahr-Bounds (D-15-06, PERM-02).
pub(crate) fn validate_willensbekundung_date(
    date: time::Date,
    today: time::Date,
) -> Vec<ValidationFailureItem> {
    let current_fy = today.year();
    let next_fy = current_fy + 1;
    if date.year() == current_fy || date.year() == next_fy {
        Vec::new()
    } else {
        vec![ValidationFailureItem {
            field: Arc::from("willensbekundung_date"),
            message: Arc::from(format!(
                "must be in fiscal year {} or {}",
                current_fy, next_fy
            )),
        }]
    }
}
```

    2. Tests im existing `#[cfg(test)] mod tests`-Block ergaenzen — direkt nach den 6 `compute_effective_date`-Tests:

```rust
#[test]
fn test_validate_willensbekundung_aktuelles_jahr_valid() {
    let today = time::Date::from_calendar_date(2026, time::Month::March, 15).unwrap();
    let date = time::Date::from_calendar_date(2026, time::Month::June, 15).unwrap();
    assert!(validate_willensbekundung_date(date, today).is_empty());
}

#[test]
fn test_validate_willensbekundung_naechstes_jahr_valid() {
    let today = time::Date::from_calendar_date(2026, time::Month::March, 15).unwrap();
    let date = time::Date::from_calendar_date(2027, time::Month::June, 15).unwrap();
    assert!(validate_willensbekundung_date(date, today).is_empty());
}

#[test]
fn test_validate_willensbekundung_vorjahr_invalid() {
    let today = time::Date::from_calendar_date(2026, time::Month::March, 15).unwrap();
    let date = time::Date::from_calendar_date(2025, time::Month::December, 31).unwrap();
    let errors = validate_willensbekundung_date(date, today);
    assert_eq!(errors.len(), 1);
    assert_eq!(&*errors[0].field, "willensbekundung_date");
    assert!(errors[0].message.contains("2026"));
    assert!(errors[0].message.contains("2027"));
}

#[test]
fn test_validate_willensbekundung_uebernaechstes_jahr_invalid() {
    let today = time::Date::from_calendar_date(2026, time::Month::March, 15).unwrap();
    let date = time::Date::from_calendar_date(2028, time::Month::January, 1).unwrap();
    let errors = validate_willensbekundung_date(date, today);
    assert_eq!(errors.len(), 1);
    assert_eq!(&*errors[0].field, "willensbekundung_date");
}

#[test]
fn test_validate_willensbekundung_today_31_dezember_naechstes_jahr_valid() {
    let today = time::Date::from_calendar_date(2026, time::Month::December, 31).unwrap();
    let date = time::Date::from_calendar_date(2027, time::Month::December, 31).unwrap();
    assert!(validate_willensbekundung_date(date, today).is_empty());
}

#[test]
fn test_validate_willensbekundung_schaltjahr_29_februar_valid() {
    let today = time::Date::from_calendar_date(2024, time::Month::January, 15).unwrap();
    let date = time::Date::from_calendar_date(2024, time::Month::February, 29).unwrap();
    assert!(validate_willensbekundung_date(date, today).is_empty());
}
```

    Hinweis: `ValidationFailureItem` ist als `pub use` aus dem `genossi_service`-Crate-Root verfuegbar. Falls Compile-Fehler: alternativer Pfad `use genossi_service::ServiceError;` und `ValidationFailureItem` aus dem `use`-Pfad pruefen via `grep -n 'pub struct ValidationFailureItem' genossi_service/src/`.
  </action>
  <verify>
    <automated>cargo test -p genossi_service_impl --lib membership_adjust::tests::test_validate_willensbekundung 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p genossi_service_impl --lib membership_adjust::tests::test_validate_willensbekundung_aktuelles_jahr_valid` exits 0
    - `cargo test -p genossi_service_impl --lib membership_adjust::tests::test_validate_willensbekundung_naechstes_jahr_valid` exits 0
    - `cargo test -p genossi_service_impl --lib membership_adjust::tests::test_validate_willensbekundung_vorjahr_invalid` exits 0
    - `cargo test -p genossi_service_impl --lib membership_adjust::tests::test_validate_willensbekundung_uebernaechstes_jahr_invalid` exits 0
    - `cargo test -p genossi_service_impl --lib membership_adjust::tests::test_validate_willensbekundung_today_31_dezember_naechstes_jahr_valid` exits 0
    - `cargo test -p genossi_service_impl --lib membership_adjust::tests::test_validate_willensbekundung_schaltjahr_29_februar_valid` exits 0
    - `grep -n 'pub(crate) fn validate_willensbekundung_date' genossi_service_impl/src/membership_adjust.rs` returns 1 line
    - `grep -c 'OffsetDateTime::now_utc' genossi_service_impl/src/membership_adjust.rs` returns 0 (Pure-Function ohne now()-Call — D-15-07)
  </acceptance_criteria>
  <done>
    Pure-Function + 6 Edge-Case-Tests gruen. Service-Methoden in Plan 02/03 koennen `validate_willensbekundung_date(req.willensbekundung_date, today)` aufrufen.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: recalc_dates zu pub(crate) Free-Function refaktorieren</name>
  <files>genossi_service_impl/src/member_action.rs</files>
  <read_first>
    - genossi_service_impl/src/member_action.rs (Z. 1-30 Imports + gen_service_impl; Z. 155-203 compute_dates + recalc_dates)
    - genossi_dao/src/member.rs (Z. 97-117 MemberDao-Trait, `update_dates` muss als Trait-Methode existieren)
    - genossi_dao/src/member_action.rs (MemberActionDao-Trait — `find_by_member_id` muss existieren)
    - .planning/phases/15-service-rest-kuendigung-aufstockung/15-PATTERNS.md Section Pattern 4
    - .planning/phases/15-service-rest-kuendigung-aufstockung/15-CONTEXT.md D-15-04
  </read_first>
  <behavior>
    - Test 1: Free-Function `recalc_dates` ist als `pub(crate) async fn` mit generischen Bounds (`Md: MemberDao<Transaction=Tx>`, `Mad: MemberActionDao<Transaction=Tx>`, `Tx: Transaction + Clone`) verfuegbar
    - Test 2: Bestehende Methode `MemberActionServiceImpl::recalc_dates` delegiert an die Free-Function — Body ist genau 1 Aufruf
    - Test 3: Alle bestehenden Tests in `cargo test -p genossi_service_impl member_action` bleiben gruen (kein Behavior-Change)
  </behavior>
  <action>
    1. In `genossi_service_impl/src/member_action.rs` — direkt nach `compute_dates` (Z. 177) eine neue `pub(crate) async fn recalc_dates` Free-Function einfuegen:

```rust
/// Free-Function-Version von `recalc_dates` (D-15-04).
///
/// Vorher als private Methode auf `MemberActionServiceImpl<Deps>` — jetzt extrahiert,
/// damit `MembershipAdjustServiceImpl::cancel_membership` (Phase 15) ohne
/// `MemberActionServiceImpl`-Coupling auf dieselbe Logik zugreifen kann.
pub(crate) async fn recalc_dates<Md, Mad, Tx>(
    member_dao: &Md,
    member_action_dao: &Mad,
    member_id: Uuid,
    tx: Tx,
) -> Result<(), ServiceError>
where
    Md: genossi_dao::member::MemberDao<Transaction = Tx> + ?Sized,
    Mad: genossi_dao::member_action::MemberActionDao<Transaction = Tx> + ?Sized,
    Tx: genossi_dao::Transaction + Clone,
{
    let member = member_dao
        .find_by_id(member_id, tx.clone())
        .await?
        .ok_or(ServiceError::EntityNotFound(member_id))?;

    let actions = member_action_dao
        .find_by_member_id(member_id, tx.clone())
        .await?;

    let (join_date, exit_date) = compute_dates(&member, &actions);

    member_dao
        .update_dates(member_id, join_date, exit_date, tx)
        .await?;

    Ok(())
}
```

    2. Bestehende `impl<Deps>::recalc_dates`-Methode (Z. 180-203) zu Delegations-Wrapper umbauen:

```rust
impl<Deps: MemberActionServiceDeps> MemberActionServiceImpl<Deps> {
    async fn recalc_dates(
        &self,
        member_id: Uuid,
        tx: Deps::Transaction,
    ) -> Result<(), ServiceError> {
        recalc_dates(&*self.member_dao, &*self.member_action_dao, member_id, tx).await
    }

    // recalc_migrated bleibt UNVERAENDERT (Z. 205-229)
}
```

    Wichtig:
    - `?Sized` auf den Generic-Bounds erlaubt Aufruf mit `&*self.member_dao` (Arc<dyn MemberDao>)
    - `Tx: Clone` ist erforderlich, weil die Logik `tx.clone()` zweimal nutzt
    - Free-Function-Doc-Comment dokumentiert D-15-04 als Refactor-Grund
    - Kein Behavior-Change — bestehende Tests muessen alle weiter gruen sein
  </action>
  <verify>
    <automated>cargo build -p genossi_service_impl 2>&amp;1 | tail -5 &amp;&amp; cargo test -p genossi_service_impl --lib member_action 2>&amp;1 | tail -10 &amp;&amp; grep -nE 'pub\(crate\) async fn recalc_dates' /home/neosam/programming/rust/projects/genossi3/genossi_service_impl/src/member_action.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p genossi_service_impl` exits 0
    - `cargo test -p genossi_service_impl --lib member_action` exits 0
    - `grep -cE 'pub\(crate\) async fn recalc_dates' genossi_service_impl/src/member_action.rs` returns 1
    - `grep -cE 'async fn recalc_dates' genossi_service_impl/src/member_action.rs` returns 2 (Free-Function + Wrapper)
    - Wrapper-Methode delegiert: `grep -A2 'async fn recalc_dates' genossi_service_impl/src/member_action.rs | grep -c 'recalc_dates(&\*self.member_dao'` >= 1
  </acceptance_criteria>
  <done>
    `recalc_dates` ist als Free-Function aufrufbar (Plan 02 nutzt sie aus `MembershipAdjustServiceImpl::cancel_membership`). MemberActionService-Behavior unveraendert.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public API → trait surface | `MembershipAdjustService`-Trait wird Phase 15+ Konsumenten-Code ausgesetzt; Mock-API steht Tests von downstream-Crates zur Verfuegung |
| pure-function → caller | `validate_willensbekundung_date` ist pure; nimmt nur `Date`+`Date` entgegen (kein I/O, keine globale Mutable State) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-01 | Tampering | recalc_dates Free-Function | mitigate | Free-Function ist `pub(crate)` — bleibt im Crate; kein externer Caller kann die Member-Date-Logik austauschen. Wrapper-Methode in `MemberActionServiceImpl::recalc_dates` bleibt private, delegiert nur — kein neuer Public-API-Endpunkt. |
| T-15-01-02 | Information Disclosure | validate_willensbekundung_date message | accept | Error-Message enthaelt nur `today.year()` und `today.year()+1` — kein Member-ID, keine Audit-Hash, keine internen Pfade. Risiko niedrig. |
| T-15-01-03 | Denial of Service | validate_willensbekundung_date | mitigate | Pure-Function laeuft in O(1); kein Loop, kein I/O. DoS-Surface = 0. |
| T-15-01-04 | Repudiation | recalc_dates-Behavior-Drift | mitigate | Free-Function-Refactor ist verhaltensneutral (Test-Suite `cargo test -p genossi_service_impl --lib member_action` ist Acceptance-Gate). Kein neuer Audit-Eintrag-Pfad. |
| T-15-01-05 | Elevation of Privilege | trait Mock-Sichtbarkeit | accept | `#[automock]` generiert `MockMembershipAdjustService` mit dem gleichen Sichtbarkeitsbereich wie der Trait selbst (`pub`). Keine zusaetzliche Privilege-Eskalation moeglich; Mock ist nur in Test-Builds aktiv (per mockall-Konvention). |
</threat_model>

<verification>
- `cargo build -p genossi_service && cargo build -p genossi_service_impl` exits 0
- `cargo test -p genossi_service_impl --lib membership_adjust` exits 0 (Pure-Function-Tests gruen)
- `cargo test -p genossi_service_impl --lib member_action` exits 0 (Refactor verhaltensneutral)
- Trait-Datei + Modul-Registrierung + Mock-Verfuegbarkeit per grep verifiziert
- Free-Function `recalc_dates` per grep verifiziert
</verification>

<success_criteria>
- Alle 6 Validate-Tests `test_validate_willensbekundung_*` gruen
- `cargo test -p genossi_service_impl` kompiliert clean (kein Regression)
- Wave 2 (Plan 02 `cancel_membership`) kann `MembershipAdjustService`-Trait nutzen und `recalc_dates`-Free-Function aufrufen
- Wave 3 (Plan 03 `increase_shares`) kann den Trait erweitern und `validate_willensbekundung_date`-Function nutzen
</success_criteria>

<output>
After completion, create `.planning/phases/15-service-rest-kuendigung-aufstockung/15-01-SUMMARY.md` per template at `.claude/get-shit-done/templates/summary.md`.
</output>
