---
phase: 13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder
plan: 03
type: tdd
wave: 2
depends_on: [01]
files_modified:
  - genossi_service_impl/src/pdf_generation.rs
autonomous: true
requirements: [BRIEF-01]
tags: [phase-13, pdf, typst, render, sys-inputs, bundle]

must_haves:
  truths:
    - "PdfGenerator::render_repayment_letter rendert EIN PDF aus phase + member + RepaymentContext"
    - "PdfGenerator::render_repayment_letter_bundle rendert EIN gebuendeltes PDF mit #pagebreak() zwischen N Recipients (D-13-01)"
    - "Beide Renderer nutzen sys.inputs-JSON-Pattern (D-LETT-01, RESEARCH Pattern 4)"
    - "Member.bank_account=None wird als JSON null serialisiert (Pitfall #5) — Template's #if-Switch handelt das"
    - "build_inputs_repayment_letter setzt member, repayment, today JSON-Keys (Pattern 4)"
    - "Bundle nutzt 'recipients'-Array (RESEARCH Pattern 6) und EIN Typst-Compile (kein PDF-Merge)"
    - "Render-Funktion ist synchron (Typst ist nicht-async) — Caller (Service) commitet Tx VOR Aufruf (Pitfall #2)"
    - "Plan 03 touchiert NUR pdf_generation.rs — Bundle-Template + DEFAULT_TEMPLATES-Eintraege sind bereits in Plan 01 angelegt"
  artifacts:
    - path: "genossi_service_impl/src/pdf_generation.rs"
      provides: "render_repayment_letter + render_repayment_letter_bundle + build_inputs_repayment_letter + build_inputs_repayment_letters_bundle"
      contains: "render_repayment_letter"
  key_links:
    - from: "PdfGenerator::render_repayment_letter"
      to: "templates/defaults/auszahlungs_anschreiben.typ"
      via: "template_base.join(template_path)"
      pattern: "auszahlungs_anschreiben"
    - from: "PdfGenerator::render_repayment_letter_bundle"
      to: "templates/defaults/auszahlungs_anschreiben_bundle.typ"
      via: "template_base.join(template_path)"
      pattern: "auszahlungs_anschreiben_bundle"
    - from: "build_inputs_repayment_letter"
      to: "RepaymentContext"
      via: "serde_json::json!({ \"share_count\": ctx.share_count, ...})"
      pattern: "share_count"
---

<objective>
PdfGenerator-Erweiterung: zwei neue synchron Render-Funktionen — `render_repayment_letter` (1 Member → 1 PDF) und `render_repayment_letter_bundle` (N Members → 1 PDF mit `#pagebreak()` zwischen Briefen). Aufruf-Konvention 1:1 wie bestehende `render_repayment_list`/`render_attendance_list`.

**Saubere Wave-Separation:** Plan 01 hat bereits beide Templates (Single + Bundle) angelegt UND in DEFAULT_TEMPLATES registriert. Plan 03 modifiziert AUSSCHLIESSLICH `genossi_service_impl/src/pdf_generation.rs` — kein Template-Code, keine template_storage.rs-Aenderung.

Purpose: D-13-01 (Hybrid-Bundle: N persistierte + 1 transientes Bundle). Bundle ueber EINEN Typst-Compile-Aufruf statt N+1, kein neues Crate-Dependency.
Output:
- `pub fn render_repayment_letter(template_path, template_base, phase, member, ctx) -> Result<Vec<u8>>`
- `pub fn render_repayment_letter_bundle(template_path, template_base, phase, recipients: &[(MemberEntity, RepaymentContext)]) -> Result<Vec<u8>>`
- Helper `build_inputs_repayment_letter` + `build_inputs_repayment_letters_bundle`
- Unit-Tests fuer Inputs-Konstruktion (sys.inputs JSON-Shape)
- Smoke-Tests: real-render mit echtem Template + Sample-Daten → Result startet mit `%PDF-`
- Bundle-Smoke-Test: `bundle_bytes.len() > single_bytes.len() * 1.5` (Heuristik fuer "mehrere Seiten") oder konkretere Page-Count-Verifikation
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder/13-CONTEXT.md
@.planning/phases/13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder/13-RESEARCH.md
@.planning/phases/13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder/13-PATTERNS.md

<interfaces>
<!-- Existing PdfGenerator render-method pattern — genossi_service_impl/src/pdf_generation.rs:386-441 -->
```rust
pub fn render_repayment_list(
    &self,
    template_path: &str,
    template_base: &Path,
    phase: &RepaymentPhaseEntity,
    rows: &[RepaymentExportRow],
) -> Result<Vec<u8>, ServiceError> {
    let full_path = template_base.join(template_path);
    let source_text = std::fs::read_to_string(&full_path).map_err(|e| { /* InternalError */ })?;
    let inputs = build_inputs_repayment(phase, rows);
    let world = TemplateWorld::new(&source_text, template_path, template_base.to_path_buf(),
        inputs, &self.fonts, &self.book, &self.package_cache);
    let result = typst::compile::<PagedDocument>(&world);
    let document = match result.output { Ok(doc) => doc, Err(d) => return Err(ServiceError::InternalError(...)) };
    // typst_pdf::pdf(&document, ...) -> Vec<u8>
}
```

<!-- Existing build_inputs pattern — pdf_generation.rs:776-826 -->
```rust
fn build_inputs_repayment(phase: &RepaymentPhaseEntity, rows: &[RepaymentExportRow]) -> Dict {
    let mut inputs = Dict::new();
    let date_str = time::OffsetDateTime::now_utc().date().to_string();
    let meta = serde_json::json!({ "title": ..., "fiscal_year": ..., ... });
    inputs.insert(Str::from("meta"), Value::Str(Str::from(serde_json::to_string(&meta).unwrap().as_str())));
    // rows JSON-Array...
    inputs
}
```

<!-- genossi_dao::member::MemberEntity — extracted from genossi_dao/src/member.rs -->
```rust
pub struct MemberEntity {
    pub id: Uuid,
    pub member_number: i64,
    pub salutation: Option<Arc<str>>,
    pub title: Option<Arc<str>>,
    pub first_name: Arc<str>,
    pub last_name: Arc<str>,
    pub street: Option<Arc<str>>,
    pub house_number: Option<Arc<str>>,
    pub postal_code: Option<Arc<str>>,
    pub city: Option<Arc<str>>,
    pub bank_account: Option<Arc<str>>,   // IBAN — NULL möglich (D-13-06 Punkt 3)
    // ... weitere Felder
}
```

<!-- Plan 01 Output (bereits angelegt): -->
<!--   templates/defaults/auszahlungs_anschreiben.typ — Single-Letter mit #let render-letter(...) -->
<!--   templates/defaults/auszahlungs_anschreiben_bundle.typ — Bundle-Wrapper, importiert render-letter -->
<!--   genossi_service_impl/src/template_storage.rs — DEFAULT_TEMPLATES enthaelt BEIDE Eintraege -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: build_inputs_repayment_letter + build_inputs_repayment_letters_bundle Helper</name>
  <files>genossi_service_impl/src/pdf_generation.rs</files>
  <read_first>
    - genossi_service_impl/src/pdf_generation.rs (gesamte Datei — mind. Z. 770-830 fuer build_inputs_repayment-Pattern, Z. 386-441 fuer render_repayment_list)
    - genossi_dao/src/member.rs (MemberEntity-Definition, alle Felder die in JSON serialisiert werden)
    - genossi_service/src/repayment_context.rs (RepaymentContext-Struct aus Plan 02)
    - .planning/phases/13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder/13-PATTERNS.md §`genossi_service_impl/src/pdf_generation.rs` (build_inputs Pattern)
    - .planning/phases/13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder/13-RESEARCH.md §Pattern 4 (sys.inputs JSON-Kontext)
  </read_first>
  <behavior>
    - Test 1: `build_inputs_repayment_letter(phase, member, ctx)` setzt drei Top-Level-Keys: `"member"`, `"repayment"`, `"today"`
    - Test 2: `member`-JSON enthaelt `member_number`, `first_name`, `last_name`, `salutation`, `title`, `street`, `house_number`, `postal_code`, `city`, `bank_account` (alle 10 Felder)
    - Test 3: `member.bank_account = None` → JSON-Wert ist `null` (NICHT String) — Pitfall #5 Mitigation
    - Test 4: `repayment`-JSON enthaelt `share_count`, `payout_amount`, `fiscal_year`
    - Test 5: `today` ist ISO-Date `YYYY-MM-DD`
    - Test 6: `build_inputs_repayment_letters_bundle(phase, recipients)` setzt `"recipients"`-Key als JSON-Array
    - Test 7: Bundle-Recipient-Array enthaelt fuer jedes Tuple ein Objekt mit `member` + `repayment` Subkeys
    - Test 8: Bundle setzt `today` + ggf. `meta` (mit `phase_id`, `fiscal_year`, `recipient_count`)
  </behavior>
  <action>
    **In `genossi_service_impl/src/pdf_generation.rs` zwei neue private Helper-Funktionen direkt unter `build_inputs_repayment` einfuegen.**

    **Imports (oben in Datei pruefen/ergaenzen):**
    ```rust
    use genossi_service::repayment_context::RepaymentContext;
    use genossi_dao::member::MemberEntity;
    // Typst-Imports bereits vorhanden (Dict, Str, Value)
    ```

    **`build_inputs_repayment_letter`-Funktion:**
    ```rust
    /// Phase 13 D-13-01: Inputs fuer EIN Single-Letter-PDF (1 Member).
    /// JSON-Shape spiegelt das Default-Template templates/defaults/auszahlungs_anschreiben.typ.
    /// Pitfall #5: member.bank_account=None → JSON null (Template hat #if-Switch).
    fn build_inputs_repayment_letter(
        _phase: &RepaymentPhaseEntity,
        member: &MemberEntity,
        ctx: &RepaymentContext,
    ) -> Dict {
        let mut inputs = Dict::new();

        let date_str = time::OffsetDateTime::now_utc().date().to_string();
        inputs.insert(
            Str::from("today"),
            Value::Str(Str::from(date_str.as_str())),
        );

        let member_json = serde_json::json!({
            "member_number": member.member_number,
            "salutation": member.salutation.as_ref().map(|s| s.as_ref()),
            "title": member.title.as_ref().map(|s| s.as_ref()),
            "first_name": member.first_name.as_ref(),
            "last_name": member.last_name.as_ref(),
            "street": member.street.as_ref().map(|s| s.as_ref()),
            "house_number": member.house_number.as_ref().map(|s| s.as_ref()),
            "postal_code": member.postal_code.as_ref().map(|s| s.as_ref()),
            "city": member.city.as_ref().map(|s| s.as_ref()),
            "bank_account": member.bank_account.as_ref().map(|s| s.as_ref()),
        });
        let member_str = serde_json::to_string(&member_json).expect("member json serialisable");
        inputs.insert(Str::from("member"), Value::Str(Str::from(member_str.as_str())));

        let repayment_json = serde_json::json!({
            "share_count": ctx.share_count,
            "payout_amount": ctx.payout_amount,
            "fiscal_year": ctx.fiscal_year,
        });
        let repayment_str =
            serde_json::to_string(&repayment_json).expect("repayment json serialisable");
        inputs.insert(
            Str::from("repayment"),
            Value::Str(Str::from(repayment_str.as_str())),
        );

        inputs
    }
    ```

    **`build_inputs_repayment_letters_bundle`-Funktion:**
    ```rust
    /// Phase 13 D-13-01: Inputs fuer Bundle-PDF (N Members in EINEM Compile).
    /// 'recipients'-Array enthaelt pro Member ein Objekt mit member+repayment-Sub-JSON.
    /// Reihenfolge bestimmt der Caller (Service sortiert nach member_number ASC — Pitfall #10).
    fn build_inputs_repayment_letters_bundle(
        phase: &RepaymentPhaseEntity,
        recipients: &[(MemberEntity, RepaymentContext)],
    ) -> Dict {
        let mut inputs = Dict::new();

        let date_str = time::OffsetDateTime::now_utc().date().to_string();
        inputs.insert(
            Str::from("today"),
            Value::Str(Str::from(date_str.as_str())),
        );

        let meta = serde_json::json!({
            "fiscal_year": phase.fiscal_year,
            "recipient_count": recipients.len(),
            "phase_id": phase.id.to_string(),
        });
        inputs.insert(
            Str::from("meta"),
            Value::Str(Str::from(serde_json::to_string(&meta).unwrap().as_str())),
        );

        let recipient_values: Vec<serde_json::Value> = recipients
            .iter()
            .map(|(member, ctx)| {
                serde_json::json!({
                    "member": {
                        "member_number": member.member_number,
                        "salutation": member.salutation.as_ref().map(|s| s.as_ref()),
                        "title": member.title.as_ref().map(|s| s.as_ref()),
                        "first_name": member.first_name.as_ref(),
                        "last_name": member.last_name.as_ref(),
                        "street": member.street.as_ref().map(|s| s.as_ref()),
                        "house_number": member.house_number.as_ref().map(|s| s.as_ref()),
                        "postal_code": member.postal_code.as_ref().map(|s| s.as_ref()),
                        "city": member.city.as_ref().map(|s| s.as_ref()),
                        "bank_account": member.bank_account.as_ref().map(|s| s.as_ref()),
                    },
                    "repayment": {
                        "share_count": ctx.share_count,
                        "payout_amount": ctx.payout_amount,
                        "fiscal_year": ctx.fiscal_year,
                    },
                })
            })
            .collect();
        let recipients_str =
            serde_json::to_string(&serde_json::Value::Array(recipient_values))
                .expect("recipients json serialisable");
        inputs.insert(
            Str::from("recipients"),
            Value::Str(Str::from(recipients_str.as_str())),
        );

        inputs
    }
    ```

    **Unit-Tests im `#[cfg(test)] mod tests` von `pdf_generation.rs` ergaenzen** (mind. 8 Tests laut behavior block):
    ```rust
    #[test]
    fn test_build_inputs_repayment_letter_has_three_top_level_keys() {
        let phase = sample_phase(12000);
        let member = sample_member_with_iban();
        let ctx = sample_ctx(3, "360,00", 2025);
        let dict = build_inputs_repayment_letter(&phase, &member, &ctx);
        // Phase-11-Pattern: pruefen, dass die drei Keys gesetzt sind via Iteration.
        let keys: Vec<String> = dict.iter().map(|(k, _)| k.as_str().to_string()).collect();
        assert!(keys.contains(&"member".to_string()));
        assert!(keys.contains(&"repayment".to_string()));
        assert!(keys.contains(&"today".to_string()));
    }

    #[test]
    fn test_build_inputs_repayment_letter_bank_account_null_is_json_null() {
        // Pitfall #5: bank_account=None muss JSON null sein.
        let phase = sample_phase(12000);
        let member = sample_member_without_iban();
        let ctx = sample_ctx(3, "360,00", 2025);
        let dict = build_inputs_repayment_letter(&phase, &member, &ctx);
        let member_json: serde_json::Value = serde_json::from_str(
            extract_str_input(&dict, "member").as_str()
        ).unwrap();
        assert_eq!(member_json["bank_account"], serde_json::Value::Null);
    }

    #[test]
    fn test_build_inputs_repayment_letter_repayment_keys() {
        let phase = sample_phase(12000);
        let member = sample_member_with_iban();
        let ctx = sample_ctx(5, "600,00", 2025);
        let dict = build_inputs_repayment_letter(&phase, &member, &ctx);
        let repayment_json: serde_json::Value = serde_json::from_str(
            extract_str_input(&dict, "repayment").as_str()
        ).unwrap();
        assert_eq!(repayment_json["share_count"], 5);
        assert_eq!(repayment_json["payout_amount"], "600,00");
        assert_eq!(repayment_json["fiscal_year"], 2025);
    }

    #[test]
    fn test_build_inputs_bundle_recipients_is_array() {
        let phase = sample_phase(12000);
        let recipients = vec![
            (sample_member_with_iban(), sample_ctx(3, "360,00", 2025)),
            (sample_member_without_iban(), sample_ctx(2, "240,00", 2025)),
        ];
        let dict = build_inputs_repayment_letters_bundle(&phase, &recipients);
        let recipients_json: serde_json::Value = serde_json::from_str(
            extract_str_input(&dict, "recipients").as_str()
        ).unwrap();
        assert!(recipients_json.is_array());
        assert_eq!(recipients_json.as_array().unwrap().len(), 2);
    }

    #[test]
    fn test_build_inputs_bundle_meta_has_recipient_count() {
        let phase = sample_phase(12000);
        let recipients = vec![(sample_member_with_iban(), sample_ctx(3, "360,00", 2025))];
        let dict = build_inputs_repayment_letters_bundle(&phase, &recipients);
        let meta_json: serde_json::Value = serde_json::from_str(
            extract_str_input(&dict, "meta").as_str()
        ).unwrap();
        assert_eq!(meta_json["recipient_count"], 1);
        assert_eq!(meta_json["fiscal_year"], 2025);
    }
    ```

    Test-Helpers `sample_phase`, `sample_member_with_iban`, `sample_member_without_iban`, `sample_ctx`, `extract_str_input` koennen lokal im test-mod definiert werden — `extract_str_input` extrahiert den String aus einem `Dict`-Eintrag.
  </action>
  <verify>
    <automated>cargo test -p genossi_service_impl --lib pdf_generation::tests::test_build_inputs_repayment_letter -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'fn build_inputs_repayment_letter\b' genossi_service_impl/src/pdf_generation.rs` returns 1
    - `rg 'fn build_inputs_repayment_letters_bundle' genossi_service_impl/src/pdf_generation.rs` returns 1
    - `rg 'recipients' genossi_service_impl/src/pdf_generation.rs` returns >=1 (Bundle-Key)
    - `rg 'bank_account' genossi_service_impl/src/pdf_generation.rs` returns >=2 (single + bundle helper)
    - `rg 'fn test_build_inputs_repayment_letter' genossi_service_impl/src/pdf_generation.rs` returns >=3
    - `rg 'fn test_build_inputs_bundle' genossi_service_impl/src/pdf_generation.rs` returns >=2
    - `cargo test -p genossi_service_impl --lib pdf_generation::tests::test_build_inputs_repayment_letter_bank_account_null_is_json_null` exit 0
    - `cargo test -p genossi_service_impl --lib pdf_generation::tests::test_build_inputs_bundle` exit 0
    - `cargo build -p genossi_service_impl` exit 0
  </acceptance_criteria>
  <done>
    Beide build_inputs-Helper existieren, 5+ Tests verifizieren JSON-Shape inkl. NULL-IBAN-Edge-Case.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: render_repayment_letter + render_repayment_letter_bundle (sync Render-Methoden)</name>
  <files>genossi_service_impl/src/pdf_generation.rs</files>
  <read_first>
    - genossi_service_impl/src/pdf_generation.rs (Render-Methoden — render_repayment_list:386-441 als 1:1-Vorbild, render_attendance_list:313-370 als zweites Vorbild)
    - templates/defaults/auszahlungs_anschreiben.typ (Plan 01 Output — Single-Template mit render-letter)
    - templates/defaults/auszahlungs_anschreiben_bundle.typ (Plan 01 Output — Bundle-Wrapper)
    - .planning/phases/13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder/13-PATTERNS.md §pdf_generation.rs (Skelett mit typst::compile + typst_pdf::pdf)
  </read_first>
  <behavior>
    - Test 1: `render_repayment_letter` mit echtem Sample-Template + Sample-Member-mit-IBAN → Result `Ok(Vec<u8>)`, beginnt mit `b"%PDF-"`
    - Test 2: `render_repayment_letter` mit Member ohne IBAN → Result `Ok` (kein Render-Fehler), PDF wird erzeugt (NULL-IBAN-Switch greift)
    - Test 3: `render_repayment_letter` mit nicht-existentem Template-Pfad → `Err(ServiceError::InternalError(_))` mit "template not found"-Substring
    - Test 4: `render_repayment_letter_bundle` mit 2 Recipients → PDF >= Single-Letter-PDF-Groesse * 1.5 (Heuristik fuer "mehrere Seiten")
    - Test 5: Smoke: PDF beginnt mit `%PDF-` und Laenge >= 1000 bytes
  </behavior>
  <action>
    **Plan 03 modifiziert AUSSCHLIESSLICH `pdf_generation.rs`.** Templates + DEFAULT_TEMPLATES-Eintraege sind bereits in Plan 01 angelegt — kein template_storage.rs-Touching mehr.

    **Zwei neue Methoden auf `PdfGenerator` ergaenzen** (direkt nach `render_repayment_list`).

    **`render_repayment_letter`:**
    ```rust
    /// Phase 13 D-13-01: Render EIN Single-Letter-PDF fuer 1 Member.
    /// Synchrone Funktion (Typst ist nicht-async) — Caller commitet Tx VOR Aufruf (Pitfall #2).
    /// Template-Pfad ist typischerweise "auszahlungs_anschreiben.typ" (Plan 01 Output).
    pub fn render_repayment_letter(
        &self,
        template_path: &str,
        template_base: &Path,
        phase: &RepaymentPhaseEntity,
        member: &MemberEntity,
        ctx: &RepaymentContext,
    ) -> Result<Vec<u8>, ServiceError> {
        let full_path = template_base.join(template_path);
        let source_text = std::fs::read_to_string(&full_path).map_err(|e| {
            if e.kind() == std::io::ErrorKind::NotFound {
                ServiceError::InternalError(Arc::from(format!(
                    "template not found: {}",
                    full_path.display()
                ).as_str()))
            } else {
                ServiceError::InternalError(Arc::from(format!(
                    "template io error: {}",
                    e
                ).as_str()))
            }
        })?;

        let inputs = build_inputs_repayment_letter(phase, member, ctx);

        let world = TemplateWorld::new(
            &source_text,
            template_path,
            template_base.to_path_buf(),
            inputs,
            &self.fonts,
            &self.book,
            &self.package_cache,
        );

        let result = typst::compile::<PagedDocument>(&world);
        let document = match result.output {
            Ok(doc) => doc,
            Err(diagnostics) => {
                let messages: Vec<String> = diagnostics
                    .iter()
                    .map(|d| format!("{}", d.message))
                    .collect();
                return Err(ServiceError::InternalError(Arc::from(format!(
                    "typst compile errors: {}",
                    messages.join("\n")
                ).as_str())));
            }
        };

        let pdf = typst_pdf::pdf(&document, &typst_pdf::PdfOptions::default())
            .map_err(|e| ServiceError::InternalError(Arc::from(
                format!("pdf serialisation failed: {:?}", e).as_str()
            )))?;

        Ok(pdf)
    }
    ```

    **`render_repayment_letter_bundle`:**
    ```rust
    /// Phase 13 D-13-01: Render Bundle-PDF mit N Briefen in EINEM Typst-Compile.
    /// Recipients ist die Sortier-Reihenfolge (Pitfall #10 — Caller sortiert).
    /// Template ist typischerweise "auszahlungs_anschreiben_bundle.typ" (Plan 01 Output),
    /// das via `#import "auszahlungs_anschreiben.typ": render-letter` die Single-Source-of-Truth
    /// nutzt und mit `#pagebreak()` zwischen Recipients trennt.
    pub fn render_repayment_letter_bundle(
        &self,
        template_path: &str,
        template_base: &Path,
        phase: &RepaymentPhaseEntity,
        recipients: &[(MemberEntity, RepaymentContext)],
    ) -> Result<Vec<u8>, ServiceError> {
        let full_path = template_base.join(template_path);
        let source_text = std::fs::read_to_string(&full_path).map_err(|e| {
            if e.kind() == std::io::ErrorKind::NotFound {
                ServiceError::InternalError(Arc::from(format!(
                    "template not found: {}",
                    full_path.display()
                ).as_str()))
            } else {
                ServiceError::InternalError(Arc::from(format!(
                    "template io error: {}",
                    e
                ).as_str()))
            }
        })?;

        let inputs = build_inputs_repayment_letters_bundle(phase, recipients);

        let world = TemplateWorld::new(
            &source_text,
            template_path,
            template_base.to_path_buf(),
            inputs,
            &self.fonts,
            &self.book,
            &self.package_cache,
        );

        let result = typst::compile::<PagedDocument>(&world);
        let document = match result.output {
            Ok(doc) => doc,
            Err(diagnostics) => {
                let messages: Vec<String> = diagnostics
                    .iter()
                    .map(|d| format!("{}", d.message))
                    .collect();
                return Err(ServiceError::InternalError(Arc::from(format!(
                    "typst compile errors: {}",
                    messages.join("\n")
                ).as_str())));
            }
        };

        let pdf = typst_pdf::pdf(&document, &typst_pdf::PdfOptions::default())
            .map_err(|e| ServiceError::InternalError(Arc::from(
                format!("pdf serialisation failed: {:?}", e).as_str()
            )))?;

        Ok(pdf)
    }
    ```

    **Tests:**

    a) `test_render_repayment_letter_smoke` — echte Render-Call mit Sample-Daten gegen `templates/defaults/auszahlungs_anschreiben.typ`, Result startet mit `%PDF-`, Laenge >= 1000 bytes
    b) `test_render_repayment_letter_null_iban_renders_ok` — Member ohne `bank_account` rendert ohne Fehler (Pitfall #5 + D-13-06)
    c) `test_render_repayment_letter_template_not_found` — falscher Path → `Err` mit "template not found"-Substring
    d) `test_render_repayment_letter_bundle_smoke` — 2 Recipients → PDF startet mit `%PDF-` UND ist groesser als Single-PDF * 1.5:
       ```rust
       #[test]
       fn test_render_repayment_letter_bundle_smoke() {
           let gen = PdfGenerator::new(/* test-init */);
           let base = std::path::PathBuf::from("templates/defaults");
           let phase = sample_phase(12000);
           let m1 = sample_member_with_iban();
           let m2 = sample_member_without_iban();
           let ctx = sample_ctx(3, "360,00", 2025);

           let single_bytes = gen
               .render_repayment_letter("auszahlungs_anschreiben.typ", &base, &phase, &m1, &ctx)
               .expect("single render ok");

           let recipients = vec![(m1.clone(), ctx.clone()), (m2, ctx.clone())];
           let bundle_bytes = gen
               .render_repayment_letter_bundle("auszahlungs_anschreiben_bundle.typ", &base, &phase, &recipients)
               .expect("bundle render ok");

           assert!(bundle_bytes.starts_with(b"%PDF-"));
           // Bundle muss deutlich groesser als Single sein (mehrere Seiten via #pagebreak).
           // Heuristik: bundle >= 1.5x single (PDFs haben Strukturen-Overhead, aber 2 Briefe
           // ergeben mind. ~doppelte Content-Menge).
           assert!(
               bundle_bytes.len() > (single_bytes.len() as f64 * 1.5) as usize,
               "Bundle ({} bytes) muss mind. 1.5x Single ({} bytes) sein — sonst rendert Bundle-Template nicht beide Briefe",
               bundle_bytes.len(),
               single_bytes.len(),
           );
       }
       ```

    Test-Setup: nutze `PdfGenerator::new()` (bestehend) + Sample-Member + Sample-Phase + Sample-Context. Template-Base sollte auf `templates/defaults/`-Pfad ueber Projekt-Root zeigen — analog wie bestehende `test_render_repayment_list_smoke`-Tests.

    **Anmerkung:** Sollten Tests gegen Repo-Pfade (`templates/defaults/...`) im Test-Runner nicht funktionieren (CWD-Drift), darf das Test einen temp_dir verwenden und die beiden Plan-01-Template-Files dorthin kopieren. Plan-Discretion.
  </action>
  <verify>
    <automated>cargo test -p genossi_service_impl --lib pdf_generation::tests::test_render_repayment_letter</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'pub fn render_repayment_letter\b' genossi_service_impl/src/pdf_generation.rs` returns 1
    - `rg 'pub fn render_repayment_letter_bundle' genossi_service_impl/src/pdf_generation.rs` returns 1
    - `rg 'typst::compile' genossi_service_impl/src/pdf_generation.rs | wc -l` returns >=3 (existing + 2 new)
    - **Plan 03 Scope-Gate (KRITISCH):** `git diff --name-only` zeigt nur `genossi_service_impl/src/pdf_generation.rs` als modified — KEIN Touch an template_storage.rs oder templates/defaults/*.typ (das ist Plan 01's Verantwortung)
    - `cargo test -p genossi_service_impl --lib pdf_generation::tests::test_render_repayment_letter_smoke` exit 0
    - `cargo test -p genossi_service_impl --lib pdf_generation::tests::test_render_repayment_letter_null_iban_renders_ok` exit 0
    - `cargo test -p genossi_service_impl --lib pdf_generation::tests::test_render_repayment_letter_template_not_found` exit 0
    - `cargo test -p genossi_service_impl --lib pdf_generation::tests::test_render_repayment_letter_bundle_smoke` exit 0
    - `cargo build -p genossi_service_impl` exit 0
  </acceptance_criteria>
  <done>
    Beide Render-Methoden in pdf_generation.rs; 4 Smoke-Tests gruen (inkl. NULL-IBAN-Edge-Case und Bundle-Size-Check); kein Touch an Plan-01-Artefakten.
  </done>
</task>

</tasks>

<threat_model>
- **Typst-Compile-Crash bei Layout-Drift im Plan-01-Template**: MEDIUM — wenn das Single-Template `render-letter` nicht korrekt exportiert, brechen Single- UND Bundle-Render. Mitigation: Smoke-Test mit echtem File rendert tatsaechlich; Plan 07 E2E-Test visualisiert.
- **PII-Leak via build_inputs-Logging**: LOW — `serde_json::to_string` rendert JSON ohne Logging-Side-Effect; PII bleibt im PDF-Output (D-13-09 erwartet). Mitigation: keine `tracing::*`-Calls in den build_inputs-Helpers.
- **Bundle-Render-Out-of-Memory bei sehr grossem N**: MEDIUM — bei N=200+ Recipients koennte das Bundle-PDF >50MB werden. Mitigation: Plan 04 fuehrt server-side Limit (z.B. 200) ein; fuer realistische Genossi-Use-Cases (10-30 Member) nicht relevant.
- **Template-not-found-Bypass beim Render**: HIGH — wenn template_path falsch ist und das Template nicht provisioniert wurde, faellt Letter-Service mit InternalError 500. Mitigation: Plan 01 DEFAULT_TEMPLATES-Registrierung garantiert beide Files auf Startup; Smoke-Test "template_not_found" verifiziert die Error-Pfad-Semantik.
- **Path-Traversal via template_path**: LOW — `template_base.join(template_path)` mit konstantem hardcoded "auszahlungs_anschreiben.typ"/"_bundle.typ" lässt keinen User-Input zu. Mitigation: Plan 04 ruft mit Konstanten, nicht mit User-Input.
- **Bundle-Size-Heuristik (>1.5x Single) ist false-positive-anfaellig**: LOW — falls Bundle nur knapp groesser ist, koennte der Test flaken. Mitigation: 1.5x ist konservativ (zwei volle Briefe ergeben praktisch >2x); falls trotzdem Flakiness, alternativ Page-Count via typst::layout::PagedDocument-Inspektion pruefen — Plan-Discretion.
</threat_model>

<verification>
- cargo test -p genossi_service_impl --lib pdf_generation gruen (alle neuen Tests + bestehende)
- cargo build -p genossi_service_impl exit 0
- Plan-03-Scope-Gate: nur pdf_generation.rs modifiziert (kein Touch an Plan-01-Artefakten)
</verification>

<success_criteria>
1. Render-Methoden sind verfuegbar fuer Plan 04 Service-Impl
2. Smoke-Tests belegen: Single + Bundle PDFs werden tatsaechlich rendered ohne Crash
3. NULL-IBAN-Edge-Case (Pitfall #5) ist verifiziert (D-13-06 Brief-Baustein 3 funktioniert)
4. Bundle-Size-Test belegt: Bundle rendert tatsaechlich mehrere Recipients (nicht stillschweigend nur 1)
</success_criteria>

<output>
After completion, create `.planning/phases/13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder/13-03-SUMMARY.md`
</output>
