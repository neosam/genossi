---
phase: 13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - genossi_service/src/member_document.rs
  - genossi_service_impl/src/template_storage.rs
  - templates/defaults/auszahlungs_anschreiben.typ
  - templates/defaults/auszahlungs_anschreiben_bundle.typ
autonomous: true
requirements: [BRIEF-01]
tags: [phase-13, foundation, document-type, template, typst, letter, bundle]

must_haves:
  truths:
    - "DocumentType::RepaymentLetter exists and round-trips through as_str/from_str"
    - "DocumentType::RepaymentLetter.is_singleton() == false (D-13-08 — re-generation allowed)"
    - "DocumentType::RepaymentLetter.template_path() == None (Brief-Pfad ueber Service, nicht ueber Mapping)"
    - "Single-Letter-Template auszahlungs_anschreiben.typ exportiert `render-letter(member, repayment, today)` als Typst-Funktion (Single-Source-of-Truth fuer Bundle)"
    - "Bundle-Template auszahlungs_anschreiben_bundle.typ importiert render-letter via #import und iteriert mit #pagebreak() zwischen Recipients (Single-Source-of-Truth — KEIN duplizierter Brief-Body)"
    - "BEIDE Default-Templates sind in DEFAULT_TEMPLATES registriert (D-13-05) und werden auf Startup provisioniert"
    - "Single-Template enthaelt 4 Bausteine: Reference-Block, Auszahlungsbetrag-Absatz, IBAN-Switch (#if member.bank_account != none), Vorstands-Signatur (D-13-06)"
    - "Templates sind UI-editierbar via existierender /templates-Editor (D-13-05)"
    - "Single-Template enthaelt KEINEN SEPA-Verwendungszweck (D-13-07)"
    - "Brief-Body-Strings (Carolin Weidmann, Anrede, IBAN-Switch) leben NUR im Single-Template — keine Duplikate im Bundle"
  artifacts:
    - path: "genossi_service/src/member_document.rs"
      provides: "DocumentType::RepaymentLetter variant + tests"
      contains: "RepaymentLetter"
    - path: "genossi_service_impl/src/template_storage.rs"
      provides: "DEFAULT_TEMPLATES entries fuer auszahlungs_anschreiben.typ UND auszahlungs_anschreiben_bundle.typ"
      contains: "auszahlungs_anschreiben"
    - path: "templates/defaults/auszahlungs_anschreiben.typ"
      provides: "Single-Letter-Template + exportierte render-letter-Funktion (letter-pro:3.0.0)"
      min_lines: 50
    - path: "templates/defaults/auszahlungs_anschreiben_bundle.typ"
      provides: "Bundle-Wrapper: importiert render-letter + iteriert ueber recipients mit pagebreak"
      min_lines: 10
  key_links:
    - from: "genossi_service_impl/src/template_storage.rs"
      to: "templates/defaults/auszahlungs_anschreiben.typ"
      via: "include_bytes!(\"../../templates/defaults/auszahlungs_anschreiben.typ\")"
      pattern: "include_bytes!.*auszahlungs_anschreiben\\.typ"
    - from: "genossi_service_impl/src/template_storage.rs"
      to: "templates/defaults/auszahlungs_anschreiben_bundle.typ"
      via: "include_bytes!(\"../../templates/defaults/auszahlungs_anschreiben_bundle.typ\")"
      pattern: "include_bytes!.*auszahlungs_anschreiben_bundle\\.typ"
    - from: "templates/defaults/auszahlungs_anschreiben_bundle.typ"
      to: "templates/defaults/auszahlungs_anschreiben.typ"
      via: "#import \"auszahlungs_anschreiben.typ\": render-letter"
      pattern: "#import \"auszahlungs_anschreiben\\.typ\""
    - from: "templates/defaults/auszahlungs_anschreiben.typ"
      to: "PdfGenerator sys.inputs JSON-Kontext"
      via: "json.decode(sys.inputs.at(\"member\")) + json.decode(sys.inputs.at(\"repayment\"))"
      pattern: "sys\\.inputs\\.at"
---

<objective>
Foundation-Wave: neue DocumentType-Variante + BEIDE Default-Templates (Single + Bundle) + DEFAULT_TEMPLATES-Registrierung. Kein Service-Code, kein Renderer-Code — diese Wave macht alle Template-Artefakte bereit, auf denen Plans 02-07 aufbauen. Plan 03 modifiziert nur noch pdf_generation.rs.

Purpose: D-13-05 + D-13-06 + D-13-08 + D-13-01-Bundle-Foundation implementieren. Single-Source-of-Truth via Typst-`#let render-letter(...)`-Funktion verhindert Brief-Body-Drift zwischen Single und Bundle.
Output:
- Erweiterte DocumentType-Enum (5 Match-Arms erweitert, alle 4 Test-Funktionen ergaenzt)
- Single-Letter-Template `auszahlungs_anschreiben.typ` mit `#let render-letter(member, repayment, today) = { ... }`
- Bundle-Template `auszahlungs_anschreiben_bundle.typ` (importiert render-letter, iteriert ueber recipients, pagebreak dazwischen)
- ZWEI DEFAULT_TEMPLATES-Eintraege mit `include_bytes!`
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder/13-CONTEXT.md
@.planning/phases/13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder/13-RESEARCH.md
@.planning/phases/13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder/13-PATTERNS.md

<interfaces>
<!-- Existing DocumentType enum + helpers — extracted from genossi_service/src/member_document.rs:48-101 -->
```rust
pub enum DocumentType {
    JoinDeclaration,
    JoinConfirmation,
    ShareIncrease,
    Other,
    RepaymentMail,   // Phase 10 D-09
}

impl DocumentType {
    pub fn as_str(&self) -> &str { /* 5 arms */ }
    pub fn from_str(s: &str) -> Option<Self> { /* 5 arms */ }
    pub fn is_singleton(&self) -> bool {
        matches!(self, DocumentType::JoinDeclaration | DocumentType::JoinConfirmation)
    }
    pub fn template_path(&self) -> Option<&str> { /* per-variant explicit, NO wildcard */ }
}
```

<!-- Existing DEFAULT_TEMPLATES entry pattern — extracted from genossi_service_impl/src/template_storage.rs:27-34 -->
```rust
DefaultTemplate {
    path: "auszahlungsliste.typ",
    content: include_bytes!("../../templates/defaults/auszahlungsliste.typ"),
},
```

<!-- Layout-Vorbild: templates/zahlungsanfrage.typ (letter-pro:3.0.0 letter-simple, Falzmarken, Logo, Signatur) -->
<!-- sys.inputs Vorbild: templates/defaults/auszahlungsliste.typ -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DocumentType::RepaymentLetter-Variante + Tests</name>
  <files>genossi_service/src/member_document.rs</files>
  <read_first>
    - genossi_service/src/member_document.rs (gesamte Datei, mind. Z. 48-300 — Enum + 4 helper-Funktionen + bestehende Tests fuer RepaymentMail-Variante als Test-Vorbild)
    - .planning/phases/13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder/13-PATTERNS.md §`genossi_service/src/member_document.rs:48-101`
  </read_first>
  <behavior>
    - Test 1 (as_str): `DocumentType::RepaymentLetter.as_str()` returns `"repayment_letter"`
    - Test 2 (from_str): `DocumentType::from_str("repayment_letter")` returns `Some(DocumentType::RepaymentLetter)`
    - Test 3 (is_singleton): `assert!(!DocumentType::RepaymentLetter.is_singleton())` — Begruendung in Comment auf D-13-08 verweisen
    - Test 4 (template_path): `assert_eq!(DocumentType::RepaymentLetter.template_path(), None)` — Begruendung in Comment auf D-LETT-04 verweisen (Template lebt unter eigenem Slug, nicht ueber DocumentType-Mapping erreichbar)
    - Test 5 (from_str unknown): `DocumentType::from_str("unknown_xyz")` returns `None` (Regression — bestehender Test muss noch passen)
  </behavior>
  <action>
    Erweitere `DocumentType`-Enum in `genossi_service/src/member_document.rs` um eine neue Variante `RepaymentLetter`. Strikte Vorgaben:

    1. **Variante hinzufuegen** (im Enum-Block, nach `RepaymentMail`):
       ```rust
       /// Phase 13 D-LETT-04 / D-13-05: PDF-Anschreiben fuer Auszahlungsphase.
       /// Persisted file im document_storage. is_singleton=false (D-13-08), template_path=None.
       RepaymentLetter,
       ```
    2. **`as_str()`**: Match-Arm `DocumentType::RepaymentLetter => "repayment_letter"` ergaenzen.
    3. **`from_str()`**: Match-Arm `"repayment_letter" => Some(DocumentType::RepaymentLetter)` ergaenzen.
    4. **`is_singleton()`**: Variante NICHT in die `matches!(...)`-Liste aufnehmen (false durch Standard-Fallthrough — D-13-08: Re-Generierung erlaubt). Vor der Funktion einen Kommentar setzen, der D-13-08 zitiert.
    5. **`template_path()`**: Explizit-per-Variante-Match-Arm `DocumentType::RepaymentLetter => None` hinzufuegen — KEINE Wildcard `_ =>` verwenden (Phase-10-Pattern). Begruendung als Doc-Kommentar: D-LETT-04 (Template wird vom Service mit hardcoded Pfaden `"auszahlungs_anschreiben.typ"` / `"auszahlungs_anschreiben_bundle.typ"` aufgerufen, nicht ueber dieses Mapping).
    6. **Tests** im bestehenden `#[cfg(test)] mod tests`-Block: ergaenze 4 neue Tests im Stil der bestehenden RepaymentMail-Tests (`test_repayment_mail_as_str` etc.):
       - `test_repayment_letter_as_str`
       - `test_repayment_letter_from_str`
       - `test_repayment_letter_is_singleton_false_per_d13_08` mit `assert!(!DocumentType::RepaymentLetter.is_singleton(), "D-13-08: Re-Generierung erlaubt — KEIN singleton")`
       - `test_repayment_letter_template_path_none`
    7. **Imports/Sichtbarkeit**: keine Aenderungen noetig (Variante erbt vom `pub enum`).

    KEINE Aenderungen an `MemberDocument`-Struct, DAO oder anderen Files in dieser Task.
  </action>
  <verify>
    <automated>cargo test -p genossi_service --lib member_document::tests::test_repayment_letter</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'DocumentType::RepaymentLetter' genossi_service/src/member_document.rs` returns >=5 Matches (Enum-Variant + 4 Match-Arms)
    - `rg '"repayment_letter"' genossi_service/src/member_document.rs` returns 2 Matches (as_str + from_str)
    - `rg 'test_repayment_letter_' genossi_service/src/member_document.rs` returns 4 Matches
    - `cargo test -p genossi_service --lib member_document::tests::test_repayment_letter` exit 0
    - `cargo test -p genossi_service --lib member_document` exit 0 (bestehende RepaymentMail-Tests bleiben gruen)
    - `cargo build -p genossi_service` exit 0
    - Grep-Gate: `rg 'DocumentType::RepaymentLetter\s*=>\s*None' genossi_service/src/member_document.rs` returns >=1 (template_path explizit, keine Wildcard)
  </acceptance_criteria>
  <done>
    Variante RepaymentLetter im Enum + 4 helper-Match-Arms vollstaendig; 4 neue Tests gruen; cargo build sauber.
  </done>
</task>

<task type="auto">
  <name>Task 2: Single-Letter-Template (mit render-letter-Funktion) + Bundle-Template + DEFAULT_TEMPLATES-Registrierung beider</name>
  <files>templates/defaults/auszahlungs_anschreiben.typ, templates/defaults/auszahlungs_anschreiben_bundle.typ, genossi_service_impl/src/template_storage.rs</files>
  <read_first>
    - templates/zahlungsanfrage.typ (gesamte Datei, Layout-Vorbild fuer letter-pro:3.0.0 + Falzmarken + Logo + Signatur)
    - templates/defaults/auszahlungsliste.typ (sys.inputs-JSON-Kontext-Vorbild)
    - genossi_service_impl/src/template_storage.rs (gesamte Datei — DEFAULT_TEMPLATES-Array, Provisioning-Logik)
    - .planning/phases/13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder/13-PATTERNS.md §`templates/defaults/auszahlungs_anschreiben.typ` (komplettes Layout-Skelett)
    - .planning/phases/13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder/13-CONTEXT.md §D-13-06 (4 Brief-Bausteine), §D-13-07 (KEIN SEPA-Verwendungszweck)
    - .planning/phases/13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder/13-RESEARCH.md §Pattern 5 (letter-simple Layout) + §Pattern 6 (Bundle-Loop)
  </read_first>
  <action>
    **Strategie (Single-Source-of-Truth via Typst-Funktion):**
    Der Brief-Body-Code lebt EINMAL als `#let render-letter(member, repayment, today) = { ... }` im Single-Template. Bundle-Template macht `#import "auszahlungs_anschreiben.typ": render-letter` und iteriert mit `#pagebreak()` zwischen Recipients. Kein duplizierter Brief-Body, keine Drift-Gefahr.

    **Schritt 1: Single-Letter-Template `templates/defaults/auszahlungs_anschreiben.typ` erstellen.**

    Aufbau wie folgt (alle wortgleichen Strings + Variablen-Verweise sind verbindlich, der `show: letter-simple.with(...)`-Block bleibt am Top-Level damit das Layout greift):

    ```typst
    // Phase 13 D-13-05 / D-13-06: RepaymentLetter Default-Template.
    // Single-Source-of-Truth fuer Brief-Body via `render-letter`-Funktion.
    // Bundle-Variante (auszahlungs_anschreiben_bundle.typ) importiert diese Funktion
    // und iteriert ueber recipients[] — siehe Plan 13-01 must_haves.

    #import "@preview/letter-pro:3.0.0": letter-simple

    #set text(lang: "de")

    // ─── Single-Letter-Modus: sys.inputs lesen ────────────────────────────────────
    #let member = json.decode(sys.inputs.at("member"))
    #let repayment = json.decode(sys.inputs.at("repayment"))
    #let today = sys.inputs.at("today")

    // ─── Helper: Anrede-String ableiten (Herr -> "Lieber", Frau -> "Liebe", sonst "Hallo")
    #let anrede-for(member) = if member.salutation == "Herr" {
        "Lieber"
      } else if member.salutation == "Frau" {
        "Liebe"
      } else {
        "Hallo"
      }

    // ─── Helper: Name-Block ableiten (mit/ohne title) ─────────────────────────────
    #let name-for(member) = if member.title != none {
        [#member.title #member.first_name #member.last_name]
      } else {
        [#member.first_name #member.last_name]
      }

    // ─── render-letter: EXPORTED FUNCTION — Single-Source-of-Truth Brief-Body ────
    // Wird sowohl direkt im Single-Mode unten aufgerufen als auch von
    // auszahlungs_anschreiben_bundle.typ via `#import "auszahlungs_anschreiben.typ": render-letter`.
    #let render-letter(member, repayment, today) = {
      let name = name-for(member)
      let anrede = anrede-for(member)

      // Sender + Recipient-Block via letter-simple
      show: letter-simple.with(
        sender: (
          name: "nebenan & unverpackt München W. eG",
          address: "Willibaldstr. 18, 80687 München",
          extra: [
            Telefon: #link("tel:08954637600")[+089 - 54 63 76 00]\
            Mitgliederverwaltung: #link("mailto:mv@nebenan-unverpackt.de")[mv\@nebenan-unverpackt.de]\
          ],
        ),
        recipient: [
          #name \
          #member.street #member.house_number \
          #member.postal_code #member.city
        ],
        date: [#today],
        subject: "Auszahlung deiner Anteile",
        folding-marks: true,
      )

      place(top + left, dx: -0.55cm, dy: -0.5cm,
            image("nebenan-unverpackt-logo.svg", width: 5cm))

      line(length: 16.5cm, stroke: 0.5pt + gray)

      // ─── D-13-06 Baustein 1: Reference-Block ────────────────────────────────────
      table(
        columns: (1fr, 1fr),
        stroke: none,
        [*Mitgliedsnummer:*], [#member.member_number],
        [*Anteile zur Auszahlung:*], [#repayment.share_count],
        [*Auszahlungsbetrag:*], [#repayment.payout_amount €],
      )

      line(length: 16.5cm, stroke: 0.5pt + gray)
      v(1cm)

      // ─── D-13-06 Baustein 2: Anrede + Auszahlungsbetrag-Absatz ──────────────────
      [#anrede #name,]
      v(0.5em)
      [deine Anteile aus dem Geschäftsjahr #repayment.fiscal_year werden in Kürze ausgezahlt.]
      v(0.5em)

      // ─── D-13-06 Baustein 3: IBAN-Switch (Pitfall #5) ───────────────────────────
      if member.bank_account != none [
        Wir überweisen den Betrag in Höhe von #repayment.payout_amount € auf deine
        hinterlegte IBAN: *#member.bank_account*.
      ] else [
        *Wir haben keine IBAN von dir hinterlegt* — bitte teile sie uns unter
        #link("mailto:mv@nebenan-unverpackt.de")[mv\@nebenan-unverpackt.de] mit,
        damit wir dir den Betrag in Höhe von #repayment.payout_amount € überweisen können.
      ]

      v(1cm)

      // ─── D-13-06 Baustein 4: Vorstands-Signatur (hardcoded) ─────────────────────
      [Herzliche Grüße,]
      v(0.5em)
      [Carolin Weidmann, Dina Beier und Simon Goller]
    }

    // ─── Single-Letter-Modus: render-letter direkt aufrufen ──────────────────────
    #render-letter(member, repayment, today)
    ```

    **WICHTIG zur Typst-Syntax:** Falls `show: letter-simple.with(...)` innerhalb einer Funktion nicht erlaubt ist (Typst-Scope-Regel), kann der Layout-Aufruf alternativ via context-Block oder via `set` umgesetzt werden. Implementer prueft beim ersten Compile-Versuch und passt die Funktionsdefinition entsprechend an. Im Notfall darf das `show: letter-simple.with(...)`-Setup AUSSERHALB der Funktion stehen und die Funktion liefert nur den Brief-Inhalt — solange das Single-Source-of-Truth-Prinzip gewahrt bleibt (kein duplizierter Brief-Body im Bundle).

    **D-13-07 (KEIN SEPA-Verwendungszweck):** weder Single noch Bundle duerfen "Verwendungszweck" enthalten.

    **Schritt 2: Bundle-Template `templates/defaults/auszahlungs_anschreiben_bundle.typ` erstellen.**

    ```typst
    // Phase 13 D-13-01: Bundle-Variante — N Briefe in EINEM Typst-Compile,
    // mit #pagebreak() zwischen Recipients.
    // Single-Source-of-Truth: importiert render-letter aus dem Single-Letter-Template.

    #import "auszahlungs_anschreiben.typ": render-letter

    #set text(lang: "de")

    #let recipients = json.decode(sys.inputs.at("recipients"))
    #let today = sys.inputs.at("today")

    #for (i, r) in recipients.enumerate() {
      render-letter(r.member, r.repayment, today)
      if i < recipients.len() - 1 {
        pagebreak()
      }
    }
    ```

    **Schritt 3: BEIDE DEFAULT_TEMPLATES-Eintraege in `genossi_service_impl/src/template_storage.rs` ergaenzen.**

    Direkt UNTER dem bestehenden `auszahlungsliste.typ`-Eintrag (vermutlich Z. 27-34) ergaenzen:
    ```rust
    // Phase 13 D-13-05: RepaymentLetter Single-Letter Default-Template.
    // UI-editierbar via /templates-Editor; DEFAULT_TEMPLATES liefert nur den Initial-Wert.
    // RESEARCH §Pitfall #1: ohne diesen Eintrag faellt das Letter-Service
    // mit "template not found"-InternalError beim ersten Render.
    DefaultTemplate {
        path: "auszahlungs_anschreiben.typ",
        content: include_bytes!("../../templates/defaults/auszahlungs_anschreiben.typ"),
    },
    // Phase 13 D-13-01: RepaymentLetter Bundle-Wrapper-Template.
    // Importiert render-letter aus auszahlungs_anschreiben.typ (Single-Source-of-Truth)
    // und iteriert ueber recipients[] mit #pagebreak() dazwischen.
    DefaultTemplate {
        path: "auszahlungs_anschreiben_bundle.typ",
        content: include_bytes!("../../templates/defaults/auszahlungs_anschreiben_bundle.typ"),
    },
    ```

    **Schritt 4: Tests in `template_storage.rs` erweitern** — verifiziere BEIDE neuen Eintraege:
    ```rust
    #[test]
    fn test_default_templates_contains_auszahlungs_anschreiben_single() {
        assert!(DEFAULT_TEMPLATES.iter().any(|t| t.path == "auszahlungs_anschreiben.typ"));
        let entry = DEFAULT_TEMPLATES.iter()
            .find(|t| t.path == "auszahlungs_anschreiben.typ").unwrap();
        assert!(!entry.content.is_empty());
        let text = std::str::from_utf8(entry.content).expect("template is UTF-8");
        assert!(text.contains("letter-pro"), "letter-pro-Import muss vorhanden sein");
        assert!(text.contains("sys.inputs"), "sys.inputs-Pattern muss verwendet werden");
        assert!(text.contains("bank_account"), "IBAN-Switch muss vorhanden sein (D-13-06)");
        assert!(text.contains("Carolin Weidmann"), "Vorstands-Signatur (D-13-06 Baustein 4)");
        assert!(text.contains("render-letter"), "render-letter-Funktion muss exportiert sein (Single-Source-of-Truth)");
        assert!(!text.contains("Verwendungszweck"), "D-13-07: KEIN SEPA-Verwendungszweck");
    }

    #[test]
    fn test_default_templates_contains_auszahlungs_anschreiben_bundle() {
        assert!(DEFAULT_TEMPLATES.iter().any(|t| t.path == "auszahlungs_anschreiben_bundle.typ"));
        let entry = DEFAULT_TEMPLATES.iter()
            .find(|t| t.path == "auszahlungs_anschreiben_bundle.typ").unwrap();
        assert!(!entry.content.is_empty());
        let text = std::str::from_utf8(entry.content).expect("template is UTF-8");
        assert!(text.contains("#import \"auszahlungs_anschreiben.typ\""),
            "Bundle MUSS render-letter aus Single-Template importieren (Single-Source-of-Truth)");
        assert!(text.contains("render-letter"), "Bundle ruft render-letter auf");
        assert!(text.contains("pagebreak"), "Bundle MUSS #pagebreak() zwischen Briefen setzen (D-13-01)");
        assert!(text.contains("recipients"), "Bundle iteriert ueber recipients-Array");
        // Drift-Schutz: Brief-Body-Strings duerfen NICHT im Bundle dupliziert sein.
        assert!(!text.contains("Carolin Weidmann"),
            "Drift-Schutz: Vorstands-Signatur lebt NUR im Single-Template (Single-Source-of-Truth)");
        assert!(!text.contains("Mitgliedsnummer"),
            "Drift-Schutz: Reference-Block-Strings leben NUR im Single-Template");
    }
    ```

    KEINE Aenderungen an PdfGenerator, Service-Layer, oder REST-Layer in dieser Task.
  </action>
  <verify>
    <automated>cargo test -p genossi_service_impl --lib template_storage</automated>
  </verify>
  <acceptance_criteria>
    - File existiert: `test -f templates/defaults/auszahlungs_anschreiben.typ`
    - File existiert: `test -f templates/defaults/auszahlungs_anschreiben_bundle.typ`
    - `rg '#let render-letter' templates/defaults/auszahlungs_anschreiben.typ` returns >=1 (Single-Source-of-Truth Funktion)
    - `rg 'letter-pro' templates/defaults/auszahlungs_anschreiben.typ` returns >=1 (Layout-Import)
    - `rg 'sys.inputs.at' templates/defaults/auszahlungs_anschreiben.typ` returns >=3 (member, repayment, today)
    - `rg '#if member\.bank_account != none' templates/defaults/auszahlungs_anschreiben.typ` returns 1 (D-13-06 IBAN-Switch, Pitfall #5)
    - `rg 'mv@nebenan-unverpackt.de' templates/defaults/auszahlungs_anschreiben.typ` returns >=1 (NULL-IBAN-Hinweis, D-13-06)
    - `rg 'Carolin Weidmann, Dina Beier und Simon Goller' templates/defaults/auszahlungs_anschreiben.typ` returns 1 (Vorstands-Signatur, D-13-06)
    - `rg 'Verwendungszweck' templates/defaults/auszahlungs_anschreiben.typ` returns 0 (D-13-07: KEIN SEPA-Verwendungszweck)
    - `rg 'Auszahlung deiner Anteile' templates/defaults/auszahlungs_anschreiben.typ` returns 1 (Subject)
    - `rg '#import "auszahlungs_anschreiben.typ": render-letter' templates/defaults/auszahlungs_anschreiben_bundle.typ` returns 1 (Single-Source-of-Truth-Import)
    - `rg 'pagebreak\(\)' templates/defaults/auszahlungs_anschreiben_bundle.typ` returns >=1 (D-13-01 Multi-Letter-Druck)
    - `rg 'recipients' templates/defaults/auszahlungs_anschreiben_bundle.typ` returns >=1
    - **Drift-Schutz (KRITISCH):** `rg 'Carolin Weidmann' templates/defaults/auszahlungs_anschreiben_bundle.typ` returns 0 (Brief-Body-Strings duerfen NICHT dupliziert sein — Single-Source-of-Truth)
    - **Drift-Schutz:** `rg 'Mitgliedsnummer' templates/defaults/auszahlungs_anschreiben_bundle.typ` returns 0
    - `rg 'auszahlungs_anschreiben\.typ' genossi_service_impl/src/template_storage.rs` returns >=2 (path + include_bytes fuer Single)
    - `rg 'auszahlungs_anschreiben_bundle\.typ' genossi_service_impl/src/template_storage.rs` returns >=2 (path + include_bytes fuer Bundle)
    - `rg 'include_bytes!\("../../templates/defaults/auszahlungs_anschreiben\.typ"\)' genossi_service_impl/src/template_storage.rs` returns 1
    - `rg 'include_bytes!\("../../templates/defaults/auszahlungs_anschreiben_bundle\.typ"\)' genossi_service_impl/src/template_storage.rs` returns 1
    - `cargo test -p genossi_service_impl --lib template_storage::tests::test_default_templates_contains_auszahlungs_anschreiben_single` exit 0
    - `cargo test -p genossi_service_impl --lib template_storage::tests::test_default_templates_contains_auszahlungs_anschreiben_bundle` exit 0
    - `cargo build -p genossi_service_impl` exit 0 (verifiziert dass beide include_bytes! die Files finden)
  </acceptance_criteria>
  <done>
    Single-Template mit exportierter render-letter-Funktion + Bundle-Template mit #import + 4 D-13-06-Bausteinen (Single) + zwei DEFAULT_TEMPLATES-Eintraege; Drift-Schutz-Greps gruen.
  </done>
</task>

</tasks>

<threat_model>
- **Template-Source-Tampering (Build-Time)**: LOW — `include_bytes!` haerdcodet beide Files zum Compile-Zeitpunkt, kein Runtime-Path-Traversal moeglich. Mitigation: `templates/defaults/`-Pfad im Repo unter git-Versionskontrolle.
- **PII-Exposure im Default-Wortlaut**: LOW — Default-Templates enthalten nur Vorstands-Signatur (oeffentlich) und Sender-Adresse (oeffentlich), keine Mitglieds-PII. Mitigation: Member-Daten kommen ausschliesslich via `sys.inputs` zur Render-Zeit.
- **NULL-IBAN als Render-Fehler (Pitfall #5)**: HIGH (if not mitigated) — wenn Template `#member.bank_account` ohne `#if`-Guard referenziert, faellt Typst-Compile fuer Member ohne IBAN. Mitigation: Acceptance-Criteria erzwingt `#if member.bank_account != none`-Grep-Treffer (Plan 07 E2E-Test "IBAN-NULL" verifiziert end-to-end).
- **Brief-Body-Drift zwischen Single und Bundle**: HIGH (if not mitigated) — wenn Bundle den Brief-Body dupliziert statt importiert, fuehrt eine UI-Edit des Single-Templates dazu, dass Bundle-PDFs den alten Wortlaut zeigen. Mitigation: Drift-Schutz-Greps (Bundle darf "Carolin Weidmann", "Mitgliedsnummer" nicht enthalten); `#import "...": render-letter`-Grep-Gate.
- **Self-invalidating grep gate fuer "Verwendungszweck"**: LOW — Acceptance prueft `rg 'Verwendungszweck' ... returns 0` auf reines Typst-File ohne header-Komentare die das Wort enthalten — sauber.
</threat_model>

<verification>
- cargo test -p genossi_service --lib member_document gruen (bestehende Tests + 4 neue)
- cargo test -p genossi_service_impl --lib template_storage gruen (mind. 2 neue Tests)
- cargo build -p genossi_service_impl exit 0 (beide include_bytes! finden die Files)
- Beide Template-Files existieren; Bundle importiert render-letter aus Single (Single-Source-of-Truth)
- Drift-Schutz: Brief-Body-Strings sind NICHT im Bundle dupliziert
</verification>

<success_criteria>
1. `DocumentType::RepaymentLetter` ist verfuegbar fuer Plans 02-07
2. `auszahlungs_anschreiben.typ` ist auf Startup ueber `provision_defaults()` im `TEMPLATE_PATH` verfuegbar
3. `auszahlungs_anschreiben_bundle.typ` ist auf Startup verfuegbar und importiert die Single-Letter-Logik (kein Duplikat)
4. Vorstand kann nach Startup das Single-Template via `/templates`-Editor lesen+schreiben — Bundle-Variante uebernimmt die Aenderungen automatisch (D-13-05)
</success_criteria>

<output>
After completion, create `.planning/phases/13-repaymentletter-bulk-anschreiben-f-r-nicht-email-mitglieder/13-01-SUMMARY.md`
</output>
