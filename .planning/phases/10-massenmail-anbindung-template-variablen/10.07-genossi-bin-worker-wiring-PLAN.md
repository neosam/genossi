---
phase: 10-massenmail-anbindung-template-variablen
plan: 07
type: execute
wave: 4
depends_on:
  - 06
files_modified:
  - genossi_bin/src/lib.rs
autonomous: true
requirements:
  - MAIL-01
  - MAIL-02
  - MAIL-03
  - MAIL-04

must_haves:
  truths:
    - "RestStateImpl-Struct enthaelt nach Task 1 fuenf neue Felder: member_document_dao, repayment_phase_dao, repayment_entry_dao, mail_template_dao, worker_member_document_dao (oder ein analoger eindeutiger Name)"
    - "RestStateImpl::new() initialisiert und persistiert diese 5 DAOs in self statt sie nur als lokale Variablen zu halten"
    - "RestStateImpl::start_mail_worker uebergibt 6 neue Deps an genossi_mail::worker::start_mail_worker"
    - "Geteilte DAOs (audit_log_dao, transaction_dao, plus die 4 neuen) via Arc::clone aus RestStateImpl-Feldern (KEINE neuen Arc::new(...)-Aufrufe im Worker-Spawn-Block)"
    - "Cargo workspace kompiliert"
  artifacts:
    - path: "genossi_bin/src/lib.rs"
      provides: "Erweiterte RestStateImpl-Struct (Task 1) + erweiterte start_mail_worker (Task 2)"
      contains: "member_document_dao: Arc<MemberDocumentDao>"
  key_links:
    - from: "RestStateImpl-Struct"
      to: "Worker-Argumente"
      via: "self.X.clone() aus persistenten Feldern"
      pattern: "member_document_dao: member_document_dao.clone\\(\\)"
    - from: "RestStateImpl::start_mail_worker"
      to: "genossi_mail::worker::start_mail_worker"
      via: "Arc::clone der 6 neuen DAOs"
      pattern: "audit_log_dao\\.clone\\(\\)"
---

<objective>
Aktualisiere `genossi_bin/src/lib.rs::RestStateImpl` so, dass die im Mail-Worker (Plan 10.06) benoetigten DAOs (a) als Struct-Felder existieren und (b) im Worker-Spawn-Block korrekt durchgereicht werden.

**KRITISCHER FAKT (per Codebase-Verifikation Z. 480-840):** Vor diesem Plan existieren NUR 2 der 6 benoetigten DAOs als RestStateImpl-Felder:
- ✓ `audit_log_dao` (Z. 523, als `Arc<AuditLogDao>`)
- ✓ `transaction_dao` (Z. 323, als `Arc<TransactionDao>`)

Die anderen 4 sind nur lokale `let`-Bindings in `RestStateImpl::new()`:
- ✗ `member_document_dao` (lokal Z. 589, nicht als Field)
- ✗ `repayment_phase_dao` (lokal Z. 751, nicht als Field)
- ✗ `repayment_entry_dao` (lokal Z. 752, nicht als Field)
- ✗ `mail_template_dao` (lokal Z. 833, nicht als Field)

`start_mail_worker` (Z. 1141) wuerde aktuell `self.member_document_dao.clone()` etc. NICHT compilieren. **Loesungs-Pfad (OPTION A — preferred per Checker):** Erweitere zuerst die Struct + new() um die 4 fehlenden Felder, dann ergaenze `start_mail_worker`.

Purpose: Macht den Worker funktionsfaehig. Ohne diesen Plan kompiliert genossi_bin nicht (Plan 10.06 brach die start_mail_worker-Signatur intentional).
Output: ein modifiziertes File, zwei klar getrennte Aenderungen (Struct-Felder + Worker-Spawn).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-massenmail-anbindung-template-variablen/10-CONTEXT.md
@.planning/phases/10-massenmail-anbindung-template-variablen/10-PATTERNS.md
@.planning/phases/10-massenmail-anbindung-template-variablen/10-06-SUMMARY.md

@genossi_bin/src/lib.rs

<interfaces>
<!-- Existing start_mail_worker wiring (genossi_bin/src/lib.rs:1141-1163) -->
```rust
pub fn start_mail_worker(&self) {
    let config_service = self.worker_config_service.clone();
    let job_dao = self.worker_job_dao.clone();
    let recipient_dao = self.worker_recipient_dao.clone();
    let attachment_dao = self.worker_attachment_dao.clone();
    let static_attachment_dao = self.worker_static_attachment_dao.clone();
    let document_storage = self.document_storage.clone();
    let member_resolver = Arc::new(PoolMemberResolver::new(self.pool.clone()));
    let inbound_mail_dao = Arc::new(InboundMailDaoType::new(self.pool.clone()));
    tokio::spawn(async move {
        genossi_mail::worker::start_mail_worker(
            config_service, job_dao, recipient_dao, attachment_dao,
            static_attachment_dao, document_storage, member_resolver, inbound_mail_dao,
        ).await;
    });
}
```

<!-- Verified struct fields in RestStateImpl (genossi_bin/src/lib.rs):
   - audit_log_dao        (Z. 523, Arc<AuditLogDao>)
   - transaction_dao      (Z. 323, Arc<TransactionDao>)
   MISSING as fields (only let-bindings in new()):
   - member_document_dao  (lokal Z. 589, Arc<MemberDocumentDao>)
   - repayment_phase_dao  (lokal Z. 751, Arc<RepaymentPhaseDao>)
   - repayment_entry_dao  (lokal Z. 752, Arc<RepaymentEntryDao>)
   - mail_template_dao    (lokal Z. 833, Arc<MailTemplateDaoType>)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Erweitere RestStateImpl-Struct + new() um 4 fehlende DAO-Felder</name>
  <files>genossi_bin/src/lib.rs</files>
  <read_first>
    - genossi_bin/src/lib.rs Z. 490-545 (struct-Definition mit audit_log_dao + transaction_dao Beispiel)
    - genossi_bin/src/lib.rs Z. 545-840 (new() — Initialisierungs-Reihenfolge der lokalen DAOs)
    - genossi_bin/src/lib.rs Z. 840-900 (Return-Struct-Literal in new() — neue Felder einfuegen)
    - genossi_bin/src/lib.rs Z. 1141-1163 (start_mail_worker — Task 2 referenziert die neuen Felder)
  </read_first>
  <action>
**Schritt 1 — Struct-Erweiterung:** Fuege 4 neue Felder zum RestStateImpl-Struct hinzu. Suche die Stelle bei `audit_log_dao: Arc<AuditLogDao>,` (Z. 523). Direkt davor oder danach fuege ein:

```rust
    // Phase 10 D-11: DAOs shared with the mail-worker for repayment-context aggregation
    // + auditable MemberDocument-create. Same Arcs as the audited services use, so
    // the worker contributes to the single per-process audit hash chain.
    member_document_dao: Arc<MemberDocumentDao>,
    repayment_phase_dao: Arc<RepaymentPhaseDao>,
    repayment_entry_dao: Arc<RepaymentEntryDao>,
    mail_template_dao: Arc<MailTemplateDaoType>,
```

**Hinweis zu Typ-Aliases:** `MemberDocumentDao`, `RepaymentPhaseDao`, `RepaymentEntryDao`, `MailTemplateDaoType` sind bereits oben in der Datei als type-Aliases definiert (siehe vorhandene `audit_log_dao: Arc<AuditLogDao>` und die bestehenden lokalen `Arc::new(MemberDocumentDao::new(...))`-Aufrufe). Falls die Aliases noch nicht im scope sind, ergaenze sie wie beim existierenden `AuditLogDao`-Alias.

**Schritt 2 — new()-Initialisierung anpassen:** Die existing `let` Bindings in `new()` (Z. 589, 751, 752, 833) BLEIBEN; nur das Struct-Literal am Ende der new()-Funktion bekommt 4 neue Eintraege. Suche das Return-Struct (`RestStateImpl { ... }`-Literal) und ergaenze:

```rust
        RestStateImpl {
            // ... existing fields unchanged ...
            audit_log_dao: audit_log_dao.clone(),
            // Phase 10 D-11: persist the worker-relevant DAOs (already constructed
            // above via Arc::new(XDao::new(pool.clone())) — reuse those Arcs).
            member_document_dao: member_document_dao.clone(),
            repayment_phase_dao: repayment_phase_dao.clone(),
            repayment_entry_dao: repayment_entry_dao.clone(),
            mail_template_dao: mail_template_dao.clone(),
            // ... rest of existing fields unchanged ...
        }
```

**Schritt 3 — Sichern, dass die lokalen Bindings noch `.clone()` ohne Move-Fehler erlauben:** Falls eine der lokalen Variablen (member_document_dao, repayment_phase_dao, repayment_entry_dao, mail_template_dao) AKTUELL ohne `.clone()` ans existierende Service-Init uebergeben wird (z.B. `member_document_dao` moved into MemberDocumentServiceImpl Z. 593), pruefe und ersetze durch `member_document_dao.clone()`. Die DAOs sind Arc-wrapped, daher ist clone billig.

Konkrete Pruefung mit grep:
```bash
# Stellen, an denen die 4 DAOs aktuell verwendet werden:
grep -n "member_document_dao\b\|repayment_phase_dao\b\|repayment_entry_dao\b\|mail_template_dao\b" genossi_bin/src/lib.rs | head -30
```

Erwartet: Jede Move-Site (z.B. `member_document_dao,` als Field-Init in einem ServiceImpl) bekommt `.clone()` angehaengt. Die Compile-Fehler beim Bauen werden alle solchen Stellen aufzeigen — fixe schrittweise bis `cargo build` clean.

**Schritt 4 — Verifikations-Compile (Task 1 isoliert):**
```bash
cargo build --workspace 2>&1 | grep -E "^error" | head -20
```
Erwartet:
- Compile-Fehler beim `start_mail_worker`-Call wegen fehlender 6 Args (das fixt Task 2).
- KEINE Errors zu den 4 neuen Struct-Feldern oder zu den Move-vs-Clone-Stellen.

Falls weitere Compile-Errors auftreten (z.B. type-Alias-Mismatch), behebe sie inline; sie sind erwartete Folgekosten des Phase-10-Wirings.
  </action>
  <verify>
    <automated>cargo build -p genossi_bin 2>&1 | grep -E "^error" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Grep: `grep -cE "^\s*member_document_dao:\s*Arc<MemberDocumentDao>," genossi_bin/src/lib.rs` returns 1 (struct field declaration)
    - Grep: `grep -cE "^\s*repayment_phase_dao:\s*Arc<RepaymentPhaseDao>," genossi_bin/src/lib.rs` returns 1
    - Grep: `grep -cE "^\s*repayment_entry_dao:\s*Arc<RepaymentEntryDao>," genossi_bin/src/lib.rs` returns 1
    - Grep: `grep -cE "^\s*mail_template_dao:\s*Arc<MailTemplateDaoType>," genossi_bin/src/lib.rs` returns 1
    - Grep: `grep -cE "member_document_dao:\s*member_document_dao\.clone\(\)" genossi_bin/src/lib.rs` returns >= 1 (struct-literal init)
    - Grep: `grep -cE "repayment_phase_dao:\s*repayment_phase_dao\.clone\(\)" genossi_bin/src/lib.rs` returns >= 1
    - Grep: `grep -cE "repayment_entry_dao:\s*repayment_entry_dao\.clone\(\)" genossi_bin/src/lib.rs` returns >= 1
    - Grep: `grep -cE "mail_template_dao:\s*mail_template_dao\.clone\(\)" genossi_bin/src/lib.rs` returns >= 1
    - Command: `cargo build -p genossi_bin` may still fail at `start_mail_worker`-Call (expected — Task 2 fixes); ANY OTHER Errors (struct-field missing, move-after-use, type mismatch) MUST be resolved
  </acceptance_criteria>
  <done>RestStateImpl-Struct hat 4 neue Felder; new() persistiert die existing lokalen DAOs in self; Worker-Wiring kann jetzt self.X.clone() rufen.</done>
</task>

<task type="auto">
  <name>Task 2: start_mail_worker DI-Wiring um 6 neue Deps erweitern</name>
  <files>genossi_bin/src/lib.rs</files>
  <read_first>
    - genossi_bin/src/lib.rs (start_mail_worker Z. 1141-1163, NACH Task 1 — neue Felder vorhanden)
    - genossi_mail/src/worker.rs (neue Signatur von Plan 10.06: 14 Args)
    - .planning/phases/10-massenmail-anbindung-template-variablen/10-06-SUMMARY.md
    - .planning/phases/10-massenmail-anbindung-template-variablen/10-PATTERNS.md "start_mail_worker Wiring"
  </read_first>
  <action>
**Voraussetzung:** Task 1 abgeschlossen — RestStateImpl hat alle 6 benoetigten DAOs als persistente Felder.

**Schritt 1:** Pruefe per grep, dass alle 6 benoetigten DAOs jetzt im `RestStateImpl`-struct als Felder existieren:
```bash
grep -cE "^\s*(audit_log_dao|transaction_dao|member_document_dao|repayment_phase_dao|repayment_entry_dao|mail_template_dao):\s*Arc<" genossi_bin/src/lib.rs
```
Erwartet: 6 (1 je Feld in der struct definition). Falls einer fehlt — STOPP und gehe zurueck zu Task 1.

**Schritt 2:** Update `start_mail_worker` (Z. 1141-1163):

```rust
pub fn start_mail_worker(&self) {
    let config_service = self.worker_config_service.clone();
    let job_dao = self.worker_job_dao.clone();
    let recipient_dao = self.worker_recipient_dao.clone();
    let attachment_dao = self.worker_attachment_dao.clone();
    let static_attachment_dao = self.worker_static_attachment_dao.clone();
    let document_storage = self.document_storage.clone();
    let member_resolver = Arc::new(PoolMemberResolver::new(self.pool.clone()));
    let inbound_mail_dao = Arc::new(InboundMailDaoType::new(self.pool.clone()));
    // Phase 10 D-11: 6 new deps for repayment-context aggregation + auditable
    // MemberDocument-create. All DAOs are shared via Arc::clone from RestStateImpl
    // fields (added in Task 1). The audit_log_dao is the SAME Arc that all other
    // audited services use — this guarantees the worker contributes to the single
    // per-process hash chain.
    let member_document_dao = self.member_document_dao.clone();
    let audit_log_dao = self.audit_log_dao.clone();
    let mail_template_dao = self.mail_template_dao.clone();
    let repayment_entry_dao = self.repayment_entry_dao.clone();
    let repayment_phase_dao = self.repayment_phase_dao.clone();
    let transaction_dao = self.transaction_dao.clone();
    tokio::spawn(async move {
        genossi_mail::worker::start_mail_worker(
            config_service,
            job_dao,
            recipient_dao,
            attachment_dao,
            static_attachment_dao,
            document_storage,
            member_resolver,
            inbound_mail_dao,
            // Phase 10 D-11 new args (same positional order as worker.rs signature)
            member_document_dao,
            audit_log_dao,
            mail_template_dao,
            repayment_entry_dao,
            repayment_phase_dao,
            transaction_dao,
        )
        .await;
    });
}
```

**Hinweis zur Typkompatibilitaet:** Der Worker (Plan 10.06) bindet `AL: AuditLogDao<Transaction = MD::Transaction>` und analog fuer RE/RP/TX. Das bedeutet, alle 6 DAOs MUESSEN den gleichen Transaction-Type haben. Im genossi_bin-Setup ist das durchgehend `TransactionImpl` (verifiziere via `grep -n "type Transaction" genossi_bin/src/lib.rs | head` — alle DAOs verwenden dieselbe Connection-Pool-Abstraktion). Falls trotzdem ein Trait-Bound-Mismatch entsteht, ist die Aufloesung:
- Genaue Type-Annotations beim `.clone()`: `let audit_log_dao: Arc<AuditLogDaoImpl> = self.audit_log_dao.clone();` (mit dem konkreten Impl-Type)
- ODER: Im worker.rs Plan 10.06 die generic-Bounds lockern auf `where AL::Transaction: From<MD::Transaction>` etc. — Falls noetig, dann zurueck zu Plan 10.06 und mit relaxer Type-Equality arbeiten. Empfehlung: Erst kompilieren, dann verfeinern.

**Schritt 3:** Verifiziere durch Workspace-Compile:
```bash
cargo build --workspace
```
Erwarte: 0 Errors.

**Schritt 4:** Smoke-Test — Starte das genossi-binary kurz und pruefe ob es ohne Crash startet:
```bash
DATABASE_URL=sqlite::memory: timeout 5 cargo run --bin genossi 2>&1 | head -30
```
Erwarte: server start log, no panic, kein worker-thread-crash. (Timeout ist normal; wichtig: kein "panicked at ..." in den ersten 5 Sekunden.)
  </action>
  <verify>
    <automated>cargo build --workspace 2>&1 | grep -E "^error" | head -10 ; cargo test --workspace --lib 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Grep: `grep -A20 "pub fn start_mail_worker" genossi_bin/src/lib.rs | grep -c "self\.member_document_dao\.clone"` >= 1
    - Grep: `grep -A20 "pub fn start_mail_worker" genossi_bin/src/lib.rs | grep -c "self\.audit_log_dao\.clone"` >= 1
    - Grep: `grep -A20 "pub fn start_mail_worker" genossi_bin/src/lib.rs | grep -c "self\.mail_template_dao\.clone"` >= 1
    - Grep: `grep -A20 "pub fn start_mail_worker" genossi_bin/src/lib.rs | grep -c "self\.repayment_entry_dao\.clone"` >= 1
    - Grep: `grep -A20 "pub fn start_mail_worker" genossi_bin/src/lib.rs | grep -c "self\.repayment_phase_dao\.clone"` >= 1
    - Grep: `grep -A30 "pub fn start_mail_worker" genossi_bin/src/lib.rs | grep -c "self\.transaction_dao\.clone"` >= 1
    - Grep: `grep -c "Arc::new(MemberDocumentDao::new(self\.pool" genossi_bin/src/lib.rs` returns 0 (Option-A — KEIN neuer Arc::new im Worker-Block; alle DAOs aus self)
    - Command: `cargo build --workspace` exit 0
    - Command: `cargo test --workspace --lib` exit 0 (no test regressions)
    - Command: `cargo clippy --workspace --all-targets` no new warnings
  </acceptance_criteria>
  <done>Workspace compiles, alle 6 Worker-Deps verdrahtet via self.X.clone() (Option A), kein Regression in existing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RestStateImpl -> Worker spawn | Shared Arcs only — no auth context crosses; tx is project-internal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07-01 | T (Tampering) | DI-substitute hostile DAO via reflection | accept | Rust monomorphization — no runtime DI substitution; Arcs come from RestStateImpl::new fixed-construction |
| T-10-07-02 | R (Repudiation) | Worker shares audit_log_dao with services | mitigate | Single Arc per process (see PHASE-7-Plan-04 "audit_log_dao geteilt mit allen audited Services" pattern) — guarantees single hash-chain. Option A (DAOs als Struct-Felder mit Arc::clone) PRESERVES this; Option B (Arc::new im Worker-Block) wuerde EINEN ZWEITEN DAO erzeugen und ist DESHALB EXPLIZIT ABGELEHNT |
| T-10-07-03 | I (Information Disclosure) | Wiring inadvertently exposes additional state | accept | Plan only adds existing Arcs to spawn-args; no new state surface |
</threat_model>

<verification>
- `cargo build --workspace` succeeds
- `cargo test --workspace` succeeds (no regressions)
- `cargo clippy --workspace --all-targets` no new warnings
- Smoke: `cargo run --bin genossi` boots without panic
</verification>

<success_criteria>
- RestStateImpl-Struct hat 4 neue persistierte DAO-Felder (member_document_dao, repayment_phase_dao, repayment_entry_dao, mail_template_dao)
- start_mail_worker uebergibt 14 args (8 existing + 6 new)
- Alle 6 neuen Args sind self.X.clone() aus RestStateImpl-Feldern — kein Arc::new(...) (Option A enforced)
- Workspace kompiliert clean
- Kein test regress
</success_criteria>

<output>
After completion, create `.planning/phases/10-massenmail-anbindung-template-variablen/10-07-SUMMARY.md`
</output>
