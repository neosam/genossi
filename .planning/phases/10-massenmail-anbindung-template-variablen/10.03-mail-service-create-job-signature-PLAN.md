---
phase: 10-massenmail-anbindung-template-variablen
plan: 03
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - genossi_mail/src/service.rs
autonomous: true
requirements:
  - MAIL-01
  - MAIL-02

must_haves:
  truths:
    - "MailService::create_job-Signatur akzeptiert template_id und repayment_phase_id als Option<Uuid>"
    - "MailServiceImpl::create_job schreibt beide Felder in den neuen MailJob"
    - "Existing Call-Sites in genossi_mail/src/rest.rs:269 (single-send) und :380 (bulk-send) passieren None,None bzw. parsed UUIDs"
    - "MockMailService (#[automock]) regeneriert automatisch — kein expliziter Mock-Code noetig, aber genossi_service_impl-Tests die MockMailService nutzen muessen die neuen Args mit-uebergeben"
  artifacts:
    - path: "genossi_mail/src/service.rs"
      provides: "MailService trait + Impl mit erweiterter create_job-Signatur"
      contains: "template_id: Option<Uuid>"
  key_links:
    - from: "create_job"
      to: "MailJob.template_id, MailJob.repayment_phase_id"
      via: "MailJob struct-init"
      pattern: "MailJob \\{[^}]*template_id:[^}]*repayment_phase_id"
---

<objective>
Erweitere `MailService::create_job` (Trait + Impl) um die zwei neuen optionalen Parameter `template_id` und `repayment_phase_id`. Aktualisiere `MailJob`-Konstruktion in `MailServiceImpl::create_job`. Behebe alle Call-Sites in genossi_mail (single-send + bulk-send vor Plan 10.04-Body-Aenderung — passieren initial None,None).

Purpose: Damit der Worker (Plan 10.06) `job.template_id` und `job.repayment_phase_id` lesen kann. Trait-Aenderung ist Breaking-Change fuer alle Aufrufer — diese werden im selben Plan gefixt.
Output: Trait + Impl + ein call-site fix in genossi_mail (das Body-Update in rest.rs:380 macht Plan 10.04 fertig).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-massenmail-anbindung-template-variablen/10-CONTEXT.md
@.planning/phases/10-massenmail-anbindung-template-variablen/10-PATTERNS.md

@genossi_mail/src/service.rs
@genossi_mail/src/rest.rs
@.planning/phases/10-massenmail-anbindung-template-variablen/10.01-mail-job-schema-erweiterung-PLAN.md

<interfaces>
<!-- Existing MailService trait signature (genossi_mail/src/service.rs:56-63) -->
```rust
#[automock]
#[async_trait]
pub trait MailService: Send + Sync + 'static {
    async fn create_job(
        &self,
        subject: &str,
        body: &str,
        recipients: Vec<RecipientInput>,
        attachment_inputs: Vec<AttachmentInput>,
        static_document_ids: Vec<Uuid>,
    ) -> Result<MailJob, MailServiceError>;
}
```

<!-- Plan 10.01 lieferte MailJob.template_id + MailJob.repayment_phase_id (Option<Uuid>) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: create_job-Signatur erweitern (Trait + Impl + MailJob-Init)</name>
  <files>genossi_mail/src/service.rs</files>
  <read_first>
    - genossi_mail/src/service.rs (Trait Z. 50-82, Impl Z. 234-321, MailJob-Init Z. 265-280)
    - genossi_mail/src/rest.rs (Call-Sites Z. 269-281 single-send + Z. 380-389 bulk-send)
    - .planning/phases/10-massenmail-anbindung-template-variablen/10-PATTERNS.md "MailService::create_job Signature"
    - .planning/phases/10-massenmail-anbindung-template-variablen/10-CONTEXT.md (D-03 fuer repayment_phase_id, D-12 fuer template_id)
  </read_first>
  <behavior>
    - Test 1 (in genossi_mail/src/service.rs Tests): `create_job` mit template_id=Some(uuid_t), repayment_phase_id=Some(uuid_p) -> returned MailJob hat job.template_id == Some(uuid_t) und job.repayment_phase_id == Some(uuid_p).
    - Test 2: `create_job` mit template_id=None, repayment_phase_id=None -> returned MailJob hat beide Felder None.
    - Test 3 (existing test soll grun bleiben, ggf. mit additional None,None Args adaptiert): Bestehender create_job-Happy-Path-Test laeuft weiter ohne Inhaltsregressionen.
  </behavior>
  <action>
**Schritt 1:** Erweitere die `MailService`-Trait-Signatur (Z. 56-63) um zwei neue Parameter am Ende:

```rust
#[automock]
#[async_trait]
pub trait MailService: Send + Sync + 'static {
    /// Create a mail job with the given recipients. Returns the created job.
    /// If attachment_inputs is non-empty, recipients must contain exactly one entry.
    /// `static_document_ids` are job-level attachments delivered to every recipient.
    /// Phase 10:
    /// - `template_id` (D-12): optional reference to MailTemplate used to render this job's
    ///   subject/body. Worker uses this to populate MemberDocument.template_id.
    /// - `repayment_phase_id` (D-03): optional reference to RepaymentPhase. If set, worker
    ///   merges per-recipient payout context (payout_amount/share_count/fiscal_year) into
    ///   the minijinja render.
    async fn create_job(
        &self,
        subject: &str,
        body: &str,
        recipients: Vec<RecipientInput>,
        attachment_inputs: Vec<AttachmentInput>,
        static_document_ids: Vec<Uuid>,
        template_id: Option<Uuid>,
        repayment_phase_id: Option<Uuid>,
    ) -> Result<MailJob, MailServiceError>;
}
```

**Schritt 2:** Aktualisiere `impl MailService::create_job` (Z. 234) mit denselben zwei zusaetzlichen Parametern. In der MailJob-Konstruktion (Z. 265-280) ersetze die `None, None`-Zwischenwerte (die Plan 10.01 gesetzt hat) mit den Parametern:

```rust
let job = MailJob {
    id: Uuid::new_v4(),
    created: now_primitive,
    deleted: None,
    version: Uuid::new_v4(),
    subject: Arc::from(subject),
    body: Arc::from(body),
    status: Arc::from("running"),
    total_count: recipients.len() as i64,
    sent_count: 0,
    failed_count: 0,
    reply_to_inbound_mail_id: None,
    template_id,           // Phase 10 D-12
    repayment_phase_id,    // Phase 10 D-03
};
```

**Schritt 3:** Fixe Call-Sites in `genossi_mail/src/rest.rs`:

(a) Z. 269-281 (`send_mail`, single-send): zwei zusaetzliche `None`-Argumente am Ende:
```rust
.create_job(
    &body.subject,
    &body.body,
    vec![RecipientInput { /* ... */ }],
    /* attachments */,
    static_document_ids,
    None,  // template_id: Phase 10 single-send is ad-hoc, no template tracking
    None,  // repayment_phase_id: Phase 10 single-send is not bulk-repayment
)
```

(b) Z. 380-389 (`send_bulk_mail`): zwei zusaetzliche `None`-Argumente als Placeholder. **WICHTIG:** Plan 10.04 wird diese Placeholders durch echte parsed UUIDs aus dem Body ersetzen. In diesem Plan: SIMPLY `None, None` damit `genossi_mail` kompiliert. Code-Kommentar einfuegen:
```rust
.create_job(
    &body.subject,
    &body.body,
    recipients,
    attachment_inputs,
    static_document_ids,
    None,  // TODO Plan 10.04: parse body.template_id
    None,  // TODO Plan 10.04: parse body.repayment_phase_id
)
```

(c) **Wichtig:** Falls in `genossi_mail` weitere `create_job`-Aufrufer existieren (z.B. Reply-Flow), suche per grep und passe sie analog mit `None, None` an:
```bash
grep -n "\.create_job(" genossi_mail/src/**/*.rs
```
Erwartet sind 2-3 Stellen. Alle muessen None,None erhalten.

**Schritt 4:** Cross-Crate-Konsumenten: `genossi_service_impl/src/application.rs` (Z. 126 nutzt `RecipientInput`, evtl. ruft auch `create_job`). Suche:
```bash
grep -rn "\.create_job(" genossi_service_impl/
```
Falls Treffer existieren (das Application-Service ruft beim Bestaetigungs-Mail-Versand `create_job` auf), ergaenze dort ebenfalls `None, None` am Ende.

**Schritt 5:** Mockall-`#[automock]` regeneriert die `MockMailService` automatisch. Wenn Tests in `genossi_service_impl/tests/...` `MockMailService::expect_create_job()` verwenden mit explizitem `with(...)`-Matcher, muessen diese Matcher um zwei zusaetzliche `eq(None)`-Klauseln (oder `always()`) erweitert werden. Suche:
```bash
grep -rn "expect_create_job\|MockMailService" --include="*.rs"
```
Pro Treffer: Pass closure-Signatur und Matcher entsprechend an (zwei zusaetzliche Argumente).

**Schritt 6:** Schreibe die 2 neuen Unit-Tests:

```rust
#[tokio::test]
async fn test_create_job_persists_template_id_and_repayment_phase_id() {
    let (service, _pool) = setup_test_service().await;  // existing helper or analog
    let template_id = Uuid::new_v4();
    let phase_id = Uuid::new_v4();
    let recipients = vec![RecipientInput {
        member_id: Some(Uuid::new_v4()),
        to_address: "test@example.com".to_string(),
    }];
    let job = service
        .create_job(
            "subject", "body", recipients, vec![], vec![],
            Some(template_id), Some(phase_id),
        )
        .await
        .unwrap();
    assert_eq!(job.template_id, Some(template_id));
    assert_eq!(job.repayment_phase_id, Some(phase_id));
}

#[tokio::test]
async fn test_create_job_with_none_template_and_phase_keeps_null() {
    let (service, _pool) = setup_test_service().await;
    let recipients = vec![RecipientInput {
        member_id: Some(Uuid::new_v4()),
        to_address: "test@example.com".to_string(),
    }];
    let job = service
        .create_job("subject", "body", recipients, vec![], vec![], None, None)
        .await
        .unwrap();
    assert_eq!(job.template_id, None);
    assert_eq!(job.repayment_phase_id, None);
}
```

Falls kein passender `setup_test_service`-Helper existiert, baue ihn lokal (in-memory SQLite + dependencies analog `genossi_bin/tests/e2e_tests.rs` Setup).
  </action>
  <verify>
    <automated>cargo build --workspace 2>&1 | grep -E "error\[" | head -5; cargo test -p genossi_mail --lib test_create_job_persists_template_id_and_repayment_phase_id 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Grep: `grep -c "template_id: Option<Uuid>," genossi_mail/src/service.rs` returns >= 2 (trait sig + impl sig)
    - Grep: `grep -c "repayment_phase_id: Option<Uuid>," genossi_mail/src/service.rs` returns >= 2
    - Grep: `grep -c "template_id," genossi_mail/src/service.rs` MailJob-init line: extract via `grep -A20 "let job = MailJob" genossi_mail/src/service.rs | grep -c template_id` >= 1
    - Grep: `grep -A2 "\.create_job(" genossi_mail/src/rest.rs | grep -c "TODO Plan 10.04"` returns >= 2 (both single + bulk got the marker)
    - Command: `cargo build --workspace` succeeds (all call-sites updated, no missing-arg errors)
    - Command: `cargo test -p genossi_mail --lib test_create_job_persists_template_id_and_repayment_phase_id` exit 0
    - Command: `cargo test -p genossi_mail --lib test_create_job_with_none_template_and_phase_keeps_null` exit 0
    - Command: `cargo test --workspace` succeeds (existing tests still green; MockMailService mockall-regeneration plus call-site updates intact)
  </acceptance_criteria>
  <done>Trait + Impl erweitert, alle Call-Sites updated, 2 neue Tests gruen, kein Workspace-Compile-Fehler.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REST -> Service | template_id/repayment_phase_id strings parsed to Uuid at REST layer (Plan 10.04); service trusts them as already-validated |
| MockService -> tests | Mockall-generated mock honors the new arity; expect_create_job() requires updated matchers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | T (Tampering) | New service args not validated | accept | template_id/repayment_phase_id are routing-only (FK to MailTemplate/RepaymentPhase); even invalid UUIDs cannot cross trust boundary at this point because REST layer (Plan 10.04) parses them with Uuid::parse_str -> 400 BadRequest |
| T-10-03-02 | I (Information Disclosure) | New args echoed in logs | mitigate | service.rs does not log the new fields; tracing instrumentation skip(...) on rest-handlers already in place |
| T-10-03-03 | D (Denial of Service) | Hostile UUIDs trigger DAO lookups | accept | Lookup happens in worker (Plan 10.06), not in create_job; create_job is a pure INSERT — UUIDs persist as bytes without dereferencing |
</threat_model>

<verification>
- `cargo build --workspace` succeeds
- `cargo test --workspace` succeeds; 2 new tests pass; no regressions
- `cargo clippy --workspace --all-targets` no new warnings
- All `.create_job(` call-sites accept the two new args
</verification>

<success_criteria>
- MailService trait + impl have the two new positional Option<Uuid> parameters
- MailServiceImpl::create_job writes both into MailJob
- All call-sites compile (None placeholder in REST handler; Plan 10.04 replaces bulk-send placeholder)
- 2 new unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-massenmail-anbindung-template-variablen/10-03-SUMMARY.md`
</output>
