---
phase: 07-repaymentphase-backend-foundation
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - genossi_service/src/repayment_phase.rs
  - genossi_service/src/lib.rs
  - genossi_service_impl/src/repayment_phase.rs
  - genossi_service_impl/src/lib.rs
autonomous: true
requirements:
  - PHAS-01
  - PHAS-02   # skeleton: open_repayment_phase setzt Status + opened_at; Auto-Befüllung kommt in Phase 8
  - PHAS-03   # skeleton: close_repayment_phase setzt Status + closed_at; Pending-Entry-Validation kommt in Phase 8
  - PHAS-04
  - PHAS-05
user_setup: []

must_haves:
  truths:
    - "Service-Trait `RepaymentPhaseService` exponiert 7 Methoden: create_repayment_phase, update_repayment_phase, open_repayment_phase, close_repayment_phase, delete_repayment_phase, get_repayment_phase, get_all_repayment_phases"
    - "`create_repayment_phase` ruft `audited_create!` und legt eine Phase mit Status `Preparation` an"
    - "`update_repayment_phase` prüft Edit-Matrix (D-04, D-07): Closed → 409, Open → fiscal_year locked (alle Mutationen atomar abgelehnt), Preparation → frei; Version-Mismatch → 409"
    - "`open_repayment_phase` blockt nicht-`Preparation` mit 409 (D-05/D-06), setzt `status = Open` + `opened_at = now()`, ruft `audited_update!`"
    - "`close_repayment_phase` blockt nicht-`Open` mit 409 (D-05/D-06), setzt `status = Closed` + `closed_at = now()`, ruft `audited_update!` — Pending-Entry-Validation für PHAS-03 ist Phase 8"
    - "`delete_repayment_phase` ruft `audited_delete!` aber NUR wenn Status `Preparation` (D-09), sonst 409"
    - "Field-Validation (D-11 fiscal_year 2000..=2100, D-12 share_value > 0) löst `ServiceError::ValidationError` aus — verwendet in create + update"
    - "Service-Impl-Code enthält 0 direkte `self.repayment_phase_dao.create(`/`.update(`-Aufrufe außerhalb der audited_*-Macro-Expansionen"
    - "D-03: `open_repayment_phase` und `close_repayment_phase` akzeptieren nur die Phase-ID — kein Request-Body, kein `version`-Field-Check; der Status-Guard ist die Concurrency-Defense (Open→Open ist 409)"
  artifacts:
    - path: "genossi_service/src/repayment_phase.rs"
      provides: "RepaymentPhase Domain-Typ + RepaymentPhaseSubmission + RepaymentPhaseUpdate + RepaymentPhaseService-Trait mit #[automock]"
      min_lines: 130
    - path: "genossi_service/src/lib.rs"
      provides: "Modul-Deklaration"
      contains: "pub mod repayment_phase"
    - path: "genossi_service_impl/src/repayment_phase.rs"
      provides: "RepaymentPhaseServiceImpl + 5 Prozesskonstanten + Field-Validator + 7 Service-Methoden + Unit-Tests"
      min_lines: 450
    - path: "genossi_service_impl/src/lib.rs"
      provides: "Modul-Deklaration"
      contains: "pub mod repayment_phase"
  key_links:
    - from: "genossi_service_impl/src/repayment_phase.rs"
      to: "audit_macros (audited_create!/audited_update!/audited_delete!)"
      via: "crate::audited_create!/crate::audited_update!/crate::audited_delete! Macro-Aufrufe"
      pattern: "crate::audited_(create|update|delete)!"
    - from: "genossi_service_impl/src/repayment_phase.rs"
      to: "genossi_dao::repayment_phase::RepaymentPhaseDao (über Deps)"
      via: "self.repayment_phase_dao"
      pattern: "self\\.repayment_phase_dao"
    - from: "update_repayment_phase / open / close / delete"
      to: "ServiceError::Conflict"
      via: "Status-Guard + Edit-Matrix"
      pattern: "ServiceError::Conflict\\(Arc::from"
---

<objective>
Phase 7 Plan 03 (Wave 3) — Service-Trait (`genossi_service`) + Service-Impl (`genossi_service_impl`) mit State-Machine, Field-Validation, Audit-Logging, optimistischem Locking und Lifecycle-Endpoints (open/close/delete).

Purpose: Trägt das Business-Verhalten — Edit-Matrix D-04, atomare Mutation-Ablehnung D-07, Reverse-Block D-06, Soft-Delete-Restriction D-09, Range-Validation D-11/D-12. Liefert die direkten Verifikations-Punkte für ROADMAP-SC#2 (Auditable greift) und SC#5 (share_value-Korrektur in Open erzeugt Audit-Entry; fiscal_year nach Open read-only).

Output:
- 1 Trait-Datei `genossi_service/src/repayment_phase.rs` mit Domain-Typ + DTOs + `#[automock]`-Trait
- 1 Impl-Datei `genossi_service_impl/src/repayment_phase.rs` mit `gen_service_impl!`-Setup, 5 Prozesskonstanten, 7 Methoden, Unit-Tests
- 2 Modul-Deklarationen (in beiden `lib.rs`-Files)
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-repaymentphase-backend-foundation/07-CONTEXT.md
@.planning/phases/07-repaymentphase-backend-foundation/07-PATTERNS.md
@.planning/phases/07-repaymentphase-backend-foundation/07-01-PLAN.md
@.planning/phases/07-repaymentphase-backend-foundation/07-02-PLAN.md
@CLAUDE.md

<!-- 1:1-Vorlagen (Assembly-Service + Assembly-Service-Impl) — Executor liest BEVOR er die neuen Dateien schreibt -->
@genossi_service/src/assembly.rs
@genossi_service_impl/src/assembly.rs
@genossi_service_impl/src/macros.rs
@genossi_service_impl/src/audit_macros.rs
@genossi_service_impl/src/lib.rs
@genossi_service/src/lib.rs
@genossi_service/src/permission.rs
@genossi_dao/src/repayment_phase.rs

<!-- Member.rs als DELETE-Pattern (Assembly hat keinen DELETE) -->
@genossi_service_impl/src/member.rs

<interfaces>
<!-- ServiceError-Variants, die wir verwenden -->
From genossi_service/src/lib.rs:
```rust
pub struct ValidationFailureItem { pub field: Arc<str>, pub message: Arc<str> }

pub enum ServiceError {
    DataAccess(Arc<str>),
    EntityNotFound(uuid::Uuid),
    ValidationError(Vec<ValidationFailureItem>),
    PermissionDenied,
    InternalError(Arc<str>),
    Conflict(Arc<str>),
    Unauthorized,
    SessionExpired,
    AuthenticationFailed,
}

impl From<DaoError> for ServiceError { /* NotFound → EntityNotFound, ConflictError → Conflict, ... */ }
```

<!-- Permission-Trait — verwenden wir 1:1 wie assembly.rs -->
From genossi_service/src/permission.rs:
```rust
#[automock]
#[async_trait]
pub trait PermissionService {
    type Context: ...;
    async fn current_user_id(&self, ctx: Authentication<Self::Context>) -> Result<Option<String>, ServiceError>;
    async fn check_permission(&self, privilege: &str, ctx: Authentication<Self::Context>) -> Result<(), ServiceError>;
}
```

<!-- audit_macros — Aufruf-Form -->
From genossi_service_impl/src/audit_macros.rs:
```rust
crate::audited_create!(self, self.<dao_field>, &entity, PROCESS_CONST, &user_id, tx);
crate::audited_update!(self, self.<dao_field>, id, &entity, PROCESS_CONST, &user_id, tx);
crate::audited_delete!(self, self.<dao_field>, id, PROCESS_CONST, &user_id, tx);
```

<!-- gen_service_impl! — Macro-Aufruf -->
From genossi_service_impl/src/macros.rs:
```rust
// Erzeugt: pub struct RepaymentPhaseServiceImpl<Deps: RepaymentPhaseServiceDeps> { Felder pro Dep }
//          pub trait RepaymentPhaseServiceDeps { type Context; type Transaction; ... }
//          impl<Deps> RepaymentPhaseService for RepaymentPhaseServiceImpl<Deps> { ... }
// Wir liefern den `async fn`-Body in einem nachfolgenden impl-Block.
```

<!-- DAO-Trait aus Plan 01 — was wir aufrufen -->
From genossi_dao/src/repayment_phase.rs (Plan 01):
```rust
async fn create(&self, entity: &RepaymentPhaseEntity, process: &str, tx) -> Result<(), DaoError>;
async fn update(&self, entity: &RepaymentPhaseEntity, process: &str, tx) -> Result<(), DaoError>;
async fn find_by_id(&self, id: Uuid, tx) -> Result<Option<RepaymentPhaseEntity>, DaoError>;  // Default-Impl im Trait
async fn all(&self, tx) -> Result<Arc<[RepaymentPhaseEntity]>, DaoError>;  // Default-Impl
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Service-Trait `genossi_service/src/repayment_phase.rs` + Modul-Deklaration in `lib.rs`</name>
  <files>genossi_service/src/repayment_phase.rs, genossi_service/src/lib.rs</files>
  <read_first>
    - genossi_service/src/assembly.rs (gesamte Datei, 239 Zeilen — 1:1-Anker; aber Phase 7 hat KEINEN AssemblyDetail/Snapshot)
    - genossi_service/src/lib.rs (ServiceError + ValidationFailureItem + Modul-Deklarations-Reihenfolge)
    - genossi_service/src/permission.rs (Authentication-Typ — wir verwenden ihn als Context-Wrapper)
    - genossi_dao/src/repayment_phase.rs (Entity, Status — wir bauen From/Into-Bridges)
    - .planning/phases/07-repaymentphase-backend-foundation/07-PATTERNS.md §4 (Service-Trait-Pattern + Domain-Substitutionen)
  </read_first>
  <behavior>
    - Test 1 (`entity_to_repayment_phase_roundtrip`): `RepaymentPhaseEntity { ... } → RepaymentPhase → RepaymentPhaseEntity` ist verlustfrei (alle 9 Felder identisch).
    - Test 2 (`test_repayment_phase_submission_constructible`): `RepaymentPhaseSubmission { fiscal_year: 2026, share_value: 12000 }` baut sich (kein Status, kein opened_at).
    - Test 3 (`test_repayment_phase_update_requires_version`): `RepaymentPhaseUpdate` hat das Pflichtfeld `version: Uuid` (compile-time-Test — Datei muss compile-clean sein).
    - Test 4 (`test_mock_repayment_phase_service_compiles`): `#[automock]` generiert `MockRepaymentPhaseService` — Test instanziiert ihn und legt `.expect_create_repayment_phase()` an (verifiziert, dass alle 7 Methoden im Mock auftauchen).
  </behavior>
  <action>
**Schritt A: `genossi_service/src/repayment_phase.rs` anlegen.**

Kopiere `genossi_service/src/assembly.rs` als Skeleton (239 Zeilen) und wende folgende Domain-Substitutionen an (1:1 nach PATTERNS.md §4):

1. **Imports** identisch zu assembly.rs (Arc, Debug, Uuid, async_trait, automock, ServiceError, Authentication, genossi_dao-Imports). Statt `genossi_dao::assembly::*` → `genossi_dao::repayment_phase::*`.
2. **Domain-Typ `RepaymentPhase`** — analog `Assembly` (assembly.rs Z. 23-69), 9 Felder, `#[derive(Clone, Debug, PartialEq, Eq)]`:
   ```rust
   pub struct RepaymentPhase {
       pub id: Uuid,
       pub fiscal_year: i32,
       pub share_value: i64,
       pub status: RepaymentPhaseStatus,
       pub opened_at: Option<time::PrimitiveDateTime>,
       pub closed_at: Option<time::PrimitiveDateTime>,
       pub created: time::PrimitiveDateTime,
       pub deleted: Option<time::PrimitiveDateTime>,
       pub version: Uuid,
   }
   ```
   Plus `impl From<&RepaymentPhaseEntity> for RepaymentPhase` und `impl From<&RepaymentPhase> for RepaymentPhaseEntity` (feldweise, beide Richtungen — wegen `Arc<str>` bei Assembly war eine clone-Kette nötig; bei `i32`/`i64` ist es direkt-copy).
3. **DTOs `RepaymentPhaseSubmission` + `RepaymentPhaseUpdate`** — analog assembly.rs Z. 72-89:
   ```rust
   #[derive(Clone, Debug)]
   pub struct RepaymentPhaseSubmission {
       pub fiscal_year: i32,
       pub share_value: i64,
   }

   #[derive(Clone, Debug)]
   pub struct RepaymentPhaseUpdate {
       pub fiscal_year: i32,
       pub share_value: i64,
       pub version: Uuid,
   }
   ```
   **Kein** Submission/Update für Lifecycle (open/close brauchen nur die ID per D-03).
4. **Service-Trait `RepaymentPhaseService`** — analog assembly.rs Z. 101-154:
   ```rust
   #[automock(type Context = (); type Transaction = genossi_dao::MockTransaction;)]
   #[async_trait]
   pub trait RepaymentPhaseService {
       type Context: Clone + Debug + PartialEq + Eq + Send + Sync + 'static;
       type Transaction: genossi_dao::Transaction;

       async fn create_repayment_phase(&self, submission: &RepaymentPhaseSubmission,
           context: Authentication<Self::Context>) -> Result<RepaymentPhase, ServiceError>;
       async fn update_repayment_phase(&self, id: Uuid, update: &RepaymentPhaseUpdate,
           context: Authentication<Self::Context>) -> Result<RepaymentPhase, ServiceError>;
       async fn open_repayment_phase(&self, id: Uuid,
           context: Authentication<Self::Context>) -> Result<RepaymentPhase, ServiceError>;
       async fn close_repayment_phase(&self, id: Uuid,
           context: Authentication<Self::Context>) -> Result<RepaymentPhase, ServiceError>;
       async fn delete_repayment_phase(&self, id: Uuid,
           context: Authentication<Self::Context>) -> Result<(), ServiceError>;
       async fn get_repayment_phase(&self, id: Uuid,
           context: Authentication<Self::Context>) -> Result<RepaymentPhase, ServiceError>;
       async fn get_all_repayment_phases(&self,
           context: Authentication<Self::Context>) -> Result<Arc<[RepaymentPhase]>, ServiceError>;
   }
   ```
   **Phase 7 weicht von Assembly ab:**
   - **Kein** `get_repayment_phase` → `RepaymentPhaseDetail`-Wrapper (Assembly hat `AssemblyDetail` mit Snapshot-Counter; Phase 7 gibt direkt `RepaymentPhase` zurück, siehe PATTERNS.md §4).
   - **Plus** `delete_repayment_phase` (Assembly hat keinen DELETE; D-09 macht Soft-Delete zur Pflicht-Operation).
5. **Test-Modul** mit den 4 Tests aus dem `<behavior>`-Block. Vorlage assembly.rs Z. 156-239 (`make_entity`, `entity_to_assembly_roundtrip`, `test_assembly_submission_constructible`, `test_assembly_update_requires_version`, `test_mock_assembly_service_compiles`). Naming exakt wie spezifiziert.

**Schritt B: Modul-Deklaration in `genossi_service/src/lib.rs`** — `pub mod repayment_phase;` alphabetisch eingefügt (zwischen `permission` und nächstem Modul).
  </action>
  <verify>
    <automated>test -f genossi_service/src/repayment_phase.rs && grep -c 'pub struct RepaymentPhase ' genossi_service/src/repayment_phase.rs | grep -q '^1$' && grep -c 'pub struct RepaymentPhaseSubmission' genossi_service/src/repayment_phase.rs | grep -q '^1$' && grep -c 'pub struct RepaymentPhaseUpdate' genossi_service/src/repayment_phase.rs | grep -q '^1$' && grep -c 'pub trait RepaymentPhaseService' genossi_service/src/repayment_phase.rs | grep -q '^1$' && grep -c 'async fn create_repayment_phase' genossi_service/src/repayment_phase.rs | grep -qE '^[1-9]' && grep -c 'async fn update_repayment_phase' genossi_service/src/repayment_phase.rs | grep -qE '^[1-9]' && grep -c 'async fn open_repayment_phase' genossi_service/src/repayment_phase.rs | grep -qE '^[1-9]' && grep -c 'async fn close_repayment_phase' genossi_service/src/repayment_phase.rs | grep -qE '^[1-9]' && grep -c 'async fn delete_repayment_phase' genossi_service/src/repayment_phase.rs | grep -qE '^[1-9]' && grep -c 'async fn get_repayment_phase' genossi_service/src/repayment_phase.rs | grep -qE '^[1-9]' && grep -c 'async fn get_all_repayment_phases' genossi_service/src/repayment_phase.rs | grep -qE '^[1-9]' && grep -q 'pub mod repayment_phase' genossi_service/src/lib.rs && cargo test -p genossi_service --lib repayment_phase 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `genossi_service/src/repayment_phase.rs` ≥ 130 Zeilen
    - Alle 7 Trait-Methoden im Trait deklariert (greppable: `async fn create_repayment_phase|async fn update_repayment_phase|...`)
    - `RepaymentPhaseSubmission` hat exakt 2 Pflicht-Felder, `RepaymentPhaseUpdate` hat exakt 3 (fiscal_year, share_value, version)
    - `cargo test -p genossi_service --lib repayment_phase` — alle 4 Tests grün
    - `cargo build -p genossi_service` ist grün
    - Modul-Deklaration in `lib.rs` vorhanden
  </acceptance_criteria>
  <done>Trait + DTOs + Mocks bauen; 4 grüne Tests; Modul registriert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Service-Impl `genossi_service_impl/src/repayment_phase.rs` mit Edit-Matrix, Validation, Audit-Macros, 7 Service-Methoden, Unit-Tests + Modul-Deklaration in `lib.rs`</name>
  <files>genossi_service_impl/src/repayment_phase.rs, genossi_service_impl/src/lib.rs</files>
  <read_first>
    - genossi_service_impl/src/assembly.rs (gesamte Datei, 1437 Zeilen — direkter 1:1-Anker; insbesondere Z. 1-385 für die 7 Methoden, Z. 387+ für Test-Patterns)
    - genossi_service_impl/src/member.rs (DELETE-Pattern — Z. 354-383, weil Assembly keinen DELETE hat)
    - genossi_service_impl/src/audit_macros.rs (Macro-Signaturen audited_create!/update!/delete!)
    - genossi_service_impl/src/macros.rs (gen_service_impl! — Deps-Setup)
    - genossi_service_impl/src/lib.rs (Modul-Deklarations-Reihenfolge + Re-Export-Pattern)
    - .planning/phases/07-repaymentphase-backend-foundation/07-CONTEXT.md §decisions (D-04, D-06, D-07, D-09, D-11, D-12 — alle in dieser Methoden-Datei relevant)
    - .planning/phases/07-repaymentphase-backend-foundation/07-PATTERNS.md §5 (Service-Impl-Domain-Substitutionen; insbesondere die Edit-Matrix-Logik für update_repayment_phase und die Vereinfachungen für open_/close_repayment_phase ohne Snapshot/Cascade)
    - CLAUDE.md §Audit Log System (4-Schritt-Checklist: Audit-Macros statt direkte DAO-Calls)
  </read_first>
  <behavior>
    Implementation behavior (durchgesetzt per Status-Guards, Edit-Matrix, Validation, Macro-Aufrufen):
    - **create_repayment_phase**: ADMIN_PRIVILEGE-Check; Field-Validation (D-11/D-12) VOR Entity-Konstruktion → bei Fehler `ServiceError::ValidationError`; baut Entity mit Status=Preparation, opened_at/closed_at=None, frische UUIDs für id+version; `audited_create!` mit Process `"repayment-phase.create"`; commit; return `RepaymentPhase::from(&entity)`.
    - **update_repayment_phase**: ADMIN-Check; load Entity (`find_by_id`, EntityNotFound auf None); Edit-Matrix-Check (D-04):
        - Closed → `Conflict("Cannot update: phase is Closed (D-04)")`
        - Open + `entity.fiscal_year != update.fiscal_year` → `Conflict("Cannot change fiscal_year: phase is Open (D-04/D-07)")` (D-07 atomar)
        - Preparation → alles erlaubt
        - Version-Check: `entity.version != update.version` → `Conflict("Version mismatch")`
        - Field-Validation (D-11/D-12 — gilt auch im Update, weil Werte korrigiert werden können)
        - Mutationen anwenden; `audited_update!` mit Process `"repayment-phase.update"`; commit.
    - **open_repayment_phase**: ADMIN-Check; load; Status-Guard `!= Preparation` → `Conflict(...)`; setze `status = Open` + `opened_at = Some(now)`; `audited_update!` mit Process `"repayment-phase.open"`; commit. **NB:** Phase 8 wird hier die Auto-Befüllung der RepaymentEntries ergänzen (PHAS-02 vervollständigt) — als Code-Kommentar markieren.
    - **close_repayment_phase**: ADMIN-Check; load; Status-Guard `!= Open` → `Conflict(...)`; setze `status = Closed` + `closed_at = Some(now)`; `audited_update!` mit Process `"repayment-phase.close"`; commit. **NB:** Phase 8 wird hier die Pending-Entry-Validation einfügen (PHAS-03 vervollständigt) — als Code-Kommentar markieren.
    - **delete_repayment_phase**: ADMIN-Check; load; Status-Guard `!= Preparation` → `Conflict("Cannot delete: status is '<X>', expected 'Preparation' (D-09)")`; `audited_delete!` mit Process `"repayment-phase.delete"`; commit; return `Ok(())`.
    - **get_repayment_phase / get_all_repayment_phases**: ADMIN-Check; load via DAO; Konversion zu Domain-Typ; return.

    Tests (`#[cfg(test)] mod tests`):
    - Test 1 (`test_create_repayment_phase_validation_rejects_fiscal_year_out_of_range`): Submission mit `fiscal_year=1999` → `Err(ServiceError::ValidationError(items))` mit Item-Feld `"fiscal_year"`. **DAO-Mock-create darf NICHT aufgerufen werden** (mockall .expect_create().times(0)).
    - Test 2 (`test_create_repayment_phase_validation_rejects_share_value_zero`): Submission mit `share_value=0` → `ValidationError` mit Item-Feld `"share_value"`.
    - Test 3 (`test_create_repayment_phase_validation_rejects_share_value_negative`): Submission mit `share_value=-100` → `ValidationError`.
    - Test 4 (`test_create_repayment_phase_success`): Submission mit `fiscal_year=2026, share_value=12000` → DAO-create wird aufgerufen, AuditLogDao-create wird aufgerufen; Status=Preparation im Result.
    - Test 5 (`test_update_repayment_phase_in_closed_returns_conflict`): Mock DAO liefert Entity mit Status=Closed; update → `Err(ServiceError::Conflict(_))` mit Substring `"Closed"`.
    - Test 6 (`test_update_repayment_phase_fiscal_year_change_in_open_returns_conflict`): Mock DAO liefert Entity mit Status=Open, fiscal_year=2026; Update mit fiscal_year=2027 → `Conflict` mit Substring `"fiscal_year"` (D-07).
    - Test 7 (`test_update_repayment_phase_share_value_change_in_open_succeeds`): Mock DAO liefert Open-Entity mit share_value=12000; Update mit share_value=13000 (gleiches fiscal_year) → Erfolg; audited_update! wird aufgerufen.
    - Test 8 (`test_update_repayment_phase_version_mismatch_returns_conflict`): Mock liefert Entity mit version=v1; Update mit version=v2 → `Conflict` mit Substring `"Version mismatch"`.
    - Test 9 (`test_open_repayment_phase_from_open_returns_conflict`): Mock liefert Entity mit Status=Open; open → `Conflict` mit Substring `"Open"` (D-06: Reverse-Block bzw. Doppel-Open-Block).
    - Test 10 (`test_open_repayment_phase_from_closed_returns_conflict`): Mock liefert Entity mit Status=Closed; open → `Conflict` (D-06).
    - Test 11 (`test_close_repayment_phase_from_preparation_returns_conflict`): Mock liefert Entity mit Status=Preparation; close → `Conflict` (D-05).
    - Test 12 (`test_delete_repayment_phase_in_open_returns_conflict`): Mock liefert Entity mit Status=Open; delete → `Conflict` mit Substring `"D-09"` (D-09).
    - Test 13 (`test_delete_repayment_phase_in_preparation_succeeds`): Mock liefert Entity mit Status=Preparation; delete → `Ok(())` und DAO-update mit `deleted = Some(_)` wird aufgerufen.
  </behavior>
  <action>
**Schritt A: `genossi_service_impl/src/repayment_phase.rs` anlegen.**

Kopiere `genossi_service_impl/src/assembly.rs` (1437 Zeilen) als Skeleton und wende die Domain-Substitutionen aus PATTERNS.md §5 an. Konkret:

1. **Imports** (Assembly-Imports übernehmen; entfernen: AssemblyMemberSnapshotDao, MemberDao, HelperTokenDao, PermissionDao — die braucht Phase 7 nicht).
2. **Prozesskonstanten** (analog assembly.rs Z. 46-50):
   ```rust
   const REPAYMENT_PHASE_PROCESS_CREATE: &str = "repayment-phase.create";
   const REPAYMENT_PHASE_PROCESS_UPDATE: &str = "repayment-phase.update";
   const REPAYMENT_PHASE_PROCESS_OPEN: &str = "repayment-phase.open";
   const REPAYMENT_PHASE_PROCESS_CLOSE: &str = "repayment-phase.close";
   const REPAYMENT_PHASE_PROCESS_DELETE: &str = "repayment-phase.delete";
   const ADMIN_PRIVILEGE: &str = "admin";
   ```
3. **`gen_service_impl!`-Wiring** (NUR 5 Deps — kein Snapshot, kein Member, kein Helper, kein PermissionDao):
   ```rust
   gen_service_impl! {
       struct RepaymentPhaseServiceImpl: RepaymentPhaseService = RepaymentPhaseServiceDeps {
           RepaymentPhaseDao: RepaymentPhaseDao<Transaction = Self::Transaction> = repayment_phase_dao,
           AuditLogDao: AuditLogDao<Transaction = Self::Transaction> = audit_log_dao,
           PermissionService: PermissionService<Context = Self::Context> = permission_service,
           UuidService: UuidService = uuid_service,
           TransactionDao: TransactionDao<Transaction = Self::Transaction> = transaction_dao,
       }
   }
   ```
4. **Field-Validator** (inline-Helper, NICHT als Trait — siehe PATTERNS.md §5 Hinweis zu `validation.rs`):
   ```rust
   fn validate_phase_fields(fiscal_year: i32, share_value: i64) -> Result<(), ServiceError> {
       let mut errors: Vec<ValidationFailureItem> = Vec::new();
       if !(2000..=2100).contains(&fiscal_year) {
           errors.push(ValidationFailureItem {
               field: Arc::from("fiscal_year"),
               message: Arc::from(format!("must be in 2000..=2100, got {}", fiscal_year)),
           });
       }
       if share_value <= 0 {
           errors.push(ValidationFailureItem {
               field: Arc::from("share_value"),
               message: Arc::from("must be > 0 (Cent)"),
           });
       }
       if !errors.is_empty() {
           return Err(ServiceError::ValidationError(errors));
       }
       Ok(())
   }
   ```
5. **`impl<Deps> RepaymentPhaseService for RepaymentPhaseServiceImpl<Deps>`** mit den 7 Methoden — präzise:
   - `create_repayment_phase`: tx, user_id, ADMIN-Check, `validate_phase_fields(submission.fiscal_year, submission.share_value)?`, Entity bauen (`status: Preparation, opened_at: None, closed_at: None, created: now, deleted: None, id+version: uuid_service.new_v4()`), `audited_create!`, commit, return `RepaymentPhase::from(&entity)`.
   - `update_repayment_phase`: tx, user_id, ADMIN-Check, `find_by_id` → EntityNotFound, **dann Edit-Matrix-Check vor allem anderen**:
     ```rust
     match entity.status {
         RepaymentPhaseStatus::Closed => {
             return Err(ServiceError::Conflict(Arc::from(
                 "Cannot update: phase is Closed (D-04)",
             )));
         }
         RepaymentPhaseStatus::Open => {
             if entity.fiscal_year != update.fiscal_year {
                 return Err(ServiceError::Conflict(Arc::from(
                     "Cannot change fiscal_year: phase is Open (D-04/D-07)",
                 )));
             }
         }
         RepaymentPhaseStatus::Preparation => {}
     }
     ```
     dann `if entity.version != update.version → Conflict("Version mismatch")`, dann `validate_phase_fields(...)?`, dann `entity.fiscal_year = update.fiscal_year; entity.share_value = update.share_value;`, dann `audited_update!`, commit, return.
   - `open_repayment_phase`: tx, user_id, ADMIN; load; `if entity.status != Preparation → Conflict("Cannot open repayment phase: status is '<X>', expected 'Preparation'")`; setze status=Open, opened_at=Some(now); `audited_update!` mit `REPAYMENT_PHASE_PROCESS_OPEN`; commit. **Code-Kommentar:** `// PHAS-02 (Phase 8): hier wird die Auto-Befüllung der RepaymentEntries ergänzt.`
   - `close_repayment_phase`: analog, Guard `!= Open → Conflict(...)`; setze status=Closed, closed_at=Some(now); `audited_update!` mit `REPAYMENT_PHASE_PROCESS_CLOSE`; commit. **Code-Kommentar:** `// PHAS-03 (Phase 8): hier wird die Validation "alle RepaymentEntries paid_out oder soft-deleted" ergänzt.`
   - `delete_repayment_phase`: ADMIN; load; `if entity.status != Preparation → Conflict("Cannot delete: status is '<X>', expected 'Preparation' (D-09)")`; `audited_delete!` mit `REPAYMENT_PHASE_PROCESS_DELETE`; commit; return `Ok(())`.
   - `get_repayment_phase`: ADMIN; `find_by_id` → EntityNotFound; commit; return `RepaymentPhase::from(&entity)`.
   - `get_all_repayment_phases`: ADMIN; `dao.all(tx)` (Default-Impl filtert `deleted IS NULL` per D-10); collect zu `Arc<[RepaymentPhase]>`; commit; return.

   **AUDIT-CALL-DISZIPLIN (Threat T-07-03-01):** `self.repayment_phase_dao.create(` und `self.repayment_phase_dao.update(` dürfen NUR innerhalb der `audited_create!`/`audited_update!`/`audited_delete!`-Macro-Expansionen vorkommen. Im handgeschriebenen Code-Pfad sind diese Aufrufe verboten. Verifikation in acceptance_criteria via Grep (siehe unten).

6. **Test-Modul** mit den 13 Tests aus dem `<behavior>`-Block. Test-Vorlage: assembly.rs Z. 387+ — beachte: weniger Mocks nötig (`TestTxDao`, `TestRepaymentPhaseDao`, `TestAuditLogDao`, `TestPermissionService`, `TestUuidService`). Verwende `mockall::mock!`-Hand-rolled-Mocks wo nötig (Pattern aus assembly.rs).

**Schritt B: Modul-Deklaration in `genossi_service_impl/src/lib.rs`** — `pub mod repayment_phase;` alphabetisch + selektives Re-Export analog `pub use repayment_phase::{RepaymentPhaseServiceImpl, MockRepaymentPhaseServiceImpl};` (Pattern aus assembly-Re-Export, falls vorhanden; ansonsten nur das Modul deklarieren).
  </action>
  <verify>
    <automated>test -f genossi_service_impl/src/repayment_phase.rs && grep -c '"repayment-phase.create"' genossi_service_impl/src/repayment_phase.rs | grep -q '^1$' && grep -c '"repayment-phase.update"' genossi_service_impl/src/repayment_phase.rs | grep -q '^1$' && grep -c '"repayment-phase.open"' genossi_service_impl/src/repayment_phase.rs | grep -q '^1$' && grep -c '"repayment-phase.close"' genossi_service_impl/src/repayment_phase.rs | grep -q '^1$' && grep -c '"repayment-phase.delete"' genossi_service_impl/src/repayment_phase.rs | grep -q '^1$' && grep -c 'fn validate_phase_fields' genossi_service_impl/src/repayment_phase.rs | grep -q '^1$' && grep -c 'crate::audited_create!' genossi_service_impl/src/repayment_phase.rs | grep -qE '^[1-9]' && grep -c 'crate::audited_update!' genossi_service_impl/src/repayment_phase.rs | grep -qE '^[3-9]' && grep -c 'crate::audited_delete!' genossi_service_impl/src/repayment_phase.rs | grep -qE '^[1-9]' && grep -c 'D-04' genossi_service_impl/src/repayment_phase.rs | grep -qE '^[1-9]' && grep -c 'D-09' genossi_service_impl/src/repayment_phase.rs | grep -qE '^[1-9]' && grep -q 'pub mod repayment_phase' genossi_service_impl/src/lib.rs && (grep -v '^[[:space:]]*//' genossi_service_impl/src/repayment_phase.rs | grep -v '^[[:space:]]*\*' | grep -E 'self\.repayment_phase_dao\.(create|update)\(' | wc -l | grep -q '^0$') && cargo test -p genossi_service_impl --lib repayment_phase 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `genossi_service_impl/src/repayment_phase.rs` ≥ 450 Zeilen
    - Genau 5 Prozesskonstanten exakt benannt: `"repayment-phase.create"`, `".update"`, `".open"`, `".close"`, `".delete"` (1 Grep-Treffer pro String)
    - `validate_phase_fields`-Funktion existiert (1 Treffer)
    - `crate::audited_create!` ≥ 1 Aufruf (in create_repayment_phase)
    - `crate::audited_update!` ≥ 3 Aufrufe (in update_, open_, close_)
    - `crate::audited_delete!` ≥ 1 Aufruf (in delete_)
    - Edit-Matrix-Markierungen `D-04` und `D-09` als Code-Kommentare vorhanden (Doku-Anker für späteres Verständnis)
    - **AUDIT-DISZIPLIN-GREP-GATE:** `grep -v '^[[:space:]]*//' genossi_service_impl/src/repayment_phase.rs | grep -v '^[[:space:]]*\*' | grep -E 'self\.repayment_phase_dao\.(create|update)\(' | wc -l` == 0 — keine direkten Macro-bypassenden DAO-create/update-Aufrufe (Kommentare und Doc-Comments per `^//` und `^*` herausgefiltert, um Self-Invalidation des Grep-Gates zu verhindern)
    - `cargo test -p genossi_service_impl --lib repayment_phase` — alle 13 Tests grün
    - `cargo build -p genossi_service_impl` ist grün
    - Modul-Deklaration in `lib.rs` vorhanden
  </acceptance_criteria>
  <done>Service-Impl steht; 13 grüne Unit-Tests verifizieren Edit-Matrix, Lifecycle-Guards, Validierung; Audit-Disziplin per Grep-Gate gesichert; Audit-Macros korrekt verdrahtet; Plan 04 (REST-Handler) kann andocken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REST-Layer ↔ Service-Layer | Authentifizierter Vorstand-User; Service prüft `check_permission("admin", ctx)` |
| Service ↔ DAO | Audit-Macros sind die ENZIGE legitime Schreib-Route; direkte DAO-create/update sind verboten |
| Concurrent Vorstand-Sessions | Optimistic Locking via `version` im PUT — open/close haben Status-Guard als Defense |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Repudiation | Service-Code umgeht Audit-Macro und ruft `dao.create()`/`dao.update()` direkt → keine Audit-Spur | mitigate | Grep-Gate im Task-2 acceptance_criteria: `grep -v '^//' ... | grep 'self.repayment_phase_dao.(create|update)('` muss 0 Treffer haben (Kommentare per `grep -v` herausgefiltert per Nyquist-Regel, kein self-invalidating Gate). 13 Unit-Tests verifizieren zusätzlich, dass AuditLogDao-create aufgerufen wird wenn Service-Operation erfolgreich ist. |
| T-07-03-02 | Elevation of Privilege | Non-Admin-User kann RepaymentPhase erzeugen/ändern/löschen | mitigate | Jede Service-Methode beginnt mit `self.permission_service.check_permission(ADMIN_PRIVILEGE, context).await?`. ADMIN_PRIVILEGE = "admin" (1:1 wie assembly.rs Z. 50). E2E-Test in Plan 05 könnte einen 401-Negative-Path ergänzen falls relevant (aber Phase 7 ist Backend-only, Phase 12 prüft Frontend-Auth). |
| T-07-03-03 | Tampering | Concurrent Vorstand-PUTs überschreiben fremde Korrektur | mitigate | `if entity.version != update.version → Conflict("Version mismatch")`. Test 8 verifiziert. DAO-Layer hat zusätzlich `WHERE version = ?` als zweite Verteidigung (Plan 02). |
| T-07-03-04 | Tampering | PUT mit `fiscal_year=X, share_value=Y` auf Open-Phase setzt heimlich nur share_value durch | mitigate | D-07 atomare Ablehnung: WENN `entity.fiscal_year != update.fiscal_year` AND Status=Open → SOFORT Conflict, KEINE share_value-Mutation. Test 6 verifiziert (Negative-Path mit fiscal_year-Change). |
| T-07-03-05 | Denial of Service | Service-Methoden öffnen eine Transaktion und vergessen commit bei Early-Return → Pool-Exhaustion | mitigate | Standard-Pattern aus assembly.rs: `let tx = self.transaction_dao.use_transaction(None).await?;` ist scope-lokal; bei Early-Return per `?` oder `return Err(...)` läuft der `Drop` der Transaction → Rollback. Kein explizites cleanup nötig (Pattern-Konsistenz mit Assembly-Impl). |
| T-07-03-06 | Information Disclosure | `get_all_repayment_phases` listet soft-gelöschte Phasen | mitigate | DAO-Default-Impl `all()` filtert `deleted IS NULL` per D-10. REST-Endpoint sieht nur aktive Phasen. (Plan 04 prüft im REST-Test gegebenenfalls explizit.) |
| T-07-03-07 | Tampering | `delete_repayment_phase` setzt `deleted` auf Open/Closed-Phase → Audit-Inkonsistenz mit Phase-8/9-Daten | mitigate | D-09 Status-Guard `!= Preparation → Conflict(...)`. Test 12 verifiziert Negative-Path. |
</threat_model>

<verification>
- `cargo build -p genossi_service && cargo build -p genossi_service_impl` ist grün
- `cargo test -p genossi_service --lib repayment_phase` — 4 Tests grün
- `cargo test -p genossi_service_impl --lib repayment_phase` — 13 Tests grün
- Audit-Disziplin-Grep-Gate erfolgreich (0 direkte create/update-Calls außerhalb Macro)
- Alle 5 Prozesskonstanten korrekt benannt (entry points für E2E-Audit-Verifikation in Plan 05)
</verification>

<success_criteria>
- **ROADMAP SC#2 (Auditable + audited_create!/audited_update! greifen):** vollständig erfüllt durch Task 2
- **ROADMAP SC#5 (share_value-Korrektur in Open erzeugt Audit-Eintrag; fiscal_year nach Open read-only):** Service-Verhalten vollständig durch Edit-Matrix umgesetzt; E2E-Verifikation in Plan 05
- **PHAS-01:** create_repayment_phase erfüllt
- **PHAS-02:** open_repayment_phase setzt Status — Auto-Befüllung explizit als Phase-8-TODO im Code-Kommentar markiert
- **PHAS-03:** close_repayment_phase setzt Status — Pending-Entry-Validation explizit als Phase-8-TODO im Code-Kommentar markiert
- **PHAS-04:** update_repayment_phase erlaubt share_value-Korrektur in Open via audited_update!
- **PHAS-05:** alle Schreiboperationen laufen ausschließlich über audited_*-Macros (Grep-Gate verifiziert)
</success_criteria>

<output>
Nach Abschluss erstellen unter `.planning/phases/07-repaymentphase-backend-foundation/07-03-SUMMARY.md`:
- Was wurde gebaut (Trait + Impl + Edit-Matrix + 5 Prozesskonstanten + 13 Tests)
- Audit-Disziplin-Grep-Gate-Ergebnis (Anzahl Treffer = 0)
- Verlinkung: Plan 04 (REST) erwartet die genauen Methoden-Signaturen aus Task 1
- Code-Kommentare für Phase 8 (PHAS-02 Auto-Befüllung in open_, PHAS-03 Pending-Validation in close_)
</output>
