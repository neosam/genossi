---
phase: 07-repaymentphase-backend-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/sqlite/20260529190437_create_repayment_phase_table.sql
  - genossi_dao/src/repayment_phase.rs
  - genossi_dao/src/lib.rs
autonomous: true
requirements:
  - PHAS-01
  - PHAS-05
user_setup: []

must_haves:
  truths:
    - "DB-Migration legt `repayment_phase`-Tabelle mit allen geforderten Spalten an (BLOB-UUID, fiscal_year INTEGER NOT NULL, share_value INTEGER NOT NULL in Cent, status TEXT NOT NULL, opened_at TEXT NULL, closed_at TEXT NULL, created TEXT NOT NULL, deleted TEXT NULL, version BLOB NOT NULL)"
    - "Status-Enum `RepaymentPhaseStatus` existiert mit drei englischen Varianten (Preparation, Open, Closed) — analog AssemblyStatus"
    - "Entity `RepaymentPhaseEntity` ist als auditpflichtig markiert (impl Auditable mit entity_type=\"repayment_phase\", entity_id=self.id, audit_fields() liefert genau 5 Felder OHNE id/version/created/deleted)"
    - "DAO-Trait `RepaymentPhaseDao` existiert mit den 3 Pflicht-Methoden (dump_all/create/update) + Default-Impls (all/find_by_id) mit deleted-IS-NULL-Filterung"
    - "Modul-Deklaration `pub mod repayment_phase;` in `genossi_dao/src/lib.rs` ist alphabetisch korrekt einsortiert"
  artifacts:
    - path: "migrations/sqlite/20260529190437_create_repayment_phase_table.sql"
      provides: "DDL für repayment_phase-Tabelle + 2 Indizes (status, deleted)"
      contains: "CREATE TABLE IF NOT EXISTS repayment_phase"
    - path: "genossi_dao/src/repayment_phase.rs"
      provides: "RepaymentPhaseStatus enum + RepaymentPhaseEntity struct + Auditable impl + RepaymentPhaseDao trait + Unit-Tests"
      min_lines: 220
    - path: "genossi_dao/src/lib.rs"
      provides: "Modul-Deklaration für repayment_phase"
      contains: "pub mod repayment_phase"
  key_links:
    - from: "genossi_dao/src/repayment_phase.rs"
      to: "genossi_dao/src/auditable.rs"
      via: "impl Auditable for RepaymentPhaseEntity"
      pattern: "impl\\s+(?:crate::auditable::)?Auditable\\s+for\\s+RepaymentPhaseEntity"
    - from: "genossi_dao/src/repayment_phase.rs"
      to: "genossi_dao::Transaction trait"
      via: "type Transaction = ... bound auf #[async_trait] pub trait RepaymentPhaseDao"
      pattern: "type\\s+Transaction\\s*:\\s*crate::Transaction"
---

<objective>
Phase 7 Plan 01 (Wave 1) — Migration + DAO-Trait + Entity + Auditable-Impl für das neue auditpflichtige Aggregat `RepaymentPhase`.

Purpose: Fundament der gesamten Phase 7. Etabliert das DB-Schema und die DAO-Schnittstelle, an die alle nachfolgenden Plans (02 SQLite-Impl, 03 Service, 04 REST, 05 E2E) andocken. Bricht aus dem Assembly-Pattern (Phase 1 Plan 03) — direkt 1:1 als Vorlage übernehmbar.

Output:
- 1 neue Migration `migrations/sqlite/20260529190437_create_repayment_phase_table.sql` mit `CREATE TABLE` + 2 Indizes
- 1 neue Modul-Datei `genossi_dao/src/repayment_phase.rs` mit `RepaymentPhaseStatus`, `RepaymentPhaseEntity`, `Auditable`-Impl, `RepaymentPhaseDao`-Trait + Unit-Tests
- 1 Modul-Deklaration in `genossi_dao/src/lib.rs`
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-repaymentphase-backend-foundation/07-CONTEXT.md
@.planning/phases/07-repaymentphase-backend-foundation/07-PATTERNS.md
@CLAUDE.md

<!-- 1:1-Vorlage (Assembly-Aggregat) — Executor liest diese Dateien BEVOR er die neuen Dateien schreibt -->
@migrations/sqlite/20260502000000_create_assembly_table.sql
@genossi_dao/src/assembly.rs
@genossi_dao/src/auditable.rs
@genossi_dao/src/lib.rs

<interfaces>
<!-- Kontrakte aus Codebase, die Executor unverändert verwendet — kein Codebase-Scavenger-Hunt nötig. -->

From genossi_dao/src/auditable.rs (Trait-Definition, die wir implementieren):
```rust
pub trait Auditable {
    fn entity_type() -> &'static str;
    fn entity_id(&self) -> Uuid;
    fn audit_fields(&self) -> Vec<(&'static str, Option<String>)>;
    // (Plus Default-Impls für audit_event/diff — vom Trait selbst geliefert.)
}
```

From genossi_dao/src/lib.rs (Transaction-Trait + DaoError):
```rust
pub trait Transaction: ... { /* Lock-Pattern mit tx.lock().await */ }

pub enum DaoError {
    DatabaseError(Arc<str>),
    ParseError(Arc<str>),
    NotFound,
    ConflictError(Arc<str>),
}
```

From genossi_dao/src/assembly.rs (direkt nachzubauender Pattern-Anker — komplette Datei kopieren, dann Domain-Substitutionen anwenden):
```rust
// Z. 9-42: Status-Enum mit as_str/from_str/Default
// Z. 44-56: Entity-Struct mit id/...status/opened_at/closed_at/created/deleted/version
// Z. 58-94: Auditable-Impl mit format_dt-Closure und Sentinel "<invalid datetime>"
// Z. 96-138: DAO-Trait mit #[automock(type Transaction = crate::MockTransaction;)] + dump_all/create/update/all/find_by_id
// Z. 140-251: Test-Modul mit Status-Roundtrip + Auditable-Diff-Tests
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration `20260529190437_create_repayment_phase_table.sql` anlegen</name>
  <files>migrations/sqlite/20260529190437_create_repayment_phase_table.sql</files>
  <read_first>
    - migrations/sqlite/20260502000000_create_assembly_table.sql (1:1-Vorlage)
    - .planning/phases/07-repaymentphase-backend-foundation/07-CONTEXT.md §entity-Skeleton (Felder-Liste) + D-08 (KEIN UNIQUE-Constraint) + D-13 (opened_at/closed_at als Spalten)
    - .planning/phases/07-repaymentphase-backend-foundation/07-PATTERNS.md §1 (Migration-Domain-Substitutionen)
    - CLAUDE.md §Entity Structure (UUID BLOB, ISO8601, version)
  </read_first>
  <behavior>
    - Test 1: `cargo sqlx prepare --check` ist nicht direkt nutzbar (offline mode), aber `cargo build` läuft erfolgreich und `cargo test -p genossi_dao_impl_sqlite` kann die Migration im in-memory SQLite ausführen (wird in Plan 02 verifiziert).
    - Test 2: `grep -c 'CREATE TABLE IF NOT EXISTS repayment_phase' migrations/sqlite/20260529190437_create_repayment_phase_table.sql` ergibt `1`.
    - Test 3: Genau die 9 Spalten in genau dieser Reihenfolge (id BLOB PK, fiscal_year INTEGER NOT NULL, share_value INTEGER NOT NULL, status TEXT NOT NULL DEFAULT 'Preparation', opened_at TEXT, closed_at TEXT, created TEXT NOT NULL, deleted TEXT, version BLOB NOT NULL) — analog Assembly-Migration aber `name`/`date`/`location` ersetzt durch `fiscal_year`/`share_value`.
    - Test 4: KEIN UNIQUE-Constraint auf `fiscal_year` (D-08).
    - Test 5: Genau 2 Indizes — `idx_repayment_phase_status ON repayment_phase(status)` und `idx_repayment_phase_deleted ON repayment_phase(deleted)`. KEIN `idx_repayment_phase_fiscal_year` (kein Such-Pattern in Phase 7; falls Phase 8 ihn braucht, dort).
  </behavior>
  <action>
Kopiere `migrations/sqlite/20260502000000_create_assembly_table.sql` als Vorlage und wende folgende Domain-Substitutionen exakt an:

1. Tabellenname: `assembly` → `repayment_phase` (überall in der Datei: CREATE TABLE-Statement + alle CREATE INDEX-Statements)
2. Spalte `name TEXT NOT NULL` → entfällt komplett (nicht ersetzen, ersatzlos streichen)
3. Spalte `date TEXT NOT NULL` → `fiscal_year INTEGER NOT NULL` (per D-11 validiert im Service-Layer, Range 2000..=2100; KEINE SQL-CHECK-Constraint hier)
4. Spalte `location TEXT` → `share_value INTEGER NOT NULL` (per D-12 validiert im Service-Layer, > 0; KEINE SQL-CHECK-Constraint hier; SQLite-INTEGER ist 8-Byte → i64 in Rust → CONTEXT.md `<specifics>` "share_value als i64 (Cent)")
5. Spalte `status TEXT NOT NULL DEFAULT 'Preparation'` bleibt unverändert (D-01: englische Status-Strings; Default ist `Preparation` analog Assembly)
6. `opened_at TEXT`, `closed_at TEXT`, `created TEXT NOT NULL`, `deleted TEXT`, `version BLOB NOT NULL` bleiben unverändert (D-13)
7. Indizes: streiche `idx_assembly_date`. Behalte (umbenannt): `idx_repayment_phase_status ON repayment_phase(status)` und `idx_repayment_phase_deleted ON repayment_phase(deleted)`. Erwarteter Index-Count: 2.
8. WICHTIG: KEIN UNIQUE-Constraint auf `fiscal_year` per D-08 (mehrere Phasen pro Geschäftsjahr sind explizit erlaubt). Kein Partial-Unique-Index. KEINE FOREIGN KEYs (Phase 8 verwendet `RepaymentEntry.phase_id` als FK, nicht andersrum).

Speichern unter `migrations/sqlite/20260529190437_create_repayment_phase_table.sql`. Datei ist 13-15 SQL-Zeilen lang (vs. 16 bei Assembly).

Per D-08, D-13: keine zusätzlichen Constraints/Indizes über die genannten hinaus.
  </action>
  <verify>
    <automated>test -f migrations/sqlite/20260529190437_create_repayment_phase_table.sql && grep -c 'CREATE TABLE IF NOT EXISTS repayment_phase' migrations/sqlite/20260529190437_create_repayment_phase_table.sql | grep -q '^1$' && grep -c 'fiscal_year INTEGER NOT NULL' migrations/sqlite/20260529190437_create_repayment_phase_table.sql | grep -q '^1$' && grep -c 'share_value INTEGER NOT NULL' migrations/sqlite/20260529190437_create_repayment_phase_table.sql | grep -q '^1$' && grep -c 'CREATE INDEX IF NOT EXISTS idx_repayment_phase_' migrations/sqlite/20260529190437_create_repayment_phase_table.sql | grep -q '^2$' && ! grep -qi 'UNIQUE' migrations/sqlite/20260529190437_create_repayment_phase_table.sql && ! grep -q 'name TEXT' migrations/sqlite/20260529190437_create_repayment_phase_table.sql && cargo build -p genossi_dao_impl_sqlite 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `migrations/sqlite/20260529190437_create_repayment_phase_table.sql`
    - `grep -c 'CREATE TABLE IF NOT EXISTS repayment_phase' <file>` == 1
    - `grep -c 'fiscal_year INTEGER NOT NULL' <file>` == 1
    - `grep -c 'share_value INTEGER NOT NULL' <file>` == 1
    - `grep -c 'CREATE INDEX IF NOT EXISTS idx_repayment_phase_' <file>` == 2 (status + deleted, kein fiscal_year-Index)
    - `grep -ci 'UNIQUE' <file>` == 0 (D-08)
    - `grep -c 'name TEXT' <file>` == 0 (Assembly-Spalte raus)
    - `cargo build -p genossi_dao_impl_sqlite` ist grün (Migration darf nicht kompilieren-blockieren, weil sqlx-CLI sie zur Build-Zeit liest, falls offline-cache existiert)
  </acceptance_criteria>
  <done>Migration-Datei ist committed-ready; passt zum Schema in CONTEXT.md `<entity-Skeleton>`; keine UNIQUE-Constraints; 2 Indizes; cargo build grün.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `genossi_dao/src/repayment_phase.rs` mit Status-Enum, Entity, Auditable-Impl, DAO-Trait + Unit-Tests anlegen + Modul-Deklaration in `lib.rs`</name>
  <files>genossi_dao/src/repayment_phase.rs, genossi_dao/src/lib.rs</files>
  <read_first>
    - genossi_dao/src/assembly.rs (gesamte Datei, 251 Zeilen — DIES ist der direkte 1:1-Anker)
    - genossi_dao/src/auditable.rs (Trait-Signaturen für entity_type/entity_id/audit_fields)
    - genossi_dao/src/lib.rs (Modul-Deklarations-Reihenfolge — alphabetisch)
    - .planning/phases/07-repaymentphase-backend-foundation/07-PATTERNS.md §2 (DAO-Trait-Pattern + Domain-Substitutionen)
    - .planning/phases/07-repaymentphase-backend-foundation/07-CONTEXT.md §entity-Skeleton (RepaymentPhaseEntity Felder-Liste) + D-01 (englische Status-Strings) + D-13 (opened_at/closed_at als Felder)
    - CLAUDE.md §Audit Log System (4-Schritt-Checklist: Auditable-Trait ist Schritt 1)
  </read_first>
  <behavior>
    - Test 1 (`test_repayment_phase_status_roundtrip`): Für jede der 3 Varianten gilt `RepaymentPhaseStatus::from_str(status.as_str()).unwrap() == status`.
    - Test 2 (`test_repayment_phase_status_strings_are_english`): `RepaymentPhaseStatus::Preparation.as_str() == "Preparation"`, `Open.as_str() == "Open"`, `Closed.as_str() == "Closed"` (D-01).
    - Test 3 (`test_repayment_phase_status_invalid_string`): `RepaymentPhaseStatus::from_str("Vorbereitung")` liefert `Err(DaoError::ParseError(_))` (deutsche Strings werden NICHT akzeptiert).
    - Test 4 (`test_repayment_phase_status_default_is_preparation`): `RepaymentPhaseStatus::default() == RepaymentPhaseStatus::Preparation`.
    - Test 5 (`test_auditable_entity_type_is_repayment_phase`): `RepaymentPhaseEntity::entity_type() == "repayment_phase"` (D-13: Audit-Lesbarkeit).
    - Test 6 (`test_auditable_fields_count_and_excludes_metadata`): `audit_fields()` liefert genau 5 Einträge (`fiscal_year`, `share_value`, `status`, `opened_at`, `closed_at`); KEIN Eintrag heißt `id`, `version`, `created` oder `deleted` (Auditable-Konvention aus assembly.rs Z. 80-93).
    - Test 7 (`test_auditable_diff_detects_status_change`): zwei Entities, die sich nur in `status` (Preparation → Open) unterscheiden, liefern via `Auditable::diff(...)` genau 1 Diff-Eintrag mit Feld `"status"`.
  </behavior>
  <action>
**Schritt A: `genossi_dao/src/repayment_phase.rs` anlegen.**

Kopiere `genossi_dao/src/assembly.rs` (251 Zeilen) als Skeleton und wende folgende Domain-Substitutionen an (1:1 nach PATTERNS.md §2):

1. **Imports** — identisch zu assembly.rs (Uuid, Arc, time::PrimitiveDateTime, Iso8601, async_trait, automock, DaoError).
2. **Status-Enum** — `pub enum AssemblyStatus { Preparation, Open, Closed }` → `pub enum RepaymentPhaseStatus { Preparation, Open, Closed }`:
   - `impl RepaymentPhaseStatus { pub fn as_str(&self) -> &'static str { ... } pub fn from_str(s: &str) -> Result<Self, DaoError> { ... } }` — Pattern-Match auf "Preparation"/"Open"/"Closed" → entsprechende Variante; unbekannter String → `DaoError::ParseError(Arc::from(format!("Unknown repayment phase status: {}", s)))`.
   - `impl Default for RepaymentPhaseStatus { fn default() -> Self { RepaymentPhaseStatus::Preparation } }`.
3. **Entity-Struct** — `pub struct RepaymentPhaseEntity` mit exakt diesen Feldern in dieser Reihenfolge (per CONTEXT.md `<entity-Skeleton>`):
   ```rust
   pub id: Uuid,
   pub fiscal_year: i32,                  // CONTEXT.md <specifics> "fiscal_year als i32"
   pub share_value: i64,                  // CONTEXT.md <specifics> "share_value als i64 (Cent)"
   pub status: RepaymentPhaseStatus,
   pub opened_at: Option<time::PrimitiveDateTime>,
   pub closed_at: Option<time::PrimitiveDateTime>,
   pub created: time::PrimitiveDateTime,
   pub deleted: Option<time::PrimitiveDateTime>,
   pub version: Uuid,
   ```
   Derives: `#[derive(Clone, Debug, PartialEq, Eq)]`.
4. **Auditable-Impl** — `impl crate::auditable::Auditable for RepaymentPhaseEntity`:
   - `entity_type() -> &'static str { "repayment_phase" }`
   - `entity_id(&self) -> Uuid { self.id }`
   - `audit_fields(&self) -> Vec<(&'static str, Option<String>)>` mit `format_dt`-Closure identisch zu assembly.rs Z. 70-82 (inkl. `unwrap_or_else` mit `tracing::error!`-Logging und Sentinel `"<invalid datetime>"` — NICHT vereinfachen, das ist die WR-08-Lektion). Tracing-Label `entity = "repayment_phase"`. Vec hat exakt 5 Einträge in dieser Reihenfolge:
     ```rust
     ("fiscal_year", Some(self.fiscal_year.to_string())),
     ("share_value", Some(self.share_value.to_string())),
     ("status", Some(self.status.as_str().to_string())),
     ("opened_at", self.opened_at.as_ref().map(format_dt)),
     ("closed_at", self.closed_at.as_ref().map(format_dt)),
     ```
   - **WICHTIG:** KEIN `id`/`version`/`created`/`deleted` in `audit_fields()` — Auditable-Konvention.
5. **DAO-Trait** — `#[automock(type Transaction = crate::MockTransaction;)] #[async_trait] pub trait RepaymentPhaseDao { type Transaction: crate::Transaction; ... }` mit den 3 Pflicht-Methoden (dump_all/create/update) + 2 Default-Impls (all/find_by_id), exakt analog assembly.rs Z. 96-138. Filter-Pattern `.iter().filter(|e| e.deleted.is_none())` für `all()` (D-10).
6. **Test-Modul** — `#[cfg(test)] mod tests { ... }` mit den 7 Tests aus dem `<behavior>`-Block. Vorlage: assembly.rs Z. 140-251 (test_assembly_status_roundtrip, test_assembly_status_strings_are_english, test_assembly_status_invalid_string, test_assembly_status_default_is_preparation, test_auditable_entity_type_is_assembly, test_auditable_fields_count_and_excludes, test_auditable_diff_detects_status_change). Anzahl-Assertion auf 5 anpassen (vorher 6 bei Assembly wg. name/date/location, jetzt 5: fiscal_year/share_value/status/opened_at/closed_at).

**Schritt B: Modul-Deklaration in `genossi_dao/src/lib.rs`.**

Öffne `genossi_dao/src/lib.rs`. Füge `pub mod repayment_phase;` alphabetisch korrekt ein — zwischen `pub mod permission;` und dem nächsten alphabetisch folgenden Modul (vermutlich `pub mod session;` oder ein anderes mit `s`/`t`/`u`-Anfang). Erst die existierenden Mod-Deklarationen lesen, um exakte Einsortier-Position zu finden, dann INSERT mit Edit-Tool (NICHT mit Bash-Heredoc).
  </action>
  <verify>
    <automated>test -f genossi_dao/src/repayment_phase.rs && grep -c 'pub enum RepaymentPhaseStatus' genossi_dao/src/repayment_phase.rs | grep -q '^1$' && grep -q 'Preparation' genossi_dao/src/repayment_phase.rs && grep -q 'Open' genossi_dao/src/repayment_phase.rs && grep -q 'Closed' genossi_dao/src/repayment_phase.rs && grep -c 'pub struct RepaymentPhaseEntity' genossi_dao/src/repayment_phase.rs | grep -q '^1$' && grep -c 'impl crate::auditable::Auditable for RepaymentPhaseEntity' genossi_dao/src/repayment_phase.rs | grep -q '^1$' && grep -c '#\[async_trait\]' genossi_dao/src/repayment_phase.rs | grep -q '^1$' && grep -c 'pub trait RepaymentPhaseDao' genossi_dao/src/repayment_phase.rs | grep -q '^1$' && grep -q 'fiscal_year: i32' genossi_dao/src/repayment_phase.rs && grep -q 'share_value: i64' genossi_dao/src/repayment_phase.rs && grep -q 'pub mod repayment_phase' genossi_dao/src/lib.rs && cargo test -p genossi_dao --lib repayment_phase 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `genossi_dao/src/repayment_phase.rs`, ≥ 220 lines
    - `grep -c 'pub enum RepaymentPhaseStatus' genossi_dao/src/repayment_phase.rs` == 1
    - `grep -c 'pub struct RepaymentPhaseEntity' genossi_dao/src/repayment_phase.rs` == 1
    - `grep -c 'impl crate::auditable::Auditable for RepaymentPhaseEntity' genossi_dao/src/repayment_phase.rs` == 1
    - `grep -c 'pub trait RepaymentPhaseDao' genossi_dao/src/repayment_phase.rs` == 1
    - Field types match exactly: `fiscal_year: i32`, `share_value: i64`, `status: RepaymentPhaseStatus`, `opened_at: Option<time::PrimitiveDateTime>`, `closed_at: Option<time::PrimitiveDateTime>` (greppable strings)
    - `audit_fields()`-Vec ist exakt 5 Einträge — manuell prüfbar via Lesen der Methode; greppable: `grep -c '("fiscal_year",\\|("share_value",\\|("status",\\|("opened_at",\\|("closed_at",' genossi_dao/src/repayment_phase.rs | grep -q '^5$'`
    - `grep -c '("id",\\|("version",\\|("created",\\|("deleted",' genossi_dao/src/repayment_phase.rs` == 0 (Konvention: Auditable schließt diese Metadaten-Felder aus)
    - `genossi_dao/src/lib.rs` enthält `pub mod repayment_phase;`
    - `cargo test -p genossi_dao --lib repayment_phase` — alle 7 Tests grün
    - `cargo build -p genossi_dao` ist grün
  </acceptance_criteria>
  <done>`genossi_dao/src/repayment_phase.rs` existiert mit Status-Enum + Entity + Auditable + DAO-Trait + 7 grünen Unit-Tests; `genossi_dao/src/lib.rs` deklariert das Modul; `cargo test -p genossi_dao` läuft grün durch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB-Layer ↔ Service-Layer | Service-Code übergibt UUIDs/Timestamps an die DAO-Schicht — keine User-Inputs auf dieser Ebene |
| Test-Code ↔ Production-Code | `#[automock]`-erzeugte Mocks dürfen nicht im Production-Build landen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | `RepaymentPhaseEntity.audit_fields()` Reihenfolge | mitigate | Pattern-konsistent mit `AssemblyEntity::audit_fields()` (assembly.rs Z. 70-93). Wenn Reihenfolge sich später ändert, bricht Hash-Chain-Konsistenz für historische Einträge — daher in Plan 03 Service-Tests die Audit-Diff-Reihenfolge per Unit-Test einfrieren. |
| T-07-01-02 | Information Disclosure | `audit_fields()` exponiert sensitive Datenpunkte | accept | `fiscal_year` und `share_value` sind betriebs-öffentliche Genossenschaftsdaten (im Geschäftsbericht ausgewiesen); kein PII. Audit-Log ist sowieso Vorstand-only via OIDC (Plan 04). |
| T-07-01-03 | Repudiation | Auditable-Implementierung wird vergessen oder umgangen | mitigate | Plan 03 ruft NUR `audited_create!`/`audited_update!`/`audited_delete!` auf (KEINE direkten `dao.create()`/`dao.update()`/`dao.update_with_deleted()`-Aufrufe). Code-Review-Checkliste in Plan 03 erzwingt `grep -c 'self.repayment_phase_dao.create(\\|self.repayment_phase_dao.update(' genossi_service_impl/src/repayment_phase.rs == 0` außerhalb der Macro-Pfade. |
| T-07-01-04 | Denial of Service | Sehr großer `i64`-`share_value` löst INTEGER-Overflow in späteren Phase-9/11-Berechnungen | accept | Service-Layer (Plan 03) validiert nur `> 0`. `i64::MAX` Cent = ~92 Trillionen Euro — kein realistisches Risiko (Genossenschafts-Buchhaltungs-Domain). Phase 9 (PAYO) und Phase 11 (Export-Multiplikation) müssen separat gegen Overflow härten, falls jemals relevant — out of Phase 7. |
| T-07-01-05 | Spoofing | `RepaymentPhaseStatus::from_str` akzeptiert deutsche Strings | mitigate | Test 3 (`test_repayment_phase_status_invalid_string`) verifiziert, dass `"Vorbereitung"` zu `DaoError::ParseError` führt. Nur die 3 englischen Strings sind akzeptiert (D-01). |
</threat_model>

<verification>
- `cargo build -p genossi_dao` ist grün
- `cargo test -p genossi_dao --lib repayment_phase` — alle 7 Tests grün
- Migration-Datei existiert, hat keine UNIQUE-Constraints, hat 2 Indizes
- Modul-Deklaration in `lib.rs` ist alphabetisch korrekt eingefügt
- Audit-Field-Liste enthält exakt 5 Einträge (fiscal_year, share_value, status, opened_at, closed_at) ohne id/version/created/deleted
</verification>

<success_criteria>
- **ROADMAP SC#1 (Migration legt `repayment_phase`-Tabelle an):** vollständig erfüllt durch Task 1
- **ROADMAP SC#2 (Auditable-Implementierung, audited_*-Macros greifen):** Teil 1 (Auditable-Trait-Impl) erfüllt durch Task 2; Teil 2 (Macros greifen tatsächlich) wird in Plan 03 verifiziert
- **PHAS-01 (RepaymentPhase mit fiscal_year + share_value anlegbar):** Datenmodell-Foundation steht (Entity + Tabelle); REST-Endpoint kommt Plan 04
- **PHAS-05 (RepaymentPhase auditpflichtig):** `Auditable`-Trait implementiert; konkrete `audited_*!`-Aufrufe kommen Plan 03

Phase-7-Plans 02-05 hängen alle an diesem Plan (depends_on: [01]).
</success_criteria>

<output>
Nach Abschluss erstellen unter `.planning/phases/07-repaymentphase-backend-foundation/07-01-SUMMARY.md`:
- Was wurde gebaut (Migration + Entity + DAO-Trait + Auditable)
- Welche Plan-IDs hängen davon ab (02, 03, 04, 05)
- Domain-Substitutionen, die Plan 02+ direkt übernehmen müssen (i32 vs i64, ORDER BY-Wechsel auf `fiscal_year DESC, created DESC`, kein Detail-Wrapper-Typ)
- Test-Ergebnisse (7 Tests grün, Anzahl-Asserts auf 5 statt 6)
</output>
