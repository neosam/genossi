---
phase: 25
plan: 05
type: execute
wave: 4
depends_on:
  - 25-04
files_modified:
  - genossi-frontend/src/api.rs
  - genossi-frontend/src/component/application_document_slot.rs
  - genossi-frontend/src/component/application_detail.rs
  - genossi-frontend/src/component/mod.rs
  - genossi-frontend/src/i18n/mod.rs
  - genossi-frontend/src/i18n/de.rs
  - genossi-frontend/src/i18n/en.rs
  - genossi_bin/tests/e2e_tests.rs
  - .planning/phases/25-application-file-upload-audited-carryover/25-UAT-CHECKLIST.md
autonomous: false
requirements:
  - APDOC-05
  - APDOC-04
  - APDOC-02
must_haves:
  truths:
    - "A new `ApplicationDocumentSlot` component under `genossi-frontend/src/component/` handles both states (empty → Upload-Button; filled → filename+size+date + Download/Replace/Delete icons) with `r#type: \"button\"` on every interactive button"
    - "Application-Detail-Page composes the slot only when `application.status == ApplicationStatusTO::Offen` (after confirm() the application becomes Bestaetigt and the doc lives on the Member)"
    - "Frontend `api.rs` gains `upload_application_document / get_application_document / delete_application_document / application_document_download_url` mirroring the MemberDocument functions"
    - "i18n keys added to BOTH `de.rs` AND `en.rs` (never only one — locale-drift regression from Phase 24)"
    - "One new e2e test in `genossi_bin/tests/e2e_tests.rs` proves the full flow: upload → confirm → assert MemberDocument exists on Member AND `application_document.deleted != NULL` AND audit hashchain still `/api/audit/verify` = OK"
    - "A UAT checklist enumerates admin-vs-nonadmin, MIME reject (415), body-limit reject (413), replace-in-place, confirm-with-doc, confirm-without-doc"
  artifacts:
    - genossi-frontend/src/component/application_document_slot.rs
    - genossi-frontend/src/api.rs (extended)
    - genossi_bin/tests/e2e_tests.rs (extended)
    - .planning/phases/25-application-file-upload-audited-carryover/25-UAT-CHECKLIST.md
  key_links:
    - "Backend URL: `/api/applications/{application_id}/document` — must match `document_download_url` frontend helper"
    - "Slot is composed inside `application_detail.rs` between the Detail fields and the Confirm/Reject buttons"
    - "e2e test uses real HTTP client (reqwest multipart) against a mock_auth server on a random port — mirror existing e2e_tests.rs infrastructure"
---

<objective>
Wave 4: Frontend `ApplicationDocumentSlot` component + `api.rs` upload/download/delete helpers + integration into `application_detail.rs` (only when status=Offen) + i18n keys in both locales + one end-to-end HTTP test that proves the audited Move-transfer + UAT-CHECKLIST.md.

Purpose: This is the ship gate — the user-visible surface (APDOC-05) plus the e2e proof that the whole cascade wires correctly. The e2e test is the single most important gate for the phase.
Output: One reusable component + api client + e2e test + UAT checklist ready for Vorstand smoke-test.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-application-file-upload-audited-carryover/25-CONTEXT.md
@.planning/phases/25-application-file-upload-audited-carryover/25-RESEARCH.md
@.planning/phases/25-application-file-upload-audited-carryover/25-04-SUMMARY.md
@genossi-frontend/src/component/application_detail.rs
@genossi-frontend/src/api.rs
@genossi-frontend/src/i18n/mod.rs
@genossi-frontend/src/i18n/de.rs
@genossi-frontend/src/i18n/en.rs
@genossi_bin/tests/e2e_tests.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frontend api.rs helpers + i18n keys (both locales) + ApplicationDocumentSlot component + Application-Detail integration</name>
  <files>genossi-frontend/src/api.rs, genossi-frontend/src/i18n/mod.rs, genossi-frontend/src/i18n/de.rs, genossi-frontend/src/i18n/en.rs, genossi-frontend/src/component/application_document_slot.rs, genossi-frontend/src/component/mod.rs, genossi-frontend/src/component/application_detail.rs</files>
  <read_first>
    - genossi-frontend/src/api.rs Zeilen 319–410 (MemberDocument upload/delete/download-url — the template for FormData + fetch + serde_wasm_bindgen deserialization)
    - genossi-frontend/src/component/application_detail.rs (full file — anchor points: after Detail fields, before the Confirm/Reject dialog block; the component takes `application: ApplicationTO` + `on_changed: EventHandler<()>`)
    - genossi-frontend/src/component/mod.rs (find how existing components are re-exported — likely `pub mod X;`)
    - genossi-frontend/src/i18n/mod.rs (find `enum Key` and existing `MailEditor*`/`ApplicationDetails` variants to see the naming convention)
    - genossi-frontend/src/i18n/de.rs + en.rs (parallel arm patterns — every key MUST have both De AND En arms per genossi-frontend/CLAUDE.md)
    - genossi-frontend/CLAUDE.md (Component-First: ALWAYS extract multi-state UI into `src/component/`; both locales required)
    - .claude/CLAUDE.md project memory `feedback_dioxus_button_type.md` (`r#type: "button"` + onclick, never form-submit — Phase 17 hotfix e245013)
    - 25-CONTEXT.md decision #8 (empty state = Upload-Button; filled = filename+size+date + Download/Replace/Delete icons)
  </read_first>
  <action>
Six coordinated edits — each self-contained. Present in the order Executor should perform them:

**A. `genossi-frontend/src/api.rs` — add 4 functions.**

Append after the existing `member_document` block (around line 425):

1. `pub async fn upload_application_document(config: &Config, application_id: Uuid, file: web_sys::File) -> Result<ApplicationDocumentTO, AppError>` — mirror `upload_member_document` (Zeilen 329–383) but:
   - URL: `format!("{}/api/applications/{application_id}/document", config.backend)`
   - FormData: only append `"file"` with `append_with_blob_and_filename`; DO NOT append `document_type` or `description`.
   - Deserialize the response as `ApplicationDocumentTO` (from `rest_types::ApplicationDocumentTO`).

2. `pub async fn get_application_document(config: &Config, application_id: Uuid) -> Result<Option<ApplicationDocumentTO>, AppError>` — GET the URL; if 404, return `Ok(None)`; if 200, return `Ok(Some(...))`; else error. Note: since the endpoint returns file bytes when present, we need a JSON-alternative. Instead: expose GET as a HEAD-like check — but simpler for the frontend, add a small companion service method. **Decision:** the frontend list use-case is served by the ApplicationTO itself carrying an optional `document: Option<ApplicationDocumentTO>` field — but that requires a backend change. To keep this plan scoped, take the SIMPLE route: **use `reqwest::Client::new().head(url)` to test existence, then read a companion metadata endpoint**. NO — that means an extra endpoint. Cleaner: **remove `get_application_document` from api.rs and instead have the component call a lightweight metadata endpoint at `/api/applications/{id}/document?meta=1` that returns the TO as JSON when `meta=1`**. 
   
   Simpler still — extend the backend GET route in Plan 04 Task 1: if query parameter `meta=1` is present, respond with `ApplicationDocumentTO` JSON; otherwise stream the file. **However, since Plan 04 is already sealed by this dependency chain, DO THIS in this task via a new small route addition:** modify `genossi_rest/src/application_document.rs` (already introduced in Plan 04) to accept `Query<HashMap<String, String>>` and branch on `meta=1`. Add this handler-change as sub-step A0 BEFORE the frontend changes — see A0 below.

**A0. Backend micro-addition (predependency of A2 frontend query):** In `genossi_rest/src/application_document.rs`, modify the download handler to accept `Query<HashMap<String,String>>`. If the query map contains `meta` with value `"1"`, respond with 200 + `ApplicationDocumentTO` JSON. Otherwise stream bytes as before. Rationale: single URL for the slot component to poll metadata AND to download; keeps the route surface at three (POST/GET/DELETE) with an optional query flag. Update the `#[utoipa::path]` `params` to declare the optional `meta` query. Add a small integration/unit test in `genossi_rest/src/application_document.rs` that both branches respond correctly against a mock service.

Now `get_application_document(config, application_id)` in `api.rs` fetches `{}/api/applications/{application_id}/document?meta=1`; expect 200 → parse JSON → `Ok(Some(TO))`; expect 404 → `Ok(None)`.

3. `pub async fn delete_application_document(config: &Config, application_id: Uuid) -> Result<(), AppError>` — mirror `delete_member_document` (Zeilen 385–398).

4. `pub fn application_document_download_url(config: &Config, application_id: Uuid) -> String` — returns `format!("{}/api/applications/{application_id}/document", config.backend)` (no `?meta=1` — this is the byte-stream URL used as the `<a href>` target for download).

**B. `genossi-frontend/src/i18n/mod.rs` — add 6 Key variants.**

Add to `enum Key` (find the `MailEditor*` or `ApplicationDetails` variants and add alphabetically):
- `ApplicationDocumentUpload`
- `ApplicationDocumentReplace`
- `ApplicationDocumentDownload`
- `ApplicationDocumentDelete`
- `ApplicationDocumentEmptyState`
- `ApplicationDocumentDeleteConfirm`

**C. `genossi-frontend/src/i18n/de.rs` — add 6 arms in the `Locale::De` match block.**

- `Key::ApplicationDocumentUpload => "Antrag hochladen",`
- `Key::ApplicationDocumentReplace => "Ersetzen",`
- `Key::ApplicationDocumentDownload => "Herunterladen",`
- `Key::ApplicationDocumentDelete => "Löschen",`
- `Key::ApplicationDocumentEmptyState => "Kein Antrags-Dokument hinterlegt.",`
- `Key::ApplicationDocumentDeleteConfirm => "Dieses Antrags-Dokument wirklich löschen?",`

**D. `genossi-frontend/src/i18n/en.rs` — add the same 6 arms in the `Locale::En` block.**

- `Key::ApplicationDocumentUpload => "Upload application",`
- `Key::ApplicationDocumentReplace => "Replace",`
- `Key::ApplicationDocumentDownload => "Download",`
- `Key::ApplicationDocumentDelete => "Delete",`
- `Key::ApplicationDocumentEmptyState => "No application document attached.",`
- `Key::ApplicationDocumentDeleteConfirm => "Really delete this application document?",`

Both locales in the SAME commit — locale drift is a documented anti-pattern (per genossi-frontend/CLAUDE.md).

**E. `genossi-frontend/src/component/application_document_slot.rs` — new component.**

```
#[component]
pub fn ApplicationDocumentSlot(
    application_id: Uuid,
    on_changed: EventHandler<()>,
) -> Element {
    // State: document: Signal<Option<ApplicationDocumentTO>>
    // Effect: on mount + when application_id changes, fetch via api::get_application_document.
    // Empty state: Upload button opens <input type="file"> (hidden) via web_sys ref pattern.
    // Filled state: filename + size formatted (KB/MB) + created date DD.MM.YYYY.
    //   Download button/anchor: href = application_document_download_url; download attribute
    //   Replace button: opens file input; on select calls upload_application_document, on Ok updates signal + on_changed.call(()).
    //   Delete button: opens native web_sys::window().confirm_with_message using ApplicationDocumentDeleteConfirm; on OK calls delete_application_document, on Ok clears signal + on_changed.call(()).
    // ALL buttons: r#type: "button" + onclick (never form-submit — Dioxus reload bug).
    // Error state: local error signal displayed in a red banner (reuse ErrorAlert component if present).
    rsx! { ... }
}
```

Concrete rules:
- Import: `use crate::api::{self, application_document_download_url, upload_application_document, get_application_document, delete_application_document};` and `use rest_types::ApplicationDocumentTO;`.
- Loading state: `use_signal(|| true)` while the initial GET is in flight (mirror existing use_loader pattern from Phase 999.4 backlog is deferred — copy the `use_signal(|| true) + use_effect + spawn` triple from `applications_page.rs`).
- File input: hidden `input { r#type: "file", accept: ".pdf,.png,.jpg,.jpeg,.webp,.txt,.doc,.docx,.odt,.xls,.xlsx,.ods", style: "display: none", onchange: ... }` — get the File via `evt.files()`.
- Size format: `let (val, unit) = if size < 1024*1024 { (size / 1024, "KB") } else { (size / (1024*1024), "MB") };`.
- Date format: use existing `iso8601`/`created`-to-DE-format helper if present in codebase; otherwise inline: `time::format_description::parse("[day].[month].[year]").ok().and_then(|f| doc.created.format(&f).ok()).unwrap_or_default()`.

**F. `genossi-frontend/src/component/mod.rs` — re-export the new component.**

Add `pub mod application_document_slot;` and `pub use application_document_slot::ApplicationDocumentSlot;` (mirror `application_detail` export pattern in the same file).

**G. `genossi-frontend/src/component/application_detail.rs` — integrate the slot.**

Wrap the slot in a `if is_open {` guard (line 30 already computes `is_open = application.status == ApplicationStatusTO::Offen`). Insert AFTER the Detail-fields block (around line 168 — right before the Confirm/Reject buttons) a new block:

```
if is_open {
    ApplicationDocumentSlot {
        application_id: app_id,
        on_changed: move |_| on_changed.call(()),
    }
}
```

Use `Edit` (scoped) — do NOT rewrite this file.

Comment-text discipline: any `//` comment in `application_document_slot.rs` MUST NOT contain the literal string `r#type: "submit"` (would defeat the negative-grep gate).
  </action>
  <acceptance_criteria>
    - `test -f genossi-frontend/src/component/application_document_slot.rs` returns success.
    - `grep -c "pub mod application_document_slot" genossi-frontend/src/component/mod.rs` equals `1`.
    - `grep -c "ApplicationDocumentSlot" genossi-frontend/src/component/application_detail.rs` at least `1`.
    - `grep -c "upload_application_document\|get_application_document\|delete_application_document\|application_document_download_url" genossi-frontend/src/api.rs` at least `4`.
    - `grep -c "ApplicationDocumentUpload\|ApplicationDocumentReplace\|ApplicationDocumentDownload\|ApplicationDocumentDelete\|ApplicationDocumentEmptyState\|ApplicationDocumentDeleteConfirm" genossi-frontend/src/i18n/mod.rs` at least `6`.
    - `grep -c "ApplicationDocumentUpload" genossi-frontend/src/i18n/de.rs` equals `1`.
    - `grep -c "ApplicationDocumentUpload" genossi-frontend/src/i18n/en.rs` equals `1`.
    - **Both-locales gate:** `for k in ApplicationDocumentUpload ApplicationDocumentReplace ApplicationDocumentDownload ApplicationDocumentDelete ApplicationDocumentEmptyState ApplicationDocumentDeleteConfirm; do [ "$(grep -c "$k" genossi-frontend/src/i18n/de.rs)" -ge 1 ] && [ "$(grep -c "$k" genossi-frontend/src/i18n/en.rs)" -ge 1 ] || { echo "Missing locale arm for $k"; exit 1; }; done`.
    - `grep -v '^//' genossi-frontend/src/component/application_document_slot.rs | grep -c "r#type: \"submit\""` equals `0` (must be `"button"`).
    - `grep -c "r#type: \"button\"" genossi-frontend/src/component/application_document_slot.rs` at least `2` (multiple interactive buttons).
    - `cargo check -p genossi_rest -p genossi_bin` succeeds (backend `meta=1` change still compiles).
    - `cargo check --target wasm32-unknown-unknown -p genossi-frontend` succeeds (frontend WASM build).
    - `cargo test -p genossi_rest application_document` runs (the meta=1 branch unit test passes).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; test -f genossi-frontend/src/component/application_document_slot.rs; grep -q "pub mod application_document_slot" genossi-frontend/src/component/mod.rs; grep -q "ApplicationDocumentSlot" genossi-frontend/src/component/application_detail.rs; [ "$(grep -cE "upload_application_document|get_application_document|delete_application_document|application_document_download_url" genossi-frontend/src/api.rs)" -ge 4 ]; for k in ApplicationDocumentUpload ApplicationDocumentReplace ApplicationDocumentDownload ApplicationDocumentDelete ApplicationDocumentEmptyState ApplicationDocumentDeleteConfirm; do grep -q "$k" genossi-frontend/src/i18n/de.rs || { echo "Missing De arm for $k"; exit 1; }; grep -q "$k" genossi-frontend/src/i18n/en.rs || { echo "Missing En arm for $k"; exit 1; }; done; [ "$(grep -v "^//" genossi-frontend/src/component/application_document_slot.rs | grep -c "r#type: \"submit\"")" = "0" ]; cargo check -p genossi_rest 2>&amp;1 | tail -5; cargo check -p genossi_bin 2>&amp;1 | tail -5'</automated>
  </verify>
  <done>New slot component compiles, both locales updated, api.rs helpers added, application_detail composes the slot only for Offen applications, backend meta=1 branch added and tested, no submit-type buttons (only "button" type).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: End-to-end HTTP test for Upload → confirm → audited MemberDocument + audit-verify green</name>
  <files>genossi_bin/tests/e2e_tests.rs</files>
  <read_first>
    - genossi_bin/tests/e2e_tests.rs (full file — existing e2e helpers: `test_server_url`, `TestServer` startup, seeding helpers, reqwest client usage; find the `#[tokio::test]` blocks for `application_confirm_*` patterns)
    - genossi_rest/src/test_server.rs (server-with-random-port helper)
    - 25-RESEARCH.md § "Test-Strategie" (E2E genossi_bin: `Upload → confirm → MemberDocument sichtbar am Member + application_documents.deleted != NULL + Audit-Row exists`)
    - genossi_service_impl/src/audit_log.rs OR `/api/audit/verify` route in genossi_rest/src/audit_log.rs (to construct the verify-endpoint call)
  </read_first>
  <behavior>
    - Test E2E-1 (`application_upload_confirm_carryover_audited`): Bring up a test server with in-memory SQLite; POST an application (public join endpoint or admin API); POST a multipart file to `/api/applications/{id}/document`; assert 201 + `ApplicationDocumentTO` in body; POST `/api/applications/{id}/confirm`; assert 200 + `ApplicationTO` with `status == "Bestaetigt"`; GET `/api/members/{new_member_id}/documents` and assert exactly ONE MemberDocument with `document_type == "other"` and `description` starting with `Original-Antrag (übernommen bei Bestätigung am `; assert `GET /api/applications/{id}/document?meta=1` returns 404 (soft-deleted); assert `GET /api/audit/verify` returns 200 + hash-chain-valid JSON.
    - Test E2E-2 (`application_upload_confirm_missing_file_rolls_back`): Same setup, but between upload and confirm, physically delete the file from the storage directory (via `std::fs::remove_file` on the storage base_path computed from env var); POST confirm; assert HTTP 500 (or 4xx per RestError mapping); assert `GET /api/applications/{id}` still returns `status == "Offen"`; assert `GET /api/audit/verify` still returns 200.
    - Test E2E-3 (`application_upload_replace_in_place`): Upload once, then upload again (same application); assert the SECOND POST returns 201 with a `version` different from the first; GET `?meta=1` returns exactly one active row.
  </behavior>
  <action>
Append 3 `#[tokio::test]` functions to `genossi_bin/tests/e2e_tests.rs`. Follow the existing pattern for test-server setup exactly (do NOT invent new infrastructure). Concrete guidance:

- Reuse `TestServer::start()` (or the existing equivalent — read the file first). It should return a `TestServer` with `.url()`, `.pool()`, and shutdown on drop.
- Use `reqwest::Client::builder().build()?` for the HTTP client. Multipart upload uses `reqwest::multipart::Form::new().part("file", reqwest::multipart::Part::bytes(vec![0u8; 1024]).file_name("antrag.pdf").mime_str("application/pdf")?)`.
- Seeding a mock-auth admin context: mock_auth mode automatically injects a `Context` with all privileges (see `genossi_service_impl/src/session.rs:119-137` per the memory note); no manual token needed.
- Creating an application: either POST to `/api/applications/join` (public route) with an `ApplicationSubmission` body, or seed directly via a helper if one exists. Prefer the HTTP path for realism.
- Computing storage path for Test E2E-2: read `DOCUMENT_STORAGE_PATH` env var (or default `./documents`); path = `{base}/applications/{app_id}/{doc_id}.{ext}` per Pitfall 7 / Plan 03 Task 2. Or: retrieve the path via a helper endpoint if one exists. Simpler: after upload, list the file with `walkdir` under the base to grab the actual path; delete it.
- For audit-verify: `GET {server_url}/api/audit/verify`; parse the JSON `{valid: bool}` (verify shape by reading `genossi_rest/src/audit_log.rs` first); assert `valid == true`.
- For Test E2E-3, extract the `version` field from both responses' `ApplicationDocumentTO` and assert they differ.

Do NOT skip cleanup: use `TestServer::shutdown()` if it exists, or rely on `Drop`. Do NOT hardcode ports — use `test_server_url` helper.

Comment-text discipline: do NOT reference the literal substring `Bestaetigt` in test comments — always dereference via the enum variant (this avoids double-counting in grep gates that check the enum was actually asserted).
  </action>
  <acceptance_criteria>
    - `grep -c "async fn application_upload_confirm_carryover_audited" genossi_bin/tests/e2e_tests.rs` equals `1`.
    - `grep -c "async fn application_upload_confirm_missing_file_rolls_back" genossi_bin/tests/e2e_tests.rs` equals `1`.
    - `grep -c "async fn application_upload_replace_in_place" genossi_bin/tests/e2e_tests.rs` equals `1`.
    - `grep -c "audit/verify" genossi_bin/tests/e2e_tests.rs` at least `2` (called from tests E2E-1 and E2E-2).
    - `grep -c "Original-Antrag (übernommen bei Bestätigung am" genossi_bin/tests/e2e_tests.rs` at least `1`.
    - `cargo test --test e2e_tests application_upload_confirm_carryover_audited -- --nocapture` passes.
    - `cargo test --test e2e_tests application_upload_confirm_missing_file_rolls_back -- --nocapture` passes.
    - `cargo test --test e2e_tests application_upload_replace_in_place -- --nocapture` passes.
    - Existing e2e tests in the file still pass (no regressions): `cargo test --test e2e_tests` full run has 0 new failures.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; grep -q "async fn application_upload_confirm_carryover_audited" genossi_bin/tests/e2e_tests.rs; grep -q "async fn application_upload_confirm_missing_file_rolls_back" genossi_bin/tests/e2e_tests.rs; grep -q "async fn application_upload_replace_in_place" genossi_bin/tests/e2e_tests.rs; [ "$(grep -c "audit/verify" genossi_bin/tests/e2e_tests.rs)" -ge 2 ]; cargo test --test e2e_tests application_upload -- --nocapture 2>&amp;1 | tail -30'</automated>
  </verify>
  <done>Three e2e tests pass and prove: audited carryover (E2E-1), missing-file rollback (E2E-2), replace-in-place (E2E-3). Full e2e suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Vorstand-facing UAT checklist + auto-approved regression pass</name>
  <files>.planning/phases/25-application-file-upload-audited-carryover/25-UAT-CHECKLIST.md</files>
  <what-built>
    Backend REST endpoints POST/GET/DELETE `/api/applications/{id}/document`, extended `confirm()` cascade with audited MemberDocument carryover + missing-file rollback, and Dioxus `ApplicationDocumentSlot` component integrated into Application-Detail page. Three passing e2e tests pin the audit-critical carryover behavior. This checkpoint records the manual UAT checklist for the Vorstand smoke session and auto-approves after the regression suite passes cleanly.
  </what-built>
  <action>
Create `.planning/phases/25-application-file-upload-audited-carryover/25-UAT-CHECKLIST.md` with the 12-step list defined in `<how-to-verify>` below. File format mirrors Phase 24's `24-UAT-CHECKLIST.md` — bullet checkbox list, HARD FAIL GATES explicitly marked. Then run the automated regression portion (Steps 1–3) inline; on pass, auto-approve per Auto Mode.
  </action>
  <how-to-verify>
    Create `.planning/phases/25-application-file-upload-audited-carryover/25-UAT-CHECKLIST.md` with the following checkbox list (fill in file paths + URLs verbatim):

    1. `cargo test --workspace` runs and reports 0 new failures compared to `.planning/STATE.md` baseline. (HARD FAIL GATE — automatable regression)
    2. `cargo clippy --all-targets -- -D warnings` clean. (HARD FAIL GATE — automatable)
    3. `cargo fmt -- --check` clean. (HARD FAIL GATE — automatable)
    4. Start backend + frontend per project skill `run-rust-backend-and-frontend`; navigate to Applications list; open an Offen application; verify Slot shows empty state with "Antrag hochladen" button (DE locale).
    5. Click "Antrag hochladen", pick a valid PDF (<50 MB), observe 201 response + slot flips to filled state showing filename + size + upload date DD.MM.YYYY.
    6. Click "Ersetzen", pick a different PDF, observe filename changes; download the file and byte-diff against the second file to prove replace-in-place.
    7. Click "Löschen", confirm the native browser dialog with "Dieses Antrags-Dokument wirklich löschen?", observe slot returns to empty state.
    8. Upload again, then click "Bestätigen" on the application; observe application status flips to Bestaetigt; navigate to the new Member's detail page → Documents tab → verify a MemberDocument named "other" with description starting with `Original-Antrag (übernommen bei Bestätigung am DD.MM.YYYY)` where the date matches today. (HARD FAIL GATE — this is the APDOC-03 audit-critical proof)
    9. `GET /api/audit/verify` returns 200 + `{valid: true}` (HARD FAIL GATE — audit hashchain valid).
    10. Attempt upload via curl WITHOUT session cookie / with a non-admin cookie; observe 401 or 403; observe `application_documents` table has NO new rows (via `sqlite3 genossi.db 'SELECT COUNT(*) FROM application_documents;'`). (HARD FAIL GATE — CR-02/APDOC-02 regression proof)
    11. Attempt upload with a `.exe` file; observe 415 + JSON body listing `allowed_extensions`.
    12. Attempt upload with a >50 MB file (create via `dd if=/dev/urandom of=big.bin bs=1M count=60`); observe 413.

    Steps 1–3 auto-approve on success. Steps 4–12 are the browser-interactive walkthrough deferred to the Vorstand smoke session per project convention (mirror Phase 24 UAT approach). Set `<resume-signal>` accordingly.
  </how-to-verify>
  <verify>
    <automated>bash -c 'test -f .planning/phases/25-application-file-upload-audited-carryover/25-UAT-CHECKLIST.md &amp;&amp; grep -c "HARD FAIL GATE" .planning/phases/25-application-file-upload-audited-carryover/25-UAT-CHECKLIST.md | awk "{if(\$1&gt;=5)exit 0; else exit 1}"'</automated>
  </verify>
  <done>UAT-CHECKLIST.md exists with all 12 steps, ≥5 HARD FAIL GATES marked, automated Steps 1–3 pass, browser walkthrough deferred to Vorstand smoke session, checkpoint auto-approved.</done>
  <resume-signal>Type "approved" once the automated portion (Steps 1–3) passes and the UAT-CHECKLIST.md file is committed. Browser-interactive walkthrough (Steps 4–12) is deferred to the Vorstand smoke session before merge (do NOT hold the phase for it).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → REST (multipart) | User-picked file; validated at REST via `DefaultBodyLimit` + `lookup_allowed_mime`. |
| Browser → REST (DELETE) | Idempotent operation; permission enforced by service. |
| e2e test → REST | Test client uses real HTTP + reqwest multipart; validates end-to-end contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-05-01 | Tampering | Dioxus button form-submit reload bug | high | mitigate | All interactive buttons in `ApplicationDocumentSlot` use `r#type: "button"` + `onclick`. Acceptance grep gate rejects any `r#type: "submit"`. |
| T-25-05-02 | Information Disclosure | Locale drift causing wrong-language error | low | mitigate | Both-locales gate in acceptance criteria ensures every new i18n key exists in `de.rs` AND `en.rs`. |
| T-25-05-03 | Denial of Service | Confirm-time missing-file NOT rolling back | high | mitigate | e2e test E2E-2 pins the rollback contract. If executor breaks it, test fails and the phase does not ship. |
| T-25-05-04 | Repudiation | Audit hashchain invalidated by carryover | high | mitigate | e2e test E2E-1 asserts `/api/audit/verify` returns `valid: true` after confirm cascade. |
| T-25-05-05 | Elevation of Privilege | Unauthenticated upload | high | mitigate | UAT Step 10 + service-layer CR-02 unit test (Plan 03 Test 3) prove no side-effect on unauthorized calls. |
| T-25-05-SC | Tampering | package installs | low | accept | No new packages in frontend or backend (reqwest, walkdir already in dev-deps; dioxus, web-sys already in main deps). |
</threat_model>

<verification>
```bash
# Regression suite
cargo build --workspace
cargo test --workspace
cargo test --test e2e_tests
cargo fmt -- --check
cargo clippy --all-targets -- -D warnings
# WASM build
cargo check --target wasm32-unknown-unknown -p genossi-frontend
# Locale sanity
for k in ApplicationDocumentUpload ApplicationDocumentReplace ApplicationDocumentDownload ApplicationDocumentDelete ApplicationDocumentEmptyState ApplicationDocumentDeleteConfirm; do
  grep -q "$k" genossi-frontend/src/i18n/de.rs && grep -q "$k" genossi-frontend/src/i18n/en.rs || { echo "$k missing in a locale"; exit 1; }
done
```
</verification>

<success_criteria>
- `ApplicationDocumentSlot` component reusable, in `component/`, only rendered when `status=Offen`.
- api.rs helpers mirror the MemberDocument pattern.
- Backend `?meta=1` branch on GET route allows the slot to poll metadata without downloading bytes.
- Both locales carry all 6 new keys.
- Three e2e tests pass: audited carryover, missing-file rollback, replace-in-place.
- UAT checklist filed with 12 steps + 5 HARD FAIL GATES + auto-approved regression portion.
- Full workspace `cargo test` + `cargo clippy` + `cargo fmt --check` green.
</success_criteria>

<output>
Create `.planning/phases/25-application-file-upload-audited-carryover/25-05-SUMMARY.md` when done. Three jj commits:
- `feat(25-05): add ApplicationDocumentSlot component + api.rs helpers + i18n keys (De+En) + backend ?meta=1 branch (APDOC-05)`
- `test(25-05): add 3 e2e HTTP tests for upload/confirm-carryover/missing-file-rollback/replace-in-place`
- `docs(25-05): add 25-UAT-CHECKLIST.md with 12 steps + 5 HARD FAIL GATES`
</output>
