---
phase: 25
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - APDOC-03
must_haves:
  truths:
    - "APDOC-03 in REQUIREMENTS.md and ROADMAP.md agree that the confirm() cascade uses MOVE / ownership-transfer semantics (not copy)"
    - "The wording explicitly mentions soft-delete of the application_documents row and physical move of the file to the member path"
    - "The auditable MemberDocument description format is spelled out: 'Original-Antrag (übernommen bei Bestätigung am DD.MM.YYYY)'"
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
  key_links:
    - "REQUIREMENTS.md APDOC-03 line ↔ ROADMAP.md Phase 25 Success Criteria #3 (must say the same thing verbatim on the move-semantics claim)"
    - "REQUIREMENTS.md 'Out of Scope' section MUST NOT still list 'Move-on-Activation: Antrags-Dokument wird kopiert, nicht verschoben' after the fix"
---

<objective>
Fix the APDOC-03 wording drift between the discussion decision (Move semantics per CONTEXT.md decision #3) and the two locked source-of-truth documents (REQUIREMENTS.md line 41 and ROADMAP.md Phase 25 Success Criteria #3) BEFORE any code is written, so subsequent plans can be checked against a consistent APDOC-03. Also remove the contradicting "Move-on-Activation" entry from the REQUIREMENTS.md "Out of Scope" section.

Purpose: Without this fix, every downstream plan that implements Move semantics will look like a scope reduction against the printed requirement. The doku fix is the first commit of the phase so the checker/reviewer see a consistent goal.
Output: REQUIREMENTS.md and ROADMAP.md aligned on Move semantics + updated MemberDocument description format.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-application-file-upload-audited-carryover/25-CONTEXT.md
@.planning/phases/25-application-file-upload-audited-carryover/25-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: APDOC-03 wording sync in REQUIREMENTS.md and ROADMAP.md + remove contradicting Out-of-Scope bullet</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md line 41 (current APDOC-03 with "kopiert (nicht verschoben)") and line 65 in "Out of Scope" section (currently: "Move-on-Activation: Antrags-Dokument wird kopiert, nicht verschoben (Application behält seine Datei).")
    - .planning/ROADMAP.md Phase 25 Success Criteria #3 (currently: "Beim `confirm` einer `Application` wird ein hinterlegtes Antrags-Dokument **kopiert** (nicht verschoben) ...")
    - .planning/phases/25-application-file-upload-audited-carryover/25-CONTEXT.md `<decisions>` #3 (Move semantics — Ownership-Übergabe)
    - .planning/phases/25-application-file-upload-audited-carryover/25-RESEARCH.md section `## Runtime Requirements Wording Update (APDOC-03)` (verbatim replacement text)
  </read_first>
  <action>
Apply three scoped Edit operations. Do NOT rewrite either file:

1) In `.planning/REQUIREMENTS.md`, replace the APDOC-03 line (line 41) with the new wording per 25-RESEARCH.md `## Runtime Requirements Wording Update`. Exact new text:

- `[ ] **APDOC-03**: Beim Aktivieren (\`confirm\`) einer \`Application\` wird ein hinterlegtes Antrags-Dokument **übernommen** (Ownership-Übergabe — Move-Semantik: die \`application_documents\`-Zeile wird soft-deleted und die Datei physisch an den Member-Pfad verschoben) und als auditiertes \`MemberDocument\` am Mitglied angelegt — innerhalb derselben atomaren Aktivierungs-Transaktion, via \`audited_create!\` unter \`APPLICATION_SERVICE_PROCESS\`, mit \`DocumentType::Other\` + beschreibender Bezeichnung („Original-Antrag (übernommen bei Bestätigung am DD.MM.YYYY)“).`

2) In `.planning/REQUIREMENTS.md`, remove the bullet under `## Out of Scope (explizite Ausschlüsse)` that reads exactly `- **Move-on-Activation**: Antrags-Dokument wird kopiert, nicht verschoben (Application behält seine Datei).` — this now contradicts APDOC-03. Delete the bullet entirely (leave the other Out-of-Scope bullets intact).

3) In `.planning/ROADMAP.md` Phase 25 Success Criteria #3, replace the current bullet with:

- `Beim \`confirm\` einer \`Application\` wird ein hinterlegtes Antrags-Dokument **übernommen** (Ownership-Übergabe — Move-Semantik: die \`application_documents\`-Zeile wird soft-deleted und die Datei physisch an den Member-Pfad verschoben) und als auditiertes \`MemberDocument\` am Mitglied angelegt — innerhalb derselben atomaren Aktivierungs-Transaktion, via \`audited_create!\` unter \`APPLICATION_SERVICE_PROCESS\` mit \`DocumentType::Other\` + beschreibender Bezeichnung („Original-Antrag (übernommen bei Bestätigung am DD.MM.YYYY)“). (APDOC-03)`

Do not touch the `> **Audit-Hinweis (Phase 25):**` block or the other four success criteria in ROADMAP.md. Do not touch REQUIREMENTS.md `## Traceability` table (APDOC-03 stays "pending"). Per D-01/D-02 (context): use Edit tool, NEVER Write, on these files; multiple small Edit calls if necessary.

Rationale reference in your commit body: cite CONTEXT.md decision #3 and 25-RESEARCH.md section "Runtime Requirements Wording Update (APDOC-03)".
  </action>
  <acceptance_criteria>
    - `grep -n "übernommen" .planning/REQUIREMENTS.md` returns the new APDOC-03 line.
    - `grep -c "kopiert (nicht verschoben)" .planning/REQUIREMENTS.md` returns `0`.
    - `grep -c "kopiert (nicht verschoben)" .planning/ROADMAP.md` returns `0`.
    - `grep -c "Move-on-Activation" .planning/REQUIREMENTS.md` returns `0`.
    - `grep -c "Ownership-Übergabe" .planning/REQUIREMENTS.md` returns at least `1`.
    - `grep -c "Ownership-Übergabe" .planning/ROADMAP.md` returns at least `1`.
    - `grep -c "Original-Antrag (übernommen bei Bestätigung am DD.MM.YYYY)" .planning/REQUIREMENTS.md` returns at least `1`.
    - `grep -c "Original-Antrag (übernommen bei Bestätigung am DD.MM.YYYY)" .planning/ROADMAP.md` returns at least `1`.
    - REQUIREMENTS.md `## Traceability` table still lists APDOC-03 as `Phase 25 | pending` (no accidental status flip).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; grep -c "kopiert (nicht verschoben)" .planning/REQUIREMENTS.md | grep -qx 0 &amp;&amp; grep -c "kopiert (nicht verschoben)" .planning/ROADMAP.md | grep -qx 0 &amp;&amp; grep -c "Move-on-Activation" .planning/REQUIREMENTS.md | grep -qx 0 &amp;&amp; grep -q "Ownership-Übergabe" .planning/REQUIREMENTS.md &amp;&amp; grep -q "Ownership-Übergabe" .planning/ROADMAP.md &amp;&amp; grep -q "Original-Antrag (übernommen bei Bestätigung am DD.MM.YYYY)" .planning/REQUIREMENTS.md &amp;&amp; grep -q "Original-Antrag (übernommen bei Bestätigung am DD.MM.YYYY)" .planning/ROADMAP.md &amp;&amp; grep -q "APDOC-03 | Phase 25 | pending" .planning/REQUIREMENTS.md'</automated>
  </verify>
  <done>REQUIREMENTS.md and ROADMAP.md agree on Move / Ownership-Transfer semantics; no residual "kopiert (nicht verschoben)" wording; Out-of-Scope contradiction removed; description format spelled out identically in both files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary — this plan is documentation-only. No code, no runtime effect.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01-01 | Repudiation | REQUIREMENTS.md APDOC-03 wording | low | mitigate | Cite CONTEXT.md decision #3 and 25-RESEARCH.md "Runtime Requirements Wording Update" in the jj commit message so the historical decision trail stays reconstructable. |
| T-25-01-SC | Tampering | package installs | low | accept | No packages installed by this plan (docs-only). Full package legitimacy handled in later plans that touch build config (none in this phase — no new crates per RESEARCH.md `## Package Legitimacy Audit`). |
</threat_model>

<verification>
```bash
# Consistency check across both docs
grep -c "kopiert (nicht verschoben)" .planning/REQUIREMENTS.md .planning/ROADMAP.md
grep -c "Ownership-Übergabe" .planning/REQUIREMENTS.md .planning/ROADMAP.md
grep -c "Original-Antrag (übernommen bei Bestätigung am DD.MM.YYYY)" .planning/REQUIREMENTS.md .planning/ROADMAP.md
```
</verification>

<success_criteria>
- REQUIREMENTS.md APDOC-03 line describes Move / Ownership-Übergabe with the exact description format.
- ROADMAP.md Phase 25 Success Criteria #3 says the same thing verbatim on the move-semantics claim.
- REQUIREMENTS.md "Out of Scope" bullet contradicting Move semantics is removed.
- No accidental status changes in REQUIREMENTS.md `## Traceability` table.
</success_criteria>

<output>
Create `.planning/phases/25-application-file-upload-audited-carryover/25-01-SUMMARY.md` when done. Commit both file changes with jj using message `docs(25-01): sync APDOC-03 wording to Move semantics (per CONTEXT.md decision #3)`.
</output>
