---
phase: 03-attendance-aggregat-cascade-invalidation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - genossi_dao/src/helper_token.rs
  - genossi_dao_impl_sqlite/src/helper_token.rs
autonomous: true
requirements: [ATTN-06]
tags: [sqlite, dao, helper-token, cascade-discovery]

threat_model:
  trust_boundaries:
    - "DAO-Layer ist innerhalb der App vertrauenswürdig — neue Method wird in Plan 05 vom AssemblyServiceImpl::close_assembly aufgerufen."
  threats:
    - id: "T-03-02-01"
      category: "Information Disclosure"
      component: "HelperTokenDao::list_session_ids_for_assembly (SELECT-Query)"
      disposition: "mitigate"
      mitigation: "Method exponiert ausschließlich session_id-Strings (keine Member-PII). Aufrufer (Plan 05) verwendet die IDs nur für PermissionDao::delete_session, nie als Response-Payload. WHERE-Filter auf assembly_id (parameterisiert via .bind) verhindert Cross-Assembly-Listing."
    - id: "T-03-02-02"
      category: "Tampering"
      component: "SQLx-Bind"
      disposition: "mitigate"
      mitigation: "assembly_id wird als Vec<u8> via .bind übergeben (parameterisiert) — keine String-Konkatenation. UUID-Validierung auf Caller-Seite in Plan 05."
    - id: "T-03-02-03"
      category: "Denial of Service"
      component: "Cascade-Loop bei vielen Helper-Tokens"
      disposition: "accept"
      mitigation: "GVs haben realistisch 5-20 Helfer. Die Method liefert Vec<Arc<str>> ohne Pagination — bei extremem Edge-Case (>1000 Tokens) wäre der Cascade-Loop in Plan 05 spürbar, aber hier nicht praxisrelevant. Out-of-scope für Phase 3 (Generalprobe in Phase 5 evaluiert)."

must_haves:
  truths:
    - "D-12: HelperTokenDao::list_session_ids_for_assembly liefert für eine assembly_id alle session_id-Werte aus helper_token, gefiltert auf session_id IS NOT NULL AND deleted IS NULL — Cascade-Discovery via session_id-FK aus Phase 2"
    - "Method liefert Vec<Arc<str>> (geordnet nach Insertion ist nicht zwingend, aber implementierungsabhängig stabil)"
    - "Method liefert leeres Vec für assembly_id ohne Helper-Tokens oder mit nur revoked/never-redeemed Tokens"
    - "MockHelperTokenDao (auto-generiert via #[automock]) bekommt automatisch eine expect_list_session_ids_for_assembly()-Builder-Method"
    - "D-12: PermissionDao bekommt KEINE helper-spezifische Method (Schicht-Trennung sauber); Discovery liegt im HelperTokenDao, Cleanup im PermissionDao"
  artifacts:
    - path: "genossi_dao/src/helper_token.rs"
      provides: "Trait-Erweiterung um list_session_ids_for_assembly"
      contains: "fn list_session_ids_for_assembly"
    - path: "genossi_dao_impl_sqlite/src/helper_token.rs"
      provides: "SQLx-Impl von list_session_ids_for_assembly + Modul-Test"
      contains: "list_session_ids_for_assembly"
  key_links:
    - from: "AssemblyServiceImpl::close_assembly (Plan 05)"
      to: "HelperTokenDao::list_session_ids_for_assembly"
      via: "Cascade-Discovery vor PermissionDao::delete_session-Loop"
      pattern: "list_session_ids_for_assembly"
---

<objective>
Plan 02 erweitert die bestehende `HelperTokenDao` um eine einzige neue Method `list_session_ids_for_assembly`, die Plan 05 (AssemblyServiceImpl::close_assembly Cascade-Erweiterung) als Discovery-Schritt verwendet. Schließt damit die Cascade-Anker-Lücke zwischen Phase 2 (helper_token.session_id-Spalte existiert) und Phase 3 (Cascade-Invalidation in close_assembly) auf DAO-Ebene.

Implementiert D-12: Cascade-Discovery via `helper_token.session_id` — neue DAO-Method auf HelperTokenDao (NICHT auf PermissionDao), nutzt direkt den `session_id`-FK, der in Phase 2 (D-01) eingeführt wurde.

Purpose: Phase 2 hat den `session_id`-FK auf helper_token (D-01) genau für diesen Use-Case eingeführt. Phase 3 holt die Daten ab. Trennung in eigenen Plan, weil (a) andere Phase-3-Plans nichts davon brauchen außer Plan 05 und (b) der Plan damit klein bleibt (~10 % context).
Output: Eine neue Trait-Method auf HelperTokenDao + ihre SQLite-Impl + ein Modul-Test gegen sqlite::memory:.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-attendance-aggregat-cascade-invalidation/03-CONTEXT.md
@.planning/phases/03-attendance-aggregat-cascade-invalidation/03-RESEARCH.md
@.planning/phases/03-attendance-aggregat-cascade-invalidation/03-PATTERNS.md
@.planning/phases/02-helfer-token-session-authcontext-helper/02-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Bestehender Trait — Vorbild für die Erweiterung: -->
From genossi_dao/src/helper_token.rs (existiert seit Phase 2):
```rust
#[automock(type Transaction = crate::MockTransaction;)]
#[async_trait]
pub trait HelperTokenDao {
    type Transaction: crate::Transaction;
    async fn dump_all(&self, tx: Self::Transaction) -> Result<Arc<[HelperTokenEntity]>, DaoError>;
    async fn create(&self, entity: &HelperTokenEntity, process: &str, tx: Self::Transaction) -> Result<(), DaoError>;
    async fn update(&self, entity: &HelperTokenEntity, process: &str, tx: Self::Transaction) -> Result<(), DaoError>;
    async fn atomic_redeem(&self, token_hash: &str, used_at: time::PrimitiveDateTime, tx: Self::Transaction) -> Result<Option<(Uuid, Uuid)>, DaoError>;
    async fn set_session_id(&self, token_id: Uuid, session_id: &str, tx: Self::Transaction) -> Result<(), DaoError>;
    async fn lookup_status(&self, token_hash: &str, tx: Self::Transaction) -> Result<Option<(Option<time::PrimitiveDateTime>, Option<time::PrimitiveDateTime>)>, DaoError>;
    async fn all_for_assembly(&self, assembly_id: Uuid, tx: Self::Transaction) -> Result<Arc<[HelperTokenEntity]>, DaoError>;
    // PHASE 3: hierhin kommt list_session_ids_for_assembly
}
```

<!-- Vorbild für die SQLx-Impl (gleiche Datei): -->
From genossi_dao_impl_sqlite/src/helper_token.rs::all_for_assembly (Phase 2):
```rust
async fn all_for_assembly(
    &self,
    assembly_id: Uuid,
    tx: Self::Transaction,
) -> Result<Arc<[HelperTokenEntity]>, DaoError> {
    let aid = assembly_id.as_bytes().to_vec();
    let rows = sqlx::query_as::<_, HelperTokenDb>(
        "SELECT id, assembly_id, memo, ... FROM helper_token \
         WHERE assembly_id = ? AND deleted IS NULL ORDER BY created DESC",
    )
    .bind(aid)
    .fetch_all(tx.tx.lock().await.as_mut())
    .await
    .map_err(|e| DaoError::DatabaseError(Arc::from(e.to_string())))?;
    // ...
}
```

<!-- helper_token-Schema (Phase 2): -->
- Tabelle: helper_token
- Relevante Spalten für diese Method: assembly_id (BLOB), session_id (TEXT, NULLABLE — wird beim Redeem gesetzt), deleted (TEXT, NULLABLE — Soft-Delete)
- Migration: migrations/sqlite/20260503000000_create_helper_token_table.sql
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HelperTokenDao Trait + SQLite-Impl Erweiterung</name>
  <files>
    genossi_dao/src/helper_token.rs,
    genossi_dao_impl_sqlite/src/helper_token.rs
  </files>
  <read_first>
    - genossi_dao/src/helper_token.rs (komplette Datei — Trait-Definition mit #[automock]; Stelle, an der die neue Method eingefügt wird, nach `all_for_assembly`; Method-Doc-Comment-Stil als Vorbild)
    - genossi_dao_impl_sqlite/src/helper_token.rs (komplette Datei — Impl-Block, insbesondere `all_for_assembly` lines 290-307 als Vorbild für den neuen Body; Tests am Ende der Datei als Setup-Vorbild)
    - migrations/sqlite/20260503000000_create_helper_token_table.sql (Schema von helper_token, insbesondere session_id-Spalte und FK)
    - .planning/phases/03-attendance-aggregat-cascade-invalidation/03-CONTEXT.md (D-12)
    - .planning/phases/03-attendance-aggregat-cascade-invalidation/03-RESEARCH.md §Konkrete Code-Recommendations lines 1019-1054 (verbatim Trait + Impl)
    - .planning/phases/03-attendance-aggregat-cascade-invalidation/03-PATTERNS.md §"genossi_dao/src/helper_token.rs (MODIFY)" lines 531-596 (Pitfall 4 zu mock-Erweiterung)
  </read_first>
  <behavior>
    - Test 1 `test_list_session_ids_for_assembly_returns_redeemed_only`: Setup 1 Assembly + 3 Helper-Tokens — Token A redeemed mit session_id="sess-A", Token B noch offen (session_id=NULL), Token C revoked (deleted=NOT NULL); rufe `list_session_ids_for_assembly(aid, tx)` → Vec mit genau 1 Element ["sess-A"].
    - Test 2 `test_list_session_ids_for_assembly_empty_for_unknown_assembly`: Random-UUID ohne Tokens → leeres Vec.
    - Test 3 `test_list_session_ids_for_assembly_excludes_other_assemblies`: 2 Assemblies mit je 1 redeemed Token; Method liefert nur die session_id der angefragten Assembly.
  </behavior>
  <action>
**Schritt 1 — Trait-Erweiterung in `genossi_dao/src/helper_token.rs`:** Implementiert D-12 (Cascade-Discovery via session_id-FK).

Füge die neue Method **nach** `all_for_assembly` (vor dem schließenden `}` des Trait-Blocks) ein. Verbatim (RESEARCH lines 1023-1033):

```rust
    /// D-12: Cascade-Discovery for AssemblyServiceImpl::close_assembly (Phase 3).
    /// Returns all currently-bound helper-session ids for the given assembly.
    /// Filters out null session_ids (revoked or never-redeemed tokens) and
    /// soft-deleted token rows. Order is implementation-defined but stable
    /// within a single SQLite snapshot.
    async fn list_session_ids_for_assembly(
        &self,
        assembly_id: Uuid,
        tx: Self::Transaction,
    ) -> Result<Vec<Arc<str>>, DaoError>;
```

**Schritt 2 — SQLx-Impl in `genossi_dao_impl_sqlite/src/helper_token.rs`:**

Füge **nach** `all_for_assembly` (im `impl HelperTokenDao for HelperTokenDaoImpl`-Block) ein. Verbatim (RESEARCH lines 1037-1054):

```rust
async fn list_session_ids_for_assembly(
    &self,
    assembly_id: Uuid,
    tx: Self::Transaction,
) -> Result<Vec<Arc<str>>, DaoError> {
    let aid = assembly_id.as_bytes().to_vec();
    let rows: Vec<String> = sqlx::query_scalar(
        "SELECT session_id FROM helper_token \
         WHERE assembly_id = ? AND session_id IS NOT NULL AND deleted IS NULL",
    )
    .bind(aid)
    .fetch_all(tx.tx.lock().await.as_mut())
    .await
    .map_err(|e| DaoError::DatabaseError(Arc::from(e.to_string())))?;
    Ok(rows.into_iter().map(|s| Arc::from(s.as_str())).collect())
}
```

**Schritt 3 — 3 neue Tests** im bestehenden Test-Modul am Ende von `genossi_dao_impl_sqlite/src/helper_token.rs`. Reuse das bestehende `setup_pool()`-Helper-Pattern aus dieser Datei. Falls bestehende Setup-Helpers eine Assembly anlegen, nachnutzen; sonst Setup-Code aus den Phase-2-Tests in dieser Datei kopieren.

Test-Skelett für Test 1:
```rust
#[tokio::test]
async fn test_list_session_ids_for_assembly_returns_redeemed_only() {
    let pool = setup_pool().await;
    let dao = HelperTokenDaoImpl::new(pool.clone());
    let tx = ... // begin_transaction analog zu existing tests
    let assembly_id = ... // INSERT assembly
    // Setup: 3 Tokens
    // Token A: redeemed
    insert_helper_token(&pool, assembly_id, "tok-a-hash", Some("sess-A"), None /* not deleted */).await;
    // Token B: open
    insert_helper_token(&pool, assembly_id, "tok-b-hash", None, None).await;
    // Token C: revoked
    let now_str = format_dt_iso(now());
    insert_helper_token(&pool, assembly_id, "tok-c-hash", Some("sess-C"), Some(&now_str)).await;

    let result = dao.list_session_ids_for_assembly(assembly_id, tx).await.unwrap();
    assert_eq!(result.len(), 1);
    assert_eq!(result[0].as_ref(), "sess-A");
}
```

(Falls `insert_helper_token`-Helper nicht existiert, schreibe ihn als sqlx::query("INSERT INTO helper_token ...")-Wrapper oder reuse `dao.create(...)` für Token A/B + direktes UPDATE für deleted.)

**Was NICHT machen:**
- KEIN `MockHelperTokenDao::expect_*`-Code in dieser Datei modifizieren — `#[automock]` regeneriert das automatisch (Pitfall 4 in RESEARCH §1299-1306).
- KEIN Touch der handgeschriebenen `mock! { TestHelperTokenDao { ... } }` in `genossi_service_impl/src/assembly.rs::tests` — das ist Plan 05's Aufgabe (nach der Trait-Erweiterung würde Plan 05 dort die neue Method ergänzen).
- KEINE Änderungen am helper_token-Schema (Phase 2 hat session_id schon angelegt).
- KEINE `audited_*!`-Macros (Listing ist read-only).
  </action>
  <verify>
    <automated>cargo build -p genossi_dao -p genossi_dao_impl_sqlite 2>&amp;1 | tail -10 &amp;&amp; cargo test -p genossi_dao_impl_sqlite list_session_ids_for_assembly --no-fail-fast -- --nocapture 2>&amp;1 | tail -40</automated>
  </verify>
  <done>
    - `cargo build -p genossi_dao -p genossi_dao_impl_sqlite` exits 0.
    - `cargo test -p genossi_dao_impl_sqlite list_session_ids_for_assembly` exits 0; mindestens 3 Tests laufen.
    - `grep -c 'fn list_session_ids_for_assembly' genossi_dao/src/helper_token.rs` = 1.
    - `grep -c 'fn list_session_ids_for_assembly' genossi_dao_impl_sqlite/src/helper_token.rs` = 1.
    - `grep -c 'SELECT session_id FROM helper_token' genossi_dao_impl_sqlite/src/helper_token.rs` = 1.
    - `grep -c 'WHERE assembly_id = ? AND session_id IS NOT NULL AND deleted IS NULL' genossi_dao_impl_sqlite/src/helper_token.rs` = 1.
    - `cargo test -p genossi_service_impl assembly` (Phase 2 close_assembly tests) exits 0 — Pitfall 4: Trait-Erweiterung darf bestehende Phase-2-Tests **nicht** brechen.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service → DAO | AssemblyServiceImpl::close_assembly (Plan 05) ruft list_session_ids_for_assembly mit einer bereits via Status-Check geprüften assembly_id auf. |

## STRIDE Threat Register

Siehe Frontmatter `threat_model:` — drei Threats (T-03-02-01 bis T-03-02-03):
- **T-03-02-01** Information Disclosure: Method exponiert nur session_id-Strings (keine PII), Caller verwendet sie ausschließlich für DELETE.
- **T-03-02-02** SQL Injection: parameterisierte Bind-Args.
- **T-03-02-03** DoS: bei realistischen GVs (5-20 Helfer) trivial; out-of-scope für Phase 3.
</threat_model>

<verification>
- `cargo build` exits 0.
- `cargo test -p genossi_dao_impl_sqlite list_session_ids_for_assembly` exits 0.
- Bestehende Phase-2-Tests `cargo test -p genossi_service_impl assembly close_assembly` und `cargo test -p genossi_dao_impl_sqlite helper_token` bleiben grün (Pitfall 4 verifiziert).
- `cargo fmt -- --check` exits 0.
- `cargo clippy -p genossi_dao -p genossi_dao_impl_sqlite --no-deps -- -D warnings` exits 0.
</verification>

<success_criteria>
- HelperTokenDao::list_session_ids_for_assembly als Trait-Method existiert und ist via #[automock] mockbar.
- SQLite-Impl liefert genau die session_id-Werte aus helper_token mit Filter `assembly_id = ? AND session_id IS NOT NULL AND deleted IS NULL`.
- 3 grüne Modul-Tests verifizieren das Verhalten gegen eine echte sqlite::memory:.
- Bereit für Konsumption in Plan 05 (AssemblyServiceImpl::close_assembly Cascade-Loop).
</success_criteria>

<output>
Nach Abschluss: erstelle `.planning/phases/03-attendance-aggregat-cascade-invalidation/03-02-SUMMARY.md` mit:
- Liste der modifizierten Files
- Trait-Method-Signatur (verbatim)
- SQL-Statement (verbatim)
- Test-Liste + Status (alle grün)
- Hinweis auf Plan-05-Konsument (close_assembly Cascade-Loop)
- Pitfall-4-Note: handgeschriebene `TestHelperTokenDao` mock_set in `genossi_service_impl/src/assembly.rs::tests` muss in Plan 05 erweitert werden — nicht in diesem Plan.
</output>
</output>
