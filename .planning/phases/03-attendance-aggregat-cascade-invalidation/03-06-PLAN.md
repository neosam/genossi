---
phase: 03-attendance-aggregat-cascade-invalidation
plan: 06
type: execute
wave: 4
depends_on: ["03-05"]
files_modified:
  - genossi_rest/src/attendance.rs
  - genossi_rest/src/lib.rs
  - genossi_bin/src/lib.rs
  - genossi_bin/tests/e2e_tests.rs
autonomous: true
requirements: [ASSY-04, ASSY-06, ATTN-01, ATTN-02, ATTN-03, ATTN-04, ATTN-05, ATTN-06, SYNC-02]
tags: [rest, axum, di-wiring, e2e, cascade, pii-test, race-test, hash-chain]

threat_model:
  trust_boundaries:
    - "HTTP → Axum-Router: TLS-Termination-Ebene (Production via Caddy/Nginx) — out-of-scope für Phase 3, wird in Phase 5 dokumentiert."
    - "Axum-Middleware → Handler: context_extractor (Phase 2) füllt Extension<Context>; Handler ruft extract_auth_context für Konversion zu Authentication<Context>."
    - "Handler → AttendanceService: Plan 05 macht alle Authorization-Checks; Plan 06 mappt nur HTTP-Status-Codes."
  threats:
    - id: "T-03-06-01"
      category: "Information Disclosure"
      component: "GET /api/attendance/{aid}/members JSON-Response"
      disposition: "mitigate"
      mitigation: "AttendanceMemberTO ist 7-Feld-Whitelist (Plan 04). E2E-Test test_attendance_members_response_has_no_pii_fields iteriert die JSON-Keys gegen Whitelist+Blacklist (Pitfall 6 in RESEARCH §1351-1383). Bei jeder zukünftigen MemberEntity-Erweiterung MUSS dieser Test fehlschlagen, wenn ein neues Feld versehentlich propagiert wird."
    - id: "T-03-06-02"
      category: "Elevation of Privilege"
      component: "REST-Handler error mapping (D-26)"
      disposition: "mitigate"
      mitigation: "Lokales map_attendance_error mappt ServiceError::PermissionDenied → RestError::Forbidden(403) (anstelle des globalen 401-Mappings). Das verhindert dass Helper-cross-assembly-Versuche fälschlich als Auth-Fehler (401: 'login again') statt Permission-Fehler (403: 'no access') bei den Clients landen — wichtig für die Frontend-Diskrimination in Phase 4 (kein automatisches Re-Login bei 403)."
    - id: "T-03-06-03"
      category: "Tampering"
      component: "PUT/DELETE Idempotenz-Garantie"
      disposition: "mitigate"
      mitigation: "ATTN-03/04 Idempotenz wird in Plan 01 + 05 garantiert (UPSERT + Soft-Delete-Flip). E2E-Race-Test test_attendance_upsert_race_one_row_two_200ok verifiziert dass zwei parallele PUTs auf dieselbe (aid, mid) genau einen Eintrag erzeugen (SYNC-02)."
    - id: "T-03-06-04"
      category: "Repudiation"
      component: "Audit-Hash-Chain-Stability"
      disposition: "mitigate"
      mitigation: "Test test_attendance_toggle_burst_does_not_pollute_audit_chain verifiziert dass 100 Toggles weder Audit-Einträge erzeugen (entries.len unverändert vor/nach Burst) noch die Hash-Chain brechen (/api/audit/verify liefert valid=true). Das schützt gegen ATTN-05-Verletzung durch versehentlich eingebaute audited_*!-Macros."
    - id: "T-03-06-05"
      category: "Denial of Service"
      component: "Stats-Polling-Endpoint"
      disposition: "accept"
      mitigation: "Bestehender api_rate_layer (60 req/min/IP) deckt Stats-Endpoint ab. Phase 4 polled ~5s = 12 req/min/IP — weit unter 60. Per-IP-Rate-Limit speziell für Stats wird in Phase 5 (Generalprobe) re-evaluiert (Discretion 6 in RESEARCH)."
    - id: "T-03-06-06"
      category: "Tampering"
      component: "Cascade-Test setup_with_pool"
      disposition: "accept"
      mitigation: "Test exposed direkt das SqlitePool-Handle. Out-of-scope-Risiko für Production (nur in Tests verwendet)."

must_haves:
  truths:
    - "D-21: POST/PUT/DELETE/GET-Handler für /api/attendance/{aid}/members?q=, /api/attendance/{aid}/{mid} (PUT+DELETE), /api/assembly/{aid}/stats existieren — Endpoints REST-konform getrennt nach Verb"
    - "D-26: Handler nutzen lokales map_attendance_error für PermissionDenied → 403 Forbidden (RESEARCH-Conflict-1-Resolution: differential mapping verhindert globales 401, das Phase 1/2 brechen würde)"
    - "AttendanceServiceImpl wird in genossi_bin/src/lib.rs::RestStateImpl::new() instanziiert mit 6 Deps"
    - "AssemblyServiceImpl-Konstruktor in genossi_bin/src/lib.rs erweitert um 2 neue Deps (helper_token_dao, permission_dao)"
    - "AttendanceRestState-Trait in RestStateImpl implementiert"
    - "D-21: Router nest(\"/api/attendance/{assembly_id}\", attendance::generate_attendance_route()) registriert"
    - "D-21: Router-Eintrag für /api/assembly/{assembly_id}/stats existiert (semantisch ein Assembly-Aspekt, Implementation in AttendanceService per D-23)"
    - "OpenApi-Doc registriert AttendanceMemberTO + AttendanceStatsTO Schemas"
    - "D-28: REST-Handler-Identifier (list_attendance_members, mark_attendance_present, mark_attendance_absent, get_assembly_stats, AttendanceRestState, generate_attendance_route, generate_stats_route, ApiDoc) und URL-Pfade (/api/attendance, /api/assembly/{aid}/stats, /members) durchgängig englisch — konsistent mit Phase 1/2"
    - "6 E2E-Tests: SYNC-02-Race, SC#8-Cascade, ATTN-01-PII-Leak, ATTN-05-Hash-Chain, ASSY-06-Post-Close-Edit, ATTN-02-Substring-Search — alle grün"
  artifacts:
    - path: "genossi_rest/src/attendance.rs"
      provides: "4 Handler + AttendanceRestState-Trait + generate_route + ApiDoc + map_attendance_error"
      exports: ["AttendanceRestState", "generate_route", "ApiDoc"]
    - path: "genossi_rest/src/lib.rs"
      provides: "Router-Nest + ApiDoc-Registration"
      contains: "attendance::generate_route"
    - path: "genossi_bin/src/lib.rs"
      provides: "AttendanceServiceImpl-Wiring + AssemblyServiceImpl-Erweiterung um 2 Deps + AttendanceRestState-Impl"
      contains: "AttendanceServiceImpl"
    - path: "genossi_bin/tests/e2e_tests.rs"
      provides: "5 neue E2E-Tests"
      exports: ["test_attendance_upsert_race_one_row_two_200ok", "test_close_assembly_cascade_invalidates_helper_sessions", "test_attendance_members_response_has_no_pii_fields", "test_attendance_toggle_burst_does_not_pollute_audit_chain", "test_vorstand_can_edit_attendance_after_close", "test_attendance_members_substring_search_filters_by_query_param"]
  key_links:
    - from: "genossi_rest/src/lib.rs Router"
      to: "attendance::generate_route"
      via: ".nest(\"/api/attendance/{assembly_id}\", attendance::generate_route())"
      pattern: "nest.*api/attendance.*attendance::generate_route"
    - from: "RestStateImpl"
      to: "AttendanceServiceImpl"
      via: "AttendanceRestState-Trait-Impl"
      pattern: "impl genossi_rest::attendance::AttendanceRestState"
---

<objective>
Plan 06 schließt Phase 3 ab: REST-Schicht (4 Handler), Router-Registration, OpenAPI-Doc, DI-Wiring im genossi_bin (sowohl neue AttendanceServiceImpl als auch erweiterte AssemblyServiceImpl), und 6 End-to-End-Tests die alle 9 Phase-Requirements an einem real-laufenden HTTP-Server verifizieren.

Purpose: Plan 06 ist der Integrationspunkt — er bringt die in Plan 01-05 isoliert getesteten Bausteine zusammen und beweist via E2E dass sie als komplettes System die Requirements (PII-Restriktion, idempotenter PUT, Cascade-Invalidation, Vorstand-Post-Close-Edit, Audit-Stille) erfüllen.
Output: Eine neue REST-Datei + 4 modifizierte Files + 6 grüne E2E-Tests.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-attendance-aggregat-cascade-invalidation/03-CONTEXT.md
@.planning/phases/03-attendance-aggregat-cascade-invalidation/03-RESEARCH.md
@.planning/phases/03-attendance-aggregat-cascade-invalidation/03-PATTERNS.md
@.planning/phases/03-attendance-aggregat-cascade-invalidation/03-01-SUMMARY.md
@.planning/phases/03-attendance-aggregat-cascade-invalidation/03-02-SUMMARY.md
@.planning/phases/03-attendance-aggregat-cascade-invalidation/03-03-SUMMARY.md
@.planning/phases/03-attendance-aggregat-cascade-invalidation/03-04-SUMMARY.md
@.planning/phases/03-attendance-aggregat-cascade-invalidation/03-05-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- REST-Handler-Pattern (Phase 1 Vorbild): -->
From genossi_rest/src/assembly.rs:
```rust
pub trait AssemblyRestState: Clone + Send + Sync + 'static {
    type AssemblyService: AssemblyService<Context = crate::ContextType> + Send + Sync + 'static;
    fn assembly_service(&self) -> Arc<Self::AssemblyService>;
}

#[instrument(skip(rest_state))]
#[utoipa::path(post, tag = "Assemblies", path = "/{id}/close", ...)]
pub async fn close_assembly<RestState: RestStateDef + AssemblyRestState>(
    rest_state: State<RestState>,
    Extension(context): Extension<Context>,
    Path(id): Path<Uuid>,
) -> Response {
    error_handler(
        (async {
            let auth = crate::extract_auth_context(Some(context))?;
            let assembly = rest_state.assembly_service().close_assembly(id, auth).await?;
            Ok(Response::builder().status(200).body(...))
        }).await,
    )
}

pub fn generate_route<RestState: RestStateDef + AssemblyRestState>() -> Router<RestState> {
    Router::new()
        .route("/{id}/close", post(close_assembly::<RestState>))
}

#[derive(OpenApi)]
#[openapi(paths(...), components(schemas(...)))]
pub struct ApiDoc;
```

<!-- RestError-Variants aus genossi_rest/src/lib.rs:75-95: -->
pub enum RestError {
    NotFound, BadRequest(String), Conflict(String), Unauthorized,
    UnsupportedMediaType(String), InternalError(String),
    Forbidden(String),  // exists, used by helper-redeem
    Gone(String),
}

<!-- Differential-Mapping-Pattern aus genossi_rest/src/helper_token.rs:287-313 (Vorbild für map_attendance_error): -->
let result = ...await;
let success = match result {
    Ok(s) => s,
    Err(ServiceError::Conflict(payload)) => {
        let p = payload.as_ref();
        if p == "already_used" { return Err(RestError::Gone(...)); }
        ...
    }
    Err(other) => return Err(other.into()),
};

<!-- extract_auth_context aus genossi_rest/src/lib.rs:50-74: -->
pub fn extract_auth_context(context: Option<Context>) -> Result<Authentication<Context>, RestError>;

<!-- RestStateImpl pattern in genossi_bin/src/lib.rs (Phase 1+2): -->
RestStateImpl { assembly_service: Arc<AssemblyService>, helper_token_service: Arc<HelperTokenService>, ... }
impl genossi_rest::assembly::AssemblyRestState for RestStateImpl {
    type AssemblyService = AssemblyService;
    fn assembly_service(&self) -> Arc<Self::AssemblyService> { self.assembly_service.clone() }
}

<!-- E2E-Pattern aus genossi_bin/tests/e2e_tests.rs (Phase 1+2): -->
- setup() / setup_with_pool() für TestServer-Erzeugung
- create_open_assembly_for_helper_test, create_helper_token_for_test als wiederverwendbare Helpers
- HLPR-04 Race-Test mit tokio::join!
- HLPR-07 Audit-Verify-Test mit /api/audit/verify und entity_type-Filter
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: REST-Handler + Router + ApiDoc</name>
  <files>
    genossi_rest/src/attendance.rs,
    genossi_rest/src/lib.rs
  </files>
  <read_first>
    - genossi_rest/src/assembly.rs (komplette Datei — Handler-Pattern + RestState-Trait + generate_route + ApiDoc als Vorbild)
    - genossi_rest/src/helper_token.rs (lines 280-348 — Differential-Error-Mapping als Vorbild für map_attendance_error)
    - genossi_rest/src/lib.rs (Router-Bauplan, insbesondere Lines 266-269 für ApiDoc-nest und 549-560 für Router-nest; lines 75-111 für RestError + From<ServiceError>; lines 50-74 für extract_auth_context)
    - genossi_rest_types/src/lib.rs (Plan 04 — AttendanceMemberTO, AttendanceStatsTO)
    - genossi_service/src/attendance.rs (Plan 04 — AttendanceService-Trait)
    - .planning/phases/03-attendance-aggregat-cascade-invalidation/03-CONTEXT.md (D-21, D-26)
    - .planning/phases/03-attendance-aggregat-cascade-invalidation/03-RESEARCH.md §Pattern 7 (Handler) + §DECISION CONFLICT 1 (lines 521-546 — map_attendance_error)
    - .planning/phases/03-attendance-aggregat-cascade-invalidation/03-PATTERNS.md §"genossi_rest/src/attendance.rs" (lines 381-498)
  </read_first>
  <behavior>
    - 4 Handler-Funktionen kompilieren ohne Fehler.
    - Router-Tests: kein eigener Modul-Test in dieser Aufgabe; das Verhalten verifiziert sich in Task 3 (E2E-Tests).
    - utoipa-ApiDoc-Generation läuft (verify durch `cargo build` — wenn ToSchema fehlt, bricht der Build).
  </behavior>
  <action>
**Schritt 1 — Datei `genossi_rest/src/attendance.rs` anlegen:**

```rust
use std::sync::Arc;

use axum::{
    body::Body,
    extract::{Path, Query, State},
    response::Response,
    routing::{delete, get, put},
    Extension, Json, Router,
};
use genossi_rest_types::{AttendanceMemberTO, AttendanceStatsTO};
use genossi_service::attendance::AttendanceService;
use genossi_service::ServiceError;
use serde::Deserialize;
use tracing::instrument;
use utoipa::{IntoParams, OpenApi, ToSchema};
use uuid::Uuid;

use crate::{error_handler, Context, RestError, RestStateDef};

pub trait AttendanceRestState: Clone + Send + Sync + 'static {
    type AttendanceService: AttendanceService<Context = crate::ContextType> + Send + Sync + 'static;
    fn attendance_service(&self) -> Arc<Self::AttendanceService>;
}

/// CONFLICT 1 (RESEARCH.md §DECISION CONFLICTS): differential mapping —
/// PermissionDenied → 403 Forbidden for attendance endpoints (D-26).
/// All other ServiceError-variants delegate to the global From<ServiceError>
/// in genossi_rest/src/lib.rs:95-111, so 401 stays the default for non-attendance
/// endpoints (Phase 1+2 endpoints unchanged).
fn map_attendance_error(e: ServiceError) -> RestError {
    match e {
        ServiceError::PermissionDenied => RestError::Forbidden("forbidden".to_string()),
        other => other.into(),
    }
}

#[derive(Debug, Deserialize, IntoParams)]
pub struct ListMembersQuery {
    /// Substring filter on name or member number (ATTN-02).
    pub q: Option<String>,
}

#[instrument(skip(rest_state))]
#[utoipa::path(
    get,
    tag = "Attendance",
    path = "/members",
    params(("assembly_id" = Uuid, Path, description = "Assembly ID"), ListMembersQuery),
    responses(
        (status = 200, description = "Reduced member list (ATTN-01, ATTN-06)", body = [AttendanceMemberTO]),
        (status = 401, description = "Unauthorized"),
        (status = 403, description = "Forbidden (helper wrong assembly or not Open / non-admin)"),
        (status = 404, description = "Assembly not found"),
    ),
)]
pub async fn list_attendance_members<RestState: RestStateDef + AttendanceRestState>(
    rest_state: State<RestState>,
    Extension(context): Extension<Context>,
    Path(assembly_id): Path<Uuid>,
    Query(query): Query<ListMembersQuery>,
) -> Response {
    error_handler(
        (async {
            let auth = crate::extract_auth_context(Some(context))?;
            let rows = rest_state
                .attendance_service()
                .list_members(assembly_id, query.q, auth)
                .await
                .map_err(map_attendance_error)?;
            let tos: Vec<AttendanceMemberTO> = rows.iter().map(AttendanceMemberTO::from).collect();
            let body = serde_json::to_string(&tos)
                .map_err(|e| RestError::InternalError(format!("serialize: {}", e)))?;
            Ok(Response::builder()
                .status(200)
                .header("content-type", "application/json")
                .body(Body::from(body))
                .unwrap())
        })
        .await,
    )
}

#[instrument(skip(rest_state))]
#[utoipa::path(
    put,
    tag = "Attendance",
    path = "/{member_id}",
    params(
        ("assembly_id" = Uuid, Path, description = "Assembly ID"),
        ("member_id" = Uuid, Path, description = "Member ID"),
    ),
    responses(
        (status = 200, description = "Marked present (idempotent — ATTN-03)"),
        (status = 401, description = "Unauthorized"),
        (status = 403, description = "Forbidden"),
        (status = 404, description = "Assembly or member not in snapshot"),
    ),
)]
pub async fn mark_attendance_present<RestState: RestStateDef + AttendanceRestState>(
    rest_state: State<RestState>,
    Extension(context): Extension<Context>,
    Path((assembly_id, member_id)): Path<(Uuid, Uuid)>,
) -> Response {
    error_handler(
        (async {
            let auth = crate::extract_auth_context(Some(context))?;
            rest_state
                .attendance_service()
                .mark_present(assembly_id, member_id, auth)
                .await
                .map_err(map_attendance_error)?;
            Ok(Response::builder().status(200).body(Body::empty()).unwrap())
        })
        .await,
    )
}

#[instrument(skip(rest_state))]
#[utoipa::path(
    delete,
    tag = "Attendance",
    path = "/{member_id}",
    params(
        ("assembly_id" = Uuid, Path, description = "Assembly ID"),
        ("member_id" = Uuid, Path, description = "Member ID"),
    ),
    responses(
        (status = 200, description = "Marked absent (idempotent — ATTN-04, returns 200 even on no-op)"),
        (status = 401, description = "Unauthorized"),
        (status = 403, description = "Forbidden"),
        (status = 404, description = "Assembly or member not in snapshot"),
    ),
)]
pub async fn mark_attendance_absent<RestState: RestStateDef + AttendanceRestState>(
    rest_state: State<RestState>,
    Extension(context): Extension<Context>,
    Path((assembly_id, member_id)): Path<(Uuid, Uuid)>,
) -> Response {
    error_handler(
        (async {
            let auth = crate::extract_auth_context(Some(context))?;
            rest_state
                .attendance_service()
                .mark_absent(assembly_id, member_id, auth)
                .await
                .map_err(map_attendance_error)?;
            Ok(Response::builder().status(200).body(Body::empty()).unwrap())
        })
        .await,
    )
}

#[instrument(skip(rest_state))]
#[utoipa::path(
    get,
    tag = "Attendance",
    path = "/stats",  // Mounted under /api/assembly/{assembly_id}/stats — see lib.rs router.
    params(("assembly_id" = Uuid, Path, description = "Assembly ID")),
    responses(
        (status = 200, description = "Live counter (ASSY-04)", body = AttendanceStatsTO),
        (status = 401, description = "Unauthorized"),
        (status = 403, description = "Forbidden"),
        (status = 404, description = "Assembly not found"),
    ),
)]
pub async fn get_assembly_stats<RestState: RestStateDef + AttendanceRestState>(
    rest_state: State<RestState>,
    Extension(context): Extension<Context>,
    Path(assembly_id): Path<Uuid>,
) -> Response {
    error_handler(
        (async {
            let auth = crate::extract_auth_context(Some(context))?;
            let stats = rest_state
                .attendance_service()
                .stats(assembly_id, auth)
                .await
                .map_err(map_attendance_error)?;
            let to = AttendanceStatsTO::from(&stats);
            let body = serde_json::to_string(&to)
                .map_err(|e| RestError::InternalError(format!("serialize: {}", e)))?;
            Ok(Response::builder()
                .status(200)
                .header("content-type", "application/json")
                .body(Body::from(body))
                .unwrap())
        })
        .await,
    )
}

/// Router for `/api/attendance/{assembly_id}/...` routes (D-21).
/// Note: `/api/assembly/{assembly_id}/stats` (D-21) is registered separately
/// in genossi_rest/src/lib.rs because it lives under the assembly namespace.
pub fn generate_attendance_route<RestState: RestStateDef + AttendanceRestState>() -> Router<RestState> {
    Router::new()
        .route("/members", get(list_attendance_members::<RestState>))
        .route("/{member_id}",
            put(mark_attendance_present::<RestState>)
                .delete(mark_attendance_absent::<RestState>),
        )
}

/// Router for `/api/assembly/{assembly_id}/stats` (D-21).
pub fn generate_stats_route<RestState: RestStateDef + AttendanceRestState>() -> Router<RestState> {
    Router::new()
        .route("/stats", get(get_assembly_stats::<RestState>))
}

#[derive(OpenApi)]
#[openapi(
    paths(
        list_attendance_members,
        mark_attendance_present,
        mark_attendance_absent,
        get_assembly_stats,
    ),
    components(schemas(AttendanceMemberTO, AttendanceStatsTO)),
)]
pub struct ApiDoc;
```

**Schritt 2 — `genossi_rest/src/lib.rs` modifizieren:**

**2a — `pub mod attendance;`** unter den anderen `pub mod`-Statements einfügen.

**2b — Router-Erweiterung.** Suche die Stelle wo `/api/assembly` und `/api/helper` bzw. ähnliche `.nest(...)` aufgerufen werden (lines ~549-560 laut RESEARCH/PATTERNS). Füge dort hinzu:

```rust
.nest("/api/attendance/{assembly_id}", attendance::generate_attendance_route())
.nest("/api/assembly/{assembly_id}", attendance::generate_stats_route())
```

**Wichtig:** Die assembly-namespace-route `/api/assembly` ist möglicherweise schon mit `assembly::generate_route()` belegt. Prüfe Order — Axum-merge-Verhalten ist deterministisch, aber falls die Routen kollidieren, mounte stats inline:

```rust
.route("/api/assembly/{assembly_id}/stats", get(attendance::get_assembly_stats::<RestStateImpl>))
```

(Inline funktioniert nur wenn RestStateImpl konkret bekannt ist; sonst als nest beibehalten und prüfen dass beide nests koexistieren.)

**2c — ApiDoc-Registration.** Suche den ApiDoc-Block / OpenApi-Builder und füge die zwei Schemas + den Phase-3-ApiDoc nest hinzu:

```rust
(path = "/api/attendance/{assembly_id}", api = attendance::ApiDoc),
```

(Die genaue Form hängt von der bestehenden lib.rs-Architektur ab — siehe lines 266-269 als Vorbild.)

**Was NICHT machen:**
- KEIN globales From<ServiceError>::PermissionDenied → Forbidden mappen (würde Phase 1/2 Endpoints brechen — Conflict 1 Resolution).
- KEINE Validierung der Body-Inhalte für PUT/DELETE — die haben empty bodies.
- KEIN Re-Implementieren des extract_auth_context (Phase 1+2 hat das).
- KEIN Try-Auth-Middleware-Skip — alle Phase-3-Endpoints sind durch context_extractor (Phase 2 Auth-Middleware-Chain) gesichert.
- KEIN Response-Body für PUT/DELETE-Erfolge — empty body ist korrekt (D-21: 200 OK, body irrelevant).
  </action>
  <verify>
    <automated>cargo build -p genossi_rest 2>&amp;1 | tail -15 &amp;&amp; cargo test -p genossi_rest --no-fail-fast 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    - `cargo build -p genossi_rest` exits 0.
    - `grep -c 'pub mod attendance' genossi_rest/src/lib.rs` = 1.
    - `grep -c 'pub fn generate_attendance_route' genossi_rest/src/attendance.rs` = 1.
    - `grep -c 'pub fn generate_stats_route\|/api/assembly/{assembly_id}/stats' genossi_rest/src/attendance.rs genossi_rest/src/lib.rs` ≥ 1 (stats-route registriert).
    - `grep -c 'fn map_attendance_error' genossi_rest/src/attendance.rs` = 1.
    - `grep -c 'ServiceError::PermissionDenied => RestError::Forbidden' genossi_rest/src/attendance.rs` = 1.
    - `grep -c 'AttendanceRestState' genossi_rest/src/attendance.rs` ≥ 2 (Trait-Def + Handler-Bound).
    - `grep -c '#\[utoipa::path' genossi_rest/src/attendance.rs` = 4 (vier Handler).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DI-Wiring in genossi_bin (RestStateImpl + AssemblyServiceImpl-Erweiterung)</name>
  <files>
    genossi_bin/src/lib.rs
  </files>
  <read_first>
    - genossi_bin/src/lib.rs (komplette Datei oder mindestens lines 376-720 — RestStateImpl-Struct, RestStateImpl::new, AssemblyServiceImpl-Wiring lines ~596-604, AssemblyRestState-Impl lines ~1156-1162; lines 147-200 für Service-Type-Aliases + ServiceDependencies-Structs)
    - genossi_dao_impl_sqlite/src/attendance.rs (Plan 01 — AttendanceDaoImpl::new)
    - genossi_service_impl/src/attendance.rs (Plan 05 — AttendanceServiceImpl + AttendanceServiceDeps)
    - genossi_rest/src/attendance.rs (Plan 06 Task 1 — AttendanceRestState-Trait)
    - .planning/phases/03-attendance-aggregat-cascade-invalidation/03-PATTERNS.md §"genossi_bin/src/lib.rs" (lines 729-792)
  </read_first>
  <behavior>
    - genossi_bin baut.
    - Server-Boot funktioniert mit den neuen Wirings (verify indirekt durch E2E-Tests in Task 3).
  </behavior>
  <action>
**Schritt 1 — Type-Aliases am Datei-Anfang erweitern.** Suche `type AssemblyService = ...` und `type HelperTokenService = ...` (lines ~170-250); füge analog hinzu:

```rust
type AttendanceService =
    genossi_service_impl::attendance::AttendanceServiceImpl<AttendanceServiceDependencies>;
```

**Schritt 2 — Deps-Struct anlegen** (Pattern aus AssemblyServiceDependencies lines ~147-168):

```rust
pub struct AttendanceServiceDependencies;
unsafe impl Send for AttendanceServiceDependencies {}
unsafe impl Sync for AttendanceServiceDependencies {}

impl genossi_service_impl::attendance::AttendanceServiceDeps for AttendanceServiceDependencies {
    type Context = Context;
    type Transaction = Transaction;
    type AttendanceDao = AttendanceDao;            // type alias auf AttendanceDaoImpl<TransactionImpl>-Wrapper (siehe genossi_bin/src/lib.rs DAO-Aliases)
    type AssemblyDao = AssemblyDao;
    type MemberDao = MemberDao;
    type AssemblyMemberSnapshotDao = AssemblyMemberSnapshotDao;
    type PermissionService = PermissionService;
    type TransactionDao = TransactionDao;
}
```

(Die genauen DAO-Type-Aliases sind in genossi_bin/src/lib.rs schon definiert — verify durch grep `type AssemblyDao =`, `type MemberDao =`, etc. Falls AttendanceDao-Alias noch nicht existiert: `type AttendanceDao = genossi_dao_impl_sqlite::attendance::AttendanceDaoImpl<TransactionImpl>;` einfügen. Wenn der Wrap-Pattern anders aussieht — z. B. `Arc<...>`-Wrap — folge dem bestehenden DAO-Pattern exakt.)

**Schritt 3 — AssemblyServiceDependencies erweitern** (Plan 05 Task 2 hat 2 neue Deps eingeführt):

```rust
impl genossi_service_impl::assembly::AssemblyServiceDeps for AssemblyServiceDependencies {
    type Context = Context;
    type Transaction = Transaction;
    type AssemblyDao = AssemblyDao;
    type AssemblyMemberSnapshotDao = AssemblyMemberSnapshotDao;
    type MemberDao = MemberDao;
    type AuditLogDao = AuditLogDao;
    type PermissionService = PermissionService;
    type UuidService = UuidService;
    type TransactionDao = TransactionDao;
    // NEU Phase 3:
    type HelperTokenDao = HelperTokenDao;
    type PermissionDao = PermissionDao;
}
```

**Schritt 4 — RestStateImpl-Struct erweitern** (lines ~376-416). Füge Field:

```rust
pub struct RestStateImpl {
    // ... bestehende Felder ...
    pub attendance_service: Arc<AttendanceService>,
}
```

**Schritt 5 — RestStateImpl::new (oder analoger Konstruktor — Pattern aus lines ~580+) modifizieren:**

```rust
let attendance_dao = Arc::new(AttendanceDaoImpl::new(pool.clone()));
let attendance_service = Arc::new(genossi_service_impl::attendance::AttendanceServiceImpl {
    attendance_dao,
    assembly_dao: assembly_dao.clone(),
    member_dao: member_dao.clone(),
    assembly_member_snapshot_dao: assembly_member_snapshot_dao.clone(),
    permission_service: permission_service.clone(),
    transaction_dao: transaction_dao.clone(),
    _phantom: std::marker::PhantomData,
});
```

**Wichtig — Reihenfolge der Constructions:**
1. `assembly_member_snapshot_dao` muss VOR der `assembly_service`-Konstruktion mit `.clone()` referenzierbar sein (Pattern aus PATTERNS.md §"genossi_bin/src/lib.rs" line 759). Falls die bestehende lib.rs es per `let assembly_service = AssemblyServiceImpl { ..., assembly_member_snapshot_dao, ... }` direkt **moved** (statt cloned), MUSS die Konstruktion umgestellt werden auf `let assembly_member_snapshot_dao = Arc::new(...); let assembly_service = AssemblyServiceImpl { ..., assembly_member_snapshot_dao: assembly_member_snapshot_dao.clone(), ... };` — ansonsten bricht der attendance_service-Build.
2. `helper_token_dao` und `permission_dao` müssen ebenfalls VOR der `assembly_service`-Konstruktion verfügbar sein (Plan 05 hat AssemblyServiceImpl um diese 2 Deps erweitert). Falls der bestehende Code `helper_token_dao` NACH `assembly_service` konstruiert (line ~610 laut PATTERNS.md), muss die Reihenfolge umgestellt werden.

**Schritt 6 — AssemblyServiceImpl-Konstruktor erweitern** um die 2 neuen Felder (helper_token_dao, permission_dao):

Verifiziere dass im bestehenden `let assembly_service = Arc::new(AssemblyServiceImpl { ... })` die zwei neuen Felder ergänzt werden:

```rust
let assembly_service = Arc::new(genossi_service_impl::assembly::AssemblyServiceImpl {
    assembly_dao: assembly_dao.clone(),
    assembly_member_snapshot_dao: assembly_member_snapshot_dao.clone(),
    member_dao: member_dao.clone(),
    audit_log_dao: audit_log_dao.clone(),
    permission_service: permission_service.clone(),
    uuid_service: uuid_service.clone(),
    transaction_dao: transaction_dao.clone(),
    // NEU Phase 3:
    helper_token_dao: helper_token_dao.clone(),
    permission_dao: permission_dao.clone(),
    _phantom: std::marker::PhantomData,
});
```

**Schritt 7 — RestStateImpl Self {...} Initialisierung erweitern** (lines ~680-718). Füge `attendance_service` zur Field-Liste.

**Schritt 8 — AttendanceRestState-Trait-Impl** am Ende der Datei (oder analog zu AssemblyRestState bei lines ~1156-1162):

```rust
impl genossi_rest::attendance::AttendanceRestState for RestStateImpl {
    type AttendanceService = AttendanceService;
    fn attendance_service(&self) -> Arc<Self::AttendanceService> {
        self.attendance_service.clone()
    }
}
```

**Was NICHT machen:**
- KEINE Änderungen an Cargo.toml (Plan 01-05 haben keine neuen Crates eingeführt).
- KEINE Migration-Auto-Run-Logik anpassen (sqlx::migrate läuft schon beim Boot — die neue 20260504-Migration wird automatisch erkannt).
- KEIN Worker-Spawn für Attendance (kein Hintergrund-Job in Phase 3).
  </action>
  <verify>
    <automated>cargo build --bin genossi 2>&amp;1 | tail -20 &amp;&amp; cargo build -p genossi_bin 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    - `cargo build --bin genossi` exits 0.
    - `cargo build -p genossi_bin` exits 0.
    - `grep -c 'AttendanceServiceImpl' genossi_bin/src/lib.rs` ≥ 2 (type-alias + Konstruktor).
    - `grep -c 'AttendanceServiceDependencies' genossi_bin/src/lib.rs` ≥ 2 (struct + impl).
    - `grep -c 'attendance_service' genossi_bin/src/lib.rs` ≥ 3 (struct-field + Self {} + RestState-Trait).
    - `grep -c 'AttendanceRestState' genossi_bin/src/lib.rs` = 1 (impl-Block).
    - `grep -c 'helper_token_dao\.clone()' genossi_bin/src/lib.rs` ≥ 2 (assembly_service + ggf. anderswo).
    - `grep -c 'permission_dao\.clone()' genossi_bin/src/lib.rs` ≥ 1 (assembly_service Cascade-Erweiterung).
    - Bestehende E2E-Tests (`cargo test -p genossi_bin --test e2e_tests assembly` und `helper_token`) bleiben grün.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 6 E2E-Tests (Race, Cascade, PII-Leak, Hash-Chain, Post-Close-Edit, Substring-Search)</name>
  <files>
    genossi_bin/tests/e2e_tests.rs
  </files>
  <read_first>
    - genossi_bin/tests/e2e_tests.rs (suche und lese: setup() / setup_with_pool() lines ~3447-3465; create_open_assembly_for_helper_test lines ~8603-8637; create_helper_token_for_test lines ~8640-8663; HLPR-04-Race-Test lines ~8784-8819; HLPR-07-Audit-Verify lines ~9171-9279)
    - genossi_rest/src/test_server.rs (TestServer-Helpers, prüfe ob `pool()`-Getter existiert; falls nicht, dort ergänzen)
    - genossi_rest_types/src/lib.rs (AttendanceMemberTO, AttendanceStatsTO, AssemblyDetailTO, AssemblyStatusTO für JSON-Asserts; VerifyResponseTO für audit-verify)
    - .planning/phases/03-attendance-aggregat-cascade-invalidation/03-CONTEXT.md (D-26 Status-Codes)
    - .planning/phases/03-attendance-aggregat-cascade-invalidation/03-RESEARCH.md §Discretion 4 (Race-Test-Skelett); §Pitfall 7 (Hash-Chain-Test); §Pitfall 8 (Cascade-Test); §Pitfall 6 (PII-Leak-Test); §Vorstand-Post-Close-Edit-Test (lines 1504-1540)
    - .planning/phases/03-attendance-aggregat-cascade-invalidation/03-PATTERNS.md §"genossi_bin/tests/e2e_tests.rs" (lines 797-871)
  </read_first>
  <behavior>
    - 6 E2E-Tests laufen grün gegen einen real-laufenden Test-Server mit in-memory SQLite.
    - Jeder Test verifiziert eine spezifische Phase-3-Acceptance-Kriterium.
  </behavior>
  <action>
**Vorbereitung — TestServer::pool()-Getter sicherstellen:**

Greppe `grep -n "pub fn pool\|pub fn get_pool" genossi_rest/src/test_server.rs` — falls noch nicht vorhanden, ergänze einen Getter:

```rust
impl TestServer {
    pub fn pool(&self) -> Arc<SqlitePool> {
        self.rest_state.pool.clone()
    }
}
```

(Genaue Field-Name hängt von TestServer-Struktur ab — verify durch Lesen.)

Falls die bestehende `setup_with_pool() -> (TestServer, Arc<SqlitePool>)`-Helper bereits den Pool zurückgibt (RESEARCH §Pitfall 8 line 1497 erwähnt es), reicht das — kein Getter nötig.

**Schritt 1 — Helper am Test-Modul-Anfang ergänzen** (oder reusen):

```rust
// Erzeugt eine offene GV mit N Snapshot-Mitgliedern und liefert (assembly_id, Vec<member_id>).
async fn create_open_assembly_with_members(
    client: &reqwest::Client,
    server: &TestServer,
    n_members: usize,
) -> (Uuid, Vec<Uuid>) {
    // 1) N Member via POST /api/member anlegen
    let mut member_ids = Vec::new();
    for i in 0..n_members {
        let resp = client.post(server.url("/api/member"))
            .json(&serde_json::json!({
                "member_number": 1000 + i,
                "first_name": format!("Vorname{}", i),
                "last_name": format!("Nachname{}", i),
                // ... weitere Pflichtfelder gemäß CreateMemberRequest aus existing tests
            }))
            .send().await.unwrap();
        assert_eq!(resp.status(), StatusCode::OK);
        let m: serde_json::Value = resp.json().await.unwrap();
        member_ids.push(Uuid::parse_str(m["id"].as_str().unwrap()).unwrap());
    }
    // 2) GV anlegen + öffnen (open zieht Snapshot aller Member)
    let aid = create_open_assembly_for_helper_test(client, server).await;
    (aid, member_ids)
}
```

(Alternative: reuse die genaue Form aus den Phase-2-HLPR-Test-Helpers — `create_open_assembly_for_helper_test` legt schon eine GV an mit Snapshot, sodass nur Member vorher angelegt werden müssen.)

**Schritt 2 — Test 1: SYNC-02 Race** (verbatim aus RESEARCH §Discretion 4 lines 660-696):

```rust
#[tokio::test]
async fn test_attendance_upsert_race_one_row_two_200ok() {
    let server = setup().await;
    let client = reqwest::Client::new();
    let (aid, mids) = create_open_assembly_with_members(&client, &server, 1).await;
    let mid = mids[0];

    let url = server.url(&format!("/api/attendance/{}/{}", aid, mid));

    let (resp_a, resp_b) = tokio::join!(
        client.put(&url).send(),
        client.put(&url).send(),
    );
    let status_a = resp_a.unwrap().status();
    let status_b = resp_b.unwrap().status();

    assert_eq!(status_a, StatusCode::OK, "first PUT must be 200 OK (idempotent UPSERT)");
    assert_eq!(status_b, StatusCode::OK, "second PUT must be 200 OK (idempotent UPSERT, SYNC-02)");

    let stats_resp = client.get(server.url(&format!("/api/assembly/{}/stats", aid)))
        .send().await.unwrap();
    let stats: AttendanceStatsTO = stats_resp.json().await.unwrap();
    assert_eq!(stats.present, 1, "Race must produce exactly ONE present-row, not two (SYNC-02)");
    assert_eq!(stats.total, 1);
}
```

**Schritt 3 — Test 2: SC#8 Cascade** (verbatim aus RESEARCH §Pitfall 8 lines 1455-1494):

```rust
#[tokio::test]
async fn test_close_assembly_cascade_invalidates_helper_sessions() {
    let (server, pool) = setup_with_pool().await;
    let client = reqwest::Client::new();
    let assembly_id = create_open_assembly_for_helper_test(&client, &server).await;
    let (token_id, code) = create_helper_token_for_test(&client, &server, assembly_id, "Cascade-Anna").await;

    // Redeem über echten Pfad — schreibt session-Row + setzt session_id auf helper_token.
    let resp = client.post(server.url("/api/helper/redeem"))
        .json(&serde_json::json!({ "code": code })).send().await.unwrap();
    assert_eq!(resp.status(), StatusCode::OK);

    // Pre-Close: Session existiert in DB.
    let session_id_before: Option<String> = sqlx::query_scalar(
        "SELECT session_id FROM helper_token WHERE id = ?")
        .bind(token_id.as_bytes().to_vec())
        .fetch_one(&*pool).await.unwrap();
    let sid = session_id_before.expect("session_id must be set after redeem");
    let session_count_before: i64 = sqlx::query_scalar(
        "SELECT COUNT(*) FROM session WHERE id = ?")
        .bind(&sid).fetch_one(&*pool).await.unwrap();
    assert_eq!(session_count_before, 1, "session row must exist before close");

    // Close.
    let resp = client.post(server.url(&format!("/api/assembly/{}/close", assembly_id)))
        .send().await.unwrap();
    assert_eq!(resp.status(), StatusCode::OK);

    // Post-Close: session ist weg (Cascade D-11/D-12).
    let session_count_after: i64 = sqlx::query_scalar(
        "SELECT COUNT(*) FROM session WHERE id = ?")
        .bind(&sid).fetch_one(&*pool).await.unwrap();
    assert_eq!(session_count_after, 0,
        "Cascade must delete the session row (D-11/D-12)");
}
```

**Schritt 4 — Test 3: ATTN-01 PII-Leak** (verbatim aus RESEARCH §Pitfall 6 lines 1361-1383):

```rust
#[tokio::test]
async fn test_attendance_members_response_has_no_pii_fields() {
    let server = setup().await;
    let client = reqwest::Client::new();
    let (aid, _mids) = create_open_assembly_with_members(&client, &server, 1).await;

    let resp = client.get(server.url(&format!("/api/attendance/{}/members", aid)))
        .send().await.unwrap();
    assert_eq!(resp.status(), StatusCode::OK);
    let json: serde_json::Value = resp.json().await.unwrap();
    let members = json.as_array().expect("response must be JSON array");
    assert!(!members.is_empty(), "test setup must include at least one snapshot member");

    let m = &members[0];
    let obj = m.as_object().expect("member entry must be object");

    // Whitelist: nur diese 7 Keys dürfen vorkommen (Optional-Felder können fehlen).
    let allowed: std::collections::HashSet<&str> = [
        "member_number", "first_name", "last_name", "salutation",
        "title", "is_present", "member_id",
    ].iter().copied().collect();
    for key in obj.keys() {
        assert!(allowed.contains(key.as_str()),
            "AttendanceMemberTO leaked unauthorized field: '{}'", key);
    }

    // Blacklist (defense-in-depth): explizit verbotene PII-Keys.
    for forbidden in ["email", "iban", "bank_account", "street", "house_number",
                      "postal_code", "city", "comment", "join_date", "exit_date",
                      "birth_date", "phone"] {
        assert!(m.get(forbidden).is_none(),
            "AttendanceMemberTO leaked PII field '{}'", forbidden);
    }
}
```

**Schritt 5 — Test 4: ATTN-05 Hash-Chain Stability** (verbatim aus RESEARCH §Pitfall 7 lines 1395-1433):

**W-4-Fix — read_first VOR dem Test schreiben:** verifiziere den Audit-Endpoint-Vertrag explizit:
```bash
grep -n 'entity_type\|fn list_audit\|paged' genossi_rest/src/audit_log.rs | head -20
grep -n 'GET.*api/audit\|audit?entity_type' genossi_bin/tests/e2e_tests.rs | head -10
```

Erwartung: Phase-2-HLPR-07-Test (~genossi_bin/tests/e2e_tests.rs:9241-9278 laut Phase-2-Layout) ruft `/api/audit/verify` auf — dort siehst du die VerifyResponseTO-Form. Für die Filterung nach entity_type:
- **Falls `entity_type`-Query-Param existiert** (grep zeigt ihn als Handler-Param): nutze `?entity_type=attendance` wie unten gezeigt. Filtert serverseitig.
- **Falls KEIN entity_type-Filter existiert**: nutze die Query OHNE `?entity_type=...` und vergleiche `entries.len()` vor/nach dem 100-Toggle-Burst über die Gesamt-Audit-Count. Der Effekt — 0 neue Audit-Einträge für Attendance — bleibt verifizierbar, weil zwischen den beiden Snapshots NUR Attendance-Toggles passieren (kein anderer audit-relevanter Vorgang). Falls der Test-Setup zusätzliche audit-erzeugende Operationen hat (z. B. Member-Create im Helper), berücksichtige diese im Erwartungswert (count_after = count_before + bekannte_Audit-Erzeugungen, und die müssen != Attendance sein).

```rust
#[tokio::test]
async fn test_attendance_toggle_burst_does_not_pollute_audit_chain() {
    let server = setup().await;
    let client = reqwest::Client::new();
    let (aid, mids) = create_open_assembly_with_members(&client, &server, 1).await;
    let mid = mids[0];

    // Audit-Log-Größe VOR den Toggles via paged-listing.
    // Wenn entity_type-Filter NICHT existiert (siehe read_first oben), entferne
    // den Query-Param und filtere clientseitig oder vergleiche die Gesamt-Count.
    let resp_before = client.get(server.url("/api/audit?entity_type=attendance"))
        .send().await.unwrap();
    let before_paged: serde_json::Value = resp_before.json().await.unwrap();
    let count_before = before_paged["entries"].as_array().map(|e| e.len()).unwrap_or(0);

    // 50 PUT + 50 DELETE im Wechsel = 100 Toggles.
    let url = server.url(&format!("/api/attendance/{}/{}", aid, mid));
    for i in 0..100 {
        let resp = if i % 2 == 0 {
            client.put(&url).send().await.unwrap()
        } else {
            client.delete(&url).send().await.unwrap()
        };
        assert_eq!(resp.status(), StatusCode::OK, "toggle {} failed", i);
    }

    // Audit-Log-Größe NACH den Toggles — muss UNVERÄNDERT sein (D-08, ATTN-05).
    let resp_after = client.get(server.url("/api/audit?entity_type=attendance"))
        .send().await.unwrap();
    let after_paged: serde_json::Value = resp_after.json().await.unwrap();
    let count_after = after_paged["entries"].as_array().map(|e| e.len()).unwrap_or(0);
    assert_eq!(count_before, count_after,
        "ATTN-05: 100 attendance toggles must NOT add audit entries (before={}, after={})",
        count_before, count_after);

    // Defense-in-Depth: hash-chain bleibt valid.
    let verify_resp = client.get(server.url("/api/audit/verify")).send().await.unwrap();
    assert_eq!(verify_resp.status(), StatusCode::OK);
    let verify: genossi_rest_types::VerifyResponseTO = verify_resp.json().await.unwrap();
    assert!(verify.valid, "hash chain must remain valid");
    assert!(verify.broken_links.is_empty(),
        "no broken links allowed; got {:?}", verify.broken_links);
}
```

**Schritt 6 — Test 5: ASSY-06 Vorstand Post-Close-Edit** (verbatim aus RESEARCH lines 1504-1540):

```rust
#[tokio::test]
async fn test_vorstand_can_edit_attendance_after_close() {
    let server = setup().await;
    let client = reqwest::Client::new();
    let (aid, mids) = create_open_assembly_with_members(&client, &server, 1).await;
    let mid = mids[0];

    // 1) Vorstand markiert Mitglied als anwesend (Open).
    let resp = client.put(server.url(&format!("/api/attendance/{}/{}", aid, mid)))
        .send().await.unwrap();
    assert_eq!(resp.status(), StatusCode::OK);

    // 2) GV schließen.
    let resp = client.post(server.url(&format!("/api/assembly/{}/close", aid)))
        .send().await.unwrap();
    assert_eq!(resp.status(), StatusCode::OK);

    // 3) Vorstand entfernt nachträglich Anwesenheit (Closed).
    let resp = client.delete(server.url(&format!("/api/attendance/{}/{}", aid, mid)))
        .send().await.unwrap();
    assert_eq!(resp.status(), StatusCode::OK,
        "ASSY-06: Vorstand muss nach close noch DELETE können (D-20)");

    // 4) Stats reflektiert die Änderung.
    let resp = client.get(server.url(&format!("/api/assembly/{}/stats", aid)))
        .send().await.unwrap();
    let stats: AttendanceStatsTO = resp.json().await.unwrap();
    assert_eq!(stats.present, 0, "post-close-DELETE must update stats");
    assert_eq!(stats.total, 1);

    // 5) GV-Status bleibt Closed (kein Re-Open).
    let resp = client.get(server.url(&format!("/api/assembly/{}", aid)))
        .send().await.unwrap();
    let detail: AssemblyDetailTO = resp.json().await.unwrap();
    assert_eq!(detail.assembly.status, AssemblyStatusTO::Closed,
        "ASSY-06: post-close-edit must not change status (D-20)");
}
```

(Falls AssemblyDetailTO eine andere Form hat als angenommen — z. B. `assembly` ist nicht das exakte Feld-Name — adjustiere die JSON-Pfade entsprechend; verify durch `grep -n 'pub struct AssemblyDetailTO' genossi_rest_types/src/lib.rs`.)

**Schritt 6.5 — Test 6: ATTN-02 Substring-Search End-to-End** (W-3-Fix: explizite E2E-Verifikation der ?q=-Query-Param-Extraktion durch den Handler):

```rust
#[tokio::test]
async fn test_attendance_members_substring_search_filters_by_query_param() {
    let server = setup().await;
    let client = reqwest::Client::new();

    // 2 Members anlegen mit unterschiedlichen Nachnamen.
    // Wir nutzen create_open_assembly_with_members NICHT, weil wir die exakten
    // last_name-Werte kontrollieren müssen. Stattdessen direkter POST.
    let m1_resp = client.post(server.url("/api/member"))
        .json(&serde_json::json!({
            "member_number": 2001,
            "first_name": "Anna",
            "last_name": "Müller",
            // ... übrige Pflichtfelder gemäß CreateMemberRequest aus existing tests
        }))
        .send().await.unwrap();
    assert_eq!(m1_resp.status(), StatusCode::OK);

    let m2_resp = client.post(server.url("/api/member"))
        .json(&serde_json::json!({
            "member_number": 2002,
            "first_name": "Bert",
            "last_name": "Schmidt",
            // ... übrige Pflichtfelder
        }))
        .send().await.unwrap();
    assert_eq!(m2_resp.status(), StatusCode::OK);

    // GV anlegen + öffnen → Snapshot enthält beide Members.
    let aid = create_open_assembly_for_helper_test(&client, &server).await;

    // Suche mit ?q=Müll — URL-encoded für den Umlaut.
    let resp = client
        .get(server.url(&format!("/api/attendance/{}/members?q=M%C3%BCll", aid)))
        .send().await.unwrap();
    assert_eq!(resp.status(), StatusCode::OK);

    let members: Vec<AttendanceMemberTO> = resp.json().await.unwrap();
    assert_eq!(members.len(), 1,
        "ATTN-02: ?q=Müll muss exakt 1 Treffer liefern (Müller match, Schmidt nicht), got {} entries",
        members.len());
    assert_eq!(members[0].last_name.as_deref(), Some("Müller"),
        "ATTN-02: Treffer muss Müller sein, got {:?}", members[0].last_name);

    // Defense-in-Depth: Suche ohne q liefert beide.
    let resp_all = client
        .get(server.url(&format!("/api/attendance/{}/members", aid)))
        .send().await.unwrap();
    assert_eq!(resp_all.status(), StatusCode::OK);
    let all: Vec<AttendanceMemberTO> = resp_all.json().await.unwrap();
    assert_eq!(all.len(), 2,
        "Ohne ?q müssen beide Snapshot-Members zurückkommen, got {}", all.len());
}
```

(Field-Name `last_name` auf `AttendanceMemberTO` — verify durch `grep -n 'pub struct AttendanceMemberTO\|last_name' genossi_rest_types/src/lib.rs`. Falls der Field-Type nicht `Option<String>` sondern z. B. `String` ist, anpassen — der Test-Kern bleibt gleich. Pattern-Vorbild: existing list-tests aus Phase 1+2 falls vorhanden — `grep -n 'GET.*members\|q=' genossi_bin/tests/e2e_tests.rs` zeigt Vorbilder.)

**Schritt 7 — Importliste am Test-File-Anfang** ergänzen (falls nicht vorhanden):

```rust
use genossi_rest_types::{AttendanceMemberTO, AttendanceStatsTO, AssemblyDetailTO, AssemblyStatusTO, VerifyResponseTO};
```

**Was NICHT machen:**
- KEIN Helfer-Token-basierten PUT-Test in diesem Plan — der Helper-Auth-Pfad braucht den vollen Cookie-Setup (Phase-2-redeem) der in Test 2 (Cascade) bereits exemplarisch enthalten ist. Phase-3-Helper-Cross-Assembly-Permission-Test ist als **bonus test 6** möglich, aber nicht zwingend für die 5 Must-Have-Tests.
- KEIN Long-Running-Test (>30s) — die 100-Toggle-Burst sollte auf in-memory SQLite < 5s sein.
- KEIN Test auf den absoluten `marked_at`-Timestamp — UPSERT überschreibt ihn, das ist erwartet.
  </action>
  <verify>
    <automated>cargo test --test e2e_tests --no-fail-fast -- --nocapture test_attendance test_close_assembly_cascade test_vorstand_can_edit 2>&amp;1 | tail -120</automated>
  </verify>
  <done>
    - `cargo test --test e2e_tests test_attendance_upsert_race_one_row_two_200ok` exits 0.
    - `cargo test --test e2e_tests test_close_assembly_cascade_invalidates_helper_sessions` exits 0.
    - `cargo test --test e2e_tests test_attendance_members_response_has_no_pii_fields` exits 0.
    - `cargo test --test e2e_tests test_attendance_toggle_burst_does_not_pollute_audit_chain` exits 0.
    - `cargo test --test e2e_tests test_vorstand_can_edit_attendance_after_close` exits 0.
    - `cargo test --test e2e_tests test_attendance_members_substring_search_filters_by_query_param` exits 0.
    - `grep -c '^async fn test_attendance\|^async fn test_close_assembly_cascade\|^async fn test_vorstand_can_edit' genossi_bin/tests/e2e_tests.rs` ≥ 6 (alle 6 neuen Tests vorhanden).
    - `grep -c 'AttendanceStatsTO\|AttendanceMemberTO' genossi_bin/tests/e2e_tests.rs` ≥ 4 (TOs werden genutzt).
    - **Bestehende E2E-Tests** (`cargo test --test e2e_tests` für ALLE Tests) bleiben grün — Wave-4-Plan darf keinen Regress in Phase 1+2 verursachen.
  </done>
</task>

</tasks>

<threat_model>
Siehe Frontmatter `threat_model:` — sechs Threats. Wichtigste:
- **T-03-06-01** PII-Leak: durch E2E-Test test_attendance_members_response_has_no_pii_fields verifiziert.
- **T-03-06-02** Differential 401-vs-403: lokales map_attendance_error.
- **T-03-06-03** Idempotenz: durch Race-Test verifiziert.
- **T-03-06-04** Audit-Stille: durch Hash-Chain-Stability-Test verifiziert.
</threat_model>

<verification>
- `cargo build --bin genossi` exits 0.
- `cargo test --test e2e_tests` (komplett) exits 0 — alle Phase-1+2+3-E2E-Tests grün.
- `cargo fmt -- --check` exits 0.
- `cargo clippy --all-targets --all-features --no-deps -- -D warnings` exits 0.
- Manuelle Smoke: Server starten, Swagger-UI öffnen, Attendance-Endpoints sichtbar (verifizier optional via `cargo run --bin genossi` + browser).
</verification>

<success_criteria>
- Alle 9 Phase-3-Requirements (ASSY-04, ASSY-06, ATTN-01..06, SYNC-02) sind durch mindestens einen E2E-Test verifiziert:
  - ASSY-04 → stats-Endpoint wird in test_attendance_upsert_race und test_vorstand_can_edit getestet.
  - ASSY-06 → test_vorstand_can_edit_attendance_after_close.
  - ATTN-01 → test_attendance_members_response_has_no_pii_fields.
  - ATTN-02 → DIRECT via test_attendance_members_substring_search_filters_by_query_param (W-3-Fix: explizite ?q=-Query-Param-Verifikation, 2 Members + 1 Treffer).
  - ATTN-03 → test_attendance_upsert_race verifiziert dass 2x PUT = 1 Row.
  - ATTN-04 → test_attendance_toggle_burst (50× DELETE im Wechsel mit PUT) und test_vorstand_can_edit (DELETE nach close).
  - ATTN-05 → test_attendance_toggle_burst_does_not_pollute_audit_chain.
  - ATTN-06 → test_vorstand_can_edit_attendance_after_close (admin-Pfad funktioniert) + bestehende Phase-2-context-extractor-coverage.
  - SYNC-02 → test_attendance_upsert_race_one_row_two_200ok.
- ROADMAP-Phase-3-SC#8 (Cascade-Invalidation) → test_close_assembly_cascade_invalidates_helper_sessions.
- DI-Wiring vollständig: alle 5 RestStateImpl-Erweiterungen funktionieren ohne Build-Fehler.
- Bestehende Phase-1+2-E2E-Tests bleiben grün.
</success_criteria>

<output>
Nach Abschluss: erstelle `.planning/phases/03-attendance-aggregat-cascade-invalidation/03-06-SUMMARY.md` mit:
- Liste der erstellten/modifizierten Files
- Endpoint-Karte (Method + Path + Response-Status für alle 4 Endpoints)
- DI-Wiring-Diff (welche Felder zu RestStateImpl + AssemblyServiceImpl hinzukamen)
- 5 E2E-Tests mit Status + welches Phase-Requirement sie beweisen
- Hinweis auf Phase 4 (Frontend) als Next-Step — die in Phase 4 konsumierten REST-Schemas (AttendanceMemberTO, AttendanceStatsTO) sind jetzt fertig
- Phase-Verification: alle 9 Phase-Requirements abgedeckt + Audit-Hash-Chain bleibt intakt
</output>
