---
phase: 03-attendance-aggregat-cascade-invalidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/sqlite/20260504000000_create_attendance_table.sql
  - genossi_dao/src/attendance.rs
  - genossi_dao/src/lib.rs
  - genossi_dao_impl_sqlite/src/attendance.rs
  - genossi_dao_impl_sqlite/src/lib.rs
autonomous: true
requirements: [ATTN-03, ATTN-04, SYNC-02]
tags: [sqlite, dao, migration, upsert, soft-delete, attendance]

threat_model:
  trust_boundaries:
    - "DB-Layer ist innerhalb der App vertrauenswürdig — DAO-Methoden werden nur vom Service-Layer aufgerufen, der in Plan 05 die Auth-Funnel davorschaltet."
  threats:
    - id: "T-03-01-01"
      category: "Tampering"
      component: "AttendanceDaoImpl::upsert_present (UPSERT-SQL)"
      disposition: "mitigate"
      mitigation: "Alle Bind-Parameter via sqlx::query(...).bind(...) — keine String-Konkatenation, kein format!() mit User-Input. assembly_id/member_id sind Uuid (16-Byte BLOB), marked_by_user_id wird vom Service mit synthetischer ID 'helper:<uuid>' oder OIDC-User-ID gesetzt; Rohwerte werden parameterisiert."
    - id: "T-03-01-02"
      category: "Information Disclosure"
      component: "AttendanceDaoImpl::list_members_for_assembly (LEFT-JOIN SQL)"
      disposition: "mitigate"
      mitigation: "SELECT-Liste exponiert NUR die 7 erlaubten Spalten (id, member_number, first_name, last_name, salutation, title, is_present). Keine SELECT m.* — explizites Spalten-Whitelisting verhindert PII-Leak bei späteren MemberEntity-Erweiterungen. Substring-Pattern wird mit format!('%{}%', s.trim()) gewrappt — LIKE-Sonderzeichen (%/_) im Suchbegriff sind funktional egal, kein Injection-Risiko (parameterisiert)."
    - id: "T-03-01-03"
      category: "Denial of Service"
      component: "AttendanceDaoImpl::list_members_for_assembly (Substring-Search)"
      disposition: "accept"
      mitigation: "Substring-LIKE auf full-table-scan akzeptabel bei Genossenschafts-Größenordnung (10-500 Mitglieder, sub-millisecond auf SQLite-in-Memory). Kein Min-Length-Filter, kein Pagination — Discretion-Decision in CONTEXT/RESEARCH explizit aufgenommen. Phase 5 (Generalprobe) re-evaluiert unter realer Last."
    - id: "T-03-01-04"
      category: "Tampering"
      component: "FOREIGN KEY assembly_id/member_id"
      disposition: "accept"
      mitigation: "FK ON DELETE RESTRICT ist documentary (PRAGMA foreign_keys ist project-weit OFF — siehe assembly_member_snapshot.rs:34-44). Defense-in-depth: snapshot-membership-check (D-27) wird in Plan 05 im AttendanceServiceImpl::mark_present/mark_absent aktiv ausgeführt, BEVOR der DAO touched wird."

must_haves:
  truths:
    - "Migration legt Tabelle attendance mit Composite-PK (assembly_id, member_id), FK ON DELETE RESTRICT, optional partial index auf assembly_id WHERE deleted IS NULL an"
    - "Aufruf AttendanceDao::upsert_present 5x hintereinander für (aid, mid) erzeugt genau 1 Row; deleted ist NULL; marked_at = letzter Aufruf"
    - "Aufruf AttendanceDao::soft_delete für (aid, mid) ohne vorhandene Row liefert Ok(()) (kein NotFound)"
    - "Aufruf AttendanceDao::soft_delete für (aid, mid) MIT Row setzt deleted = now()"
    - "AttendanceDao::list_members_for_assembly mit search=None liefert alle Snapshot-Mitglieder mit is_present=true|false"
    - "AttendanceDao::list_members_for_assembly mit search=Some('Maxi') liefert nur Mitglieder, deren last_name||' '||first_name oder member_number 'Maxi' substring-enthält (case-insensitive für ASCII)"
    - "AttendanceDao::count_present_by_assembly liefert COUNT der attendance-Rows mit deleted IS NULL für die assembly_id"
    - "AttendanceDao::is_in_snapshot liefert true gdw. (aid, mid) in assembly_member_snapshot existiert"
    - "D-01: attendance-Tabelle hat exakt 5 Spalten (assembly_id, member_id, marked_at, marked_by_user_id, deleted) — KEIN id, KEIN version"
    - "D-02: Schema-Vorbild ist assembly_member_snapshot (leichtgewichtige Join-Tabelle), NICHT assembly.rs (vollwertiges Aggregat)"
    - "D-03: Toggle-Off-Modell ist Soft-Delete-Flip via UPDATE, nicht Hard-Delete"
    - "D-04: UNIQUE-Index ist regulärer composite-PK UNIQUE(assembly_id, member_id), nicht partial — SQLite-UPSERT braucht plain UNIQUE für ON CONFLICT-Targeting"
    - "D-05: Atomarer Toggle-On via SQLite UPSERT (INSERT … ON CONFLICT(assembly_id, member_id) DO UPDATE SET …, deleted = NULL) — single statement, race-frei"
    - "D-06: Atomarer Toggle-Off via UPDATE-Soft-Delete; rows_affected wird ignoriert (idempotenter No-Op auf nicht-existierender Row)"
    - "D-07: KEIN unmarked_by-Feld auf attendance — minimaler Schema-Footprint, Wer-hat-ausgetragen ist Edge-Case ohne Verband-Anforderung"
    - "D-08: KEIN Audit auf AttendanceEntity (keine Auditable-Impl, keine audited_*!-Macros) — konform mit ATTN-05"
    - "D-09: Soft-Delete-Slot wird produktiv genutzt — Toggle-Off setzt deleted, Toggle-On überschreibt es auf NULL"
    - "D-10: Migration-Filename folgt YYYYMMDDHHMMSS_create_attendance_table.sql-Konvention (englisch)"
    - "D-25: Substring-Search ist im DAO ausgeführt (LIKE in SQL), nicht im Service-Memory-Filter"
    - "D-27: AttendanceDao::is_in_snapshot stellt die DAO-Method bereit, die der Service in Plan 05 für die snapshot-Membership-Prüfung nutzt"
    - "D-28: Code-Identifier (AttendanceEntity, AttendanceDao, AttendanceMemberRow) und Tabelle (attendance) sind durchgängig englisch"
  artifacts:
    - path: "migrations/sqlite/20260504000000_create_attendance_table.sql"
      provides: "attendance-Tabellen-Schema mit Composite-PK + FK + optional partial Index"
      contains: "CREATE TABLE IF NOT EXISTS attendance"
    - path: "genossi_dao/src/attendance.rs"
      provides: "AttendanceEntity + AttendanceMemberRow + AttendanceDao Trait + #[automock]"
      exports: ["AttendanceEntity", "AttendanceMemberRow", "AttendanceDao", "MockAttendanceDao"]
    - path: "genossi_dao_impl_sqlite/src/attendance.rs"
      provides: "AttendanceDaoImpl mit 5 SQLx-Methods + module-tests"
      exports: ["AttendanceDaoImpl"]
  key_links:
    - from: "genossi_dao/src/lib.rs"
      to: "genossi_dao::attendance"
      via: "pub mod attendance;"
      pattern: "pub mod attendance"
    - from: "genossi_dao_impl_sqlite/src/lib.rs"
      to: "genossi_dao_impl_sqlite::attendance"
      via: "pub mod attendance;"
      pattern: "pub mod attendance"
    - from: "AttendanceDaoImpl::upsert_present"
      to: "attendance-Tabelle"
      via: "INSERT ... ON CONFLICT(assembly_id, member_id) DO UPDATE"
      pattern: "ON CONFLICT.assembly_id, member_id."
---

<objective>
Plan 01 legt das Attendance-Aggregat als leichtgewichtige Join-Tabelle (assembly ↔ member, kein id/version, kein Audit) auf DAO-Ebene an. Schließt damit ATTN-03 (idempotenter PUT via UPSERT), ATTN-04 (idempotentes DELETE via Soft-Delete-Flip) und SYNC-02 (race-freies UPSERT-Atomicity) auf SQL-Ebene. Liefert das Trait + die SQLite-Impl + die Migration in einem Plan, weil sie zusammen denselben Vertrag bilden und ohne einander nicht testbar sind.

Implementiert die User-Decisions D-01 (Schema), D-02 (Schema-Vorbild assembly_member_snapshot), D-03 (Soft-Delete-Flip), D-04 (UNIQUE non-partial), D-05 (UPSERT-Toggle-On), D-06 (UPDATE-Soft-Delete-Toggle-Off), D-07 (kein unmarked_by-Feld), D-08 (kein Audit für attendance), D-09 (Soft-Delete-Slot produktiv), D-10 (Migration-Filename), D-25 (Substring-Search im DAO), D-27 (Snapshot-Membership-DAO-Method), D-28 (englische Naming-Konvention).

Purpose: Foundation für alle nachfolgenden Phase-3-Plans. Service-Layer (Plan 04/05) und REST-Layer (Plan 06) bauen auf diesem DAO auf; Plan 03 (HelperTokenDao-Erweiterung) und Plan 04 (Service-Trait) laufen parallel zu diesem Plan in Wave 1.
Output: Eine neue Migration, ein neues DAO-Trait + Entity-Modul, eine SQLite-Impl mit 5 Methods, je ein Modul-Test pro Method.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-attendance-aggregat-cascade-invalidation/03-CONTEXT.md
@.planning/phases/03-attendance-aggregat-cascade-invalidation/03-RESEARCH.md
@.planning/phases/03-attendance-aggregat-cascade-invalidation/03-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Pattern-Vorbild für die Migration: composite PK + FK pattern -->
From migrations/sqlite/20260502000001_create_assembly_member_snapshot_table.sql:
```sql
CREATE TABLE IF NOT EXISTS assembly_member_snapshot (
    assembly_id BLOB NOT NULL,
    member_id BLOB NOT NULL,
    captured_at TEXT NOT NULL,
    PRIMARY KEY (assembly_id, member_id),
    FOREIGN KEY (assembly_id) REFERENCES assembly(id),
    FOREIGN KEY (member_id) REFERENCES member(id)
);
CREATE INDEX IF NOT EXISTS idx_assembly_member_snapshot_assembly_id
    ON assembly_member_snapshot(assembly_id);
```

<!-- Pattern-Vorbild für FK ON DELETE RESTRICT (Phase 2): -->
From migrations/sqlite/20260503000000_create_helper_token_table.sql:
```sql
FOREIGN KEY (assembly_id) REFERENCES assembly(id) ON DELETE RESTRICT,
```

<!-- AssemblyMemberSnapshotDao: Schema-Vorbild für leichtgewichtige Trait-Form -->
From genossi_dao/src/assembly_member_snapshot.rs:
```rust
#[derive(Clone, Debug, PartialEq, Eq)]
pub struct AssemblyMemberSnapshotEntity {
    pub assembly_id: Uuid,
    pub member_id: Uuid,
    pub captured_at: time::PrimitiveDateTime,
}

#[automock(type Transaction = crate::MockTransaction;)]
#[async_trait]
pub trait AssemblyMemberSnapshotDao {
    type Transaction: crate::Transaction;
    async fn create(...) -> Result<(), DaoError>;
    async fn create_batch(...) -> Result<(), DaoError>;
    async fn find_by_assembly_id(...) -> Result<Arc<[...]>, DaoError>;
    async fn count_by_assembly_id(&self, assembly_id: Uuid, tx: Self::Transaction) -> Result<u64, DaoError>;
}
```

<!-- DaoError-Variants (genossi_dao/src/lib.rs): -->
DaoError::DatabaseError(Arc<str>), DaoError::ParseError(Arc<str>), DaoError::NotFound, DaoError::ConflictError(Arc<str>)

<!-- TransactionImpl-Bind-Pattern aus genossi_dao_impl_sqlite/src/helper_token.rs (Phase 2): -->
- `tx.tx.lock().await.as_mut()` als Executor für sqlx::query/query_as/query_scalar.
- format_dt-Helper: `dt.assume_utc().format(&time::format_description::well_known::Iso8601::DEFAULT)`.
- BLOB-Bind via `assembly_id.as_bytes().to_vec()`.

<!-- helper_token.rs liefert Vorbilder für UPSERT-Idiom + query_scalar COUNT-Idiom: -->
- INSERT ... ON CONFLICT DO UPDATE bereits etabliert in genossi_dao_impl_sqlite/src/permission.rs::ensure_user_exists.
- query_scalar::<_, i64> für COUNT-Aggregations bereits etabliert in helper_token.rs:167-173.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration + AttendanceDao Trait + Entity-Module</name>
  <files>
    migrations/sqlite/20260504000000_create_attendance_table.sql,
    genossi_dao/src/attendance.rs,
    genossi_dao/src/lib.rs
  </files>
  <read_first>
    - migrations/sqlite/20260502000001_create_assembly_member_snapshot_table.sql (Composite-PK + FK pattern; Vorbild für leichtgewichtige Join-Tabelle)
    - migrations/sqlite/20260503000000_create_helper_token_table.sql (FK ON DELETE RESTRICT pattern, WR-03 PRAGMA-Comment-Pattern)
    - genossi_dao/src/assembly_member_snapshot.rs (komplette Datei — Trait + Entity + automock + tests-Modul; identische Form für AttendanceDao)
    - genossi_dao/src/helper_token.rs (Trait-Form mit non-CRUD methods wie atomic_redeem; Vorbild für upsert_present/soft_delete-Signatur)
    - genossi_dao/src/lib.rs (pub mod-Liste; Position für `pub mod attendance;`)
    - .planning/phases/03-attendance-aggregat-cascade-invalidation/03-CONTEXT.md (D-01 bis D-10, D-24, D-27, D-28)
    - .planning/phases/03-attendance-aggregat-cascade-invalidation/03-PATTERNS.md (§"genossi_dao/src/attendance.rs" lines 69-138, §"migrations/sqlite/..." lines 30-66)
    - .planning/phases/03-attendance-aggregat-cascade-invalidation/03-RESEARCH.md §Konkrete Code-Recommendations lines 745-841 (verbatim Trait-Skelett)
  </read_first>
  <behavior>
    - Test 1 (in genossi_dao/src/attendance.rs::tests): Construct AttendanceEntity { assembly_id: Uuid::new_v4(), member_id: Uuid::new_v4(), marked_at, marked_by_user_id: Arc::from("helper:abc"), deleted: None } — assert that exactly these 5 fields exist (compile-time contract).
    - Test 2 (in genossi_dao/src/attendance.rs::tests): Construct AttendanceMemberRow { member_id, member_number: 42, first_name: Arc::from("Max"), last_name: Arc::from("Müller"), salutation: Some(Arc::from("Herr")), title: None, is_present: true } — 7 fields, kein PII-Feld (per Whitelist gegenüber MemberEntity).
    - Test 3: MockAttendanceDao kann via `MockAttendanceDao::new()` erzeugt werden (verifiziert dass `#[automock(type Transaction = crate::MockTransaction;)]` korrekt aufgesetzt ist).
  </behavior>
  <action>
**Schritt 1 — Migration anlegen** (Datei: `migrations/sqlite/20260504000000_create_attendance_table.sql`) — implementiert D-01, D-04, D-09, D-10:

```sql
-- NOTE (WR-03): FOREIGN KEY clauses below are DOCUMENTARY only.
-- This codebase does not enable `PRAGMA foreign_keys=ON`. The Service layer
-- (Plan 05: AttendanceServiceImpl::mark_present/mark_absent) performs an
-- explicit snapshot-membership check (D-27) before any UPSERT, which is the
-- operative protection. The FK clauses document the intended referential
-- semantics for future operators reading the schema.

CREATE TABLE IF NOT EXISTS attendance (
    assembly_id BLOB NOT NULL,
    member_id BLOB NOT NULL,
    marked_at TEXT NOT NULL,
    marked_by_user_id TEXT NOT NULL,
    deleted TEXT,
    PRIMARY KEY (assembly_id, member_id),
    FOREIGN KEY (assembly_id) REFERENCES assembly(id) ON DELETE RESTRICT,
    FOREIGN KEY (member_id) REFERENCES member(id) ON DELETE RESTRICT
);

-- Composite-PK is automatically UNIQUE → ON CONFLICT(assembly_id, member_id) target works (D-04, D-05).
-- Optional partial index — beschleunigt count_present_by_assembly + LEFT JOIN auf is_present.
CREATE INDEX IF NOT EXISTS idx_attendance_assembly_present
    ON attendance(assembly_id) WHERE deleted IS NULL;
```

**Schritt 2 — AttendanceDao Trait + Entities** (neue Datei `genossi_dao/src/attendance.rs`) — implementiert D-01, D-02, D-07, D-08, D-24, D-25, D-27, D-28:

Verbatim aus RESEARCH §Konkrete Code-Recommendations (lines 745-841). Wichtige Punkte:
- Imports: `async_trait`, `mockall::automock`, `std::sync::Arc`, `uuid::Uuid`, `crate::DaoError`.
- `AttendanceEntity { assembly_id: Uuid, member_id: Uuid, marked_at: time::PrimitiveDateTime, marked_by_user_id: Arc<str>, deleted: Option<time::PrimitiveDateTime> }` — **exakt diese 5 Felder, KEIN id, KEIN version** (per D-01); KEIN unmarked_by (per D-07).
- `AttendanceMemberRow { member_id: Uuid, member_number: i64, first_name: Arc<str>, last_name: Arc<str>, salutation: Option<Arc<str>>, title: Option<Arc<str>>, is_present: bool }` — **exakt diese 7 Felder, KEIN email/iban/address/comment** (per D-24, ATTN-01).
- `#[automock(type Transaction = crate::MockTransaction;)]` + `#[async_trait]` auf `pub trait AttendanceDao`.
- 5 Trait-Methods (verbatim Signaturen):
  - `async fn upsert_present(&self, assembly_id: Uuid, member_id: Uuid, marked_at: time::PrimitiveDateTime, marked_by_user_id: &str, tx: Self::Transaction) -> Result<(), DaoError>;`
  - `async fn soft_delete(&self, assembly_id: Uuid, member_id: Uuid, deleted_at: time::PrimitiveDateTime, tx: Self::Transaction) -> Result<(), DaoError>;`
  - `async fn list_members_for_assembly(&self, assembly_id: Uuid, search: Option<&str>, tx: Self::Transaction) -> Result<Arc<[AttendanceMemberRow]>, DaoError>;`
  - `async fn count_present_by_assembly(&self, assembly_id: Uuid, tx: Self::Transaction) -> Result<u64, DaoError>;`
  - `async fn is_in_snapshot(&self, assembly_id: Uuid, member_id: Uuid, tx: Self::Transaction) -> Result<bool, DaoError>;`
- KEINE `Auditable`-Impl auf AttendanceEntity (D-08, ATTN-05 — bewusst KEIN Audit).
- Doc-Comment auf AttendanceEntity / AttendanceMemberRow muss explizit erwähnen: "PII-Leak-Guard: Whitelist von 7 Feldern; KEIN MemberTO-Conversion erlaubt — siehe ATTN-01" (per Pitfall 6).

**Schritt 3 — Test-Modul am Ende** (in derselben Datei, `#[cfg(test)] mod tests { ... }`):

Drei Tests gemäß `<behavior>`. Vorbild: `assembly_member_snapshot.rs:47-66`.

**Schritt 4 — `genossi_dao/src/lib.rs` modifizieren**: füge `pub mod attendance;` hinzu — alphabetisch nach `pub mod assembly_member_snapshot;` einsortieren.

**Was NICHT machen:**
- KEIN `Auditable`-Impl auf AttendanceEntity (D-08).
- KEIN `find_by_id` / `dump_all` / `create` / `update` (Trait-default-Methods) — AttendanceDao folgt **nicht** dem Standard-CRUD-Trait, weil keine Identity. Verbatim die 5 Methods aus RESEARCH übernehmen.
- KEIN `id`/`version`-Feld auf AttendanceEntity (D-01).
- KEIN `unmarked_by`-Feld (D-07).
- KEIN `From<&MemberEntity> for AttendanceMemberRow` und KEIN `From<&MemberTO> for AttendanceMemberRow` — die Konversion läuft DAO-intern aus dem SELECT (Plan 01-Task-2). Für TO-Conversion siehe Plan 04.
  </action>
  <verify>
    <automated>cargo build -p genossi_dao 2>&amp;1 | tail -20 &amp;&amp; cargo test -p genossi_dao attendance --no-fail-fast 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    - Datei `migrations/sqlite/20260504000000_create_attendance_table.sql` existiert; `grep -c 'CREATE TABLE IF NOT EXISTS attendance' migrations/sqlite/20260504000000_create_attendance_table.sql` ≥ 1.
    - Datei `genossi_dao/src/attendance.rs` existiert; `grep -c 'pub struct AttendanceEntity' genossi_dao/src/attendance.rs` = 1; `grep -c 'pub struct AttendanceMemberRow' genossi_dao/src/attendance.rs` = 1; `grep -c 'pub trait AttendanceDao' genossi_dao/src/attendance.rs` = 1.
    - `grep -c 'pub mod attendance' genossi_dao/src/lib.rs` = 1.
    - `cargo build -p genossi_dao` exits 0.
    - `cargo test -p genossi_dao attendance` exits 0; mindestens 3 Tests laufen.
    - `grep -E 'pub (assembly_id|member_id|marked_at|marked_by_user_id|deleted): ' genossi_dao/src/attendance.rs | wc -l` = 5 (exakt die 5 AttendanceEntity-Felder per D-01).
    - `grep -c 'audited_create\|Auditable' genossi_dao/src/attendance.rs` = 0 (KEIN Audit per D-08).
    - `grep -c 'unmarked_by' genossi_dao/src/attendance.rs` = 0 (KEIN unmarked_by per D-07).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AttendanceDaoImpl SQLite + Modul-Tests</name>
  <files>
    genossi_dao_impl_sqlite/src/attendance.rs,
    genossi_dao_impl_sqlite/src/lib.rs
  </files>
  <read_first>
    - genossi_dao_impl_sqlite/src/helper_token.rs (komplette Datei — sqlx::query bind-pattern, format_dt-helper, query_scalar-COUNT-Idiom, lock-await-as_mut-Pattern)
    - genossi_dao_impl_sqlite/src/assembly_member_snapshot.rs (Schema-Vorbild + create-Body)
    - genossi_dao_impl_sqlite/src/permission.rs::ensure_user_exists (etabliertes UPSERT-ON-CONFLICT-Idiom im Projekt)
    - genossi_dao_impl_sqlite/src/lib.rs (pub mod-Liste; Position für `pub mod attendance;`)
    - .planning/phases/03-attendance-aggregat-cascade-invalidation/03-RESEARCH.md §Pattern 1, §Pattern 2, §Pattern 3, §Pattern 4 (lines 280-365); §Konkrete Code-Recommendations lines 843-1017 (verbatim Impl-Body); §Pitfall 1, 2, 3 (lines 1273-1297)
    - .planning/phases/03-attendance-aggregat-cascade-invalidation/03-PATTERNS.md §"genossi_dao_impl_sqlite/src/attendance.rs" (lines 141-208)
  </read_first>
  <behavior>
    - Test 1 `test_upsert_present_idempotent_5x_creates_one_row`: Connect zu sqlite::memory:, run migration; insert 1 assembly + 1 member + 1 snapshot row directly; rufe `upsert_present(aid, mid, now, "user1", tx)` 5x in Folge auf; nach jedem Call assert SELECT COUNT(*) FROM attendance WHERE assembly_id=? AND member_id=? = 1; nach den 5 Calls assert deleted IS NULL und marked_by_user_id == "user1".
    - Test 2 `test_soft_delete_then_upsert_resets_deleted`: nach `upsert_present` → `soft_delete(aid, mid, now)` → SELECT deleted IS NOT NULL; danach erneut `upsert_present(aid, mid, now2, "user2")` → SELECT deleted IS NULL UND marked_by_user_id == "user2".
    - Test 3 `test_soft_delete_on_nonexistent_row_is_ok`: `soft_delete(random_aid, random_mid, now)` ohne vorher INSERT — assert Result::Ok(()) und 0 affected rows-Behavior (kein Panic, kein Error).
    - Test 4 `test_list_members_for_assembly_filters_by_snapshot_and_substring`: Setup 2 Members (Müller, Schmidt) + nur Müller im Snapshot; rufe `list_members_for_assembly(aid, None, tx)` → Ergebnis hat 1 Eintrag mit last_name=="Müller"; rufe mit `Some("schmi")` → leeres Ergebnis (Schmidt nicht im snapshot); rufe mit `Some("müll")` → 1 Eintrag (case-insensitive ASCII).
    - Test 5 `test_count_present_by_assembly_excludes_soft_deleted`: 2 Members im Snapshot + Anwesenheits-UPSERT für beide → count == 2; soft_delete für einen → count == 1.
    - Test 6 `test_is_in_snapshot_true_false`: Member im Snapshot → true; anderer Member nicht im Snapshot → false.
  </behavior>
  <action>
**Schritt 1 — Datei `genossi_dao_impl_sqlite/src/attendance.rs` anlegen.** Implementiert D-03 (Soft-Delete via UPDATE), D-05 (UPSERT), D-06 (UPDATE-Soft-Delete-No-Op), D-25 (Substring-Search im DAO).

Verbatim aus RESEARCH §Konkrete Code-Recommendations (lines 843-1017). Wichtige Punkte:

- Imports: `async_trait::async_trait`, `genossi_dao::attendance::{AttendanceDao, AttendanceMemberRow}`, `genossi_dao::DaoError`, `sqlx::SqlitePool`, `std::sync::Arc`, `time::PrimitiveDateTime`, `uuid::Uuid`, `crate::TransactionImpl`.
- Module-level `fn format_dt(dt: &PrimitiveDateTime) -> Result<String, DaoError>` (verbatim aus RESEARCH line 857-863) — **NICHT aus assembly.rs::parse_datetime importieren**, weil format_dt im selben Modul nur lokal benötigt wird (helper_token.rs hat ein analoges lokales format_dt).
- `#[derive(Debug, sqlx::FromRow)] struct AttendanceMemberRowDb { id: Vec<u8>, member_number: i64, first_name: String, last_name: String, salutation: Option<String>, title: Option<String>, is_present: i64 }` — i64 für CASE-WHEN-Ergebnis (SQLite gibt INTEGER).
- `pub struct AttendanceDaoImpl { pub pool: Arc<SqlitePool> }` + `pub fn new(pool) -> Self`.
- `impl AttendanceDao for AttendanceDaoImpl { type Transaction = TransactionImpl; ... }` mit den 5 Methods:

**upsert_present** — verbatim SQL (D-05, RESEARCH Pattern 1):
```rust
sqlx::query(
    "INSERT INTO attendance (assembly_id, member_id, marked_at, marked_by_user_id, deleted) \
     VALUES (?, ?, ?, ?, NULL) \
     ON CONFLICT(assembly_id, member_id) DO UPDATE SET \
        marked_at = excluded.marked_at, \
        marked_by_user_id = excluded.marked_by_user_id, \
        deleted = NULL",
)
.bind(assembly_id.as_bytes().to_vec())
.bind(member_id.as_bytes().to_vec())
.bind(format_dt(&marked_at)?)
.bind(marked_by_user_id.to_string())
.execute(tx.tx.lock().await.as_mut())
.await
.map_err(|e| DaoError::DatabaseError(Arc::from(e.to_string())))?;
Ok(())
```

**soft_delete** — UPDATE; **rows_affected wird IGNORIERT** (D-06: idempotent No-Op auf nicht-existierender Row):
```rust
sqlx::query(
    "UPDATE attendance SET deleted = ? WHERE assembly_id = ? AND member_id = ?",
)
.bind(format_dt(&deleted_at)?)
.bind(assembly_id.as_bytes().to_vec())
.bind(member_id.as_bytes().to_vec())
.execute(tx.tx.lock().await.as_mut())
.await
.map_err(|e| DaoError::DatabaseError(Arc::from(e.to_string())))?;
Ok(())
```

**list_members_for_assembly** — verbatim SQL (D-25, RESEARCH Pattern 3, lines 935-979):
```rust
let aid = assembly_id.as_bytes().to_vec();
let pattern: Option<String> = search.map(|s| format!("%{}%", s.trim()));
let rows = sqlx::query_as::<_, AttendanceMemberRowDb>(
    "SELECT \
        m.id, m.member_number, m.first_name, m.last_name, \
        m.salutation, m.title, \
        CASE WHEN a.assembly_id IS NOT NULL AND a.deleted IS NULL THEN 1 ELSE 0 END AS is_present \
     FROM assembly_member_snapshot s \
     JOIN member m ON m.id = s.member_id AND m.deleted IS NULL \
     LEFT JOIN attendance a \
         ON a.assembly_id = s.assembly_id AND a.member_id = m.id \
     WHERE s.assembly_id = ? \
       AND ( ? IS NULL \
             OR (m.last_name || ' ' || m.first_name) LIKE ? COLLATE NOCASE \
             OR CAST(m.member_number AS TEXT) LIKE ? \
           ) \
     ORDER BY m.last_name COLLATE NOCASE, m.first_name COLLATE NOCASE",
)
.bind(aid)
.bind(pattern.as_deref())
.bind(pattern.as_deref())
.bind(pattern.as_deref())
.fetch_all(tx.tx.lock().await.as_mut())
.await
.map_err(|e| DaoError::DatabaseError(Arc::from(e.to_string())))?;

let result: Vec<AttendanceMemberRow> = rows.into_iter().map(|r| {
    AttendanceMemberRow {
        member_id: Uuid::from_slice(&r.id).expect("invalid member uuid"),
        member_number: r.member_number,
        first_name: Arc::from(r.first_name.as_str()),
        last_name: Arc::from(r.last_name.as_str()),
        salutation: r.salutation.map(|s| Arc::from(s.as_str())),
        title: r.title.map(|s| Arc::from(s.as_str())),
        is_present: r.is_present != 0,
    }
}).collect();
Ok(Arc::from(result))
```

**count_present_by_assembly** — query_scalar (RESEARCH Pattern 4):
```rust
let aid = assembly_id.as_bytes().to_vec();
let count = sqlx::query_scalar::<_, i64>(
    "SELECT COUNT(*) FROM attendance WHERE assembly_id = ? AND deleted IS NULL",
)
.bind(aid)
.fetch_one(tx.tx.lock().await.as_mut())
.await
.map_err(|e| DaoError::DatabaseError(Arc::from(e.to_string())))?;
Ok(count as u64)
```

**is_in_snapshot** — query_scalar mit COUNT auf assembly_member_snapshot:
```rust
let aid = assembly_id.as_bytes().to_vec();
let mid = member_id.as_bytes().to_vec();
let count = sqlx::query_scalar::<_, i64>(
    "SELECT COUNT(*) FROM assembly_member_snapshot \
     WHERE assembly_id = ? AND member_id = ?",
)
.bind(aid)
.bind(mid)
.fetch_one(tx.tx.lock().await.as_mut())
.await
.map_err(|e| DaoError::DatabaseError(Arc::from(e.to_string())))?;
Ok(count > 0)
```

**Schritt 2 — Test-Modul** am Ende der Datei (`#[cfg(test)] mod tests`).

Setup-Helper (am Anfang des Test-Moduls): `async fn setup() -> (Arc<SqlitePool>, TransactionImpl, Uuid /* aid */, Uuid /* mid_in_snap */, Uuid /* mid_not_in_snap */)`:
- Connect zu `sqlite::memory:`.
- `sqlx::migrate!("../../migrations/sqlite").run(&pool).await.unwrap();` (Pfad relativ zu genossi_dao_impl_sqlite/src — falls Pfad nicht korrekt, prüfe Vorbild-Tests in helper_token.rs).
- 1 Assembly via direktem SQL INSERT (oder reuse helpers aus assembly.rs::tests); 2 Member mit member_number 100/200 und last_name "Müller"/"Schmidt"; 1 Snapshot-Row für mid_in_snap.
- Begin TransactionImpl.

Schreibe die 6 Tests aus `<behavior>`. Verbatim-SQL-Asserts gegen die DB (z. B. `sqlx::query_scalar::<_, i64>("SELECT COUNT(*) FROM attendance WHERE assembly_id=? AND member_id=?").bind(...).fetch_one(...)`).

**Schritt 3 — `genossi_dao_impl_sqlite/src/lib.rs` modifizieren**: füge `pub mod attendance;` hinzu — alphabetisch nach `pub mod assembly_member_snapshot;`.

**Was NICHT machen:**
- KEINE `sqlx::query!` / `sqlx::query_as!` Macros (Pitfall 1+2: würden `cargo sqlx prepare` verlangen). Nutze ausschließlich `sqlx::query(...)`, `sqlx::query_as::<_, T>(...)`, `sqlx::query_scalar::<_, T>(...)`.
- KEIN RETURNING im UPSERT (D-05: nur INSERT … ON CONFLICT … DO UPDATE).
- KEIN partial UNIQUE-Index als CONFLICT-Target — Composite-PK reicht (D-04, Discretion §1).
- KEINE `audited_*!`-Macros (D-08).
  </action>
  <verify>
    <automated>cargo build -p genossi_dao_impl_sqlite 2>&amp;1 | tail -10 &amp;&amp; cargo test -p genossi_dao_impl_sqlite attendance --no-fail-fast -- --nocapture 2>&amp;1 | tail -60</automated>
  </verify>
  <done>
    - `cargo build -p genossi_dao_impl_sqlite` exits 0.
    - `cargo test -p genossi_dao_impl_sqlite attendance` exits 0; mindestens 6 Tests laufen.
    - `grep -c 'pub mod attendance' genossi_dao_impl_sqlite/src/lib.rs` = 1.
    - `grep -c 'ON CONFLICT(assembly_id, member_id) DO UPDATE' genossi_dao_impl_sqlite/src/attendance.rs` = 1.
    - `grep -c 'CASE WHEN a.assembly_id IS NOT NULL AND a.deleted IS NULL THEN 1 ELSE 0 END AS is_present' genossi_dao_impl_sqlite/src/attendance.rs` = 1.
    - `grep -c 'COLLATE NOCASE' genossi_dao_impl_sqlite/src/attendance.rs` ≥ 3 (filter + 2 in ORDER BY).
    - `grep -cE 'sqlx::query!|sqlx::query_as!' genossi_dao_impl_sqlite/src/attendance.rs` = 0 (Pitfall 1+2 vermieden).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service → DAO | Service-Layer ruft DAO mit bereits validierten Eingaben (UUIDs, geprüfter assembly_id) auf. Im Plan 01-Scope wird der DAO ohne Auth-Funnel direkt im Test ausgeübt — produktive Aufrufe folgen erst in Plan 05. |
| Test → DB | sqlite::memory: pro Test, vollständige Isolation. |

## STRIDE Threat Register

Siehe Frontmatter `threat_model:` — vier Threats (T-03-01-01 bis T-03-01-04) mit Mitigations bzw. expliziten Acceptance-Begründungen. Wichtigste:
- **T-03-01-01** Tampering via SQL-Injection: parameterisierte Bind-Parameter (sqlx::query.bind), keine String-Konkatenation.
- **T-03-01-02** PII-Leak: SELECT-Whitelist auf 7 Spalten, kein SELECT m.*.
- **T-03-01-03** DoS via Substring-Search: akzeptiert für Genossenschafts-Größenordnung; Re-Evaluierung in Phase 5.
- **T-03-01-04** FK-Bypass: durch PRAGMA foreign_keys=OFF documentary; defense-in-depth über D-27-Service-Check (in Plan 05).
</threat_model>

<verification>
- `cargo build` exits 0.
- `cargo test -p genossi_dao -p genossi_dao_impl_sqlite attendance` exits 0.
- `cargo fmt -- --check` exits 0.
- `cargo clippy -p genossi_dao -p genossi_dao_impl_sqlite --no-deps -- -D warnings` exits 0.
- Migration auto-run beim Server-Start funktioniert (Verifikation indirekt über Plan-06-E2E-Tests, in diesem Plan nicht direkt).
</verification>

<success_criteria>
- ATTN-03 (idempotenter PUT) auf SQL-Ebene erfüllt: Test 1 verifiziert 5x UPSERT = 1 Row.
- ATTN-04 (idempotentes DELETE) auf SQL-Ebene erfüllt: Test 3 verifiziert No-Op.
- SYNC-02 (Race) auf SQL-Ebene vorbereitet: UPSERT-Atomicity ist DB-Eigenschaft, der eigentliche tokio::join!-Race-Test landet in Plan 06.
- AttendanceDao-Trait + SQLite-Impl bereit für Service-Konsumption in Plan 05.
- Migration läuft sauber ohne Schema-Konflikt.
</success_criteria>

<output>
Nach Abschluss: erstelle `.planning/phases/03-attendance-aggregat-cascade-invalidation/03-01-SUMMARY.md` mit:
- Liste der erstellten/modifizierten Files
- SQL-Schema der attendance-Tabelle (verbatim)
- Trait-Signatur AttendanceDao (verbatim)
- Test-Liste + Status (alle grün)
- Verweis auf Plan 02/03/04 als Wave-1-Parallelplans, auf Plan 05 als Konsument
</output>
</output>
