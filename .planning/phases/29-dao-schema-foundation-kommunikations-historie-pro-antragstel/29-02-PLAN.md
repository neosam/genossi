---
phase: 29-dao-schema-foundation-kommunikations-historie-pro-antragstel
plan: 02
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - genossi_mail/src/dao.rs
  - genossi_mail/src/dao_sqlite.rs
  - genossi_mail/src/service.rs
  - genossi_service_impl/src/application.rs
  - genossi_bin/tests/e2e_tests.rs
autonomous: true
requirements: [APHIST-01, APHIST-03]

must_haves:
  truths:
    - "get_application_communications(application_id) liefert die als Antragsteller gesendete Mail als outbound-only Historie-Eintrag zurueck; fuer eine fremde application_id liefert sie keine Zeilen; Soft-Delete-Filter (r.deleted IS NULL AND j.deleted IS NULL) wird respektiert — APHIST-01, Read-Haelfte von Success-Kriterium 1 (DAO-Ebene, KEIN REST-Endpoint in dieser Phase)."
    - "Nach confirm() einer Application erscheint die zuvor als Antragsteller gesendete Erinnerung in der Mitglieds-Timeline des neuen Mitglieds (e2e: seed → confirm → get_member_communications(new_member_id) enthaelt Eintrag) — APHIST-03 / Success-Kriterium 3, via D2 Option A."
    - "Der Carry-over setzt AUSSCHLIESSLICH die genuine neue member_id (aus uuid_service.new_v4, application.rs:326) auf mail_recipients-Zeilen mit passendem application_id — NIEMALS die Application-UUID (Pitfall 2 gewahrt); Member-Timeline-Query bleibt unveraendert."
    - "Der Back-fill laeuft post-commit best-effort (nach commit(tx), application.rs:541), NICHT atomar in confirm()s Transaktion — Mail-DAO auf separater Pool-Connection; Fehler loggt tracing::warn! und rollt confirm() NICHT zurueck."
  artifacts:
    - "genossi_mail/src/dao.rs — CommunicationDao::get_application_communications + MailRecipientDao::link_application_to_member (Trait-Methoden, #[automock] erweitert Mocks automatisch)"
    - "genossi_mail/src/dao_sqlite.rs — Impl beider Methoden (outbound-only Query + UPDATE) + DAO-Tests"
    - "genossi_mail/src/service.rs — MailService::link_application_recipients_to_member + Impl"
    - "genossi_service_impl/src/application.rs — post-commit best-effort Carry-over-Hook in confirm() + angepasste Mock-Erwartungen der confirm()-Erfolgs-Tests"
    - "genossi_bin/tests/e2e_tests.rs — e2e Carry-over-Test (Erinnerung → confirm → sichtbar in Member-Timeline)"
  key_links:
    - "confirm() (post-commit :541) → mail_service.link_application_recipients_to_member(id, member_id) → recipient_dao.link_application_to_member → UPDATE mail_recipients SET member_id=? WHERE application_id=? AND member_id IS NULL"
    - "get_member_communications(new_member_id) (UNVERAENDERT) liest die zurueckgeschriebene Zeile → Carry-over sichtbar"
    - "get_application_communications(application_id) → outbound-only SELECT (WHERE r.application_id=?1) → CommunicationEntry (subjekt-agnostisch, kein Struct-Change)"
---

<objective>
Aufbauend auf der `application_id`-Linkage aus Plan 29-01 liefert dieser Plan (a) die outbound-only
Read-Methode `get_application_communications` fuer die Antragsteller-Timeline (DAO-Ebene) und
(b) den D2-Carry-over: nach `confirm()` wird die genuine neue `member_id` auf die passenden
`mail_recipients`-Zeilen zurueckgeschrieben (post-commit best-effort), sodass die als Antragsteller
gesendete Erinnerung automatisch in der UNVERAENDERTEN Mitglieds-Timeline-Query erscheint.

Purpose: APHIST-01 (Read) + APHIST-03 (Carry-over). D2 = **Option A** (Back-fill echte member_id) —
laut 29-RESEARCH.md die einzige Variante, die ohne Audit-Ripple und ohne Umbau der getesteten
Member-Timeline-Query auskommt (Option C brauecht ein Feld auf der auditierten ApplicationEntity und
briecht `test_auditable_fields_count == 11`; Option B brauecht faktisch denselben Stored-Link).

Output: Zwei neue DAO-Methoden + eine MailService-Methode + ein confirm()-Hook + DAO-Tests + e2e.
KEIN REST-Endpoint (`GET /api/applications/{id}/communications` ist Phase-31-Scope; hier nur
DAO-Verifikation). KEIN neues DI-Wiring in genossi_bin (CommunicationDao/MailRecipientDao/MailService
sind bereits verdrahtet; neue Trait-Methoden erfordern kein Re-Wiring).
</objective>

<execution_context>
@/home/neosam/programming/projects/genossi/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/projects/genossi/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-dao-schema-foundation-kommunikations-historie-pro-antragstel/29-RESEARCH.md
@.planning/phases/29-dao-schema-foundation-kommunikations-historie-pro-antragstel/29-PATTERNS.md
@.planning/phases/29-dao-schema-foundation-kommunikations-historie-pro-antragstel/29-01-PLAN.md
</context>

<artifacts_this_phase_produces>
Symbole, die Plan 29-02 neu erzeugt (fuer Phase 31 relevant):
- **`CommunicationDao::get_application_communications(application_id) -> Result<Arc<[CommunicationEntry]>, MailDaoError>`** (genossi_mail/src/dao.rs, Impl dao_sqlite.rs) — outbound-only; Phase 31 mountet den REST-Endpoint darauf.
- **`MailRecipientDao::link_application_to_member(application_id, member_id) -> Result<(), MailDaoError>`** (genossi_mail/src/dao.rs, Impl dao_sqlite.rs) — UPDATE-Back-fill.
- **`MailService::link_application_recipients_to_member(application_id, member_id) -> Result<(), MailServiceError>`** (genossi_mail/src/service.rs) — Service-Fassade fuer den confirm()-Hook.
- confirm()-Post-Commit-Carry-over-Hook (genossi_service_impl/src/application.rs).

Voraussetzung (aus 29-01): `mail_recipients.application_id`-Spalte + Struct-Feld + Persistenz existieren bereits.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DAO get_application_communications (outbound-only) + link_application_to_member (UPDATE)</name>
  <files>genossi_mail/src/dao.rs, genossi_mail/src/dao_sqlite.rs</files>
  <read_first>
    - genossi_mail/src/dao.rs:258-285 (CommunicationEntry subjekt-agnostisch :259; CommunicationDao-Trait :280, get_member_communications :281; #[automock] :278)
    - genossi_mail/src/dao.rs:91-107 (MailRecipientDao-Trait, #[automock] :91; bestehende Methoden create/find_by_job_id/next_pending/update)
    - genossi_mail/src/dao_sqlite.rs:1042-1097 (get_member_communications-Impl: inbound UNION outbound; Outbound-Zweig :1067-1084 = Klon-Vorlage; Soft-Delete-Filter :1082-1084; alle 12 SELECT-Spalten fuer CommunicationEntryDb-FromRow)
    - genossi_mail/src/dao_sqlite.rs:330-345 (update() als Muster fuer ein einfaches parametrisiertes UPDATE) und :47 (parse_optional_uuid) und :285 (self.pool.as_ref())
  </read_first>
  <behavior>
    - get_application_communications: gegebene application_id mit einer gesendeten outbound-Zeile → genau dieser Eintrag zurueck (direction outbound, korrekte subject/to_address/status).
    - get_application_communications: fremde application_id (keine Zeilen) → leeres Arr.
    - get_application_communications: Zeile mit r.deleted gesetzt ODER zugehoeriger Job mit j.deleted gesetzt → NICHT zurueckgeliefert (Soft-Delete-Filter).
    - get_application_communications: KEIN inbound-Eintrag taucht auf (Antragsteller haben keine assigned_member_id — nur der Outbound-Zweig existiert).
    - link_application_to_member: UPDATE setzt member_id auf allen mail_recipients-Zeilen mit passendem application_id UND member_id IS NULL; Zeilen fremder application_id bleiben unangetastet; anschliessend liefert get_member_communications(new_member_id) den Eintrag.
  </behavior>
  <action>
    1. `genossi_mail/src/dao.rs`:
       - `CommunicationDao`-Trait (:280) um
         `async fn get_application_communications(&self, application_id: Uuid) -> Result<Arc<[CommunicationEntry]>, MailDaoError>;`
         erweitern (neben get_member_communications). `CommunicationEntry` NICHT aendern.
       - `MailRecipientDao`-Trait (:93) um
         `async fn link_application_to_member(&self, application_id: Uuid, member_id: Uuid) -> Result<(), MailDaoError>;`
         erweitern.
       - `#[automock]` bleibt auf beiden Traits — MockCommunicationDao/MockMailRecipientDao erhalten die
         neuen Methoden automatisch.
    2. `genossi_mail/src/dao_sqlite.rs`:
       - Impl `get_application_communications`: reduzierter Klon von `get_member_communications`. NUR der
         Outbound-Zweig (den inbound-`SELECT ... UNION ALL` WEGLASSEN), `WHERE r.application_id = ?1`
         statt `r.member_id = ?1`, Soft-Delete-Filter `AND r.deleted IS NULL AND j.deleted IS NULL`
         BEIBEHALTEN, `ORDER BY date DESC`. ALLE 12 SELECT-Spalten (inkl. der `NULL AS inbox_id` /
         `NULL AS from_address` / `NULL AS inbound_*`-Platzhalter) beibehalten, damit
         `query_as::<_, CommunicationEntryDb>` per FromRow passt. Bind `application_id.as_bytes().to_vec()`,
         `fetch_all(self.pool.as_ref())`.
       - Impl `link_application_to_member`: `UPDATE mail_recipients SET member_id = ? WHERE application_id = ? AND member_id IS NULL`
         mit Binds `member_id.as_bytes().to_vec()` und `application_id.as_bytes().to_vec()`, ausgefuehrt
         auf `self.pool.as_ref()`. Bewusst `member_id IS NULL` in der WHERE-Klausel, damit bereits
         zugeordnete Zeilen nicht ueberschrieben werden.
    3. DAO-Tests im `#[cfg(test)]`-Modul (In-Memory-SQLite, bestehende Infrastruktur) entsprechend der
       fuenf Behavior-Punkte oben: outbound-Roundtrip, fremde application_id → leer, Soft-Delete
       (Recipient- und Job-deleted) → ausgeschlossen, link → get_member_communications sichtbar.
  </action>
  <acceptance_criteria>
    - `cargo test -p genossi_mail` exit 0; die neuen DAO-Tests decken alle fuenf Behavior-Punkte ab.
    - `get_application_communications`-Query enthaelt `WHERE r.application_id = ?1` und `r.deleted IS NULL AND j.deleted IS NULL`, aber KEINEN `inbound`/`UNION`-Zweig (Source-Assertion outbound-only).
    - `link_application_to_member`-UPDATE enthaelt `SET member_id = ?` und `WHERE application_id = ? AND member_id IS NULL` (Source-Assertion Pitfall 2: setzt member_id, gefiltert; nie application-uuid nach member_id).
    - Bestehende `get_member_communications`-Tests bleiben gruen (Member-Query unveraendert).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p genossi_mail 2>&amp;1 | tail -15</automated>
  </verify>
  <done>Beide DAO-Methoden implementiert und getestet; outbound-only Read korrekt (inkl. Soft-Delete + fremd-application_id-Ausschluss); link_application_to_member schreibt genuine member_id gefiltert zurueck; Member-Query unveraendert.</done>
</task>

<task type="auto">
  <name>Task 2: MailService::link_application_recipients_to_member + confirm() post-commit Carry-over-Hook</name>
  <files>genossi_mail/src/service.rs, genossi_service_impl/src/application.rs</files>
  <read_first>
    - genossi_mail/src/service.rs:66-149 (MailService-Trait, #[automock] :66; bestehende Methoden als Signatur-Muster) und :291-330 (MailServiceImpl-Struct, `recipient_dao: Arc<R>` :301) und :622-628 (get_reached_member_ids: Muster `self.recipient_dao.<methode>` Delegation)
    - genossi_service_impl/src/application.rs:26-40 (ApplicationServiceDeps; `MailService = mail_service` :39 — bereits vorhandene Dep, KEINE neue) und :288-341 (confirm-Start, member_id = uuid_service.new_v4 :326) und :541-556 (commit(tx) :541 + best-effort Datei-Cleanup :547-555 = exaktes Hook-Muster)
    - genossi_service_impl/src/application.rs:987 (MockMailService::new im Test-Setup), :1058-1176 (test_confirm_with_document_... — Erfolgs-confirm, erreicht post-commit), :1192-1250 (test_confirm_without_document_skips_carryover — Erfolgs-confirm, erreicht post-commit), :996-1047 (perm-denied: commit times(0)), :1263-1331 (rollback: kein commit)
  </read_first>
  <action>
    1. `genossi_mail/src/service.rs`:
       - `MailService`-Trait (~nach :149) um
         `async fn link_application_recipients_to_member(&self, application_id: Uuid, member_id: Uuid) -> Result<(), MailServiceError>;`
         erweitern. `#[automock]` erzeugt die Mock-Methode automatisch.
       - Impl auf `MailServiceImpl`: delegiert an
         `self.recipient_dao.link_application_to_member(application_id, member_id).await?` (Muster:
         get_reached_member_ids-Delegation :622-628); `MailDaoError` konvertiert via bestehendem
         `From<MailDaoError> for MailServiceError` (service.rs:29) zu `MailServiceError`.
    2. `genossi_service_impl/src/application.rs` — Carry-over-Hook in `confirm()` NACH `commit(tx)`
       (:541), als weiterer best-effort-Schritt neben dem Datei-Cleanup (Muster :547-555): rufe
       `self.mail_service.link_application_recipients_to_member(id, member_id)` auf (`id` =
       application_id-Parameter, `member_id` = die genuine neue Member-UUID aus :326). Bei Fehler
       `tracing::warn!(application_id = %id, member_id = %member_id, error = ?e, "...best-effort")` —
       KEIN `?`, KEIN Rollback (Mail-DAO laeuft auf separater Pool-Connection, nie atomar in `tx`;
       29-RESEARCH.md:204-208). NIEMALS `application.id` in ein `member_id`-Feld schreiben — der Hook
       uebergibt member_id und application_id als getrennte Argumente.
    3. Mock-Erwartungen der confirm()-Erfolgs-Tests anpassen: `test_confirm_with_document_...` (:1058)
       und `test_confirm_without_document_skips_carryover` (:1192) erreichen den Post-Commit-Hook →
       ihrem `MockMailService` je
       `.expect_link_application_recipients_to_member().returning(|_, _| Ok(()))` hinzufuegen.
       `test_confirm_cr02_permission_denied_...` (:996, commit times(0)) und
       `test_confirm_missing_file_rolls_back_...` (:1263, kein commit) erreichen den Hook NICHT →
       keine Erwartung noetig (mockall panickt nur bei ungesetzt-aber-aufgerufen).
  </action>
  <acceptance_criteria>
    - `cargo test -p genossi_mail` und `cargo test -p genossi_service_impl` exit 0.
    - Der confirm()-Hook steht NACH `self.transaction_dao.commit(tx)` (:541) und nutzt `tracing::warn!` statt `?` (Source-Assertion: post-commit best-effort, kein Rollback).
    - `link_application_recipients_to_member` delegiert an `recipient_dao.link_application_to_member` (Source-Assertion).
    - Die beiden confirm()-Erfolgs-Tests haben eine `expect_link_application_recipients_to_member`-Erwartung; sie bleiben gruen.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p genossi_mail -p genossi_service_impl 2>&amp;1 | tail -20</automated>
  </verify>
  <done>MailService-Methode + confirm()-Post-Commit-Hook implementiert; best-effort (warn, kein Rollback); confirm()-Unit-Tests mit Mock-Erwartung gruen; keine neue DI-Dep noetig.</done>
</task>

<task type="auto">
  <name>Task 3: e2e Carry-over — Erinnerung → confirm → sichtbar in Member-Timeline</name>
  <files>genossi_bin/tests/e2e_tests.rs</files>
  <read_first>
    - genossi_bin/tests/e2e_tests.rs:6740 (test_confirm_application_creates_member — Setup Application Offen + confirm + Member-Assert) und :6867 (application_upload_confirm_carryover_audited — Phase-25-Carry-over-Vorbild)
    - genossi_bin/tests/e2e_tests.rs:4799 (seed_inbound_mail — Muster fuer direktes DAO/Pool-Seeding im e2e) — analog Vorgehen zum Seeden einer mail_recipients-Zeile mit application_id
    - genossi_mail/src/service.rs create_job-Signatur (:356-367) — falls die Erinnerung ueber den MailService statt roh via Pool geseedet wird
    - 29-RESEARCH.md:431-433 (e2e-Hinweis: Seeding via DAO/Service, NICHT via HTTP — POST /api/applications/{id}/mail kommt erst Phase 31)
  </read_first>
  <action>
    Neuen e2e-Test in `genossi_bin/tests/e2e_tests.rs` (bestehende Confirm-e2e-Tests als Vorbild):
    1. Setup: Test-Server/Pool starten, eine Application mit Status `Offen` anlegen (Muster
       test_confirm_application_creates_member).
    2. Erinnerung seeden: EINE `mail_recipients`-Zeile mit gesetztem `application_id` (= app.id) und
       `member_id: None`, Status `sent`, mit zugehoerigem `mail_jobs`-Eintrag (Subject z.B.
       "Zahlungserinnerung") — direkt via Mail-Service/DAO oder rohem Pool-Insert (KEIN HTTP-Send-
       Endpoint; der ist Phase 31). Anschliessend optional per `get_application_communications(app.id)`
       verifizieren, dass die Erinnerung als outbound-Eintrag sichtbar ist.
    3. `confirm(app.id)` ueber den regulaeren Pfad (Service oder HTTP-confirm-Endpoint wie in den
       bestehenden confirm-e2e) → neues Mitglied entsteht (new_member_id).
    4. Assert: `get_member_communications(new_member_id)` enthaelt die geseedete Erinnerung (der
       post-commit Back-fill hat member_id zurueckgeschrieben). Zusaetzlich asserten, dass die
       Erinnerung eben NICHT ueber eine Application-UUID-in-member_id-Kopplung sichtbar ist, sondern
       ueber die genuine new_member_id (Pitfall 2).
  </action>
  <acceptance_criteria>
    - `cargo test -p genossi_bin --test e2e_tests <neuer_testname>` exit 0.
    - Der Test seedet die Erinnerung mit `application_id = app.id` UND `member_id = None` (Behavior-Assertion Pitfall 2).
    - Nach `confirm()` liefert `get_member_communications(new_member_id)` die Erinnerung (Success-Kriterium 3 / APHIST-03).
    - Der Test nutzt KEINEN `POST /api/applications/{id}/mail`-Aufruf (Scope-Grenze: Endpoint ist Phase 31).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p genossi_bin --test e2e_tests 2>&amp;1 | tail -20</automated>
  </verify>
  <done>e2e beweist: Antragsteller-Erinnerung (via application_id geseedet, member_id None) erscheint nach confirm() in der Timeline des neuen Mitglieds, via genuiner new_member_id — kein HTTP-Send-Endpoint verwendet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `confirm()` (Service-Tx) → Mail-DAO (separate Pool-Connection) | Der Carry-over-UPDATE laeuft ausserhalb der confirm()-Transaktion — Konsistenz ist best-effort, nicht atomar |
| Application-Timeline-Read → Antragsteller-Daten | outbound-only Query bestimmt, welche Kommunikation einem Antragsteller/Mitglied zugeordnet sichtbar ist |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-02 | Tampering / Information Disclosure | Carry-over `link_application_to_member` UPDATE | high | mitigate | UPDATE setzt ausschliesslich die genuine neue member_id (uuid_service.new_v4, application.rs:326), `WHERE application_id = ? AND member_id IS NULL`; NIE die Application-UUID in member_id. e2e (Task 3) verifiziert, dass genau die richtige Member-Timeline die Erinnerung sieht — verhindert Sichtbarkeit fremder Timeline-Daten. |
| T-29-03 | Information Disclosure | `get_application_communications`-Query (DSGVO-Datenminimierung) | medium | mitigate | Outbound-only (inbound-UNION-Zweig bewusst weggelassen — Antragsteller haben keine assigned_member_id), `WHERE r.application_id = ?1`, Soft-Delete-Filter `r.deleted/j.deleted IS NULL` beibehalten; DAO-Test deckt fremd-application_id → leer und deleted → ausgeschlossen ab. |
| T-29-06 | Denial of Service (Konsistenz) | best-effort Post-Commit-Back-fill schlaegt fehl | low | accept | Mail-DAO auf separater Pool-Connection → Back-fill kann nicht atomar sein; Fehler loggt tracing::warn!, Mitglied bleibt korrekt erstellt, Carry-over-Zeile per Re-Run nachholbar (recoverable, konsistent mit bestehendem Post-Commit-Best-Effort). |
| T-29-SC | Tampering | npm/pip/cargo installs | low | accept | Keine neue Dependency ("add nothing"); kein Package-Install-Task in dieser Phase. |
</threat_model>

<verification>
- `cargo test -p genossi_mail -p genossi_service_impl` gruen (DAO-Methoden + MailService-Methode + confirm()-Hook).
- `cargo test -p genossi_bin --test e2e_tests` gruen inkl. Carry-over-e2e.
- get_application_communications ist outbound-only + Soft-Delete-korrekt; Member-Query unveraendert.
- Carry-over setzt nur genuine member_id, post-commit best-effort.
</verification>

<success_criteria>
- Antragsteller-Timeline auf DAO-Ebene lesbar (get_application_communications), outbound-only.
- D2 Option A: nach confirm() ist die Erinnerung in der Member-Timeline des neuen Mitglieds sichtbar (e2e).
- member_id-Namespace sauber (nur genuine member_id zurueckgeschrieben); confirm()-Rollback-Semantik unberuehrt (best-effort).
- Kein REST-Endpoint, kein neues DI-Wiring, keine neue Dependency, kein Audit-Feld.
</success_criteria>

<output>
Create `.planning/phases/29-dao-schema-foundation-kommunikations-historie-pro-antragstel/29-02-SUMMARY.md` when done
</output>
