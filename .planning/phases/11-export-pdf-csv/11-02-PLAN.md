---
phase: 11-export-pdf-csv
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - genossi_service/src/repayment_export.rs
  - genossi_service/src/lib.rs
autonomous: true
requirements:
  - EXPO-03
user_setup: []
tags:
  - service-trait
  - domain-types
  - export
must_haves:
  truths:
    - "`pub trait RepaymentExportService` existiert mit `#[automock]` und `async export(phase_id, format, include, context) -> Result<RepaymentExport, ServiceError>`"
    - "`ExportFormat`-Enum hat genau eine Variante: `Pdf` (D-12: CSV gestrichen)"
    - "`ExportInclude`-Enum hat 3 Varianten: `Open`, `All`, `Paid` mit `Default = Open` (D-03)"
    - "`RepaymentExport`-Bundle-Struct hat `bytes`, `content_type`, `filename` und manuelles Debug-Impl mit `bytes_len` (nicht Bytes-Dump, Pitfall #6)"
    - "Modul wird in `genossi_service/src/lib.rs` als `pub mod repayment_export;` exportiert"
  artifacts:
    - path: "genossi_service/src/repayment_export.rs"
      provides: "Service-Trait + Domain-Types ExportFormat/ExportInclude/RepaymentExport"
      min_lines: 80
    - path: "genossi_service/src/lib.rs"
      provides: "Modul-Deklaration `pub mod repayment_export;`"
      contains: "pub mod repayment_export"
  key_links:
    - from: "genossi_service/src/lib.rs"
      to: "genossi_service/src/repayment_export.rs"
      via: "pub mod repayment_export;"
      pattern: "pub mod repayment_export"
---

<objective>
Foundation-Plan Wave 1: legt das Service-Trait-Modul `genossi_service::repayment_export` an mit `RepaymentExportService`-Trait, den Domain-Typen `ExportFormat` (nur Pdf laut D-12), `ExportInclude` (Open/All/Paid mit Default=Open laut D-03) und dem `RepaymentExport`-Bundle (bytes/content_type/filename).

Purpose: Trait-Definition lebt im `genossi_service`-Crate (Interface-Schicht), Impl spaeter im `genossi_service_impl`-Crate (Plan 11.03). Diese Trennung erlaubt Mock-basiertes Testen und entspricht der Genossi-Layered-Architecture-Konvention.

Output: 1 neue Datei (`genossi_service/src/repayment_export.rs`) + 1 Edit (`genossi_service/src/lib.rs` Modul-Deklaration). 1:1-Replikat von `attendance_export.rs` (158 LOC) mit angepassten Enum-Varianten.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-export-pdf-csv/11-CONTEXT.md
@.planning/phases/11-export-pdf-csv/11-RESEARCH.md
@.planning/phases/11-export-pdf-csv/11-PATTERNS.md

@genossi_service/src/attendance_export.rs
@genossi_service/src/lib.rs

<interfaces>
<!-- Existing types this plan depends on. -->

From genossi_service/src/permission.rs:
```rust
pub enum Authentication<C> {
    Full,
    Context(C),
}
```

From genossi_service/src/lib.rs:
```rust
pub enum ServiceError {
    DataAccess(Arc<str>),
    EntityNotFound(uuid::Uuid),
    ValidationError(Vec<ValidationFailureItem>),
    PermissionDenied,
    InternalError(Arc<str>),
    Conflict(Arc<str>),
    Unauthorized,
    SessionExpired,
    AuthenticationFailed,
}
```

From genossi_service/src/attendance_export.rs (1:1-Vorbild):
```rust
pub enum ExportFormat { Csv, Pdf, Xlsx }
pub enum ExportInclude { All, Present }
impl Default for ExportInclude { fn default() -> Self { ExportInclude::All } }

pub struct AttendanceExport {
    pub bytes: Vec<u8>,
    pub content_type: &'static str,
    pub filename: String,
}
impl std::fmt::Debug for AttendanceExport { /* prints bytes_len, not bytes */ }

#[automock(type Context=(); type Transaction = genossi_dao::MockTransaction;)]
#[async_trait]
pub trait AttendanceExportService {
    type Context: Clone + Debug + Send + Sync + 'static;
    type Transaction: genossi_dao::Transaction;
    async fn export(
        &self,
        assembly_id: Uuid,
        format: ExportFormat,
        include: ExportInclude,
        context: Authentication<Self::Context>,
    ) -> Result<AttendanceExport, ServiceError>;
}
```

<!-- New types this plan creates (consumed by plans 11.03 / 11.04 / 11.05 / 11.06). -->

To be created in genossi_service/src/repayment_export.rs:
```rust
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum ExportFormat { Pdf }   // D-12: NUR Pdf, kein Csv/Xlsx

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum ExportInclude { Open, All, Paid }
impl Default for ExportInclude { fn default() -> Self { ExportInclude::Open } }  // D-03

pub struct RepaymentExport {
    pub bytes: Vec<u8>,
    pub content_type: &'static str,
    pub filename: String,
}
// Custom Debug — bytes_len, not bytes (Pitfall #6)

#[automock(type Context=(); type Transaction = genossi_dao::MockTransaction;)]
#[async_trait]
pub trait RepaymentExportService {
    type Context: Clone + Debug + Send + Sync + 'static;
    type Transaction: genossi_dao::Transaction;
    async fn export(
        &self,
        phase_id: Uuid,
        format: ExportFormat,
        include: ExportInclude,
        context: Authentication<Self::Context>,
    ) -> Result<RepaymentExport, ServiceError>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Service-Trait + Domain-Types + Unit-Tests</name>
  <files>
    genossi_service/src/repayment_export.rs (NEW)
    genossi_service/src/lib.rs (MODIFY)
  </files>
  <read_first>
    - genossi_service/src/attendance_export.rs (vollstaendig, 158 LOC — 1:1 Pattern-Vorlage; insbesondere Z. 17-23 Imports, Z. 27-49 Enums + Default, Z. 58-72 Bundle + Debug, Z. 74-96 Trait + automock, Z. 98-156 Unit-Tests)
    - genossi_service/src/lib.rs (Modul-Deklarationen — Position fuer neues `pub mod repayment_export;`)
    - genossi_service/src/permission.rs (Authentication<C>-Enum-Definition)
    - .planning/phases/11-export-pdf-csv/11-PATTERNS.md §2 (Pattern Assignment fuer genossi_service/src/repayment_export.rs)
    - .planning/phases/11-export-pdf-csv/11-CONTEXT.md (D-03 Default=Open, D-12 nur Pdf)
  </read_first>
  <behavior>
    - `ExportInclude::default()` ist `ExportInclude::Open` (D-03 lockd)
    - `ExportFormat` hat genau 1 Variante: `Pdf` (D-12 streicht Csv/Xlsx)
    - `RepaymentExport::Debug`-Output zeigt `bytes_len: N`, NICHT die Bytes selbst (Pitfall #6)
    - `RepaymentExportService::ExpectExport` (mockall-generiert) kompiliert
    - `From<ExportInclude> for ExportInclude` ist offensichtlich (Identity, kein Test noetig)
  </behavior>
  <action>
    SCHRITT 1: Lege Datei `genossi_service/src/repayment_export.rs` mit folgender Struktur an (1:1 angepasste Variante von `attendance_export.rs`):

    ```rust
    //! Phase 11 (EXPO-01..03, EXPO-05): RepaymentExportService trait + domain types.
    //!
    //! Read-only export of Auszahlungsliste (PDF) for RepaymentPhase.
    //! Vorbild: genossi_service/src/attendance_export.rs (Phase 6).
    //! Anpassungen:
    //!   - D-12: NUR Pdf (kein Csv/Xlsx)
    //!   - D-03: Default-Include = Open (Banking-Vorlage-Use-Case)
    //!   - D-10: Export erlaubt fuer Open ODER Closed (Impl in Plan 11.03)

    use async_trait::async_trait;
    use mockall::automock;
    use std::fmt::Debug;
    use uuid::Uuid;

    use crate::permission::Authentication;
    use crate::ServiceError;

    /// Output formats. D-12: NUR Pdf in Phase 11. Re-Add von Csv ist additiv
    /// (neue Variante hier + Match-Arm im Service-Impl + Format-Whitelist im REST).
    #[derive(Clone, Copy, Debug, PartialEq, Eq)]
    pub enum ExportFormat {
        Pdf,
    }

    /// Include filter for the export.
    /// D-01: Open  = RepaymentEntryStatus in {Open, Contacted}
    /// D-02: All   = Open + Contacted + PaidOut
    /// D-02: Paid  = nur PaidOut
    /// Soft-deleted entries/members werden in JEDEM Filter ausgeschlossen.
    #[derive(Clone, Copy, Debug, PartialEq, Eq)]
    pub enum ExportInclude {
        Open,
        All,
        Paid,
    }

    impl Default for ExportInclude {
        /// D-03: Default ist Open fuer Banking-Workflow ("noch nicht ausbezahlt").
        fn default() -> Self {
            ExportInclude::Open
        }
    }

    /// Export bundle returned by `RepaymentExportService::export`.
    /// Manuelles Debug-Impl: druckt `bytes_len` statt der Bytes, sonst Megabyte-Spam
    /// bei Test-Failures (Pitfall #6 aus 11-RESEARCH.md).
    pub struct RepaymentExport {
        pub bytes: Vec<u8>,
        pub content_type: &'static str,
        pub filename: String,
    }

    impl std::fmt::Debug for RepaymentExport {
        fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
            f.debug_struct("RepaymentExport")
                .field("bytes_len", &self.bytes.len())
                .field("content_type", &self.content_type)
                .field("filename", &self.filename)
                .finish()
        }
    }

    /// Phase 11 Read-only Export-Service fuer Auszahlungslisten.
    ///
    /// - Vorstand-only via PermissionService::check_permission("admin", ...) — D-11
    /// - Null `audited_*!`-Calls im Impl (Plan 11.03 Grep-Gate-Test) — EXPO-05
    /// - Permission-Funnel-Order: load_by_id (404) -> admin-check (403) -> status-check (409) — D-10
    #[automock(type Context=(); type Transaction = genossi_dao::MockTransaction;)]
    #[async_trait]
    pub trait RepaymentExportService {
        type Context: Clone + Debug + Send + Sync + 'static;
        type Transaction: genossi_dao::Transaction;

        async fn export(
            &self,
            phase_id: Uuid,
            format: ExportFormat,
            include: ExportInclude,
            context: Authentication<Self::Context>,
        ) -> Result<RepaymentExport, ServiceError>;
    }

    #[cfg(test)]
    mod tests {
        use super::*;

        #[test]
        fn test_export_include_default_is_open() {
            // D-03: Default ist Open fuer Banking-Workflow.
            assert_eq!(ExportInclude::default(), ExportInclude::Open);
        }

        #[test]
        fn test_export_format_only_has_pdf_variant() {
            // D-12: CSV gestrichen, NUR Pdf in Phase 11.
            // Compile-time-Guarantee: dieses Match-Statement bricht wenn neue Variante hinzukommt.
            let f = ExportFormat::Pdf;
            match f {
                ExportFormat::Pdf => {}
            }
        }

        #[test]
        fn test_export_include_has_three_variants() {
            // D-01/D-02: Open, All, Paid.
            let variants = [
                ExportInclude::Open,
                ExportInclude::All,
                ExportInclude::Paid,
            ];
            assert_eq!(variants.len(), 3);
        }

        #[test]
        fn test_repayment_export_bundle_construction() {
            let e = RepaymentExport {
                bytes: vec![0xDE, 0xAD, 0xBE, 0xEF],
                content_type: "application/pdf",
                filename: "auszahlung-2026-open.pdf".to_string(),
            };
            assert_eq!(e.bytes.len(), 4);
            assert_eq!(e.content_type, "application/pdf");
            assert!(e.filename.ends_with(".pdf"));
        }

        #[test]
        fn test_repayment_export_debug_hides_bytes() {
            // Pitfall #6: Debug-Impl darf KEINE Bytes-Hex-Dumps drucken.
            let e = RepaymentExport {
                bytes: vec![0xDE, 0xAD],
                content_type: "application/pdf",
                filename: "x.pdf".to_string(),
            };
            let dbg = format!("{:?}", e);
            assert!(dbg.contains("bytes_len"), "Debug should show bytes_len");
            assert!(!dbg.contains("0xDE"), "Debug must not leak raw bytes");
            assert!(!dbg.contains("[222"), "Debug must not leak Vec-format bytes (decimal)");
        }

        #[test]
        fn test_mock_repayment_export_service_compiles() {
            // mockall #[automock] muss MockRepaymentExportService generieren.
            // Wenn dieser Test compiliert, ist die automock-Annotation korrekt.
            let _mock = MockRepaymentExportService::new();
        }
    }
    ```

    SCHRITT 2: Oeffne `genossi_service/src/lib.rs`. Suche die Modul-Deklaration `pub mod attendance_export;`. Fuege direkt DANACH eine neue Zeile ein:

    ```rust
    pub mod repayment_export;
    ```

    (Falls die Modul-Deklarationen alphabetisch sortiert sind, plaziere `repayment_export` zwischen `repayment_entry` und `session`.)

    SCHRITT 3: Verifiziere mit `cargo build -p genossi_service` und `cargo test -p genossi_service repayment_export`.
  </action>
  <verify>
    <automated>
      cargo build -p genossi_service 2>&1 | grep -c "^error" ;
      cargo test -p genossi_service --lib repayment_export 2>&1 | tail -15;
      grep -cE "^pub mod repayment_export" /home/neosam/programming/rust/projects/genossi3/genossi_service/src/lib.rs;
      grep -cE "pub trait RepaymentExportService" /home/neosam/programming/rust/projects/genossi3/genossi_service/src/repayment_export.rs;
      grep -cE "enum ExportFormat" /home/neosam/programming/rust/projects/genossi3/genossi_service/src/repayment_export.rs;
      grep -cE "ExportInclude::Open" /home/neosam/programming/rust/projects/genossi3/genossi_service/src/repayment_export.rs
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^pub mod repayment_export" genossi_service/src/lib.rs` == 1
    - `grep -cE "pub trait RepaymentExportService" genossi_service/src/repayment_export.rs` == 1
    - `grep -cE "ExportFormat::Pdf" genossi_service/src/repayment_export.rs` >= 1
    - `grep -cE "ExportFormat::Csv" genossi_service/src/repayment_export.rs` == 0 (D-12 streicht CSV)
    - `grep -cE "ExportInclude::default\\(\\)" genossi_service/src/repayment_export.rs` >= 1 (Default-Impl-Test referenziert)
    - `cargo build -p genossi_service 2>&1 | grep -c "^error"` == 0
    - `cargo test -p genossi_service --lib repayment_export 2>&1 | grep "test result: ok"` >= 1 (alle 6 Tests gruen)
  </acceptance_criteria>
  <done>
    Datei `genossi_service/src/repayment_export.rs` existiert mit Trait + 3 Enums (1 ExportFormat::Pdf-Variante, 3 ExportInclude-Varianten, Default=Open) + Bundle-Struct + custom Debug-Impl + 6 Unit-Tests. Modul ist in `lib.rs` deklariert. `cargo test -p genossi_service repayment_export` zeigt 6 PASSED.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Trait-Interface ↔ Impl | Trait-Definition lebt in `genossi_service`-Crate; Impl in `genossi_service_impl`-Crate (Plan 11.03). Mocks via `#[automock]` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-001 | Information Disclosure | `RepaymentExport::Debug` | mitigate | Manuelles Debug-Impl druckt nur `bytes_len`, NICHT die Bytes (Pitfall #6 aus 11-RESEARCH.md). Test `test_repayment_export_debug_hides_bytes` verifiziert. Severity: LOW. |
| T-002 | Tampering | `ExportFormat`-Enum | mitigate | `ExportFormat::Csv` darf NICHT vorhanden sein (D-12). Compile-Time-Test `test_export_format_only_has_pdf_variant` mit exhaustivem Match wird brechen, falls jemand `Csv` hinzufuegt. Severity: LOW. |
| T-003 | Repudiation | Trait-Definition | accept | Reine Interface-Datei, KEINE `audited_*!`-Calls moeglich (Trait-Definitionen haben keine Bodies). Grep-Gate ist in Plan 11.03 (Service-Impl). Severity: N/A. |
</threat_model>

<verification>
- Alle 6 Unit-Tests gruen (`cargo test -p genossi_service repayment_export`).
- `MockRepaymentExportService::new()` kompiliert (mockall-Generation funktioniert).
- `ExportInclude::default()` liefert `Open` (D-03 codifiziert).
- `ExportFormat` hat EXAKT 1 Variante (`Pdf`); D-12 verifiziert via exhaustivem Match-Test.
</verification>

<success_criteria>
- EXPO-03 Domain-Foundation: `ExportInclude::Open|All|Paid` mit Default=Open in der Type-Definition kodiert.
- Modul ist von Plan 11.03 (Impl) + Plan 11.04 (REST) konsumierbar.
- Bundle-Struct mit `RepaymentExport { bytes, content_type, filename }` ist die Schnittstelle Service → REST.
- D-12 codifiziert: kein CSV-Code-Pfad in Trait-Layer.
</success_criteria>

<output>
After completion, create `.planning/phases/11-export-pdf-csv/11-02-SUMMARY.md`
</output>
