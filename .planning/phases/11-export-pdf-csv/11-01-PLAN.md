---
phase: 11-export-pdf-csv
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/defaults/auszahlungsliste.typ
  - genossi_service_impl/src/template_storage.rs
  - genossi_service_impl/src/pdf_generation.rs
autonomous: true
requirements:
  - EXPO-02
user_setup: []
tags:
  - pdf
  - typst
  - template
  - rendering
must_haves:
  truths:
    - "Fresh-Install schreibt `auszahlungsliste.typ` per `provision_defaults()` auf Disk (Pitfall #1 verhindert)"
    - "PdfGenerator::render_repayment_list existiert mit Signatur `(template_path, template_base, phase, rows) -> Result<Vec<u8>, ServiceError>`"
    - "Typst-Template rendert 6-Spalten-Tabelle mit Repeat-Header und Sort-Reihenfolge wie vom Service uebergeben (per D-09)"
    - "Verwendungszweck wird vom Service als pre-computed String mit ORIGINAL-UMLAUT 'Anteilsrückzahlung' uebergeben (D-04/D-05 wird im Service, NICHT im Template implementiert; Umlaute bleiben erhalten — KEINE ASCII-Sanitization)"
    - "Domain-Constraint: `share_count_to_pay_out >= 0` und `share_value > 0` -> `total_cents` ist immer non-negativ; `.abs()`-Calls sind UNNOETIG und werden vermieden (Phase-10-D-04-Pattern: `format!(\"{},{:02}\", cents / 100, cents % 100)`)"
    - "Optional: Summenzeile (Anzahl Eintraege + Gesamtbetrag) im Template-Footer (Planner-Discretion erlaubt)"
    - "D-07: Empty-String-Pattern fuer fehlende IBAN — `Member.bank_account` ist `Option<String>`, im Service per `unwrap_or_default()` zu leerem String konvertiert; im Typst-Template wird die IBAN-Spalte fuer leeren String als leere Zelle gerendert (kein Crash, kein Visual-Marker). Analog Phase-6-Pattern (`attendance_export.rs:276`)."
  artifacts:
    - path: "templates/defaults/auszahlungsliste.typ"
      provides: "Typst-Template mit 6-Spalten-Tabelle (Mitgliedsnummer, Name, IBAN, Anteile, Betrag, Verwendungszweck) + optionaler Summenzeile"
      min_lines: 30
    - path: "genossi_service_impl/src/template_storage.rs"
      provides: "DEFAULT_TEMPLATES-Eintrag fuer auszahlungsliste.typ via include_bytes!"
      contains: "auszahlungsliste.typ"
    - path: "genossi_service_impl/src/pdf_generation.rs"
      provides: "pub fn render_repayment_list + struct RepaymentExportRow + build_inputs_repayment-Helper"
      contains: "render_repayment_list"
  key_links:
    - from: "genossi_service_impl/src/pdf_generation.rs::render_repayment_list"
      to: "templates/defaults/auszahlungsliste.typ"
      via: "fs::read_to_string(template_base.join(template_path))"
      pattern: "template_base.join.*auszahlungsliste"
    - from: "genossi_service_impl/src/template_storage.rs::DEFAULT_TEMPLATES"
      to: "templates/defaults/auszahlungsliste.typ"
      via: "include_bytes!"
      pattern: "include_bytes!\\(.*auszahlungsliste\\.typ"
---

<objective>
Foundation-Plan fuer Phase 11: legt das Typst-Template `auszahlungsliste.typ` an, registriert es im `DEFAULT_TEMPLATES`-Array und ergaenzt den `PdfGenerator` um die neue Methode `render_repayment_list`. Kein Service, kein REST, kein DI - reine Render-Infrastruktur.

Purpose: Ohne dieses Fundament wuerde der Service-Impl (Plan 11.03) den Renderer nicht aufrufen koennen, und Fresh-Installs wuerden den Export mit "template not found" abbrechen (Pitfall #1 aus RESEARCH.md §Common Pitfalls).

Output: 1 neue Datei (`auszahlungsliste.typ`), 2 modifizierte Dateien (`template_storage.rs`, `pdf_generation.rs`). Eingaben sind `RepaymentPhaseEntity` (Phase 7) + Liste `RepaymentExportRow` (neue Service-Impl-lokale Struct). Ausgabe ist `Vec<u8>` PDF-Bytes.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-export-pdf-csv/11-CONTEXT.md
@.planning/phases/11-export-pdf-csv/11-RESEARCH.md
@.planning/phases/11-export-pdf-csv/11-PATTERNS.md

@templates/defaults/teilnehmerliste.typ
@templates/defaults/_layout.typ
@genossi_service_impl/src/template_storage.rs
@genossi_service_impl/src/pdf_generation.rs

<interfaces>
<!-- Existing types this plan depends on (already in tree). -->

From genossi_dao/src/repayment_phase.rs:
```rust
pub enum RepaymentPhaseStatus { Preparation, Open, Closed }
pub struct RepaymentPhaseEntity {
    pub id: Uuid,
    pub fiscal_year: i32,
    pub share_value: i64,   // Cents
    pub status: RepaymentPhaseStatus,
    pub opened_at: Option<PrimitiveDateTime>,
    pub closed_at: Option<PrimitiveDateTime>,
    pub created: PrimitiveDateTime,
    pub deleted: Option<PrimitiveDateTime>,
    pub version: Uuid,
}
```

From genossi_service_impl/src/pdf_generation.rs (existing methods to mirror):
```rust
pub fn render_attendance_list(
    &self,
    template_path: &str,
    template_base: &Path,
    assembly: &AssemblyEntity,
    rows: &[AttendanceMemberRow],
    present: u64,
    total: Option<u64>,
) -> Result<Vec<u8>, ServiceError>;

// Private helper around pdf_generation.rs:602-652
fn build_inputs_attendance(...) -> Dict;
```

<!-- New types this plan creates (consumed by plans 11.02 / 11.03). -->

To be created in pdf_generation.rs:
```rust
pub struct RepaymentExportRow {
    pub member_number: i64,
    pub name: String,        // "first_name last_name"
    pub iban: String,        // Member.bank_account.unwrap_or_default()
    pub share_count: i32,
    pub amount_str: String,  // "60,00" (deutsche Lokalisierung; Phase-10-D-04-Pattern OHNE .abs())
    pub purpose: String,     // D-04: "Anteilsrückzahlung GJ {fy} {mn} {fn} {ln}" — Umlaute bleiben (D-05)
}

pub fn render_repayment_list(
    &self,
    template_path: &str,
    template_base: &Path,
    phase: &RepaymentPhaseEntity,
    rows: &[RepaymentExportRow],
) -> Result<Vec<u8>, ServiceError>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Typst-Template anlegen und in DEFAULT_TEMPLATES registrieren</name>
  <files>
    templates/defaults/auszahlungsliste.typ (NEW)
    genossi_service_impl/src/template_storage.rs (MODIFY)
  </files>
  <read_first>
    - templates/defaults/teilnehmerliste.typ (1:1 Pattern-Vorlage fuer Repeat-Header, json.decode, _layout.typ-Import — gesamtes File, ~43 LOC)
    - templates/defaults/_layout.typ (Letter-Layout-Show-Rule, gesamtes File ~38 LOC)
    - genossi_service_impl/src/template_storage.rs (DEFAULT_TEMPLATES-Array Z. 10-27, include_bytes!-Pattern und DefaultTemplate-Struct-Definition)
    - .planning/phases/11-export-pdf-csv/11-RESEARCH.md §Pattern 4 (Z. 312-361, vollstaendiges Template-Skelett)
    - .planning/phases/11-export-pdf-csv/11-CONTEXT.md §Claude's Discretion "Typst-Template-Layout" (Spalten-Verteilung auto/1fr/1fr/auto/auto/1fr)
  </read_first>
  <behavior>
    - Template kompiliert ohne Typst-Fehler bei einem leeren rows-Array (`rows: []`)
    - Template rendert 6 Spalten mit Header `Nr.`, `Name`, `IBAN`, `Anteile`, `Betrag`, `Verwendungszweck`
    - Header wird via `table.header(repeat: true, ...)` auf jeder Seite wiederholt
    - meta.fiscal_year erscheint im Untertitel ("Geschaeftsjahr {fiscal_year}")
    - meta.row_count erscheint als "{n} Auszahlung(en)"
    - Optional aktivierte Summenzeile zeigt `meta.row_count` Eintraege + `meta.total_amount_str` Euro
    - `provision_defaults()` schreibt das Template auf Disk wenn nicht vorhanden
  </behavior>
  <action>
    SCHRITT 1: Lege Datei `templates/defaults/auszahlungsliste.typ` an mit folgendem Inhalt-Skelett (1:1 angepasste Variante von teilnehmerliste.typ aus RESEARCH §Pattern 4):

    ```typst
    // Auszahlungsliste fuer RepaymentPhase — Phase 11 (EXPO-02)
    // Inputs via sys.inputs:
    //   meta (JSON string): {
    //     "title": str,
    //     "date": str,
    //     "fiscal_year": int,
    //     "row_count": int,
    //     "total_amount_str": str,
    //     "phase_id": str,
    //   }
    //   rows (JSON string): [{
    //     "member_number": int,
    //     "name": str,
    //     "iban": str,
    //     "share_count": int,
    //     "amount_str": str,
    //     "purpose": str,
    //   }, ...]

    #import "_layout.typ": letter

    #let meta = json.decode(sys.inputs.at("meta"))
    #let rows = json.decode(sys.inputs.at("rows"))

    #show: letter.with(
      title: meta.title,
      date: meta.date,
    )

    #text(size: 11pt)[
      *Geschaeftsjahr #meta.fiscal_year — #meta.row_count Auszahlung(en)*
    ]

    #v(0.5cm)

    #table(
      columns: (auto, 1fr, auto, auto, auto, 1fr),
      align: (right, left, left, right, right, left),
      stroke: 0.5pt,
      table.header(
        repeat: true,
        [*Nr.*], [*Name*], [*IBAN*], [*Anteile*], [*Betrag*], [*Verwendungszweck*],
      ),
      ..rows.map(r => (
        [#r.member_number],
        [#r.name],
        [#r.iban],
        [#r.share_count],
        [#r.amount_str],
        [#r.purpose],
      )).flatten()
    )

    // Summenzeile (Planner-Discretion, CONTEXT.md erlaubt): Nice-to-Have fuer Banking-Vorstand
    #v(0.5cm)
    #text(size: 10pt)[
      Gesamt: *#meta.row_count Eintraege* — Summe *#meta.total_amount_str EUR*
    ]
    ```

    SCHRITT 2: Oeffne `genossi_service_impl/src/template_storage.rs`. Suche das Array `const DEFAULT_TEMPLATES: &[DefaultTemplate] = &[ ... ];` (Z. 10-27 laut RESEARCH). Fuege VOR dem schliessenden `];` einen neuen Eintrag an:

    ```rust
    // Phase 11 (EXPO-01..03): Auszahlungslisten-Export PDF template.
    // Required by RepaymentExportServiceImpl::export(ExportFormat::Pdf) — Plan 11.03.
    DefaultTemplate {
        path: "auszahlungsliste.typ",
        content: include_bytes!("../../templates/defaults/auszahlungsliste.typ"),
    },
    ```

    Reihenfolge nach `teilnehmerliste.typ`-Eintrag erhaelt die chronologische Phase-Sortierung im Array.

    SCHRITT 3: KEINE anderen Aenderungen. Das Template wird in Plan 11.03 vom Service aufgerufen; hier nur Foundation.
  </action>
  <verify>
    <automated>
      cargo build -p genossi_service_impl 2>&1 | grep -E "error|warning: unused" | head -20;
      grep -c "auszahlungsliste.typ" /home/neosam/programming/rust/projects/genossi3/genossi_service_impl/src/template_storage.rs;
      ls -la /home/neosam/programming/rust/projects/genossi3/templates/defaults/auszahlungsliste.typ
    </automated>
  </verify>
  <acceptance_criteria>
    - `ls /home/neosam/programming/rust/projects/genossi3/templates/defaults/auszahlungsliste.typ` exit 0 (Datei existiert)
    - `grep -c "table.header(" /home/neosam/programming/rust/projects/genossi3/templates/defaults/auszahlungsliste.typ` >= 1 (Repeat-Header vorhanden)
    - `grep -c "#import \"_layout.typ\"" /home/neosam/programming/rust/projects/genossi3/templates/defaults/auszahlungsliste.typ` == 1 (Layout-Import)
    - `grep -c "auszahlungsliste.typ" /home/neosam/programming/rust/projects/genossi3/genossi_service_impl/src/template_storage.rs` >= 2 (1× include_bytes! + 1× path)
    - `cargo build -p genossi_service_impl 2>&1 | grep -c "^error" ` == 0
  </acceptance_criteria>
  <done>
    Template-Datei existiert mit 6-Spalten-Repeat-Header. `template_storage.rs` enthaelt den neuen `DefaultTemplate`-Eintrag. `cargo build -p genossi_service_impl` ist fehlerfrei.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PdfGenerator::render_repayment_list + RepaymentExportRow-Struct + build_inputs_repayment</name>
  <files>
    genossi_service_impl/src/pdf_generation.rs (MODIFY)
  </files>
  <read_first>
    - genossi_service_impl/src/pdf_generation.rs (gesamtes File — insbesondere render_attendance_list Z. 279-336 und build_inputs_attendance Z. 602-652 als 1:1 Pattern-Vorlage)
    - .planning/phases/11-export-pdf-csv/11-RESEARCH.md §Q1 Example (Z. 447-505: render_repayment_list-Signatur + RepaymentExportRow-Struct)
    - .planning/phases/11-export-pdf-csv/11-PATTERNS.md §7 (Pattern Assignment fuer pdf_generation.rs)
    - genossi_dao/src/repayment_phase.rs (RepaymentPhaseEntity-Felder fiscal_year + share_value)
  </read_first>
  <behavior>
    - `RepaymentExportRow`-Struct ist `pub` und enthaelt die 6 Felder: `member_number: i64, name: String, iban: String, share_count: i32, amount_str: String, purpose: String`
    - `render_repayment_list` mit Signatur `pub fn render_repayment_list(&self, template_path: &str, template_base: &Path, phase: &RepaymentPhaseEntity, rows: &[RepaymentExportRow]) -> Result<Vec<u8>, ServiceError>` existiert
    - Bei nicht vorhandenem Template: `ServiceError::InternalError("template not found: ...")` (mirror von render_attendance_list)
    - Build_inputs_repayment baut `meta`+`rows` als JSON-Strings im `Dict`
    - `meta.total_amount_str` ist die Summe aller `share_count × share_value` Cents formatiert als "12345,67" (Phase-10-D-04-Pattern OHNE `.abs()` — REVISION-Fix B3)
    - Test-Funktion `test_render_repayment_list_with_empty_rows` rendert erfolgreich (PDF-Bytes mit `%PDF-` Magic)
    - Test-Funktion `test_render_repayment_list_with_two_rows` rendert erfolgreich, Bytes-Laenge > 1KB UND nutzt `Anteilsrückzahlung` (mit ü) als purpose-String (REVISION-Fix B1: D-04 wortwoertlich) sowie einen Member-Namen mit Umlaut `Hans Müller` (REVISION-Fix W6)
    - Test-Pfade verwenden `env!("CARGO_MANIFEST_DIR")` statt relativem `"../templates/..."` — REVISION-Fix W5
  </behavior>
  <action>
    SCHRITT 1: Imports am Anfang von `pdf_generation.rs` ergaenzen — falls noch nicht vorhanden:
    ```rust
    use genossi_dao::repayment_phase::RepaymentPhaseEntity;
    ```

    SCHRITT 2: Direkt nach der existierenden Struct-Definition `AttendanceMemberRow` (oder am gleichen Modul-Level) folgende neue Struct hinzufuegen:

    ```rust
    /// Phase 11 (EXPO-02): Rendering-Bundle pro Auszahlungseintrag.
    /// Pre-computed im Service-Impl (Plan 11.03); Renderer baut nur JSON.
    /// `amount_str` und `purpose` sind D-04 (Verwendungszweck-Schema mit Umlauten) +
    /// Phase-10-D-04 (Euro-Format "12,00") konform vorbereitet.
    /// D-05: Sonderzeichen ä/ö/ü/ß bleiben unveraendert (keine ASCII-Sanitization).
    #[derive(Debug, Clone)]
    pub struct RepaymentExportRow {
        pub member_number: i64,
        pub name: String,
        pub iban: String,
        pub share_count: i32,
        pub amount_str: String,
        pub purpose: String,
    }
    ```

    SCHRITT 3: Direkt nach der existierenden Methode `render_attendance_list` (Z. ~336) im `impl PdfGenerator`-Block folgende Methode hinzufuegen — exakt analog `render_attendance_list`:

    ```rust
    /// Phase 11 (EXPO-02): Rendert die Auszahlungsliste fuer eine RepaymentPhase als PDF.
    /// Pre-computed strings (amount_str, purpose) werden vom Service uebergeben — D-04/D-05.
    pub fn render_repayment_list(
        &self,
        template_path: &str,
        template_base: &Path,
        phase: &RepaymentPhaseEntity,
        rows: &[RepaymentExportRow],
    ) -> Result<Vec<u8>, ServiceError> {
        let full_path = template_base.join(template_path);
        let source_text = std::fs::read_to_string(&full_path).map_err(|e| {
            if e.kind() == std::io::ErrorKind::NotFound {
                ServiceError::InternalError(Arc::from(format!(
                    "template not found: {}",
                    full_path.display()
                )))
            } else {
                ServiceError::InternalError(Arc::from(format!("template io error: {}", e)))
            }
        })?;

        let inputs = build_inputs_repayment(phase, rows);

        let world = TemplateWorld::new(
            &source_text,
            template_path,
            template_base.to_path_buf(),
            inputs,
            &self.fonts,
            &self.book,
            &self.package_cache,
        );

        let result = typst::compile::<PagedDocument>(&world);
        let document = match result.output {
            Ok(doc) => doc,
            Err(diagnostics) => {
                let messages: Vec<String> =
                    diagnostics.iter().map(|d| format!("{:?}", d.message)).collect();
                return Err(ServiceError::InternalError(Arc::from(format!(
                    "typst compile error: {}",
                    messages.join("; ")
                ))));
            }
        };

        let pdf_bytes = typst_pdf::pdf(&document, &typst_pdf::PdfOptions::default())
            .map_err(|e| ServiceError::InternalError(Arc::from(format!(
                "typst pdf serialize error: {:?}", e
            ))))?;

        Ok(pdf_bytes)
    }
    ```

    SCHRITT 4: Direkt nach `build_inputs_attendance` (Z. ~652) im File-Scope folgende Funktion hinzufuegen.

    **REVISION-Fix B3 (Phase-10-D-04-Pattern-Konsistenz):** `format!("{},{:02}", cents / 100, cents % 100)` OHNE `.abs()` — Domain-Constraint `share_count_to_pay_out >= 0` und `share_value > 0` garantieren non-negative `total_cents`. `.abs()` wuerde Inkonsistenz mit PATTERNS.md §S9 und Phase-10-D-04 einfuehren.

    ```rust
    /// Phase 11: Baut sys.inputs-Dict fuer auszahlungsliste.typ.
    /// `meta` und `rows` werden als JSON-Strings injiziert; Template macht
    /// `json.decode(sys.inputs.at("..."))`.
    /// REVISION-Fix B3: total_amount_str ohne .abs() — Phase-10-D-04-Pattern (PATTERNS.md §S9).
    /// Domain-Constraint: share_count > 0 und share_value > 0 => total_cents >= 0.
    fn build_inputs_repayment(
        phase: &RepaymentPhaseEntity,
        rows: &[RepaymentExportRow],
    ) -> typst::foundations::Dict {
        use typst::foundations::{Dict, Str, Value};

        let mut inputs = Dict::new();

        // ISO date string (use today; Phase 6 uses assembly.date which we don't have here).
        let date_str = time::OffsetDateTime::now_utc()
            .date()
            .to_string();

        // Total amount: SUM(share_count × share_value_cent), formatted "EUR,CC" (deutsch).
        // REVISION-Fix B3: Pattern aus Phase-10-D-04 / PATTERNS.md §S9 — OHNE .abs().
        let total_cents: i64 = rows
            .iter()
            .map(|r| (r.share_count as i64) * phase.share_value)
            .sum();
        let total_amount_str = format!("{},{:02}", total_cents / 100, total_cents % 100);

        let meta = serde_json::json!({
            "title": format!("Auszahlungsliste Geschaeftsjahr {}", phase.fiscal_year),
            "date": date_str,
            "fiscal_year": phase.fiscal_year,
            "row_count": rows.len(),
            "total_amount_str": total_amount_str,
            "phase_id": phase.id.to_string(),
        });

        let row_values: Vec<serde_json::Value> = rows
            .iter()
            .map(|r| serde_json::json!({
                "member_number": r.member_number,
                "name": r.name,
                "iban": r.iban,
                "share_count": r.share_count,
                "amount_str": r.amount_str,
                "purpose": r.purpose,
            }))
            .collect();
        let rows_json = serde_json::Value::Array(row_values);

        inputs.insert(
            Str::from("meta"),
            Value::Str(Str::from(serde_json::to_string(&meta).unwrap().as_str())),
        );
        inputs.insert(
            Str::from("rows"),
            Value::Str(Str::from(serde_json::to_string(&rows_json).unwrap().as_str())),
        );
        inputs
    }
    ```

    SCHRITT 5: Im `#[cfg(test)] mod tests { ... }`-Block am Datei-Ende (sollte existieren — pdf_generation.rs hat bereits Tests fuer render_attendance_list) folgende zwei Tests hinzufuegen.

    **REVISION-Fix W5 (Pfad-Determinismus):** Template-Pfade werden via `env!("CARGO_MANIFEST_DIR")` absolut gemacht. Der Cargo-Working-Dir kann je nach Aufruf (`cargo test` aus Workspace-Root vs. aus Crate-Dir) variieren; `CARGO_MANIFEST_DIR` zeigt IMMER auf das `genossi_service_impl/`-Verzeichnis und ist damit reproduzierbar.

    **REVISION-Fix B1 + W6 (D-04/D-05 wortwoertlich):** Test 2 verifiziert den exakten String `"Anteilsrückzahlung GJ 2026 1234 Hans Müller"` mit ORIGINALEM `ü` als purpose und Member-Namen mit Umlaut (`Hans Müller`). Damit ist der D-04-Wortlaut und die D-05-No-Sanitization-Politik IM TEST verankert.

    ```rust
    #[test]
    fn test_render_repayment_list_with_empty_rows() {
        // REVISION-Fix W5: CARGO_MANIFEST_DIR macht den Pfad deterministisch unabhaengig
        // vom Cargo-Working-Dir (Workspace-Root vs. Crate-Dir).
        let manifest = std::path::Path::new(env!("CARGO_MANIFEST_DIR"));
        let template_base = std::env::temp_dir().join("genossi_phase11_test_empty");
        std::fs::create_dir_all(&template_base).unwrap();
        std::fs::copy(
            manifest.join("../templates/defaults/_layout.typ"),
            template_base.join("_layout.typ"),
        ).unwrap();
        std::fs::copy(
            manifest.join("../templates/defaults/auszahlungsliste.typ"),
            template_base.join("auszahlungsliste.typ"),
        ).unwrap();

        let gen_ = PdfGenerator::new().unwrap();
        let phase = RepaymentPhaseEntity {
            id: uuid::Uuid::new_v4(),
            fiscal_year: 2026,
            share_value: 12000, // 120 EUR in cents
            status: genossi_dao::repayment_phase::RepaymentPhaseStatus::Open,
            opened_at: None,
            closed_at: None,
            created: time::OffsetDateTime::now_utc().into(),
            deleted: None,
            version: uuid::Uuid::new_v4(),
        };
        let rows: Vec<RepaymentExportRow> = Vec::new();
        let res = gen_.render_repayment_list("auszahlungsliste.typ", &template_base, &phase, &rows);
        assert!(res.is_ok(), "render failed: {:?}", res.err());
        let bytes = res.unwrap();
        assert!(bytes.starts_with(b"%PDF-"), "PDF magic bytes missing");
    }

    #[test]
    fn test_render_repayment_list_with_two_rows() {
        // REVISION-Fix W5: deterministischer Pfad via CARGO_MANIFEST_DIR.
        let manifest = std::path::Path::new(env!("CARGO_MANIFEST_DIR"));
        let template_base = std::env::temp_dir().join("genossi_phase11_test_two");
        std::fs::create_dir_all(&template_base).unwrap();
        std::fs::copy(
            manifest.join("../templates/defaults/_layout.typ"),
            template_base.join("_layout.typ"),
        ).unwrap();
        std::fs::copy(
            manifest.join("../templates/defaults/auszahlungsliste.typ"),
            template_base.join("auszahlungsliste.typ"),
        ).unwrap();

        let gen_ = PdfGenerator::new().unwrap();
        let phase = RepaymentPhaseEntity {
            id: uuid::Uuid::new_v4(),
            fiscal_year: 2026,
            share_value: 12000,
            status: genossi_dao::repayment_phase::RepaymentPhaseStatus::Open,
            opened_at: None,
            closed_at: None,
            created: time::OffsetDateTime::now_utc().into(),
            deleted: None,
            version: uuid::Uuid::new_v4(),
        };
        // REVISION-Fix B1 + W6: D-04 wortwoertlich mit ORIGINAL-Umlaut "Anteilsrückzahlung"
        // (ü, NICHT ue). D-05: keine ASCII-Sanitization. Member-Name "Hans Müller" hat
        // ebenfalls ein ü — verifiziert dass Typst Umlaute korrekt rendert.
        let rows = vec![
            RepaymentExportRow {
                member_number: 1234,
                name: "Hans Müller".to_string(),
                iban: "DE89370400440532013000".to_string(),
                share_count: 1,
                amount_str: "120,00".to_string(),
                purpose: "Anteilsrückzahlung GJ 2026 1234 Hans Müller".to_string(),
            },
            RepaymentExportRow {
                member_number: 5678,
                name: "Erika Beispiel".to_string(),
                iban: String::new(), // IBAN-NULL-Case (D-06)
                share_count: 2,
                amount_str: "240,00".to_string(),
                purpose: "Anteilsrückzahlung GJ 2026 5678 Erika Beispiel".to_string(),
            },
        ];

        // Sanity-Assertion: purpose-Strings enthalten ORIGINAL-Umlaut (D-04 wortwoertlich).
        assert!(rows[0].purpose.contains('ü'), "D-04 violated: purpose must contain 'ü' from 'Anteilsrückzahlung'");
        assert_eq!(rows[0].purpose, "Anteilsrückzahlung GJ 2026 1234 Hans Müller");

        let res = gen_.render_repayment_list("auszahlungsliste.typ", &template_base, &phase, &rows);
        assert!(res.is_ok(), "render failed: {:?}", res.err());
        let bytes = res.unwrap();
        assert!(bytes.starts_with(b"%PDF-"));
        assert!(bytes.len() > 1000, "PDF too small ({} bytes)", bytes.len());
    }
    ```
  </action>
  <verify>
    <automated>
      cargo build -p genossi_service_impl 2>&1 | grep -c "^error" ;
      cargo test -p genossi_service_impl --lib render_repayment_list 2>&1 | tail -10;
      grep -cE "fn render_repayment_list" /home/neosam/programming/rust/projects/genossi3/genossi_service_impl/src/pdf_generation.rs;
      grep -cE "struct RepaymentExportRow" /home/neosam/programming/rust/projects/genossi3/genossi_service_impl/src/pdf_generation.rs;
      grep -cE "fn build_inputs_repayment" /home/neosam/programming/rust/projects/genossi3/genossi_service_impl/src/pdf_generation.rs;
      grep -c "Anteilsrückzahlung" /home/neosam/programming/rust/projects/genossi3/genossi_service_impl/src/pdf_generation.rs
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "fn render_repayment_list" genossi_service_impl/src/pdf_generation.rs` == 1
    - `grep -cE "struct RepaymentExportRow" genossi_service_impl/src/pdf_generation.rs` == 1
    - `grep -cE "fn build_inputs_repayment" genossi_service_impl/src/pdf_generation.rs` == 1
    - `grep -c "Anteilsrückzahlung" genossi_service_impl/src/pdf_generation.rs` >= 2 (REVISION-Fix B1: 2× im Test-Setup mit ORIGINAL-Umlaut)
    - `grep -c "Anteilsrueckzahlung" genossi_service_impl/src/pdf_generation.rs` == 0 (KEINE ASCII-Variante — D-05)
    - `grep -c "\.abs()" genossi_service_impl/src/pdf_generation.rs | tr -d '\n'` (im build_inputs_repayment-Block) == 0 — REVISION-Fix B3
    - `grep -c "CARGO_MANIFEST_DIR" genossi_service_impl/src/pdf_generation.rs` >= 2 (REVISION-Fix W5: 2 Test-Funktionen nutzen den Manifest-Pfad)
    - `cargo build -p genossi_service_impl 2>&1 | grep -c "^error"` == 0
    - `cargo test -p genossi_service_impl --lib test_render_repayment_list_with_empty_rows 2>&1 | grep -c "test result: ok"` >= 1
    - `cargo test -p genossi_service_impl --lib test_render_repayment_list_with_two_rows 2>&1 | grep -c "test result: ok"` >= 1
  </acceptance_criteria>
  <done>
    PdfGenerator hat eine neue oeffentliche Methode `render_repayment_list`, die `RepaymentExportRow`-Listen rendert. `build_inputs_repayment` baut JSON-Strings fuer `meta` + `rows` mit `format!("{},{:02}", cents / 100, cents % 100)` (OHNE `.abs()` — Phase-10-D-04-Konsistenz). Beide Unit-Tests gruen; Test 2 nutzt ORIGINAL-Umlaut `Anteilsrückzahlung` (D-04 wortwoertlich) und Member-Name `Hans Müller` (D-05 no-sanitization).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer ↔ Disk | Typst-Templates werden via `provision_defaults` auf Disk geschrieben und beim Render gelesen. Fresh-Install-Fehler = 500 (nicht Datenleck) |
| Service ↔ Renderer | Service uebergibt bereits gefilterte Daten (Plan 11.03); Renderer ist reine Format-Transformation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-001 | Tampering | `auszahlungsliste.typ` | mitigate | Template via `include_bytes!` in Binary kompiliert (kein User-Editier-Pfad in Plan 11); `provision_defaults` schreibt nur, wenn Datei fehlt — kein Overwrite. Fresh-Install-Test (Pitfall #1) verifiziert dass DEFAULT_TEMPLATES-Eintrag greift. Severity: LOW. |
| T-002 | Information Disclosure | `build_inputs_repayment` | accept | Plan 11.01 erhaelt keine PII direkt (nur leere Test-Rows in Tests); echte Daten kommen erst in Plan 11.03. Bound: nicht im Scope dieses Plans. Severity: N/A. |
| T-003 | Denial of Service | `render_repayment_list` mit grossen Row-Listen | accept | Typst hat keine bekannten Render-Limits unter Phase-6-Datenvolumen (50-100 Eintraege). Phase-Groesse ist realistisch begrenzt durch Genossenschafts-Mitgliederzahl. Tower-Governor-Rate-Limit existiert global. Severity: LOW. |
| T-006 | Repudiation | Audit-Chain-Bruch | mitigate | Plan 11.01 enthaelt KEINE `audited_*!`-Macros (template_storage + pdf_generation sind reine Render-Infrastruktur). Grep-Gate gehoert in Plan 11.03 (Service-Impl). Severity: N/A. |
</threat_model>

<verification>
- Beide Unit-Tests (`test_render_repayment_list_with_empty_rows`, `test_render_repayment_list_with_two_rows`) sind gruen.
- `grep -c "auszahlungsliste.typ" genossi_service_impl/src/template_storage.rs` == 2 (1× include_bytes + 1× path).
- `grep -c "Anteilsrückzahlung" genossi_service_impl/src/pdf_generation.rs` >= 2 (D-04 wortwoertlich; REVISION-Fix B1).
- `grep -c "Anteilsrueckzahlung" genossi_service_impl/src/pdf_generation.rs` == 0 (KEINE ASCII-Sanitization).
- `cargo build -p genossi_service_impl` ist fehlerfrei.
- Render-Output startet mit `%PDF-` Magic-Bytes.
</verification>

<success_criteria>
- Datei `templates/defaults/auszahlungsliste.typ` existiert (>= 30 LOC, enthaelt 6 Spalten-Header).
- `DEFAULT_TEMPLATES`-Array hat neuen Eintrag fuer `auszahlungsliste.typ`.
- `PdfGenerator::render_repayment_list` Methode existiert mit dokumentierter Signatur und Tests.
- `RepaymentExportRow`-Struct ist `pub` und exportierbar fuer Plan 11.02/11.03.
- `build_inputs_repayment`-Helper aggregiert `meta.total_amount_str` korrekt OHNE `.abs()` (REVISION-Fix B3, Phase-10-D-04-Konsistenz): Test 2 verifiziert "360,00" fuer 2 Eintraege bei 12000 cent share_value (1×120 + 2×120 = 360 EUR).
- D-04 wortwoertlich verankert: Test 2 verwendet `"Anteilsrückzahlung GJ 2026 1234 Hans Müller"` als purpose mit ORIGINAL-Umlaut (REVISION-Fix B1 + W6).
</success_criteria>

<output>
After completion, create `.planning/phases/11-export-pdf-csv/11-01-SUMMARY.md`
</output>
