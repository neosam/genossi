---
phase: 11-export-pdf-csv
plan: 05
type: execute
wave: 4
depends_on:
  - 11-04-PLAN.md
files_modified:
  - genossi_bin/src/lib.rs
autonomous: true
requirements:
  - EXPO-01
  - EXPO-05
user_setup: []
tags:
  - di-wiring
  - bin
must_haves:
  truths:
    - "`RestStateImpl` hat ein neues Feld `repayment_export_service: Arc<RepaymentExportService>`"
    - "`RepaymentExportServiceDependencies`-Type-Alias + Deps-Trait-Impl existieren im bin-Crate"
    - "Service-Konstruktion in `RestStateImpl::new()` ruft `Arc::new(RepaymentExportServiceImpl { ... })` mit shared Arcs auf — kein zweiter PdfGenerator/TemplateBase-Bau"
    - "`pdf_generator` und `template_base` werden via Arc::clone() mit AttendanceExportServiceImpl geteilt (Single-Arc-per-Process)"
    - "`impl RepaymentExportRestState for RestStateImpl` existiert und liefert `Arc<RepaymentExportService>`"
    - "Nach diesem Plan kompiliert `cargo build -p genossi_bin` fehlerfrei (REST-Layer kann nun via RestStateImpl konsumiert werden)"
  artifacts:
    - path: "genossi_bin/src/lib.rs"
      provides: "DI-Wiring fuer RepaymentExportService — 5 Edit-Stellen (Type-Aliases, Feld, Construction, Initializer, RestState-Trait-Impl)"
      contains: "repayment_export_service"
  key_links:
    - from: "genossi_bin/src/lib.rs::RestStateImpl::new"
      to: "genossi_service_impl/src/repayment_export.rs::RepaymentExportServiceImpl"
      via: "Arc::new(RepaymentExportServiceImpl { ... })"
      pattern: "RepaymentExportServiceImpl"
    - from: "genossi_bin/src/lib.rs::impl RepaymentExportRestState for RestStateImpl"
      to: "genossi_rest/src/repayment_export.rs::RepaymentExportRestState"
      via: "fn repayment_export_service(&self) -> Arc<...>"
      pattern: "fn repayment_export_service"
---

<objective>
Wave-4-Wiring-Plan: ergaenzt `genossi_bin/src/lib.rs` um die 5 Edit-Stellen fuer das DI-Wiring der `RepaymentExportServiceImpl`-Instanz. Wiederverwendet alle bestehenden Arcs (`pdf_generator`, `template_storage`, `repayment_phase_dao`, `repayment_entry_dao`, `member_dao`, `permission_service`, `transaction_dao`) — keine neuen DAOs/Services. Nach diesem Plan kompiliert die gesamte Workspace, REST-Endpoint ist HTTP-erreichbar (aber noch nicht E2E-getestet — das ist Plan 11.06).

Purpose: Trennt das Wiring (mechanische Replikation des AttendanceExport-Wiring-Patterns aus bin/lib.rs:266-289, 520-522, 815-830, 918-922, 1460-1468) von der Logik (Plan 11.03) und vom REST-Layer (Plan 11.04). Hat eigene Wave, weil es BEIDE Vorgaenger-Plans braucht und ohne sie nicht compilebar ist.

Output: 1 modifizierte Datei (`genossi_bin/src/lib.rs` mit 5 Edits, alle additiv).
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-export-pdf-csv/11-CONTEXT.md
@.planning/phases/11-export-pdf-csv/11-RESEARCH.md
@.planning/phases/11-export-pdf-csv/11-PATTERNS.md
@.planning/phases/11-export-pdf-csv/11-03-PLAN.md
@.planning/phases/11-export-pdf-csv/11-04-PLAN.md

@genossi_bin/src/lib.rs
@genossi_service/src/repayment_export.rs
@genossi_service_impl/src/repayment_export.rs

<interfaces>
<!-- Existing Arc<...> fields in RestStateImpl (already wired through Phase 7-10). -->

From genossi_bin/src/lib.rs (existing fields & local vars):
```rust
// Existing Arcs in RestStateImpl that we WILL reuse for RepaymentExportServiceImpl:
pdf_generator: Arc<PdfGenerator>,            // shared w/ AttendanceExport
template_storage: Arc<TemplateStorage>,      // for base_path
repayment_phase_dao: Arc<RepaymentPhaseDao>, // Phase 7
repayment_entry_dao: Arc<RepaymentEntryDao>, // Phase 8
member_dao: Arc<MemberDao>,                  // Phase 1
permission_service: Arc<PermissionService>,  // global
transaction_dao: Arc<TransactionDao>,        // global

// Existing Attendance Export wiring (1:1 Vorbild):
attendance_export_service: Arc<AttendanceExportService>,  // line ~520
```

From genossi_rest/src/repayment_export.rs (Plan 11.04):
```rust
pub trait RepaymentExportRestState: Clone + Send + Sync + 'static {
    type RepaymentExportService: RepaymentExportService<Context = Context> + Send + Sync + 'static;
    fn repayment_export_service(&self) -> Arc<Self::RepaymentExportService>;
}
```

From genossi_service_impl/src/repayment_export.rs (Plan 11.03):
```rust
pub trait RepaymentExportServiceDeps: Send + Sync + 'static {
    type Context: ...;
    type Transaction: Transaction;
    type RepaymentPhaseDao: RepaymentPhaseDao<Transaction = Self::Transaction> + Send + Sync;
    type RepaymentEntryDao: RepaymentEntryDao<Transaction = Self::Transaction> + Send + Sync;
    type MemberDao: MemberDao<Transaction = Self::Transaction> + Send + Sync;
    type PermissionService: PermissionService<Context = Self::Context> + Send + Sync;
    type TransactionDao: TransactionDao<Transaction = Self::Transaction> + Send + Sync;
}

pub struct RepaymentExportServiceImpl<Deps: RepaymentExportServiceDeps> {
    pub transaction_dao: Arc<Deps::TransactionDao>,
    pub permission_service: Arc<Deps::PermissionService>,
    pub repayment_phase_dao: Arc<Deps::RepaymentPhaseDao>,
    pub repayment_entry_dao: Arc<Deps::RepaymentEntryDao>,
    pub member_dao: Arc<Deps::MemberDao>,
    pub pdf_generator: Arc<PdfGenerator>,
    pub template_base: Arc<PathBuf>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 5 Edit-Stellen in genossi_bin/src/lib.rs (Type-Aliases + Feld + Construction + Initializer + RestState-Trait-Impl)</name>
  <files>
    genossi_bin/src/lib.rs (MODIFY)
  </files>
  <read_first>
    - genossi_bin/src/lib.rs (gesamtes File — insbesondere Z. 266-289 fuer AttendanceExportServiceDependencies + type Alias; Z. 520-522 fuer attendance_export_service-Feld; Z. 815-830 fuer attendance_export_service-Construction; Z. 918-922 fuer Struct-Initializer; Z. 1460-1468 fuer RestState-Trait-Impl-Block)
    - .planning/phases/11-export-pdf-csv/11-RESEARCH.md §Q15 Z. 822-883 (vollstaendige Wiring-Bloecke fuer Phase 11)
    - .planning/phases/11-export-pdf-csv/11-PATTERNS.md §10 (5 Edit-Stellen)
    - genossi_service_impl/src/repayment_export.rs (Plan 11.03: Struct-Felder + Deps-Trait)
    - genossi_rest/src/repayment_export.rs (Plan 11.04: RepaymentExportRestState-Trait)
  </read_first>
  <action>
    SCHRITT 1: STELLE 1 — `RepaymentExportServiceDependencies` Type-Alias-Block. Suche im File den AttendanceExportServiceDependencies-Block (`bin/lib.rs:266-289`). Direkt DANACH einfuegen:

    ```rust
    // Phase 11 (EXPO-01..03, EXPO-05): RepaymentExportServiceImpl DI-Aliases.
    pub struct RepaymentExportServiceDependencies;
    unsafe impl Send for RepaymentExportServiceDependencies {}
    unsafe impl Sync for RepaymentExportServiceDependencies {}

    impl genossi_service_impl::repayment_export::RepaymentExportServiceDeps
        for RepaymentExportServiceDependencies
    {
        type Context = Context;
        type Transaction = Transaction;
        type RepaymentPhaseDao = RepaymentPhaseDao;
        type RepaymentEntryDao = RepaymentEntryDao;
        type MemberDao = MemberDao;
        type PermissionService = PermissionService;
        type TransactionDao = TransactionDao;
    }

    type RepaymentExportService =
        genossi_service_impl::repayment_export::RepaymentExportServiceImpl<
            RepaymentExportServiceDependencies,
        >;
    ```

    SCHRITT 2: STELLE 2 — Feld auf `RestStateImpl`-Struct. Suche im File die Zeile mit `attendance_export_service: Arc<AttendanceExportService>,` (`bin/lib.rs:520-522`). Direkt DANACH einfuegen:

    ```rust
    // Phase 11 (EXPO-01..03, EXPO-05): RepaymentExportServiceImpl exposed
    // to REST handlers via RepaymentExportRestState (D-DI wiring).
    repayment_export_service: Arc<RepaymentExportService>,
    ```

    SCHRITT 3: STELLE 3 — Construction in `RestStateImpl::new()`. Suche im File den Block:
    ```rust
    let attendance_export_service = Arc::new(
        genossi_service_impl::attendance_export::AttendanceExportServiceImpl { ... }
    );
    ```
    (laut RESEARCH: `bin/lib.rs:815-830`). Direkt DANACH einfuegen:

    ```rust
    // Phase 11 (EXPO-01..03, EXPO-05): RepaymentExportServiceImpl (D-11 read-only,
    // D-10 status-gate, D-04 hardcoded purpose).
    // Re-uses existing `pdf_generator` and `template_storage` Arcs (same as Phase 6).
    let repayment_export_service = Arc::new(
        genossi_service_impl::repayment_export::RepaymentExportServiceImpl::<
            RepaymentExportServiceDependencies,
        > {
            transaction_dao: transaction_dao.clone(),
            permission_service: permission_service.clone(),
            repayment_phase_dao: repayment_phase_dao.clone(),
            repayment_entry_dao: repayment_entry_dao.clone(),
            member_dao: member_dao.clone(),
            pdf_generator: pdf_generator.clone(),
            template_base: Arc::new(template_storage.base_path().to_path_buf()),
        },
    );
    ```

    **Hinweis:** Falls bei AttendanceExport bereits ein `let attendance_template_base = Arc::new(template_storage.base_path().to_path_buf());`-Helper existiert, kann das wiederverwendet werden statt einer zweiten Allokation. Pruefe das im File. Falls nicht, schreibt der Block selbst die neue Arc.

    SCHRITT 4: STELLE 4 — Struct-Initializer in `RestStateImpl::new()`-Return-Block. Suche im File die Zeile mit `attendance_export_service,` (laut RESEARCH `bin/lib.rs:918-922`, im `RestStateImpl { ... }`-Struct-Literal). Direkt DANACH einfuegen:

    ```rust
    // Phase 11
    repayment_export_service,
    ```

    SCHRITT 5: STELLE 5 — RestState-Trait-Impl. Suche im File den Block:
    ```rust
    impl genossi_rest::attendance_export::AttendanceExportRestState for RestStateImpl { ... }
    ```
    (laut RESEARCH `bin/lib.rs:1460-1468`). Direkt DANACH einfuegen:

    ```rust
    // Phase 11 (EXPO-01..03, EXPO-05): expose RepaymentExportService to REST handlers.
    impl genossi_rest::repayment_export::RepaymentExportRestState for RestStateImpl {
        type RepaymentExportService = RepaymentExportService;
        fn repayment_export_service(&self) -> Arc<Self::RepaymentExportService> {
            self.repayment_export_service.clone()
        }
    }
    ```

    SCHRITT 6: Build + Verify.
  </action>
  <verify>
    <automated>
      cargo build 2>&1 | grep -c "^error" ;
      grep -cE "type RepaymentExportService =" /home/neosam/programming/rust/projects/genossi3/genossi_bin/src/lib.rs;
      grep -cE "repayment_export_service: Arc<RepaymentExportService>" /home/neosam/programming/rust/projects/genossi3/genossi_bin/src/lib.rs;
      grep -cE "RepaymentExportServiceImpl::<" /home/neosam/programming/rust/projects/genossi3/genossi_bin/src/lib.rs;
      grep -cE "fn repayment_export_service\\(&self\\)" /home/neosam/programming/rust/projects/genossi3/genossi_bin/src/lib.rs;
      grep -cE "impl genossi_rest::repayment_export::RepaymentExportRestState" /home/neosam/programming/rust/projects/genossi3/genossi_bin/src/lib.rs
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "type RepaymentExportService =" genossi_bin/src/lib.rs` == 1
    - `grep -cE "repayment_export_service: Arc<RepaymentExportService>" genossi_bin/src/lib.rs` == 1 (Feld)
    - `grep -cE "RepaymentExportServiceImpl::<" genossi_bin/src/lib.rs` >= 1 (Construction-Stelle)
    - `grep -cE "fn repayment_export_service\\(&self\\)" genossi_bin/src/lib.rs` == 1 (Trait-Impl)
    - `grep -cE "impl genossi_rest::repayment_export::RepaymentExportRestState" genossi_bin/src/lib.rs` == 1
    - `grep -cE "pdf_generator: pdf_generator.clone\\(\\)" genossi_bin/src/lib.rs` >= 2 (mind. AttendanceExport + RepaymentExport teilen sich den Arc)
    - `cargo build 2>&1 | grep -c "^error"` == 0 (volle Workspace-Build, KEINE Compile-Fehler mehr)
    - `cargo build -p genossi_bin 2>&1 | grep -c "^error"` == 0
  </acceptance_criteria>
  <done>
    `genossi_bin/src/lib.rs` enthaelt alle 5 Edit-Stellen. Workspace-Build (`cargo build`) ist fehlerfrei. RepaymentExportService ist als Arc in RestStateImpl verfuegbar und wird via Trait-Impl an REST-Handler weitergegeben.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DI ↔ Service | Konstruktion in `RestStateImpl::new()` ist die einzige Stelle, wo Service-Deps instanziert werden; verwendet Arc::clone() statt Arc::new(Dao::new(pool)) (Single-Arc-per-Process — Phase-10-Plan-10.07 Pattern) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-001 | Tampering | RestStateImpl-Wiring | mitigate | `cargo build` als Compile-Time-Gate verifiziert dass alle Deps-Trait-Constraints erfuellt sind. Falsche DAO-Reuse wuerde nicht kompilieren. Severity: LOW. |
| T-006 | Repudiation | Audit-Chain-Inkonsistenz durch separaten DAO-Stack | mitigate | `repayment_phase_dao`, `repayment_entry_dao`, `member_dao` werden via `.clone()` geteilt mit den existierenden audited Services (Phase 7-9). Single-Arc-per-DAO-Process garantiert konsistente Hash-Chain. Severity: LOW. |
| T-007 | Resource Exhaustion | mehrfache PdfGenerator-Allokationen | mitigate | `pdf_generator.clone()` (Arc::clone) statt zweitem `PdfGenerator::new()`-Call — Font-Cache, Package-Cache, World-Resolver werden geteilt. Severity: LOW. |
</threat_model>

<verification>
- `cargo build` (gesamte Workspace) ist fehlerfrei.
- `cargo build -p genossi_bin` ist fehlerfrei.
- Alle 5 Edit-Stellen sind via grep verifizierbar.
- Phase-10-Pattern "Single-Arc-per-Process" wird eingehalten: keine neue `Arc::new(MemberDao::new(...))`-Allokation, nur `.clone()` der existierenden Arcs.
</verification>

<success_criteria>
- EXPO-01 (PDF-Export funktional): Full-Workspace-Build kompiliert; REST-Endpoint ist HTTP-erreichbar (ohne dass er bereits getestet ist — das macht Plan 11.06).
- EXPO-05 (Vorstand-only, kein Audit): geteilter `audit_log_dao` mit existierenden audited Services bleibt konsistent (kein neuer Audit-Log-Path).
- Phase-10-Plan-10.07-Pattern "Single-Arc-per-Process" befolgt.
</success_criteria>

<output>
After completion, create `.planning/phases/11-export-pdf-csv/11-05-SUMMARY.md`
</output>
