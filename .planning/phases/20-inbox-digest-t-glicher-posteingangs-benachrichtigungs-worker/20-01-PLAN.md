---
phase: 20-inbox-digest-t-glicher-posteingangs-benachrichtigungs-worker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/sqlite/20260626000000_create_digest_state_table.sql
  - genossi_mail/src/dao.rs
  - genossi_mail/src/dao_sqlite.rs
autonomous: true
requirements: [DIGEST-03]
must_haves:
  truths:
    - "Eine neue dedizierte SQLite-Tabelle digest_state (bewusst NICHT der Config-KV-Store) persistiert das letzte Digest-Versanddatum (D-03)"
    - "DigestStateDao kann das letzte Versanddatum lesen (None = noch nie gesendet) und upserten"
    - "Wiederholtes set_last_sent_date überschreibt denselben Singleton-Eintrag (kein Duplikat)"
  artifacts:
    - path: "migrations/sqlite/20260626000000_create_digest_state_table.sql"
      provides: "digest_state Tabelle (Singleton-State für last_sent_date)"
      contains: "CREATE TABLE IF NOT EXISTS digest_state"
    - path: "genossi_mail/src/dao.rs"
      provides: "DigestStateDao Trait mit get_last_sent_date / set_last_sent_date"
      contains: "pub trait DigestStateDao"
    - path: "genossi_mail/src/dao_sqlite.rs"
      provides: "DigestStateDaoSqlite Implementierung mit upsert"
      contains: "pub struct DigestStateDaoSqlite"
  key_links:
    - from: "genossi_mail/src/dao_sqlite.rs::DigestStateDaoSqlite"
      to: "digest_state Tabelle"
      via: "INSERT ... ON CONFLICT DO UPDATE upsert"
      pattern: "ON CONFLICT"
---

<objective>
Die DB-Foundation für die „genau-ein-Versand-pro-Kalendertag"-Garantie (D-03) anlegen: eine dedizierte
SQLite-Tabelle `digest_state` (NICHT der Config-KV-Store), ein `DigestStateDao`-Trait im `genossi_mail`-Crate
und die SQLite-Implementierung mit upsert-Semantik — inklusive In-Memory-Unit-Tests.

Purpose: Der Digest-Worker (Plan 02) braucht einen persistenten „letztes Versanddatum"-Zustand, um Dedup
und Nachhol-Garantie (D-01) zu realisieren. Diese Foundation ist unabhängig vom Worker und kann parallel
zur Frontend-Arbeit (Plan 03) laufen.

Output: Migration + DigestStateDao-Trait + DigestStateDaoSqlite + Tests.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/20-inbox-digest-t-glicher-posteingangs-benachrichtigungs-worker/20-CONTEXT.md
@.planning/phases/20-inbox-digest-t-glicher-posteingangs-benachrichtigungs-worker/20-PATTERNS.md

<interfaces>
<!-- Bestehende Konventionen aus der Codebase, die der Executor direkt verwenden soll. -->

Aus genossi_mail/src/dao.rs (Kopf + Fehlertyp, wiederverwenden):
```rust
use async_trait::async_trait;
use mockall::automock;
use std::sync::Arc;
use uuid::Uuid;

#[derive(Debug, Clone)]
pub enum MailDaoError {
    DatabaseError(Arc<str>),
    NotFound,
}
```

Aus genossi_config/src/dao_sqlite.rs (upsert-Vorbild, exakt übernehmen für set):
```rust
sqlx::query(
    "INSERT INTO config_entries (key, value, value_type) VALUES (?, ?, ?)
     ON CONFLICT(key) DO UPDATE SET value = excluded.value, value_type = excluded.value_type",
)
.bind(...).execute(self.pool.as_ref()).await
.map_err(|e| ConfigDaoError::DatabaseError(Arc::from(e.to_string())))?;
```

Aus genossi_mail/src/dao_sqlite.rs:849-857 (Struct + new Pattern, identisch für jeden DAO):
```rust
pub struct InboundMailDaoSqlite { pool: Arc<SqlitePool> }
impl InboundMailDaoSqlite {
    pub fn new(pool: Arc<SqlitePool>) -> Self { Self { pool } }
}
```

In-Memory-Test-Setup (genossi_config/src/dao_sqlite.rs:93-128): SqlitePool::connect("sqlite::memory:")
+ CREATE TABLE inline + Arc::new(pool).

Letzte vorhandene Migration: `migrations/sqlite/20260625000000_add_postal_status_to_member.sql`
→ neuer Timestamp MUSS > 20260625000000 sein.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration für digest_state Singleton-Tabelle</name>
  <read_first>
    - migrations/sqlite/20260608000000_create_inbound_mail_attachments_table.sql (Form/Stil-Vorbild: CREATE TABLE IF NOT EXISTS)
    - migrations/sqlite/20260625000000_add_postal_status_to_member.sql (letzter Timestamp — neuer muss größer sein)
  </read_first>
  <action>
    Lege die Datei `migrations/sqlite/20260626000000_create_digest_state_table.sql` an (Timestamp > 20260625000000,
    Format `<UTC-timestamp>_create_<name>_table.sql`). Verwende die KV-artige, upsert-freundliche Variante (Variante A
    aus 20-PATTERNS.md), damit das set per `ON CONFLICT(key)` exakt das ConfigDaoSqlite-Pattern spiegelt:

    ```sql
    CREATE TABLE IF NOT EXISTS digest_state (
        key TEXT PRIMARY KEY NOT NULL,
        value TEXT NOT NULL
    );
    ```

    Dies ist eine eigene Tabelle, bewusst NICHT der Config-KV-Store (D-03). Der einzige Key wird `last_sent_date`
    sein, der Value ein ISO-Datum im Format `YYYY-MM-DD`. Keine weiteren Spalten, kein Index nötig (max. 1 Row).
    Migrationen laufen automatisch beim Serverstart (sqlx migrate).
  </action>
  <verify>
    <automated>test -f migrations/sqlite/20260626000000_create_digest_state_table.sql && grep -q "CREATE TABLE IF NOT EXISTS digest_state" migrations/sqlite/20260626000000_create_digest_state_table.sql</automated>
  </verify>
  <acceptance_criteria>
    - Datei `migrations/sqlite/20260626000000_create_digest_state_table.sql` existiert
    - Datei enthält exakt den String `CREATE TABLE IF NOT EXISTS digest_state`
    - Datei enthält `key TEXT PRIMARY KEY` und `value TEXT NOT NULL`
    - Timestamp-Präfix `20260626000000` ist lexikografisch größer als `20260625000000`
  </acceptance_criteria>
  <done>Migration-Datei existiert mit Singleton-KV-Tabelle digest_state(key, value).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DigestStateDao Trait + DigestStateDaoSqlite Impl + Tests</name>
  <files>genossi_mail/src/dao.rs, genossi_mail/src/dao_sqlite.rs</files>
  <read_first>
    - genossi_mail/src/dao.rs (Kopf-Imports, MailDaoError, InboundMailAttachmentDao Trait-Form als Vorbild)
    - genossi_mail/src/dao_sqlite.rs (Zeilen 1-57 parse_datetime/format_datetime Helper; 849-948 InboundMailDaoSqlite Struct/new/Pool-Handling/Test-Setup)
    - genossi_config/src/dao_sqlite.rs (Zeilen 51-76 get/set upsert; 93-128 In-Memory-Test-Setup)
  </read_first>
  <behavior>
    - Test 1: get_last_sent_date auf leerer Tabelle liefert Ok(None)
    - Test 2: nach set_last_sent_date(2026-06-26) liefert get_last_sent_date Ok(Some(2026-06-26))
    - Test 3: zweites set_last_sent_date(2026-06-27) überschreibt → get liefert Ok(Some(2026-06-27)), und SELECT COUNT(*) FROM digest_state == 1 (Upsert, kein zweiter Eintrag)
  </behavior>
  <action>
    (a) In `genossi_mail/src/dao.rs` einen neuen Trait hinzufügen (mit `#[automock]` ist Pflicht — alle Traits im
    Crate sind mockbar; erzeugt `MockDigestStateDao` für Plan-02-Worker-Tests). `time::Date` als Datums-Typ:

    ```rust
    #[automock]
    #[async_trait]
    pub trait DigestStateDao: Send + Sync + 'static {
        /// Letztes Digest-Versanddatum (None = noch nie gesendet).
        async fn get_last_sent_date(&self) -> Result<Option<time::Date>, MailDaoError>;
        /// Setzt (upsert) das letzte Digest-Versanddatum.
        async fn set_last_sent_date(&self, date: time::Date) -> Result<(), MailDaoError>;
    }
    ```

    (b) In `genossi_mail/src/dao_sqlite.rs` die Implementierung anlegen (gleiches Struct/new-Pattern wie
    InboundMailDaoSqlite):

    ```rust
    pub struct DigestStateDaoSqlite {
        pool: Arc<SqlitePool>,
    }
    impl DigestStateDaoSqlite {
        pub fn new(pool: Arc<SqlitePool>) -> Self { Self { pool } }
    }

    const LAST_SENT_DATE_KEY: &str = "last_sent_date";

    #[async_trait]
    impl DigestStateDao for DigestStateDaoSqlite {
        async fn get_last_sent_date(&self) -> Result<Option<time::Date>, MailDaoError> {
            let row: Option<(String,)> = sqlx::query_as(
                "SELECT value FROM digest_state WHERE key = ?",
            )
            .bind(LAST_SENT_DATE_KEY)
            .fetch_optional(self.pool.as_ref())
            .await
            .map_err(|e| MailDaoError::DatabaseError(Arc::from(e.to_string())))?;

            match row {
                None => Ok(None),
                Some((s,)) => {
                    let fmt = time::macros::format_description!("[year]-[month]-[day]");
                    let date = time::Date::parse(&s, &fmt)
                        .map_err(|e| MailDaoError::DatabaseError(Arc::from(e.to_string())))?;
                    Ok(Some(date))
                }
            }
        }

        async fn set_last_sent_date(&self, date: time::Date) -> Result<(), MailDaoError> {
            let fmt = time::macros::format_description!("[year]-[month]-[day]");
            let value = date.format(&fmt)
                .map_err(|e| MailDaoError::DatabaseError(Arc::from(e.to_string())))?;
            sqlx::query(
                "INSERT INTO digest_state (key, value) VALUES (?, ?)
                 ON CONFLICT(key) DO UPDATE SET value = excluded.value",
            )
            .bind(LAST_SENT_DATE_KEY)
            .bind(value)
            .execute(self.pool.as_ref())
            .await
            .map_err(|e| MailDaoError::DatabaseError(Arc::from(e.to_string())))?;
            Ok(())
        }
    }
    ```

    Importiere `DigestStateDao` und `MailDaoError` oben im dao_sqlite.rs (gleicher `use crate::dao::...`-Block wie
    die anderen DAOs). Falls `time::macros::format_description` nicht verfügbar ist, nutze die im dao_sqlite.rs bereits
    vorhandenen parse/format-Helper-Konventionen (Zeilen 14-37) analog für `time::Date`.

    (c) Tests in einem `#[cfg(test)] mod`-Block in dao_sqlite.rs (Pattern aus genossi_config/src/dao_sqlite.rs:93-128):
    `setup_db()` öffnet `sqlite::memory:`, führt `CREATE TABLE digest_state (key TEXT PRIMARY KEY NOT NULL, value TEXT NOT NULL)`
    aus, gibt `Arc::new(pool)` zurück. Drei `#[tokio::test]` gemäß <behavior> oben. Für `time::Date` in Tests:
    `time::Date::from_calendar_date(2026, time::Month::June, 26).unwrap()`.

    `genossi_mail/src/lib.rs` deklariert `pub mod dao;` und `pub mod dao_sqlite;` bereits — keine lib.rs-Änderung nötig.
  </action>
  <verify>
    <automated>cargo test -p genossi_mail digest_state 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `genossi_mail/src/dao.rs` enthält `pub trait DigestStateDao` mit `#[automock]` darüber
    - `genossi_mail/src/dao.rs` enthält `async fn get_last_sent_date` und `async fn set_last_sent_date`
    - `genossi_mail/src/dao_sqlite.rs` enthält `pub struct DigestStateDaoSqlite` und `impl DigestStateDao for DigestStateDaoSqlite`
    - `genossi_mail/src/dao_sqlite.rs` enthält `ON CONFLICT(key) DO UPDATE SET value = excluded.value`
    - `cargo test -p genossi_mail digest_state` exits 0 mit mindestens 3 bestandenen Tests
    - `cargo build -p genossi_mail` exits 0
  </acceptance_criteria>
  <done>DigestStateDao + DigestStateDaoSqlite existieren, upsert funktioniert, 3 Tests grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App-Prozess → SQLite | Schreibt/liest ein internes Versanddatum; kein externer Input erreicht diese Tabelle direkt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | digest_state value | accept | Wert ist ein intern erzeugtes Datum (`time::Date`), kein Nutzer-Input; SQL via parametrisierte Binds (kein String-Concat) |
| T-20-02 | Information Disclosure | digest_state Tabelle | accept | Enthält nur ein Datum, keine PII; gleiche Vertrauensgrenze wie übrige Backend-Tabellen |
</threat_model>

<verification>
- `cargo build -p genossi_mail` grün
- `cargo test -p genossi_mail digest_state` grün (≥3 Tests)
- Migration-Datei vorhanden mit korrektem Timestamp-Präfix
</verification>

<success_criteria>
- Tabelle digest_state per Migration anlegbar (Singleton-KV)
- DigestStateDao liest None bei leerer Tabelle und persistiert/überschreibt das Versanddatum per Upsert
- MockDigestStateDao steht für Plan-02-Worker-Tests zur Verfügung
</success_criteria>

<output>
After completion, create `.planning/phases/20-inbox-digest-t-glicher-posteingangs-benachrichtigungs-worker/20-01-SUMMARY.md`
</output>
