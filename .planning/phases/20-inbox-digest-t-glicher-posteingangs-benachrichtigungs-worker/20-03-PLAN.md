---
phase: 20-inbox-digest-t-glicher-posteingangs-benachrichtigungs-worker
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - genossi-frontend/src/page/config_page.rs
autonomous: true
requirements: [DIGEST-01, DIGEST-02, DIGEST-07]
must_haves:
  truths:
    - "Vorstand trägt auf der Config-Seite Empfänger-Adressen (komma-getrennt) und Versand-Uhrzeit ein"
    - "Beide Werte bleiben nach Reload erhalten (aus Config-KV-Store geladen)"
    - "Ungültige E-Mail-Adressen oder ungültige HH:MM-Uhrzeit werden vor dem Speichern inline gemeldet"
    - "Ein leeres Empfänger-Feld ist erlaubt und deaktiviert das Feature (DIGEST-07/D-14)"
  artifacts:
    - path: "genossi-frontend/src/page/config_page.rs"
      provides: "Config-Abschnitt 'Posteingangs-Benachrichtigung' mit Empfänger-Feld + Uhrzeit + Speichern + Inline-Validierung"
      contains: "Posteingangs-Benachrichtigung"
  key_links:
    - from: "config_page.rs Digest-Abschnitt"
      to: "Config-KV-Store über api::set_config_entry"
      via: "Speichern von digest_recipients + digest_send_time"
      pattern: "digest_recipients"
    - from: "config_page.rs reload()"
      to: "digest-Signals"
      via: "get_config_value populate nach Load"
      pattern: "get_config_value\\(&data, \"digest"
---

<objective>
Auf der Config-Seite (`config_page.rs`) einen eigenen Abschnitt „Posteingangs-Benachrichtigung" anlegen
(im Stil der bestehenden SMTP/IMAP-Blöcke, D-12 — NICHT in den IMAP-Block integriert) mit einem
komma-getrennten Empfänger-Textfeld (D-05), einem Uhrzeit-Feld (HH:MM, D-02) und einem Speichern-Button,
der `digest_recipients` und `digest_send_time` über das bestehende Config-REST persistiert — inklusive
clientseitiger Inline-Validierung (D-13) und Deaktivierung über leeres Empfänger-Feld (D-14/DIGEST-07).

Purpose: Erfüllt DIGEST-01 (Empfänger pflegen) + DIGEST-02 (Uhrzeit konfigurieren) + DIGEST-07
(leeres Feld = deaktiviert). Reines Frontend, unabhängig vom Backend-Worker → läuft parallel in Wave 1.
Die Config-Keys (`digest_recipients`, `digest_send_time`) sind identisch zu denen, die der Worker (Plan 02) liest.

Output: neuer CollapsibleSection-Abschnitt + Signals + reload-Populate + Save-Flow + Inline-Validierung.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/20-inbox-digest-t-glicher-posteingangs-benachrichtigungs-worker/20-CONTEXT.md
@.planning/phases/20-inbox-digest-t-glicher-posteingangs-benachrichtigungs-worker/20-PATTERNS.md

<interfaces>
<!-- Echte Signaturen/Helfer aus der Codebase — direkt verwenden, keine neuen Helfer erfinden. -->

Vorhandene Helfer (config_page.rs:12-22) — WIEDERVERWENDEN, nicht neu schreiben:
```rust
fn get_config_value(entries: &[ConfigEntryTO], key: &str) -> String { ... }   // find key → value oder ""
fn has_config_key(entries: &[ConfigEntryTO], key: &str) -> bool { ... }
```

Save-API (genossi-frontend/src/api.rs:633):
```rust
pub async fn set_config_entry(config: &Config, key: &str, value: &str, value_type: &str)
    -> Result<ConfigEntryTO, AppError>;
```

reload()-Closure (config_page.rs:101-184): lädt entries via api::get_config_entries, ruft danach
`get_config_value(&data, "...").set(...)` für jedes Feld; am Ende `entries.set(data)` + `loading.set(false)`.

Signal-State-Vorbild (IMAP-Block, config_page.rs:48-58):
```rust
let mut imap_host = use_signal(|| String::new());
let mut imap_poll_interval = use_signal(|| "300".to_string());
let mut imap_saving = use_signal(|| false);
```

Bestehende Fehler-/Erfolg-Signals (config_page.rs:29-30): `error: Signal<Option<api::AppError>>`,
`success_msg: Signal<Option<String>>`.

Save-Flow-Vorbild (IMAP-Block, config_page.rs:721-777): Felder lesen → `spawn` → `digest_saving.set(true)` →
`error.set(None)` + `success_msg.set(None)` → `CONFIG.read().clone()` → für jedes (key,value,vtype) `api::set_config_entry`
→ bei Erfolg `success_msg.set(Some(...))` + `reload()` → `digest_saving.set(false)`.

CollapsibleSection-Wrapper (config_page.rs:558 IMAP, 466 Mail-Footer): `CollapsibleSection { title: "...".to_string(), div { class: "space-y-4", ... } }`.
Bestehende Abschnitte enden vor dem nächsten CollapsibleSection — neuen Abschnitt zwischen IMAP-Abschnitt
(endet ~Zeile 782) und WebDAV-Abschnitt (Zeile 783) einfügen, thematisch passend zu Mail/Inbox.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Digest-Signals + reload-Populate</name>
  <files>genossi-frontend/src/page/config_page.rs</files>
  <read_first>
    - genossi-frontend/src/page/config_page.rs (Zeilen 27-99 Signal-Deklarationen inkl. IMAP-Block 48-58; Zeilen 101-184 reload-Closure mit Populate-Pattern)
  </read_first>
  <action>
    (a) Bei den Signal-Deklarationen (nahe Zeile 58, direkt nach dem IMAP-Signal-Block) neue Signals anlegen:
    ```rust
    // Digest / Posteingangs-Benachrichtigung (Phase 20)
    let mut digest_recipients = use_signal(|| String::new());
    let mut digest_send_time = use_signal(|| "08:00".to_string());
    let mut digest_saving = use_signal(|| false);
    ```
    Defaults: leeres Empfänger-Feld (D-14: leer = deaktiviert), Uhrzeit-Default "08:00".

    (b) In der `reload()`-Closure (im Erfolgs-`Ok(data)`-Zweig, nahe Zeile 136-140, nach dem IMAP-Populate) ergänzen:
    ```rust
    digest_recipients.set(get_config_value(&data, "digest_recipients"));
    let dst = get_config_value(&data, "digest_send_time");
    if !dst.is_empty() {
        digest_send_time.set(dst);
    }
    ```
    Empfänger werden direkt gesetzt (leerer String ist gültig). Die Uhrzeit nur überschreiben, wenn ein Wert
    existiert (sonst bleibt der "08:00"-Default). Config-Keys exakt `digest_recipients` und `digest_send_time`
    (identisch zum Worker in Plan 02).
  </action>
  <verify>
    <automated>grep -q "digest_recipients = use_signal" genossi-frontend/src/page/config_page.rs && grep -q "get_config_value(&data, \"digest_recipients\")" genossi-frontend/src/page/config_page.rs</automated>
  </verify>
  <acceptance_criteria>
    - config_page.rs enthält `let mut digest_recipients = use_signal`
    - config_page.rs enthält `let mut digest_send_time = use_signal`
    - config_page.rs enthält `let mut digest_saving = use_signal`
    - config_page.rs enthält `get_config_value(&data, "digest_recipients")` in der reload-Closure
    - config_page.rs enthält `get_config_value(&data, "digest_send_time")`
  </acceptance_criteria>
  <done>Signals deklariert, reload populiert beide Werte aus dem Config-KV-Store.</done>
</task>

<task type="auto">
  <name>Task 2: CollapsibleSection-Abschnitt mit Feldern, Inline-Validierung und Save-Flow</name>
  <files>genossi-frontend/src/page/config_page.rs</files>
  <read_first>
    - genossi-frontend/src/page/config_page.rs (Zeilen 558-782 kompletter IMAP-CollapsibleSection-Block als RSX-/Save-Vorbild; Zeile 783 WebDAV-Abschnitt — davor einfügen)
    - genossi-frontend/CLAUDE.md (Component-First — Begründung warum inline hier konsistent ist)
  </read_first>
  <action>
    Füge einen neuen `CollapsibleSection`-Block direkt VOR dem WebDAV-Abschnitt (Zeile 783) ein. Aufbau analog
    zum IMAP-Block (config_page.rs:558-782):

    ```rust
    CollapsibleSection { title: "Posteingangs-Benachrichtigung".to_string(),
        div { class: "space-y-4",
            p { class: "text-sm text-gray-600",
                "Tägliche Zusammenfassung offener Posteingangs-Mails. Mehrere Empfänger komma-getrennt. Leeres Empfänger-Feld deaktiviert die Benachrichtigung."
            }
            // Empfänger-Feld
            div {
                label { class: "block text-sm font-medium text-gray-700 mb-1", "Empfänger (komma-getrennt)" }
                input {
                    class: "w-full border rounded px-3 py-2",
                    r#type: "text",
                    placeholder: "vorstand@genossenschaft.de, kontakt@genossenschaft.de",
                    value: "{digest_recipients}",
                    oninput: move |e| digest_recipients.set(e.value()),
                }
            }
            // Uhrzeit-Feld
            div {
                label { class: "block text-sm font-medium text-gray-700 mb-1", "Versand-Uhrzeit (HH:MM, Server-Zeit)" }
                input {
                    class: "w-full border rounded px-3 py-2",
                    r#type: "text",
                    placeholder: "08:00",
                    value: "{digest_send_time}",
                    oninput: move |e| digest_send_time.set(e.value()),
                }
            }
            // Speichern-Button
            button {
                class: "bg-blue-500 hover:bg-blue-600 text-white px-6 py-2 rounded disabled:opacity-50",
                r#type: "button",
                disabled: *digest_saving.read(),
                onclick: move |_| {
                    let recipients = digest_recipients.read().clone();
                    let send_time = digest_send_time.read().clone();

                    // Inline-Validierung (D-13) VOR dem spawn
                    // Empfänger: leer ist erlaubt (D-14); sonst jede komma-getrennte Adresse grob prüfen
                    let trimmed = recipients.trim();
                    if !trimmed.is_empty() {
                        let mut invalid = false;
                        for addr in trimmed.split(',') {
                            let a = addr.trim();
                            // grobes Format: nicht-leer, genau ein '@', nicht-leer links/rechts, '.' rechts vom '@'
                            let parts: Vec<&str> = a.split('@').collect();
                            if parts.len() != 2 || parts[0].is_empty() || parts[1].is_empty() || !parts[1].contains('.') {
                                invalid = true;
                                break;
                            }
                        }
                        if invalid {
                            error.set(Some(api::AppError::from("Ungültige E-Mail-Adresse im Empfänger-Feld".to_string())));
                            return;
                        }
                    }
                    // Uhrzeit HH:MM validieren
                    let tparts: Vec<&str> = send_time.trim().split(':').collect();
                    let time_ok = tparts.len() == 2
                        && tparts[0].parse::<u8>().map(|h| h <= 23).unwrap_or(false)
                        && tparts[1].parse::<u8>().map(|m| m <= 59).unwrap_or(false);
                    if !time_ok {
                        error.set(Some(api::AppError::from("Ungültige Uhrzeit (Format HH:MM, z.B. 08:00)".to_string())));
                        return;
                    }

                    spawn(async move {
                        digest_saving.set(true);
                        error.set(None);
                        success_msg.set(None);
                        let config = CONFIG.read().clone();
                        let mut all_ok = true;
                        let entries_to_save: Vec<(&str, String, &str)> = vec![
                            ("digest_recipients", recipients, "string"),
                            ("digest_send_time", send_time, "string"),
                        ];
                        for (key, value, vtype) in &entries_to_save {
                            if let Err(e) = api::set_config_entry(&config, key, value, vtype).await {
                                error.set(Some(e));
                                all_ok = false;
                                break;
                            }
                        }
                        if all_ok {
                            success_msg.set(Some("Posteingangs-Benachrichtigung gespeichert".to_string()));
                            reload();
                        }
                        digest_saving.set(false);
                    });
                },
                if *digest_saving.read() { "Speichere…" } else { "Speichern" }
            }
        }
    }
    ```

    Wichtige Details:
    - `r#type: "button"` + onclick (NICHT form-onsubmit) wegen des bekannten Dioxus-Button-Reload-Bugs (Memory feedback_dioxus_button_type).
    - Empfänger-Feld darf leer gespeichert werden (D-14): die Validierung überspringt leeres `trimmed`, der Save speichert
      den leeren String, der Worker (Plan 02) skippt dann ohne Fehler (DIGEST-07).
    - Beide Keys werden als `"string"` value_type gespeichert (D-05/D-14: Empfänger ist ein einzelnes Textfeld).
    - Falls `api::AppError::from(String)` nicht existiert, nutze die im Crate übliche AppError-Konstruktion für
      Client-seitige Validierungsfehler (prüfe genossi-frontend/src/api.rs AppError-Varianten; z.B. eine
      `AppError::Message(...)`/`AppError::Other(...)`-Variante). Ziel: error-Signal mit lesbarer Meldung füllen,
      die im bestehenden ErrorAlert-Block (config_page.rs) angezeigt wird.

    Component-First-Hinweis (genossi-frontend/CLAUDE.md): SMTP/IMAP sind aktuell inline in config_page.rs; ein
    einmaliger Config-Abschnitt im gleichen Stil ist konsistent mit dem Bestand — keine separate Component nötig.
  </action>
  <verify>
    <automated>cd genossi-frontend && cargo check 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - config_page.rs enthält `CollapsibleSection { title: "Posteingangs-Benachrichtigung"`
    - config_page.rs enthält ein input mit `oninput: move |e| digest_recipients.set(e.value())`
    - config_page.rs enthält ein input mit `oninput: move |e| digest_send_time.set(e.value())`
    - config_page.rs enthält `("digest_recipients", recipients, "string")` und `("digest_send_time", send_time, "string")` im Save-Flow
    - config_page.rs enthält die HH:MM-Validierung (`split(':')` + `parse::<u8>`) und die E-Mail-Format-Prüfung (`split('@')`)
    - config_page.rs Save-Button verwendet `r#type: "button"` (kein form-onsubmit)
    - `cargo check` (genossi-frontend) exits 0
  </acceptance_criteria>
  <done>Neuer Config-Abschnitt rendert, validiert inline und speichert beide Keys; cargo check grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vorstand-Browser → Config-REST | Authentifizierter Vorstand pflegt Empfänger + Uhrzeit über die Config-Seite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | Tampering | digest_recipients Eingabe | mitigate | Clientseitige Inline-Validierung (D-13): jede komma-getrennte Adresse muss grobes `@`-Format mit Domain-Punkt erfüllen; ungültige Eingabe wird vor dem Speichern abgewiesen. Backend-Worker trimmt/filtert zusätzlich (Plan 02 parse_recipients) |
| T-20-08 | Tampering | digest_send_time Eingabe | mitigate | HH:MM-Validierung (0–23 / 0–59) vor dem Speichern; Worker parst defensiv (parse_send_time → None bei ungültig → skip) |
| T-20-09 | Elevation of Privilege | Config-REST Endpoint | accept | Bestehender Config-Endpoint ist bereits Vorstand-geschützt; dieses Plan fügt keine neue Auth-Fläche hinzu |
</threat_model>

<verification>
- `cargo check` (genossi-frontend) grün
- Manuell (optional, kein Blocker): Config-Seite öffnen, Empfänger + Uhrzeit eintragen, speichern, Reload → Werte erhalten;
  ungültige Adresse / Uhrzeit → Inline-Fehler; leeres Empfänger-Feld → speicherbar
</verification>

<success_criteria>
- DIGEST-01: Empfänger-Adressen pflegbar (speichern + ändern), bleiben nach Reload erhalten
- DIGEST-02: Versand-Uhrzeit konfigurierbar, bleibt nach Reload erhalten
- DIGEST-07/D-14: leeres Empfänger-Feld ist gültig und deaktiviert das Feature
- D-13: ungültige E-Mail / Uhrzeit wird inline gemeldet, kein Speichern
</success_criteria>

<output>
After completion, create `.planning/phases/20-inbox-digest-t-glicher-posteingangs-benachrichtigungs-worker/20-03-SUMMARY.md`
</output>
