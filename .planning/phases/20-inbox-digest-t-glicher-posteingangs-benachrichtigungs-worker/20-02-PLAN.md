---
phase: 20-inbox-digest-t-glicher-posteingangs-benachrichtigungs-worker
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - genossi_mail/src/digest.rs
  - genossi_mail/src/lib.rs
  - genossi_bin/src/lib.rs
  - genossi_bin/src/main.rs
autonomous: true
requirements: [DIGEST-03, DIGEST-04, DIGEST-05, DIGEST-06, DIGEST-07]
must_haves:
  truths:
    - "Ein Hintergrund-Worker pollt periodisch (~60s, KEIN sleep-bis-Uhrzeit) und vergleicht Server-Lokalzeit (TZ env, kein chrono-tz) + letztes Versanddatum (D-02, D-04)"
    - "Zur konfigurierten Uhrzeit erhält jeder konfigurierte Empfänger (komma-getrennt gepflegtes Feld) genau eine Digest-Mail pro Kalendertag (D-05)"
    - "Ein verpasstes Zeitfenster wird beim nächsten Lauf nachgeholt (Catch-up, D-01)"
    - "Bei leerem Posteingang geht keine Mail raus (DIGEST-04)"
    - "Ohne konfigurierte Empfänger geht keine Mail raus, kein Fehler (DIGEST-07)"
    - "Die Digest-Mail ist Plain-Text mit hardcodiertem deutschem Body (kein minijinja), listet jede offene Mail mit Titel, Absender, Eingangszeit (neueste zuerst), nennt die Anzahl offener Mails im Betreff und enthält einen aus APP_URL gebauten {APP_URL}/inbox Deep-Link (D-08, D-09, D-10, D-11)"
    - "Versand als Einzelmail pro Empfänger; ein fehlerhafter Empfänger blockiert die übrigen nicht und das Versanddatum wird trotzdem gesetzt (kein Retry) (D-06, D-07)"
  artifacts:
    - path: "genossi_mail/src/digest.rs"
      provides: "Digest-Worker (poll-loop) + reine, unit-getestete Helfer (parse_recipients, parse_send_time, is_due, build_digest_subject, build_digest_body)"
      contains: "pub async fn start_digest_worker"
      min_lines: 120
    - path: "genossi_bin/src/lib.rs"
      provides: "start_digest_worker DI-Wiring-Methode"
      contains: "fn start_digest_worker"
    - path: "genossi_bin/src/main.rs"
      provides: "Spawn-Aufruf beim Serverstart"
      contains: "start_digest_worker"
  key_links:
    - from: "genossi_mail/src/digest.rs"
      to: "DigestStateDao (Plan 01)"
      via: "get_last_sent_date / set_last_sent_date für Dedup + Catch-up"
      pattern: "set_last_sent_date"
    - from: "genossi_mail/src/digest.rs"
      to: "MailService::send_test_mail_with_body"
      via: "Einzelmail pro Empfänger in Schleife"
      pattern: "send_test_mail_with_body"
    - from: "genossi_bin/src/main.rs"
      to: "RestStateImpl::start_digest_worker"
      via: "rest_state.start_digest_worker()"
      pattern: "start_digest_worker"
---

<objective>
Den Digest-Worker (`genossi_mail/src/digest.rs`) bauen — exakt nach dem Vorbild `timestamp_worker.rs`:
ein config-getriebener poll-loop (~60s), der Server-Lokalzeit + letztes Versanddatum vergleicht, bei
Fälligkeit alle offenen (nicht-archivierten) Mails sammelt, pro konfiguriertem Empfänger genau eine
Plain-Text-Digest-Mail verschickt und das Versanddatum persistiert. Plus DI-Wiring in lib.rs/main.rs.

Purpose: Erfüllt den Kern der Phase — DIGEST-03/04/05/06/07. Die Versand-Logik wird in reine,
unit-testbare Funktionen ausgelagert (parse_recipients, parse_send_time, is_due, build_digest_subject,
build_digest_body), sodass alle Edge-Cases (Catch-up, leerer Posteingang, kein Empfänger, Betreff-/Body-
Format) ohne laufenden Loop getestet werden.

Output: digest.rs (Worker + Helfer + Tests), mod-Deklaration, start_digest_worker in lib.rs, Spawn in main.rs.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/20-inbox-digest-t-glicher-posteingangs-benachrichtigungs-worker/20-CONTEXT.md
@.planning/phases/20-inbox-digest-t-glicher-posteingangs-benachrichtigungs-worker/20-PATTERNS.md
@.planning/phases/20-inbox-digest-t-glicher-posteingangs-benachrichtigungs-worker/20-01-SUMMARY.md

<interfaces>
<!-- Echte Signaturen aus der Codebase — direkt verwenden, keine Exploration nötig. -->

ConfigService (genossi_config/src/service.rs:31-34):
```rust
pub trait ConfigService: Send + Sync + 'static {
    async fn get_all(&self) -> Result<Arc<[ConfigEntry]>, ConfigServiceError>;
    async fn get(&self, key: &str) -> Result<ConfigEntry, ConfigServiceError>;
    async fn set(&self, entry: &ConfigEntry) -> Result<(), ConfigServiceError>;
}
```
ConfigEntry (genossi_config/src/dao.rs:12-15): `key: Arc<str>`, `value: Arc<str>`, `value_type: Arc<str>`.

InboxService (genossi_mail/src/inbox.rs:451-452):
```rust
pub trait InboxService: Send + Sync + 'static {
    async fn list(&self) -> Result<Arc<[InboundMail]>, MailServiceError>;
    // ... weitere Methoden
}
```
InboundMail (genossi_mail/src/dao.rs:270-290) Felder für den Digest:
`from_address: Arc<str>`, `subject: Arc<str>`, `received_at: time::PrimitiveDateTime`, `archived: bool`.
HINWEIS: InboxService::list() liefert bereits ORDER BY received_at DESC (neueste zuerst, D-10) —
KEINE erneute Sortierung nötig. Nur `.filter(|m| !m.archived)` für offene Mails.

MailService (genossi_mail/src/service.rs:107-112):
```rust
async fn send_test_mail_with_body(&self, to: &str, subject: &str, body: &str)
    -> Result<(), MailServiceError>;
```
Baut SMTP-Transport selbst via load_smtp_config — KEINE SMTP-Logik im Worker duplizieren. Plain-Text (D-08).

DigestStateDao (genossi_mail/src/dao.rs, Plan 01):
```rust
async fn get_last_sent_date(&self) -> Result<Option<time::Date>, MailDaoError>;
async fn set_last_sent_date(&self, date: time::Date) -> Result<(), MailDaoError>;
```

timestamp_worker Loop-Skelett (genossi_service_impl/src/timestamp_worker.rs:24-69) — exakt diese Struktur:
get_all() → bei Fehler error+sleep+continue → Werte ableiten → Aktion → sleep am Loop-Ende.

APP_URL Deep-Link (genossi_rest/src/helper_token.rs:38-41, D-11):
```rust
let app_url = std::env::var("APP_URL").unwrap_or_else(|_| "http://localhost:3000/".to_string());
let deep_link = format!("{}/inbox", app_url.trim_end_matches('/'));
```

DI-Wiring (genossi_bin/src/lib.rs): Felder `inbox_service: Arc<InboxServiceType>` (Zeile 630),
`mail_service: Arc<MailServiceType>` (Zeile 628) existieren bereits → in start_digest_worker klonen.
DigestStateDaoSqlite via `DigestStateDaoSqlite::new(self.pool.clone())` bauen (Pattern lib.rs:1464).
Worker-Spawn-Vorbild: start_timestamp_worker (lib.rs:1443-1454). main.rs Spawn-Vorbild: main.rs:54-55.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Digest-Worker + reine Helfer + Unit-Tests (digest.rs)</name>
  <files>genossi_mail/src/digest.rs, genossi_mail/src/lib.rs</files>
  <read_first>
    - genossi_service_impl/src/timestamp_worker.rs (komplett — Loop-Skelett, free-function Config-Reader, Test-Pattern mit make_entry)
    - genossi_mail/src/inbox.rs (Zeilen 451-488 InboxService-Trait; 764-819 start_inbox_worker/poll_once ConfigMissing-Skip)
    - genossi_mail/src/service.rs (Zeilen 52-116 MailService-Trait inkl. send_test_mail_with_body)
    - genossi_mail/src/dao.rs (Zeilen 270-290 InboundMail-Felder; DigestStateDao aus Plan 01)
    - genossi_rest/src/helper_token.rs (Zeilen 36-41 APP_URL Deep-Link)
  </read_first>
  <behavior>
    - parse_recipients: "a@x.de, b@y.de" → vec!["a@x.de","b@y.de"]; "" → vec![]; "  " → vec![] (trim, leere Einträge raus)
    - parse_send_time: "08:30" → Some((8,30)); "8:5" → Some((8,5)); "" → None; "abc" → None; "25:00" → None; "08:99" → None
    - is_due(now_local, send_time=(8,0), last_sent_date): heute schon gesendet → false; nie gesendet & now >= 08:00 → true (Catch-up D-01); nie gesendet & now < 08:00 → false; gestern gesendet & now >= 08:00 → true; gestern gesendet & now < 08:00 → false
    - build_digest_subject(5) → "Posteingang: 5 offene Mails"; build_digest_subject(1) → enthält "1"
    - build_digest_body: enthält jede Mail-Zeile (subject + from_address + received_at), neueste zuerst (Reihenfolge der Eingabe beibehalten), und den Deep-Link {app_url}/inbox; nicht-leerer Body
  </behavior>
  <action>
    Lege `genossi_mail/src/digest.rs` an und füge `pub mod digest;` in `genossi_mail/src/lib.rs` hinzu (alphabetisch
    bei den anderen `pub mod`-Zeilen einsortieren).

    Config-Keys (D-05, Claude's Discretion → festgelegt): `"digest_recipients"` (komma-getrenntes Textfeld) und
    `"digest_send_time"` (Format "HH:MM"). Diese Strings MÜSSEN identisch zu denen im Frontend (Plan 03) sein.

    Reine Helfer-Funktionen (Test-Oberfläche, free functions wie in timestamp_worker.rs:8-22):

    ```rust
    use genossi_config::dao::ConfigEntry;

    pub(crate) fn parse_recipients(entries: &[ConfigEntry]) -> Vec<String> {
        entries.iter()
            .find(|e| e.key.as_ref() == "digest_recipients")
            .map(|e| e.value.as_ref()
                .split(',')
                .map(|s| s.trim())
                .filter(|s| !s.is_empty())
                .map(|s| s.to_string())
                .collect())
            .unwrap_or_default()
    }

    pub(crate) fn parse_send_time(entries: &[ConfigEntry]) -> Option<(u8, u8)> {
        let raw = entries.iter().find(|e| e.key.as_ref() == "digest_send_time")?;
        let mut parts = raw.value.as_ref().split(':');
        let h: u8 = parts.next()?.trim().parse().ok()?;
        let m: u8 = parts.next()?.trim().parse().ok()?;
        if parts.next().is_some() { return None; }
        if h > 23 || m > 59 { return None; }
        Some((h, m))
    }

    /// D-01 Catch-up + D-03 ein-Versand-pro-Tag. now_local = Server-Lokalzeit.
    pub(crate) fn is_due(
        now_local: time::OffsetDateTime,
        send_time: (u8, u8),
        last_sent_date: Option<time::Date>,
    ) -> bool {
        let today = now_local.date();
        if last_sent_date == Some(today) { return false; } // heute schon gesendet
        let (h, m) = send_time;
        let now_minutes = now_local.hour() as u32 * 60 + now_local.minute() as u32;
        let send_minutes = h as u32 * 60 + m as u32;
        now_minutes >= send_minutes // Uhrzeit erreicht/überschritten → fällig (Catch-up inklusive)
    }

    pub(crate) fn build_digest_subject(count: usize) -> String {
        format!("Posteingang: {} offene Mails", count)
    }

    pub(crate) fn build_digest_body(mails: &[&genossi_mail::dao::InboundMail], inbox_url: &str) -> String {
        let mut body = String::from("Guten Tag,\n\nim Posteingang liegen offene Nachrichten:\n\n");
        for m in mails {
            body.push_str(&format!(
                "- {} (von: {}, eingegangen: {})\n",
                m.subject, m.from_address, m.received_at
            ));
        }
        body.push_str(&format!("\nZum Posteingang: {}\n", inbox_url));
        body
    }
    ```
    Hinweis: Falls `genossi_mail::dao::InboundMail` nicht direkt importierbar im Signatur-Pfad, nutze `crate::dao::InboundMail`.
    Die `received_at`-Formatierung darf das Default-`Display` von `PrimitiveDateTime` verwenden (lesbar, kein eigener Formatter nötig).

    Worker-Loop (exakt timestamp_worker.rs:24-69 nachbauen, Bounds wie inbox.rs:764-776):

    ```rust
    const POLL_INTERVAL_SECS: u64 = 60;

    pub async fn start_digest_worker<C, I, M, S>(
        config_service: Arc<C>,
        inbox_service: Arc<I>,
        mail_service: Arc<M>,
        digest_state_dao: Arc<S>,
    ) where
        C: genossi_config::service::ConfigService,
        I: crate::inbox::InboxService,
        M: crate::service::MailService,
        S: crate::dao::DigestStateDao,
    {
        tracing::info!("Digest worker started");
        loop {
            let entries = match config_service.get_all().await {
                Ok(e) => e,
                Err(e) => {
                    tracing::error!("Digest worker: failed to read config: {:?}", e);
                    tokio::time::sleep(std::time::Duration::from_secs(POLL_INTERVAL_SECS)).await;
                    continue;
                }
            };
            let recipients = parse_recipients(&entries);
            let send_time = parse_send_time(&entries);

            // DIGEST-07/D-14: keine Empfänger ODER keine gültige Uhrzeit ⇒ skip ohne Fehler
            if recipients.is_empty() || send_time.is_none() {
                tracing::debug!("Digest worker: no recipients or no send time, skipping");
                tokio::time::sleep(std::time::Duration::from_secs(POLL_INTERVAL_SECS)).await;
                continue;
            }
            let send_time = send_time.unwrap();

            // Server-Lokalzeit (D-02). Fallback auf now_utc, falls now_local fehlschlägt.
            let now_local = time::OffsetDateTime::now_local()
                .unwrap_or_else(|_| time::OffsetDateTime::now_utc());

            let last_sent = digest_state_dao.get_last_sent_date().await.unwrap_or(None);

            if is_due(now_local, send_time, last_sent) {
                // offene Mails laden (DIGEST-04/05)
                match inbox_service.list().await {
                    Ok(mails) => {
                        let offen: Vec<&crate::dao::InboundMail> =
                            mails.iter().filter(|m| !m.archived).collect();
                        if offen.is_empty() {
                            tracing::debug!("Digest worker: inbox empty, not sending (DIGEST-04)");
                            // KEIN set_last_sent_date — leerer Tag gilt nicht als erledigt
                        } else {
                            let app_url = std::env::var("APP_URL")
                                .unwrap_or_else(|_| "http://localhost:3000/".to_string());
                            let inbox_url = format!("{}/inbox", app_url.trim_end_matches('/'));
                            let subject = build_digest_subject(offen.len());
                            let body = build_digest_body(&offen, &inbox_url);

                            // Einzelmail pro Empfänger (D-06), Fehler loggen+weiter (D-07)
                            for recipient in &recipients {
                                match mail_service
                                    .send_test_mail_with_body(recipient, &subject, &body)
                                    .await
                                {
                                    Ok(()) => tracing::info!("Digest worker: sent to {}", recipient),
                                    Err(e) => tracing::error!(
                                        "Digest worker: send to {} failed: {:?}", recipient, e
                                    ),
                                }
                            }
                            // Versanddatum trotzdem setzen (D-07: Tag gilt als erledigt, kein Retry)
                            if let Err(e) = digest_state_dao.set_last_sent_date(now_local.date()).await {
                                tracing::error!("Digest worker: failed to persist sent date: {:?}", e);
                            }
                        }
                    }
                    Err(e) => tracing::error!("Digest worker: failed to list inbox: {:?}", e),
                }
            }
            tokio::time::sleep(std::time::Duration::from_secs(POLL_INTERVAL_SECS)).await;
        }
    }
    ```

    Imports oben in digest.rs: `use std::sync::Arc;`. `time` ist Workspace-Dependency.

    Unit-Tests in `#[cfg(test)] mod tests` (Pattern timestamp_worker.rs:72-119 mit make_entry-Helper). Decke ALLE
    <behavior>-Fälle ab: parse_recipients (mehrere/leer/whitespace), parse_send_time (gültig/ungültig/out-of-range/leer),
    is_due (heute-schon-gesendet=false, nie-gesendet-vor-Uhrzeit=false, nie-gesendet-nach-Uhrzeit=true Catch-up,
    gestern-gesendet-nach-Uhrzeit=true, gestern-gesendet-vor-Uhrzeit=false), build_digest_subject (Anzahl im Text),
    build_digest_body (Mail-Zeile + Deep-Link enthalten). Für is_due-Tests `time::OffsetDateTime` über
    `time::macros::datetime!` oder aus `Date::from_calendar_date` + `Time::from_hms` konstruieren. Für build_digest_body-Tests
    eine InboundMail-Instanz mit allen Pflichtfeldern bauen (oder einen kleinen Test-Helper) — orientiere dich an
    bestehenden InboundMail-Konstruktionen in inbox.rs Tests (Zeile ~1185).

    KEINE Loop-Tests nötig (Loop ist nur Glue um die getesteten Helfer). Optional: ein Worker-Smoke-Test mit
    MockDigestStateDao/MockInboxService/MockMailService ist erlaubt, aber nicht erforderlich.
  </action>
  <verify>
    <automated>cargo test -p genossi_mail digest 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `genossi_mail/src/digest.rs` enthält `pub async fn start_digest_worker`
    - `genossi_mail/src/lib.rs` enthält `pub mod digest;`
    - digest.rs enthält die Funktionen `parse_recipients`, `parse_send_time`, `is_due`, `build_digest_subject`, `build_digest_body`
    - digest.rs enthält den String `"digest_recipients"` und `"digest_send_time"`
    - digest.rs enthält `send_test_mail_with_body` (Versand-Schleife) und `set_last_sent_date`
    - digest.rs enthält `now_local()` (Server-Lokalzeit, D-02) und `APP_URL` Deep-Link mit `/inbox`
    - `cargo test -p genossi_mail digest` exits 0 mit Tests für alle 6 <behavior>-Gruppen (≥12 Asserts gesamt)
    - `cargo build -p genossi_mail` exits 0
  </acceptance_criteria>
  <done>Worker + reine Helfer existieren, alle Edge-Cases (Catch-up, leerer Posteingang, kein Empfänger, Format) unit-getestet, grün.</done>
</task>

<task type="auto">
  <name>Task 2: DI-Wiring (start_digest_worker in lib.rs + Spawn in main.rs)</name>
  <files>genossi_bin/src/lib.rs, genossi_bin/src/main.rs</files>
  <read_first>
    - genossi_bin/src/lib.rs (Zeilen 580-630 Typ-Aliase + Felder inbox_service/mail_service; 1443-1464 start_timestamp_worker + DAO-new-Pattern)
    - genossi_bin/src/main.rs (Zeilen 51-67 Worker-Spawns + tracing::info)
  </read_first>
  <action>
    (a) In `genossi_bin/src/lib.rs` oben bei den Typ-Aliasen (nahe Zeile 580) ergänzen:
    ```rust
    type DigestStateDaoType = genossi_mail::dao_sqlite::DigestStateDaoSqlite;
    ```

    (b) Neue Methode `start_digest_worker(&self)` analog `start_timestamp_worker` (lib.rs:1443-1454), direkt nach
    `start_inbox_worker` einfügen. Die Felder `self.inbox_service` und `self.mail_service` existieren bereits
    (Zeilen 628/630); die DigestStateDao wird ad-hoc aus dem Pool gebaut (Pattern lib.rs:1464):
    ```rust
    pub fn start_digest_worker(&self) {
        let config_dao = ConfigDao::new(self.pool.clone());
        let config_service = Arc::new(ConfigService::new(config_dao));
        let inbox_service = self.inbox_service.clone();
        let mail_service = self.mail_service.clone();
        let digest_state_dao = Arc::new(DigestStateDaoType::new(self.pool.clone()));
        tokio::spawn(async move {
            genossi_mail::digest::start_digest_worker(
                config_service,
                inbox_service,
                mail_service,
                digest_state_dao,
            )
            .await;
        });
    }
    ```
    Prüfe, dass `ConfigDao` und `ConfigService` in lib.rs bereits importiert sind (start_timestamp_worker nutzt sie
    bereits — ja). Falls der Worker-Loop einen eigenen ConfigService-Klon braucht und `worker_inbox_config_service`
    bereits existiert (Zeile 672), darf alternativ dieser geklont werden; der ad-hoc-Build oben ist jedoch konsistent
    mit start_timestamp_worker.

    (c) In `genossi_bin/src/main.rs` direkt nach dem `start_inbox_worker()`-Block (Zeile 54-55) ergänzen:
    ```rust
    rest_state.start_digest_worker();
    tracing::info!("Digest worker started");
    ```
  </action>
  <verify>
    <automated>cargo build -p genossi_bin 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `genossi_bin/src/lib.rs` enthält `fn start_digest_worker` und `genossi_mail::digest::start_digest_worker`
    - `genossi_bin/src/lib.rs` enthält `type DigestStateDaoType` oder baut `DigestStateDaoSqlite::new(self.pool.clone())`
    - `genossi_bin/src/main.rs` enthält `rest_state.start_digest_worker();`
    - `cargo build -p genossi_bin` exits 0
    - `cargo build` (Workspace) exits 0
  </acceptance_criteria>
  <done>Digest-Worker wird beim Serverstart gespawnt; Workspace baut grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config (Vorstand) → Worker | Empfänger-Adressen kommen aus dem Config-KV-Store, gepflegt von authentifizierten Vorständen |
| Worker → SMTP | Versand der Digest-Mail via bestehenden SMTP-Transport |
| Env (APP_URL) → Deep-Link | Basis-URL aus vertrauenswürdiger Deployment-Env, kein Nutzer-Input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | Tampering | digest_recipients Config-Wert | mitigate | Empfänger-Adressen werden im Frontend (Plan 03, D-13) auf grobes E-Mail-Format geprüft; Worker splittet und trimmt, leere Einträge werden verworfen (parse_recipients) |
| T-20-04 | Information Disclosure | Digest-Mail-Inhalt | accept | Body enthält Betreff/Absender/Eingangszeit offener Mails; Empfänger sind vom Vorstand kontrolliert; Einzelmail pro Empfänger (D-06) verhindert gegenseitiges Sehen der Empfänger |
| T-20-05 | Tampering/Injection | Deep-Link APP_URL | accept | URL stammt aus vertrauenswürdiger Deployment-Env (D-11), kein Nutzer-Input; fester `/inbox`-Pfad angehängt |
| T-20-06 | Denial of Service | poll-loop ~60s | accept | Festes Poll-Intervall (60s), kein unbeschränkter Loop; ein-Versand-pro-Tag via DigestStateDao garantiert |
</threat_model>

<verification>
- `cargo test -p genossi_mail digest` grün (alle 6 Helfer-Gruppen)
- `cargo build` (Workspace) grün
- `cargo clippy --all-targets` ohne neue Fehler (best effort)
</verification>

<success_criteria>
- Worker pollt periodisch, sendet zur konfigurierten Uhrzeit genau eine Mail pro Empfänger pro Kalendertag
- Catch-up nach verpasstem Fenster funktioniert (D-01, is_due-Test)
- Leerer Posteingang ODER kein Empfänger ⇒ kein Versand, kein Fehler (DIGEST-04/07)
- Digest-Mail listet Titel/Absender/Eingangszeit (neueste zuerst) + funktionierenden /inbox-Deep-Link (DIGEST-05/06)
- Fehlerhafter Empfänger blockiert die übrigen nicht, Versanddatum wird trotzdem gesetzt (D-06/D-07)
</success_criteria>

<output>
After completion, create `.planning/phases/20-inbox-digest-t-glicher-posteingangs-benachrichtigungs-worker/20-02-SUMMARY.md`
</output>
