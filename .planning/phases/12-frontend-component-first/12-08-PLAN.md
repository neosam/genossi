---
phase: 12-frontend-component-first
plan: 08
type: execute
wave: 5
depends_on: [01, 02, 05, 07]
files_modified:
  - genossi-frontend/src/component/repayment_entry_list.rs
  - genossi-frontend/src/component/mod.rs
  - genossi-frontend/src/page/repayment_phase_details.rs
autonomous: true
requirements: [UI-03]
tags: [frontend, component, table, multi-select, filter]

must_haves:
  truths:
    - "Neuer Component genossi-frontend/src/component/repayment_entry_list.rs mit 7 Spalten (Mitgliedsnummer, Name, Anteile, Betrag, IBAN, Status, Actions) — D-10"
    - "Multi-Select via Per-Row-Checkbox + Header-Checkbox 'Alle auswaehlen'; immer sichtbar (Tablet-tauglich, D-11)"
    - "Header-Action-Leiste mit 3 Bulk-Buttons: 'Als angeschrieben markieren' (D-20), 'Als ausbezahlt markieren' (UI-05 — opens PaidOut-Confirm in Plan 12-10), 'Mail an N ausgewaehlte' (UI-06 — Plan 12-13 verdrahtet Redirect); jeweils mit Count-Badge; disabled bei 0 Selection (D-11)"
    - "Status-Filter-Tab-Strip-im-Tab: 'Alle | Offen | Angeschrieben | Ausbezahlt' mit Count-Badges; client-side Filter (D-12)"
    - "Default-Sort: Mitgliedsnummer ASC, Sekundaer created ASC (D-14); Pure-Func mit Unit-Test"
    - "Client-Side-Join Member ↔ Entry via MEMBERS-Global-Signal; bei Member-Mismatch (member_id nicht in MEMBERS-Liste) wird Name/IBAN als '—' gerendert (defensive Rendering, Pitfall 8)"
    - "Betrag-Spalte via crate::component::repayment_format::format_payout_eur (aus Plan 12-02)"
    - "Status-Spalte via crate::component::RepaymentEntryStatusBadge (aus Plan 12-02)"
    - "Inline-Cell-Edit fuer Anteile via crate::component::EditableShareCountCell (aus Plan 12-07); disabled wenn Status == PaidOut (D-13)"
    - "Action-Spalte: Soft-Delete-Trash-Icon nur wenn Status != PaidOut (ENTR-05) + Confirm-Modal (D-14 Trash-Confirm)"
    - "Empty-States: Phase geoeffnet + 0 Auto-Befuellt -> 'Keine Eintraege — Vorjahres-Austritte fehlen. Eintrag manuell hinzufuegen.' (i18n RepaymentEntryEmptyAutoFill); Filter-0-Treffer -> 'Keine Eintraege mit diesem Status.' (RepaymentEntryEmptyFilter) (D-14)"
    - "Detail-Page integriert RepaymentEntryList in den Einträge-Tab anstelle des TODO-Stubs aus Plan 12-05"
    - "MEMBERS-Refresh-Trigger fuer Initial-Mount via service::member::refresh_members (Pitfall 8)"
    - "EventHandler-Props fuer Bulk-Aktionen + on_add (Plan 12-09 mountet Add-Modal) + on_paidout_request (Plan 12-10 oeffnet Confirm-Modal) + on_mail_request (Plan 12-13 redirektet)"
    - "Test-Helper make_member listet ALLE MemberTO-Felder explizit auf (Option A; keine #[derive(Default)]-Abhaengigkeit, keine ..Default::default()-Spread) — die Feldliste mirror'd den Backend-MemberTO-Schema-Stand wie er in genossi_rest_types/src/lib.rs:165-235 deklariert ist"
    - "D-01 Button-Pattern haelt fuer alle neuen Buttons"
  artifacts:
    - path: "genossi-frontend/src/component/repayment_entry_list.rs"
      provides: "#[component] RepaymentEntryList + 4 Pure-Helper (filter, sort, summary)"
      contains: "#[component]"
      min_lines: 300
    - path: "genossi-frontend/src/page/repayment_phase_details.rs"
      provides: "Einträge-Tab ruft RepaymentEntryList statt TODO-Stub"
      contains: "RepaymentEntryList"
  key_links:
    - from: "RepaymentEntryList row"
      to: "EditableShareCountCell on_save -> api::update_repayment_entry"
      via: "EventHandler<i32>"
      pattern: "EditableShareCountCell"
    - from: "Status-Filter-Tabs"
      to: "filter_entries_by_status pure-func"
      via: "client-side filter"
      pattern: "filter_entries_by_status"
    - from: "Row-Render"
      to: "MEMBERS Global-Signal"
      via: "Client-Side-Join member.first_name"
      pattern: "MEMBERS.read"
---

<objective>
UI-03: Kern-Component RepaymentEntryList mit 7-Spalten-Tabelle, Multi-Select, Status-Filter, Inline-Cell-Edit, Soft-Delete, und Header-Action-Leiste fuer Bulk-Aktionen. Reusen ALLER bestehenden Bausteine (MemberSearch-Pattern aus mail_page, Modal, ToastContainer, EditableShareCountCell, RepaymentEntryStatusBadge, format_payout_eur).

Purpose: Diese Component ist die zentrale UX der Phase 12 — alle Vorstand-Aktionen passieren hier. Component-First-Anker: zukuenftige Listen-Cases (CSV-Export-Tab, andere Filter-Sortier-Vorgaenge) klonen dieses Pattern.

Output: Eine grosse aber konsistente Component-Datei (~300+ Zeilen) + Re-Export + Wire-up in der Detail-Page.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-frontend-component-first/12-CONTEXT.md
@.planning/phases/12-frontend-component-first/12-RESEARCH.md
@.planning/phases/12-frontend-component-first/12-PATTERNS.md
@.planning/phases/12-frontend-component-first/12-01-PLAN.md
@.planning/phases/12-frontend-component-first/12-02-PLAN.md
@.planning/phases/12-frontend-component-first/12-05-PLAN.md
@.planning/phases/12-frontend-component-first/12-07-PLAN.md

<interfaces>
<!-- Component-API (von Detail-Page in Plan 12-05+ benutzt) -->
#[component]
pub fn RepaymentEntryList(
    phase: RepaymentPhaseTO,
    on_changed: EventHandler<()>,               // Reload-Trigger nach Mutation
    on_add: EventHandler<()>,                   // Open Add-Entry-Modal (Plan 12-09)
    on_paidout_request: EventHandler<Vec<RepaymentEntryTO>>,  // Open PaidOut-Confirm (Plan 12-10)
    on_mail_request: EventHandler<Vec<Uuid>>,   // Redirect zu /mail (Plan 12-13)
    on_error: EventHandler<String>,             // Toast-Trigger
) -> Element;

<!-- API aus Plan 12-01 -->
api::list_repayment_entries(phase_id) -> Result<Vec<RepaymentEntryTO>, _>
api::update_repayment_entry(id, &UpdateRepaymentEntryRequest{share_count_to_pay_out, status, version}) -> Result<RepaymentEntryTO, _>
api::delete_repayment_entry(id) -> Result<(), _>
api::batch_toggle_repayment_status(&BatchStatusRequest { entry_ids, target_status: Contacted }) -> Result<(), _>

<!-- Pure-Helpers (definiert im File-Top mit Tests) -->
enum StatusFilter { All, Open, Contacted, PaidOut }
fn filter_entries_by_status(entries: &[RepaymentEntryTO], filter: StatusFilter) -> Vec<RepaymentEntryTO>
fn sort_entries_default(entries: &[RepaymentEntryTO], members: &[MemberTO]) -> Vec<RepaymentEntryTO>
fn member_for_entry<'a>(entry: &RepaymentEntryTO, members: &'a [MemberTO]) -> Option<&'a MemberTO>
fn entry_counts_by_status(entries: &[RepaymentEntryTO]) -> StatusCounts  // (all, open, contacted, paidout)

<!-- MemberTO Backend-Schema (verifiziert in genossi_rest_types/src/lib.rs:165-235) -->
pub struct MemberTO {
    pub id: Option<Uuid>,
    pub member_number: i64,
    pub first_name: String,
    pub last_name: String,
    pub salutation: Option<SalutationTO>,
    pub title: Option<String>,
    pub email: Option<String>,
    pub company: Option<String>,
    pub comment: Option<String>,
    pub street: Option<String>,
    pub house_number: Option<String>,
    pub postal_code: Option<String>,
    pub city: Option<String>,
    pub join_date: time::Date,
    pub shares_at_joining: i32,
    pub current_shares: i32,
    pub current_balance: i64,
    pub action_count: i32,
    pub migrated: bool,
    pub exit_date: Option<time::Date>,
    pub bank_account: Option<String>,
    pub status: MemberStatusTO,
    pub created: Option<time::PrimitiveDateTime>,
    pub deleted: Option<time::PrimitiveDateTime>,
    pub version: Option<Uuid>,
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure-Helper-Funktionen + Unit-Tests in repayment_entry_list.rs</name>
  <read_first>
    - genossi-frontend/src/component/member_search.rs (Z. 9-35 + 135-247 fuer filter-Pattern + tests)
    - genossi-frontend/src/api.rs (MemberTO struct — fuer member_number, first_name, last_name, bank_account; aus rest-types re-exportiert)
    - genossi_rest_types/src/lib.rs (Z. 165-235 fuer das KOMPLETTE MemberTO-Schema; Test-Helper make_member listet ALLE Felder explizit auf, weil MemberTO KEIN #[derive(Default)] hat — `rg "impl Default for MemberTO|#\\[derive\\([^)]*Default" genossi_rest_types/src/lib.rs` ergibt 0 Treffer)
    - .planning/phases/12-frontend-component-first/12-PATTERNS.md §4 RepaymentEntryList "Pure-Helper fuer Tests"
    - .planning/phases/12-frontend-component-first/12-CONTEXT.md (D-10 Spalten, D-12 Filter, D-14 Sort)
  </read_first>
  <behavior>
    - filter_entries_by_status mit Mix liefert nur passende Status zurueck
    - filter_entries_by_status mit StatusFilter::All liefert ALLE Entries (auch deleted falls vorhanden — Plan-Discretion: deleted vorab via Backend gefiltert; alternativ defensive Frontend-Filter retain(!deleted))
    - sort_entries_default sortiert Mitgliedsnummer ASC mit created ASC als Tie-Breaker
    - sort_entries_default mit unbekanntem member_id (kein Match in members) sortiert solche Entries ans Ende (defensive)
    - member_for_entry returns Some bei valider member_id
    - member_for_entry returns None bei unbekannter member_id
    - entry_counts_by_status liefert (all, open, contacted, paidout) Tupel mit korrekten Counts; leere Liste -> (0,0,0,0)
  </behavior>
  <action>
    Erstelle `genossi-frontend/src/component/repayment_entry_list.rs` mit dem Pure-Helper-Block:

    ```rust
    //! Repayment-Entry-Liste (Phase 12 Plan 12-08, UI-03).
    //!
    //! 7-Spalten-Tabelle + Multi-Select + Status-Filter + Inline-Cell-Edit.
    //! Component-First: reusen MemberSearch-Pattern (mail_page.rs), Modal,
    //! ToastContainer, EditableShareCountCell, RepaymentEntryStatusBadge,
    //! format_payout_eur.
    //!
    //! Default-Sort: Mitgliedsnummer ASC, created ASC (D-14).
    //! Status-Filter ist client-side (Backend liefert immer alle, Phase 8 D-10).

    use dioxus::prelude::*;
    use rest_types::MemberTO;
    use uuid::Uuid;

    use crate::api::{
        self, BatchStatusRequest, RepaymentEntryStatusTO, RepaymentEntryTO, RepaymentPhaseStatusTO,
        RepaymentPhaseTO, UpdateRepaymentEntryRequest,
    };
    use crate::component::repayment_format::format_payout_eur;
    use crate::component::{EditableShareCountCell, Modal, RepaymentEntryStatusBadge};
    use crate::i18n::{use_i18n, Key};
    use crate::service::config::CONFIG;
    use crate::service::member::MEMBERS;

    #[derive(Debug, Clone, Copy, PartialEq, Eq)]
    pub enum StatusFilter {
        All,
        Open,
        Contacted,
        PaidOut,
    }

    #[derive(Debug, Clone, Copy, PartialEq, Eq)]
    pub struct StatusCounts {
        pub all: usize,
        pub open: usize,
        pub contacted: usize,
        pub paidout: usize,
    }

    pub fn filter_entries_by_status(entries: &[RepaymentEntryTO], filter: StatusFilter) -> Vec<RepaymentEntryTO> {
        entries
            .iter()
            .filter(|e| match filter {
                StatusFilter::All => true,
                StatusFilter::Open => matches!(e.status, RepaymentEntryStatusTO::Open),
                StatusFilter::Contacted => matches!(e.status, RepaymentEntryStatusTO::Contacted),
                StatusFilter::PaidOut => matches!(e.status, RepaymentEntryStatusTO::PaidOut),
            })
            .cloned()
            .collect()
    }

    pub fn entry_counts_by_status(entries: &[RepaymentEntryTO]) -> StatusCounts {
        let mut c = StatusCounts { all: entries.len(), open: 0, contacted: 0, paidout: 0 };
        for e in entries {
            match e.status {
                RepaymentEntryStatusTO::Open => c.open += 1,
                RepaymentEntryStatusTO::Contacted => c.contacted += 1,
                RepaymentEntryStatusTO::PaidOut => c.paidout += 1,
            }
        }
        c
    }

    pub fn member_for_entry<'a>(entry: &RepaymentEntryTO, members: &'a [MemberTO]) -> Option<&'a MemberTO> {
        members.iter().find(|m| m.id == Some(entry.member_id))
    }

    /// D-14 Default-Sort: Mitgliedsnummer ASC, created ASC sekundaer.
    /// Entries ohne Member-Match (member_for_entry == None) sortieren ans Ende.
    pub fn sort_entries_default(entries: &[RepaymentEntryTO], members: &[MemberTO]) -> Vec<RepaymentEntryTO> {
        let mut result: Vec<RepaymentEntryTO> = entries.to_vec();
        result.sort_by(|a, b| {
            let ma = member_for_entry(a, members);
            let mb = member_for_entry(b, members);
            match (ma, mb) {
                (Some(a_m), Some(b_m)) => {
                    a_m.member_number.cmp(&b_m.member_number)
                        .then_with(|| a.created.cmp(&b.created))
                }
                (Some(_), None) => std::cmp::Ordering::Less,
                (None, Some(_)) => std::cmp::Ordering::Greater,
                (None, None) => a.created.cmp(&b.created),
            }
        });
        result
    }

    #[cfg(test)]
    mod tests {
        use super::*;
        use rest_types::MemberStatusTO;
        use time::macros::date;

        /// Option A (Issue #3): MemberTO hat KEIN #[derive(Default)]
        /// (verifiziert via `rg "impl Default for MemberTO|#\[derive\([^)]*Default" genossi_rest_types/src/lib.rs` → 0).
        /// Daher listet make_member ALLE Felder explizit auf — KEIN ..Default::default()-Spread.
        /// Wenn das Backend-Schema waechst (neues Feld), bricht der Compiler hier — bewusste Pflicht-Sync.
        fn make_member(id: Uuid, number: i64) -> MemberTO {
            MemberTO {
                id: Some(id),
                member_number: number,
                first_name: format!("First{number}"),
                last_name: format!("Last{number}"),
                salutation: None,
                title: None,
                email: None,
                company: None,
                comment: None,
                street: None,
                house_number: None,
                postal_code: None,
                city: None,
                join_date: date!(2020 - 01 - 01),
                shares_at_joining: 1,
                current_shares: 1,
                current_balance: 0,
                action_count: 0,
                migrated: false,
                exit_date: None,
                bank_account: None,
                status: MemberStatusTO::Active,
                created: None,
                deleted: None,
                version: None,
            }
        }

        fn make_entry(id: Uuid, member_id: Uuid, status: RepaymentEntryStatusTO, created_iso: &str) -> RepaymentEntryTO {
            RepaymentEntryTO {
                id,
                member_id,
                phase_id: Uuid::new_v4(),
                share_count_to_pay_out: 1,
                status,
                created: Some(created_iso.into()),
                deleted: None,
                version: None,
            }
        }

        #[test]
        fn filter_by_status_open() {
            let e1 = make_entry(Uuid::new_v4(), Uuid::new_v4(), RepaymentEntryStatusTO::Open, "2026-01-01T00:00:00Z");
            let e2 = make_entry(Uuid::new_v4(), Uuid::new_v4(), RepaymentEntryStatusTO::Contacted, "2026-01-02T00:00:00Z");
            let e3 = make_entry(Uuid::new_v4(), Uuid::new_v4(), RepaymentEntryStatusTO::Open, "2026-01-03T00:00:00Z");
            let filtered = filter_entries_by_status(&[e1, e2, e3], StatusFilter::Open);
            assert_eq!(filtered.len(), 2);
        }

        #[test]
        fn filter_by_status_all_returns_all() {
            let e1 = make_entry(Uuid::new_v4(), Uuid::new_v4(), RepaymentEntryStatusTO::Open, "2026-01-01T00:00:00Z");
            let e2 = make_entry(Uuid::new_v4(), Uuid::new_v4(), RepaymentEntryStatusTO::PaidOut, "2026-01-02T00:00:00Z");
            let filtered = filter_entries_by_status(&[e1, e2], StatusFilter::All);
            assert_eq!(filtered.len(), 2);
        }

        #[test]
        fn counts_correct() {
            let e1 = make_entry(Uuid::new_v4(), Uuid::new_v4(), RepaymentEntryStatusTO::Open, "2026-01-01T00:00:00Z");
            let e2 = make_entry(Uuid::new_v4(), Uuid::new_v4(), RepaymentEntryStatusTO::Open, "2026-01-02T00:00:00Z");
            let e3 = make_entry(Uuid::new_v4(), Uuid::new_v4(), RepaymentEntryStatusTO::Contacted, "2026-01-03T00:00:00Z");
            let e4 = make_entry(Uuid::new_v4(), Uuid::new_v4(), RepaymentEntryStatusTO::PaidOut, "2026-01-04T00:00:00Z");
            let c = entry_counts_by_status(&[e1, e2, e3, e4]);
            assert_eq!(c.all, 4);
            assert_eq!(c.open, 2);
            assert_eq!(c.contacted, 1);
            assert_eq!(c.paidout, 1);
        }

        #[test]
        fn counts_empty_returns_zeros() {
            let c = entry_counts_by_status(&[]);
            assert_eq!(c.all, 0);
            assert_eq!(c.open, 0);
            assert_eq!(c.contacted, 0);
            assert_eq!(c.paidout, 0);
        }

        #[test]
        fn member_for_entry_finds_match() {
            let mid = Uuid::new_v4();
            let m = make_member(mid, 42);
            let e = make_entry(Uuid::new_v4(), mid, RepaymentEntryStatusTO::Open, "2026-01-01T00:00:00Z");
            assert_eq!(member_for_entry(&e, &[m]).map(|m| m.member_number), Some(42));
        }

        #[test]
        fn member_for_entry_returns_none_on_mismatch() {
            let m = make_member(Uuid::new_v4(), 42);
            let e = make_entry(Uuid::new_v4(), Uuid::new_v4(), RepaymentEntryStatusTO::Open, "2026-01-01T00:00:00Z");
            assert!(member_for_entry(&e, &[m]).is_none());
        }

        #[test]
        fn sort_by_member_number_asc() {
            let m1 = make_member(Uuid::new_v4(), 100);
            let m2 = make_member(Uuid::new_v4(), 50);
            let m3 = make_member(Uuid::new_v4(), 75);
            let e1 = make_entry(Uuid::new_v4(), m1.id.unwrap(), RepaymentEntryStatusTO::Open, "2026-01-01T00:00:00Z");
            let e2 = make_entry(Uuid::new_v4(), m2.id.unwrap(), RepaymentEntryStatusTO::Open, "2026-01-02T00:00:00Z");
            let e3 = make_entry(Uuid::new_v4(), m3.id.unwrap(), RepaymentEntryStatusTO::Open, "2026-01-03T00:00:00Z");
            let members = vec![m1.clone(), m2.clone(), m3.clone()];
            let sorted = sort_entries_default(&[e1, e2, e3], &members);
            assert_eq!(member_for_entry(&sorted[0], &members).unwrap().member_number, 50);
            assert_eq!(member_for_entry(&sorted[1], &members).unwrap().member_number, 75);
            assert_eq!(member_for_entry(&sorted[2], &members).unwrap().member_number, 100);
        }

        #[test]
        fn sort_entries_without_member_at_end() {
            let m1 = make_member(Uuid::new_v4(), 50);
            let m1_id = m1.id.unwrap();
            let unknown_member_id = Uuid::new_v4();
            let e1 = make_entry(Uuid::new_v4(), unknown_member_id, RepaymentEntryStatusTO::Open, "2026-01-01T00:00:00Z");
            let e2 = make_entry(Uuid::new_v4(), m1_id, RepaymentEntryStatusTO::Open, "2026-01-02T00:00:00Z");
            let sorted = sort_entries_default(&[e1, e2], &[m1]);
            // Member-matched first, unmatched last
            assert_eq!(sorted[0].member_id, m1_id);
            assert_eq!(sorted[1].member_id, unknown_member_id);
        }
    }
    ```

    **Wichtig zum Test-Helper (Option A, Issue #3 BLOCKER):**
    - `MemberTO` hat KEIN `#[derive(Default)]` und KEIN `impl Default` (verifiziert: `rg "impl Default for MemberTO|#\\[derive\\([^)]*Default" genossi_rest_types/src/lib.rs` → 0 Treffer).
    - `make_member` MUSS daher alle 26 Felder explizit setzen (siehe Block oben). KEIN `..Default::default()`-Spread, KEIN `#[derive(Default)]` im Backend ergänzen.
    - Wenn das Backend-Schema waechst (neues Pflicht-Feld in `MemberTO`), bricht der Compiler hier — bewusste Pflicht-Sync, NICHT silent-Initialisierung.
    - Backend-Lock-Regel (Phase-12-Backend ist gelocked): wir aendern KEINE Backend-Strukturen, daher Option B aus dem Revisions-Brief (Backend `#[derive(Default)] for MemberTO`) ist VERBOTEN.

    **Pflicht-Imports im Test-Modul:**
    - `rest_types::MemberStatusTO` (fuer den `status: MemberStatusTO::Active`-Default)
    - `time::macros::date` (fuer `join_date: date!(2020 - 01 - 01)`)
    - Falls einer dieser Imports fehlt: `cargo build -p genossi-frontend` schlaegt mit E0432 fehl.

    **Default-Werte-Begruendung im Helper:**
    - `MemberStatusTO::Active`: harmlose default-Annahme (verifiziere via `rg "pub enum MemberStatusTO" genossi_rest_types/src/lib.rs` dass `Active` eine Variante ist; alternativ erste Variante des Enums)
    - `join_date: date!(2020 - 01 - 01)`: synthetisches Datum fuer Test
    - alle Option-Felder = None: minimale Inputs
    - `shares_at_joining`, `current_shares` = 1: Member hat mindestens einen Anteil (Domain-realistisch)
    - `current_balance`, `action_count` = 0
    - `migrated` = false, `version` = None
  </action>
  <verify>
    <automated>cargo test -p genossi-frontend --lib component::repayment_entry_list::tests</automated>
  </verify>
  <done>
    - 8 Unit-Tests PASS
    - Pure-Funcs sind `pub` (Plan 12-09 + 12-10 koennen sie reusen falls noetig)
    - `rg "fn filter_entries_by_status|fn sort_entries_default|fn entry_counts_by_status|fn member_for_entry" genossi-frontend/src/component/repayment_entry_list.rs` >= 4 Treffer
    - `rg "\\.\\.\\Default::default\\(\\)" genossi-frontend/src/component/repayment_entry_list.rs` = 0 (Option A: explizit alle Felder, kein Spread)
    - `rg "#\\[derive\\([^)]*Default" genossi_rest_types/src/lib.rs` = 0 (Backend bleibt unveraendert — Option B ist VERBOTEN per Phase-12-Backend-Lock)
  </done>
</task>

<task type="auto">
  <name>Task 2: RepaymentEntryList Component vollstaendig + Detail-Page-Integration</name>
  <read_first>
    - genossi-frontend/src/component/repayment_entry_list.rs (aus Task 1 — pure-helpers + skeleton)
    - genossi-frontend/src/page/mail_page.rs (Z. 51-77 fuer selected_member_ids-Signal-Pattern; Z. 232-373 fuer Multi-Select-Recipient-Row-Pattern; Z. 342-364 fuer Header-Action-Buttons)
    - genossi-frontend/src/component/attendance_list.rs (Z. 1-80 fuer Per-Row-Toggle-Pattern)
    - genossi-frontend/src/page/repayment_phase_details.rs (aus Plan 12-05 — Einträge-Tab Stub mit "TODO Plan 12-08")
    - genossi-frontend/src/service/member.rs (komplett — MEMBERS-Signal + refresh_members)
    - .planning/phases/12-frontend-component-first/12-PATTERNS.md §4 (RepaymentEntryList Skelett)
    - .planning/phases/12-frontend-component-first/12-CONTEXT.md (D-10..D-14)
  </read_first>
  <action>
    **Schritt 1 — Vollstaendige RepaymentEntryList-Implementation** in der gleichen Datei UNTERHALB des Test-Modules (oder direkt nach den Pure-Funcs):

    Die Component ist gross — strukturiere sie so:
    1. Component-Header (`#[component] pub fn RepaymentEntryList(...) -> Element`)
    2. Lokale Signals: `entries`, `loading`, `selected_ids: Vec<Uuid>`, `status_filter: StatusFilter`, `show_delete_confirm: Option<Uuid>` (None=hidden; Some(entry_id) = zeige Confirm fuer dieses entry)
    3. `use_effect`: parallel `refresh_members()` UND `load_entries()` (Pitfall 8 — MEMBERS muss da sein)
    4. Header-Action-Leiste: 3 Bulk-Buttons + Add-Button
    5. Status-Filter-Tab-Strip
    6. Tabelle: thead (7 Spalten + Header-Checkbox) + tbody (Row-Render mit join)
    7. Empty-State-Branches
    8. Delete-Confirm-Modal

    Vollstaendiger Code-Skelett (Plan ist zu lang fuer alle 250+ LOC; siehe PATTERNS.md §4 + die hier gezeigten Bloecke):

    ```rust
    #[component]
    pub fn RepaymentEntryList(
        phase: RepaymentPhaseTO,
        on_changed: EventHandler<()>,
        on_add: EventHandler<()>,
        on_paidout_request: EventHandler<Vec<RepaymentEntryTO>>,
        on_mail_request: EventHandler<Vec<Uuid>>,
        on_error: EventHandler<String>,
    ) -> Element {
        let i18n = use_i18n();
        let phase_id = phase.id;
        let phase_status = phase.status;
        let share_value = phase.share_value;
        let readonly_mode = matches!(phase_status, RepaymentPhaseStatusTO::Closed); // D-08

        let mut entries = use_signal(Vec::<RepaymentEntryTO>::new);
        let mut loading = use_signal(|| true);
        let mut selected_ids = use_signal(Vec::<Uuid>::new);
        let mut status_filter = use_signal(|| StatusFilter::All);
        let mut delete_confirm_for = use_signal(|| Option::<Uuid>::None);

        let load_entries = move || {
            spawn(async move {
                loading.set(true);
                let config = CONFIG.read().clone();
                match api::list_repayment_entries(&config, phase_id).await {
                    Ok(list) => entries.set(list),
                    Err(e) => on_error.call(e.message),
                }
                loading.set(false);
            });
        };

        // Pitfall 8: parallel MEMBERS-Refresh
        use_effect(move || {
            spawn(async move {
                crate::service::member::refresh_members().await;
            });
            load_entries();
        });

        // Computed: Member-Liste fuer Join
        let members_state = MEMBERS.read();
        let members_vec = members_state.items.clone();
        let entries_vec = entries.read().clone();
        let counts = entry_counts_by_status(&entries_vec);
        let filtered = filter_entries_by_status(&entries_vec, *status_filter.read());
        let sorted = sort_entries_default(&filtered, &members_vec);

        rsx! {
            div { class: "flex flex-col gap-4",

                // ── Status-Filter-Tab-Strip-im-Tab (D-12) ──
                div { class: "flex gap-2 border-b pb-2",
                    StatusFilterTab { label: format!("{} ({})", i18n.t(Key::RepaymentEntryFilterAll), counts.all), is_selected: matches!(*status_filter.read(), StatusFilter::All), on_click: move |_| status_filter.set(StatusFilter::All) }
                    StatusFilterTab { label: format!("{} ({})", i18n.t(Key::RepaymentEntryStatusOpen), counts.open), is_selected: matches!(*status_filter.read(), StatusFilter::Open), on_click: move |_| status_filter.set(StatusFilter::Open) }
                    StatusFilterTab { label: format!("{} ({})", i18n.t(Key::RepaymentEntryStatusContacted), counts.contacted), is_selected: matches!(*status_filter.read(), StatusFilter::Contacted), on_click: move |_| status_filter.set(StatusFilter::Contacted) }
                    StatusFilterTab { label: format!("{} ({})", i18n.t(Key::RepaymentEntryStatusPaidOut), counts.paidout), is_selected: matches!(*status_filter.read(), StatusFilter::PaidOut), on_click: move |_| status_filter.set(StatusFilter::PaidOut) }
                }

                // ── Header-Action-Leiste (D-11) ──
                if !readonly_mode {
                    {
                        let selected_count = selected_ids.read().len();
                        rsx! {
                            div { class: "flex flex-wrap gap-2 items-center",
                                button {
                                    r#type: "button",
                                    class: "bg-blue-600 hover:bg-blue-700 text-white px-3 py-2 rounded text-sm",
                                    onclick: move |_| on_add.call(()),
                                    "{i18n.t(Key::RepaymentEntryAdd)}"
                                }
                                button {
                                    r#type: "button",
                                    class: if selected_count == 0 { "bg-gray-200 text-gray-500 px-3 py-2 rounded text-sm cursor-not-allowed" } else { "bg-blue-600 hover:bg-blue-700 text-white px-3 py-2 rounded text-sm" },
                                    disabled: selected_count == 0,
                                    onclick: move |_| {
                                        let ids = selected_ids.read().clone();
                                        on_mail_request.call(ids);
                                    },
                                    "{i18n.t(Key::RepaymentEntryBulkMailButton)} ({selected_count})"
                                }
                                button {
                                    r#type: "button",
                                    class: if selected_count == 0 { "bg-gray-200 text-gray-500 px-3 py-2 rounded text-sm cursor-not-allowed" } else { "bg-blue-600 hover:bg-blue-700 text-white px-3 py-2 rounded text-sm" },
                                    disabled: selected_count == 0,
                                    onclick: move |_| {
                                        let ids = selected_ids.read().clone();
                                        let on_changed = on_changed.clone();
                                        let on_error = on_error.clone();
                                        spawn(async move {
                                            let config = CONFIG.read().clone();
                                            let req = BatchStatusRequest {
                                                entry_ids: ids.clone(),
                                                target_status: RepaymentEntryStatusTO::Contacted,
                                            };
                                            match api::batch_toggle_repayment_status(&config, &req).await {
                                                Ok(()) => {
                                                    selected_ids.set(Vec::new());
                                                    on_changed.call(());
                                                }
                                                Err(e) => on_error.call(e.message),
                                            }
                                        });
                                    },
                                    "{i18n.t(Key::RepaymentEntryMarkContacted)} ({selected_count})"
                                }
                                button {
                                    r#type: "button",
                                    class: if selected_count == 0 { "bg-gray-200 text-gray-500 px-3 py-2 rounded text-sm cursor-not-allowed" } else { "bg-red-600 hover:bg-red-700 text-white px-3 py-2 rounded text-sm" },
                                    disabled: selected_count == 0,
                                    onclick: move |_| {
                                        let ids: Vec<Uuid> = selected_ids.read().clone();
                                        let selected_entries: Vec<RepaymentEntryTO> = entries.read().iter().filter(|e| ids.contains(&e.id)).cloned().collect();
                                        on_paidout_request.call(selected_entries);
                                    },
                                    "{i18n.t(Key::RepaymentEntryMarkPaidOut)} ({selected_count})"
                                }
                            }
                        }
                    }
                }

                // ── Tabelle ──
                if *loading.read() {
                    p { class: "text-gray-500 text-center py-8", "{i18n.t(Key::Loading)}" }
                } else if sorted.is_empty() {
                    div { class: "text-center py-12 text-gray-500",
                        if matches!(*status_filter.read(), StatusFilter::All) && entries_vec.is_empty() {
                            "{i18n.t(Key::RepaymentEntryEmptyAutoFill)}"
                        } else {
                            "{i18n.t(Key::RepaymentEntryEmptyFilter)}"
                        }
                    }
                } else {
                    table { class: "min-w-full divide-y divide-gray-200 text-sm",
                        thead { class: "bg-gray-50",
                            tr {
                                if !readonly_mode {
                                    th { class: "px-2 py-2",
                                        // Header-Checkbox "Alle auswaehlen" (D-11)
                                        input {
                                            r#type: "checkbox",
                                            checked: {
                                                let sorted_ids: Vec<Uuid> = sorted.iter().map(|e| e.id).collect();
                                                !sorted_ids.is_empty() && sorted_ids.iter().all(|id| selected_ids.read().contains(id))
                                            },
                                            onchange: move |e| {
                                                let checked = e.value() == "true";
                                                let sorted_ids: Vec<Uuid> = sorted.iter().map(|e| e.id).collect();
                                                if checked {
                                                    selected_ids.set(sorted_ids);
                                                } else {
                                                    selected_ids.set(Vec::new());
                                                }
                                            },
                                        }
                                    }
                                }
                                th { class: "px-2 py-2 text-left", "{i18n.t(Key::RepaymentEntryColMemberNumber)}" }
                                th { class: "px-2 py-2 text-left", "{i18n.t(Key::RepaymentEntryColName)}" }
                                th { class: "px-2 py-2 text-right", "{i18n.t(Key::RepaymentEntryColShares)}" }
                                th { class: "px-2 py-2 text-right", "{i18n.t(Key::RepaymentEntryColAmount)}" }
                                th { class: "px-2 py-2 text-left", "{i18n.t(Key::RepaymentEntryColIban)}" }
                                th { class: "px-2 py-2 text-left", "{i18n.t(Key::RepaymentEntryColStatus)}" }
                                if !readonly_mode {
                                    th { class: "px-2 py-2 text-right", "{i18n.t(Key::RepaymentEntryColActions)}" }
                                }
                            }
                        }
                        tbody { class: "divide-y divide-gray-200 bg-white",
                            for e in sorted.iter() {
                                {
                                    let entry = e.clone();
                                    let member = member_for_entry(&entry, &members_vec).cloned();
                                    let member_number = member.as_ref().map(|m| m.member_number.to_string()).unwrap_or_else(|| "—".into());
                                    let name = member.as_ref().map(|m| format!("{} {}", m.first_name, m.last_name)).unwrap_or_else(|| "—".into());
                                    let iban = member.as_ref().and_then(|m| m.bank_account.clone()).filter(|s| !s.is_empty()).unwrap_or_else(|| "—".into());
                                    let amount_str = format_payout_eur(entry.share_count_to_pay_out, share_value);
                                    let entry_id = entry.id;
                                    let entry_version = entry.version;
                                    let is_paidout = matches!(entry.status, RepaymentEntryStatusTO::PaidOut);
                                    let is_selected = selected_ids.read().contains(&entry_id);
                                    rsx! {
                                        tr { key: "{entry_id}",
                                            if !readonly_mode {
                                                td { class: "px-2 py-2",
                                                    input {
                                                        r#type: "checkbox",
                                                        checked: is_selected,
                                                        onchange: move |ev| {
                                                            let checked = ev.value() == "true";
                                                            if checked {
                                                                selected_ids.write().push(entry_id);
                                                            } else {
                                                                selected_ids.write().retain(|id| *id != entry_id);
                                                            }
                                                        },
                                                    }
                                                }
                                            }
                                            td { class: "px-2 py-2", "{member_number}" }
                                            td { class: "px-2 py-2", "{name}" }
                                            td { class: "px-2 py-2 text-right",
                                                if readonly_mode || is_paidout {
                                                    span { "{entry.share_count_to_pay_out}" }
                                                } else {
                                                    EditableShareCountCell {
                                                        value: entry.share_count_to_pay_out,
                                                        disabled: false,
                                                        on_save: move |new_count: i32| {
                                                            let on_changed = on_changed.clone();
                                                            let on_error = on_error.clone();
                                                            let Some(version) = entry_version else {
                                                                on_error.call("Eintrag hat keine Version — bitte neu laden".into());
                                                                return;
                                                            };
                                                            let req = UpdateRepaymentEntryRequest {
                                                                share_count_to_pay_out: Some(new_count),
                                                                status: None,
                                                                version,
                                                            };
                                                            spawn(async move {
                                                                let config = CONFIG.read().clone();
                                                                match api::update_repayment_entry(&config, entry_id, &req).await {
                                                                    Ok(_) => on_changed.call(()),
                                                                    Err(e) => on_error.call(e.message),
                                                                }
                                                            });
                                                        },
                                                    }
                                                }
                                            }
                                            td { class: "px-2 py-2 text-right", "{amount_str}" }
                                            td { class: "px-2 py-2", "{iban}" }
                                            td { class: "px-2 py-2",
                                                RepaymentEntryStatusBadge { status: entry.status }
                                            }
                                            if !readonly_mode {
                                                td { class: "px-2 py-2 text-right",
                                                    if !is_paidout {
                                                        button {
                                                            r#type: "button",
                                                            class: "text-red-600 hover:text-red-800",
                                                            title: i18n.t(Key::RepaymentEntryDelete).to_string(),
                                                            onclick: move |_| delete_confirm_for.set(Some(entry_id)),
                                                            ""
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }

                // ── Delete-Confirm-Modal (D-14) ──
                if let Some(eid) = *delete_confirm_for.read() {
                    Modal {
                        div { class: "flex flex-col gap-4",
                            h2 { class: "text-xl font-semibold", "{i18n.t(Key::RepaymentEntryDeleteConfirm)}" }
                            div { class: "flex gap-2 justify-end",
                                button {
                                    r#type: "button",
                                    class: "px-4 py-2 text-gray-700 hover:bg-gray-100 rounded min-h-[44px]",
                                    onclick: move |_| delete_confirm_for.set(None),
                                    "{i18n.t(Key::Cancel)}"
                                }
                                button {
                                    r#type: "button",
                                    class: "bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded min-h-[44px]",
                                    onclick: move |_| {
                                        delete_confirm_for.set(None);
                                        let on_changed = on_changed.clone();
                                        let on_error = on_error.clone();
                                        spawn(async move {
                                            let config = CONFIG.read().clone();
                                            match api::delete_repayment_entry(&config, eid).await {
                                                Ok(()) => on_changed.call(()),
                                                Err(e) => on_error.call(e.message),
                                            }
                                        });
                                    },
                                    "{i18n.t(Key::RepaymentEntryDelete)}"
                                }
                            }
                        }
                    }
                }
            }
        }
    }

    #[component]
    fn StatusFilterTab(label: String, is_selected: bool, on_click: EventHandler<()>) -> Element {
        let class = if is_selected {
            "px-3 py-1 rounded bg-blue-500 text-white text-sm"
        } else {
            "px-3 py-1 rounded bg-gray-200 hover:bg-gray-300 text-gray-700 text-sm"
        };
        rsx! {
            button {
                r#type: "button",
                class: "{class}",
                onclick: move |_| on_click.call(()),
                "{label}"
            }
        }
    }
    ```

    **Schritt 2 — Re-Export in `genossi-frontend/src/component/mod.rs`:**
    ```rust
    pub mod repayment_entry_list;
    pub use repayment_entry_list::{RepaymentEntryList, StatusFilter};
    ```

    **Schritt 3 — Detail-Page-Integration `genossi-frontend/src/page/repayment_phase_details.rs`:**

    Im EntriesTab-Branch (aus Plan 12-05; Stub mit "TODO Plan 12-08") ersetzen:
    ```rust
    "entries" => match status_value {
        RepaymentPhaseStatusTO::Preparation => rsx! {
            div { class: "text-center py-12 text-gray-500",
                "{i18n.t(Key::RepaymentEntriesNotOpenYet)}"
            }
        },
        _ => rsx! {
            RepaymentEntryList {
                phase: phase_for_entries,
                on_changed: move |_| load_phase(),
                on_add: move |_| {
                    // Plan 12-09 verdrahtet hier ein Add-Modal-Signal in der Detail-Page;
                    // bis dahin: Toast als Placeholder-Indikator
                    show_toast(&mut toast_messages, &mut toast_counter, "Add-Modal kommt in Plan 12-09".into());
                },
                on_paidout_request: move |_entries: Vec<RepaymentEntryTO>| {
                    // Plan 12-10 verdrahtet hier den PaidOut-Confirm-Modal-Signal
                    show_toast(&mut toast_messages, &mut toast_counter, "PaidOut-Confirm kommt in Plan 12-10".into());
                },
                on_mail_request: move |_ids: Vec<Uuid>| {
                    // Plan 12-13 verdrahtet hier den Mail-Redirect
                    show_toast(&mut toast_messages, &mut toast_counter, "Mail-Redirect kommt in Plan 12-13".into());
                },
                on_error: move |msg: String| show_toast(&mut toast_messages, &mut toast_counter, msg),
            }
        },
    },
    ```

    **D-01 Button-Gate fuer beide Dateien:**
    ```bash
    rg -P '(?ms)button\s*\{(?:(?!\}).)*?\}' genossi-frontend/src/component/repayment_entry_list.rs genossi-frontend/src/page/repayment_phase_details.rs | grep -v 'r#type:' | grep -c 'button {'
    # Erwartet: 0
    ```

    **D-08 readonly_mode Branches:** Bei `phase_status == Closed` zeigt die Component KEINE Header-Action-Leiste (Add/Bulk-Buttons), KEINE Checkbox-Spalte, KEINEN Inline-Edit-EditableShareCountCell (Anteile als plain span), und KEINE Trash-Action. Tab-Strip-Filter bleibt nutzbar (read-only-View).
  </action>
  <verify>
    <automated>cargo build -p genossi-frontend && cargo test -p genossi-frontend --lib component::repayment_entry_list</automated>
  </verify>
  <done>
    - cargo build -p genossi-frontend exit 0
    - cargo test -p genossi-frontend --lib component::repayment_entry_list zeigt alle Tests PASS
    - `rg "TODO Plan 12-08" genossi-frontend/src/page/repayment_phase_details.rs` = 0 (Detail-Page-Stub ersetzt)
    - `rg "RepaymentEntryList \{" genossi-frontend/src/page/repayment_phase_details.rs` = 1 (Component-Mount)
    - `rg "EditableShareCountCell \{" genossi-frontend/src/component/repayment_entry_list.rs` = 1 (Plan 12-07 Reuse)
    - `rg "RepaymentEntryStatusBadge \{" genossi-frontend/src/component/repayment_entry_list.rs` = 1 (Plan 12-02 Reuse)
    - `rg "format_payout_eur\(" genossi-frontend/src/component/repayment_entry_list.rs` >= 1 (Plan 12-02 Reuse)
    - `rg "api::list_repayment_entries|api::update_repayment_entry|api::delete_repayment_entry|api::batch_toggle_repayment_status" genossi-frontend/src/component/repayment_entry_list.rs` = 4 verschiedene Treffer
    - D-01 Grep-Gate fuer beide Dateien = 0
  </done>
</task>

</tasks>

<verification>
- `cargo build -p genossi-frontend` exit 0
- `cargo test -p genossi-frontend --lib component::repayment_entry_list` alle Tests PASS
- D-01 Grep-Gate fuer repayment_entry_list.rs + repayment_phase_details.rs haelt
- Component-First-Anker: keine inline-Status-Pills, kein inline-Cell-Edit-Markup; alles ueber existierende/neue Reuse-Components
- D-08: phase.status == Closed rendert read-only-View
- D-06: phase.status == Preparation zeigt weiterhin den Hinweis-Box-Branch (aus Plan 12-05 unveraendert)
- Option A (Test-Helper): KEIN ..Default::default(), KEIN #[derive(Default)] im Backend (rg-verifizierbar)
</verification>

<success_criteria>
Vorstand sieht in der Detail-Page Einträge-Tab eine 7-Spalten-Tabelle mit allen Auto-Befuellten Eintraegen. Klick auf Mitgliedsnummer-Checkbox + 'Als angeschrieben markieren' wirft die Batch-Status-Aktion. Inline-Cell-Edit der Anteile aendert sie atomar via PUT. Soft-Delete via Trash + Confirm. Empty-State zeigt den passenden Text.

Drei der vier Bulk-Buttons sind in dieser Wave noch nicht voll verdrahtet — die Header-Action-Leiste zeigt einen Toast als Placeholder. Plans 12-09/12-10/12-13 ersetzen die `_ => show_toast(...)`-Placeholder mit echtem Modal/Redirect.
</success_criteria>

<output>
After completion, create `.planning/phases/12-frontend-component-first/12-08-SUMMARY.md` documenting:
- 4 Pure-Funcs + ihre Test-Coverage
- Component-Aufruf-Vertrag (EventHandler-Props sind die Verdrahtungs-Punkte fuer 12-09/12-10/12-13)
- readonly_mode-Branches (D-08)
- MEMBERS-Refresh-Trigger im use_effect (Pitfall 8)
- Option A im Test-Helper (alle MemberTO-Felder explizit; bewusste Compile-Time-Sync mit Backend-Schema)
- Hinweis fuer Plan 12-09/12-10/12-13: in repayment_phase_details.rs die 3 EventHandler ersetzen
</output>
