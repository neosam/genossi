---
phase: 12-frontend-component-first
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - genossi-frontend/src/router.rs
  - genossi-frontend/src/page/mod.rs
  - genossi-frontend/src/component/top_bar.rs
autonomous: true
requirements: [UI-01, UI-02]
tags: [frontend, router, navigation]

must_haves:
  truths:
    - "Route-Enum hat zwei neue Varianten RepaymentPhases {} und RepaymentPhaseDetails { id: String } mit korrekten #[route(...)]-Macros (D-25)"
    - "page/mod.rs deklariert pub mod repayment_phases und pub mod repayment_phase_details + Re-Exports der Page-Components"
    - "router.rs hat pub use crate::page::RepaymentPhases + RepaymentPhaseDetails am Top"
    - "top_bar.rs Vorstand-Nav hat einen neuen NavItem 'Anteils-Rückzahlung' in der mitglieder_items-Gruppe direkt nach Assemblies (D-27 Placement-Discretion: Mitglieder-Workflow-Affinität)"
    - "Stub-Pages exist (Pages werden in 12-04/12-05 implementiert) — cargo build muss exit 0; Stubs sind temporäre rsx!-Returns mit 'TODO Plan 12-04'/'TODO Plan 12-05'-Text — verhindert Compile-Error im Wave 2 Schritt 1"
  artifacts:
    - path: "genossi-frontend/src/page/repayment_phases.rs"
      provides: "#[component] pub fn RepaymentPhases() -> Element (Stub — Plan 12-04 implementiert)"
      contains: "TODO Plan 12-04"
    - path: "genossi-frontend/src/page/repayment_phase_details.rs"
      provides: "#[component] pub fn RepaymentPhaseDetails(id: String) -> Element (Stub — Plan 12-05 implementiert)"
      contains: "TODO Plan 12-05"
    - path: "genossi-frontend/src/router.rs"
      provides: "Route-Enum mit RepaymentPhases + RepaymentPhaseDetails"
      contains: "/repayment-phases"
    - path: "genossi-frontend/src/component/top_bar.rs"
      provides: "NavItem 'Anteils-Rückzahlung' in mitglieder_items"
      contains: "Route::RepaymentPhases {}"
  key_links:
    - from: "genossi-frontend/src/router.rs::Route"
      to: "RepaymentPhases page component"
      via: "#[route] macro"
      pattern: "#\\[route\\(\"/repayment-phases\"\\)\\]"
    - from: "genossi-frontend/src/component/top_bar.rs"
      to: "Route::RepaymentPhases"
      via: "NavItem.route"
      pattern: "Route::RepaymentPhases\\s*\\{\\}"
---

<objective>
Routing-Skelett für die zwei neuen Pages — Routes registriert, Stubs vorhanden, Nav-Item sichtbar. Stubs verhindern Build-Failure, bis Plan 12-04 (Listen-Page) und Plan 12-05 (Detail-Page) sie ersetzen.

Purpose: Klein, isoliert, ermöglicht parallele Plan-Arbeit. Ein Developer kann ab jetzt im Browser zu `/repayment-phases` navigieren und sieht „TODO Plan 12-04" — das ist genug für Smoke-Tests und Route-Validierung.

Output: Route-Enum + 2 Stub-Pages + 1 NavItem.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-frontend-component-first/12-CONTEXT.md
@.planning/phases/12-frontend-component-first/12-RESEARCH.md
@.planning/phases/12-frontend-component-first/12-PATTERNS.md

<interfaces>
<!-- Existing Route-Enum-Block (router.rs Z. 24-69) -->
#[derive(Routable, Clone, PartialEq, Debug)]
pub enum Route {
    #[route("/")] Home {},
    ...
    #[route("/assemblies")] Assemblies {},
    #[route("/assemblies/:id")] AssemblyDetails { id: String },
    ...
}

<!-- Existing TopBar mitglieder_items (top_bar.rs Z. 44-70) - "show_admin" gate is correct one -->
let mut mitglieder_items = Vec::new();
if show_members { ... }
if show_templates { ... }
if show_admin {
    mitglieder_items.push(NavItem { label: i18n.t(Key::Applications).to_string(), route: Route::ApplicationsPage {} });
    mitglieder_items.push(NavItem { label: i18n.t(Key::Assemblies).to_string(), route: Route::Assemblies {} });
    // ← HIER neuer Push direkt nach Assemblies
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stub-Pages + page/mod.rs Re-Exports</name>
  <read_first>
    - genossi-frontend/src/page/mod.rs (komplett — Pattern-Vorlage für `pub mod ...; pub use ...::PageName;`)
    - genossi-frontend/src/page/assemblies.rs (Z. 1-30 für Imports-Pattern der Listen-Page-Stubs; nicht voll implementieren — nur die Struktur kopieren)
    - genossi-frontend/src/page/assembly_details.rs (Z. 1-50 für Detail-Page-Stub-Struktur — Komponenten-Signatur `#[component] pub fn AssemblyDetails(id: String) -> Element`)
  </read_first>
  <action>
    **Schritt 1 — Neue Stub-Datei `genossi-frontend/src/page/repayment_phases.rs`:**
    ```rust
    use dioxus::prelude::*;

    /// Phase 12 Plan 12-04 implementiert diese Liste. Stub damit der Router
    /// in Plan 12-03 schon eine Ziel-Component hat.
    #[component]
    pub fn RepaymentPhases() -> Element {
        rsx! {
            div { class: "container mx-auto px-4 py-6",
                h1 { class: "text-2xl font-bold", "Anteils-Rückzahlungsphasen" }
                p { class: "text-gray-500 mt-4", "TODO Plan 12-04: Listen-Page (UI-01)" }
            }
        }
    }
    ```

    **Schritt 2 — Neue Stub-Datei `genossi-frontend/src/page/repayment_phase_details.rs`:**
    ```rust
    use dioxus::prelude::*;

    /// Phase 12 Plan 12-05 implementiert das 3-Tab-Layout. Stub damit der Router
    /// in Plan 12-03 schon eine Ziel-Component mit `id: String`-Prop hat.
    #[component]
    pub fn RepaymentPhaseDetails(id: String) -> Element {
        rsx! {
            div { class: "container mx-auto px-4 py-6",
                h1 { class: "text-2xl font-bold", "Phase-Details" }
                p { class: "text-gray-500 mt-4", "TODO Plan 12-05: 3-Tab-Layout (UI-02). Phase-ID: {id}" }
            }
        }
    }
    ```

    **Schritt 3 — `genossi-frontend/src/page/mod.rs` erweitern** (Pattern Z. 3-21 + 22-41):
    ```rust
    // ─── existing pub mod ─── …
    // ─── Phase 12 ─── repayment pages ───────────────────────────────────
    pub mod repayment_phase_details;
    pub mod repayment_phases;

    // ─── existing pub use ─── …
    // ─── Phase 12 ─── repayment pages re-exports ────────────────────────
    pub use repayment_phase_details::RepaymentPhaseDetails;
    pub use repayment_phases::RepaymentPhases;
    ```

    Reihenfolge in alphabetischer Konsistenz mit umgebenden Re-Exports (mod.rs hat eingespielten Alpha-Sort).
  </action>
  <verify>
    <automated>cargo build -p genossi-frontend</automated>
  </verify>
  <done>
    - cargo build -p genossi-frontend exit 0
    - `ls genossi-frontend/src/page/repayment_phases.rs genossi-frontend/src/page/repayment_phase_details.rs` — beide existieren
    - `rg "pub use repayment_phases::RepaymentPhases|pub use repayment_phase_details::RepaymentPhaseDetails" genossi-frontend/src/page/mod.rs` = 2 Treffer
    - `rg "TODO Plan 12-04" genossi-frontend/src/page/repayment_phases.rs` = 1 Treffer (Stub-Markierung sichtbar für späteren Replace-Test)
    - `rg "TODO Plan 12-05" genossi-frontend/src/page/repayment_phase_details.rs` = 1 Treffer
  </done>
</task>

<task type="auto">
  <name>Task 2: Route-Enum + TopBar NavItem (D-25, D-27)</name>
  <read_first>
    - genossi-frontend/src/router.rs (Z. 1-22 für pub-use-Pattern; Z. 24-69 für Route-Enum mit Assembly-Routes als 1:1-Vorbild)
    - genossi-frontend/src/component/top_bar.rs (Z. 40-70 für mitglieder_items mit show_admin-Gate als Vorbild)
    - .planning/phases/12-frontend-component-first/12-CONTEXT.md (D-25 Routes mit RequirePrivilege admin; D-27 Top-Bar Nav)
    - .planning/phases/12-frontend-component-first/12-PATTERNS.md §9 router.rs + §10 top_bar.rs
  </read_first>
  <action>
    **Schritt 1 — `genossi-frontend/src/router.rs` erweitern:**

    A. **Top-of-File pub-use ergänzen** (Z. 1-22, alphabetisch konsistent):
    ```rust
    pub use crate::page::RepaymentPhaseDetails;
    pub use crate::page::RepaymentPhases;
    ```

    B. **Route-Enum erweitern** (direkt nach den Assembly-Routes `#[route("/assemblies/:id")] AssemblyDetails { id: String }`, Z. ~37):
    ```rust
        // ── Phase 12 ── Anteils-Rückzahlung (Vorstand-only, admin-gated über RequirePrivilege in der Page) ──
        #[route("/repayment-phases")]
        RepaymentPhases {},
        #[route("/repayment-phases/:id")]
        RepaymentPhaseDetails { id: String },
    ```

    **Schritt 2 — `genossi-frontend/src/component/top_bar.rs` erweitern** (Z. 62-70 — innerhalb des `if show_admin { mitglieder_items.push(...) }`-Blocks, direkt NACH dem Assemblies-Push):
    ```rust
    if show_admin {
        mitglieder_items.push(NavItem {
            label: i18n.t(Key::Applications).to_string(),
            route: Route::ApplicationsPage {},
        });
        mitglieder_items.push(NavItem {
            label: i18n.t(Key::Assemblies).to_string(),
            route: Route::Assemblies {},
        });
        // ── Phase 12 D-27 ── Anteils-Rückzahlung neben Assemblies (Mitglieder-Workflow-Affinität) ──
        mitglieder_items.push(NavItem {
            label: i18n.t(Key::RepaymentPhases).to_string(),
            route: Route::RepaymentPhases {},
        });
    }
    ```

    **D-01 Check:** Diese Datei wird in 12-15 als Bestandteil des Grep-Gates getestet (top_bar.rs ist KEIN repayment_*.rs-File, daher außerhalb des engen Grep-Gate-Scopes; aber die Konvention ist auch hier gepflegt — push-Pattern enthält keinen neuen Button).

    **D-25 Auth-Gate:** Die Routes selbst sind keine Auth-Wrapper. Plan 12-04 und 12-05 implementieren `RequirePrivilege { privilege: "admin", ... }` in der jeweiligen Page-Component-Body. Der Top-Bar-Push selbst ist bereits hinter `if show_admin { ... }` gegated — Vorstand-only ist also DOUBLE-gated (Nav sichtbar nur für Admin + Page-Body führt zusätzlich `RequirePrivilege` durch).
  </action>
  <verify>
    <automated>cargo build -p genossi-frontend</automated>
  </verify>
  <done>
    - cargo build -p genossi-frontend exit 0
    - `rg '#\[route\("/repayment-phases"\)\]' genossi-frontend/src/router.rs` = 1 Treffer
    - `rg '#\[route\("/repayment-phases/:id"\)\]' genossi-frontend/src/router.rs` = 1 Treffer
    - `rg "Route::RepaymentPhases\s*\{\}" genossi-frontend/src/component/top_bar.rs` = 1 Treffer (NavItem)
    - `rg "Key::RepaymentPhases\b" genossi-frontend/src/component/top_bar.rs` = 1 Treffer (Label-Lookup)
    - Reihenfolge im mitglieder_items.push-Block: Applications → Assemblies → RepaymentPhases (Mitglieder-Workflow-Affinität laut D-27)
  </done>
</task>

</tasks>

<verification>
- `cargo build -p genossi-frontend` exit 0
- Manueller Smoke-Test (out-of-scope für automated verify, aber als Hinweis): `dx serve` und Navigation zu `/repayment-phases` zeigt „TODO Plan 12-04" — bestätigt Route-Wiring funktioniert
- `cargo test -p genossi-frontend --lib` weiterhin grün
- Vorstand-Auth-Gate ist via `show_admin`-Gate im NavItem-Push verteidigt (Page-Bodys folgen in 12-04 + 12-05 mit zusätzlichem RequirePrivilege-Wrap)
</verification>

<success_criteria>
Routen sind navigierbar (mit Stub-Inhalt), NavItem ist sichtbar für Vorstand. Pläne 12-04 und 12-05 ersetzen jeweils nur den Stub-Body, ohne Router-Refactor.
</success_criteria>

<output>
After completion, create `.planning/phases/12-frontend-component-first/12-03-SUMMARY.md` documenting:
- Konkrete Edit-Locations in router.rs + top_bar.rs (Zeilennummern nach Edit)
- Entscheidung: NavItem-Placement in mitglieder_items (statt verwaltung_items) — Begründung Mitglieder-Workflow-Affinität laut D-27 Discretion
- Hinweis dass die zwei Stub-Pages in 12-04/12-05 ersetzt werden — wenn jemand das hier vergisst, hat der Vorstand „TODO Plan 12-04" in der Produktion (Phase-12-Verify-Phase muss das mitprüfen)
</output>
