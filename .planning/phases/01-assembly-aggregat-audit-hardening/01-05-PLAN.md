---
phase: 01-assembly-aggregat-audit-hardening
plan: 05
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03, 01-04]
files_modified:
  - genossi_bin/tests/e2e_tests.rs
autonomous: true
requirements: [ASSY-07]
requirements_addressed: [ASSY-07]
must_haves:
  truths:
    - "E2E-Test test_assembly_lifecycle_audit_chain_intact existiert in genossi_bin/tests/e2e_tests.rs (D-12)"
    - "Test fuehrt Sequenz aus: POST /api/assembly (201) -> POST /api/assembly/{id}/open (200) -> POST /api/assembly/{id}/close (200)"
    - "Test ruft GET /api/audit/verify und assertet response.valid==true und broken_links empty (D-12, ASSY-07)"
    - "Test assertet total_entries >= 3 (mindestens create, open, close)"
    - "Test ruft GET /api/audit/assembly/{id} und prueft, dass die Process-Strings 'assembly.create', 'assembly.open', 'assembly.close' alle vorkommen (D-11, RESEARCH §11)"
    - "Test verifiziert opened.status==Open, closed.status==Closed, opened.opened_at.is_some(), closed.closed_at.is_some()"
    - "Negativ-Test test_close_assembly_from_preparation_returns_conflict deckt Pitfall 3 ab"
    - "Negativ-Test test_open_assembly_from_closed_returns_conflict deckt Pitfall 3 ab"
    - "Tests verwenden setup() aus e2e_tests.rs (In-Memory-SQLite, real HTTP via reqwest, mock_auth feature)"
  artifacts:
    - path: genossi_bin/tests/e2e_tests.rs
      provides: "E2E-Tests fuer Assembly-Lifecycle und Audit-Hashchain-Verifikation"
      exports: ["test_assembly_lifecycle_audit_chain_intact", "test_close_assembly_from_preparation_returns_conflict", "test_open_assembly_from_closed_returns_conflict"]
  key_links:
    - from: "test_assembly_lifecycle_audit_chain_intact"
      to: "GET /api/audit/verify"
      via: "client.get(server.url(\"/api/audit/verify\"))"
      pattern: '/api/audit/verify'
    - from: "test_assembly_lifecycle_audit_chain_intact"
      to: "Process-Identifier-Filter (D-11)"
      via: "GET /api/audit/assembly/{id} -> assert process strings"
      pattern: '"assembly\.(create|open|close)"'
---

<objective>
End-to-End-Verifikation der Phase-1-Lieferung. Ein Hauptest und zwei Negativ-Tests in `genossi_bin/tests/e2e_tests.rs`. Wave 3, depends on Plans 01-04.

Purpose: Belegt durchgaengig — von HTTP-Request bis Audit-Hashchain — dass der Lifecycle korrekt funktioniert und die Audit-Chain intakt bleibt (ASSY-07). Zusaetzlich: zwei Negativ-Tests fuer State-Transition-Conflicts (Pitfall 3).
Output: 3 neue Tests in der bestehenden `e2e_tests.rs`-Datei (kein neues File per D-12). `cargo test --test e2e_tests` gruen.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-assembly-aggregat-audit-hardening/01-CONTEXT.md
@.planning/phases/01-assembly-aggregat-audit-hardening/01-RESEARCH.md
@.planning/phases/01-assembly-aggregat-audit-hardening/01-PATTERNS.md
@.planning/phases/01-assembly-aggregat-audit-hardening/01-01-SUMMARY.md
@.planning/phases/01-assembly-aggregat-audit-hardening/01-02-SUMMARY.md
@.planning/phases/01-assembly-aggregat-audit-hardening/01-03-SUMMARY.md
@.planning/phases/01-assembly-aggregat-audit-hardening/01-04-SUMMARY.md
@.planning/codebase/TESTING.md
@CLAUDE.md
@genossi_bin/tests/e2e_tests.rs
@genossi_rest/src/audit_log.rs

<interfaces>
<!-- Aus Plan 02 (genossi_rest_types): -->
```rust
pub struct AssemblyTO { id: Uuid, name, date, location, status: AssemblyStatusTO, opened_at, closed_at, created, deleted, version }
pub struct AssemblyDetailTO { assembly: AssemblyTO, snapshot_member_count: u64 }
pub struct CreateAssemblyRequest { name: String, date: Option<PrimitiveDateTime>, location: Option<String> }
pub enum AssemblyStatusTO { Preparation, Open, Closed }
```

<!-- Bestehender Audit-Verify-Endpoint (genossi_rest/src/audit_log.rs): -->
```rust
// GET /api/audit/verify
// Returns:
pub struct VerifyResponseTO {
    pub valid: bool,
    pub total_entries: u64,
    pub broken_links: Vec<...>,  // Liste von gebrochenen Hash-Links
}

// GET /api/audit/{entity_type}/{entity_id}
// Returns Vec<AuditLogEntryTO>; jeder Eintrag hat .process: String und .field_name etc.
pub struct AuditLogEntryTO {
    pub id: Uuid,
    pub entity_type: String,
    pub entity_id: Uuid,
    pub process: String,  // <-- "assembly.create" / "assembly.open" / "assembly.close"
    pub action: String,   // "create" oder "update"
    pub field_name: String,
    /* ... */
}
```

<!-- setup() in genossi_bin/tests/e2e_tests.rs (Zeilen 23-37): -->
```rust
async fn setup() -> genossi_rest::test_server::test_support::TestServer {
    let pool = Arc::new(SqlitePool::connect("sqlite::memory:").await.unwrap());
    sqlx::migrate!("../migrations/sqlite").run(&*pool).await.unwrap();
    let rest_state = RestStateImpl::new(pool);
    start_test_server(rest_state).await
}
```

<!-- Vorlage Test (genossi_bin/tests/e2e_tests.rs:7499-7523, test_audit_verify_after_operations): -->
```rust
#[tokio::test]
async fn test_audit_verify_after_operations() {
    let server = setup().await;
    let client = reqwest::Client::new();
    /* ... POST member, then GET /api/audit/verify, assert valid */
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hauptest test_assembly_lifecycle_audit_chain_intact + zwei Negativ-Tests</name>
  <files>genossi_bin/tests/e2e_tests.rs</files>
  <read_first>
    - genossi_bin/tests/e2e_tests.rs (Zeilen 1-50: setup(), Zeilen 7499-7523: test_audit_verify_after_operations als 1:1-Vorlage; weitere bestehende Tests fuer Pattern-Referenz)
    - genossi_rest/src/audit_log.rs (Verify-Endpoint, AuditLogEntryTO, VerifyResponseTO Schema)
    - genossi_rest_types/src/lib.rs (AssemblyTO, CreateAssemblyRequest, AssemblyStatusTO aus Plan 02)
    - .planning/phases/01-assembly-aggregat-audit-hardening/01-RESEARCH.md (§11 E2E-Test Sketch und Code-Beispiel 6)
    - .planning/phases/01-assembly-aggregat-audit-hardening/01-PATTERNS.md (Pattern fuer e2e_tests.rs)
    - .planning/phases/01-assembly-aggregat-audit-hardening/01-CONTEXT.md (D-11, D-12)
  </read_first>
  <behavior>
    - `cargo test --test e2e_tests test_assembly_lifecycle_audit_chain_intact` gruen
    - Test sendet POST mit JSON `{"name":"GV 2026", "date":"2026-06-15T18:00:00.000000000", "location":"Vereinsheim"}` und expectet 201 + AssemblyTO mit status=Preparation
    - Test sendet POST `/api/assembly/{id}/open` (kein Body) und expectet 200 + AssemblyTO mit status=Open + opened_at.is_some()
    - Test sendet POST `/api/assembly/{id}/close` und expectet 200 + AssemblyTO mit status=Closed + closed_at.is_some()
    - Test sendet GET `/api/audit/verify` und prueft response.valid==true UND response.broken_links empty UND response.total_entries >= 3
    - Test sendet GET `/api/audit/assembly/{id}` und prueft, dass die Process-Strings "assembly.create", "assembly.open", "assembly.close" alle vorkommen
    - Negativ-Test 1: nach create (Status=Preparation), POST `/api/assembly/{id}/close` returnt 409
    - Negativ-Test 2: nach create+open+close (Status=Closed), POST `/api/assembly/{id}/open` returnt 409
  </behavior>
  <action>
    Anhaengen an `genossi_bin/tests/e2e_tests.rs` — drei neue Tests am Datei-Ende (kein neues File, per D-12):

    **Hilfsimports fuer den Test (am Top der Datei pruefen, falls fehlend hinzufuegen):**
    - `use genossi_rest_types::{AssemblyTO, AssemblyDetailTO, AssemblyStatusTO, CreateAssemblyRequest};`
    - Ggf. die existierenden imports durch Pruefung von `cat genossi_bin/tests/e2e_tests.rs | head -30` ergaenzen.

    **Test 1: `test_assembly_lifecycle_audit_chain_intact`** (per D-12, ASSY-07, RESEARCH §11):
    ```rust
    #[tokio::test]
    async fn test_assembly_lifecycle_audit_chain_intact() {
        let server = setup().await;
        let client = reqwest::Client::new();

        // 1) Create assembly (status=Preparation)
        let create_body = serde_json::json!({
            "name": "GV 2026",
            "date": "2026-06-15T18:00:00.000000000",
            "location": "Vereinsheim",
        });
        let response = client
            .post(server.url("/api/assembly"))
            .json(&create_body)
            .send().await.unwrap();
        assert_eq!(response.status(), reqwest::StatusCode::CREATED, "create should return 201");
        let created: AssemblyTO = response.json().await.unwrap();
        let assembly_id = created.id;
        assert_eq!(created.status, AssemblyStatusTO::Preparation);
        assert!(created.opened_at.is_none());
        assert!(created.closed_at.is_none());

        // 2) Open assembly (status=Preparation -> Open + Snapshot-Befuellung)
        let response = client
            .post(server.url(&format!("/api/assembly/{}/open", assembly_id)))
            .send().await.unwrap();
        assert_eq!(response.status(), reqwest::StatusCode::OK, "open should return 200");
        let opened: AssemblyTO = response.json().await.unwrap();
        assert_eq!(opened.status, AssemblyStatusTO::Open);
        assert!(opened.opened_at.is_some(), "opened_at must be set after open");

        // 3) Close assembly (status=Open -> Closed)
        let response = client
            .post(server.url(&format!("/api/assembly/{}/close", assembly_id)))
            .send().await.unwrap();
        assert_eq!(response.status(), reqwest::StatusCode::OK, "close should return 200");
        let closed: AssemblyTO = response.json().await.unwrap();
        assert_eq!(closed.status, AssemblyStatusTO::Closed);
        assert!(closed.closed_at.is_some(), "closed_at must be set after close");

        // 4) Verify audit hash chain intact
        let response = client
            .get(server.url("/api/audit/verify"))
            .send().await.unwrap();
        assert_eq!(response.status(), reqwest::StatusCode::OK);
        let verify: genossi_rest_types::VerifyResponseTO = response.json().await.unwrap();
        assert!(verify.valid, "Audit hash chain must be valid after lifecycle");
        assert!(verify.broken_links.is_empty(), "broken_links must be empty");
        assert!(verify.total_entries >= 3, "expected >=3 audit entries (create+open+close), got {}", verify.total_entries);

        // 5) Verify each lifecycle process appears in the audit log for this assembly
        let response = client
            .get(server.url(&format!("/api/audit/assembly/{}", assembly_id)))
            .send().await.unwrap();
        assert_eq!(response.status(), reqwest::StatusCode::OK);
        let entries: Vec<genossi_rest_types::AuditLogEntryTO> = response.json().await.unwrap();
        let processes: std::collections::HashSet<&str> = entries.iter().map(|e| e.process.as_str()).collect();
        assert!(processes.contains("assembly.create"), "missing assembly.create process; got {:?}", processes);
        assert!(processes.contains("assembly.open"), "missing assembly.open process; got {:?}", processes);
        assert!(processes.contains("assembly.close"), "missing assembly.close process; got {:?}", processes);
    }
    ```

    **Test 2: `test_close_assembly_from_preparation_returns_conflict`** (Pitfall 3):
    ```rust
    #[tokio::test]
    async fn test_close_assembly_from_preparation_returns_conflict() {
        let server = setup().await;
        let client = reqwest::Client::new();

        // Create (status=Preparation)
        let create_body = serde_json::json!({
            "name": "GV 2026",
            "date": "2026-06-15T18:00:00.000000000",
            "location": null,
        });
        let response = client.post(server.url("/api/assembly"))
            .json(&create_body).send().await.unwrap();
        assert_eq!(response.status(), reqwest::StatusCode::CREATED);
        let created: AssemblyTO = response.json().await.unwrap();

        // Direct close without open should return 409
        let response = client
            .post(server.url(&format!("/api/assembly/{}/close", created.id)))
            .send().await.unwrap();
        assert_eq!(response.status(), reqwest::StatusCode::CONFLICT, "close from Preparation must be 409");
    }
    ```

    **Test 3: `test_open_assembly_from_closed_returns_conflict`** (Pitfall 3):
    ```rust
    #[tokio::test]
    async fn test_open_assembly_from_closed_returns_conflict() {
        let server = setup().await;
        let client = reqwest::Client::new();

        // Create -> Open -> Close
        let create_body = serde_json::json!({
            "name": "GV 2026",
            "date": "2026-06-15T18:00:00.000000000",
            "location": null,
        });
        let response = client.post(server.url("/api/assembly"))
            .json(&create_body).send().await.unwrap();
        let created: AssemblyTO = response.json().await.unwrap();
        let id = created.id;

        let response = client.post(server.url(&format!("/api/assembly/{}/open", id))).send().await.unwrap();
        assert_eq!(response.status(), reqwest::StatusCode::OK);

        let response = client.post(server.url(&format!("/api/assembly/{}/close", id))).send().await.unwrap();
        assert_eq!(response.status(), reqwest::StatusCode::OK);

        // Re-open after close -> 409
        let response = client.post(server.url(&format!("/api/assembly/{}/open", id))).send().await.unwrap();
        assert_eq!(response.status(), reqwest::StatusCode::CONFLICT, "open from Closed must be 409");
    }
    ```

    **WICHTIG**: Vor dem Schreiben der Tests pruefe per Read auf `genossi_rest/src/audit_log.rs`, ob `VerifyResponseTO` und `AuditLogEntryTO` exakt diese Feldnamen haben. Falls Feldnamen anders heissen (z. B. `entries: Vec<...>` statt direkt Vec, oder `mismatches` statt `broken_links`), passe den Test entsprechend an. Genau dieselben Feldnamen verwenden, wie sie im aktuellen Audit-Log-Code stehen — nicht erfinden.

    Falls `AuditLogEntryTO.process` nicht direkt als public Field exponiert ist, aber ein `details`-Feld o.ae. existiert, das den Process-String enthaelt: nutze stattdessen den passenden Accessor-Pfad. Der Verify-Endpoint und der by-entity-id Endpoint sind in genossi_rest/src/audit_log.rs definiert; deren Response-Schema ist authoritative.

    **Hinweis zum mock_auth-Feature**: Bestehende Tests im File laufen mit `feature = "mock_auth"`; Permission-Check wird durchgewinkt. Falls die Tests einen expliziten Auth-Header brauchen (z. B. `X-Mock-User` o.ae.), schaue im bestehenden `test_audit_verify_after_operations`-Test wie der Header gesetzt wird und uebernimm das Pattern.
  </action>
  <verify>
    <automated>cargo test --test e2e_tests test_assembly 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test e2e_tests test_assembly_lifecycle_audit_chain_intact` exit 0
    - `cargo test --test e2e_tests test_close_assembly_from_preparation_returns_conflict` exit 0
    - `cargo test --test e2e_tests test_open_assembly_from_closed_returns_conflict` exit 0
    - `grep -c "fn test_assembly_lifecycle_audit_chain_intact" genossi_bin/tests/e2e_tests.rs` == 1
    - `grep -c "fn test_close_assembly_from_preparation_returns_conflict" genossi_bin/tests/e2e_tests.rs` == 1
    - `grep -c "fn test_open_assembly_from_closed_returns_conflict" genossi_bin/tests/e2e_tests.rs` == 1
    - `grep -c "/api/audit/verify" genossi_bin/tests/e2e_tests.rs | grep -E '^[2-9]|[0-9]{2,}'` (≥ 2 Vorkommen — bestehender Test + neuer Test)
    - `grep -c "/api/audit/assembly/" genossi_bin/tests/e2e_tests.rs` ≥ 1
    - `grep -c "assembly.create" genossi_bin/tests/e2e_tests.rs` ≥ 1
    - `grep -c "assembly.open" genossi_bin/tests/e2e_tests.rs` ≥ 1
    - `grep -c "assembly.close" genossi_bin/tests/e2e_tests.rs` ≥ 1
    - `grep -c "StatusCode::CONFLICT" genossi_bin/tests/e2e_tests.rs | grep -E '^[2-9]'` (≥ 2 Vorkommen — die zwei Negativ-Tests)
    - `cargo test --test e2e_tests` exit 0 (alle bestehenden + neuen Tests gruen)
  </acceptance_criteria>
  <done>
    Drei E2E-Tests im File `genossi_bin/tests/e2e_tests.rs` und alle drei gruen. ASSY-07 ist test-belegt: Hash-Chain bleibt nach Lifecycle-Aktionen intakt, alle drei Process-Identifier sind im Audit-Log praesent. Pitfall 3 ist test-belegt durch zwei Negativ-Tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-Server (e2e) | In-Memory-SQLite, mock_auth feature; reqwest-Client als externer Caller. Unterscheidet sich von Production primaer durch Auth-Pfad (mock_auth statt OIDC) — der Lifecycle und die Audit-Hashchain-Logik sind identisch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Tampering | Audit-Hashchain-Bruch nach Phase-1-Lifecycle | mitigate | E2E-Test ruft `/api/audit/verify` und assertet `valid==true` und `broken_links` empty (D-12, ASSY-07); falls Plan 03 die Tx-Atomizitaet falsch implementiert hat, schlaegt der Test hier fehl |
| T-01-05-02 | Tampering | State-Transition-Bypass | mitigate | Zwei Negativ-Tests fuer Pitfall 3 (Close from Preparation 409; Open from Closed 409); falls Service-Guards fehlen, schlagen die Tests hier fehl |
| T-01-05-03 | Repudiation | Process-Identifier inkonsistent | mitigate | Test prueft, dass alle drei Process-Strings ("assembly.create", "assembly.open", "assembly.close") im Audit-Log existieren (D-11) |
</threat_model>

<verification>
- `cargo test --test e2e_tests` gruen (alle bestehenden + 3 neue Tests)
- `cargo test --workspace` gruen (Gesamt-Regression-Check)
- `cargo build --release -p genossi_bin` gruen (Smoke-Check)
- `cargo clippy --all-targets --all-features` ohne neue Warnings
- Lokaler Smoke (optional, manuell): `cargo run --bin genossi` starten, `curl -X POST http://localhost:3000/api/assembly -H "Content-Type: application/json" -d '{"name":"Test","date":"2026-06-01T18:00:00.000000000"}'` -> 201
</verification>

<success_criteria>
- 3 neue E2E-Tests in genossi_bin/tests/e2e_tests.rs (D-12: kein neues File)
- test_assembly_lifecycle_audit_chain_intact: durchlaeuft Create -> Open -> Close, dann Verify-Hash-Chain + Process-Identifier-Check (ASSY-07)
- test_close_assembly_from_preparation_returns_conflict: deckt Pitfall 3
- test_open_assembly_from_closed_returns_conflict: deckt Pitfall 3
- Alle 3 Tests gruen via `cargo test --test e2e_tests`
- `cargo test --workspace` gruen — keine Regressionen
- D-11 (Process-Identifier) ist im Audit-Log nachweisbar
- D-12 (CI-E2E-Test gegen Verify-Endpoint) ist erfuellt
</success_criteria>

<output>
Erstelle nach Abschluss `.planning/phases/01-assembly-aggregat-audit-hardening/01-05-SUMMARY.md` mit Test-Output, Total-Entries-Anzahl und Bestaetigung dass Phase-1-Goal erreicht ist (ASSY-01..05, 07 alle test-belegt).
</output>
