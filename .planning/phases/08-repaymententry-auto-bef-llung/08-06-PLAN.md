---
phase: 08-repaymententry-auto-bef-llung
plan: 06
type: execute
wave: 5
depends_on: ["08-05"]
files_modified:
  - genossi_bin/tests/e2e_tests.rs
autonomous: true
requirements: [ENTR-01, ENTR-02, ENTR-03, ENTR-04, ENTR-05, ENTR-06, PHAS-02, PHAS-03]
must_haves:
  truths:
    - "E2E-Tests verifizieren Auto-Fill beim Phase-Open (PHAS-02 / ENTR-01) end-to-end gegen real-laufendem HTTP-Server"
    - "E2E-Tests verifizieren manuellen POST /api/repayment-entry mit Happy + 3 Validation-Fails (ENTR-02 / D-11)"
    - "E2E-Tests verifizieren Status-Toggle Open↔Contacted (ENTR-06) und PaidOut-Reject (D-05)"
    - "E2E-Tests verifizieren DELETE /api/repayment-entry/{id} mit Soft-Delete + PaidOut-Reject (ENTR-05)"
    - "E2E-Tests verifizieren Batch-Toggle Happy + all-or-nothing (D-08)"
    - "E2E-Tests verifizieren Close-Validation mit pending_member_numbers im 409-Body (PHAS-03 / D-15)"
    - "E2E-Tests verifizieren 0-Entry-Close ist erlaubt (D-14)"
    - "E2E-Tests verifizieren Audit-Hashchain via /api/audit/verify nach kompletter Phase-8-Lifecycle valid=true"
    - "Phase-7-bestehende E2E-Tests bleiben grün (kein Regress)"
    - "Helper postet Members an /api/members (Plural — verifiziert per grep auf genossi_rest/src/lib.rs:559)"
  artifacts:
    - path: "genossi_bin/tests/e2e_tests.rs"
      provides: "11+ neue E2E-Tests für Phase 8 + 2 Helper-Funktionen (create_member_with_exit_date + create_open_repayment_phase)"
      contains: "test_open_phase_triggers_auto_fill"
      contains_2: "test_close_phase_with_pending_entries_returns_409_with_member_numbers"
  key_links:
    - from: "test_open_phase_triggers_auto_fill"
      to: "POST /api/repayment-phase/{id}/open"
      via: "create 3 members with exit_date in FY → open phase → list entries → 3 entries returned"
      pattern: "auto.?fill"
    - from: "test_audit_chain_intact_after_phase_8_lifecycle"
      to: "/api/audit/verify"
      via: "create → open → batch-toggle → delete → /verify response.valid == true"
      pattern: "valid.*true"
threat_model:
  threats:
    - id: T-08-06-01
      description: "E2E test uses real-laufende DB; flakiness from port conflicts or migrations (Availability)"
      asvs_ref: V14.5.1
      severity: low
      mitigation: "start_test_server bindet random port + in-memory SQLite + sqlx::migrate! auto-runs. Phase-3/4/7-E2E-Suite ist bewährt mit 248+ Tests. Plan 06 nutzt dieselbe Infrastruktur."
    - id: T-08-06-02
      description: "Test asserts on member-number string format that differs from Service-Implementation (False-Positive)"
      asvs_ref: V14.5.1
      severity: low
      mitigation: "Plan-04 Service-Impl gibt Mitgliedsnummern als plain to_string() raus (z.B. '1', '5', '+5 weitere'). Test prüft auf Substring-Match mit beiden Formaten zur Robustheit (assert! body.contains(\"pending_count\") + body.contains(member_number.to_string())). Format kann Plan-04-Architekt-Discretion abweichen → wenn Test fehlschlägt, an Implementation anpassen."
    - id: T-08-06-03
      description: "Audit-Chain-Test passes locally but breaks on multi-process deploys (False-Positive)"
      asvs_ref: V10.1.1
      severity: low
      mitigation: "E2E nutzt single-process in-memory SQLite; hash chain ist deterministic single-writer (Phase-1-Konvention). Multi-Prozess-Sharding ist OUT OF SCOPE für v1.1."
---

<objective>
Phase 8 Plan 06: End-to-End-Tests gegen real-laufenden HTTP-Server mit in-memory SQLite. Verifiziert die Phase-8-Funktionalität durchgehend vom HTTP-Layer durch Service → DAO → DB inklusive Audit-Hashchain-Konsistenz.

Purpose: Erfüllt die Verifikation aller ROADMAP Phase 8 Success Criteria (5 Success Criteria + alle 8 Requirements ENTR-01..06 + PHAS-02 + PHAS-03). Sichert Regress gegen Phase-7-Bestand.

Output: 1 Datei modifiziert (genossi_bin/tests/e2e_tests.rs); 11+ neue Tests; 2 neue Helper-Funktionen.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-repaymententry-auto-bef-llung/08-CONTEXT.md
@.planning/phases/08-repaymententry-auto-bef-llung/08-PATTERNS.md
@genossi_bin/tests/e2e_tests.rs
@genossi_rest_types/src/lib.rs
@genossi_rest/src/test_server.rs

<interfaces>
<!-- Aus genossi_bin/tests/e2e_tests.rs (Phase-7-Vorlage Z. 10553-10999) -->

```rust
async fn setup() -> genossi_rest::test_server::test_support::TestServer { ... }
fn sample_member() -> MemberTO { ... }
async fn create_test_member(client: &reqwest::Client, server: &TestServer) -> MemberTO { ... }

// Phase-7-Helper:
async fn create_preparation_repayment_phase(
    client: &reqwest::Client,
    server: &TestServer,
    fiscal_year: i32,
    share_value: i64,
) -> RepaymentPhaseTO { ... }

// Phase-7-Lifecycle-Test:
#[tokio::test]
async fn test_repayment_phase_lifecycle_audit_chain_intact() {
    let server = setup().await;
    let client = reqwest::Client::new();
    let phase = create_preparation_repayment_phase(&client, &server, 2026, 12000).await;
    // ... POST /open, PUT, POST /close, GET /api/audit/verify
}
```

<!-- Erwartete Phase-8-Endpoints (aus Plan 05):
- POST /api/repayment-entry
- GET /api/repayment-entry?phase_id=<uuid>
- GET /api/repayment-entry/{id}
- PUT /api/repayment-entry/{id}
- DELETE /api/repayment-entry/{id}
- POST /api/repayment-entry/batch-status
- POST /api/repayment-phase/{id}/open (Phase 7 — jetzt mit Auto-Fill)
- POST /api/repayment-phase/{id}/close (Phase 7 — jetzt mit Pending-Validation)

REAL Member-Endpoint (verified per grep auf genossi_rest/src/lib.rs:559):
- POST /api/members  (PLURAL, nicht Singular!)
-->
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 11+ E2E-Tests + 2 Helper-Funktionen in genossi_bin/tests/e2e_tests.rs</name>
  <files>genossi_bin/tests/e2e_tests.rs</files>
  <read_first>
    - genossi_bin/tests/e2e_tests.rs (komplette Datei lesen — bestehende Phase-7-Tests am Datei-Ende als Vorlage, setup() / sample_member() / create_test_member() / create_preparation_repayment_phase() im Header-Bereich)
    - genossi_rest_types/src/lib.rs (aus Plan 05 Task 1 — RepaymentEntryTO + Requests)
    - genossi_rest/src/lib.rs (Z. 559 — verifizieren dass member-route Plural `/api/members` ist)
    - .planning/phases/08-repaymententry-auto-bef-llung/08-PATTERNS.md §12 (E2E-Test-Liste + Test-Setup-Pattern)
    - .planning/phases/08-repaymententry-auto-bef-llung/08-CONTEXT.md (alle D-Decisions als Test-Spec)
  </read_first>
  <behavior>
    Test 1: test_open_phase_triggers_auto_fill — 3 Members (1 ohne current_shares > 0, 1 mit exit_date im FY, 1 weiterer im FY) → open → GET ?phase_id liefert 2 Einträge
    Test 2: test_open_phase_auto_fill_zero_members — 0 Members → open → Phase status Open, GET ?phase_id liefert 0 Einträge (D-14)
    Test 3: test_open_phase_skips_member_without_exit_date — 1 Member ohne exit_date → 0 Einträge erzeugt
    Test 4: test_open_phase_skips_member_outside_fiscal_year — 1 Member exit_date im Folgejahr → 0 Einträge erzeugt
    Test 5: test_manual_add_entry_happy_path — POST /api/repayment-entry mit gültigem Body in Open-Phase → 201 + RepaymentEntryTO mit Status Open
    Test 6: test_manual_add_entry_phase_not_open_returns_409 — POST in Preparation-Phase → 409 (D-11.1)
    Test 7: test_manual_add_entry_share_count_exceeds_returns_400 — POST mit share_count > member.current_shares → 400 (D-11.3)
    Test 8: test_update_entry_status_open_to_contacted_succeeds — PUT mit status=Contacted → 200 + entry.status==Contacted (D-06)
    Test 9: test_update_entry_status_paid_out_returns_409 — PUT mit status=PaidOut → 409 (D-05)
    Test 10: test_delete_entry_in_open_succeeds — DELETE auf Open-Entry → 204 + nachfolgender GET 404 (ENTR-05)
    Test 11: test_batch_toggle_happy_path — POST /batch-status mit 2 IDs, target=Contacted → 200 + beide entries.status==Contacted
    Test 12: test_batch_toggle_paid_out_target_returns_400 — POST /batch-status mit target=PaidOut → 400 (D-07)
    Test 13: test_close_phase_with_pending_entries_returns_409_with_member_numbers — Phase mit 1 Open Entry → close → 409 + Body enthält "pending_count" und Mitgliedsnummer (D-15)
    Test 14: test_close_phase_with_zero_entries_succeeds — Phase mit 0 Entries → close → 200 (D-14)
    Test 15: test_audit_chain_intact_after_phase_8_lifecycle — Vollzyklus (create phase → open mit auto-fill → batch-toggle → delete) → /api/audit/verify returnt valid=true
  </behavior>
  <action>
    **Modifiziere `genossi_bin/tests/e2e_tests.rs`** — alle neuen Tests am Datei-Ende (NACH der bestehenden Phase-7-Block) ergänzen.

    **Schritt 1 — Imports** (am Anfang der Datei oder im bestehenden use-Block ergänzen):
    ```rust
    use genossi_rest_types::{
        // ... bestehende imports ...
        BatchStatusRequest, CreateRepaymentEntryRequest, RepaymentEntryStatusTO,
        RepaymentEntryTO, UpdateRepaymentEntryRequest,
    };
    ```

    **Schritt 2 — 2 neue Helper-Funktionen** (am Datei-Ende, VOR den neuen Tests):

    > KORREKTUR (W-01 aus Checker-Review): Member-Endpoint ist `/api/members` (PLURAL), verified per `grep -n 'api/members' genossi_rest/src/lib.rs:559`. Der Helper postet an die Plural-Route, identisch zum bestehenden `create_test_member()`-Helper.

    ```rust
    /// Phase 8 helper — Member mit explizitem exit_date + current_shares für Auto-Fill-Tests.
    async fn create_member_with_exit_date(
        client: &reqwest::Client,
        server: &genossi_rest::test_server::test_support::TestServer,
        member_number: i64,
        fiscal_year: i32,
        current_shares: i32,
    ) -> MemberTO {
        let mut m = sample_member();
        m.member_number = member_number;
        m.current_shares = current_shares;
        m.shares_at_joining = current_shares;
        m.exit_date = Some(
            time::Date::from_calendar_date(fiscal_year, time::Month::June, 15).unwrap()
        );
        let response = client
            .post(server.url("/api/members"))  // Plural (D-14 / lib.rs:559)
            .json(&m)
            .send()
            .await
            .expect("create_member_with_exit_date POST failed");
        assert!(
            response.status().is_success(),
            "create_member_with_exit_date expected 2xx, got {}",
            response.status()
        );
        response.json().await.expect("decode MemberTO failed")
    }

    /// Phase 8 helper — Preparation-Phase erzeugen UND öffnen (triggert Auto-Fill).
    async fn create_open_repayment_phase(
        client: &reqwest::Client,
        server: &genossi_rest::test_server::test_support::TestServer,
        fiscal_year: i32,
        share_value: i64,
    ) -> RepaymentPhaseTO {
        let phase = create_preparation_repayment_phase(client, server, fiscal_year, share_value).await;
        let phase_id = phase.id;
        let open_resp = client
            .post(server.url(&format!("/api/repayment-phase/{}/open", phase_id)))
            .send()
            .await
            .expect("open_phase POST failed");
        assert_eq!(
            open_resp.status(),
            reqwest::StatusCode::OK,
            "open_phase must return 200"
        );
        open_resp.json().await.expect("decode opened RepaymentPhaseTO failed")
    }
    ```

    > Hinweis: Falls Phase-7-Open-Response auf 200 statt anderem Status-Code baut, an dortige Konvention anpassen.

    **Schritt 3 — 15 E2E-Tests** (alle am Datei-Ende):

    ```rust
    #[tokio::test]
    async fn test_open_phase_triggers_auto_fill() {
        let server = setup().await;
        let client = reqwest::Client::new();
        let fiscal_year = 2026;

        // Setup: 3 Members
        // - M1: exit_date in FY 2026, current_shares=5 → SOLL Eintrag bekommen
        // - M2: exit_date in FY 2026, current_shares=0 → KEIN Eintrag (D-02)
        // - M3: exit_date in FY 2026, current_shares=3 → SOLL Eintrag bekommen
        let _m1 = create_member_with_exit_date(&client, &server, 1, fiscal_year, 5).await;
        let _m2 = create_member_with_exit_date(&client, &server, 2, fiscal_year, 0).await;
        let _m3 = create_member_with_exit_date(&client, &server, 3, fiscal_year, 3).await;

        let phase = create_open_repayment_phase(&client, &server, fiscal_year, 12000).await;

        let resp = client
            .get(server.url(&format!("/api/repayment-entry?phase_id={}", phase.id)))
            .send()
            .await
            .unwrap();
        assert_eq!(resp.status(), 200);
        let entries: Vec<RepaymentEntryTO> = resp.json().await.unwrap();
        assert_eq!(entries.len(), 2, "expected 2 entries (skip M2 with current_shares=0)");
        // Reihenfolge: member_number ASC → 1, 3
        for e in &entries {
            assert!(matches!(e.status, RepaymentEntryStatusTO::Open));
            assert_eq!(e.phase_id, phase.id);
        }
    }

    #[tokio::test]
    async fn test_open_phase_auto_fill_zero_members() {
        let server = setup().await;
        let client = reqwest::Client::new();
        // Keine Members angelegt
        let phase = create_open_repayment_phase(&client, &server, 2026, 12000).await;
        assert_eq!(format!("{:?}", phase.status), "Open");

        let resp = client
            .get(server.url(&format!("/api/repayment-entry?phase_id={}", phase.id)))
            .send()
            .await
            .unwrap();
        let entries: Vec<RepaymentEntryTO> = resp.json().await.unwrap();
        assert_eq!(entries.len(), 0, "0-Entry-Case D-14");
    }

    #[tokio::test]
    async fn test_open_phase_skips_member_without_exit_date() {
        let server = setup().await;
        let client = reqwest::Client::new();
        // Member ohne exit_date (default sample_member)
        let _m = create_test_member(&client, &server).await;
        let phase = create_open_repayment_phase(&client, &server, 2026, 12000).await;

        let entries: Vec<RepaymentEntryTO> = client
            .get(server.url(&format!("/api/repayment-entry?phase_id={}", phase.id)))
            .send().await.unwrap().json().await.unwrap();
        assert_eq!(entries.len(), 0);
    }

    #[tokio::test]
    async fn test_open_phase_skips_member_outside_fiscal_year() {
        let server = setup().await;
        let client = reqwest::Client::new();
        // Member exit_date 2027 (next year)
        let _m = create_member_with_exit_date(&client, &server, 1, 2027, 5).await;
        let phase = create_open_repayment_phase(&client, &server, 2026, 12000).await;

        let entries: Vec<RepaymentEntryTO> = client
            .get(server.url(&format!("/api/repayment-entry?phase_id={}", phase.id)))
            .send().await.unwrap().json().await.unwrap();
        assert_eq!(entries.len(), 0);
    }

    #[tokio::test]
    async fn test_manual_add_entry_happy_path() {
        let server = setup().await;
        let client = reqwest::Client::new();
        let phase = create_open_repayment_phase(&client, &server, 2026, 12000).await;
        let m = create_test_member(&client, &server).await;

        let body = CreateRepaymentEntryRequest {
            phase_id: phase.id,
            member_id: m.id.unwrap(),
            share_count_to_pay_out: 2,
        };
        let resp = client
            .post(server.url("/api/repayment-entry"))
            .json(&body)
            .send().await.unwrap();
        assert_eq!(resp.status(), 201);
        let entry: RepaymentEntryTO = resp.json().await.unwrap();
        assert_eq!(entry.share_count_to_pay_out, 2);
        assert!(matches!(entry.status, RepaymentEntryStatusTO::Open));
    }

    #[tokio::test]
    async fn test_manual_add_entry_phase_not_open_returns_409() {
        let server = setup().await;
        let client = reqwest::Client::new();
        // Phase bleibt in Preparation
        let phase = create_preparation_repayment_phase(&client, &server, 2026, 12000).await;
        let m = create_test_member(&client, &server).await;

        let body = CreateRepaymentEntryRequest {
            phase_id: phase.id,
            member_id: m.id.unwrap(),
            share_count_to_pay_out: 2,
        };
        let resp = client
            .post(server.url("/api/repayment-entry"))
            .json(&body)
            .send().await.unwrap();
        assert_eq!(resp.status(), 409, "Phase not Open → 409 (D-11.1)");
    }

    #[tokio::test]
    async fn test_manual_add_entry_share_count_exceeds_returns_400() {
        let server = setup().await;
        let client = reqwest::Client::new();
        let phase = create_open_repayment_phase(&client, &server, 2026, 12000).await;
        let m = create_test_member(&client, &server).await;
        // sample_member hat current_shares == 3

        let body = CreateRepaymentEntryRequest {
            phase_id: phase.id,
            member_id: m.id.unwrap(),
            share_count_to_pay_out: 999,  // exceeds
        };
        let resp = client
            .post(server.url("/api/repayment-entry"))
            .json(&body)
            .send().await.unwrap();
        assert_eq!(resp.status(), 400, "share_count > current_shares → 400 (D-11.3)");
        let body_text = resp.text().await.unwrap();
        assert!(
            body_text.contains("share_count_to_pay_out"),
            "Body must mention field name; got: {}",
            body_text
        );
    }

    #[tokio::test]
    async fn test_update_entry_status_open_to_contacted_succeeds() {
        let server = setup().await;
        let client = reqwest::Client::new();
        let phase = create_open_repayment_phase(&client, &server, 2026, 12000).await;
        let m = create_test_member(&client, &server).await;

        let create_body = CreateRepaymentEntryRequest {
            phase_id: phase.id, member_id: m.id.unwrap(), share_count_to_pay_out: 1,
        };
        let entry: RepaymentEntryTO = client
            .post(server.url("/api/repayment-entry"))
            .json(&create_body).send().await.unwrap().json().await.unwrap();

        let update_body = UpdateRepaymentEntryRequest {
            share_count_to_pay_out: None,
            status: Some(RepaymentEntryStatusTO::Contacted),
            version: entry.version.unwrap(),
        };
        let resp = client
            .put(server.url(&format!("/api/repayment-entry/{}", entry.id)))
            .json(&update_body).send().await.unwrap();
        assert_eq!(resp.status(), 200);
        let updated: RepaymentEntryTO = resp.json().await.unwrap();
        assert!(matches!(updated.status, RepaymentEntryStatusTO::Contacted));
    }

    #[tokio::test]
    async fn test_update_entry_status_paid_out_returns_409() {
        let server = setup().await;
        let client = reqwest::Client::new();
        let phase = create_open_repayment_phase(&client, &server, 2026, 12000).await;
        let m = create_test_member(&client, &server).await;

        let create_body = CreateRepaymentEntryRequest {
            phase_id: phase.id, member_id: m.id.unwrap(), share_count_to_pay_out: 1,
        };
        let entry: RepaymentEntryTO = client
            .post(server.url("/api/repayment-entry"))
            .json(&create_body).send().await.unwrap().json().await.unwrap();

        let update_body = UpdateRepaymentEntryRequest {
            share_count_to_pay_out: None,
            status: Some(RepaymentEntryStatusTO::PaidOut),  // D-05 reject
            version: entry.version.unwrap(),
        };
        let resp = client
            .put(server.url(&format!("/api/repayment-entry/{}", entry.id)))
            .json(&update_body).send().await.unwrap();
        assert_eq!(resp.status(), 409, "PaidOut via PUT → 409 (D-05)");
    }

    #[tokio::test]
    async fn test_delete_entry_in_open_succeeds() {
        let server = setup().await;
        let client = reqwest::Client::new();
        let phase = create_open_repayment_phase(&client, &server, 2026, 12000).await;
        let m = create_test_member(&client, &server).await;

        let create_body = CreateRepaymentEntryRequest {
            phase_id: phase.id, member_id: m.id.unwrap(), share_count_to_pay_out: 1,
        };
        let entry: RepaymentEntryTO = client
            .post(server.url("/api/repayment-entry"))
            .json(&create_body).send().await.unwrap().json().await.unwrap();

        let resp = client
            .delete(server.url(&format!("/api/repayment-entry/{}", entry.id)))
            .send().await.unwrap();
        assert_eq!(resp.status(), 204);

        let get_resp = client
            .get(server.url(&format!("/api/repayment-entry/{}", entry.id)))
            .send().await.unwrap();
        assert_eq!(get_resp.status(), 404, "soft-deleted → 404 GET");
    }

    #[tokio::test]
    async fn test_batch_toggle_happy_path() {
        let server = setup().await;
        let client = reqwest::Client::new();
        let fiscal_year = 2026;
        let _m1 = create_member_with_exit_date(&client, &server, 1, fiscal_year, 5).await;
        let _m2 = create_member_with_exit_date(&client, &server, 2, fiscal_year, 5).await;
        let phase = create_open_repayment_phase(&client, &server, fiscal_year, 12000).await;

        // Auto-Fill erzeugte 2 Entries
        let entries: Vec<RepaymentEntryTO> = client
            .get(server.url(&format!("/api/repayment-entry?phase_id={}", phase.id)))
            .send().await.unwrap().json().await.unwrap();
        assert_eq!(entries.len(), 2);

        let body = BatchStatusRequest {
            entry_ids: entries.iter().map(|e| e.id).collect(),
            target_status: RepaymentEntryStatusTO::Contacted,
        };
        let resp = client
            .post(server.url("/api/repayment-entry/batch-status"))
            .json(&body).send().await.unwrap();
        assert_eq!(resp.status(), 200);
        let updated: Vec<RepaymentEntryTO> = resp.json().await.unwrap();
        assert_eq!(updated.len(), 2);
        for u in &updated {
            assert!(matches!(u.status, RepaymentEntryStatusTO::Contacted));
        }
    }

    #[tokio::test]
    async fn test_batch_toggle_paid_out_target_returns_400() {
        let server = setup().await;
        let client = reqwest::Client::new();
        let body = BatchStatusRequest {
            entry_ids: vec![uuid::Uuid::new_v4()],
            target_status: RepaymentEntryStatusTO::PaidOut,  // D-07 reject
        };
        let resp = client
            .post(server.url("/api/repayment-entry/batch-status"))
            .json(&body).send().await.unwrap();
        assert_eq!(resp.status(), 400, "PaidOut as target → 400 (D-07)");
    }

    #[tokio::test]
    async fn test_close_phase_with_pending_entries_returns_409_with_member_numbers() {
        let server = setup().await;
        let client = reqwest::Client::new();
        let fiscal_year = 2026;
        let _m1 = create_member_with_exit_date(&client, &server, 42, fiscal_year, 5).await;
        let phase = create_open_repayment_phase(&client, &server, fiscal_year, 12000).await;

        // Auto-Fill erzeugte 1 Entry (M42, status Open) → pending
        let resp = client
            .post(server.url(&format!("/api/repayment-phase/{}/close", phase.id)))
            .send().await.unwrap();
        assert_eq!(resp.status(), 409, "pending entries → 409 (PHAS-03 / D-15)");
        let body = resp.text().await.unwrap();
        assert!(body.contains("pending_count"), "Body must contain pending_count; got: {}", body);
        assert!(body.contains("42"), "Body must contain member_number 42; got: {}", body);
    }

    #[tokio::test]
    async fn test_close_phase_with_zero_entries_succeeds() {
        let server = setup().await;
        let client = reqwest::Client::new();
        // Phase ohne Member-Setup → Auto-Fill erzeugt 0 Entries
        let phase = create_open_repayment_phase(&client, &server, 2026, 12000).await;

        let resp = client
            .post(server.url(&format!("/api/repayment-phase/{}/close", phase.id)))
            .send().await.unwrap();
        assert_eq!(resp.status(), 200, "0-Entry-Close OK (D-14)");
    }

    #[tokio::test]
    async fn test_audit_chain_intact_after_phase_8_lifecycle() {
        let server = setup().await;
        let client = reqwest::Client::new();
        let fiscal_year = 2026;
        let _m1 = create_member_with_exit_date(&client, &server, 1, fiscal_year, 5).await;
        let _m2 = create_member_with_exit_date(&client, &server, 2, fiscal_year, 5).await;
        let phase = create_open_repayment_phase(&client, &server, fiscal_year, 12000).await;

        // Auto-Fill 2 Entries
        let entries: Vec<RepaymentEntryTO> = client
            .get(server.url(&format!("/api/repayment-entry?phase_id={}", phase.id)))
            .send().await.unwrap().json().await.unwrap();
        assert_eq!(entries.len(), 2);

        // Batch-Toggle alle auf Contacted
        let _ = client
            .post(server.url("/api/repayment-entry/batch-status"))
            .json(&BatchStatusRequest {
                entry_ids: entries.iter().map(|e| e.id).collect(),
                target_status: RepaymentEntryStatusTO::Contacted,
            }).send().await.unwrap();

        // Delete 1 Entry (Soft-Delete)
        let _ = client
            .delete(server.url(&format!("/api/repayment-entry/{}", entries[0].id)))
            .send().await.unwrap();

        // Audit-Chain prüfen
        let verify_resp = client
            .get(server.url("/api/audit/verify"))
            .send().await.unwrap();
        assert_eq!(verify_resp.status(), 200);
        let verify_body: serde_json::Value = verify_resp.json().await.unwrap();
        assert_eq!(
            verify_body["valid"], true,
            "Audit chain must remain valid after Phase 8 lifecycle; body: {}",
            verify_body
        );
    }
    ```

    **Schritt 4 — Phase-7-Regress-Check** (kein Code-Change, nur run):
    - `cargo test --test e2e_tests` muss alle 255 Phase-7-Tests + die 15 neuen Tests grün haben (270 total).
  </action>
  <verify>
    <automated>cargo build --tests -p genossi_bin 2>&1 | tail -5 && cargo test --test e2e_tests 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test_open_phase_triggers_auto_fill" genossi_bin/tests/e2e_tests.rs` == 1
    - `grep -c "test_open_phase_auto_fill_zero_members" genossi_bin/tests/e2e_tests.rs` == 1
    - `grep -c "test_manual_add_entry_happy_path" genossi_bin/tests/e2e_tests.rs` == 1
    - `grep -c "test_manual_add_entry_phase_not_open_returns_409" genossi_bin/tests/e2e_tests.rs` == 1
    - `grep -c "test_manual_add_entry_share_count_exceeds_returns_400" genossi_bin/tests/e2e_tests.rs` == 1
    - `grep -c "test_update_entry_status_open_to_contacted_succeeds" genossi_bin/tests/e2e_tests.rs` == 1
    - `grep -c "test_update_entry_status_paid_out_returns_409" genossi_bin/tests/e2e_tests.rs` == 1
    - `grep -c "test_delete_entry_in_open_succeeds" genossi_bin/tests/e2e_tests.rs` == 1
    - `grep -c "test_batch_toggle_happy_path" genossi_bin/tests/e2e_tests.rs` == 1
    - `grep -c "test_batch_toggle_paid_out_target_returns_400" genossi_bin/tests/e2e_tests.rs` == 1
    - `grep -c "test_close_phase_with_pending_entries_returns_409_with_member_numbers" genossi_bin/tests/e2e_tests.rs` == 1
    - `grep -c "test_close_phase_with_zero_entries_succeeds" genossi_bin/tests/e2e_tests.rs` == 1
    - `grep -c "test_audit_chain_intact_after_phase_8_lifecycle" genossi_bin/tests/e2e_tests.rs` == 1
    - `grep -c "async fn create_member_with_exit_date" genossi_bin/tests/e2e_tests.rs` == 1
    - `grep -c "async fn create_open_repayment_phase" genossi_bin/tests/e2e_tests.rs` == 1
    - Plural-Member-Route: `grep -c '"/api/members"' genossi_bin/tests/e2e_tests.rs` >= 1 (Helper postet an Plural — W-01)
    - Kein neuer Singular-Member-POST aus Phase-8-Code: `grep -nE '\.post\(server\.url\("/api/member"\)' genossi_bin/tests/e2e_tests.rs | wc -l` == 0
    - `cargo build --tests -p genossi_bin` exit 0
    - `cargo test --test e2e_tests` exit 0 mit Phase-7-Bestand (mind. 255) + 15 neue Tests grün (≥ 270 total)
  </acceptance_criteria>
  <done>15+ neue E2E-Tests + 2 Helper grün; alle Phase-7-E2E-Tests bleiben grün; Audit-Chain bleibt valid nach Phase-8-Lifecycle; Plural-Member-Endpoint korrekt verwendet.</done>
</task>

</tasks>

<verification>
- `cargo test --test e2e_tests` ≥ 270 Tests grün (255 Phase-7-Baseline + 15 Phase-8-neu)
- Audit-Chain bleibt valid nach Phase-8-Lifecycle
- Alle 8 Requirements (ENTR-01..06 + PHAS-02 + PHAS-03) E2E verifiziert
- Helper-POST geht an `/api/members` (Plural)
</verification>

<success_criteria>
- ROADMAP Phase 8 SC#1 (Migration), SC#2 (Auto-Fill), SC#3 (Manueller Create), SC#4 (Batch-Toggle), SC#5 (Close-Validation) alle E2E-belegt
- Audit-Hashchain bleibt valid (Test 15)
- Phase-7-E2E-Suite bleibt grün (kein Regress)
- 0-Entry-Close ist erlaubt (D-14)
</success_criteria>

<output>
After completion, create `.planning/phases/08-repaymententry-auto-bef-llung/08-06-SUMMARY.md`
</output>
