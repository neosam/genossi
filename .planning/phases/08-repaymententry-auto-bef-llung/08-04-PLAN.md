---
phase: 08-repaymententry-auto-bef-llung
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - genossi_service_impl/src/repayment_phase.rs
autonomous: true
requirements: [PHAS-02, PHAS-03]
must_haves:
  truths:
    - "RepaymentPhaseServiceImpl::open_repayment_phase befüllt in derselben Tx wie der Status-Übergang Preparation→Open alle Mitglieder mit exit_date BETWEEN {fy}-01-01 AND {fy}-12-31 AND current_shares > 0 als RepaymentEntries (PHAS-02, ENTR-01)"
    - "Auto-Fill nutzt N einzelne audited_create!-Calls statt batch-without-audit (D-03)"
    - "Auto-Fill läuft genau einmal beim open_phase, keine Re-Fill-Action (D-04)"
    - "Auto-Fill-Member-Filter: deleted IS NULL (via member_dao.all), exit_date IN FY (D-01), current_shares > 0 (D-02); KEIN is_normal()-Filter"
    - "Auto-Fill sortiert nach member_number ASC für deterministische Audit-Reihenfolge (CONTEXT Claude's Discretion)"
    - "Auto-Fill-Audit-Identifikation: gemeinsamer process-String 'repayment-phase.open' + zeitgleicher timestamp-Range (D-03 Klarstellung — KEINE shared transaction_id; jeder audited_create! generiert eigene transaction_id per Macro-Konvention, audit_log.rs:65)"
    - "RepaymentPhaseServiceImpl::close_repayment_phase blockt mit ServiceError::Conflict wenn pending entries existieren (status != PaidOut AND deleted IS NULL) (PHAS-03, D-13)"
    - "Close-Conflict-Body enthält pending_count + bis zu 20 Mitgliedsnummern (D-15)"
    - "0-Entry-Close ist erlaubt (D-14)"
    - "RepaymentPhaseServiceDeps erweitert um RepaymentEntryDao + MemberDao (Single-Tx-Multi-DAO Pattern)"
  artifacts:
    - path: "genossi_service_impl/src/repayment_phase.rs"
      provides: "Erweiterte open_repayment_phase mit Auto-Fill-Block + erweiterte close_repayment_phase mit Pending-Validation; Deps-Struct um RepaymentEntryDao + MemberDao erweitert"
      contains: "find_by_phase_id"
      contains_2: "fy_start"
  key_links:
    - from: "open_repayment_phase Auto-Fill-Block"
      to: "member_dao.all + N audited_create! auf repayment_entry_dao"
      via: "gleiche tx wie der Status-Übergang; jeder Eintrag eigene transaction_id; Identifikation via process-String + timestamp"
      pattern: "for member in targets"
    - from: "close_repayment_phase Pending-Check"
      to: "repayment_entry_dao.find_by_phase_id + member_dao.all"
      via: "pending = filter status != PaidOut AND deleted IS NULL"
      pattern: "pending_member_numbers"
threat_model:
  threats:
    - id: T-08-04-01
      description: "Auto-Fill partial-success leaves phase in Open with incomplete entries (Tampering)"
      asvs_ref: V11.1.4
      severity: high
      mitigation: "Auto-Fill läuft in derselben Tx wie der Status-Übergang Preparation→Open. Erster Fehler in den N audited_create-Calls → Tx-Drop = Rollback; Phase bleibt in Preparation. Unit-Test test_open_phase_auto_fill_atomic_on_dao_failure."
    - id: T-08-04-02
      description: "Race condition: phase opens twice in parallel, duplicating entries (Tampering)"
      asvs_ref: V11.1.4
      severity: medium
      mitigation: "Phase-State-Guard (status == Preparation vor Auto-Fill) blockt zweiten Open-Call mit Conflict. Optimistic-Locking via version-bump beim Status-Update verhindert silent overwrites."
    - id: T-08-04-03
      description: "Re-Fill via repeated open creates duplicate entries (Tampering)"
      asvs_ref: V11.1.4
      severity: medium
      mitigation: "Phase-State-Guard 'expected Preparation' (Phase 7 D-06 Reverse-Transition verboten) + D-04 'Auto-Fill exactly once'. Re-Open ist konsequent gesperrt."
    - id: T-08-04-04
      description: "Close conflict body leaks PII via member numbers to wrong actor (Information Disclosure)"
      asvs_ref: V8.1.1
      severity: low
      mitigation: "Endpoint is ADMIN_PRIVILEGE-only via PermissionService.check_permission. Vorstand sieht ohnehin alle Mitgliedsnummern in der Member-Liste; kein neuer Disclosure-Pfad."
    - id: T-08-04-05
      description: "Close skips pending validation when DAO returns empty list erroneously (Tampering)"
      asvs_ref: V11.1.4
      severity: medium
      mitigation: "find_by_phase_id Default-Impl filtert phase_id + deleted IS NULL deterministisch (Plan 01). Unit-Test test_close_phase_with_pending_entries_returns_conflict deckt den Negative-Path; E2E-Test in Plan 06 verifiziert mit echter SQLite."
---

<objective>
Phase 8 Plan 04: Erweiterung der bestehenden Phase-7-`RepaymentPhaseServiceImpl` um:
1. **Auto-Befüllung** in `open_repayment_phase` (PHAS-02 / ENTR-01) — atomar in derselben Tx wie der Status-Übergang Preparation→Open
2. **Pending-Validation** in `close_repayment_phase` (PHAS-03 / D-13/D-14/D-15) — blockt Close wenn Entries ≠ PaidOut UND nicht soft-deleted existieren

Purpose: Vervollständigt die in Phase 7 als Skeleton angelegten Übergänge. Foundation für Plan 05 (REST exposiert das via /api/repayment-phase/{id}/open + /close) und Plan 06 (E2E-Tests).

Output: 1 Datei modifiziert (genossi_service_impl/src/repayment_phase.rs), Deps erweitert um RepaymentEntryDao + MemberDao, Auto-Fill-Block + Pending-Check eingefügt.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-repaymententry-auto-bef-llung/08-CONTEXT.md
@.planning/phases/08-repaymententry-auto-bef-llung/08-PATTERNS.md
@genossi_service_impl/src/repayment_phase.rs
@genossi_service_impl/src/assembly.rs
@genossi_service_impl/src/audit_macros.rs
@genossi_service_impl/src/audit_log.rs
@genossi_dao/src/repayment_entry.rs
@genossi_dao/src/repayment_phase.rs
@genossi_dao/src/member.rs

<interfaces>
<!-- Auto-Fill-Vorlage aus genossi_service_impl/src/assembly.rs:181-258 (open_assembly) -->

```rust
async fn open_assembly(&self, id: Uuid, context: Authentication<Self::Context>) -> Result<Assembly, ServiceError> {
    let tx = self.transaction_dao.use_transaction(None).await?;
    let user_id = self.permission_service.current_user_id(context.clone()).await?
        .unwrap_or_else(|| "SYSTEM".to_string());
    self.permission_service.check_permission(ADMIN_PRIVILEGE, context).await?;

    let mut entity = self.assembly_dao.find_by_id(id, tx.clone()).await?
        .ok_or(ServiceError::EntityNotFound(id))?;

    if entity.status != AssemblyStatus::Preparation {
        return Err(ServiceError::Conflict(Arc::from(format!(
            "Cannot open assembly: status is '{}', expected 'Preparation'",
            entity.status.as_str()
        ))));
    }

    let now_offset = time::OffsetDateTime::now_utc();
    let now_pdt = time::PrimitiveDateTime::new(now_offset.date(), now_offset.time());
    let opened_date = now_offset.date();
    entity.status = AssemblyStatus::Open;
    entity.opened_at = Some(now_pdt);

    crate::audited_update!(self, self.assembly_dao, id, &entity, ASSEMBLY_PROCESS_OPEN, &user_id, tx);

    // Snapshot-Filter (assembly: is_normal + join_date + exit_date; Phase 8 anders)
    let all_members = self.member_dao.all(tx.clone()).await?;
    let snapshot_entities: Vec<AssemblyMemberSnapshotEntity> = all_members.iter()
        .filter(|m| m.status.is_normal())
        .filter(|m| m.join_date <= opened_date)
        .filter(|m| m.exit_date.map_or(true, |d| d > opened_date))
        .map(|m| AssemblyMemberSnapshotEntity {
            assembly_id: id, member_id: m.id, captured_at: now_pdt,
        })
        .collect();

    self.assembly_member_snapshot_dao.create_batch(&snapshot_entities, ASSEMBLY_PROCESS_OPEN, tx.clone()).await?;

    self.transaction_dao.commit(tx).await?;
    Ok(Assembly::from(&entity))
}
```

<!-- Bestehende open_repayment_phase / close_repayment_phase in repayment_phase.rs sind Skeleton — Phase 8 ergänzt NACH bzw. VOR dem audited_update! -->

<!-- RepaymentEntryEntity (aus Plan 01) + RepaymentEntryDao (aus Plan 01) + RepaymentEntryStatus (aus Plan 01) -->

<!-- MemberEntity-Felder: id, member_number i64, current_shares i32, exit_date Option<time::Date>, deleted Option<PrimitiveDateTime> -->

<!-- WICHTIGE Audit-Macro-Konvention (verified per Read auf audit_log.rs:65):
     build_create_entries ruft uuid_fn() für EINE NEUE transaction_id pro Call.
     N audited_create!-Calls erzeugen N verschiedene transaction_ids.
     Die Identifikation des "Phase-Open-Blocks" erfolgt über den gemeinsamen
     process-String "repayment-phase.open" + zeitgleichen timestamp-Range.
     KEINE Macro-Variation für shared transaction_id implementieren — D-03 Klarstellung. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deps-Erweiterung + Auto-Fill in open_repayment_phase + Pending-Validation in close_repayment_phase</name>
  <files>genossi_service_impl/src/repayment_phase.rs</files>
  <read_first>
    - genossi_service_impl/src/repayment_phase.rs (komplette Datei lesen — gen_service_impl!-Deklaration, open_repayment_phase, close_repayment_phase, Tests)
    - genossi_service_impl/src/assembly.rs (Z. 181-258 für Auto-Fill-Pattern; Z. 261-291 für Status-Guard-Pattern in close_assembly)
    - genossi_service_impl/src/audit_macros.rs (audited_create!-Signatur 6-Arg)
    - genossi_service_impl/src/audit_log.rs (Z. 60-70 — bestätigt: transaction_id pro Call neu)
    - genossi_dao/src/repayment_entry.rs (aus Plan 01 — RepaymentEntryDao + Entity + Status)
    - genossi_dao/src/member.rs (MemberEntity-Felder: id, member_number, current_shares, exit_date, deleted)
    - genossi_dao/src/repayment_phase.rs (RepaymentPhaseStatus für Status-Guard)
    - .planning/phases/08-repaymententry-auto-bef-llung/08-CONTEXT.md (D-01, D-02, D-03 inkl. Klarstellung, D-04, D-13, D-14, D-15, PHAS-02, PHAS-03, ENTR-01)
    - .planning/phases/08-repaymententry-auto-bef-llung/08-PATTERNS.md §6 (Auto-Fill + Close-Validation Pattern)
  </read_first>
  <behavior>
    - Test 1: open_phase mit 0 Members → 0 entries erzeugt, phase ist Open (D-14 0-Entry-Case erlaubt)
    - Test 2: open_phase mit 3 Members alle im FY exit_date, alle current_shares > 0 → 3 audited_create! Aufrufe in 1 Tx
    - Test 3: open_phase ignoriert Members mit current_shares == 0 (D-02)
    - Test 4: open_phase ignoriert Members mit exit_date OUTSIDE fiscal_year (D-01)
    - Test 5: open_phase ignoriert Members mit exit_date == None (D-01: BETWEEN-Filter)
    - Test 6: open_phase ruft audited_create N-mal auf für Entries mit RepaymentEntryEntity (predicate-Match auf entity_type == "repayment_entry") — Reihenfolge per member_number ASC sortiert in der `targets`-Vec, Mockall-Sequence-Match NICHT erforderlich (transaction_id ist pro Call neu — siehe D-03 Klarstellung)
    - Test 7: open_phase ist atomar: DAO-create-Fail im 2. Entry → Tx rollt zurück, phase bleibt Preparation
    - Test 8: open_phase rejected wenn phase.status != Preparation → Conflict (bestehendes Verhalten unverändert)
    - Test 9: close_phase mit 0 entries succeeds (D-14)
    - Test 10: close_phase mit nur PaidOut + soft-deleted entries succeeds (D-13)
    - Test 11: close_phase mit 1 Open entry → Conflict, error message enthält "pending" + Mitgliedsnummer (D-15)
    - Test 12: close_phase mit 25 pending entries → Conflict, pending_count==25, member-numbers-Liste hat genau 20 + "+5 weitere" (D-15)
    - Test 13: close_phase rejected wenn phase.status != Open → Conflict (bestehendes Verhalten unverändert)
  </behavior>
  <action>
    **Schritt 1 — Imports erweitern** in `genossi_service_impl/src/repayment_phase.rs`:

    Falls noch nicht vorhanden, ergänze:
    ```rust
    use genossi_dao::member::MemberDao;
    use genossi_dao::repayment_entry::{RepaymentEntryDao, RepaymentEntryEntity, RepaymentEntryStatus};
    use std::collections::HashMap;
    ```

    **Schritt 2 — gen_service_impl!-Deps erweitern**:

    Bestehender Block:
    ```rust
    gen_service_impl! {
        struct RepaymentPhaseServiceImpl: RepaymentPhaseService = RepaymentPhaseServiceDeps {
            RepaymentPhaseDao: RepaymentPhaseDao<Transaction = Self::Transaction> = repayment_phase_dao,
            AuditLogDao: AuditLogDao<Transaction = Self::Transaction> = audit_log_dao,
            PermissionService: PermissionService<Context = Self::Context> = permission_service,
            UuidService: UuidService = uuid_service,
            TransactionDao: TransactionDao<Transaction = Self::Transaction> = transaction_dao,
        }
    }
    ```

    Erweitern auf:
    ```rust
    gen_service_impl! {
        struct RepaymentPhaseServiceImpl: RepaymentPhaseService = RepaymentPhaseServiceDeps {
            RepaymentPhaseDao: RepaymentPhaseDao<Transaction = Self::Transaction> = repayment_phase_dao,
            RepaymentEntryDao: RepaymentEntryDao<Transaction = Self::Transaction> = repayment_entry_dao,
            MemberDao: MemberDao<Transaction = Self::Transaction> = member_dao,
            AuditLogDao: AuditLogDao<Transaction = Self::Transaction> = audit_log_dao,
            PermissionService: PermissionService<Context = Self::Context> = permission_service,
            UuidService: UuidService = uuid_service,
            TransactionDao: TransactionDao<Transaction = Self::Transaction> = transaction_dao,
        }
    }
    ```

    **Schritt 3 — open_repayment_phase erweitern um Auto-Fill-Block**:

    Vor dem `self.transaction_dao.commit(tx).await?;` am Ende der Methode, NACH dem `audited_update!`, folgenden Block einfügen (entferne ggf. die Phase-7-`// TODO PHAS-02 Phase 8`-Markierung):

    ```rust
    // ----- PHAS-02 / ENTR-01 (Phase 8): Auto-Befüllung der RepaymentEntries -----
    // Atomar in derselben Tx wie der Status-Übergang Preparation→Open.
    // Pattern: assembly.rs:181-258 (Single-Tx-Multi-DAO via tx.clone()).
    //
    // UNTERSCHIED zu Assembly-Snapshot: N einzelne audited_create! statt
    // batch_without_audit (D-03) — RepaymentEntries sind Lifecycle-Träger,
    // Phase-9 mark_paid_out hängt an entity_id+version.
    //
    // D-04: Auto-Fill genau einmal beim Phase-Open; keine Re-Fill-Action.
    //
    // D-03 Klarstellung: Jeder audited_create!-Call generiert intern eine EIGENE
    // transaction_id (audit_log.rs:65 uuid_fn() Call). Die N Einträge sind als
    // Folge des Phase-Open-Akts über den gemeinsamen process-String
    // REPAYMENT_PHASE_PROCESS_OPEN + den zeitgleichen timestamp-Range identifizierbar
    // (alle Calls liegen in derselben DB-Commit-Sekunde dank Single-Tx).

    let fiscal_year = entity.fiscal_year;
    let fy_start = time::Date::from_calendar_date(fiscal_year, time::Month::January, 1)
        .map_err(|e| ServiceError::InternalError(Arc::from(format!(
            "invalid fiscal_year start date: {}", e
        ))))?;
    let fy_end = time::Date::from_calendar_date(fiscal_year, time::Month::December, 31)
        .map_err(|e| ServiceError::InternalError(Arc::from(format!(
            "invalid fiscal_year end date: {}", e
        ))))?;

    // D-02: strikter Member-Filter — KEIN is_normal()-Filter
    // (Ausgeschiedene haben Status != Normal, das ist genau die Zielgruppe).
    // member_dao.all() filtert bereits deleted IS NULL per Default-Impl.
    let all_members = self.member_dao.all(tx.clone()).await?;
    let mut targets: Vec<&genossi_dao::member::MemberEntity> = all_members
        .iter()
        .filter(|m| m.exit_date.map_or(false, |d| d >= fy_start && d <= fy_end))
        .filter(|m| m.current_shares > 0)
        .collect();

    // Deterministische Audit-Reihenfolge (CONTEXT Claude's Discretion).
    // Gibt zwar nicht eine einheitliche transaction_id (siehe D-03), aber stabile
    // timestamp-Reihenfolge im Audit-Log für reproduzierbare Tests + Vorstand-Lesbarkeit.
    targets.sort_by_key(|m| m.member_number);

    for member in targets {
        let entry_now_offset = time::OffsetDateTime::now_utc();
        let entry_now_pdt = time::PrimitiveDateTime::new(
            entry_now_offset.date(), entry_now_offset.time()
        );
        let new_entry = RepaymentEntryEntity {
            id: self.uuid_service.new_v4().await,
            member_id: member.id,
            phase_id: id,
            share_count_to_pay_out: member.current_shares,
            status: RepaymentEntryStatus::Open,
            created: entry_now_pdt,
            deleted: None,
            version: self.uuid_service.new_v4().await,
        };
        // Audit-Process = REPAYMENT_PHASE_PROCESS_OPEN (gleicher String wie das
        // audited_update! oben für den Phase-Status-Übergang). Identifikation des
        // Phase-Open-Blocks im Audit-Log: filtere nach
        //   process = 'repayment-phase.open' AND timestamp BETWEEN T AND T+1s
        crate::audited_create!(
            self, self.repayment_entry_dao, &new_entry,
            REPAYMENT_PHASE_PROCESS_OPEN, &user_id, tx
        );
    }
    ```

    > Hinweis: `REPAYMENT_PHASE_PROCESS_OPEN` ist die existierende Phase-7-Konstante (verified per Read auf `repayment_phase.rs:44`). Falls deren Name lokal abweicht, an die existierende Konvention anpassen — wichtig ist die Identität mit dem `audited_update!` darüber.

    **Schritt 4 — close_repayment_phase erweitern um Pending-Validation**:

    Direkt NACH dem bestehenden Status-Guard `if entity.status != RepaymentPhaseStatus::Open` und VOR dem `entity.status = RepaymentPhaseStatus::Closed`-Assignment, folgenden Block einfügen:

    ```rust
    // ----- PHAS-03 (Phase 8): Pending-Entry-Validation -----
    // D-13: "pending" = status != PaidOut AND deleted IS NULL
    // D-14: 0-Entry-Close ist erlaubt (Phase mit 0 Eingängen darf direkt closen)
    // D-15: 409 Body enthält pending_count + bis zu 20 Mitgliedsnummern

    let all_entries = self.repayment_entry_dao.find_by_phase_id(id, tx.clone()).await?;
    let pending: Vec<&RepaymentEntryEntity> = all_entries
        .iter()
        .filter(|e| e.deleted.is_none())
        .filter(|e| e.status != RepaymentEntryStatus::PaidOut)
        .collect();

    if !pending.is_empty() {
        // Mitgliedsnummern-Lookup (Vorstand denkt in Mitgliedsnummern, nicht UUIDs — D-15)
        let all_members = self.member_dao.all(tx.clone()).await?;
        let number_by_id: HashMap<Uuid, i64> = all_members.iter()
            .map(|m| (m.id, m.member_number))
            .collect();

        let mut pending_numbers: Vec<i64> = pending.iter()
            .filter_map(|e| number_by_id.get(&e.member_id).copied())
            .collect();
        pending_numbers.sort();
        let total = pending_numbers.len();

        // D-15: max 20 Mitgliedsnummern + "+N weitere" Suffix
        let mut display_numbers: Vec<String> = pending_numbers.iter()
            .take(20)
            .map(|n| n.to_string())
            .collect();
        if total > 20 {
            display_numbers.push(format!("+{} weitere", total - 20));
        }

        // JSON-encoded Detail im Conflict-Body (REST-Layer kann das in CloseConflictResponse parsen)
        let detail = serde_json::json!({
            "error": format!("Cannot close phase: {} entries are not paid out and not deleted.", total),
            "pending_count": total,
            "pending_member_numbers": display_numbers,
        });

        return Err(ServiceError::Conflict(Arc::from(detail.to_string())));
    }
    // ----- /PHAS-03 -----
    ```

    > Architekt-Hinweis: Phase-7-Konvention erlaubt KEINE neuen `ServiceError`-Varianten. Wir nutzen den pragmatischen JSON-in-Arc-Weg im bestehenden `Conflict(Arc<str>)`. Plan 05 REST-Layer kann das JSON parsen und als strukturierten `CloseConflictResponse`-TO ausliefern oder es als opaken Body-String zurückgeben (Planner-Discretion in Plan 05).

    **Schritt 5 — Test-Mocks erweitern**:

    Die bestehenden Tests in `genossi_service_impl/src/repayment_phase.rs::tests` verwenden hand-rolled Mocks (`TestRepaymentPhaseDao`, `TestPermissionService`, etc.). Ergänze:

    1. **`TestRepaymentEntryDao`-Mock** (analog `TestRepaymentPhaseDao` in derselben Datei oder importiert aus `crate::repayment_entry::tests` — Planner-Discretion):
    ```rust
    mock! {
        pub TestRepaymentEntryDao {}
        #[async_trait]
        impl RepaymentEntryDao for TestRepaymentEntryDao {
            type Transaction = TestTransaction;
            async fn dump_all(&self, tx: TestTransaction) -> Result<Arc<[RepaymentEntryEntity]>, DaoError>;
            async fn create(&self, entity: &RepaymentEntryEntity, process: &str, tx: TestTransaction) -> Result<(), DaoError>;
            async fn update(&self, entity: &RepaymentEntryEntity, process: &str, tx: TestTransaction) -> Result<(), DaoError>;
            async fn find_by_id(&self, id: Uuid, tx: TestTransaction) -> Result<Option<RepaymentEntryEntity>, DaoError>;
            async fn find_by_phase_id(&self, phase_id: Uuid, tx: TestTransaction) -> Result<Arc<[RepaymentEntryEntity]>, DaoError>;
        }
    }
    ```

    2. **`TestMemberDao`-Mock** falls noch nicht vorhanden (analog mit MemberEntity).

    3. **`TestDeps`-Trait-Impl + `build_service`-Helper** erweitern um die neuen Felder `repayment_entry_dao` und `member_dao`.

    **Schritt 6 — Test-Cases ergänzen** (mind. 8 neue Tests im bestehenden `mod tests`):

    Test-Liste (Phase-8-spezifisch, in `repayment_phase.rs::tests`):
    - `test_open_phase_auto_fill_zero_members` — `member_dao.all` returns empty → 0 audited_create-Calls, phase status == Open (D-14)
    - `test_open_phase_auto_fill_creates_entries_for_matching_members` — 3 Members, alle exit_date in FY + current_shares > 0 → 3 audited_create-Calls
    - `test_open_phase_auto_fill_skips_members_with_zero_shares` — 1 Member current_shares=0 → 0 audited_create
    - `test_open_phase_auto_fill_skips_members_outside_fiscal_year` — 1 Member exit_date im Folgejahr → 0 audited_create
    - `test_open_phase_auto_fill_skips_members_without_exit_date` — 1 Member exit_date=None → 0 audited_create
    - `test_open_phase_auto_fill_creates_entries_for_correct_entity_type` — **REVISION W-06**: KEIN Mockall-Sequence-Match auf transaction_id (jeder Call hat eigene), STATTDESSEN predicate-Match auf entity_type:
      ```rust
      audit_log_dao
          .expect_create_entries()
          .with(
              predicate::function(|entries: &[AuditLogEntry]| {
                  // Verify entries are for repayment_entry (auto-fill block)
                  entries.iter().all(|e| &*e.entity_type == "repayment_entry")
                      && entries.iter().all(|e| &*e.process == "repayment-phase.open")
              }),
              predicate::always(),
          )
          .times(N_calls)  // N audited_create-Calls = N create_entries-Calls (jeder mit eigener transaction_id)
          .returning(|_, _| Ok(()));
      ```
      Wenn die spezifische Reihenfolge der `member_id`s im Audit-Log getestet werden soll, capture die `entries`-Argumente via `mockall::Sequence` über die N Calls + Assert post-hoc auf die Reihenfolge der `member_id`s in der `targets`-Vec.
    - `test_close_phase_with_zero_entries_succeeds` — find_by_phase_id returns empty → close succeeds (D-14)
    - `test_close_phase_with_only_paid_out_or_deleted_succeeds` — alle entries entweder PaidOut oder deleted → close succeeds
    - `test_close_phase_with_pending_entries_returns_conflict` — 1 Open + 1 Contacted entry → Conflict, Body enthält "pending_count":2 + Mitgliedsnummern-Liste

    > Hinweis: Tests müssen die bestehenden Phase-7-Tests NICHT brechen — alle Pre-Phase-7-Tests bleiben grün. Falls die Phase-7-Tests `build_service` ohne `repayment_entry_dao` / `member_dao` aufrufen, müssen sie um diese Mock-Felder erweitert werden (typisch via `quiet_*_dao()`-Helper, die alle Methoden no-op stellen — analog `make_*_dao_quiet` Pattern in Phase 7).
  </action>
  <verify>
    <automated>cargo build -p genossi_service_impl 2>&1 | tail -10 && cargo test -p genossi_service_impl repayment_phase 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RepaymentEntryDao: RepaymentEntryDao" genossi_service_impl/src/repayment_phase.rs` == 1 (gen_service_impl!-Deps-Erweiterung)
    - `grep -c "MemberDao: MemberDao" genossi_service_impl/src/repayment_phase.rs` >= 1
    - `grep -c "fy_start" genossi_service_impl/src/repayment_phase.rs` >= 1 (Auto-Fill-Block existiert)
    - `grep -c "from_calendar_date(fiscal_year" genossi_service_impl/src/repayment_phase.rs` >= 2
    - `grep -c "current_shares > 0" genossi_service_impl/src/repayment_phase.rs` >= 1 (D-02 filter)
    - `grep -c "sort_by_key" genossi_service_impl/src/repayment_phase.rs` >= 1 (D-CONTEXT Discretion: deterministische Reihenfolge)
    - `grep -c "find_by_phase_id" genossi_service_impl/src/repayment_phase.rs` >= 1 (Close-Validation nutzt es)
    - `grep -c "pending_member_numbers" genossi_service_impl/src/repayment_phase.rs` >= 1 (D-15)
    - `grep -c "weitere" genossi_service_impl/src/repayment_phase.rs` >= 1 (D-15 +N-weitere Suffix)
    - `grep -c "audited_create!" genossi_service_impl/src/repayment_phase.rs` >= 2 (Phase-7-create_repayment_phase + Phase-8-Auto-Fill)
    - `cargo build -p genossi_service_impl` exit 0
    - `cargo test -p genossi_service_impl repayment_phase` exit 0 mit mind. (Phase-7-Bestand + 8 neue Tests) grün
    - KEIN Phase-7-Test ist roter (Bestand bleibt erhalten)
  </acceptance_criteria>
  <done>Auto-Fill in open_phase und Pending-Validation in close_phase implementiert; 8+ neue Tests grün; Phase-7-Tests bleiben grün; Deps korrekt erweitert; predicate-Match auf entity_type statt sequence-Match auf transaction_id (D-03 Klarstellung).</done>
</task>

</tasks>

<verification>
- `cargo build -p genossi_service_impl` clean
- `cargo test -p genossi_service_impl repayment_phase` mind. (Phase-7-Tests + 8 neue) grün
- Auto-Fill nutzt N audited_create! statt batch_without_audit (D-03)
- Close blockt bei pending mit JSON-Body
- 0-Entry-Close erlaubt (D-14)
- Audit-Test verifiziert entity_type + process-String (NICHT transaction_id-Gruppierung — D-03 Klarstellung)
</verification>

<success_criteria>
- PHAS-02 vollständig: Phase-Open befüllt automatisch alle exit_date-Members im FY mit current_shares > 0
- PHAS-03 vollständig: Phase-Close blockt mit Mitgliedsnummern-Detail-Body
- Single-Transaction-Atomicity: Auto-Fill + Status-Update sind atomar
- D-01..D-04, D-13..D-15 alle implementiert (D-03 inkl. Klarstellung über process+timestamp-Identifikation)
</success_criteria>

<output>
After completion, create `.planning/phases/08-repaymententry-auto-bef-llung/08-04-SUMMARY.md`
</output>
