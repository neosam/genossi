---
phase: 08-repaymententry-auto-bef-llung
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - genossi_service_impl/src/repayment_phase.rs
autonomous: true
gap_closure: true
requirements: [PHAS-02, PHAS-03]
must_haves:
  truths:
    - "RepaymentPhaseServiceImpl::create_repayment_phase liest nach audited_create! die persistierte Entity erneut und returnt die aktuelle version-UUID (CR-01 Fix — Phase-7-Erbe)"
    - "RepaymentPhaseServiceImpl::update_repayment_phase liest nach audited_update! die persistierte Entity erneut (CR-01 Fix)"
    - "RepaymentPhaseServiceImpl::open_repayment_phase liest nach audited_update! die Phase-Entity erneut UND vor commit (Re-Read VOR der Auto-Fill-Schleife oder NACH; Plan: NACH der Auto-Fill-Schleife direkt vor commit, damit die zurückgegebene version die endgültig persistierte ist) (CR-01 Fix)"
    - "RepaymentPhaseServiceImpl::close_repayment_phase liest nach audited_update! die Phase-Entity erneut (CR-01 Fix)"
    - "Re-Read-Pattern ist 1:1 das MemberServiceImpl-Pattern aus member.rs:343-348 (find_by_id + ok_or(EntityNotFound))"
    - "Auto-Fill-Block in open_repayment_phase bleibt funktional UNVERÄNDERT (N audited_create!-Calls vor dem Re-Read der Phase)"
    - "Bestehende 23 RepaymentPhase-Unit-Tests bleiben grün (Mock-Erwartungen angepasst)"
    - "2 neue Unit-Tests verifizieren das Re-Read-Verhalten für (a) update_repayment_phase und (b) open_repayment_phase"
  artifacts:
    - path: "genossi_service_impl/src/repayment_phase.rs"
      provides: "Re-Read-Korrektur in 4 Lifecycle-Methoden + Mock-Erwartungen aktualisiert + 2 neue Tests"
      contains: "Re-read to get the new version UUID generated by the DAO"
  key_links:
    - from: "update_repayment_phase / open_repayment_phase / close_repayment_phase"
      to: "self.repayment_phase_dao.find_by_id(id, tx.clone())"
      via: "Re-Read NACH audited_update!, VOR commit"
      pattern: "find_by_id\\(id, tx\\.clone\\(\\)\\)\\.await\\?\\.ok_or\\(ServiceError::EntityNotFound"
    - from: "create_repayment_phase"
      to: "self.repayment_phase_dao.find_by_id(entity.id, tx.clone())"
      via: "Re-Read NACH audited_create!, VOR commit"
      pattern: "find_by_id\\(entity\\.id, tx\\.clone\\(\\)\\)|find_by_id\\(.*\\.id, tx\\.clone\\(\\)\\)"
threat_model:
  threats:
    - id: T-08-08-01
      description: "Re-Read in open_repayment_phase liest nach Auto-Fill-Failure inkonsistenten Stand (Tampering durch Tx-Rollback-Mismatch)"
      asvs_ref: V11.1.4
      severity: low
      mitigation: "Re-Read läuft INNERHALB derselben Tx wie audited_update! + Auto-Fill-Loop. Wenn Auto-Fill failt, droppt die ganze Tx vor dem Re-Read (`?`-early-return). Re-Read wird nur erreicht wenn alle audited_*-Calls grün waren — DB-Snapshot konsistent."
    - id: T-08-08-02
      description: "Fix bricht versehentlich Auto-Fill-Atomarität (Re-Read commitiert vor Auto-Fill) (Integrity)"
      asvs_ref: V11.1.4
      severity: medium
      mitigation: "Re-Read MUSS NACH dem Auto-Fill-Block, vor dem `transaction_dao.commit(tx)` stehen. NICHT zwischen audited_update! des Phase-Status und Auto-Fill-Loop. Acceptance: grep prüft dass Re-Read direkt vor commit kommt UND Auto-Fill-Block weiterhin innerhalb der Tx ist."
---

<objective>
Phase 8 Gap-Closure Plan 08 (Wave 1): Fix CR-01 (stale version response) in `RepaymentPhaseServiceImpl` — die vier Lifecycle-Methoden `create_repayment_phase` (Z. 140), `update_repayment_phase` (Z. 221), `open_repayment_phase` (Z. 355) und `close_repayment_phase` (Z. 459) lesen nach `audited_create!` / `audited_update!` die Entity neu, um die vom DAO frisch generierte version-UUID an den Client zurückzuliefern.

Purpose: Phase-7-erbte Bug-Klasse beseitigen. Phase-7-Test `test_repayment_phase_lifecycle_audit_chain_intact` (e2e_tests.rs:10592) hatte den Bug bereits dokumentiert ("NOTE on version semantics: the codebase-wide convention is that the Service returns the pre-update entity") und mit einem zusätzlichen GET umgangen — Plan 08 fixt die Wurzelursache, damit Clients nicht mehr workarounds bauen müssen.

Output: 1 Datei modifiziert (`genossi_service_impl/src/repayment_phase.rs`); 4 Methoden-Bodies um je einen Re-Read-Block erweitert (1x create, 3x lifecycle); bestehende Test-Mocks an zusätzliche find_by_id-Calls angepasst; 2 neue Tests die das Re-Read-Verhalten verifizieren.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-repaymententry-auto-bef-llung/08-VERIFICATION.md
@.planning/phases/08-repaymententry-auto-bef-llung/08-REVIEW.md
@.planning/phases/08-repaymententry-auto-bef-llung/08-CONTEXT.md
@genossi_service_impl/src/repayment_phase.rs
@genossi_service_impl/src/member.rs

<interfaces>
<!-- Re-Read-Pattern Vorlage aus member.rs:343-348 (KANONISCH — wörtlich übernehmen) -->

```rust
// genossi_service_impl/src/member.rs:343-348
// Re-read to get the new version UUID generated by the DAO
let updated = self
    .member_dao
    .find_by_id(item.id, tx.clone())
    .await?
    .ok_or(ServiceError::EntityNotFound(item.id))?;

self.transaction_dao.commit(tx).await?;
Ok(Member::from(&updated))
```

<!-- Current bug location 1: create_repayment_phase (repayment_phase.rs:130-141) -->

```rust
crate::audited_create!(
    self,
    self.repayment_phase_dao,
    &entity,
    REPAYMENT_PHASE_PROCESS_CREATE,
    &user_id,
    tx
);

self.transaction_dao.commit(tx).await?;
Ok(RepaymentPhase::from(&entity))  // <-- BUG: entity.version is from new_v4() vor DAO-Insert,
                                    // aber im allgemeinen Lifecycle-Kontext bleibt der DAO frei,
                                    // die persistierte Row zu verändern; konsistent mit MemberServiceImpl
                                    // werden wir nach Persistenz neu lesen.
```

<!-- Current bug location 2: update_repayment_phase (repayment_phase.rs:210-222) -->

```rust
crate::audited_update!(
    self,
    self.repayment_phase_dao,
    id,
    &entity,
    REPAYMENT_PHASE_PROCESS_UPDATE,
    &user_id,
    tx
);

self.transaction_dao.commit(tx).await?;
Ok(RepaymentPhase::from(&entity))  // <-- BUG: pre-update version
```

<!-- Current bug location 3: open_repayment_phase (repayment_phase.rs:266-356) -->

```rust
crate::audited_update!(self, self.repayment_phase_dao, id, &entity, REPAYMENT_PHASE_PROCESS_OPEN, &user_id, tx);

// ... Auto-Fill loop: N audited_create! Calls für repayment_entry_dao ...
// (Auto-Fill bleibt unverändert)

self.transaction_dao.commit(tx).await?;
Ok(RepaymentPhase::from(&entity))  // <-- BUG: pre-update version
```

<!-- Current bug location 4: close_repayment_phase (repayment_phase.rs:447-460) -->

```rust
crate::audited_update!(self, self.repayment_phase_dao, id, &entity, REPAYMENT_PHASE_PROCESS_CLOSE, &user_id, tx);

self.transaction_dao.commit(tx).await?;
Ok(RepaymentPhase::from(&entity))  // <-- BUG: pre-update version
```

<!-- DAO-Trait Signatur (für Mock-Erwartung) -->

```rust
// genossi_dao/src/repayment_phase.rs (Default-Impl)
async fn find_by_id(
    &self,
    id: Uuid,
    tx: Option<Self::Transaction>,
) -> Result<Option<RepaymentPhaseEntity>, DaoError>;
```

<!-- Bestätigung: Phase-7-E2E-Test dokumentiert den Bug bereits in einem Inline-Comment
     (e2e_tests.rs:10668-10675); dieser Plan fixt die Wurzelursache, der Test-Comment
     sollte in einer Follow-up-Iteration aufgeräumt werden — NICHT Scope von Plan 08. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-Read-Pattern in allen 4 RepaymentPhase-Lifecycle-Methoden implementieren</name>
  <files>genossi_service_impl/src/repayment_phase.rs</files>
  <read_first>
    - genossi_service_impl/src/repayment_phase.rs (komplette Datei; insbesondere Z. 96-141 create, Z. 143-222 update, Z. 224-356 open inkl. Auto-Fill-Block, Z. 358-460 close, sowie der Test-Modul am Datei-Ende ab Z. 544 für Mock-Pattern)
    - genossi_service_impl/src/member.rs:320-352 (KANONISCHE Re-Read-Vorlage — wörtlich kopieren)
    - .planning/phases/08-repaymententry-auto-bef-llung/08-REVIEW.md §CR-01 (komplette Begründung + Fix-Beispiel)
    - .planning/phases/08-repaymententry-auto-bef-llung/08-VERIFICATION.md (gaps-Block für CR-01)
    - genossi_bin/tests/e2e_tests.rs:10668-10685 (existierender Workaround-Kommentar im Phase-7-Lifecycle-Test — dokumentiert genau diesen Bug; Plan 08 fixt die Wurzelursache)
  </read_first>
  <behavior>
    Test 1 (NEU): `test_update_repayment_phase_rereads_after_audited_update_returns_new_version` — Mock liefert beim ersten find_by_id pre-update Entity mit version=A; nach audited_update! ein zweites find_by_id liefert version=B. Erwartung: Service-Result.version == B (nicht A). Analog zum 08-07-Plan-Test, aber für Phase-Entity.

    Test 2 (NEU): `test_open_repayment_phase_rereads_phase_entity_after_audited_update_returns_new_version` — Mock pre-update find_by_id liefert Preparation-Entity mit version=A; nach audited_update! (und nach Auto-Fill-Loop mit 0 Members für Simplicity) liefert ein weiteres find_by_id eine Open-Entity mit version=B. Member-DAO-Mock liefert leere all()-Liste (keine Auto-Fill-Iterationen). Erwartung: result.version == B && result.status == Open.

    Bestehende Tests in `repayment_phase.rs::tests` (insbesondere `test_create_*`, `test_update_*`, `test_open_*`, `test_close_*`) müssen ihre Mock-`expect_find_by_id()`-Erwartungen anpassen: pro audited-Call jetzt ein zusätzlicher find_by_id für den Re-Read.
  </behavior>
  <action>
    **Schritt 1 — Fix `create_repayment_phase` (Z. 96-141):**

    Ersetze den `audited_create!`-Block + commit + Return (Z. 130-141) durch:

    ```rust
            crate::audited_create!(
                self,
                self.repayment_phase_dao,
                &entity,
                REPAYMENT_PHASE_PROCESS_CREATE,
                &user_id,
                tx
            );

            // CR-01 Fix (Phase-7-Erbe): Re-read to get the persisted version UUID.
            // Pattern mirrors MemberServiceImpl::update (member.rs:343-348).
            // The DAO may write a fresh version internally; without this re-read the
            // Service returns the locally-constructed entity, leaving the client with
            // a version that may not match the persisted row.
            let refreshed = self
                .repayment_phase_dao
                .find_by_id(entity.id, tx.clone())
                .await?
                .ok_or(ServiceError::EntityNotFound(entity.id))?;

            self.transaction_dao.commit(tx).await?;
            Ok(RepaymentPhase::from(&refreshed))
        }
    ```

    **Schritt 2 — Fix `update_repayment_phase` (Z. 210-222):**

    Ersetze den Block:
    ```rust
            crate::audited_update!(
                self,
                self.repayment_phase_dao,
                id,
                &entity,
                REPAYMENT_PHASE_PROCESS_UPDATE,
                &user_id,
                tx
            );

            // CR-01 Fix: Re-read to get the new version UUID generated by the DAO
            // (pattern: member.rs:343-348).
            let refreshed = self
                .repayment_phase_dao
                .find_by_id(id, tx.clone())
                .await?
                .ok_or(ServiceError::EntityNotFound(id))?;

            self.transaction_dao.commit(tx).await?;
            Ok(RepaymentPhase::from(&refreshed))
        }
    ```

    **Schritt 3 — Fix `open_repayment_phase` (Z. 224-356):**

    > WICHTIG: Auto-Fill-Block MUSS UNVERÄNDERT zwischen `audited_update!` und Re-Read bleiben (innerhalb derselben Tx).

    Direkt nach der `// ----- /PHAS-02 -----`-Markierung (Z. 352), VOR `self.transaction_dao.commit(tx).await?;` (Z. 354), den Re-Read einfügen:

    ```rust
            // ----- /PHAS-02 -----

            // CR-01 Fix: Re-read the Phase entity to get the version UUID written
            // by the audited_update! on the Phase row above. The Auto-Fill loop
            // operates on a different aggregate (repayment_entry_dao) and does
            // NOT touch the Phase row, but we read the Phase row again to stay
            // pattern-consistent with member.rs:343-348 and to be robust against
            // any future DAO-level Phase-row mutation in the auto-fill block.
            let refreshed = self
                .repayment_phase_dao
                .find_by_id(id, tx.clone())
                .await?
                .ok_or(ServiceError::EntityNotFound(id))?;

            self.transaction_dao.commit(tx).await?;
            Ok(RepaymentPhase::from(&refreshed))
        }
    ```

    > Hinweis: Re-Read NACH der Auto-Fill-Loop (nicht zwischen audited_update und Auto-Fill) — so wird die endgültig persistierte Phase-Row zurückgegeben und das Pattern bleibt einheitlich.

    **Schritt 4 — Fix `close_repayment_phase` (Z. 447-460):**

    Ersetze den Block:
    ```rust
            crate::audited_update!(
                self,
                self.repayment_phase_dao,
                id,
                &entity,
                REPAYMENT_PHASE_PROCESS_CLOSE,
                &user_id,
                tx
            );

            // CR-01 Fix: Re-read to get the new version UUID generated by the DAO
            // (pattern: member.rs:343-348).
            let refreshed = self
                .repayment_phase_dao
                .find_by_id(id, tx.clone())
                .await?
                .ok_or(ServiceError::EntityNotFound(id))?;

            self.transaction_dao.commit(tx).await?;
            Ok(RepaymentPhase::from(&refreshed))
        }
    ```

    **Schritt 5 — Bestehende Tests in `repayment_phase.rs::tests` an zusätzliche find_by_id-Calls anpassen:**

    Suche alle Tests die `expect_find_by_id()` für `MockRepaymentPhaseDao` setzen:
    - Tests die `create_repayment_phase` aufrufen — pro create JETZT ein zusätzlicher find_by_id für den Re-Read
    - Tests die `update_repayment_phase` aufrufen — pro update JETZT ein zusätzlicher find_by_id
    - Tests die `open_repayment_phase` aufrufen — pro open JETZT ein zusätzlicher find_by_id (auf Phase-Entity)
    - Tests die `close_repayment_phase` aufrufen — pro close JETZT ein zusätzlicher find_by_id

    Pattern: wo bestehende Tests `.times(N)` setzen, erhöhe auf `.times(N+1)` ODER erweitere `in_sequence`-Mock-Setups um den zusätzlichen Refresh-Call.

    Beispiel für `test_update_repayment_phase_succeeds`:
    ```rust
    // VORHER:
    mock_phase_dao
        .expect_find_by_id()
        .times(1)
        .returning(...);

    // NACHHER:
    mock_phase_dao
        .expect_find_by_id()
        .times(2)  // 1x pre-update für Edit-Matrix, 1x Re-Read post-update
        .returning(...);
    ```

    Für Tests die EXPLIZIT version-Wechsel prüfen wollen, verwende `mockall::Sequence` und gib im zweiten find_by_id-Call eine Entity mit anderer version zurück.

    **Schritt 6 — 2 neue Tests anhängen am Datei-Ende des `mod tests`:**

    Test 1 (update Re-Read):
    ```rust
    /// Phase 08 Gap-Closure CR-01 — verifiziert dass update_repayment_phase
    /// nach audited_update! die Phase neu liest und die neue version-UUID
    /// zurückgibt. Vorlage: member.rs:343-348.
    #[tokio::test]
    async fn test_update_repayment_phase_rereads_after_audited_update_returns_new_version() {
        let phase_id = Uuid::new_v4();
        let version_a = Uuid::new_v4();
        let version_b = Uuid::new_v4();

        let pre = RepaymentPhaseEntity {
            id: phase_id,
            fiscal_year: 2026,
            share_value: 12000,
            status: RepaymentPhaseStatus::Preparation,
            opened_at: None,
            closed_at: None,
            created: sample_pdt(),  // bestehender Test-Helper bzw. inline gleich wie andere Tests
            deleted: None,
            version: version_a,
        };
        let post = RepaymentPhaseEntity {
            share_value: 13000,
            version: version_b,
            ..pre.clone()
        };

        let mut mock_phase_dao = MockRepaymentPhaseDao::new();
        let mut seq = mockall::Sequence::new();
        // pre-update load (Edit-Matrix + version check)
        mock_phase_dao.expect_find_by_id().times(1).in_sequence(&mut seq)
            .returning(move |_, _| Ok(Some(pre.clone())));
        // audit_macros internal load
        mock_phase_dao.expect_find_by_id().times(1).in_sequence(&mut seq)
            .returning(move |_, _| Ok(Some(pre.clone())));
        mock_phase_dao.expect_update().times(1).in_sequence(&mut seq)
            .returning(|_, _| Ok(()));
        // CR-01 Re-Read — returns version_b
        mock_phase_dao.expect_find_by_id().times(1).in_sequence(&mut seq)
            .returning(move |_, _| Ok(Some(post.clone())));

        // (Andere Deps-Mocks setup wie in test_update_repayment_phase_succeeds.)

        let service = build_test_phase_service(mock_phase_dao, /* andere mocks */);
        let update = RepaymentPhaseUpdate {
            fiscal_year: 2026,
            share_value: 13000,
            version: version_a,
        };
        let result = service
            .update_repayment_phase(phase_id, &update, mock_admin_context())
            .await
            .expect("update should succeed");

        assert_eq!(result.version, version_b, "Re-Read must return new version after update");
        assert_eq!(result.share_value, 13000, "Updated share_value must be reflected");
    }
    ```

    Test 2 (open Re-Read):
    ```rust
    /// Phase 08 Gap-Closure CR-01 — verifiziert dass open_repayment_phase nach
    /// audited_update! + Auto-Fill-Loop die Phase neu liest. Member-Mock liefert
    /// 0 Members (kein Auto-Fill-Tx-Overhead), damit der Test sich auf den
    /// Re-Read fokussiert.
    #[tokio::test]
    async fn test_open_repayment_phase_rereads_phase_entity_returns_new_version() {
        let phase_id = Uuid::new_v4();
        let version_a = Uuid::new_v4();
        let version_b = Uuid::new_v4();

        let pre = RepaymentPhaseEntity {
            id: phase_id,
            fiscal_year: 2026,
            share_value: 12000,
            status: RepaymentPhaseStatus::Preparation,
            opened_at: None,
            closed_at: None,
            created: sample_pdt(),
            deleted: None,
            version: version_a,
        };
        let post = RepaymentPhaseEntity {
            status: RepaymentPhaseStatus::Open,
            opened_at: Some(sample_pdt()),
            version: version_b,
            ..pre.clone()
        };

        let mut mock_phase_dao = MockRepaymentPhaseDao::new();
        let mut seq = mockall::Sequence::new();
        // pre-open load (transition guard)
        mock_phase_dao.expect_find_by_id().times(1).in_sequence(&mut seq)
            .returning(move |_, _| Ok(Some(pre.clone())));
        // audit_macros internal load
        mock_phase_dao.expect_find_by_id().times(1).in_sequence(&mut seq)
            .returning(move |_, _| Ok(Some(pre.clone())));
        mock_phase_dao.expect_update().times(1).in_sequence(&mut seq)
            .returning(|_, _| Ok(()));
        // CR-01 Re-Read (nach Auto-Fill-Loop — Member-Mock liefert leer)
        mock_phase_dao.expect_find_by_id().times(1).in_sequence(&mut seq)
            .returning(move |_, _| Ok(Some(post.clone())));

        // Member-Mock — empty Members → kein Auto-Fill
        let mut mock_member_dao = MockMemberDao::new();
        mock_member_dao.expect_all().times(1)
            .returning(|_| Ok(Arc::from(Vec::<MemberEntity>::new())));

        // RepaymentEntryDao-Mock — keine Calls erwartet (0 Auto-Fill-Iterationen)
        let mock_entry_dao = MockRepaymentEntryDao::new();

        // (Andere Deps-Mocks wie in test_open_repayment_phase_auto_fill_zero_members.)

        let service = build_test_phase_service(mock_phase_dao, mock_member_dao, mock_entry_dao, /* andere mocks */);
        let result = service
            .open_repayment_phase(phase_id, mock_admin_context())
            .await
            .expect("open should succeed");

        assert_eq!(result.version, version_b, "Re-Read must return new version after open");
        assert_eq!(result.status, RepaymentPhaseStatus::Open);
    }
    ```

    > Hinweis: Die exakte Mock-Times-Frequenz für `audit_macros`'s internen find_by_id hängt vom konkreten Macro ab. Passt sich an den existierenden Tests an (z.B. `test_update_repayment_phase_succeeds` zeigt die etablierte Anzahl der find_by_id-Calls).

    > Hinweis: Falls Helper `sample_pdt()`, `build_test_phase_service()`, `mock_admin_context()` nicht existieren, übernehme die in den bestehenden Phase-7-Tests etablierte Inline-Konstruktions-Form.

    **Schritt 7 — Build + Tests grün:**
    - `cargo build -p genossi_service_impl` muss clean kompilieren
    - `cargo test -p genossi_service_impl --lib repayment_phase` muss ALLE Tests grün haben (23 alte + 2 neue = 25)
  </action>
  <verify>
    <automated>cargo build -p genossi_service_impl 2>&1 | tail -5 && cargo test -p genossi_service_impl --lib repayment_phase 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "find_by_id(id, tx.clone()).await?.ok_or(ServiceError::EntityNotFound" genossi_service_impl/src/repayment_phase.rs` >= 4 (1x update, 1x open, 1x close, + die bereits existierenden Pre-Update-Reads; mit `entity.id` für create ist mind. 1 zusätzlich)
    - `grep -c "find_by_id(entity.id, tx.clone())" genossi_service_impl/src/repayment_phase.rs` >= 1 (create Re-Read)
    - `grep -nE "Re-read.*new version UUID|CR-01 Fix" genossi_service_impl/src/repayment_phase.rs | wc -l` >= 4 (Marker-Kommentare für create + update + open + close)
    - `grep -nE "Ok\(RepaymentPhase::from\(&entity\)\)" genossi_service_impl/src/repayment_phase.rs | wc -l` == 0 (alle 4 Methoden returnen jetzt `&refreshed`, nicht `&entity`)
    - `grep -nE "Ok\(RepaymentPhase::from\(&refreshed\)\)" genossi_service_impl/src/repayment_phase.rs | wc -l` >= 4
    - `grep -c "test_update_repayment_phase_rereads_after_audited_update" genossi_service_impl/src/repayment_phase.rs` == 1
    - `grep -c "test_open_repayment_phase_rereads_phase_entity" genossi_service_impl/src/repayment_phase.rs` == 1
    - Auto-Fill-Block bleibt intakt: `grep -nE "REPAYMENT_PHASE_PROCESS_OPEN" genossi_service_impl/src/repayment_phase.rs | wc -l` >= 2 (1x für Phase-Status-Update, 1x für N audited_create!-Calls)
    - Audit-Disziplin bleibt erhalten: `grep -nE "self\.repayment_phase_dao\.(create|update)\(" genossi_service_impl/src/repayment_phase.rs | grep -v '^//' | wc -l` == 0
    - `cargo build -p genossi_service_impl` exit 0
    - `cargo test -p genossi_service_impl --lib repayment_phase` exit 0 (≥ 25 Tests grün, 0 failed)
    - Re-Read-Position-Check: Re-Read in open_repayment_phase MUSS NACH der `// ----- /PHAS-02 -----`-Markierung kommen und VOR `self.transaction_dao.commit(tx)` — verify via `grep -A 20 "^.*/PHAS-02.*-----" genossi_service_impl/src/repayment_phase.rs | grep -nE "find_by_id|transaction_dao\.commit"` zeigt find_by_id VOR commit
  </acceptance_criteria>
  <done>4 Phase-Lifecycle-Methoden returnen post-update-versions; Auto-Fill bleibt funktional; Mock-Erwartungen passen; 2 neue Regressionstests; Service-Impl-Tests komplett grün; Audit-Disziplin-Grep-Gate sauber.</done>
</task>

</tasks>

<verification>
- Re-Read-Pattern korrekt in 4 Phase-Lifecycle-Methoden ergänzt
- Auto-Fill-Block in open_repayment_phase bleibt unangetastet
- Service-Impl-Unit-Tests komplett grün (≥ 25)
- Audit-Disziplin-Grep-Gate bleibt sauber
- Workspace-Build clean
</verification>

<success_criteria>
- CR-01 in RepaymentPhaseServiceImpl gefixt: create/update/open/close returnen aktuelle persistierte version-UUID
- Phase-7-Erbe der Bug-Klasse beseitigt
- Pattern 1:1 das MemberServiceImpl-Pattern (member.rs:343-348)
- Bestehende Phase-7 + Phase-8 Tests bleiben grün
- 2 neue Tests dokumentieren das Verhalten dauerhaft
</success_criteria>

<output>
After completion, create `.planning/phases/08-repaymententry-auto-bef-llung/08-08-SUMMARY.md`
</output>
</content>
</invoke>