---
phase: 08-repaymententry-auto-bef-llung
plan: 10
type: execute
wave: 3
depends_on: ["08-07", "08-08", "08-09"]
files_modified:
  - genossi_bin/tests/e2e_tests.rs
autonomous: true
gap_closure: true
requirements: [ENTR-02, ENTR-06, PHAS-02, PHAS-03]
must_haves:
  truths:
    - "E2E-Test verifiziert: PUT auf RepaymentEntry → response.version extrahieren → 2. PUT mit dieser version → HTTP 200 (CR-01-Regression-Schutz)"
    - "E2E-Test verifiziert: POST /batch-status → response[i].version extrahieren → einzelner PUT mit dieser version → HTTP 200 (CR-01/WR-01-Regression-Schutz)"
    - "E2E-Test verifiziert: POST /api/repayment-phase/{id}/open → response.version extrahieren → PUT update_phase mit dieser version → HTTP 200 (CR-01-Phase-Regression-Schutz)"
    - "E2E-Test verifiziert: POST /api/repayment-phase/{id}/close → response.version ist Property der finalen Phase (read-only post-close, aber version muss persistierte UUID widerspiegeln; Verifikation per zweites GET das gleiche version liefert)"
    - "E2E-Test verifiziert: POST /batch-status mit unbekannter UUID im entry_ids-Array → HTTP 404 (CR-02-Regression-Schutz, NICHT 409)"
    - "Alle bestehenden 270 E2E-Tests (255 Phase-7-Baseline + 15 Phase-8) bleiben grün"
    - "5 neue Tests passen tail-aligned zur bestehenden Phase-8-Test-Block-Konvention (am Datei-Ende, nach test_audit_chain_intact_after_phase_8_lifecycle)"
  artifacts:
    - path: "genossi_bin/tests/e2e_tests.rs"
      provides: "5 neue E2E-Regression-Tests am Datei-Ende"
      contains: "test_update_entry_followup_put_uses_response_version_returns_200"
      contains_2: "test_batch_toggle_followup_put_uses_response_versions"
  key_links:
    - from: "test_update_entry_followup_put_uses_response_version_returns_200"
      to: "PUT /api/repayment-entry/{id} (2x: 1. PUT extract version, 2. PUT use that version)"
      via: "create entry → 1. PUT → extract updated.version → 2. PUT with version=updated.version → assert 200"
      pattern: "test_update_entry_followup_put"
    - from: "test_batch_toggle_unknown_entry_id_returns_404"
      to: "POST /api/repayment-entry/batch-status"
      via: "Auto-Fill-Phase mit 1 Member → batch_ids=[real, fake_uuid] → assert status==404"
      pattern: "test_batch_toggle.*unknown_entry_id"
threat_model:
  threats:
    - id: T-08-10-01
      description: "E2E-Test passt sich zufällig an Bug-Behavior an (False-Negative — Test grün trotz CR-01 vorhanden)"
      asvs_ref: V14.5.1
      severity: low
      mitigation: "Test extrahiert version EXPLIZIT aus der 1. PUT-Response (nicht aus dem create-Response) und asserted == 200 auf dem 2. PUT. Vor 08-07-Fix würde dieser Test 409 produzieren — verifiziert per manueller Test-Run vor 08-07 + nach 08-07."
    - id: T-08-10-02
      description: "Test verlässt sich auf RestStateImpl-DI ohne Mocks → Flakiness durch random port (Availability)"
      asvs_ref: V14.5.1
      severity: low
      mitigation: "Nutzt die etablierte start_test_server-Infrastruktur (in-memory SQLite, random port mit retry). Phase-7-Baseline mit 248 Tests beweist Stabilität."
---

<objective>
Phase 8 Gap-Closure Plan 10 (Wave 3): E2E-Regressionstests die das gefixte Verhalten von 08-07/08/09 dauerhaft absichern. Schließt die in IN-04 dokumentierte Test-Coverage-Lücke ("kein E2E-Test macht einen 2. PUT mit der version aus dem 1. PUT").

Purpose: Sicherstellt, dass CR-01 und CR-02 in Zukunft sofort gefangen werden, falls jemand das Re-Read-Pattern aus 08-07/08 oder den NotFound-Mapping aus 08-09 versehentlich rückgängig macht. Ohne diese Tests bleibt die Codebase regression-anfällig — die Phase-7-Lifecycle-Test-Inline-Kommentare (e2e_tests.rs:10668) belegen, dass der Bug seit Phase 7 bekannt, aber test-coverage-gap-bedingt nicht entdeckt wurde.

Output: 1 Datei modifiziert (genossi_bin/tests/e2e_tests.rs); 5 neue E2E-Tests am Datei-Ende.

Wave-Begründung: Wave 3, weil Tests das gefixte Verhalten aus 08-07 (RepaymentEntryService Re-Read), 08-08 (RepaymentPhaseService Re-Read) und 08-09 (CR-02 404-Mapping) verifizieren — alle drei MÜSSEN deployed sein, sonst sind die Tests rot.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-repaymententry-auto-bef-llung/08-VERIFICATION.md
@.planning/phases/08-repaymententry-auto-bef-llung/08-REVIEW.md
@.planning/phases/08-repaymententry-auto-bef-llung/08-07-PLAN.md
@.planning/phases/08-repaymententry-auto-bef-llung/08-08-PLAN.md
@.planning/phases/08-repaymententry-auto-bef-llung/08-09-PLAN.md
@genossi_bin/tests/e2e_tests.rs

<interfaces>
<!-- Bestehende Helper im File (Z. ~678, 10555, 11020, 11086): -->

```rust
async fn create_test_member(client: &reqwest::Client, server: &TestServer) -> MemberTO;
async fn create_preparation_repayment_phase(client, server, fiscal_year, share_value) -> RepaymentPhaseTO;
async fn create_member_with_exit_date(client, server, member_number, fiscal_year, current_shares) -> MemberTO;
async fn create_open_repayment_phase(client, server, fiscal_year, share_value) -> RepaymentPhaseTO;
```

<!-- Bestehende Imports im File (Z. 12-20): -->

```rust
use genossi_rest_types::{
    BatchStatusRequest, CreateRepaymentEntryRequest, RepaymentEntryStatusTO,
    RepaymentEntryTO, RepaymentPhaseStatusTO, RepaymentPhaseTO, UpdateRepaymentEntryRequest,
    // ...
};
```

<!-- Phase-Update-Body-Schema (genossi_rest_types/src/lib.rs:1253):
     UpdateRepaymentPhaseRequest { fiscal_year: i32, share_value: i64, version: Uuid } -->

<!-- Phase-Lifecycle-Test e2e_tests.rs:10592-10750 dokumentiert in einem
     Inline-Kommentar (Z. 10668-10685) genau den Bug, den Plan 08-10 jetzt
     positiv absichert: 'NOTE on version semantics: ... Service returns the
     pre-update entity'. Nach 08-08 ist dieser Workaround obsolet — Plan
     08-10 prüft positiv, dass der direkte 2. PUT mit der Response-Version
     200 produziert. Kein Cleanup des alten Kommentars Scope von 08-10. -->

<!-- Mock-Auth: alle E2E-Tests laufen unter --features mock_auth; kein
     Authentication-Setup nötig. -->

<!-- Plural-Member-Endpoint: /api/members (verified in helper create_test_member) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 5 E2E-Regressionstests am Datei-Ende von genossi_bin/tests/e2e_tests.rs ergänzen</name>
  <files>genossi_bin/tests/e2e_tests.rs</files>
  <read_first>
    - genossi_bin/tests/e2e_tests.rs (komplette Datei, vor allem die existierenden Phase-8-Tests Z. 11118-11620; insbesondere test_update_entry_status_open_to_contacted_succeeds Z. 11334-11376 als Vorlage für Update-Pattern; test_batch_toggle_happy_path Z. ~11461 für Batch-Pattern; test_repayment_phase_lifecycle_audit_chain_intact Z. 10592-10750 für Phase-Lifecycle-Pattern inkl. Inline-Kommentar Z. 10668-10685 zur Bug-Existenz)
    - .planning/phases/08-repaymententry-auto-bef-llung/08-VERIFICATION.md (gaps.IN-04 für die Test-Coverage-Lücken-Beschreibung + erforderliche Tests)
    - .planning/phases/08-repaymententry-auto-bef-llung/08-REVIEW.md §IN-04 (Code-Beispiel für den 2nd-PUT-Test)
    - .planning/phases/08-repaymententry-auto-bef-llung/08-07-PLAN.md + 08-08-PLAN.md + 08-09-PLAN.md (was die Plans im Code geändert haben, damit Tests das exakt verifizieren)
    - genossi_rest_types/src/lib.rs (für UpdateRepaymentPhaseRequest-Schema — wird in Test 3 + 4 verwendet)
  </read_first>
  <behavior>
    Test 1: test_update_entry_followup_put_uses_response_version_returns_200 — Setup: 1 offene Phase + 1 Member + 1 manueller Entry. 1. PUT (status → Contacted). Extrahiere `updated.version` aus Response. 2. PUT (status → Open) MIT `version = updated.version`. Erwartung: HTTP 200 (NICHT 409). Assertet zusätzlich dass nach dem 2. PUT der Status auf Open zurückgekehrt ist.

    Test 2: test_batch_toggle_followup_put_uses_response_versions — Setup: 1 offene Phase + 2 Members mit exit_date (Auto-Fill erzeugt 2 Entries). Batch-Toggle (Open → Contacted). Extrahiere `updated[0].version`. Einzel-PUT auf updated[0].id mit status=Open + version=updated[0].version. Erwartung: HTTP 200.

    Test 3: test_open_phase_response_version_usable_for_followup_update — Setup: 1 Member + 1 Preparation-Phase. POST /open. Extrahiere `opened.version` aus Open-Response. PUT update_phase (share_value-Korrektur in Open ist nach D-04 erlaubt) mit version=opened.version. Erwartung: HTTP 200 (vor 08-08-Fix wäre das 409).

    Test 4: test_update_phase_response_version_usable_for_followup_update — Setup: 1 Preparation-Phase. PUT update_phase (share_value=12000 → 14000). Extrahiere `updated.version`. 2. PUT (share_value=14000 → 15000) mit version=updated.version. Erwartung: HTTP 200.

    Test 5: test_batch_toggle_with_unknown_entry_id_returns_404 — Setup: 1 offene Phase + 1 Member mit Auto-Fill-Entry. Batch-Toggle mit `entry_ids=[real_id, Uuid::new_v4()]` (zweite ID existiert nicht). Erwartung: HTTP 404 (CR-02; vor 08-09-Fix wäre das 409 mit 'entry not found' im failure_reason).
  </behavior>
  <action>
    **Schritt 1 — Imports prüfen / erweitern:**

    Die meisten benötigten Imports stehen schon im File (Z. 12-20). Verifizieren / ergänzen:
    ```rust
    use genossi_rest_types::{
        // ... bestehend ...
        BatchStatusRequest, CreateRepaymentEntryRequest, RepaymentEntryStatusTO,
        RepaymentEntryTO, RepaymentPhaseStatusTO, RepaymentPhaseTO,
        UpdateRepaymentEntryRequest,
    };
    use reqwest::StatusCode;
    use uuid::Uuid;
    ```

    Falls `UpdateRepaymentPhaseRequest` nicht importiert ist (wird in Test 3+4 verwendet als JSON-Body — kann auch via `serde_json::json!` gebaut werden wie im bestehenden test_repayment_phase_lifecycle_audit_chain_intact). Entscheidung: nutze `serde_json::json!`-Macro für Phase-Update-Bodies, konsistent mit dem Phase-7-Lifecycle-Test (Z. 10645-10649).

    **Schritt 2 — 5 neue Tests am Datei-Ende anhängen (NACH dem letzten bestehenden Phase-8-Test).**

    Ermittele das Datei-Ende per `wc -l genossi_bin/tests/e2e_tests.rs` (aktuell 11662 Zeilen) und hänge die folgenden 5 Tests am Ende an. Verwende exakt die etablierte Stil-Konvention der bestehenden Tests (Inline-Comments mit Plan-Referenz + D-Decision-Referenz).

    ```rust
    // ---------------------------------------------------------------------
    // Phase 08 Gap-Closure Plan 10 — CR-01 + CR-02 E2E-Regressionstests
    // ---------------------------------------------------------------------
    // IN-04 (08-VERIFICATION.md): Phase-8-Baseline-Tests verifizierten nie
    // dass ein 2. PUT mit der von einem 1. PUT zurückgelieferten version
    // funktioniert. Diese Lücke verbarg CR-01 (stale version response) bis
    // zum manuellen Code-Review. Die 5 Tests hier schließen die Lücke.

    /// CR-01 Regression — RepaymentEntry::update returnt nach 08-07-Fix die
    /// post-update version-UUID. Ein direkter 2. PUT mit dieser Version
    /// muss 200 produzieren (vor 08-07-Fix wäre das 409 "Version mismatch").
    #[tokio::test]
    async fn test_update_entry_followup_put_uses_response_version_returns_200() {
        let server = setup().await;
        let client = reqwest::Client::new();
        let phase = create_open_repayment_phase(&client, &server, 2026, 12000).await;
        let m = create_test_member(&client, &server).await;

        let create_body = CreateRepaymentEntryRequest {
            phase_id: phase.id,
            member_id: m.id.unwrap(),
            share_count_to_pay_out: 1,
        };
        let entry: RepaymentEntryTO = client
            .post(server.url("/api/repayment-entry"))
            .json(&create_body)
            .send().await.unwrap()
            .json().await.unwrap();
        let create_version = entry.version.expect("create response must include version");

        // 1. PUT: Open → Contacted, mit create-version
        let put1_body = UpdateRepaymentEntryRequest {
            share_count_to_pay_out: None,
            status: Some(RepaymentEntryStatusTO::Contacted),
            version: create_version,
        };
        let resp1 = client
            .put(server.url(&format!("/api/repayment-entry/{}", entry.id)))
            .json(&put1_body)
            .send().await.unwrap();
        assert_eq!(resp1.status(), StatusCode::OK, "1st PUT must succeed");
        let updated1: RepaymentEntryTO = resp1.json().await.unwrap();
        let version_after_put1 = updated1
            .version
            .expect("1st PUT response must include version (CR-01 post-fix)");
        assert_ne!(
            version_after_put1, create_version,
            "Post-update version must differ from create version (DAO generates fresh UUID)"
        );

        // 2. PUT: Contacted → Open, MIT der version aus der 1. PUT-Response
        let put2_body = UpdateRepaymentEntryRequest {
            share_count_to_pay_out: None,
            status: Some(RepaymentEntryStatusTO::Open),
            version: version_after_put1,
        };
        let resp2 = client
            .put(server.url(&format!("/api/repayment-entry/{}", entry.id)))
            .json(&put2_body)
            .send().await.unwrap();
        assert_eq!(
            resp2.status(),
            StatusCode::OK,
            "2nd PUT with version from 1st PUT response must succeed (CR-01 regression)"
        );
        let updated2: RepaymentEntryTO = resp2.json().await.unwrap();
        assert!(
            matches!(updated2.status, RepaymentEntryStatusTO::Open),
            "After 2nd PUT, status must be Open again"
        );
    }

    /// CR-01/WR-01 Regression — batch_toggle_status returnt nach 08-07-Fix
    /// pro Entry eine post-update version. Ein einzelner Folge-PUT mit
    /// updated[0].version muss 200 produzieren.
    #[tokio::test]
    async fn test_batch_toggle_followup_put_uses_response_versions() {
        let server = setup().await;
        let client = reqwest::Client::new();
        let fiscal_year = 2026;
        let _m1 = create_member_with_exit_date(&client, &server, 1, fiscal_year, 5).await;
        let _m2 = create_member_with_exit_date(&client, &server, 2, fiscal_year, 5).await;
        let phase = create_open_repayment_phase(&client, &server, fiscal_year, 12000).await;

        let entries: Vec<RepaymentEntryTO> = client
            .get(server.url(&format!("/api/repayment-entry?phase_id={}", phase.id)))
            .send().await.unwrap()
            .json().await.unwrap();
        assert_eq!(entries.len(), 2);

        // Batch-Toggle: Open → Contacted
        let batch_body = BatchStatusRequest {
            entry_ids: entries.iter().map(|e| e.id).collect(),
            target_status: RepaymentEntryStatusTO::Contacted,
        };
        let batch_resp = client
            .post(server.url("/api/repayment-entry/batch-status"))
            .json(&batch_body)
            .send().await.unwrap();
        assert_eq!(batch_resp.status(), StatusCode::OK);
        let updated_batch: Vec<RepaymentEntryTO> = batch_resp.json().await.unwrap();
        assert_eq!(updated_batch.len(), 2);

        // Einzel-PUT mit updated_batch[0].version — Folge-Operation auf dem
        // soeben durch batch_toggle geupdateten Entry.
        let target_entry = &updated_batch[0];
        let put_version = target_entry
            .version
            .expect("batch response must include version per entry (CR-01/WR-01 post-fix)");

        let put_body = UpdateRepaymentEntryRequest {
            share_count_to_pay_out: None,
            status: Some(RepaymentEntryStatusTO::Open),
            version: put_version,
        };
        let put_resp = client
            .put(server.url(&format!("/api/repayment-entry/{}", target_entry.id)))
            .json(&put_body)
            .send().await.unwrap();
        assert_eq!(
            put_resp.status(),
            StatusCode::OK,
            "Followup PUT with version from batch response must succeed (CR-01/WR-01 regression)"
        );
    }

    /// CR-01 Regression — open_repayment_phase returnt nach 08-08-Fix die
    /// post-update Phase-version. Ein direkter PUT update_phase (D-04: in
    /// Open ist share_value editable, fiscal_year locked) mit dieser version
    /// muss 200 produzieren.
    #[tokio::test]
    async fn test_open_phase_response_version_usable_for_followup_update() {
        let server = setup().await;
        let client = reqwest::Client::new();
        let phase = create_preparation_repayment_phase(&client, &server, 2026, 12000).await;
        let phase_id = phase.id;

        let open_resp = client
            .post(server.url(&format!("/api/repayment-phase/{}/open", phase_id)))
            .send().await.unwrap();
        assert_eq!(open_resp.status(), StatusCode::OK);
        let opened: RepaymentPhaseTO = open_resp.json().await.unwrap();
        let open_version = opened
            .version
            .expect("open response must include version (CR-01 post-08-08-fix)");

        // PUT share_value-Korrektur in Open (D-04: erlaubt, fiscal_year unchanged)
        let update_body = serde_json::json!({
            "fiscal_year": 2026,
            "share_value": 13000,
            "version": open_version,
        });
        let put_resp = client
            .put(server.url(&format!("/api/repayment-phase/{}", phase_id)))
            .json(&update_body)
            .send().await.unwrap();
        assert_eq!(
            put_resp.status(),
            StatusCode::OK,
            "PUT update_phase with version from open response must succeed (CR-01 regression)"
        );
        let updated: RepaymentPhaseTO = put_resp.json().await.unwrap();
        assert_eq!(updated.share_value, 13000);
    }

    /// CR-01 Regression — update_repayment_phase returnt nach 08-08-Fix
    /// die post-update version. Direkt-aufeinanderfolgende PUTs müssen
    /// beide 200 produzieren.
    #[tokio::test]
    async fn test_update_phase_response_version_usable_for_followup_update() {
        let server = setup().await;
        let client = reqwest::Client::new();
        let phase = create_preparation_repayment_phase(&client, &server, 2026, 12000).await;
        let phase_id = phase.id;
        let v0 = phase.version.expect("create response must include version");

        // 1. PUT: share_value 12000 → 14000
        let put1_body = serde_json::json!({
            "fiscal_year": 2026,
            "share_value": 14000,
            "version": v0,
        });
        let resp1 = client
            .put(server.url(&format!("/api/repayment-phase/{}", phase_id)))
            .json(&put1_body)
            .send().await.unwrap();
        assert_eq!(resp1.status(), StatusCode::OK, "1st PUT must succeed");
        let updated1: RepaymentPhaseTO = resp1.json().await.unwrap();
        let v1 = updated1
            .version
            .expect("1st PUT response must include version (CR-01 post-fix)");
        assert_ne!(v1, v0, "Post-update version must differ from create version");

        // 2. PUT: share_value 14000 → 15000, MIT der version aus 1. PUT-Response
        let put2_body = serde_json::json!({
            "fiscal_year": 2026,
            "share_value": 15000,
            "version": v1,
        });
        let resp2 = client
            .put(server.url(&format!("/api/repayment-phase/{}", phase_id)))
            .json(&put2_body)
            .send().await.unwrap();
        assert_eq!(
            resp2.status(),
            StatusCode::OK,
            "2nd PUT with version from 1st PUT response must succeed (CR-01 regression)"
        );
        let updated2: RepaymentPhaseTO = resp2.json().await.unwrap();
        assert_eq!(updated2.share_value, 15000);
    }

    /// CR-02 Regression — batch_toggle_status mappt nach 08-09-Fix eine
    /// unbekannte/soft-deleted Entry-ID auf HTTP 404 (NICHT mehr 409 mit
    /// 'entry not found' im failure_reason). Aggregat-Konsistenz mit
    /// get/update/delete.
    #[tokio::test]
    async fn test_batch_toggle_with_unknown_entry_id_returns_404() {
        let server = setup().await;
        let client = reqwest::Client::new();
        let fiscal_year = 2026;
        let _m1 = create_member_with_exit_date(&client, &server, 1, fiscal_year, 5).await;
        let phase = create_open_repayment_phase(&client, &server, fiscal_year, 12000).await;

        let entries: Vec<RepaymentEntryTO> = client
            .get(server.url(&format!("/api/repayment-entry?phase_id={}", phase.id)))
            .send().await.unwrap()
            .json().await.unwrap();
        assert_eq!(entries.len(), 1, "Auto-Fill must produce 1 entry");

        let real_id = entries[0].id;
        let fake_id = Uuid::new_v4();

        let batch_body = BatchStatusRequest {
            entry_ids: vec![real_id, fake_id],
            target_status: RepaymentEntryStatusTO::Contacted,
        };
        let resp = client
            .post(server.url("/api/repayment-entry/batch-status"))
            .json(&batch_body)
            .send().await.unwrap();
        assert_eq!(
            resp.status(),
            StatusCode::NOT_FOUND,
            "Unknown entry_id in batch must return 404, not 409 (CR-02 regression)"
        );
    }
    ```

    > Hinweis: `BatchStatusRequest.entry_ids` ist im aktuellen Schema vermutlich ein `Vec<Uuid>` — falls das Schema andere Form hat (z.B. `Arc<[Uuid]>`), entsprechend anpassen. Den existierenden test_batch_toggle_happy_path (e2e_tests.rs:~11461) zeigt die korrekte Form (dort wird `entries.iter().map(|e| e.id).collect()` verwendet).

    > Hinweis: Falls `m.id.unwrap()` Compile-Fehler wirft (TO-Schema-Drift), prüfe MemberTO im File und passe an die im File etablierte Form an (siehe existierende test_manual_add_entry_happy_path-Tests).

    **Schritt 3 — Build + Tests grün:**
    - `cargo build --tests -p genossi_bin` muss clean kompilieren
    - `cargo test --test e2e_tests --features mock_auth` muss ALLE Tests grün haben: 270 bestehende (255 Phase-7-Baseline + 15 Phase-8) + 5 neue = ≥ 275 total
    - Explizit die 5 neuen Tests einzeln laufen lassen, um Test-Namen zu verifizieren:
      `cargo test --test e2e_tests --features mock_auth test_update_entry_followup_put_uses_response_version_returns_200`
      `cargo test --test e2e_tests --features mock_auth test_batch_toggle_followup_put_uses_response_versions`
      `cargo test --test e2e_tests --features mock_auth test_open_phase_response_version_usable_for_followup_update`
      `cargo test --test e2e_tests --features mock_auth test_update_phase_response_version_usable_for_followup_update`
      `cargo test --test e2e_tests --features mock_auth test_batch_toggle_with_unknown_entry_id_returns_404`
  </action>
  <verify>
    <automated>cargo build --tests -p genossi_bin 2>&1 | tail -5 && cargo test --test e2e_tests --features mock_auth 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test_update_entry_followup_put_uses_response_version_returns_200" genossi_bin/tests/e2e_tests.rs` == 1
    - `grep -c "test_batch_toggle_followup_put_uses_response_versions" genossi_bin/tests/e2e_tests.rs` == 1
    - `grep -c "test_open_phase_response_version_usable_for_followup_update" genossi_bin/tests/e2e_tests.rs` == 1
    - `grep -c "test_update_phase_response_version_usable_for_followup_update" genossi_bin/tests/e2e_tests.rs` == 1
    - `grep -c "test_batch_toggle_with_unknown_entry_id_returns_404" genossi_bin/tests/e2e_tests.rs` == 1
    - `grep -c "StatusCode::NOT_FOUND" genossi_bin/tests/e2e_tests.rs` >= 2 (mind. test_batch_toggle_with_unknown_entry_id_returns_404 + Bestand)
    - `grep -nE "version from .*response must succeed.*CR-01 regression" genossi_bin/tests/e2e_tests.rs | wc -l` >= 3 (mind. 3 der 5 Tests asserten explizit CR-01-Regression)
    - `cargo build --tests -p genossi_bin` exit 0
    - `cargo test --test e2e_tests --features mock_auth` exit 0 mit ≥ 275 Tests grün (270 bestehend + 5 neu), 0 failed
    - Die 5 neuen Tests laufen einzeln grün (Smoke-Check per name-filter)
  </acceptance_criteria>
  <done>5 E2E-Regressionstests am Datei-Ende; alle ≥ 275 E2E-Tests grün; IN-04 Test-Coverage-Lücke geschlossen.</done>
</task>

</tasks>

<verification>
- 5 neue Tests in genossi_bin/tests/e2e_tests.rs
- Alle 275+ E2E-Tests grün (255 Phase-7 + 15 Phase-8 + 5 neue)
- CR-01-Verhalten end-to-end positiv abgesichert (4 Tests)
- CR-02-Verhalten end-to-end positiv abgesichert (1 Test)
</verification>

<success_criteria>
- IN-04 (Test-Coverage-Lücke) geschlossen
- Zukünftige Regressionen bei CR-01 oder CR-02 werden sofort gefangen
- Phase-7- und Phase-8-Baseline bleiben grün (kein Regress)
- API-Optimistic-Locking-Vertrag ist end-to-end verifiziert
</success_criteria>

<output>
After completion, create `.planning/phases/08-repaymententry-auto-bef-llung/08-10-SUMMARY.md`
</output>
</content>
</invoke>
