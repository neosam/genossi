---
phase: 08-repaymententry-auto-bef-llung
plan: 09
type: execute
wave: 2
depends_on: ["08-07"]
files_modified:
  - genossi_service_impl/src/repayment_entry.rs
  - genossi_rest/src/repayment_entry.rs
  - genossi_rest_types/src/lib.rs
autonomous: true
gap_closure: true
requirements: [ENTR-06]
must_haves:
  truths:
    - "RepaymentEntryServiceImpl::batch_toggle_status mappt missing/soft-deleted Entry-ID auf ServiceError::EntityNotFound (statt conflict_body 'entry not found')"
    - "Erste fehlende ID in der Batch-Liste rollt die gesamte Tx zurück und führt zu HTTP 404 (Aggregat-Konsistenz mit get/update/delete: alle returnen 404 für denselben Zustand)"
    - "Der Conflict-Body 'failure_reason: entry not found' tritt im neuen Code NICHT mehr auf"
    - "OpenAPI-Dokumentation für POST /api/repayment-entry/batch-status listet status=404 als möglichen Response (NotFound — first failing entry)"
    - "BatchFailureResponse-Schema-Doc-Comment in genossi_rest_types erwähnt dass NotFound-Fall NICHT mehr durch dieses Schema repräsentiert wird (404 = leerer/standard Body, nicht BatchFailureResponse)"
    - "Bestehende Tests aus 08-07 (re-read) und die neue 404-Semantik koexistieren — kein bestehender Test wird gebrochen"
    - "1 neuer Unit-Test verifiziert: nicht-existente Entry-ID im Batch → ServiceError::EntityNotFound (NICHT Conflict mit 'entry not found' Body)"
    - "1 neuer E2E-Test verifiziert: POST /batch-status mit unbekannter UUID im entry_ids-Array → HTTP 404 (NICHT 409)"
  artifacts:
    - path: "genossi_service_impl/src/repayment_entry.rs"
      provides: "batch_toggle_status NotFound-Branch refactored auf ServiceError::EntityNotFound + 1 neuer Unit-Test"
      contains: "ok_or(ServiceError::EntityNotFound(*entry_id))"
    - path: "genossi_rest/src/repayment_entry.rs"
      provides: "OpenAPI-Erweiterung für batch_toggle_status um 404-Response"
      contains: "(status = 404"
    - path: "genossi_rest_types/src/lib.rs"
      provides: "BatchFailureResponse-Schema-Doc um NotFound-Klarstellung erweitert"
      contains: "BatchFailureResponse"
  key_links:
    - from: "batch_toggle_status"
      to: "ServiceError::EntityNotFound → RestError::NotFound → HTTP 404"
      via: "ok_or(ServiceError::EntityNotFound(*entry_id))"
      pattern: "ok_or\\(ServiceError::EntityNotFound\\(\\*entry_id\\)\\)"
    - from: "OpenAPI batch_toggle_status responses"
      to: "404 NotFound entry-Doku"
      via: "#[utoipa::path] responses-Array um (status = 404, ...) erweitert"
      pattern: "status = 404"
threat_model:
  threats:
    - id: T-08-09-01
      description: "Bug-Fix führt zu inkonsistenter Status-Code-Semantik mit anderen Endpoints (Tampering)"
      asvs_ref: V11.1.4
      severity: low
      mitigation: "Fix bringt batch_toggle_status in EINKLANG mit get/update/delete im selben Aggregat — alle returnen 404 für 'entry not found'. Acceptance-Criterion verifiziert per grep dass conflict_body('entry not found') nicht mehr vorkommt."
    - id: T-08-09-02
      description: "OpenAPI-Doku-Inkonsistenz zwischen Service-Behavior und Frontend-Generated-Client (Information Disclosure via stale contract)"
      asvs_ref: V14.5.1
      severity: low
      mitigation: "Plan ergänzt 404 explizit in der utoipa::path-Annotation. ApiDoc Compile-Check stellt sicher dass Schema im OpenAPI-JSON erscheint."
---

<objective>
Phase 8 Gap-Closure Plan 09 (Wave 2): Fix CR-02 (Batch-Toggle 404 vs 409 Semantik) in `RepaymentEntryServiceImpl::batch_toggle_status` (Z. 416) und ergänze OpenAPI-Dokumentation für POST `/api/repayment-entry/batch-status` um den 404-Response.

Purpose: Aggregat-Konsistenz herstellen. Alle anderen Methoden im RepaymentEntry-Aggregat (get/update/delete) returnen 404 für "Entry nicht gefunden". Der Batch-Toggle ist die einzige Methode, die das auf 409 ("entry not found" als Conflict-failure_reason) gemappt hat — ein Frontend kann Stale-ID nicht von Domain-Konflikt unterscheiden. Plus: OpenAPI listet 404 jetzt explizit auf.

Output: 3 Dateien modifiziert: Service-Impl (Mapping-Korrektur + 1 neuer Unit-Test), REST-Handler (OpenAPI-Annotation um 404-Response erweitert), TO-Schema-Doc (Klarstellung in BatchFailureResponse-doc-comment), plus 1 neuer E2E-Test.

Wave-Begründung: Wave 2, weil Plan 08-07 dieselbe Datei `genossi_service_impl/src/repayment_entry.rs` modifiziert (Re-Read-Pattern in batch_toggle_status). 08-09 setzt auf dem 08-07-Code auf und passt die NotFound-Branch im selben Loop-Body an.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-repaymententry-auto-bef-llung/08-VERIFICATION.md
@.planning/phases/08-repaymententry-auto-bef-llung/08-REVIEW.md
@.planning/phases/08-repaymententry-auto-bef-llung/08-07-PLAN.md
@genossi_service_impl/src/repayment_entry.rs
@genossi_rest/src/repayment_entry.rs
@genossi_rest_types/src/lib.rs

<interfaces>
<!-- Current bug location: batch_toggle_status Loop-Anfang (repayment_entry.rs:411-416) -->

```rust
for (idx, entry_id) in input.entry_ids.iter().enumerate() {
    let mut entity = self
        .repayment_entry_dao
        .find_by_id(*entry_id, tx.clone())
        .await?
        .ok_or_else(|| conflict_body(idx, *entry_id, "entry not found"))?;
    // ^^^ BUG: mappt Not-Found auf 409 Conflict statt 404
```

<!-- Erwartete Korrektur (CR-02 Variante a — preferred per REVIEW.md): -->

```rust
for (idx, entry_id) in input.entry_ids.iter().enumerate() {
    let mut entity = self
        .repayment_entry_dao
        .find_by_id(*entry_id, tx.clone())
        .await?
        .ok_or(ServiceError::EntityNotFound(*entry_id))?;
    // (Verwendet `idx` weiterhin durch den Loop-Iterator — Variable bleibt benötigt für
    //  den nachfolgenden Source-Status-Check via conflict_body.)
```

<!-- Hinweis: `idx` wird im nachfolgenden conflict_body-Call (Source-Status-Check) weiter
     verwendet, daher bleibt `enumerate()` und die Variable im Loop. -->

<!-- Globales Error-Mapping (genossi_rest/src/lib.rs:97-113): -->

```rust
impl From<ServiceError> for RestError {
    fn from(err: ServiceError) -> Self {
        match err {
            ServiceError::EntityNotFound(_) => RestError::NotFound,
            ServiceError::Conflict(msg)     => RestError::Conflict(msg.to_string()),
            // ...
        }
    }
}
```
→ `EntityNotFound` → `RestError::NotFound` → HTTP 404. Kein zusätzlicher Code im REST-Layer nötig.

<!-- Current OpenAPI-Annotation für batch_toggle_status (genossi_rest/src/repayment_entry.rs:254-266): -->

```rust
#[instrument(skip(rest_state))]
#[utoipa::path(
    post,
    tag = "RepaymentEntries",
    path = "/batch-status",
    request_body = BatchStatusRequest,
    responses(
        (status = 200, description = "All entries toggled successfully", body = [RepaymentEntryTO]),
        (status = 400, description = "Validation Error (PaidOut as target_status, D-07)"),
        (status = 401, description = "Unauthorized"),
        (status = 409, description = "Conflict — first failing entry rolled back transaction (D-08). Body matches BatchFailureResponse schema.", body = BatchFailureResponse),
    ),
)]
```

<!-- Erwartete Erweiterung: zusätzlicher 404-Response: -->

```rust
        (status = 404, description = "Not Found — at least one entry_id in the batch is missing or soft-deleted. The entire transaction is rolled back (D-08 all-or-nothing). The response body is the standard NotFound payload (NOT BatchFailureResponse).", body = ()),
```

<!-- BatchFailureResponse-Schema (genossi_rest_types/src/lib.rs:~1404-1411): -->

```rust
/// Structured body for HTTP 409 batch-toggle conflicts (D-08).
/// `failure_reason` covers domain-level conflicts (e.g. "source status is 'PaidOut'")
/// but NOT "entry not found" — that case yields HTTP 404 with the standard
/// not-found body, per aggregate convention (see get/update/delete).
#[derive(...)]
pub struct BatchFailureResponse { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-02 Mapping-Fix in batch_toggle_status + Unit-Test</name>
  <files>genossi_service_impl/src/repayment_entry.rs</files>
  <read_first>
    - genossi_service_impl/src/repayment_entry.rs (komplette Datei, vor allem die VON 08-07 BEREITS MODIFIZIERTE batch_toggle_status-Methode — Loop-Body bei Z. ~411 in der aktuellen Datei; aktuell post-08-07)
    - genossi_service_impl/src/member.rs:320-352 (kanonische Service-Error-Mapping-Konvention)
    - .planning/phases/08-repaymententry-auto-bef-llung/08-REVIEW.md §CR-02 (Begründung + Fix-Variante a)
    - .planning/phases/08-repaymententry-auto-bef-llung/08-VERIFICATION.md (gaps-Block für CR-02)
    - .planning/phases/08-repaymententry-auto-bef-llung/08-07-PLAN.md (sehen welche Re-Read-Änderungen 08-07 bereits an batch_toggle_status gemacht hat, um den korrekten Diff darauf aufzusetzen)
  </read_first>
  <behavior>
    Test (NEU): `test_batch_toggle_status_unknown_entry_id_returns_entity_not_found` — Mock-find_by_id liefert Ok(None) für die übergebene UUID. Erwartung: Service returnt Err(ServiceError::EntityNotFound(uuid)) — NICHT Err(ServiceError::Conflict(...)). Assertion via `matches!(result, Err(ServiceError::EntityNotFound(_)))` und `assert!(!matches!(result, Err(ServiceError::Conflict(_))))`.

    Bestehender Test (falls vorhanden) der das alte Verhalten ("entry not found" als Conflict) verifizierte, MUSS umgeschrieben werden auf das neue Verhalten (EntityNotFound), nicht gelöscht — der Coverage-Punkt bleibt erhalten.
  </behavior>
  <action>
    **Schritt 1 — Ersetze den `.ok_or_else(|| conflict_body(idx, *entry_id, "entry not found"))?` Branch:**

    Nach 08-07 hat der Loop-Body folgende Struktur (siehe 08-07-PLAN Schritt 2). Ändere die NotFound-Zeile:

    VORHER (08-07 + alter NotFound-Mapping):
    ```rust
    for (idx, entry_id) in input.entry_ids.iter().enumerate() {
        let mut entity = self
            .repayment_entry_dao
            .find_by_id(*entry_id, tx.clone())
            .await?
            .ok_or_else(|| conflict_body(idx, *entry_id, "entry not found"))?;
        // ...
    ```

    NACHHER (08-09 Fix CR-02):
    ```rust
    for (idx, entry_id) in input.entry_ids.iter().enumerate() {
        // CR-02 Fix: NotFound-Branch mappt auf ServiceError::EntityNotFound (→ HTTP 404),
        // konsistent mit get/update/delete im gleichen Aggregat. Der vorherige
        // conflict_body("entry not found")-Mapping versteckt Stale-ID-Szenarien
        // in einer 409-Antwort und macht es einem Frontend unmöglich, eine
        // UI-Race-Condition (e.g. soft-delete im anderen Tab) von einem
        // echten Domain-Konflikt zu unterscheiden.
        let mut entity = self
            .repayment_entry_dao
            .find_by_id(*entry_id, tx.clone())
            .await?
            .ok_or(ServiceError::EntityNotFound(*entry_id))?;

        // Source-Status-Check (BLEIBT als conflict_body, weil das ein echter
        // Domain-Konflikt ist: Entry existiert, aber Status erlaubt den Toggle
        // nicht — Frontend soll Liste neu laden und es nochmal versuchen).
        if !matches!(
            entity.status,
            RepaymentEntryStatus::Open | RepaymentEntryStatus::Contacted
        ) {
            return Err(conflict_body(
                idx,
                *entry_id,
                &format!(
                    "source status is '{}', expected Open or Contacted",
                    entity.status.as_str()
                ),
            ));
        }
        // ... Rest unverändert (audited_update! + Re-Read aus 08-07)
    }
    ```

    > Hinweis: `idx` wird im `conflict_body`-Call beim Source-Status-Check weiterhin gebraucht — daher bleibt `enumerate()` und die `idx`-Variable im Loop. Nur die NotFound-Branch ist betroffen.

    **Schritt 2 — `conflict_body`-Helper-Doc aktualisieren:**

    Falls der `conflict_body`-Closure (definiert in `batch_toggle_status` bei Z. 371-379) Doc-Kommentare hat, ergänze einen Hinweis:

    ```rust
    // Helper: strukturierter 409-Body (D-08; analog CloseConflictResponse-Pattern).
    // Nutzung NUR für echte Domain-Konflikte (Source-Status nicht Open/Contacted etc.).
    // NotFound-Fälle (Entry existiert nicht / ist soft-deleted) werden seit CR-02-Fix
    // direkt auf ServiceError::EntityNotFound gemappt (→ HTTP 404), nicht auf
    // dieses 409-Schema.
    let conflict_body = |idx: usize, entry_id: Uuid, reason: &str| -> ServiceError {
        // ... unverändert
    };
    ```

    **Schritt 3 — 1 neuer Unit-Test am Datei-Ende des `mod tests`:**

    ```rust
    /// Phase 08 Gap-Closure CR-02 — verifiziert dass batch_toggle_status
    /// eine missing/soft-deleted Entry-ID auf ServiceError::EntityNotFound
    /// mappt (NICHT auf ServiceError::Conflict mit "entry not found"-Body).
    /// Aggregat-Konsistenz mit get/update/delete (alle returnen NotFound).
    #[tokio::test]
    async fn test_batch_toggle_status_unknown_entry_id_returns_entity_not_found() {
        let unknown_id = Uuid::new_v4();
        let mut mock_entry_dao = MockRepaymentEntryDao::new();
        // Mock liefert None → Entry nicht gefunden / soft-deleted
        mock_entry_dao
            .expect_find_by_id()
            .times(1)
            .returning(|_, _| Ok(None));

        // (Andere Deps-Mocks wie in test_batch_toggle_* — Member/Phase/Permission/Tx/Audit)

        let service = build_test_service(mock_entry_dao, /* andere mocks */);
        let input = RepaymentEntryBatchStatusInput {
            entry_ids: Arc::from([unknown_id].as_slice()),
            target_status: RepaymentEntryStatus::Contacted,
        };
        let result = service
            .batch_toggle_status(&input, mock_admin_context())
            .await;

        assert!(
            matches!(result, Err(ServiceError::EntityNotFound(id)) if id == unknown_id),
            "Expected EntityNotFound({}), got {:?}",
            unknown_id,
            result
        );
        assert!(
            !matches!(&result, Err(ServiceError::Conflict(msg)) if msg.contains("entry not found")),
            "Must NOT return Conflict with 'entry not found' reason (CR-02 fixed)"
        );
    }
    ```

    > Hinweis: Falls ein bestehender Test (z.B. `test_batch_toggle_*_not_found_returns_conflict` o.ä.) das alte Verhalten verifizierte, UMSCHREIBEN auf das neue Verhalten (gleiches Setup, andere Assertion), NICHT löschen. Coverage-Punkt bleibt erhalten.

    **Schritt 4 — Build + Tests grün:**
    - `cargo build -p genossi_service_impl` muss clean kompilieren
    - `cargo test -p genossi_service_impl --lib repayment_entry` muss grün haben — alte Tests (post-08-07) + neuer 09-Test
  </action>
  <verify>
    <automated>cargo build -p genossi_service_impl 2>&1 | tail -5 && cargo test -p genossi_service_impl --lib repayment_entry 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ok_or(ServiceError::EntityNotFound(\*entry_id))" genossi_service_impl/src/repayment_entry.rs` >= 1
    - `grep -c 'conflict_body(idx, \*entry_id, "entry not found")' genossi_service_impl/src/repayment_entry.rs` == 0 (alter Code entfernt)
    - `grep -c "CR-02 Fix" genossi_service_impl/src/repayment_entry.rs` >= 1 (Marker-Kommentar)
    - `grep -c "test_batch_toggle_status_unknown_entry_id_returns_entity_not_found" genossi_service_impl/src/repayment_entry.rs` == 1
    - Bestehender Source-Status-Check verwendet weiterhin conflict_body: `grep -c "conflict_body(\s*idx" genossi_service_impl/src/repayment_entry.rs` >= 1
    - `cargo build -p genossi_service_impl` exit 0
    - `cargo test -p genossi_service_impl --lib repayment_entry` exit 0 (alle Tests grün — Post-08-07-Stand + 1 neuer)
    - Re-Read-Pattern aus 08-07 bleibt erhalten: `grep -c "Re-read.*new version UUID|CR-01 Fix" genossi_service_impl/src/repayment_entry.rs` >= 2
  </acceptance_criteria>
  <done>Service-Layer NotFound-Mapping korrekt; Source-Status-Check bleibt 409; Unit-Test verifiziert beides; Service-Impl-Tests komplett grün; Plan-08-07 Re-Read-Code unangetastet.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: OpenAPI-Annotation für POST /batch-status um 404-Response erweitern + BatchFailureResponse-Doc-Comment aktualisieren</name>
  <files>genossi_rest/src/repayment_entry.rs, genossi_rest_types/src/lib.rs</files>
  <read_first>
    - genossi_rest/src/repayment_entry.rs (Z. 254-293 — batch_toggle_status Handler + utoipa::path Annotation; Z. 317-345 ApiDoc-Modul)
    - genossi_rest_types/src/lib.rs (suche `BatchFailureResponse` — typischerweise im Bereich Z. 1380-1430 mit dem Schema-Definition-Block)
    - .planning/phases/08-repaymententry-auto-bef-llung/08-REVIEW.md §CR-02 (begründet warum 404 explizit in OpenAPI braucht)
  </read_first>
  <behavior>
    OpenAPI-JSON soll nach dem Fix für POST /api/repayment-entry/batch-status einen 404-Response auflisten. Frontend-Generated-Clients (utoipa-driven) sollen den NotFound-Fall typgerecht erkennen können.

    BatchFailureResponse-doc-comment soll dokumentieren, dass "entry not found" NICHT mehr durch dieses Schema repräsentiert wird (404 nutzt Standard-NotFound-Body, nicht BatchFailureResponse).
  </behavior>
  <action>
    **Schritt 1 — genossi_rest/src/repayment_entry.rs: utoipa::path-Annotation für batch_toggle_status erweitern.**

    Finde den Block (Z. 254-266):

    ```rust
    #[instrument(skip(rest_state))]
    #[utoipa::path(
        post,
        tag = "RepaymentEntries",
        path = "/batch-status",
        request_body = BatchStatusRequest,
        responses(
            (status = 200, description = "All entries toggled successfully", body = [RepaymentEntryTO]),
            (status = 400, description = "Validation Error (PaidOut as target_status, D-07)"),
            (status = 401, description = "Unauthorized"),
            (status = 409, description = "Conflict — first failing entry rolled back transaction (D-08). Body matches BatchFailureResponse schema.", body = BatchFailureResponse),
        ),
    )]
    ```

    Ersetze durch:

    ```rust
    #[instrument(skip(rest_state))]
    #[utoipa::path(
        post,
        tag = "RepaymentEntries",
        path = "/batch-status",
        request_body = BatchStatusRequest,
        responses(
            (status = 200, description = "All entries toggled successfully", body = [RepaymentEntryTO]),
            (status = 400, description = "Validation Error (PaidOut as target_status, D-07)"),
            (status = 401, description = "Unauthorized"),
            (status = 404, description = "Not Found — at least one entry_id in the batch is missing or soft-deleted. The entire transaction is rolled back per D-08 (all-or-nothing). Aggregate-consistent with get/update/delete on /api/repayment-entry/{id}. The response body is the standard NotFound payload (NOT BatchFailureResponse)."),
            (status = 409, description = "Conflict — first failing entry rolled back transaction (D-08). Body matches BatchFailureResponse schema. Used for domain-level conflicts ONLY (e.g. source status is 'PaidOut'); for missing/soft-deleted entries see 404.", body = BatchFailureResponse),
        ),
    )]
    ```

    > Hinweis: utoipa erlaubt `(status = 404, description = "...")` ohne `body = ...` — der Body ist dann der generische NotFound-Body des Frameworks.

    **Schritt 2 — genossi_rest_types/src/lib.rs: BatchFailureResponse-Doc-Comment erweitern.**

    Finde die Definition (suche per `grep -n "pub struct BatchFailureResponse" genossi_rest_types/src/lib.rs`).

    Ersetze den bestehenden Doc-Kommentar über dem Struct durch:

    ```rust
    /// Structured body for HTTP 409 batch-toggle conflicts (D-08).
    ///
    /// Used by `POST /api/repayment-entry/batch-status` to report the FIRST
    /// failing entry that triggered the all-or-nothing transaction rollback.
    ///
    /// Scope: domain-level conflicts ONLY. Examples:
    ///   - Source status is not Open or Contacted (e.g. PaidOut → Contacted attempt)
    ///   - Version mismatch (concurrent edit elsewhere)
    ///
    /// NOT used for: missing or soft-deleted entry_ids. Those cases yield
    /// HTTP 404 with the standard NotFound payload — aggregate-consistent with
    /// `GET/PUT/DELETE /api/repayment-entry/{id}`. See CR-02 / Phase 08 Plan 09.
    #[derive(...)]
    pub struct BatchFailureResponse {
        // ... unverändert
    }
    ```

    **Schritt 3 — Build + Tests grün:**
    - `cargo build -p genossi_rest -p genossi_rest_types` muss clean kompilieren
    - `cargo test -p genossi_rest --lib repayment_entry` (Smoke-Tests) grün
    - `cargo test -p genossi_rest_types repayment_entry` grün
  </action>
  <verify>
    <automated>cargo build -p genossi_rest -p genossi_rest_types 2>&1 | tail -5 && cargo test -p genossi_rest --lib repayment_entry 2>&1 | tail -10 && cargo test -p genossi_rest_types repayment_entry 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "status = 404" genossi_rest/src/repayment_entry.rs` >= 1 (404 in batch_toggle_status OpenAPI ergänzt; daneben evtl. weitere 404 in anderen Handlern — Zählung berücksichtigt das)
    - `grep -nE "status = 404.*missing or soft-deleted" genossi_rest/src/repayment_entry.rs | wc -l` == 1 (spezifischer 404-Description für batch-status; Marker)
    - `grep -c "NOT BatchFailureResponse" genossi_rest/src/repayment_entry.rs` >= 1 (Klarstellung in OpenAPI-Description)
    - `grep -c "NOT used for: missing or soft-deleted" genossi_rest_types/src/lib.rs` >= 1 (Doc-Comment-Klarstellung in BatchFailureResponse)
    - `cargo build -p genossi_rest -p genossi_rest_types` exit 0
    - `cargo test -p genossi_rest --lib repayment_entry` exit 0
    - `cargo test -p genossi_rest_types repayment_entry` exit 0
  </acceptance_criteria>
  <done>OpenAPI für /batch-status listet 404 mit klarer Description; BatchFailureResponse-Schema-Doc grenzt 409-Scope von 404-Scope ab; alle Tests grün.</done>
</task>

</tasks>

<verification>
- Service-Layer EntityNotFound-Mapping korrekt; conflict_body("entry not found")-Pfad entfernt
- OpenAPI listet 404 explizit für /batch-status auf
- BatchFailureResponse-Doc klar abgegrenzt
- Alle Service-Impl- und REST-Tests grün
- Workspace-Build clean
</verification>

<success_criteria>
- CR-02 gefixt: Aggregat-Konsistenz wiederhergestellt (404 für NotFound, 409 nur für echte Domain-Konflikte)
- API-Vertrag dokumentiert: Frontend kann via OpenAPI-Schema Stale-ID von Domain-Konflikt unterscheiden
- Doc-Comment in TO-Schema fixiert das Verhalten dauerhaft
</success_criteria>

<output>
After completion, create `.planning/phases/08-repaymententry-auto-bef-llung/08-09-SUMMARY.md`
</output>
</content>
</invoke>