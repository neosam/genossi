---
phase: 09
plan: 03
type: execute
wave: 3
depends_on: [09-01, 09-02]
files_modified:
  - genossi_bin/src/lib.rs
autonomous: true
requirements: [PAYO-01]
estimated_time: 15min
tags: [di-wiring, configuration]

must_haves:
  truths:
    - "RepaymentEntryServiceDependencies hat 8 Type-Aliases statt 7 — MemberActionDao ist ergaenzt"
    - "RepaymentEntryServiceImpl-Konstruktor in RestStateImpl::new bekommt member_action_dao.clone() als 8. Argument"
    - "genossi_bin kompiliert clean nach diesem Plan (Whole-Workspace-Build)"
    - "Der bereits existierende Arc<MemberActionDao>-Wert in genossi_bin/src/lib.rs:563 wird per Arc::clone() an RepaymentEntryServiceImpl weitergereicht (single DAO instance per process, W-02)"
  artifacts:
    - path: "genossi_bin/src/lib.rs"
      provides: "DI-Wiring fuer MemberActionDao an RepaymentEntryServiceImpl"
      contains: "member_action_dao: member_action_dao.clone(),"
  key_links:
    - from: "let member_action_dao = Arc::new(MemberActionDao::new(pool.clone())) (Z. 563)"
      to: "RepaymentEntryServiceImpl-Konstruktor"
      via: "Arc::clone() — single DAO instance shared across all consumers"
      pattern: "member_action_dao\\.clone\\(\\)"
---

<objective>
Dependency-Injection-Wiring fuer den in Plan 09-01 ergaenzten `MemberActionDao`-Service-Dep. Eine kleine, aber Workspace-blockierende Aenderung in `genossi_bin/src/lib.rs`: (a) Type-Alias-Block `RepaymentEntryServiceDependencies` bekommt eine 8. Zeile `type MemberActionDao = MemberActionDao;`, (b) der Konstruktor-Aufruf in `RestStateImpl::new()` (Z. 765-775) bekommt `member_action_dao: member_action_dao.clone(),` als 8. Feld. Der `member_action_dao`-`Arc<...>` ist bereits Z. 563 als `Arc::new(MemberActionDao::new(pool.clone()))` instanziiert und an mehrere bestehende Services (MemberService, MemberActionService, ValidationService, MemberImportService, ApplicationService) geteilt; Phase 9 haengt sich als 6. Konsument an.

Purpose: Ohne diese Aenderung schlaegt `cargo build -p genossi_bin` mit "missing field member_action_dao" / "associated type MemberActionDao not satisfied" fehl. Plan 09-04 (E2E-Tests via genossi_bin/tests/) braucht ein lauffaehiges Binary mit gewiretem Service.

Output: 1 Datei-Modifikation mit ~2 Zeilen geaenderten Code + ggf. eine Inline-Kommentar-Zeile, die das DAO-Sharing-Pattern dokumentiert.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-auszahlungs-buchung-atomisch-auditiert/09-CONTEXT.md
@.planning/phases/09-auszahlungs-buchung-atomisch-auditiert/09-RESEARCH.md
@.planning/phases/09-auszahlungs-buchung-atomisch-auditiert/09-PATTERNS.md
@.planning/phases/09-auszahlungs-buchung-atomisch-auditiert/09-01-PLAN.md
@.planning/phases/09-auszahlungs-buchung-atomisch-auditiert/09-02-PLAN.md
@genossi_bin/src/lib.rs

<interfaces>
<!-- Aktueller Stand (vor diesem Plan) — verifiziert via grep im Quell-Code. -->

genossi_bin/src/lib.rs Z. 216-233 (CURRENT, before this plan):
```rust
pub struct RepaymentEntryServiceDependencies;

unsafe impl Send for RepaymentEntryServiceDependencies {}
unsafe impl Sync for RepaymentEntryServiceDependencies {}

impl genossi_service_impl::repayment_entry::RepaymentEntryServiceDeps
    for RepaymentEntryServiceDependencies
{
    type Context = Context;
    type Transaction = Transaction;
    type RepaymentEntryDao = RepaymentEntryDao;
    type RepaymentPhaseDao = RepaymentPhaseDao;
    type MemberDao = MemberDao;
    type AuditLogDao = AuditLogDao;
    type PermissionService = PermissionService;
    type UuidService = UuidService;
    type TransactionDao = TransactionDao;
}
```

genossi_bin/src/lib.rs Z. 364 (MemberActionDao-Type-Alias bereits da):
```rust
type MemberActionDao = genossi_dao_impl_sqlite::member_action::MemberActionDaoImpl;
```

genossi_bin/src/lib.rs Z. 563 (member_action_dao bereits konstruiert):
```rust
let member_action_dao = Arc::new(MemberActionDao::new(pool.clone()));
```

Bereits 5 bestehende Konsumenten teilen sich diesen Arc per `.clone()` (Z. 568, 577, 604, 625, 709) — Phase 9 ist Konsument #6.

genossi_bin/src/lib.rs Z. 761-775 (CURRENT RepaymentEntryServiceImpl-Konstruktor):
```rust
// Phase 8 Plan 05: RepaymentEntryServiceImpl with 7 deps.
// Uses the SAME repayment_phase_dao + repayment_entry_dao + member_dao
// Arcs as RepaymentPhaseServiceImpl above — single DAO instance per
// process per W-02.
let repayment_entry_service = Arc::new(
    genossi_service_impl::repayment_entry::RepaymentEntryServiceImpl {
        repayment_entry_dao: repayment_entry_dao.clone(),
        repayment_phase_dao: repayment_phase_dao.clone(),
        member_dao: member_dao.clone(),
        audit_log_dao: audit_log_dao.clone(),
        permission_service: permission_service.clone(),
        uuid_service: uuid_service.clone(),
        transaction_dao: transaction_dao.clone(),
    },
);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire MemberActionDao in RepaymentEntryServiceDependencies + Konstruktor</name>
  <files>genossi_bin/src/lib.rs</files>
  <read_first>
    - genossi_bin/src/lib.rs Z. 211-237 (RepaymentEntryServiceDependencies-Block — exakte Lage der Type-Alias-Liste)
    - genossi_bin/src/lib.rs Z. 360-370 (alle Type-Alias-Definitionen — bestaetige dass `type MemberActionDao = genossi_dao_impl_sqlite::member_action::MemberActionDaoImpl;` Z. 364 existiert)
    - genossi_bin/src/lib.rs Z. 560-575 (member_action_dao-Konstruktion + erste Konsumenten — bestaetige Arc-Sharing-Pattern)
    - genossi_bin/src/lib.rs Z. 761-775 (RepaymentEntryServiceImpl-Konstruktor — Phase-9-Ziel-Stelle)
    - .planning/phases/09-auszahlungs-buchung-atomisch-auditiert/09-PATTERNS.md §"genossi_bin/src/lib.rs — DI-Wiring"
    - .planning/phases/09-auszahlungs-buchung-atomisch-auditiert/09-RESEARCH.md File-Manifest Z. 737-738
  </read_first>
  <action>
**Edit 1 — `RepaymentEntryServiceDependencies`-Block (Z. 224-232):**

In den `impl ... RepaymentEntryServiceDeps for RepaymentEntryServiceDependencies`-Block, NACH `type MemberDao = MemberDao;` und VOR `type AuditLogDao = AuditLogDao;` eine NEUE Zeile einfuegen:

```rust
impl genossi_service_impl::repayment_entry::RepaymentEntryServiceDeps
    for RepaymentEntryServiceDependencies
{
    type Context = Context;
    type Transaction = Transaction;
    type RepaymentEntryDao = RepaymentEntryDao;
    type RepaymentPhaseDao = RepaymentPhaseDao;
    type MemberDao = MemberDao;
    type MemberActionDao = MemberActionDao;
    type AuditLogDao = AuditLogDao;
    type PermissionService = PermissionService;
    type UuidService = UuidService;
    type TransactionDao = TransactionDao;
}
```

Auch das `// Phase 8 Plan 05 (D-DI)`-Kommentar (Z. 211-215) aktualisieren auf "Phase 9 erweitert um MemberActionDao (Auszahlungs-Cascade, PAYO-01..04)":

```rust
// Phase 8 Plan 05 (D-DI) + Phase 9 (PAYO-01): RepaymentEntryServiceImpl wiring.
// Eight deps — RepaymentEntryDao + RepaymentPhaseDao + MemberDao + MemberActionDao
// (Phase 9: fuer audited_create! MemberAction::Verkauf im mark_paid_out-Cascade) +
// AuditLogDao + PermissionService + UuidService + TransactionDao.
// RepaymentEntryDao, RepaymentPhaseDao werden Arc-shared mit RepaymentPhaseServiceImpl;
// MemberActionDao Arc-shared mit allen 5 bestehenden Konsumenten (W-02:
// exakt 1 DAO-Konstruktor pro Prozess; Phase 9 ist Konsument #6).
```

**Edit 2 — RepaymentEntryServiceImpl-Konstruktor (Z. 765-775):**

NACH `member_dao: member_dao.clone(),` und VOR `audit_log_dao: audit_log_dao.clone(),` eine NEUE Zeile einfuegen:

```rust
        // Phase 8 Plan 05 + Phase 9: RepaymentEntryServiceImpl with 8 deps.
        // Uses the SAME repayment_phase_dao + repayment_entry_dao + member_dao
        // + member_action_dao (Phase 9) Arcs as other services — single DAO
        // instance per process per W-02. member_action_dao ist Konsument #6
        // (bereits geteilt mit MemberService, MemberActionService, ValidationService,
        // MemberImportService, ApplicationService — siehe Z. 568, 577, 604, 625, 709).
        let repayment_entry_service = Arc::new(
            genossi_service_impl::repayment_entry::RepaymentEntryServiceImpl {
                repayment_entry_dao: repayment_entry_dao.clone(),
                repayment_phase_dao: repayment_phase_dao.clone(),
                member_dao: member_dao.clone(),
                member_action_dao: member_action_dao.clone(),
                audit_log_dao: audit_log_dao.clone(),
                permission_service: permission_service.clone(),
                uuid_service: uuid_service.clone(),
                transaction_dao: transaction_dao.clone(),
            },
        );
```

**WICHTIG:** Die Reihenfolge der Felder im Struct-Literal MUSS mit der `gen_service_impl!`-Block-Reihenfolge (Plan 09-01 Edit 3) uebereinstimmen — `member_action_dao` kommt zwischen `member_dao` und `audit_log_dao`. Wenn der Executor in Plan 09-01 eine andere Reihenfolge gewaehlt hat, hier ANGLEICHEN.

**Sicherstellen, dass `member_action_dao` (Z. 563) tatsaechlich vor dem RepaymentEntryServiceImpl-Konstruktor (Z. 765) deklariert ist** — das ist gegeben (Z. 563 < 765). Falls ein Future-Refactor das umstellt: `member_action_dao` muss VOR dem Konstruktor aufgebaut sein, sonst Variable-not-found.

**Compile-Verifikation:**
```bash
cargo build -p genossi_bin
```
Sollte clean kompilieren. Wenn ein "type mismatch" auftritt: pruefe, dass Plan 09-01 Edit 3 mit dem hier verwendeten Typ-Alias-Namen synchronisiert ist (`MemberActionDao` muss in beiden Stellen identisch sein).

**Wenn ein "RepaymentEntryServiceDeps missing associated type MemberActionDao"-Fehler auftritt:** Plan 09-01 Edit 3 wurde nicht oder unvollstaendig ausgefuehrt. Erst Plan 09-01 abschliessen, dann hier weiter.
  </action>
  <verify>
    <automated>cargo build -p genossi_bin 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - genossi_bin/src/lib.rs RepaymentEntryServiceDependencies-Block contains "type MemberActionDao = MemberActionDao;" (1 occurrence in this block; total in file = 6 because of bestehende Type-Alias-Konsumenten)
    - genossi_bin/src/lib.rs RepaymentEntryServiceImpl-Konstruktor contains "member_action_dao: member_action_dao.clone()," (exactly 1 in the RepaymentEntryServiceImpl-Block; total in file = 6 occurrences)
    - `grep -c "member_action_dao: member_action_dao.clone()," /home/neosam/programming/rust/projects/genossi3/genossi_bin/src/lib.rs` outputs 6 (Phase 9 ist Konsument #6; vorher 5)
    - `cargo build -p genossi_bin` exits 0
    - `cargo build` (whole-workspace) exits 0
    - `cargo build --bin genossi` exits 0 — main binary kompiliert
    - `cargo test -p genossi_service_impl --lib repayment_entry 2>&amp;1 | grep "test result"` enthaelt "0 failed" (kein Test-Regress)
  </acceptance_criteria>
  <done>
    - Whole-Workspace-Build clean.
    - RepaymentEntryServiceImpl ist mit member_action_dao gewired und kann instanziiert werden.
    - Plan 09-04 (E2E-Tests) kann jetzt das Binary starten und gegen den Service testen.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| genossi_bin Startup | Initialisiert alle Arcs; einmal pro Prozess (single source) |
| Arc-Sharing | Mehrere Services teilen sich denselben DAO-Arc — Tx-Isolation kommt vom DAO, nicht Arc |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-03-01 | Tampering (Multiple DAO-Instanzen) | DI-Wiring | LOW | mitigate | W-02 Pattern: `let member_action_dao = Arc::new(MemberActionDao::new(pool.clone()))` ist EXAKT 1× in der Datei. Acceptance: `grep -c "let member_action_dao = Arc::new(MemberActionDao::new" /home/neosam/programming/rust/projects/genossi3/genossi_bin/src/lib.rs` outputs 1. Wenn 2+ Instanzen aufgebaut werden, koennten Audit-Calls auf unterschiedliche Verbindungs-Pools laufen (theoretisch, aber Pool ist auch geteilt) — kein direkter Sicherheitsbruch, aber Konsistenz-Risiko. |
| T-09-03-02 | Repudiation (DAO-Instanz mit falschem Pool) | DI-Wiring | LOW | accept | Der Pool ist global (`let pool = SqlitePool::connect(...)` in main.rs), und `MemberActionDao::new(pool.clone())` macht Arc-Clone des Pools, kein neuer Pool. Alle Schreibvorgaenge enden in der gleichen DB. |
| T-09-03-03 | Configuration Drift (Reihenfolge Felder vs gen_service_impl!) | DI-Wiring | MEDIUM | mitigate | Reihenfolge der Felder im Struct-Literal MUSS mit der `gen_service_impl!`-Reihenfolge in Plan 09-01 Edit 3 uebereinstimmen. Wenn nicht, schlaegt Compile mit "missing field" / "no field with this name" fehl — das ist die natuerliche Mitigation. Acceptance: `cargo build -p genossi_bin` exits 0. |

</threat_model>

<verification>

Compile-Test:
```bash
cargo build -p genossi_bin
cargo build  # whole workspace
cargo build --bin genossi
```

DAO-Sharing-Sanity-Check:
```bash
grep -c "member_action_dao: member_action_dao.clone()," /home/neosam/programming/rust/projects/genossi3/genossi_bin/src/lib.rs
# Expected: 6 (Phase 8: 5 Konsumenten, Phase 9 fuegt 1 hinzu)

grep -c "let member_action_dao = Arc::new(MemberActionDao::new" /home/neosam/programming/rust/projects/genossi3/genossi_bin/src/lib.rs
# Expected: 1 (single DAO instance per process per W-02)
```

Plan 09-01 + 09-02 + 09-03 zusammen sollten zu einem clean buildbar Binary fuehren — pruefen via:
```bash
cargo build --bin genossi --release 2>&1 | tail -3
```

</verification>

<success_criteria>

- [ ] `RepaymentEntryServiceDependencies` listet `MemberActionDao` als 8. Type-Alias
- [ ] `RepaymentEntryServiceImpl`-Konstruktor wired `member_action_dao.clone()` als 8. Feld
- [ ] `cargo build` (whole workspace) clean
- [ ] `cargo build --bin genossi` clean
- [ ] `member_action_dao: member_action_dao.clone(),` appears 6× in lib.rs (Phase 9 ist Konsument #6)
- [ ] `let member_action_dao = Arc::new(MemberActionDao::new` appears 1× (single instance per process, W-02)

</success_criteria>

<output>
Nach Plan-Ausfuehrung: `.planning/phases/09-auszahlungs-buchung-atomisch-auditiert/09-03-SUMMARY.md` mit `cargo build`-Output, Diff der genau-2 geaenderten Stellen, Bestaetigung der DAO-Sharing-Grep-Counts.
</output>
