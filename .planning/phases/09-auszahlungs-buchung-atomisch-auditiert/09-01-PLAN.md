---
phase: 09
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - genossi_service/src/repayment_entry.rs
  - genossi_service_impl/src/member_action.rs
  - genossi_service_impl/src/repayment_entry.rs
autonomous: true
requirements: [PAYO-01, PAYO-02, PAYO-03, PAYO-04]
estimated_time: 90min
tags: [cascade, audit, atomic-tx, race-defense, validation]

decisions_addressed:
  - "D-04: MemberAction-Felder vollautomatisch gesetzt im Cascade (action_type=Verkauf, shares_change=-share_count_to_pay_out, date=today via OffsetDateTime::now_utc().date(), comment=\"Anteils-Rueckzahlung Phase {fiscal_year}\", effective_date=None, transfer_member_id=None) — kein Vorstand-Input in Phase 9"
  - "D-08: Owner-Service ist RepaymentEntryServiceImpl::mark_paid_out mit direkten DAO-Macro-Calls (audited_create! + audited_update!) statt Service-zu-Service — haelt Tx-Atomarity deterministisch und vermeidet Permission-Re-Checks; MemberActionDao wird als 8. Dep in gen_service_impl! ergaenzt"
  - "D-09: 12-Schritt-Cascade-Reihenfolge (Tx-begin → Permission → Entry-Load+Status-Guards → Phase-Load+Status-Guard → Member-Load+PAYO-03 → audited_create! MemberAction → audited_update! Member → Re-Read Member → audited_update! Entry → Re-Read Entry → recalc_migrated → commit) wortgleich umgesetzt in mark_paid_out"
  - "D-10: Option (a) gewaehlt — compute_migration_status wird von pub(crate) auf pub gewechselt; mark_paid_out ruft die Funktion inline nach Cascade auf (Pattern-Konsistenz mit MemberActionServiceImpl::create und MemberServiceImpl::update)"
  - "D-11: Status-Guard ist die primaere Race-Defense — entry.status-Check (PaidOut → Conflict; Whitelist Open/Contacted) lebt VOR allen Schreib-Macros; zusaetzlich greift SQLite-WAL Write-Lock + Optimistic-Lock via audited_update! als zweite Stufe"
  - "D-13: PAYO-03-Validation (member.current_shares < entry.share_count_to_pay_out) inline in mark_paid_out NACH Member-Load und VOR audited_create! — keine separate validate_payout_shares-Helperfunktion"
  - "D-14: ValidationError-Format mit beiden Werten (Member.current_shares ({current}) is less than entry.share_count_to_pay_out ({requested})) via ServiceError::ValidationError(vec![ValidationFailureItem { field: \"share_count_to_pay_out\", message: ... }]) — REST-Mapping ist 400 Bad Request"

must_haves:
  truths:
    - "RepaymentEntryService trait exposes mark_paid_out(id, context) -> Result<RepaymentEntry, ServiceError>"
    - "RepaymentEntryServiceImpl::mark_paid_out durchlaeuft genau 3 audited_*!-Calls (1× audited_create! fuer MemberAction::Verkauf + 2× audited_update! fuer Member + RepaymentEntry) in einer einzigen SQLite-Transaktion"
    - "Alle 3 audited_*!-Calls verwenden denselben process-String 'repayment-entry.mark-paid-out' (SC #3 / D-01)"
    - "D-04: MemberAction-Cascade-Insert hat action_type=ActionType::Verkauf, shares_change=-entry.share_count_to_pay_out, date=today, comment=\"Anteils-Rueckzahlung Phase {fiscal_year}\", effective_date=None, transfer_member_id=None — vollautomatisch ohne Vorstand-Input"
    - "D-08: Cascade lebt in RepaymentEntryServiceImpl::mark_paid_out mit direkten DAO-Macro-Calls (kein Service-zu-Service); MemberActionDao wird als 8. Dep in gen_service_impl! gefuehrt"
    - "D-09: 12-Schritt-Cascade in der dokumentierten Reihenfolge implementiert (Tx-begin, Permission, Entry+Phase+Member-Load mit Status/Validation-Guards, MemberAction-create, Member-update, Re-Read, Entry-update, Re-Read, recalc_migrated, commit)"
    - "D-10: compute_migration_status ist nach Plan-Execution pub (nicht pub(crate)) und wird in mark_paid_out aufgerufen (Option a)"
    - "D-11: Status-Guard (entry.status ∈ {Open, Contacted}) ist primaere Race-Defense und feuert VOR jedem audited_*!-Call; Optimistic-Lock per audited_update! ist zweite Stufe"
    - "D-13: PAYO-03-Validation (member.current_shares < entry.share_count_to_pay_out) inline in mark_paid_out NACH Member-Load und VOR audited_create!"
    - "D-14: ValidationError-Format enthaelt beide Werte (current + requested) via ServiceError::ValidationError(vec![ValidationFailureItem { field: \"share_count_to_pay_out\", message: ... }])"
    - "Validation: Member.current_shares < entry.share_count_to_pay_out blockt mit ServiceError::ValidationError vor irgendeinem audited_*!-Call (PAYO-03 / SC #2)"
    - "Status-Guard: Entry.status == PaidOut blockt mit ServiceError::Conflict mit Message 'already paid out' (PAYO-04 / SC #4)"
    - "Re-Read None nach audited_update! mapped auf ServiceError::InternalError (Phase 8 BL-01 Pattern) — NICHT EntityNotFound"
    - "compute_migration_status ist nach Plan-Execution pub (nicht pub(crate)) und wird in mark_paid_out aufgerufen"
  artifacts:
    - path: "genossi_service/src/repayment_entry.rs"
      provides: "Trait-Methode mark_paid_out + Compile-Test-Mock-Erweiterung"
      contains: "async fn mark_paid_out"
    - path: "genossi_service_impl/src/member_action.rs"
      provides: "compute_migration_status mit erweiterter Sichtbarkeit pub"
      contains: "pub fn compute_migration_status"
    - path: "genossi_service_impl/src/repayment_entry.rs"
      provides: "mark_paid_out-Impl + MemberActionDao-Dep + REPAYMENT_ENTRY_PROCESS_MARK_PAID_OUT-const + 6 Unit-Tests + TestMemberActionDao-Mock"
      contains: "const REPAYMENT_ENTRY_PROCESS_MARK_PAID_OUT"
  key_links:
    - from: "genossi_service_impl/src/repayment_entry.rs::mark_paid_out"
      to: "audited_create! (MemberAction) + audited_update! (Member) + audited_update! (RepaymentEntry)"
      via: "3 macro calls, single Tx, single commit"
      pattern: "audited_create!.*audited_update!.*audited_update!.*transaction_dao.commit"
    - from: "genossi_service_impl/src/repayment_entry.rs::mark_paid_out"
      to: "crate::member_action::compute_migration_status"
      via: "fully-qualified path call after Re-Read"
      pattern: "crate::member_action::compute_migration_status"
---

<objective>
Backend-Cascade-Foundation fuer die atomare Auszahlungs-Buchung: `RepaymentEntryService`-Trait wird um `mark_paid_out` erweitert, `RepaymentEntryServiceImpl` bekommt eine neue `MemberActionDao`-Dep und implementiert den 11-Schritt-Cascade aus RESEARCH §"Cascade Implementation Walkthrough" (Z. 416-598). Validierung (PAYO-03), Status-Guards (PAYO-04) und Re-Read-None→InternalError (BL-01 Pattern) sind direkt im Service abgedeckt. `compute_migration_status` wird `pub` gemacht, um recalc_migrated-Logik inline aufrufen zu koennen (D-10 Option a).

Purpose: Single Source of Truth fuer den Auszahlungs-Cascade. Alle nachfolgenden Plans (REST, Wiring, E2E) bauen auf der hier definierten Trait-Methode + Impl auf.

Output: 3 Datei-Modifikationen + 6 neue Unit-Tests, die Happy-Path, 4 Reject-Pfade und 1 Re-Read-None-Pfad abdecken.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-auszahlungs-buchung-atomisch-auditiert/09-CONTEXT.md
@.planning/phases/09-auszahlungs-buchung-atomisch-auditiert/09-RESEARCH.md
@.planning/phases/09-auszahlungs-buchung-atomisch-auditiert/09-PATTERNS.md
@.planning/phases/08-repaymententry-auto-bef-llung/08-REVIEW.md
@genossi_service/src/repayment_entry.rs
@genossi_service_impl/src/repayment_entry.rs
@genossi_service_impl/src/member_action.rs
@genossi_service_impl/src/audit_macros.rs
@genossi_dao/src/member_action.rs

<interfaces>
<!-- Key types/contracts the executor will need. Extracted from codebase. -->

From genossi_dao/src/member_action.rs (Phase 8 baseline):
```rust
pub enum ActionType { Eintritt, Austritt, Todesfall, Verkauf, ... }

pub struct MemberActionEntity {
    pub id: Uuid,
    pub member_id: Uuid,
    pub action_type: ActionType,
    pub date: time::Date,
    pub shares_change: i32,
    pub transfer_member_id: Option<Uuid>,
    pub effective_date: Option<time::Date>,
    pub comment: Option<Arc<str>>,
    pub created: time::PrimitiveDateTime,
    pub deleted: Option<time::PrimitiveDateTime>,
    pub version: Uuid,
}

impl Auditable for MemberActionEntity {
    fn entity_type(&self) -> &'static str { "member_action" }
    fn entity_id(&self) -> Uuid { self.id }
    // audit_fields() returns 7 fields (member_id, action_type, date, shares_change, transfer_member_id, effective_date, comment)
}

#[automock(type Transaction = MockTransaction;)]
#[async_trait]
pub trait MemberActionDao {
    type Transaction: crate::Transaction;
    async fn dump_all(&self, tx: Self::Transaction) -> Result<Arc<[MemberActionEntity]>, DaoError>;
    async fn create(&self, entity: &MemberActionEntity, process: &str, tx: Self::Transaction) -> Result<(), DaoError>;
    async fn update(&self, entity: &MemberActionEntity, process: &str, tx: Self::Transaction) -> Result<(), DaoError>;
    async fn all(&self, tx: Self::Transaction) -> Result<Arc<[MemberActionEntity]>, DaoError>;
    async fn find_by_id(&self, id: Uuid, tx: Self::Transaction) -> Result<Option<MemberActionEntity>, DaoError>;
    async fn find_by_member_id(&self, member_id: Uuid, tx: Self::Transaction) -> Result<Arc<[MemberActionEntity]>, DaoError>;
}
```

From genossi_dao/src/member.rs (Phase 1+):
```rust
pub struct MemberEntity {
    pub id: Uuid,
    pub current_shares: i32,
    pub action_count: i32,
    pub version: Uuid,
    // ... weitere Felder
}
```

From genossi_dao/src/repayment_entry.rs (Phase 8):
```rust
pub enum RepaymentEntryStatus { Open, Contacted, PaidOut }
pub struct RepaymentEntryEntity {
    pub id: Uuid,
    pub member_id: Uuid,
    pub phase_id: Uuid,
    pub share_count_to_pay_out: i32,
    pub status: RepaymentEntryStatus,
    pub version: Uuid,
    // ...
}
```

From genossi_dao/src/repayment_phase.rs (Phase 7):
```rust
pub enum RepaymentPhaseStatus { Preparation, Open, Closed }
pub struct RepaymentPhaseEntity {
    pub fiscal_year: i32,
    pub status: RepaymentPhaseStatus,
    // ...
}
```

From genossi_service_impl/src/audit_macros.rs:
```rust
// audited_create!(self, dao, &entity, process, &user_id, tx)
// audited_update!(self, dao, id, &new_entity, process, &user_id, tx)
//   audited_update! loads old via dao.find_by_id() internally, diffs fields,
//   and writes audit entries.
```

From genossi_service_impl/src/member_action.rs (CURRENT, before this plan):
```rust
// Line 32: CURRENT visibility — Phase 9 changes this:
pub(crate) fn compute_migration_status(
    member: &genossi_dao::member::MemberEntity,
    actions: &[genossi_dao::member_action::MemberActionEntity],
) -> MigrationStatus
```

From genossi_service_impl/src/repayment_entry.rs (CURRENT, before this plan):
- Lines 48-58: `gen_service_impl!` block — has 7 deps (RepaymentEntryDao, RepaymentPhaseDao, MemberDao, AuditLogDao, PermissionService, UuidService, TransactionDao). Phase 9 adds an 8th.
- Lines 265-291: Re-Read pattern (BL-01-fixed) — exact source-comment block to reproduce
- Line 46: `const ADMIN_PRIVILEGE: &str = "admin";`
- Lines 556-718: 5 hand-rolled test mocks (`MockTestRepaymentEntryDao`, `MockTestRepaymentPhaseDao`, `MockTestMemberDao`, `MockTestAuditLogDao`, `MockTestPermissionService`)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Erweitere Trait + Visibility-Fix + Service-Impl-Deps</name>
  <files>genossi_service/src/repayment_entry.rs, genossi_service_impl/src/member_action.rs, genossi_service_impl/src/repayment_entry.rs</files>
  <read_first>
    - genossi_service/src/repayment_entry.rs (komplett — Trait-Header + Pattern aller bestehenden Trait-Methoden + Compile-Test Z. 272-283)
    - genossi_service_impl/src/member_action.rs (Z. 30-69 — compute_migration_status-Signatur + Body, Z. 200-225 recalc_migrated-Vorlage)
    - genossi_service_impl/src/repayment_entry.rs (Z. 1-58 — Imports + Konstanten + gen_service_impl!-Block; Z. 556-720 — alle 5 bestehenden Mocks; Z. 824-835 — TestDeps-Block; Z. 939-968 — build_service-Helper)
    - genossi_dao/src/member_action.rs (Z. 99-155 — MemberActionDao-Trait-Definition mit allen Methoden)
    - .planning/phases/09-auszahlungs-buchung-atomisch-auditiert/09-PATTERNS.md §"genossi_service/src/repayment_entry.rs" + §"genossi_service_impl/src/repayment_entry.rs — gen_service_impl!-Block-Erweiterung"
  </read_first>
  <behavior>
    - Trait-Test: `cargo test -p genossi_service repayment_entry::tests::test_mock_repayment_entry_service_compiles` muss gruen sein nachdem `expect_mark_paid_out()` ergaenzt ist.
    - Test: `cargo test -p genossi_service_impl --lib repayment_entry` muss WEITERHIN gruen sein (kein Behavioral-Test in Task 1, nur Compile-Erweiterungen).
    - Visibility: `compute_migration_status` ist `pub`, kein Compile-Error in `member_action.rs::recalc_migrated` (vorheriger Caller im selben Crate).
    - gen_service_impl!: 8 Deps statt 7; build von `genossi_service_impl` compiliert.
  </behavior>
  <action>
**Edit 1 — `genossi_service/src/repayment_entry.rs` (Trait-Erweiterung, ~14 LOC):**

Direkt nach `batch_toggle_status` (Z. 173-177) am Ende des `pub trait RepaymentEntryService`-Blocks, VOR dem schliessenden `}` der trait-definition, einfuegen:

```rust
    /// Mark a RepaymentEntry as PaidOut with atomic Cascade (Phase 9, PAYO-01..04):
    /// (1) audited_create! MemberAction::Verkauf with shares_change = -N,
    /// (2) audited_update! Member.current_shares -= N + action_count += 1,
    /// (3) audited_update! RepaymentEntry.status = PaidOut.
    /// All three writes commit in a single SQLite-Tx with shared process
    /// `repayment-entry.mark-paid-out` (D-01). Final per PAYO-04 (no toggle-back).
    /// Pre-Conditions: Entry.status in {Open, Contacted}, Phase.status == Open,
    /// Member.current_shares >= Entry.share_count_to_pay_out (PAYO-03).
    /// Audit-process `repayment-entry.mark-paid-out`. Requires `admin`.
    async fn mark_paid_out(
        &self,
        id: Uuid,
        context: Authentication<Self::Context>,
    ) -> Result<RepaymentEntry, ServiceError>;
```

Im Tests-Modul (Z. 272-283), den Compile-Test um eine letzte Zeile erweitern (vor dem schliessenden `}`):

```rust
        let _ = mock.expect_batch_toggle_status();
        // Phase 9 — neu (PAYO-01):
        let _ = mock.expect_mark_paid_out();
    }
```

**Edit 2 — `genossi_service_impl/src/member_action.rs:32` (Visibility, 1 LOC, in-place):**

Aenderung exakt eine Zeile:

VORHER:
```rust
pub(crate) fn compute_migration_status(
```

NACHHER:
```rust
pub fn compute_migration_status(
```

Keine weiteren Aenderungen an dieser Datei. Begruendung als Inline-Doc-Kommentar DIREKT UEBER der Funktion (zwischen ggf. vorhandenem Doc-Comment und Signatur) ergaenzen:

```rust
// Phase 9 (D-10 Option a): visibility upgraded from pub(crate) to pub to allow
// RepaymentEntryServiceImpl::mark_paid_out to call this function for the
// post-Cascade recalc_migrated step. Function is pure (no Tx, no I/O); no
// safety or correctness risk from cross-crate exposure. See PHASE-9 PATTERNS.md
// §"genossi_service_impl/src/member_action.rs — Visibility-Wechsel".
```

**Edit 3 — `genossi_service_impl/src/repayment_entry.rs` (Imports, Konstanten, gen_service_impl!-Block):**

(a) Imports ergaenzen. Nach der bestehenden Zeile `use genossi_dao::member::MemberDao;` (suche per Grep, sollte in den ersten ~30 LOC liegen) eine NEUE Zeile einfuegen:

```rust
use genossi_dao::member_action::{ActionType, MemberActionDao, MemberActionEntity};
```

(b) Konstante ergaenzen. Direkt nach Zeile 45 (`const REPAYMENT_ENTRY_PROCESS_BATCH_TOGGLE: &str = "repayment-entry.batch-toggle";`) eine NEUE Zeile einfuegen, VOR `const ADMIN_PRIVILEGE`:

```rust
const REPAYMENT_ENTRY_PROCESS_MARK_PAID_OUT: &str = "repayment-entry.mark-paid-out";
```

(c) `gen_service_impl!`-Block (Z. 48-58) erweitern. Eine NEUE Zeile einfuegen zwischen `MemberDao` und `AuditLogDao`:

```rust
gen_service_impl! {
    struct RepaymentEntryServiceImpl: RepaymentEntryService = RepaymentEntryServiceDeps {
        RepaymentEntryDao: RepaymentEntryDao<Transaction = Self::Transaction> = repayment_entry_dao,
        RepaymentPhaseDao: RepaymentPhaseDao<Transaction = Self::Transaction> = repayment_phase_dao,
        MemberDao: MemberDao<Transaction = Self::Transaction> = member_dao,
        MemberActionDao: MemberActionDao<Transaction = Self::Transaction> = member_action_dao,
        AuditLogDao: AuditLogDao<Transaction = Self::Transaction> = audit_log_dao,
        PermissionService: PermissionService<Context = Self::Context> = permission_service,
        UuidService: UuidService = uuid_service,
        TransactionDao: TransactionDao<Transaction = Self::Transaction> = transaction_dao,
    }
}
```

**WICHTIG:** Nur die Service-Impl-Trait-Bound erweitern; die Implementation von `mark_paid_out` selbst kommt in Task 2. Nach diesem Task wird `cargo build -p genossi_service_impl` einen UNDEFINIERTE-METHODE-Fehler werfen, weil `impl RepaymentEntryService for RepaymentEntryServiceImpl` noch keinen `mark_paid_out`-Body hat. **Das ist erwartet und wird in Task 2 behoben.** Stop nicht hier ab, sondern gehe DIREKT zu Task 2 weiter.

Wenn der Wille besteht, Task 1 unabhaengig gruen zu bekommen: einen vorlaeufigen `unimplemented!()`-Stub einfuegen (siehe Hinweis am Anfang von Task 2).

Per D-08 Konvention (Phase 8): die neue Dep-Zeile wird in Konstruktor-Aufrufen (genossi_bin/src/lib.rs Z. 765-775) durch Plan 09-03 ergaenzt, NICHT hier. Bis dahin schlaegt `cargo build -p genossi_bin` fehl — das wird in Plan 09-03 geheilt.
  </action>
  <verify>
    <automated>cargo test -p genossi_service repayment_entry::tests::test_mock_repayment_entry_service_compiles -- --nocapture 2>&amp;1 | grep -E "(test result|FAILED)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - genossi_service/src/repayment_entry.rs contains "async fn mark_paid_out("
    - genossi_service/src/repayment_entry.rs contains "let _ = mock.expect_mark_paid_out();"
    - genossi_service_impl/src/member_action.rs line ~32 starts with "pub fn compute_migration_status" (no `(crate)`)
    - `grep -c "pub(crate) fn compute_migration_status" /home/neosam/programming/rust/projects/genossi3/genossi_service_impl/src/member_action.rs` outputs 0
    - `grep -c "pub fn compute_migration_status" /home/neosam/programming/rust/projects/genossi3/genossi_service_impl/src/member_action.rs` outputs 1
    - genossi_service_impl/src/repayment_entry.rs contains "const REPAYMENT_ENTRY_PROCESS_MARK_PAID_OUT: &str = \"repayment-entry.mark-paid-out\""
    - genossi_service_impl/src/repayment_entry.rs contains "MemberActionDao: MemberActionDao<Transaction = Self::Transaction> = member_action_dao"
    - genossi_service_impl/src/repayment_entry.rs contains "use genossi_dao::member_action::{ActionType, MemberActionDao, MemberActionEntity};"
    - `cargo test -p genossi_service repayment_entry::tests::test_mock_repayment_entry_service_compiles` exits 0
  </acceptance_criteria>
  <done>
    - mark_paid_out-Trait-Methode definiert; mockall generiert MockRepaymentEntryService::expect_mark_paid_out automatisch.
    - compute_migration_status ist pub; Verwendung im selben Crate weiterhin gruen.
    - gen_service_impl! kennt MemberActionDao als 8. Dep — Task 2 kann ohne weitere Dep-Definition mark_paid_out implementieren.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementiere mark_paid_out-Cascade + 6 Unit-Tests + TestMemberActionDao-Mock</name>
  <files>genossi_service_impl/src/repayment_entry.rs</files>
  <read_first>
    - .planning/phases/09-auszahlungs-buchung-atomisch-auditiert/09-RESEARCH.md §"Cascade Implementation Walkthrough" (Z. 414-602) + §"Testing Strategy" (Z. 678-717) + Pitfalls #1, #4, #5, #6, #7, #13
    - .planning/phases/09-auszahlungs-buchung-atomisch-auditiert/09-PATTERNS.md §"genossi_service_impl/src/repayment_entry.rs — mark_paid_out-Impl" + §"Test-Mock-Erweiterung"
    - genossi_service_impl/src/repayment_entry.rs (komplett — fuer die exakte Stelle, an der mark_paid_out an den impl-Block angehaengt wird, und die exakte Sequence/Mock-Struktur in test_update_entry_status_open_to_contacted_succeeds Z. 1294-1360)
    - genossi_service_impl/src/repayment_entry.rs Z. 265-291 (Re-Read-Pattern mit BL-01-Source-Comments — wortgleich reproduzieren)
    - genossi_service_impl/src/member_action.rs Z. 200-225 (recalc_migrated-Vorlage)
    - genossi_service_impl/src/member_action.rs Z. 32-69 (compute_migration_status, jetzt pub)
    - genossi_service_impl/src/audit_macros.rs (Z. 5-80 — audited_create! + audited_update! Signaturen)
    - genossi_dao/src/member_action.rs Z. 99-155 (MemberActionDao-Trait fuer den TestMemberActionDao-Mock)
    - genossi_service/src/member_action.rs (fuer MigrationState-Enum-Definition — wird in compute_migration_status verwendet)
    - .planning/phases/08-repaymententry-auto-bef-llung/08-REVIEW.md BL-01 (Z. 37-78)
  </read_first>
  <behavior>
    - test_mark_paid_out_happy_path: Open-Entry, Open-Phase, Member mit current_shares=10, entry.share_count_to_pay_out=3 → Result::Ok(RepaymentEntry mit status=PaidOut + neue version). Mock-Verify: 1× audited_create auf member_action_dao, 1× audited_update auf member_dao mit current_shares=7 und action_count+=1, 1× audited_update auf repayment_entry_dao mit status=PaidOut, 1× update_migrated auf member_dao, 1× commit auf transaction_dao.
    - test_mark_paid_out_rejects_paid_out_entry: Entry.status=PaidOut → Err(ServiceError::Conflict(s)) wobei s.contains("already paid out") oder s.contains("final per PAYO-04"). 0× audited_create, 0× audited_update.
    - test_mark_paid_out_rejects_when_phase_not_open: Entry.status=Open, Phase.status=Preparation (oder Closed) → Err(ServiceError::Conflict(s)) wobei s.contains("Phase status"). 0× audited_*!.
    - test_mark_paid_out_rejects_when_current_shares_insufficient: Member.current_shares=2, entry.share_count_to_pay_out=5 → Err(ServiceError::ValidationError(vec)) wobei vec[0].field == "share_count_to_pay_out" und vec[0].message.contains("Member.current_shares") und vec[0].message.contains("2") und vec[0].message.contains("5"). 0× audited_*!.
    - test_mark_paid_out_rereads_member_none_yields_internal_error: Cascade laeuft bis nach audited_update! Member, dann find_by_id (Re-Read) gibt Ok(None) zurueck → Err(ServiceError::InternalError(s)) wobei s.contains("Re-Read") und s.contains("Member"). NICHT EntityNotFound (BL-01).
    - test_mark_paid_out_member_action_has_correct_fields: Capture-Closure auf member_action_dao.expect_create() via `withf` — assert action_entity.action_type == ActionType::Verkauf, shares_change == -3, comment.as_deref() == Some("Anteils-Rueckzahlung Phase 2026") (oder dem konfigurierten fiscal_year), transfer_member_id == None, effective_date == None.
  </behavior>
  <action>
**WICHTIG vorab:** Falls Task 1 mit einem `unimplemented!()`-Stub abgeschlossen wurde, entferne den Stub und ersetze ihn durch die volle Implementation unten. Falls Task 1 ohne Stub abgeschlossen wurde (Build-Fehler beim Wechsel), implementiere unten DIREKT.

**Edit 1 — `mark_paid_out`-Impl in den `impl<Deps: RepaymentEntryServiceDeps> RepaymentEntryService for RepaymentEntryServiceImpl<Deps>`-Block (ca. Z. 379 oder am Ende des bestehenden Impl-Blocks, NACH `batch_toggle_status`):**

Vor dem schliessenden `}` des `impl<Deps: ...> RepaymentEntryService for ...`-Blocks die folgende Methode anhaengen. **EXAKT die Reihenfolge aus RESEARCH §Cascade Walkthrough Z. 416-598 wortgleich reproduzieren** (inkl. Source-Kommentaren). Der Re-Read-Block (Z. 265-291) wird 2× verwendet — einmal fuer Member, einmal fuer RepaymentEntry — beide Source-Comments MITKOPIEREN (PATTERNS.md §Re-Read-Pattern).

```rust
    async fn mark_paid_out(
        &self,
        id: Uuid,
        context: Authentication<Self::Context>,
    ) -> Result<RepaymentEntry, ServiceError> {
        // ===== Schritt 1: Tx beginnen =====
        let tx = self.transaction_dao.use_transaction(None).await?;

        // ===== Schritt 2: Permission + user_id (Phase 8 Convention) =====
        let user_id = self
            .permission_service
            .current_user_id(context.clone())
            .await?
            .unwrap_or_else(|| "SYSTEM".to_string());
        self.permission_service
            .check_permission(ADMIN_PRIVILEGE, context)
            .await?;

        // ===== Schritt 3: Entry laden + Status-Guards (PAYO-04 D-05) =====
        let entry = self
            .repayment_entry_dao
            .find_by_id(id, tx.clone())
            .await?
            .ok_or(ServiceError::EntityNotFound(id))?;

        // PAYO-04 / D-05: PaidOut ist final
        if entry.status == RepaymentEntryStatus::PaidOut {
            return Err(ServiceError::Conflict(Arc::from(
                "Entry already paid out (final per PAYO-04, no toggle-back)",
            )));
        }
        // Status-Whitelist: nur Open oder Contacted darf markiert werden
        if !matches!(
            entry.status,
            RepaymentEntryStatus::Open | RepaymentEntryStatus::Contacted
        ) {
            return Err(ServiceError::Conflict(Arc::from(format!(
                "Entry status is '{}', expected Open or Contacted",
                entry.status.as_str()
            ))));
        }

        // ===== Schritt 4: Phase laden + Status-Guard (Defense-in-Depth) =====
        let phase = self
            .repayment_phase_dao
            .find_by_id(entry.phase_id, tx.clone())
            .await?
            .ok_or_else(|| {
                // Referentielle Inkonsistenz: Entry zeigt auf nicht-existente Phase
                // → InternalError (NICHT EntityNotFound, siehe RESEARCH Pitfall #5).
                ServiceError::InternalError(Arc::from(format!(
                    "Entry {} references missing Phase {}",
                    id, entry.phase_id
                )))
            })?;
        if phase.status != RepaymentPhaseStatus::Open {
            return Err(ServiceError::Conflict(Arc::from(format!(
                "Phase status is '{}', expected 'Open' (Defense-in-Depth)",
                phase.status.as_str()
            ))));
        }

        // ===== Schritt 5: Member laden + PAYO-03-Validation =====
        let member = self
            .member_dao
            .find_by_id(entry.member_id, tx.clone())
            .await?
            .ok_or(ServiceError::EntityNotFound(entry.member_id))?;

        // PAYO-03 / D-13 / D-14: current_shares >= share_count_to_pay_out
        if member.current_shares < entry.share_count_to_pay_out {
            return Err(ServiceError::ValidationError(vec![ValidationFailureItem {
                field: Arc::from("share_count_to_pay_out"),
                message: Arc::from(format!(
                    "Member.current_shares ({}) is less than entry.share_count_to_pay_out ({})",
                    member.current_shares, entry.share_count_to_pay_out
                )),
            }]));
        }

        // ===== Schritt 6: audited_create! MemberAction::Verkauf (D-04 auto-fields) =====
        // shares_change muss negativ sein (validate_action in member_action.rs:91-97);
        // entry.share_count_to_pay_out ist garantiert > 0 (DB-CHECK + PAYO-03-Validation).
        let now = time::OffsetDateTime::now_utc();
        let today: time::Date = now.date();
        let created = time::PrimitiveDateTime::new(now.date(), now.time());
        let comment_str = format!("Anteils-Rueckzahlung Phase {}", phase.fiscal_year);
        let action_entity = MemberActionEntity {
            id: self.uuid_service.new_v4().await,
            member_id: entry.member_id,
            action_type: ActionType::Verkauf,
            date: today,
            shares_change: -entry.share_count_to_pay_out,
            transfer_member_id: None,
            effective_date: None,
            comment: Some(Arc::from(comment_str)),
            created,
            deleted: None,
            version: self.uuid_service.new_v4().await,
        };
        crate::audited_create!(
            self,
            self.member_action_dao,
            &action_entity,
            REPAYMENT_ENTRY_PROCESS_MARK_PAID_OUT,
            &user_id,
            tx
        );

        // ===== Schritt 7: audited_update! Member (current_shares -= N, action_count += 1) =====
        let mut member_new = member.clone();
        member_new.current_shares = member.current_shares - entry.share_count_to_pay_out;
        member_new.action_count = member.action_count + 1;
        crate::audited_update!(
            self,
            self.member_dao,
            entry.member_id,
            &member_new,
            REPAYMENT_ENTRY_PROCESS_MARK_PAID_OUT,
            &user_id,
            tx
        );

        // ===== Schritt 8: Re-Read Member (BL-01 Pattern — None → InternalError, NICHT EntityNotFound) =====
        // CR-01 Fix: Re-read to get the new version UUID generated by the DAO.
        // Pattern mirrors MemberServiceImpl::update (member.rs:343-348).
        // BL-01 Fix: Re-Read runs in the SAME transaction as the audited_update!
        // above — soft-delete in the same Tx is impossible (single-writer per
        // service method), so `None` here is an internal consistency error
        // (DAO regression, Tx-Isolation break, corrupted id), NOT a user-facing
        // "entity vanished" race. Map to InternalError → HTTP 500, never 404.
        let _member_refreshed = self
            .member_dao
            .find_by_id(entry.member_id, tx.clone())
            .await?
            .ok_or_else(|| {
                ServiceError::InternalError(Arc::from(format!(
                    "Re-Read after audited_update! returned None for Member {} — \
                     internal consistency error (same-tx invariant violated)",
                    entry.member_id
                )))
            })?;

        // ===== Schritt 9: audited_update! RepaymentEntry (status = PaidOut) =====
        let mut entry_new = entry.clone();
        entry_new.status = RepaymentEntryStatus::PaidOut;
        crate::audited_update!(
            self,
            self.repayment_entry_dao,
            id,
            &entry_new,
            REPAYMENT_ENTRY_PROCESS_MARK_PAID_OUT,
            &user_id,
            tx
        );

        // ===== Schritt 10: Re-Read RepaymentEntry (fuer Response.version + BL-01 Pattern) =====
        // CR-01 Fix: Re-read to get the new version UUID generated by the DAO.
        // BL-01 Fix: same-Tx invariant — None here is internal consistency error.
        let entry_refreshed = self
            .repayment_entry_dao
            .find_by_id(id, tx.clone())
            .await?
            .ok_or_else(|| {
                ServiceError::InternalError(Arc::from(format!(
                    "Re-Read after audited_update! returned None for RepaymentEntry {} — \
                     internal consistency error (same-tx invariant violated)",
                    id
                )))
            })?;

        // ===== Schritt 11: recalc_migrated (D-10 Option a, inline) =====
        // Pattern-Konsistenz mit MemberServiceImpl::update (Z. 341) und
        // MemberActionServiceImpl::create (Z. 346-348). compute_migration_status
        // hat eine bewusste Off-by-One-Konvention (action_count + 1) —
        // siehe member_action.rs:52; NICHT korrigieren.
        let actions_for_member = self
            .member_action_dao
            .find_by_member_id(entry.member_id, tx.clone())
            .await?;
        let member_after_update = self
            .member_dao
            .find_by_id(entry.member_id, tx.clone())
            .await?
            .ok_or_else(|| {
                ServiceError::InternalError(Arc::from(
                    "Member vanished before recalc_migrated — Tx-isolation broken",
                ))
            })?;
        let mig_status = crate::member_action::compute_migration_status(
            &member_after_update,
            &actions_for_member,
        );
        let migrated = mig_status.status
            == genossi_service::member_action::MigrationState::Migrated;
        self.member_dao
            .update_migrated(entry.member_id, migrated, tx.clone())
            .await?;

        // ===== Schritt 12: commit =====
        self.transaction_dao.commit(tx).await?;
        Ok(RepaymentEntry::from(&entry_refreshed))
    }
```

**WICHTIG zu Schritt 11 — Import-Pfad:** `genossi_service::member_action::MigrationState` ist der korrekte Pfad fuer die Enum-Variante; siehe `genossi_service_impl/src/member_action.rs` wo dasselbe als `MigrationState::Migrated` verwendet wird. Falls der Import-Pfad anders ist als oben, **GREP zuerst** `MigrationState::Migrated` in `genossi_service_impl/src/member_action.rs` und dem `genossi_service`-Crate, dann den passenden Pfad uebernehmen.

**Edit 2 — Tests-Modul-Erweiterung am Ende der Datei:**

(a) Import-Ergaenzung am Anfang des Tests-Moduls (suche nach `#[cfg(test)]` und `mod tests {` ca. Z. 514+):

```rust
use genossi_dao::member_action::{ActionType, MemberActionDao, MemberActionEntity};
```

(b) Neuer Mock-Block (nach den 5 bestehenden mock!{}-Blocks, ca. Z. 720+):

```rust
mock! {
    pub TestMemberActionDao {}
    #[async_trait]
    impl MemberActionDao for TestMemberActionDao {
        type Transaction = TestTransaction;
        async fn dump_all(
            &self,
            tx: TestTransaction,
        ) -> Result<Arc<[MemberActionEntity]>, DaoError>;
        async fn create(
            &self,
            entity: &MemberActionEntity,
            process: &str,
            tx: TestTransaction,
        ) -> Result<(), DaoError>;
        async fn update(
            &self,
            entity: &MemberActionEntity,
            process: &str,
            tx: TestTransaction,
        ) -> Result<(), DaoError>;
        async fn all(
            &self,
            tx: TestTransaction,
        ) -> Result<Arc<[MemberActionEntity]>, DaoError>;
        async fn find_by_id(
            &self,
            id: Uuid,
            tx: TestTransaction,
        ) -> Result<Option<MemberActionEntity>, DaoError>;
        async fn find_by_member_id(
            &self,
            member_id: Uuid,
            tx: TestTransaction,
        ) -> Result<Arc<[MemberActionEntity]>, DaoError>;
    }
}
```

(c) `TestDeps`-Struct erweitern (suche nach `impl RepaymentEntryServiceDeps for TestDeps`, ca. Z. 824-835). EINE neue Zeile zwischen `MemberDao` und `AuditLogDao`:

```rust
    type MemberDao = MockTestMemberDao;
    type MemberActionDao = MockTestMemberActionDao;
    type AuditLogDao = MockTestAuditLogDao;
```

(d) `build_service`-Helper erweitern (suche nach `fn build_service(`, ca. Z. 939+). Neuer Parameter `action_dao: MockTestMemberActionDao` UND neue Konstruktor-Zeile `member_action_dao: Arc::new(action_dao),`. Alle bestehenden Aufrufe von `build_service(...)` MUESSEN ergaenzt werden um einen Default-MockTestMemberActionDao-Argument (kann via `MockTestMemberActionDao::new()` instanziert werden, da bestehende Tests kein Mock-Behavior darauf erwarten — sie laufen Cascade-Pfade, die mark_paid_out NICHT touchen). Falls `make_member_dao_quiet`-Helper existiert, einen analog `make_member_action_dao_quiet`-Helper schreiben, der eine leere `find_by_member_id`-Response liefert.

Pragmatisch: Default-Behavior `MockTestMemberActionDao::new()` reicht; **wenn ein bestehender Test wegen fehlender Expectations fehlschlaegt, einen `.expect_*()`-Default mit `.times(0)`** auf den fehlenden Methoden setzen oder den Test um den neuen Parameter ergaenzen.

(e) 6 neue Unit-Tests anhaengen am Ende des Tests-Moduls (nach den bestehenden Tests, vor dem `}`-Block-Ende). **Sequence-Pattern aus `test_update_entry_status_open_to_contacted_succeeds` (Z. 1294-1360) wortgleich uebernehmen** — der Re-Read braucht eine `mockall::Sequence`, weil mehrere `find_by_id`-Calls in unterschiedlicher Reihenfolge unterschiedliche Werte zurueckliefern muessen.

Cascade hat folgende find_by_id-Sequenz (Reihenfolge fuer das Sequence-Setup):
1. `repayment_entry_dao.find_by_id(id)` → Entry pre-update (status=Open)
2. `repayment_phase_dao.find_by_id(phase_id)` → Phase (status=Open)
3. `member_dao.find_by_id(member_id)` → Member pre-update (current_shares=10, action_count=N)
4. `audited_create!` ruft intern `audit_log_dao.get_last_hash` + `audit_log_dao.create` (nicht find_by_id)
5. `member_action_dao.create(entity, process, tx)` → der DAO-Call
6. `audited_update!` ruft intern `member_dao.find_by_id` → Member pre-update (alte Version, gleicher Wert wie #3, NUR fuer audit-old-snapshot)
7. `member_dao.update(...)` → success
8. `audit_log_dao.create` (member-update-audit-rows)
9. `member_dao.find_by_id(member_id)` → Member POST-update Re-Read (Schritt 8, current_shares=7) — fuer die BL-01-test_*_rereads_member_none version, hier Ok(None) zurueckliefern
10. `audited_update!` Entry: `repayment_entry_dao.find_by_id(id)` → Entry pre-update (audit-old-snapshot)
11. `repayment_entry_dao.update(...)` → success
12. `audit_log_dao.create` (entry-update-audit-rows)
13. `repayment_entry_dao.find_by_id(id)` → Entry POST-update Re-Read (status=PaidOut, neue version)
14. `member_action_dao.find_by_member_id(member_id)` → Liste mit dem neuen Verkauf-Action
15. `member_dao.find_by_id(member_id)` → Member POST-update (current_shares=7)
16. `member_dao.update_migrated(member_id, ...)` → success
17. `transaction_dao.commit(tx)` → success

Test 1 — `test_mark_paid_out_happy_path`:
```rust
#[tokio::test]
async fn test_mark_paid_out_happy_path() {
    let entry_id = Uuid::new_v4();
    let member_id = Uuid::new_v4();
    let phase_id = Uuid::new_v4();
    // ... Setup Entry (status=Open, share_count_to_pay_out=3, member_id, phase_id), Phase (status=Open, fiscal_year=2026), Member (current_shares=10, action_count=2) ...
    // Sequence-Setup wie Z. 1294-1360-Pattern, mit den 17 erwarteten Mock-Calls oben.
    // Asserts: result.is_ok(), result.unwrap().status == RepaymentEntryStatus::PaidOut, neue version != alte version.
    // ... (volle Test-Implementation analog Phase-8-Pattern)
}
```

Test 2 — `test_mark_paid_out_rejects_paid_out_entry`:
- Setup: Entry mit status=PaidOut. Nur Schritte 1+2+3 werden ausgefuehrt; Step 3 returnt fruehzeitig mit Conflict.
- Assert: matches!(result, Err(ServiceError::Conflict(s)) if s.contains("already paid out")).
- Mock-Verify: `member_action_dao.expect_create().times(0)`, `member_dao.expect_update().times(0)`, `repayment_entry_dao.expect_update().times(0)`.

Test 3 — `test_mark_paid_out_rejects_when_phase_not_open`:
- Setup: Entry status=Open, Phase status=Preparation.
- Assert: matches!(result, Err(ServiceError::Conflict(s)) if s.contains("Phase status")).
- Mock-Verify: 0× create, 0× update, 0× commit.

Test 4 — `test_mark_paid_out_rejects_when_current_shares_insufficient`:
- Setup: Entry share_count_to_pay_out=5, Member current_shares=2.
- Assert: matches!(result, Err(ServiceError::ValidationError(items))) && items.len() == 1 && items[0].field.as_ref() == "share_count_to_pay_out" && items[0].message.contains("Member.current_shares (2)") && items[0].message.contains("share_count_to_pay_out (5)").
- Mock-Verify: 0× create, 0× update.

Test 5 — `test_mark_paid_out_rereads_member_none_yields_internal_error`:
- Setup: Cascade laeuft bis Schritt 9, dann `member_dao.find_by_id` (vierter oder so Aufruf in Sequence, je nach audited_update-internal-calls) gibt `Ok(None)`.
- Pattern wortgleich Phase 8 BL-01 Test (`test_update_repayment_entry_rereads_none_yields_internal_error`).
- Assert: matches!(result, Err(ServiceError::InternalError(s)) if s.contains("Re-Read") && s.contains("Member")).
- Mock-Verify: 1× audited_create auf member_action_dao, 1× audited_update auf member_dao — kein audited_update auf repayment_entry_dao (kommt erst NACH dem Re-Read).

Test 6 — `test_mark_paid_out_member_action_has_correct_fields`:
- Setup: Standard happy-path Setup, Phase.fiscal_year=2026, Entry.share_count_to_pay_out=3.
- `member_action_dao.expect_create().withf(|action: &MemberActionEntity, process: &str, _tx| { action.action_type == ActionType::Verkauf && action.shares_change == -3 && action.transfer_member_id.is_none() && action.effective_date.is_none() && action.comment.as_deref().map(|s| s.as_ref()) == Some("Anteils-Rueckzahlung Phase 2026") && process == "repayment-entry.mark-paid-out" })`.

**Aenderungen an bestehenden Tests:** Wenn `build_service(...)` einen neuen Parameter bekommt, MUESSEN alle bestehenden Aufrufer aktualisiert werden. Pragmatischste Loesung: `build_service` behaelt 4 Parameter und liefert die `member_action_dao` als `MockTestMemberActionDao::new()` ohne Expectations; eine zweite Variante `build_service_with_action_dao(entry, phase, member, action, perm)` fuer die neuen 6 Tests bauen. Das vermeidet Aenderungen an ~30 bestehenden Tests.
  </action>
  <verify>
    <automated>cargo test -p genossi_service_impl --lib repayment_entry 2>&amp;1 | grep -E "(test result|FAILED|test test_mark_paid_out)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - genossi_service_impl/src/repayment_entry.rs contains "async fn mark_paid_out("
    - genossi_service_impl/src/repayment_entry.rs::mark_paid_out function body contains "audited_create!" exactly once (grep filtered for the function body, NOT counting comments)
    - genossi_service_impl/src/repayment_entry.rs::mark_paid_out function body contains "audited_update!" exactly twice
    - genossi_service_impl/src/repayment_entry.rs contains "REPAYMENT_ENTRY_PROCESS_MARK_PAID_OUT" at least 4 times (1× const + 3× macro args + maybe more in tests; min. 4)
    - genossi_service_impl/src/repayment_entry.rs contains "ActionType::Verkauf"
    - genossi_service_impl/src/repayment_entry.rs contains "current_shares - entry.share_count_to_pay_out"
    - genossi_service_impl/src/repayment_entry.rs contains "action_count + 1"
    - genossi_service_impl/src/repayment_entry.rs contains "ServiceError::InternalError(Arc::from(format!(" (Re-Read None mapping — appears at least 2× for Member + Entry re-reads)
    - genossi_service_impl/src/repayment_entry.rs contains "MockTestMemberActionDao"
    - genossi_service_impl/src/repayment_entry.rs contains "test_mark_paid_out_happy_path"
    - genossi_service_impl/src/repayment_entry.rs contains "test_mark_paid_out_rejects_paid_out_entry"
    - genossi_service_impl/src/repayment_entry.rs contains "test_mark_paid_out_rejects_when_phase_not_open"
    - genossi_service_impl/src/repayment_entry.rs contains "test_mark_paid_out_rejects_when_current_shares_insufficient"
    - genossi_service_impl/src/repayment_entry.rs contains "test_mark_paid_out_rereads_member_none_yields_internal_error"
    - genossi_service_impl/src/repayment_entry.rs contains "test_mark_paid_out_member_action_has_correct_fields"
    - `cargo test -p genossi_service_impl --lib repayment_entry test_mark_paid_out_happy_path` exits 0
    - `cargo test -p genossi_service_impl --lib repayment_entry test_mark_paid_out_rejects_paid_out_entry` exits 0
    - `cargo test -p genossi_service_impl --lib repayment_entry test_mark_paid_out_rejects_when_phase_not_open` exits 0
    - `cargo test -p genossi_service_impl --lib repayment_entry test_mark_paid_out_rejects_when_current_shares_insufficient` exits 0
    - `cargo test -p genossi_service_impl --lib repayment_entry test_mark_paid_out_rereads_member_none_yields_internal_error` exits 0
    - `cargo test -p genossi_service_impl --lib repayment_entry test_mark_paid_out_member_action_has_correct_fields` exits 0
    - Audit-Disziplin grep (per BL-01 fix in Phase 8): `grep -v '^//' genossi_service_impl/src/repayment_entry.rs | grep -v '^\s*//' | grep -E "self\.(member|member_action|repayment_entry|repayment_phase)_dao\.(create|update)\(" | wc -l` outputs 0 (alle Schreib-Calls gehen ueber audited_*!-Macros; reine find_by_id und update_migrated sind hier nicht gefangen)
    - Alle bestehenden Tests in `genossi_service_impl::repayment_entry::tests` weiterhin gruen (kein Regress): `cargo test -p genossi_service_impl --lib repayment_entry 2>&amp;1 | grep "test result"` enthaelt "0 failed"
  </acceptance_criteria>
  <done>
    - mark_paid_out-Cascade vollstaendig implementiert mit allen 12 Schritten aus RESEARCH §Cascade Walkthrough.
    - 6 neue Unit-Tests gruen; alle bestehenden Tests in der Datei weiterhin gruen.
    - TestMemberActionDao-Mock + TestDeps + build_service-Helper-Erweiterung kompilieren.
    - genossi_service_impl kompiliert clean; genossi_bin kompiliert noch NICHT (Plan 09-03 heilt das).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REST-Client → REST-Handler | Untrusted POST `/api/repayment-entry/{id}/mark-paid-out` aus Browser/curl |
| REST-Handler → Service | Trusted, aber ohne Permission-Check waere Eskalation moeglich |
| Service → DAO | Trusted (DAO ist im selben Prozess); aber audited_*!-Macros sind die einzige Schreib-Erlaubnis-Bahn |
| Service → audited_*!-Macros → AuditLogDao | Vertrauensbasis fuer die Hash-Chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01-01 | Elevation of Privilege | mark_paid_out (Service) | MEDIUM | mitigate | `self.permission_service.check_permission(ADMIN_PRIVILEGE, context).await?` als ZWEITER Schritt (nach `current_user_id`), VOR jedem DAO-Read und VOR allen audited_*!-Calls. Pattern-Anker `update_repayment_entry` (`repayment_entry.rs:175-184`). Acceptance: grep `check_permission(ADMIN_PRIVILEGE` in der mark_paid_out-Funktion muss exakt 1× matchen, und MUSS vor dem ersten `find_by_id`-Call stehen. Wenn fehlend → unauthenticated users koennten Auszahlungen ausloesen. |
| T-09-01-02 | Repudiation (Audit-Trail-Bypass) | mark_paid_out (Service) | HIGH | mitigate | BLOCKING acceptance: `genossi_service_impl/src/repayment_entry.rs::mark_paid_out function body contains "audited_create!" exactly once and "audited_update!" exactly twice` (siehe acceptance_criteria Task 2). Direkte DAO-create/update-Calls auf Member/MemberAction/RepaymentEntry-DAOs sind verboten (Audit-Disziplin Grep-Gate). Wenn Executor versehentlich `self.member_dao.update(...)` schreibt statt `audited_update!`, geht der Audit-Trail verloren und Hash-Chain bleibt valide aber unvollstaendig — keine Spur des Schreibvorgangs in `/api/audit/verify`-Result. |
| T-09-01-03 | Tampering / Concurrency (Race-Double-Payout) | mark_paid_out (Service + DAO) | HIGH | mitigate | Optimistic-Lock auf RepaymentEntry-Update via `UPDATE ... WHERE id = ? AND version = ?` im DAO (`genossi_dao_impl_sqlite/src/repayment_entry.rs:160-180`). Bei `rows_affected == 0` → `DaoError::ConflictError("Version mismatch")` → `ServiceError::Conflict` → HTTP 409. Plan 09-04 verifiziert end-to-end via `tokio::join!` (SC #5). Wenn fehlend → zwei parallele Auszahlungen wuerden zweimal MemberAction::Verkauf erzeugen und current_shares zweimal reduzieren (negativer Wert moeglich). |
| T-09-01-04 | Validation Bypass (PAYO-03) | mark_paid_out (Service) | HIGH | mitigate | Inline-Validation `member.current_shares < entry.share_count_to_pay_out` VOR `audited_create!`/`audited_update!`-Aufrufen. BLOCKING acceptance: Cascade-Reihenfolge ist Member-Load → ValidationError-Return → erst dann audited_create! (Step 5 muss VOR Step 6 stehen). Test 4 (`test_mark_paid_out_rejects_when_current_shares_insufficient`) verifiziert mit Mock-Expectation `times(0)` auf create+update. Wenn fehlend → current_shares koennte negativ werden, MemberAction::Verkauf koennte fuer nicht-vorhandene Anteile gebucht werden. |
| T-09-01-05 | Information Disclosure (Error Message Leak) | mark_paid_out → ValidationError | LOW | mitigate | Error-Message referenziert NUR das aktuelle Member (`Member.current_shares ({current}) is less than entry.share_count_to_pay_out ({requested})`) — keine PII (Name, Email, Mitgliedsnummer), keine cross-member-Daten. CONTEXT D-14 spezifiziert das Message-Format. |

</threat_model>

<verification>

Build clean:
```bash
cargo build -p genossi_service
cargo build -p genossi_service_impl
# Note: cargo build -p genossi_bin WILL FAIL after this plan; Plan 09-03 fixes it.
```

Unit-Tests gruen:
```bash
cargo test -p genossi_service repayment_entry::tests
cargo test -p genossi_service_impl --lib repayment_entry
```

Audit-Disziplin-Grep (BL-01 Phase 8 Pattern, filtert Kommentare):
```bash
# Direkte schreibende DAO-Calls ausserhalb audited_*!-Macros (Comment-filtered):
grep -v '^//' /home/neosam/programming/rust/projects/genossi3/genossi_service_impl/src/repayment_entry.rs \
  | grep -v '^\s*//' \
  | grep -E "self\.(member|member_action|repayment_entry|repayment_phase)_dao\.(create|update)\(" \
  | wc -l
# Expected: 0 (alle Schreibvorgaenge nur via audited_*!-Macros)
```

Cascade-Marker-Greps:
```bash
grep -c "audited_create!" /home/neosam/programming/rust/projects/genossi3/genossi_service_impl/src/repayment_entry.rs
# Expected: 2 (bereits 1 fuer create_repayment_entry, jetzt +1 fuer mark_paid_out)
grep -c "REPAYMENT_ENTRY_PROCESS_MARK_PAID_OUT" /home/neosam/programming/rust/projects/genossi3/genossi_service_impl/src/repayment_entry.rs
# Expected: >= 4 (1 const + 3 macro args; ggf. mehr in Tests)
```

</verification>

<success_criteria>

- [ ] Trait `RepaymentEntryService::mark_paid_out` exposed
- [ ] Service-Impl `mark_paid_out` implementiert mit 12-Schritt-Cascade
- [ ] 3 audited_*!-Calls mit gemeinsamem process-String (SC #3)
- [ ] PAYO-03 Validation aktiv (SC #2)
- [ ] PAYO-04 Status-Guard aktiv (SC #4)
- [ ] BL-01 Re-Read-None → InternalError (kein 404)
- [ ] compute_migration_status `pub`
- [ ] MemberActionDao als 8. Dep registriert
- [ ] 6 neue Unit-Tests, alle gruen
- [ ] Keine direkten Schreib-DAO-Calls ausserhalb audited_*! (Grep-Gate 0)
- [ ] Compile-Test-Mock fuer `mark_paid_out` ergaenzt

</success_criteria>

<output>
Nach Plan-Ausfuehrung: `.planning/phases/09-auszahlungs-buchung-atomisch-auditiert/09-01-SUMMARY.md` mit Diff-Statistik, Auflistung aller 6 Tests + Resultat, Audit-Disziplin-Grep-Output, und Begruendung etwaiger Abweichungen vom Plan.
</output>
