---
phase: 04-frontend-component-first-mit-qr-scanner-und-manual-code-fall
plan: 08
type: execute
wave: 3
depends_on: [03, 04, 06, 06b, 07]
files_modified:
  - genossi-frontend/src/page/assemblies.rs
  - genossi-frontend/src/page/assembly_details.rs
autonomous: true
requirements: [SYNC-01]
must_haves:
  truths:
    - "Vorstand kann auf /assemblies eine Liste aller GVs sehen + neue GV per Modal anlegen"
    - "Vorstand kann auf /assemblies/{id} drei Tabs nutzen: Stamm-Daten (mit Edit + Open/Close), Helfer-Tokens (List + Create-Modal mit One-Time-QrCard), Anwesenheit (3 shared Components — ATTN-06 Reuse)"
    - "GV-Lifecycle-Buttons (`GV öffnen`/`GV schließen`) zeigen Confirmation-Dialog vor Action"
    - "Token-Create-Response wird sofort als <QrCard> inline gerendert (qr_svg + code nur einmal verfügbar — Phase 2 D-21)"
    - "Anwesenheits-Tab nutzt EXAKT dieselben Components wie /helper/attendance (ATTN-06 Component-Reuse, KEIN inline-RSX-Duplikat)"
    - "Beide Pages sind hinter <RequirePrivilege privilege=\"admin\"> — Vorstand-only"
    - "SYNC-01 acceptance: refresh_signal wird nach Toggle-200-OK incrementiert; LiveCounter polled alle 5s"
  artifacts:
    - path: "genossi-frontend/src/page/assemblies.rs"
      provides: "Assemblies List-Page mit RequirePrivilege + Create-Modal"
      contains: "pub fn Assemblies, RequirePrivilege"
      min_lines: 80
    - path: "genossi-frontend/src/page/assembly_details.rs"
      provides: "AssemblyDetails 3-Tab-Page mit Open/Close + Token-Create + Anwesenheits-Tab (shared Components)"
      contains: "pub fn AssemblyDetails, TabStrip, AttendanceList"
      min_lines: 200
  key_links:
    - from: "assembly_details.rs Anwesenheits-Tab"
      to: "AttendanceList + AttendanceSearch + LiveCounter (Plan 04)"
      via: "Component-First reuse — same imports as helper_attendance.rs (Plan 09)"
      pattern: "AttendanceList.*AttendanceSearch.*LiveCounter"
    - from: "assembly_details.rs Tokens-Tab"
      to: "QrCard (Plan 05) für just-created Token"
      via: "Inline-Render mit One-Time HelperTokenCreateResponseTO"
      pattern: "QrCard.*qr_svg.*code"
    - from: "assemblies.rs"
      to: "AssemblyListRow (Plan 06)"
      via: "List rendering"
      pattern: "AssemblyListRow"
---

<objective>
Beide Vorstand-Pages ausimplementieren: `/assemblies` (Liste + Create-Modal) und `/assemblies/{id}` (3-Tab-Detail-Page). Anwesenheits-Tab reused EXAKT die Components aus Plan 04 (ATTN-06 Component-Reuse, keine inline-RSX-Duplikation).

**W-04 Component-Extraction:** Die im Original-Plan inline-deklarierten Sub-Components (TokenRow, CreateTokenForm, BasicsTab) sind in Plan 06 als eigene Components extrahiert (Component-First-Konvention). Plan 08 referenziert sie via `<TokenRow ...>`, `<CreateTokenForm ...>`, `<BasicsTab ...>`.

**W-01 update_assembly:** BasicsTab (Plan 06 W-04) implementiert Edit-Mode für GV-Stamm-Daten via D-22 update_assembly (Preparation-only). Plan 08 ruft den Component nur auf — die Edit-Logik liegt in Plan 06 BasicsTab.

Purpose: Vorstand-UI liefern; Component-First-Konsistenz (Anwesenheits-Tab = `/helper/attendance` Body); SYNC-01 acceptance via LiveCounter + refresh_signal.
Output: 2 Page-Files vollständig ausimplementiert; je ~150-250 Zeilen Komposition.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/neosam/programming/rust/projects/genossi3/.planning/phases/04-frontend-component-first-mit-qr-scanner-und-manual-code-fall/04-CONTEXT.md
@/home/neosam/programming/rust/projects/genossi3/.planning/phases/04-frontend-component-first-mit-qr-scanner-und-manual-code-fall/04-PATTERNS.md
@/home/neosam/programming/rust/projects/genossi3/.planning/phases/04-frontend-component-first-mit-qr-scanner-und-manual-code-fall/04-UI-SPEC.md
@/home/neosam/programming/rust/projects/genossi3/genossi-frontend/CLAUDE.md
@/home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/api.rs
@/home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/auth.rs
@/home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/component/modal.rs
@/home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/applications_page.rs
@/home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/access_denied.rs

<interfaces>
**Imports für beide Pages:**
```rust
use dioxus::prelude::*;
use std::str::FromStr;
use uuid::Uuid;

use crate::api::{
    self, AppError, AssemblyTO, AssemblyStatusTO, CreateAssemblyRequest,
    UpdateAssemblyRequest, HelperTokenTO, HelperTokenStatusTO,
    HelperTokenCreateResponseTO, CreateHelperTokenRequest,
};
use crate::auth::RequirePrivilege;
use crate::component::{
    AssemblyListRow, AssemblyStatusBadge, TabStrip, TabDef,
    QrCard, AttendanceList, AttendanceSearch, LiveCounter, ConnState,
    ConnectionBanner, ToastContainer, show_toast,
    BasicsTab, TokenRow, CreateTokenForm,    // W-04: extracted in Plan 06
    Modal, // existing modal.rs
};
use crate::component::TopBar;
use crate::i18n::{use_i18n, Key};
use crate::page::access_denied::AccessDeniedPage;
use crate::router::Route;
use crate::service::config::CONFIG;
```

**Pattern-Anker:**
- `genossi-frontend/src/page/applications_page.rs:48-150` — Composition + RequirePrivilege + Tabs (Vorlage; Plan 08 ersetzt inline-Tabs durch `<TabStrip>`)
- `genossi-frontend/src/component/application_create_form.rs:46-111` — Form-Modal-Pattern für Create-Forms
- `genossi-frontend/src/component/modal.rs` — Modal-Component
- `genossi-frontend/src/auth.rs:35-48` — RequirePrivilege

**UI-SPEC-Verweise:**
- §`/assemblies` (Assemblies list page) — Layout
- §`/assemblies/{id}` (AssemblyDetails) — 3-Tab + Open/Close Confirms

**Tab-Strategy (UI-SPEC + RESEARCH):**
- Active-Tab-Key in lokalem `use_signal::<String>("basics")`
- TabDef-List: `[("basics", AssemblyTabBasics), ("tokens", AssemblyTabTokens), ("attendance", AssemblyTabAttendance)]`
- Body-Branching basiert auf `match active_key.as_str() { "basics" => ..., "tokens" => ..., "attendance" => ... }`
- Anwesenheits-Tab nur wenn `assembly.status != Preparation`; vor Open: Hinweis-Text via `i18n.t(Key::AssemblyAttendanceNotOpenYet)`

**Just-Created-Token-Pattern (Phase 2 D-21):**
- `let mut just_created = use_signal(|| Option::<HelperTokenCreateResponseTO>::None);`
- Nach POST: `just_created.set(Some(response));`
- Inline rendern als `<QrCard>` (NICHT in Modal — User soll printen können)
- Bei "Schließen"-Klick: `just_created.set(None);` — danach unrecoverable (Backend speichert nicht persistent)
</interfaces>
</context>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-30 | E (Elevation of Privilege) | Vorstand-Pages ohne admin-Privilege | mitigate | Beide Pages wrappen Body mit `<RequirePrivilege privilege="admin" fallback={AccessDeniedPage}>` (existing pattern) |
| T-04-31 | T (Tampering) | XSS via assembly.name oder memo | mitigate | Dioxus escapes RSX-children by default; KEIN dangerous_inner_html in den Pages (nur QrCard hat es, qr_svg von Backend) |
| T-04-32 | I (Information Disclosure) | Token-Code/QR-SVG nach Modal-Close zugänglich | mitigate | `just_created.set(None)` löscht Code/QR aus Frontend-Memory; Backend speichert nicht (Phase 2 D-11). Vorstand muss Token re-create wenn verloren. |
| T-04-33 | T (Tampering) | Race zwischen Open-Klick und Status-Display | accept | Backend rejected Open auf nicht-Preparation-Status mit 409; Frontend zeigt deutsche Toast-Message via status_to_message |
| T-04-34 | I (Information Disclosure) | AttendanceList in Vorstand-Tab zeigt mehr als 5 Felder | mitigate | Plan 04 AttendanceList ist die SAME Component — render-Logik garantiert 5 Felder; Plan 10 grep-verifiziert |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Assemblies List-Page mit Create-Modal</name>
  <files>
    genossi-frontend/src/page/assemblies.rs
  </files>
  <read_first>
    - /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/applications_page.rs:1-150 (Liste + Modal-Pattern)
    - /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/component/application_create_form.rs:30-115 (Form-Pattern)
    - /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/component/modal.rs (Modal-Skelett)
    - /home/neosam/programming/rust/projects/genossi3/.planning/phases/04-frontend-component-first-mit-qr-scanner-und-manual-code-fall/04-UI-SPEC.md:610-619 (Assemblies list page Layout)
  </read_first>
  <action>
    **Komplett ersetzen** des Stub-Inhalts von `genossi-frontend/src/page/assemblies.rs`:
    ```rust
    //! Assemblies list page (Phase 4 Plan 08) — admin-only.
    //!
    //! Pattern aus applications_page.rs:48-150 (Liste + Create-Modal); replaces inline-rsx
    //! list-rendering with <AssemblyListRow> (Component-First).

    use dioxus::prelude::*;

    use crate::api::{self, AssemblyTO, CreateAssemblyRequest};
    use crate::auth::RequirePrivilege;
    use crate::component::{
        AssemblyListRow, Modal, ToastContainer, TopBar, show_toast,
    };
    use crate::i18n::{use_i18n, Key};
    use crate::page::access_denied::AccessDeniedPage;
    use crate::service::config::CONFIG;

    #[component]
    pub fn Assemblies() -> Element {
        let i18n = use_i18n();

        let mut assemblies = use_signal(|| Vec::<AssemblyTO>::new());
        let mut loading = use_signal(|| true);
        let mut show_create = use_signal(|| false);
        let mut toast_messages = use_signal(|| Vec::<(u64, String)>::new());
        let mut toast_counter = use_signal(|| 0u64);

        let load = move || {
            spawn(async move {
                loading.set(true);
                let config = CONFIG.read().clone();
                match api::list_assemblies(&config).await {
                    Ok(list) => assemblies.set(list),
                    Err(e) => show_toast(&mut toast_messages, &mut toast_counter, e.message),
                }
                loading.set(false);
            });
        };

        use_effect(move || { load(); });

        rsx! {
            RequirePrivilege {
                privilege: "admin",
                fallback: rsx! { AccessDeniedPage { required_privilege: "admin".to_string() } },
                TopBar {}
                div { class: "container mx-auto px-4 py-6",
                    div { class: "flex justify-between items-start mb-4",
                        h1 { class: "text-2xl font-bold mb-1", "{i18n.t(Key::Assemblies)}" }
                        button {
                            class: "bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded text-sm min-h-[44px]",
                            onclick: move |_| show_create.set(true),
                            "{i18n.t(Key::AssemblyCreate)}"
                        }
                    }

                    if *loading.read() {
                        p { class: "text-gray-500 text-center py-8", "{i18n.t(Key::Loading)}" }
                    } else if assemblies.read().is_empty() {
                        div { class: "text-center py-12",
                            p { class: "text-lg font-medium text-gray-700", "{i18n.t(Key::AssemblyEmpty)}" }
                            p { class: "text-sm text-gray-500 mt-2 mb-6", "{i18n.t(Key::AssemblyEmptyHint)}" }
                            button {
                                class: "bg-blue-600 hover:bg-blue-700 text-white px-6 py-3 rounded min-h-[44px]",
                                onclick: move |_| show_create.set(true),
                                "{i18n.t(Key::AssemblyCreate)}"
                            }
                        }
                    } else {
                        div { class: "flex flex-col",
                            for a in assemblies.read().iter() {
                                AssemblyListRow { key: "{a.id}", assembly: a.clone() }
                            }
                        }
                    }

                    if *show_create.read() {
                        Modal {
                            CreateAssemblyForm {
                                on_close: move |_| show_create.set(false),
                                on_created: move |_| {
                                    show_create.set(false);
                                    load();
                                },
                                on_error: move |msg: String| show_toast(&mut toast_messages, &mut toast_counter, msg),
                            }
                        }
                    }
                }
                ToastContainer { messages: toast_messages.into() }
            }
        }
    }

    #[component]
    fn CreateAssemblyForm(
        on_close: EventHandler<()>,
        on_created: EventHandler<()>,
        on_error: EventHandler<String>,
    ) -> Element {
        let i18n = use_i18n();
        let mut name = use_signal(String::new);
        let mut date_str = use_signal(String::new);
        let mut location = use_signal(String::new);
        let mut submitting = use_signal(|| false);

        rsx! {
            form {
                class: "flex flex-col gap-4",
                onsubmit: move |e| {
                    e.prevent_default();
                    if name.read().trim().is_empty() {
                        on_error.call(i18n.t(Key::AssemblyName).to_string());
                        return;
                    }
                    submitting.set(true);
                    let req = CreateAssemblyRequest {
                        name: name.read().trim().to_string(),
                        date: { let s = date_str.read().trim().to_string(); if s.is_empty() { None } else { Some(s) } },
                        location: { let s = location.read().trim().to_string(); if s.is_empty() { None } else { Some(s) } },
                    };
                    let on_created = on_created;
                    let on_error = on_error;
                    spawn(async move {
                        let config = CONFIG.read().clone();
                        match api::create_assembly(&config, &req).await {
                            Ok(_) => on_created.call(()),
                            Err(e) => on_error.call(e.message),
                        }
                        submitting.set(false);
                    });
                },
                h2 { class: "text-xl font-semibold", "{i18n.t(Key::AssemblyCreate)}" }
                label { class: "flex flex-col gap-1",
                    span { class: "text-sm text-gray-700", "{i18n.t(Key::AssemblyName)}" }
                    input {
                        class: "border border-gray-300 rounded px-3 py-2",
                        r#type: "text",
                        value: "{name}",
                        oninput: move |e| name.set(e.value()),
                    }
                }
                label { class: "flex flex-col gap-1",
                    span { class: "text-sm text-gray-700", "{i18n.t(Key::AssemblyDate)}" }
                    input {
                        class: "border border-gray-300 rounded px-3 py-2",
                        r#type: "datetime-local",
                        value: "{date_str}",
                        oninput: move |e| date_str.set(e.value()),
                    }
                }
                label { class: "flex flex-col gap-1",
                    span { class: "text-sm text-gray-700", "{i18n.t(Key::AssemblyLocation)}" }
                    input {
                        class: "border border-gray-300 rounded px-3 py-2",
                        r#type: "text",
                        value: "{location}",
                        oninput: move |e| location.set(e.value()),
                    }
                }
                div { class: "flex gap-2 justify-end mt-2",
                    button {
                        r#type: "button",
                        class: "px-4 py-2 text-gray-700 hover:bg-gray-100 rounded min-h-[44px]",
                        onclick: move |_| on_close.call(()),
                        "{i18n.t(Key::Cancel)}"
                    }
                    button {
                        r#type: "submit",
                        class: "bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded disabled:opacity-50 min-h-[44px]",
                        disabled: *submitting.read(),
                        "{i18n.t(Key::Save)}"
                    }
                }
            }
        }
    }
    ```

    Commit: `feat(04-08): implement Assemblies list page with create modal`
  </action>
  <verify>
    <automated>cargo build -p genossi-frontend 2>&1 | tail -3 && grep -q "RequirePrivilege" /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/assemblies.rs && grep -q "AssemblyListRow" /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/assemblies.rs && grep -q "create_assembly" /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/assemblies.rs && grep -q "ToastContainer" /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/assemblies.rs && grep -q "show_create" /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/assemblies.rs && grep -q 'privilege: "admin"' /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/assemblies.rs</automated>
  </verify>
  <done>Assemblies-Page wrapped mit RequirePrivilege="admin"; rendert AssemblyListRow + Create-Modal-Form; Toast-Container für Errors; Build grün.</done>
</task>

<task type="auto">
  <name>Task 2: AssemblyDetails 3-Tab-Page mit Open/Close + Token-Create + Anwesenheits-Tab</name>
  <files>
    genossi-frontend/src/page/assembly_details.rs
  </files>
  <read_first>
    - /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/applications_page.rs:75-110 (Tab-Pattern als ehemalige Vorlage — wird durch <TabStrip> ersetzt)
    - /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/component/modal.rs (Confirm-Dialog-Pattern)
    - /home/neosam/programming/rust/projects/genossi3/.planning/phases/04-frontend-component-first-mit-qr-scanner-und-manual-code-fall/04-UI-SPEC.md:621-631 (AssemblyDetails — 3-Tab + Status-Branching)
    - /home/neosam/programming/rust/projects/genossi3/.planning/phases/04-frontend-component-first-mit-qr-scanner-und-manual-code-fall/04-CONTEXT.md (D-08, D-10)
  </read_first>
  <action>
    **Komplett ersetzen** von `genossi-frontend/src/page/assembly_details.rs` mit der folgenden W-04-aware Struktur. KEINE inline-Definitionen von `BasicsTab`, `TokenRow`, `CreateTokenForm` — diese werden aus `crate::component::{BasicsTab, TokenRow, CreateTokenForm}` importiert (Plan 06 Task 3 hat sie als eigene Components angelegt).

    ```rust
    //! AssemblyDetails 3-tab page (Phase 4 Plan 08) — admin-only.
    //!
    //! 3 Tabs (Stamm-Daten / Helfer-Tokens / Anwesenheit). The Anwesenheit-Tab
    //! re-uses the EXACT components from /helper/attendance — ATTN-06 Component-First.
    //!
    //! W-04: BasicsTab + TokenRow + CreateTokenForm sind in Plan 06 als eigene Components
    //! extrahiert. Plan 08 importiert sie via `crate::component::*` und ruft sie auf —
    //! KEINE inline-Definitionen. Der einzige inline-helper hier ist `TokensTab`, weil er
    //! als Page-spezifischer Tab-Body-Wrapper der Liste + Create-Modal koordiniert.

    use dioxus::prelude::*;
    use std::str::FromStr;
    use uuid::Uuid;

    use crate::api::{
        self, AssemblyStatusTO, AssemblyTO, HelperTokenCreateResponseTO, HelperTokenTO,
    };
    use crate::auth::RequirePrivilege;
    use crate::component::{
        AssemblyStatusBadge, AttendanceList, AttendanceSearch, BasicsTab, ConnState,
        ConnectionBanner, CreateTokenForm, LiveCounter, Modal, QrCard, TabDef, TabStrip,
        TokenRow, ToastContainer, TopBar, show_toast,
    };
    use crate::i18n::{use_i18n, Key};
    use crate::page::access_denied::AccessDeniedPage;
    use crate::service::config::CONFIG;

    #[component]
    pub fn AssemblyDetails(id: String) -> Element {
        let i18n = use_i18n();
        let assembly_id = match Uuid::from_str(&id) {
            Ok(u) => u,
            Err(_) => return rsx! { div { class: "p-4 text-red-600", "Invalid assembly id" } },
        };

        let mut assembly = use_signal(|| Option::<AssemblyTO>::None);
        let mut loading = use_signal(|| true);
        let mut active_tab = use_signal(|| "basics".to_string());
        let mut toast_messages = use_signal(|| Vec::<(u64, String)>::new());
        let mut toast_counter = use_signal(|| 0u64);
        let mut conn_lost = use_signal(|| false);
        let mut refresh_signal = use_signal(|| 0u64);

        let load = move || {
            spawn(async move {
                loading.set(true);
                let config = CONFIG.read().clone();
                match api::get_assembly(&config, assembly_id).await {
                    Ok(a) => assembly.set(Some(a)),
                    Err(e) => show_toast(&mut toast_messages, &mut toast_counter, e.message),
                }
                loading.set(false);
            });
        };

        use_effect(move || { load(); });

        rsx! {
            RequirePrivilege {
                privilege: "admin",
                fallback: rsx! { AccessDeniedPage { required_privilege: "admin".to_string() } },
                TopBar {}
                div { class: "container mx-auto px-4 py-6",
                    if *loading.read() {
                        p { class: "text-gray-500", "{i18n.t(Key::Loading)}" }
                    } else if let Some(a) = assembly.read().clone() {
                        // Header
                        div { class: "flex items-center justify-between mb-4",
                            h1 { class: "text-2xl font-bold", "{a.name}" }
                            AssemblyStatusBadge { status: a.status.clone() }
                        }
                        // Tabs
                        TabStrip {
                            tabs: vec![
                                TabDef { key: "basics", label: i18n.t(Key::AssemblyTabBasics).to_string() },
                                TabDef { key: "tokens", label: i18n.t(Key::AssemblyTabTokens).to_string() },
                                TabDef { key: "attendance", label: i18n.t(Key::AssemblyTabAttendance).to_string() },
                            ],
                            active_key: active_tab.read().clone(),
                            on_change: move |k: String| active_tab.set(k),
                            // Body branched on active key — DELEGATES to extracted components.
                            match active_tab.read().as_str() {
                                "basics" => rsx! {
                                    // W-04 + W-01: BasicsTab handles ReadOnly/Edit toggle + open/close confirms.
                                    BasicsTab {
                                        assembly: a.clone(),
                                        on_changed: move |_| load(),
                                        on_error: move |msg: String| show_toast(&mut toast_messages, &mut toast_counter, msg),
                                    }
                                },
                                "tokens" => rsx! {
                                    TokensTab {
                                        assembly_id: assembly_id,
                                        on_error: move |msg: String| show_toast(&mut toast_messages, &mut toast_counter, msg),
                                    }
                                },
                                "attendance" => match a.status {
                                    AssemblyStatusTO::Preparation => rsx! {
                                        div { class: "text-center py-12 text-gray-500",
                                            "{i18n.t(Key::AssemblyAttendanceNotOpenYet)}"
                                        }
                                    },
                                    _ => {
                                        let polling_enabled = matches!(a.status, AssemblyStatusTO::Open);
                                        let read_only = false;
                                        let mut search_query = use_signal(String::new);
                                        rsx! {
                                            ConnectionBanner { visible: *conn_lost.read() }
                                            LiveCounter {
                                                assembly_id: assembly_id,
                                                polling_enabled: polling_enabled,
                                                on_connection_state: move |s: ConnState| {
                                                    conn_lost.set(matches!(s, ConnState::Lost));
                                                },
                                            }
                                            AttendanceSearch {
                                                on_change: move |q: String| search_query.set(q),
                                            }
                                            AttendanceList {
                                                assembly_id: assembly_id,
                                                search_query: search_query.into(),
                                                read_only: read_only,
                                                refresh_signal: refresh_signal.into(),
                                                on_toggle_success: move |_| {
                                                    refresh_signal.with_mut(|n| *n += 1);
                                                },
                                            }
                                        }
                                    }
                                },
                                _ => rsx! {},
                            }
                        }
                    }
                }
                ToastContainer { messages: toast_messages.into() }
            }
        }
    }

    // ─── TokensTab (Page-internal, NOT extracted) ──────────────────────────
    // TokensTab orchestriert die Token-Liste + Create-Modal + Just-Created-QrCard.
    // Sie nutzt die extrahierten W-04 Components TokenRow + CreateTokenForm.
    // Inline bleibt sie weil sie page-spezifischen state hält (`just_created` —
    // One-Time-Show via Phase 2 D-21).

    #[component]
    fn TokensTab(
        assembly_id: Uuid,
        on_error: EventHandler<String>,
    ) -> Element {
        let i18n = use_i18n();
        let mut tokens = use_signal(|| Vec::<HelperTokenTO>::new());
        let mut loading = use_signal(|| true);
        let mut show_create = use_signal(|| false);
        let mut just_created = use_signal(|| Option::<HelperTokenCreateResponseTO>::None);

        let load = move || {
            spawn(async move {
                loading.set(true);
                let config = CONFIG.read().clone();
                match api::list_helper_tokens(&config, assembly_id).await {
                    Ok(list) => tokens.set(list),
                    Err(e) => on_error.call(e.message),
                }
                loading.set(false);
            });
        };

        use_effect(move || { load(); });

        rsx! {
            div { class: "flex justify-between items-start mb-4",
                h2 { class: "text-xl font-semibold", "{i18n.t(Key::HelperTokens)}" }
                button {
                    class: "bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded min-h-[44px]",
                    onclick: move |_| show_create.set(true),
                    "{i18n.t(Key::HelperTokenCreate)}"
                }
            }

            // Just-created card (One-Time-Show — Phase 2 D-21)
            if let Some(resp) = just_created.read().clone() {
                div { class: "mb-6",
                    QrCard {
                        memo: resp.token.memo.clone(),
                        code: resp.code.clone(),
                        qr_svg: resp.qr_svg.clone(),
                    }
                    div { class: "flex justify-center mt-2",
                        button {
                            class: "text-sm text-gray-600 underline min-h-[44px] px-4",
                            onclick: move |_| just_created.set(None),
                            "{i18n.t(Key::Close)}"
                        }
                    }
                }
            }

            if *loading.read() {
                p { "{i18n.t(Key::Loading)}" }
            } else if tokens.read().is_empty() {
                div { class: "text-center py-12",
                    p { class: "text-lg font-medium text-gray-700", "{i18n.t(Key::HelperTokenEmpty)}" }
                    p { class: "text-sm text-gray-500 mt-2", "{i18n.t(Key::HelperTokenEmptyHint)}" }
                }
            } else {
                div { class: "flex flex-col gap-2",
                    for t in tokens.read().iter() {
                        // W-04: TokenRow is extracted to Plan 06 (crate::component::TokenRow).
                        TokenRow {
                            key: "{t.id}",
                            token: t.clone(),
                            assembly_id: assembly_id,
                            on_changed: load,
                            on_error: on_error,
                        }
                    }
                }
            }

            if *show_create.read() {
                Modal {
                    // W-04: CreateTokenForm is extracted to Plan 06 (crate::component::CreateTokenForm).
                    CreateTokenForm {
                        assembly_id: assembly_id,
                        on_close: move |_| show_create.set(false),
                        on_created: move |resp: HelperTokenCreateResponseTO| {
                            show_create.set(false);
                            just_created.set(Some(resp));
                            load();
                        },
                        on_error: on_error,
                    }
                }
            }
        }
    }
    ```

    Commit: `feat(04-08): AssemblyDetails 3-tab page reuses Plan-06 BasicsTab/TokenRow/CreateTokenForm (W-04, W-01, ATTN-06)`  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/genossi3/genossi-frontend && cargo build 2>&1 | tail -5 && grep -q "RequirePrivilege" src/page/assembly_details.rs && grep -q "TabStrip" src/page/assembly_details.rs && grep -q "AttendanceList" src/page/assembly_details.rs && grep -q "AttendanceSearch" src/page/assembly_details.rs && grep -q "LiveCounter" src/page/assembly_details.rs && grep -q "QrCard" src/page/assembly_details.rs && grep -q "ConnectionBanner" src/page/assembly_details.rs && grep -q "just_created" src/page/assembly_details.rs && grep -q "BasicsTab" src/page/assembly_details.rs && grep -q "TokenRow" src/page/assembly_details.rs && grep -q "CreateTokenForm" src/page/assembly_details.rs && (! grep -E '^\s*fn BasicsTab\(|^\s*fn TokenRow\(|^\s*fn CreateTokenForm\(' src/page/assembly_details.rs)</automated>
  </verify>
  <done>AssemblyDetails compiliert; nutzt TabStrip + alle 5 Anwesenheits-Components (AttendanceList/Search/Counter/Banner) + QrCard + die W-04 Plan-06-Components (BasicsTab/TokenRow/CreateTokenForm); 3 Tabs branched per active_key; KEINE inline-Definitionen von BasicsTab/TokenRow/CreateTokenForm (grep verifiziert); Token-Create-Modal + just_created-One-Time-Show.</done>
</task>

</tasks>

<verification>
```bash
cargo build -p genossi-frontend 2>&1 | tail -5

# Component-Reuse acceptance (ATTN-06)
# Anwesenheits-Tab in assembly_details.rs muss EXAKT die selben Components nutzen wie /helper/attendance (Plan 09)
# → später Plan 10 grep-vergleicht beide Pages
grep -E "AttendanceList|AttendanceSearch|LiveCounter|ConnectionBanner" /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/assembly_details.rs | wc -l

# Component-First-Konsistenz
grep -q "TabStrip" /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/assembly_details.rs

# Token-One-Time-Show: just_created Pattern vorhanden
grep -q "just_created" /home/neosam/programming/rust/projects/genossi3/genossi-frontend/src/page/assembly_details.rs
```

</verification>

<success_criteria>
- Beide Pages compile-grün
- assemblies.rs nutzt RequirePrivilege + AssemblyListRow + Create-Modal + ToastContainer
- assembly_details.rs nutzt RequirePrivilege + TabStrip + AssemblyStatusBadge + alle 5 Anwesenheits-Components + QrCard + die 3 W-04-Components (BasicsTab/TokenRow/CreateTokenForm)
- W-04 acceptance: assembly_details.rs hat KEINE inline-fn-Definitionen von BasicsTab, TokenRow, CreateTokenForm (alle aus crate::component:: importiert)
- W-01 acceptance: BasicsTab (in Plan 06) hat Edit-Mode-Toggle für update_assembly (Preparation-only)
- Anwesenheits-Tab branched auf Status: vor Open zeigt Hinweis-Text, sonst Components
- just_created-Pattern für Token-One-Time-Show
- Alle GV-Lifecycle-Buttons (Open/Close) zeigen Confirmation-Dialog vor API-Call
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-component-first-mit-qr-scanner-und-manual-code-fall/04-08-SUMMARY.md`.
</output>
