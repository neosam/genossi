---
phase: 23-html-mail-backend
plan: 03
type: execute
wave: 3
depends_on:
  - 23-02
files_modified:
  - genossi_mail/src/send.rs
autonomous: true
requirements:
  - HTML-01
  - HTML-02
tags: [rust, lettre, mime, multipart, alternative]

must_haves:
  truths:
    - "`build_message` signature gains an `html_body: Option<&str>` parameter positioned between `body: &str` and `attachments: &[LoadedAttachment]` (D-10)."
    - "When `html_body=None` AND `attachments=[]` → the message is a single-part text mail (byte-shape identical to Phase 22 legacy)."
    - "When `html_body=None` AND `attachments=[…]` → `multipart/mixed{ text, attachments… }` (byte-shape identical to Phase 22 legacy)."
    - "When `html_body=Some(_)` AND `attachments=[]` → `multipart/alternative{ text-first, html-second }` (D-09; RESEARCH Pitfall 5 — ordering pinned)."
    - "When `html_body=Some(_)` AND `attachments=[…]` → `multipart/mixed{ multipart/alternative{ text, html }, attachments… }` (HTML-01 with attachments)."
    - "The plain-text part is the raw `body` argument — no derivation from HTML (HTML-02)."
    - "All existing MIME-byte tests from Phase 22 still pass unchanged (regression contract for MAIL-01/02/05)."
    - "New callers pass `None` for html_body and observe zero behavior change vs. Phase 22."
  artifacts:
    - "genossi_mail/src/send.rs — build_message signature extended; 4-branch decision tree; 5 new MIME-byte tests"
  key_links:
    - "This is the ONLY file edited in this plan — no service, no worker, no REST changes. The callers (worker.rs, service.rs::send_test_mail, service.rs::send_test_mail_with_body) will be updated to pass `html_body` in Plan 04."
    - "The `SinglePart` construction for the HTML part reuses the same CTE choice (`ContentTransferEncoding::QuotedPrintable` or `EightBit`) as the text part — the encoding config knob applies uniformly to both parts of an alternative message."
    - "`ContentType::TEXT_HTML` is the header type for the HTML SinglePart — matches `text/html; charset=utf-8` on the wire."
---

<objective>
Extend the shared `build_message(...)` factory in `genossi_mail::send` with an optional `html_body: Option<&str>` parameter and the four-way decision tree from RESEARCH § "Pattern 3": (None, []) → singlepart-text (existing byte-shape); (None, [a…]) → mixed{text, a…} (existing byte-shape); (Some, []) → alternative{text, html} (new); (Some, [a…]) → mixed{ alternative{text, html}, a… } (new, HTML-01 with attachments).

Purpose: The `multipart/alternative` MIME assembly is the entire HTML-01 primitive. Isolating it in a dedicated single-file plan makes the byte-shape verifiable in isolation and lets Plan 04 focus on wiring. The Phase-22 SUMMARY (§ "Next Phase Readiness") explicitly noted this as a single-file diff.

Output: One file edited (`send.rs`); one function signature evolved; four MIME-byte tests added; every existing test still passes. NO caller updates in this plan — Plan 04 updates them by passing `None` (worker preservation until it wires HTML) and `Some(html)` (worker HTML path + test-mail-with-body).
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-html-mail-backend/23-CONTEXT.md
@.planning/phases/23-html-mail-backend/23-RESEARCH.md
@.planning/phases/22-8bit-shared-mail-body-helper/22-02-SUMMARY.md
@genossi_mail/src/send.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend build_message signature and implement the 4-branch alternative/mixed decision tree</name>
  <files>genossi_mail/src/send.rs</files>
  <read_first>
    - genossi_mail/src/send.rs (WHOLE FILE — module + tests) — this is the only file edited in this plan
    - .planning/phases/23-html-mail-backend/23-RESEARCH.md § "Pattern 3: Extended build_message signature" and § "Pitfall 5: multipart/alternative ordering"
    - .planning/phases/23-html-mail-backend/23-CONTEXT.md § "MIME-Bau (HTML-01, HTML-02)" (D-10 — where the alternative branch lives)
    - .planning/phases/22-8bit-shared-mail-body-helper/22-02-SUMMARY.md § "Test coverage (send.rs::tests)" — 7 existing MIME-byte tests to keep green
  </read_first>
  <behavior>
    - `pub fn build_message(...)` gains a NEW parameter `html_body: Option<&str>` between `body: &str` and `attachments: &[LoadedAttachment]`. Final signature:
      `pub fn build_message(from: &str, to: &str, subject: &str, body: &str, html_body: Option<&str>, attachments: &[LoadedAttachment], in_reply_to: Option<&str>, encoding: MailEncoding) -> Result<Message, MailServiceError>`
    - Inside the function, after the existing text_part construction, build `let html_part_opt: Option<SinglePart> = html_body.map(|html| SinglePart::builder().header(ContentType::TEXT_HTML).header(cte).body(html.to_string()));`. The `cte` value is the same one used for the text part (D-01 — encoding config applies uniformly).
    - Replace the final if/else between `attachments.is_empty()` with a match on `(html_part_opt, attachments.is_empty())`:
      1. `(None, true)` → `builder.singlepart(text_part)` (Phase-22 legacy path, byte-identical).
      2. `(None, false)` → `builder.multipart(MultiPart::mixed().singlepart(text_part).<attachment loop>)` (Phase-22 legacy path, byte-identical).
      3. `(Some(html), true)` → `builder.multipart(MultiPart::alternative().singlepart(text_part).singlepart(html))` — text FIRST (RESEARCH Pitfall 5).
      4. `(Some(html), false)` → build `let alt = MultiPart::alternative().singlepart(text_part).singlepart(html);` then `MultiPart::mixed().multipart(alt)` and append the attachment loop.
    - The 7 existing tests inside `#[cfg(test)] mod tests` still pass; they must each be updated to pass `None` as the new `html_body` argument (mechanical text edit; no test intent changes). This preserves the Phase-22 MAIL-01/02/05 regression bar (D-01 legacy byte-shape).
    - Five new tests appended to the tests module:
      1. `build_message_alternative_text_then_html_no_attachments` — html_body=Some, attachments=[]. Assert output contains `Content-Type: multipart/alternative` AND `Content-Type: text/plain` appears BEFORE `Content-Type: text/html` in the byte stream (text-first ordering).
      2. `build_message_alternative_text_part_is_verbatim_body` — html_body=Some, body="Hallo Jürgen". Assert the text part contains "Hallo Jürgen" (or its QP-encoded form) verbatim; assert the HTML part is not derived from the text (a distinct string was used, so this is trivially true — the test pins the contract).
      3. `build_message_mixed_wraps_alternative_when_attach` — html_body=Some, attachments=[one attachment]. Assert output contains both `multipart/mixed` AND `multipart/alternative`; assert `multipart/mixed` appears in the outer Content-Type header (top of message); assert `multipart/alternative` appears somewhere deeper in the body (the nested boundary).
      4. `build_message_legacy_singlepart_text_unchanged` — html_body=None, attachments=[]. Assert output does NOT contain `multipart/` at all AND declares `Content-Type: text/plain` at the top. Byte-shape regression for HTML-01 "legacy stays legacy".
      5. `build_message_html_part_declares_text_html_charset_utf8` — html_body=Some. Assert output contains `Content-Type: text/html` AND `charset=utf-8` on the HTML part (same charset contract as text; MAIL-01 style regression).
    - `cargo test -p genossi_mail --lib send::` passes with all 7 existing + 5 new = 12 tests.
    - `cargo build -p genossi_mail` fails at the call-site level in worker.rs and service.rs (they still pass 7 args) — this is EXPECTED. This plan's scope is `send.rs` only; call sites are updated in Plan 04. To keep the crate build-green through Plan 03 in isolation, adjust the worker.rs and service.rs call sites at THE MINIMUM level: insert `None,` for the new html_body positional argument at each of the three call sites (worker.rs::send_mail_for_recipient one call; service.rs::send_test_mail one call; service.rs::send_test_mail_with_body one call). This is a mechanical positional-argument insertion — no behavioral change. Plan 04 replaces the three `None`s with real values.
  </behavior>
  <action>Edit `genossi_mail/src/send.rs`. Change the `build_message` signature to insert `html_body: Option<&str>` between the `body: &str` and `attachments: &[LoadedAttachment]` parameters. Update the module-level doc-comment to describe the new parameter briefly (one line: "html_body — optional HTML sibling; when Some, produces a multipart/alternative with text first, then HTML, per HTML-01/D-09."). After the existing text_part construction (~line 76-79), add the html_part_opt line: `let html_part_opt = html_body.map(|html| SinglePart::builder().header(ContentType::TEXT_HTML).header(cte).body(html.to_string()));`. Replace the trailing if/else at line 94-112 with a `match (html_part_opt, attachments.is_empty()) { … }` covering all four arms per the behavior spec. Keep the `.map_err(|e| MailServiceError::SmtpError(Arc::from(e.to_string())))` conversion on each of the 4 return sites. Inside the tests module, edit each of the 7 existing tests to insert `None,` as the new positional argument before `&[]` (or `&[att]`) — a mechanical text edit. Append the 5 new tests described above. For each new test, use the existing `formatted(&msg)` helper. Assertion style examples: `assert!(text.contains("multipart/alternative"), "expected multipart/alternative in output:\n{}", text);` — always include the raw output in the panic message for debuggability (matches Phase-22 style). To keep the whole crate build-green during Plan 03, also edit the THREE non-test call sites (`genossi_mail/src/worker.rs` line ~662, `genossi_mail/src/service.rs` line ~455, `genossi_mail/src/service.rs` line ~489) to insert `None,` as the new positional argument. Grep to confirm exactly 3 non-test call sites: `grep -n 'crate::send::build_message' genossi_mail/src/` should return 3 lines total (one worker, two service). Do NOT change any Phase-22 error strings inside build_message. Do NOT change the CTE selection logic (Plan 22-02 owns it). Do NOT alter the address-parse error strings. Do NOT persist anything — this file is pure MIME.</action>
  <verify>
    <automated>cargo test -p genossi_mail --lib 2>&1 | tee /tmp/23-03-t1.log | tail -30 && grep -q '0 failed' /tmp/23-03-t1.log && cargo build -p genossi_mail 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'html_body: Option<&str>' genossi_mail/src/send.rs` returns `1` (only in the signature).
    - `grep -c 'MultiPart::alternative()' genossi_mail/src/send.rs` returns >= `1` (used in production, may also appear in tests).
    - `grep -c 'ContentType::TEXT_HTML' genossi_mail/src/send.rs` returns >= `1`.
    - `grep -c 'match (html_part_opt' genossi_mail/src/send.rs` returns `1` (single decision tree, not duplicated across branches).
    - `grep -c 'fn build_message_alternative_text_then_html_no_attachments\|fn build_message_alternative_text_part_is_verbatim_body\|fn build_message_mixed_wraps_alternative_when_attach\|fn build_message_legacy_singlepart_text_unchanged\|fn build_message_html_part_declares_text_html_charset_utf8' genossi_mail/src/send.rs` returns `5`.
    - Non-test call sites unchanged in count: `grep -c 'crate::send::build_message' genossi_mail/src/worker.rs` returns `1`; `grep -c 'crate::send::build_message' genossi_mail/src/service.rs` returns `2`.
    - `cargo build -p genossi_mail` exits 0.
    - `cargo test -p genossi_mail --lib send::` exits 0 with `0 failed` and total passing >= 12 tests (the 7 Phase-22 + 5 new).
    - `cargo clippy -p genossi_mail --lib` exits 0.
    - Pitfall 5 pinned: at least one new test's assertion literally checks that `text.find("text/plain") < text.find("text/html")` (or an equivalent ordering check) — grep for `find("text/plain")` in send.rs to confirm at least 1 occurrence.
  </acceptance_criteria>
  <done>build_message accepts html_body; the four MIME shapes are produced according to the (html_body, attachments) matrix; text-first ordering is pinned by an explicit byte-position assertion; the 7 Phase-22 tests remain green after mechanical `None,` insertions; the crate still builds because worker.rs and service.rs call sites also received the mechanical `None,` insertion; Plan 04 will replace those Nones with real values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build_message input strings → lettre MIME builder | Address strings and bodies enter the MIME builder here. All sanitization/escaping has already happened upstream (autoescape on HTML render — Plan 02; sanitize-on-store — Plan 04). This plan is pure structural MIME assembly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-06 | Spoofing | Mail-header injection via `body` or `html_body` newlines | low | mitigate | Lettre's builder API composes headers from typed inputs; `body` and `html_body` are payload bytes handed to `SinglePart::body(String)` — cannot inject headers by construction. Verified by construction; no test needed. |
| T-23-07 | Tampering | multipart/alternative ordering (text last would downgrade to plain in HTML-capable clients) | medium | mitigate | Text FIRST, HTML SECOND (RFC 2046 §5.1.4 preference model). Pinned by test `build_message_alternative_text_then_html_no_attachments` which asserts `text/plain` byte-offset < `text/html` byte-offset. Closes RESEARCH Pitfall 5. |
</threat_model>

<verification>
- `cargo build -p genossi_mail` succeeds after the three-call-site mechanical `None,` insertions.
- `cargo test -p genossi_mail --lib send::` passes all 12 tests (7 Phase-22 + 5 new).
- Text-first ordering is verified by a byte-offset assertion, not just a "contains" check.
</verification>

<success_criteria>
- `build_message` signature contains `html_body: Option<&str>` in the documented position.
- Four MIME output shapes verified via 5 new byte-level tests covering: alternative-only, alternative-inside-mixed, legacy singlepart, and the text/html + charset=utf-8 contract on the HTML part.
- Phase-22 regression contract intact: 7 pre-existing tests still pass unchanged (except for the mechanical `None,` insertion of the new positional argument).
- `cargo clippy -p genossi_mail --lib` clean.
</success_criteria>

<output>
Create `.planning/phases/23-html-mail-backend/23-03-SUMMARY.md` when done, listing:
- The updated `build_message` signature line
- Byte-shape decisions for the four (html_body, attachments) combinations
- The 5 new tests with a one-line assertion summary each
- The 3 non-test call sites that received the mechanical `None,` insertion
- Confirmation that Phase-22 MIME byte-shape is preserved for the (None, …) paths
- `jj log` hash of the atomic commit
</output>
