---
phase: 02-helfer-token-session-authcontext-helper
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - genossi_service/src/auth_types.rs
  - genossi_service_impl/src/permission.rs
  - genossi_rest/src/lib.rs
autonomous: true
requirements: [HLPR-02, HLPR-05]
must_haves:
  truths:
    - "AuthContext-Enum hat eine dritte Variante `Helper { session_id: Arc<str>, assembly_id: uuid::Uuid }` ohne cfg-Gate (D-14)"
    - "Beide Feature-Builds (mock_auth allein, oidc allein, beide kombiniert) compilieren weiterhin"
    - "PermissionServiceImpl::check_permission hat einen expliziten Pfad, der bei AuthContext::Helper {..} `Err(ServiceError::PermissionDenied)` zurückliefert (D-20 Phase-2-Stub)"
    - "REST::extract_auth_context (mock_auth + oidc) erkennt AuthContext::Helper, liefert sie aber NICHT als Authentication<Context> (Helper hat keine Vorstands-Permissions in Phase 2) — sondern Err(RestError::Unauthorized)"
    - "Alle bestehenden `match auth_context { ... }` Stellen in der Codebase haben einen Helper-Arm (Compiler-Cascade)"
    - "AuthenticatedContext (oidc) bleibt unverändert; keine Kollision mit Helper-Variante"
    - "Bestehende Phase-1-E2E-Tests laufen weiter durch (Compiler-Cascade darf keine Regression einführen)"
  artifacts:
    - path: genossi_service/src/auth_types.rs
      provides: "AuthContext::Helper Variante (D-14)"
      exports: ["AuthContext"]
    - path: genossi_service_impl/src/permission.rs
      provides: "Helper-Stub-Match-Arm in check_permission (D-20)"
    - path: genossi_rest/src/lib.rs
      provides: "REST extract_auth_context-Conversion erkennt Helper und liefert Unauthorized (Phase 2 Stub; Phase 3 erweitert)"
  key_links:
    - from: "genossi_service::auth_types::AuthContext"
      to: "AuthContext::Helper Variante"
      via: "enum AuthContext { Mock, Oidc, Helper { session_id, assembly_id } }"
      pattern: "Helper \\{ session_id: Arc<str>, assembly_id: uuid::Uuid \\}"
    - from: "PermissionServiceImpl::check_permission"
      to: "PermissionDenied for Helper"
      via: "match-arm prüft AuthContext::Helper { .. } => Err(PermissionDenied)"
      pattern: "AuthContext::Helper.*PermissionDenied"
---

<objective>
Vorbereitung der typsicheren AuthContext-Helper-Variante für Phase 3. Eine neue Enum-Variante in `genossi_service/src/auth_types.rs` (D-14, ohne cfg-Gate), Compiler-Cascade-Fixes für alle Match-Arms (alle liefern in Phase 2 `PermissionDenied`/`Unauthorized` Stub-Verhalten — D-20), und ein REST-Layer-Stub, der Helper-Sessions als nicht-vorstandsfähig behandelt.

Purpose: Phase 3 baut auf dieser Variante die positive Permission-Branch (Helfer dürfen Attendance-Endpoints aufrufen). Phase 2 muss die Type-Sichtbarkeit etablieren, damit `SessionService::extract_auth_context` (Plan 06) und der REST-Helfer-Redeem-Endpoint (Plan 07) sie konstruieren können.

Output: Workspace baut weiterhin in beiden Feature-Builds (mock_auth, oidc). Bestehende Tests grün.

Wire-Point-Begründung (D-19, Claude's Discretion): Der Assembly-Status-Check landet in Plan 06 in `SessionServiceImpl::extract_auth_context`, NICHT in der REST-Auth-Middleware. Begründung: (1) saubere Schicht-Trennung (Permission/Lifecycle ist Service-Logik), (2) leichter mockbar (Service-Tests via Mockall), (3) minimaler Eingriff in `auth_middleware.rs::extract_context_from_headers`, das schon korrekt an `session_service.extract_auth_context` delegiert (genossi_rest/src/auth_middleware.rs:101-134, KEINE Änderung nötig). Plan 06 implementiert die Logik dort.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-helfer-token-session-authcontext-helper/02-CONTEXT.md
@.planning/phases/02-helfer-token-session-authcontext-helper/02-RESEARCH.md
@.planning/phases/02-helfer-token-session-authcontext-helper/02-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@genossi_service/src/auth_types.rs
@genossi_service_impl/src/permission.rs
@genossi_rest/src/lib.rs
@genossi_rest/src/auth_middleware.rs

<interfaces>
<!-- Existing AuthContext (genossi_service/src/auth_types.rs:92-100): -->
```rust
#[derive(Debug, Clone, PartialEq, Eq)]
pub enum AuthContext {
    Mock(MockContext),
    #[cfg(feature = "oidc")]
    Oidc(Arc<str>),
}
```

<!-- Existing extract_auth_context (genossi_rest/src/lib.rs:50-73) — zwei Varianten je Feature: -->
```rust
// mock_auth:
pub fn extract_auth_context(
    context: Option<Context>,
) -> Result<genossi_service::permission::Authentication<MockContext>, RestError> {
    match context {
        Some(ctx) => Ok(genossi_service::permission::Authentication::Context(ctx)),
        None => Err(RestError::Unauthorized),
    }
}

// oidc:
pub fn extract_auth_context(
    context: Option<Context>,
) -> Result<
    genossi_service::permission::Authentication<genossi_service::auth_types::AuthenticatedContext>,
    RestError,
> {
    match context {
        Some(Some(auth_context)) => Ok(genossi_service::permission::Authentication::Context(auth_context)),
        _ => Err(RestError::Unauthorized),
    }
}
```

<!-- WICHTIG: `Context` ist in beiden Builds NICHT direkt AuthContext — er ist MockContext bzw. Option<AuthenticatedContext>.
     Das bedeutet: Die Helper-Variante des AuthContext propagiert NICHT durch die existierende Auth-Middleware-Pipeline,
     weil `auth_middleware.rs::extract_context_from_headers` die AuthContext-Variante in `request.extensions().insert(auth_context)`
     speichert — aber dann leitet `context_extractor` daraus `Context::Mock(...)` oder `AuthenticatedContext` ab.
     Phase 2 muss in Plan 06 + Plan 07 die Logik erweitern: für Helper-Sessions soll der Endpoint-Handler OHNE Vorstands-Permission durchkommen können.
     Plan 02 (dieser Plan) etabliert nur die Variante und einen Stub. -->

<!-- PermissionServiceImpl::check_permission (genossi_service_impl/src/permission.rs:28-48): -->
```rust
async fn check_permission(
    &self,
    privilege: &str,
    context: Authentication<Self::Context>,
) -> Result<(), ServiceError> {
    match context {
        Authentication::Full => Ok(()),
        Authentication::Context(ctx) => {
            let current_user = self.user_service.current_user(ctx).await?;
            // ...
        }
    }
}
```

<!-- WICHTIG: PermissionService nutzt `Authentication<Self::Context>` — NICHT `AuthContext` direkt.
     `Self::Context` ist `MockContext` (mock_auth) oder `AuthenticatedContext` (oidc).
     `AuthContext::Helper { ... }` wird in der REST-Layer-Conversion `extract_auth_context` GEPRÜFT.
     Plan 02 erweitert die REST-Conversion: bei Helper => Err(RestError::Unauthorized) (Phase-2-Stub).
     Plan 03 (Phase 3, OUT OF SCOPE für diese Phase) erweitert das auf positiven Helfer-Pfad. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AuthContext::Helper Variante in genossi_service/src/auth_types.rs</name>
  <files>genossi_service/src/auth_types.rs</files>
  <read_first>
    - genossi_service/src/auth_types.rs (gesamte Datei — bestehende Enum-Definition Zeilen 92-100)
    - .planning/phases/02-helfer-token-session-authcontext-helper/02-CONTEXT.md (D-14)
    - .planning/phases/02-helfer-token-session-authcontext-helper/02-RESEARCH.md (Pattern 2)
  </read_first>
  <behavior>
    - `AuthContext::Helper { session_id: Arc<str>, assembly_id: uuid::Uuid }` ist als dritte Variante verfügbar
    - Variante hat KEINE `#[cfg(...)]`-Annotation (D-14 — verfügbar in mock_auth UND oidc)
    - `AuthContext` bleibt `#[derive(Debug, Clone, PartialEq, Eq)]` — Helper-Variante muss diese Traits unterstützen (Arc<str> + Uuid tun das beide)
    - `cargo build` mit `--features mock_auth` (default) UND `--features oidc` muss erfolgreich durchlaufen
  </behavior>
  <action>
    Modifiziere `genossi_service/src/auth_types.rs` Zeilen 92-100 wie folgt:

    Bestehender Code (Zeilen 92-100):
    ```rust
    /// Authentication context for different modes
    #[derive(Debug, Clone, PartialEq, Eq)]
    pub enum AuthContext {
        /// Mock context for development
        Mock(MockContext),
        /// OIDC context with user ID
        #[cfg(feature = "oidc")]
        Oidc(Arc<str>),
    }
    ```

    Neuer Code:
    ```rust
    /// Authentication context for different modes
    #[derive(Debug, Clone, PartialEq, Eq)]
    pub enum AuthContext {
        /// Mock context for development
        Mock(MockContext),
        /// OIDC context with user ID
        #[cfg(feature = "oidc")]
        Oidc(Arc<str>),
        /// Helper-token session — used by /api/helper/redeem flow.
        /// Carries the issuing session's id and the bound assembly_id.
        /// In Phase 2, this variant short-circuits permission checks to
        /// `PermissionDenied` (D-20). Phase 3 introduces the positive branch
        /// where helpers may call attendance endpoints if `assembly_id` matches.
        /// NO `#[cfg(...)]` gate — the variant must exist in BOTH mock_auth and
        /// oidc builds (D-14) so REST handlers and SessionService can construct
        /// it identically across feature configurations.
        Helper {
            session_id: Arc<str>,
            assembly_id: uuid::Uuid,
        },
    }
    ```

    Stelle sicher, dass am Datei-Anfang `use uuid::Uuid;` vorhanden ist ODER dass `uuid::Uuid` in der Helper-Variante voll qualifiziert wird (siehe Code oben — voll-qualifiziert ist okay, da nur an einer Stelle benötigt). Wenn `Uuid` schon importiert ist, kann `assembly_id: Uuid,` verwendet werden — aber `uuid::Uuid` voll-qualifiziert ist defensiver (kein zusätzlicher Use nötig). Behalte den Default-Stil der Datei.

    Tests in `#[cfg(test)] mod tests` (am Datei-Ende, falls noch keine Tests existieren — sonst dort einsortieren):
    ```rust
    #[cfg(test)]
    mod tests {
        use super::*;
        use std::sync::Arc;

        #[test]
        fn test_auth_context_helper_variant_constructible() {
            let ctx = AuthContext::Helper {
                session_id: Arc::from("session-abc"),
                assembly_id: uuid::Uuid::nil(),
            };
            // Variant must be Debug + Clone + PartialEq + Eq (the existing derives)
            let cloned = ctx.clone();
            assert_eq!(ctx, cloned);
        }

        #[test]
        fn test_auth_context_helper_distinct_from_mock() {
            let helper = AuthContext::Helper {
                session_id: Arc::from("s1"),
                assembly_id: uuid::Uuid::nil(),
            };
            let mock = AuthContext::Mock(MockContext::default());
            assert_ne!(helper, mock);
        }
    }
    ```
  </action>
  <verify>
    <automated>cargo build -p genossi_service --features mock_auth 2>&1 | tail -10 && cargo build -p genossi_service --features oidc 2>&1 | tail -10 && cargo test -p genossi_service auth_types 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p genossi_service --features mock_auth` exit 0
    - `cargo build -p genossi_service --features oidc` exit 0
    - `cargo test -p genossi_service auth_types` exit 0 mit ≥ 2 Tests
    - `grep -c "Helper {" genossi_service/src/auth_types.rs` ≥ 1
    - `grep -c "session_id: Arc<str>" genossi_service/src/auth_types.rs` ≥ 1
    - `grep -c "assembly_id: uuid::Uuid" genossi_service/src/auth_types.rs` ≥ 1
    - `grep -B 2 "Helper {" genossi_service/src/auth_types.rs | grep -c '#\[cfg'` == 0 (D-14: keine cfg-Gate)
    - `grep -c "test_auth_context_helper_variant_constructible" genossi_service/src/auth_types.rs` == 1
  </acceptance_criteria>
  <done>
    `AuthContext::Helper`-Variante existiert ohne cfg-Gate; beide Feature-Builds compilieren; zwei Unit-Tests grün; Phase 3 kann auf dieser Variante aufbauen.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compiler-Cascade-Fixes (PermissionServiceImpl + ggf. weitere AuthContext-Match-Stellen)</name>
  <files>
    genossi_service_impl/src/permission.rs,
    genossi_rest/src/lib.rs
  </files>
  <read_first>
    - genossi_service_impl/src/permission.rs (gesamte Datei — bestehende check_permission Implementation)
    - genossi_rest/src/lib.rs (Zeilen 50-73 extract_auth_context für mock_auth und oidc)
    - genossi_rest/src/auth_middleware.rs (Zeilen 101-134 — bestätigt: KEINE Änderung nötig, leitet schon korrekt an SessionService::extract_auth_context)
    - .planning/phases/02-helfer-token-session-authcontext-helper/02-CONTEXT.md (D-20)
    - .planning/phases/02-helfer-token-session-authcontext-helper/02-PATTERNS.md (Sektion `genossi_service_impl/src/permission.rs` und `genossi_rest/src/lib.rs`)
  </read_first>
  <behavior>
    - Workspace-Build (`cargo build --workspace --features mock_auth` UND `--features oidc`) erfolgreich
    - Compiler-Cascade nach Task 1 ist vollständig adressiert: jede Stelle, die exhaustiv über `AuthContext` matched, hat einen `Helper {..}`-Arm
    - `PermissionServiceImpl::check_permission` ist semantisch unverändert für Mock/Oidc — der Helper-Pfad wird in der REST-Conversion (extract_auth_context) abgefangen
    - REST `extract_auth_context` (mock_auth) prüft NUR auf `MockContext`-Identität wie bisher (Helper kommt dort NICHT an, weil `Context` = `MockContext`, NICHT `AuthContext`)
    - REST `extract_auth_context` (oidc) prüft NUR auf `AuthenticatedContext`-Identität wie bisher (Helper kommt dort NICHT an)
    - **WICHTIG:** Die Helper-Variante kommt NICHT durch die bestehende `Context`-Type-Pipeline — `auth_middleware.rs` speichert `AuthContext` IN `request.extensions().insert(auth_context)`, aber `context_extractor` setzt aus dem AuthContext einen `Context` (MockContext oder Option<AuthenticatedContext>) — Helper hat dort kein Mapping. Plan 07 wird die Pipeline für den Helfer-Redeem-Endpoint umgehen (er ist öffentlich, ohne require_authentication-Middleware). Plan 02 etabliert nur die Type-Sichtbarkeit.
  </behavior>
  <action>
    **Datei 1: `genossi_service_impl/src/permission.rs`** — KEINE Änderung an `check_permission` selbst (das matched über `Authentication<Self::Context>`, nicht über `AuthContext`).

    Aber: Suche per `grep -rn "AuthContext::" genossi_service_impl/src/ genossi_service/src/` nach allen Stellen, die exhaustiv auf `AuthContext`-Varianten matchen. Falls du Match-Stellen findest, die nun einen "non-exhaustive patterns: `Helper { .. }` not covered"-Compilerfehler werfen, füge dort einen `Helper { .. } => Err(ServiceError::PermissionDenied)`-Arm ein (per D-20).

    **Erwarteter Befund:** In Phase 2 sollte es KEINE solchen Stellen im Service-Layer geben (PermissionServiceImpl matched nur Authentication-Enum, nicht AuthContext direkt; SessionServiceImpl::extract_auth_context konstruiert AuthContext, matched aber nicht). Der Compiler-Cascade tritt PRIMÄR im REST-Layer auf (siehe Datei 2).

    Wenn du tatsächlich auf Match-Stellen stößt (z.B. in Tests oder einem speziellen Auth-Pfad), füge einen Stub-Arm ein:
    ```rust
    AuthContext::Helper { .. } => {
        // Phase 2 stub (D-20): helpers have no privileges in this phase.
        // Phase 3 will replace this with the positive attendance-endpoint branch.
        Err(ServiceError::PermissionDenied)
    }
    ```

    **Datei 2: `genossi_rest/src/lib.rs`** — KEINE Änderung an den bestehenden `extract_auth_context`-Funktionen (Zeilen 50-73). Sie matchen über `Context`, nicht über `AuthContext`.

    ABER: Wenn `cargo build --workspace` einen non-exhaustive-pattern-Fehler in einer anderen Datei wirft (z.B. wenn jemand eine `match auth_context { AuthContext::Mock(..) => .., AuthContext::Oidc(..) => .. }`-Stelle in der Codebase hat), füge dort den Helper-Arm hinzu — Phase-2-Stub-Behavior:
    ```rust
    AuthContext::Helper { .. } => Err(RestError::Unauthorized),  // Phase 2 stub (D-20)
    ```

    Konkrete Schritte:
    1. Run `grep -rn "AuthContext::" genossi_service/ genossi_service_impl/ genossi_rest/ genossi_bin/ genossi_dao/ genossi_dao_impl_sqlite/ genossi_rest_types/ --include='*.rs' | grep -v test`
    2. Für jede Match-Stelle: prüfe per `cargo build --workspace --features mock_auth 2>&1 | grep -A 5 "non-exhaustive"`, ob der Compiler dort einen Fehler wirft
    3. Falls ja: füge Helper-Arm mit Phase-2-Stub-Behavior ein (PermissionDenied im Service / Unauthorized im REST)
    4. Falls nein (kein Compiler-Error): Datei bleibt unangetastet

    Falls in Plan 01 die REST-Conversion-Funktion `extract_auth_context` direkt über AuthContext matched (was sie aktuell NICHT tut, sie matched über Context), wäre die Erweiterung dort nötig. Aktueller Code (Zeilen 50-58 mock_auth, 60-72 oidc) matched aber nicht über AuthContext, sondern über `Context::MockContext` (direkt) bzw. `Option<AuthenticatedContext>`. Die Helper-Variante findet keinen Eingang in diesen Pfad — das ist ABSICHTLICH so:
    - Phase-2-Helfer-Redeem-Endpoint (Plan 07) ist ÖFFENTLICH (kein extract_auth_context-Call)
    - Phase-2-Vorstand-Endpoints (Plan 07) prüfen `admin`-Permission — Helfer-Sessions wären dafür nicht geeignet ohnehin
    - Phase 3 (out of scope) wird die Pipeline so erweitern, dass Helfer-Sessions Attendance-Endpoints aufrufen können

    Falls auch nach `grep` keine Compiler-Cascade-Fix-Stellen aufzufinden sind, **dokumentiere das in der SUMMARY** als Befund — das bestätigt, dass Plan 02 minimal-invasiv ist.

    Ein optionaler defensiver Test (am Ende von `genossi_rest/src/lib.rs::tests` mod, falls dort schon Tests sind, oder neuer mod tests):
    ```rust
    #[test]
    fn test_helper_variant_compiles_in_both_features() {
        // Smoke test: AuthContext::Helper kann in diesem Crate konstruiert werden,
        // ohne cfg-Annotation. Wenn Cargo den Variante hinter einem Feature-Flag
        // versteckt, bricht dieser Test in dem Build, der das Feature nicht hat.
        use genossi_service::auth_types::AuthContext;
        use std::sync::Arc;
        let _ = AuthContext::Helper {
            session_id: Arc::from("test"),
            assembly_id: uuid::Uuid::nil(),
        };
    }
    ```
  </action>
  <verify>
    <automated>cargo build --workspace --features mock_auth 2>&1 | tail -15 && cargo build --workspace --no-default-features --features oidc 2>&1 | tail -15 && cargo test -p genossi_service_impl permission 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace --features mock_auth` exit 0 (Default-Build)
    - `cargo build --workspace --no-default-features --features oidc` exit 0 (OIDC-Build)
    - `cargo test --workspace --features mock_auth` exit 0 — alle bisherigen Tests bleiben grün
    - `grep -rn "AuthContext::Helper" genossi_service_impl/ genossi_rest/ --include='*.rs' | grep -v "test_\|#\[test\]\|fn test"` ergibt entweder keine oder nur PermissionDenied/Unauthorized-Stub-Stellen (NICHT positiv-Pfad)
    - Falls Stub-Arme hinzugefügt wurden: `grep -c "Phase 2 stub.*D-20" genossi_service_impl/src/permission.rs genossi_rest/src/lib.rs` ≥ Anzahl der hinzugefügten Stellen (Kommentar-Hinweis)
    - `cargo clippy --workspace --all-targets --features mock_auth 2>&1 | grep -c "non-exhaustive"` == 0
    - `cargo clippy --workspace --all-targets --no-default-features --features oidc 2>&1 | grep -c "non-exhaustive"` == 0
  </acceptance_criteria>
  <done>
    Workspace baut in beiden Feature-Builds; Cascade-Fixes (falls nötig) sind dokumentiert; alle bestehenden Tests bleiben grün; Plan 06 kann auf der `AuthContext::Helper`-Variante aufbauen, ohne Compiler-Probleme.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service→permission | Helper-Sessions kommen über REST-Pipeline und werden in Plan 06 (SessionServiceImpl::extract_auth_context) zur AuthContext::Helper-Variante. Plan 02 stellt sicher, dass jede Match-Stelle den Helper-Pfad explizit als PermissionDenied behandelt — keine accidental-Bypass-Risiken. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Elevation of Privilege | AuthContext::Helper bypasses permission checks | mitigate | D-20: alle Match-Stellen liefern explizit Err(PermissionDenied) für Helper-Variante in Phase 2; positiver Pfad wird erst in Phase 3 erlaubt nach präziser assembly_id-Prüfung. Compiler-Cascade garantiert Vollständigkeit. |
| T-02-02-02 | Spoofing | Forge a Helper context to impersonate Vorstand | mitigate | AuthContext::Helper kann nur via SessionServiceImpl::extract_auth_context (Plan 06) konstruiert werden, das die Session-Existenz und assembly_id aus dem session.claims-JSON ableitet. Kein REST-Endpoint nimmt Helper-Identifier direkt vom Client. |
| T-02-02-03 | Tampering | Modify the Helper variant after construction | accept | Variante ist immutable (Rust enum, Arc<str>); kein Mutationspfad in Phase 2. |
</threat_model>

<verification>
- `cargo build --workspace --features mock_auth` grün
- `cargo build --workspace --no-default-features --features oidc` grün
- `cargo test --workspace --features mock_auth` grün — alle Phase-1-E2E-Tests laufen weiter durch
- `cargo test -p genossi_service auth_types` grün mit ≥ 2 neuen Tests
- `cargo clippy --workspace --all-targets` ohne neue Warnings (insbesondere keine `non-exhaustive patterns`-Warnings nach dem Cascade-Fix)
- `cargo fmt --check` grün
</verification>

<success_criteria>
- `AuthContext::Helper { session_id, assembly_id }`-Variante existiert in `genossi_service/src/auth_types.rs` ohne cfg-Gate (D-14)
- Compiler-Cascade ist vollständig adressiert; alle Builds grün in beiden Feature-Konfigurationen
- Wenn Stub-Match-Arme hinzugefügt werden, dokumentieren sie D-20 explizit ("Phase 2 stub")
- Bestehende Phase-1-E2E-Tests sind regression-frei
- Plan 06 (Wave 2) kann die Variante in `SessionServiceImpl::extract_auth_context` konstruieren, ohne weitere Type-Setup-Arbeit
- Wire-Point-Begründung (D-19) ist explizit dokumentiert: Status-Check landet in Plan 06 in `SessionServiceImpl::extract_auth_context`, nicht in der REST-Auth-Middleware
</success_criteria>

<output>
Erstelle nach Abschluss `.planning/phases/02-helfer-token-session-authcontext-helper/02-02-SUMMARY.md` mit:
- Anzahl der Cascade-Fix-Stellen (0 oder mehr)
- Files modifiziert mit konkreten Zeilen-Bereichen
- Bestätigung, dass beide Feature-Builds grün sind
- Hinweis für Plan 06 zum Verbrauch der Variante
- Wire-Point-Begründung (D-19) für Plan 06: Status-Check in SessionServiceImpl::extract_auth_context (cleaner Schicht-Trennung; mockbar via SessionServiceDeps)
</output>
