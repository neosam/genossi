---
phase: 02-helfer-token-session-authcontext-helper
plan: 05
type: execute
wave: 2
depends_on: [02-01, 02-03, 02-04]
files_modified:
  - genossi_service_impl/src/helper_token.rs
  - genossi_service_impl/src/lib.rs
autonomous: true
requirements: [HLPR-01, HLPR-02, HLPR-06, HLPR-07]
must_haves:
  truths:
    - "HelperTokenServiceImpl uses gen_service_impl! mit 7 Deps (locked, validated against macros.rs): HelperTokenDao, AssemblyDao, AuditLogDao, PermissionService, PermissionDao, SessionService, UuidService, TransactionDao — TransactionDao zählt als Plumbing wie in AssemblyServiceImpl; total 8 Felder im struct, davon 7 zusätzlich neben TransactionDao"
    - "create_helper_token: prüft admin-Permission, lädt Assembly (status in {Preparation, Open}), generiert Crockford-Code (10 chars, OsRng) + sha256_hex(code), baut HelperTokenEntity, ruft audited_create! mit process \"helper_token.create\" (D-07), rendert qr_svg via qrcode 0.14 EcLevel::Q mit URL `${APP_URL}/helper?code={code}` (D-12), commitet (D-25/Pitfall-3)"
    - "create_helper_token returnt HelperTokenCreated { token, code, qr_svg } — Code+qr_svg ONE-TIME (D-21)"
    - "list_for_assembly: prüft admin, ruft helper_token_dao.all_for_assembly, mappt zu HelperToken[] (D-21)"
    - "revoke_helper_token: prüft admin, lädt Assembly + Token, prüft assembly.status in {Preparation, Open} (D-23) AND token.used_at IS NULL (D-03), setzt revoked_at = now, ruft helper_token_dao.update (NICHT audited — D-08), liefert 409 für Used (D-03) und 409 für assembly Closed (D-23)"
    - "redeem_helper_token: validiert Code-Format (10 chars Crockford alphabet, ServiceError::ValidationError bei mismatch — D-09/D-24-400), berechnet sha256_hex(code), ruft helper_token_dao.atomic_redeem (D-25), bei 0 rows: differential lookup_status für 404/410/403 (D-24), verifiziert assembly.status==Open (D-18/D-24-403), ruft permission_dao.ensure_user_exists(`helper:<token_id>`, HELPER_USER_PROCESS=`helper-token-redeem`) (D-17 — eigener Process-Tag, nicht der inventur-token-auto-register-Tag aus session.rs:182-188) und session_service.create_session_with_claims (low-level, NICHT der ensure_user_and_create_session_with_claims-Wrapper) mit user_id=`helper:<token_id>`, expires=86400s, claims=`{\"kind\":\"helper\",\"assembly_id\":\"<uuid>\"}` (D-15/D-16/D-18), ruft helper_token_dao.set_session_id im SELBEN tx (Pitfall 3), commit, returnt HelperRedeemSuccess"
    - "Process-Konstante HELPER_TOKEN_PROCESS_CREATE = \"helper_token.create\" (D-07 dot-notation, Phase-1-D-11-Konvention)"
    - "Process-Konstante HELPER_USER_PROCESS = \"helper-token-redeem\" (D-17 — Tag für permission_dao.ensure_user_exists beim synthetischen Helper-User; bewusst NICHT der inventur-token-auto-register-Tag aus dem session_service-Wrapper)"
    - "ServiceError-Discriminator-Strings für D-24-Mapping: Conflict(\"already_used\") für 410, Conflict(\"revoked\") für 403, Conflict(\"assembly_not_open\") für 403"
    - "Klartext-Code wird NIRGENDS persistiert oder geloggt (auch nicht im tracing::debug!) — D-11"
    - "APP_URL wird via env::var gelesen mit Default \"http://localhost:3000/\" für Mock-Auth-Build (Plan 07 ergänzt fail-fast für OIDC-Build)"
    - "audited_create! ruft mit user_id aus permission_service.current_user_id() oder Fallback \"SYSTEM\" auf (Phase-1-Pattern aus assembly.rs:74-78)"
  artifacts:
    - path: genossi_service_impl/src/helper_token.rs
      provides: "HelperTokenServiceImpl + Crockford-Codegen + SHA256 + QR-Render + Atomic-Redeem-Orchestrierung"
      exports: ["HelperTokenServiceImpl", "HelperTokenServiceDeps"]
    - path: genossi_service_impl/src/lib.rs
      provides: "Modul-Re-export"
  key_links:
    - from: "HelperTokenServiceImpl::create_helper_token"
      to: "audit-Hashchain"
      via: 'audited_create!(self, self.helper_token_dao, &entity, "helper_token.create", &user_id, tx)'
      pattern: 'audited_create!.*"helper_token\.create"'
    - from: "HelperTokenServiceImpl::redeem_helper_token"
      to: "atomic redeem path (D-25)"
      via: "self.helper_token_dao.atomic_redeem(&token_hash, now, tx.clone()).await -> permission_dao.ensure_user_exists(helper_user_id, HELPER_USER_PROCESS) -> session_service.create_session_with_claims -> helper_token_dao.set_session_id"
      pattern: "atomic_redeem.*ensure_user_exists.*create_session_with_claims.*set_session_id"
    - from: "HelperTokenServiceImpl::redeem_helper_token"
      to: "differential 404/410/403 lookup (D-24)"
      via: "lookup_status when atomic_redeem returns None"
      pattern: "lookup_status.*EntityNotFound\\|already_used\\|revoked\\|assembly_not_open"
---

<objective>
Service-Implementierung des Helper-Token-Aggregats. Zentrale Datei `genossi_service_impl/src/helper_token.rs` mit `gen_service_impl!`-DI, vier Service-Methoden (create, list, revoke, redeem), drei freien Helper-Funktionen (Crockford-Codegen, SHA256-Hex, QR-Render) und der atomic-Redeem-Orchestrierung.

Purpose: Die zentrale Domain-Logik. HLPR-01 (create+QR), HLPR-02 (atomic redeem), HLPR-06 (list+revoke), HLPR-07 (audit on create) sind hier verdrahtet. Plan 06 (SessionService-Erweiterung) und Plan 07 (REST + DI-Wiring) bauen darauf auf.

Output: Compile-grüne Service-Implementation mit ≥ 6 Unit-Tests (Mocks via Mockall — DAO + AssemblyDao + SessionService + PermissionService).
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-helfer-token-session-authcontext-helper/02-CONTEXT.md
@.planning/phases/02-helfer-token-session-authcontext-helper/02-RESEARCH.md
@.planning/phases/02-helfer-token-session-authcontext-helper/02-PATTERNS.md
@.planning/phases/02-helfer-token-session-authcontext-helper/02-01-SUMMARY.md
@.planning/phases/02-helfer-token-session-authcontext-helper/02-04-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@genossi_service_impl/src/assembly.rs
@genossi_service_impl/src/audit_macros.rs
@genossi_service_impl/src/macros.rs
@genossi_service_impl/src/session.rs
@genossi_dao/src/helper_token.rs
@genossi_service/src/helper_token.rs

<interfaces>
<!-- gen_service_impl! macro syntax (genossi_service_impl/src/macros.rs): -->
```rust
gen_service_impl! {
    struct ServiceImpl: ServiceTrait = ServiceDeps {
        DepName: TraitName<Transaction = Self::Transaction> = field_name,
        // ...
    }
}
```

<!-- Phase-1-Service-Implementation als Vorlage (genossi_service_impl/src/assembly.rs:67-110): -->
```rust
async fn create_assembly(...) -> Result<Assembly, ServiceError> {
    let tx = self.transaction_dao.use_transaction(None).await?;
    let user_id = self.permission_service.current_user_id(context.clone()).await?
        .unwrap_or_else(|| "SYSTEM".to_string());
    self.permission_service.check_permission(ADMIN_PRIVILEGE, context).await?;

    let now = time::OffsetDateTime::now_utc();
    let created = time::PrimitiveDateTime::new(now.date(), now.time());

    let entity = AssemblyEntity { id: self.uuid_service.new_v4().await, /* ... */ };
    crate::audited_create!(self, self.assembly_dao, &entity, ASSEMBLY_PROCESS_CREATE, &user_id, tx);
    self.transaction_dao.commit(tx).await?;
    Ok(Assembly::from(&entity))
}
```

<!-- audited_create! macro (genossi_service_impl/src/audit_macros.rs:1-36) — required fields: -->
```
$self braucht: audit_log_dao, uuid_service
audited_create!($self, $dao, $entity, $process, $user_id, $tx)
```

<!-- session_service: low-level vs. wrapper. D-17 erfordert eigenen Process-Tag,
     deshalb gehen wir NICHT über den Wrapper (der hardcoded den inventur-Tag setzt),
     sondern rufen permission_dao.ensure_user_exists + create_session_with_claims separat. -->
```rust
// LOW-LEVEL (Plan 02-05 verwendet diesen Pfad):
//   genossi_service::session::SessionService::create_session_with_claims
async fn create_session_with_claims(
    &self,
    user_id: &str,
    expires_in_seconds: i64,
    claims: Option<String>,
) -> Result<UserSession, ServiceError>;

// PERMISSION-DAO (für synthetischen Helper-User-Auto-Register, D-17):
//   genossi_dao::permission::PermissionDao::ensure_user_exists
async fn ensure_user_exists(&self, user_id: &str, process: &str) -> Result<(), DaoError>;

// WRAPPER (NICHT verwendet — hardcoded inventur-token-auto-register; session.rs:175-189):
async fn ensure_user_and_create_session_with_claims(
    &self,
    user_id: &str,
    expires_in_seconds: i64,
    claims: Option<String>,
) -> Result<UserSession, ServiceError> {
    self.permission_dao.ensure_user_exists(user_id, "inventur-token-auto-register").await?;
    self.create_session_with_claims(user_id, expires_in_seconds, claims).await
}
```

<!-- AssemblyDao::find_by_id (default-impl filtert deleted IS NULL): -->
```rust
async fn find_by_id(&self, id: Uuid, tx: Self::Transaction) -> Result<Option<AssemblyEntity>, DaoError>;
```

<!-- AssemblyStatus enum: -->
```rust
pub enum AssemblyStatus { Preparation, Open, Closed }
```

<!-- HelperTokenDao methods (Plan 01 output): -->
```rust
async fn atomic_redeem(&self, token_hash: &str, used_at: PrimitiveDateTime, tx: Self::Transaction) -> Result<Option<(Uuid, Uuid)>, DaoError>;
async fn set_session_id(&self, token_id: Uuid, session_id: &str, tx: Self::Transaction) -> Result<(), DaoError>;
async fn lookup_status(&self, token_hash: &str, tx: Self::Transaction) -> Result<Option<(Option<PrimitiveDateTime>, Option<PrimitiveDateTime>)>, DaoError>;
async fn all_for_assembly(&self, assembly_id: Uuid, tx: Self::Transaction) -> Result<Arc<[HelperTokenEntity]>, DaoError>;
```

<!-- HelperTokenService trait (Plan 04 output) — methods to implement: -->
```rust
async fn create_helper_token(&self, assembly_id: Uuid, sub: &HelperTokenSubmission, ctx: Authentication<Self::Context>) -> Result<HelperTokenCreated, ServiceError>;
async fn list_for_assembly(&self, assembly_id: Uuid, ctx: Authentication<Self::Context>) -> Result<Arc<[HelperToken]>, ServiceError>;
async fn revoke_helper_token(&self, assembly_id: Uuid, token_id: Uuid, ctx: Authentication<Self::Context>) -> Result<HelperToken, ServiceError>;
async fn redeem_helper_token(&self, code: &str) -> Result<HelperRedeemSuccess, ServiceError>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Free helper functions (Crockford codegen + SHA256-Hex + QR render)</name>
  <files>genossi_service_impl/src/helper_token.rs</files>
  <read_first>
    - genossi_service_impl/src/assembly.rs (gesamte Datei — Imports und gen_service_impl!-Pattern)
    - genossi_service_impl/Cargo.toml (sha2 + qrcode + rand sind nach Plan 03 verfügbar)
    - .planning/phases/02-helfer-token-session-authcontext-helper/02-CONTEXT.md (D-09, D-10, D-11, D-12, D-13)
    - .planning/phases/02-helfer-token-session-authcontext-helper/02-RESEARCH.md (Pattern 3 Crockford, Pattern 4 QR-SVG; Code Examples)
    - .planning/phases/02-helfer-token-session-authcontext-helper/02-PATTERNS.md (Pattern für genossi_service_impl/src/helper_token.rs — die drei Hilfsfunktionen verbatim)
  </read_first>
  <behavior>
    - `generate_crockford_code(10)` liefert einen 10-Zeichen-String aus dem Alphabet `0123456789ABCDEFGHJKMNPQRSTVWXYZ` (32 Zeichen)
    - Verteilung ist gleichverteilt (b & 0x1f auf zufällige OsRng-Bytes)
    - Aufgerufen 1000-mal: kein Duplikat erwartet (mathematisch ~50 bit Entropie); aber Test soll nicht statistisch sein, sondern Format-Pruefung: alle Zeichen aus dem Alphabet, len == 10
    - `sha256_hex("ABC")` liefert lowercase 64-Zeichen-Hex ohne Prefix; deterministisch (gleicher Input → gleiches Ergebnis)
    - `render_qr_svg("https://example.com/helper?code=ABC1234567")` liefert einen String, der mit `<svg` beginnt und `</svg>` enthält
    - `validate_code_format("ABC1234567")` returnt Ok(()); `validate_code_format("ABC")` returnt Err mit field=code, message=invalid_length; `validate_code_format("abc1234567")` returnt Err (lowercase nicht im Alphabet); `validate_code_format("ABCD1234U7")` returnt Err (U nicht im Alphabet)
  </behavior>
  <action>
    Erstelle `genossi_service_impl/src/helper_token.rs` mit folgendem Datei-Anfang (Imports + Konstanten + drei Helper-Funktionen). Die DI-Skeleton und die Service-Methoden folgen in Task 2 — diese Aufgabe legt die Datei und die Hilfsfunktionen an, deren Korrektheit isoliert testbar ist.

    ```rust
    //! Service-layer implementation of the helper_token aggregate (Phase 2).
    //!
    //! Lifecycle: Open → Used (atomic redeem) | Open → Revoked (vorstand revoke).
    //! Status is DERIVED from columns (D-02): no Status field on the entity.
    //!
    //! Audit (D-07/D-08):
    //!   - create: audited via `audited_create!` with process "helper_token.create"
    //!   - update (revoke / set_session_id): NOT audited (D-08)
    //!
    //! Token codegen + storage:
    //!   - 10-char Crockford-Base32 plaintext, OsRng (D-09/D-10)
    //!   - Stored as SHA256(code) hex-lowercase (D-11)
    //!   - Plaintext returned ONCE in HelperTokenCreated.code; never logged
    //!
    //! Atomic redeem (D-25, Pitfall 3):
    //!   1. validate_code_format → 400
    //!   2. sha256_hex(code)
    //!   3. helper_token_dao.atomic_redeem in tx → Some/None
    //!   4. on None: lookup_status → 404 / 410 / 403 (D-24)
    //!   5. assembly_dao.find_by_id → status==Open or 403
    //!   6. permission_dao.ensure_user_exists(helper:<token_id>, HELPER_USER_PROCESS="helper-token-redeem") + session_service.create_session_with_claims (claims kind=helper)
    //!   7. helper_token_dao.set_session_id IN SAME TX (Pitfall 3)
    //!   8. commit → return HelperRedeemSuccess

    use async_trait::async_trait;
    use genossi_dao::assembly::{AssemblyDao, AssemblyStatus};
    use genossi_dao::audit_log::AuditLogDao;
    use genossi_dao::helper_token::{HelperTokenDao, HelperTokenEntity};
    use genossi_dao::permission::PermissionDao;
    use genossi_dao::TransactionDao;
    use genossi_service::helper_token::{
        HelperRedeemSuccess, HelperToken, HelperTokenCreated, HelperTokenService,
        HelperTokenSubmission,
    };
    use genossi_service::permission::{Authentication, PermissionService};
    use genossi_service::session::SessionService;
    use genossi_service::uuid_service::UuidService;
    use genossi_service::{ServiceError, ValidationFailureItem};
    use qrcode::render::svg;
    use qrcode::{EcLevel, QrCode};
    use rand::{rngs::OsRng, RngCore};
    use sha2::{Digest, Sha256};
    use std::sync::Arc;
    use time::format_description::well_known::Iso8601;
    use uuid::Uuid;

    use crate::gen_service_impl;

    /// Process identifier for the audit hash chain (D-07, dot-notation per Phase-1-D-11).
    const HELPER_TOKEN_PROCESS_CREATE: &str = "helper_token.create";

    /// Required privilege for token-management endpoints (D-21).
    const ADMIN_PRIVILEGE: &str = "admin";

    /// Auto-register process tag for the synthetic helper user (D-17).
    const HELPER_USER_PROCESS: &str = "helper-token-redeem";

    /// Crockford-Base32 alphabet (D-09): excludes I, L, O, U for human readability.
    const CROCKFORD_ALPHABET: &[u8; 32] = b"0123456789ABCDEFGHJKMNPQRSTVWXYZ";

    /// Plaintext code length (D-09): fixed 10 chars => 50 bit entropy.
    const CODE_LENGTH: usize = 10;

    /// Session lifetime in seconds (D-18): 24h ab redeem.
    const HELPER_SESSION_LIFETIME_SECS: i64 = 24 * 60 * 60;

    /// Generate a fresh 10-char Crockford-Base32 plaintext code.
    /// Uses OsRng (cryptographically secure, D-10).
    /// Bias-Analyse: `b & 0x1f` partitioniert 256 source-bytes auf 32 buckets gleichmäßig.
    pub fn generate_crockford_code() -> String {
        let mut buf = [0u8; CODE_LENGTH];
        OsRng.fill_bytes(&mut buf);
        buf.iter()
            .map(|&b| CROCKFORD_ALPHABET[(b & 0x1f) as usize] as char)
            .collect()
    }

    /// SHA256 of input, lowercase hex (D-11). Used as token_hash.
    /// Salting NOT required: 50-bit pre-image entropy makes rainbow tables irrelevant.
    pub fn sha256_hex(input: &str) -> String {
        let mut hasher = Sha256::new();
        hasher.update(input.as_bytes());
        format!("{:x}", hasher.finalize())
    }

    /// Render a QR-Code-SVG with `EcLevel::Q` (high error-correction for printed codes; D-13).
    pub fn render_qr_svg(payload: &str) -> Result<String, ServiceError> {
        let code = QrCode::with_error_correction_level(payload.as_bytes(), EcLevel::Q)
            .map_err(|e| ServiceError::InternalError(Arc::from(format!("QR generate failed: {}", e))))?;
        Ok(code.render::<svg::Color>().build())
    }

    /// Validate that a redeem-code matches the D-09 format.
    /// Returns `ServiceError::ValidationError` mapped to HTTP 400 (D-24).
    pub fn validate_code_format(code: &str) -> Result<(), ServiceError> {
        let mut errors: Vec<ValidationFailureItem> = Vec::new();
        if code.chars().count() != CODE_LENGTH {
            errors.push(ValidationFailureItem {
                field: Arc::from("code"),
                message: Arc::from(format!("invalid_length (expected {})", CODE_LENGTH)),
            });
        } else if !code.chars().all(|c| (CROCKFORD_ALPHABET as &[u8]).contains(&(c as u8))) {
            errors.push(ValidationFailureItem {
                field: Arc::from("code"),
                message: Arc::from("invalid_alphabet (use Crockford base32 uppercase)"),
            });
        }
        if errors.is_empty() {
            Ok(())
        } else {
            Err(ServiceError::ValidationError(errors))
        }
    }

    /// Read APP_URL with mock-friendly default (D-12, RESEARCH-A4).
    /// In OIDC build APP_URL is required at server start (Plan 07 wires fail-fast); in
    /// mock_auth (Tests) we accept the default to keep e2e-tests hermetic.
    fn app_url() -> String {
        std::env::var("APP_URL").unwrap_or_else(|_| "http://localhost:3000/".to_string())
    }

    // ============================================================================
    // Service-Impl + 4 Methoden — Task 2 fügt diese Sektion hinzu
    // ============================================================================
    ```

    Tests in `#[cfg(test)] mod tests` am Datei-Ende (Task 2 ergänzt Service-Methoden-Tests; hier nur Helper-Tests):

    ```rust
    #[cfg(test)]
    mod helper_fn_tests {
        use super::*;

        #[test]
        fn test_generate_crockford_code_length_and_alphabet() {
            for _ in 0..100 {
                let code = generate_crockford_code();
                assert_eq!(code.len(), 10, "code must be 10 chars; got {}", code);
                for c in code.chars() {
                    assert!(
                        (CROCKFORD_ALPHABET as &[u8]).contains(&(c as u8)),
                        "char {} not in Crockford alphabet", c
                    );
                }
            }
        }

        #[test]
        fn test_generate_crockford_code_is_random() {
            // Two consecutive calls should differ with overwhelming probability (50-bit entropy).
            let a = generate_crockford_code();
            let b = generate_crockford_code();
            assert_ne!(a, b, "two consecutive codes equal — RNG broken? a={}, b={}", a, b);
        }

        #[test]
        fn test_sha256_hex_deterministic() {
            assert_eq!(sha256_hex("ABC1234567"), sha256_hex("ABC1234567"));
            assert_ne!(sha256_hex("ABC1234567"), sha256_hex("ABC1234568"));
            // Length must be 64 hex chars (256 bit).
            assert_eq!(sha256_hex("anything").len(), 64);
            // Lowercase only.
            assert!(sha256_hex("X").chars().all(|c| c.is_ascii_lowercase() || c.is_ascii_digit()));
        }

        #[test]
        fn test_render_qr_svg_starts_with_svg_tag() {
            let svg = render_qr_svg("https://example.com/helper?code=ABC1234567").unwrap();
            assert!(svg.starts_with("<?xml") || svg.starts_with("<svg"), "qr svg must start with xml or svg tag; got: {}", &svg[..50.min(svg.len())]);
            assert!(svg.contains("</svg>"), "qr svg must contain closing tag");
        }

        #[test]
        fn test_validate_code_format_accepts_valid() {
            assert!(validate_code_format("ABC1234567").is_ok());
            assert!(validate_code_format("0123456789").is_ok());
            assert!(validate_code_format("ZYXWVTSRQP").is_ok());
        }

        #[test]
        fn test_validate_code_format_rejects_wrong_length() {
            assert!(matches!(validate_code_format("ABC"), Err(ServiceError::ValidationError(_))));
            assert!(matches!(validate_code_format("ABCDEFGHIJK"), Err(ServiceError::ValidationError(_))));
            assert!(matches!(validate_code_format(""), Err(ServiceError::ValidationError(_))));
        }

        #[test]
        fn test_validate_code_format_rejects_invalid_alphabet() {
            // Lowercase
            assert!(matches!(validate_code_format("abc1234567"), Err(ServiceError::ValidationError(_))));
            // Forbidden chars I, L, O, U (Crockford excludes these)
            assert!(matches!(validate_code_format("ABCI234567"), Err(ServiceError::ValidationError(_))));
            assert!(matches!(validate_code_format("ABCL234567"), Err(ServiceError::ValidationError(_))));
            assert!(matches!(validate_code_format("ABCO234567"), Err(ServiceError::ValidationError(_))));
            assert!(matches!(validate_code_format("ABCU234567"), Err(ServiceError::ValidationError(_))));
        }
    }
    ```
  </action>
  <verify>
    <automated>cargo test -p genossi_service_impl helper_fn_tests 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p genossi_service_impl` exit 0
    - `cargo test -p genossi_service_impl helper_fn_tests` exit 0 mit ≥ 7 Tests
    - `grep -c 'const HELPER_TOKEN_PROCESS_CREATE: &str = "helper_token.create"' genossi_service_impl/src/helper_token.rs` == 1
    - `grep -c 'const CROCKFORD_ALPHABET: &.u8; 32. = b"0123456789ABCDEFGHJKMNPQRSTVWXYZ"' genossi_service_impl/src/helper_token.rs` == 1
    - `grep -c 'const CODE_LENGTH: usize = 10' genossi_service_impl/src/helper_token.rs` == 1
    - `grep -c 'const HELPER_SESSION_LIFETIME_SECS: i64 = 24 \* 60 \* 60' genossi_service_impl/src/helper_token.rs` == 1
    - `grep -c "pub fn generate_crockford_code" genossi_service_impl/src/helper_token.rs` == 1
    - `grep -c "pub fn sha256_hex" genossi_service_impl/src/helper_token.rs` == 1
    - `grep -c "pub fn render_qr_svg" genossi_service_impl/src/helper_token.rs` == 1
    - `grep -c "OsRng.fill_bytes" genossi_service_impl/src/helper_token.rs` == 1
    - `grep -c "EcLevel::Q" genossi_service_impl/src/helper_token.rs` == 1
    - `grep -c "Sha256::new" genossi_service_impl/src/helper_token.rs` == 1
  </acceptance_criteria>
  <done>
    Datei existiert mit Imports, 4 Konstanten, 4 freien Hilfsfunktionen (generate_crockford_code, sha256_hex, render_qr_svg, validate_code_format) und ≥7 Unit-Tests grün. Bereit für Task 2 (Service-Impl-Body).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HelperTokenServiceImpl mit gen_service_impl! + 4 Service-Methoden</name>
  <files>
    genossi_service_impl/src/helper_token.rs,
    genossi_service_impl/src/lib.rs
  </files>
  <read_first>
    - genossi_service_impl/src/helper_token.rs (Task-1-Ergebnis — Imports + Konstanten + Helper-Fns)
    - genossi_service_impl/src/assembly.rs (gesamte Datei — Vorlage für Service-Methoden mit Audit-Macros, Lifecycle-Guards, Optional-TX)
    - genossi_service_impl/src/macros.rs (gen_service_impl!-Macro-Definition)
    - genossi_service_impl/src/audit_macros.rs (audited_create!-Macro)
    - genossi_service_impl/src/session.rs (Zeilen 52-82: create_session_with_claims — low-level, wir verwenden DIESEN; Zeilen 175-189: ensure_user_and_create_session_with_claims-Wrapper, hardcoded "inventur-token-auto-register" — NICHT verwenden, D-17 fordert eigenen Tag)
    - genossi_dao/src/permission.rs (PermissionDao::ensure_user_exists Signatur)
    - genossi_service/src/session.rs (SessionService trait-Definition für Mockall)
    - genossi_service/src/permission.rs (PermissionService trait + Authentication enum)
    - genossi_dao/src/assembly.rs (AssemblyStatus enum)
    - .planning/phases/02-helfer-token-session-authcontext-helper/02-CONTEXT.md (D-03, D-07, D-08, D-12, D-15, D-16, D-17, D-18, D-23, D-24, D-25)
    - .planning/phases/02-helfer-token-session-authcontext-helper/02-RESEARCH.md (Architecture-Diagramm REDEEM PATH; Pitfall 3)
  </read_first>
  <behavior>
    - HelperTokenServiceImpl baut via gen_service_impl! mit 8 Deps
    - create_helper_token: prüft admin, lädt Assembly (status in {Preparation, Open}; sonst Conflict), generiert Code, hash, qr_svg; ruft audited_create!; commitet
    - list_for_assembly: prüft admin, ruft helper_token_dao.all_for_assembly, mappt zu HelperToken[]
    - revoke_helper_token: prüft admin, lädt Assembly (status in {Preparation, Open}; sonst Conflict), lädt Token (existieren + token.assembly_id == path-assembly_id), prüft used_at IS NULL (sonst Conflict("already_used")), setzt revoked_at, ruft helper_token_dao.update (NICHT audited per D-08)
    - redeem_helper_token: validate_code → sha256_hex → atomic_redeem → bei None differential lookup_status → bei Some assembly_dao.find_by_id assembly.status==Open prüfen → user_id=`helper:<token_id>` → claims=JSON → permission_dao.ensure_user_exists(helper_user_id, HELPER_USER_PROCESS) → session_service.create_session_with_claims (low-level) → set_session_id IN SAME TX → commit
    - ServiceError-Discriminator-Strings für D-24:
      * already_used → 410
      * revoked → 403
      * assembly_not_open → 403
      * EntityNotFound für unknown hash → 404
      * ValidationError für invalid format → 400
    - Klartext-Code wird NIRGENDS geloggt
  </behavior>
  <action>
    Erweitere `genossi_service_impl/src/helper_token.rs` (Task-1-Datei) um die DI-Skeleton und die 4 Service-Methoden. Konkrete Sektionen:

    **gen_service_impl! DI-Skeleton** (nach den Helper-Funktionen, vor dem cfg(test)-Block):

    Die Deps-Liste ist gegen `genossi_service_impl/src/macros.rs` und das Phase-1-`AssemblyServiceImpl`-Skeleton in `genossi_service_impl/src/assembly.rs:50-60` validiert. Das Macro generiert pro Eintrag ein `pub $field_attr: Arc<Deps::$field_name>` und einen Trait-Eintrag `type $field_name: $field_type + Sync + Send + 'static`. SessionService hat KEIN Transaction-Associated-Type — daher wird er als `SessionService = session_service` ohne `<Transaction = ...>`-Bound eingetragen (Macro-zulässig: `$field_type:path` greift jeden Pfad inkl. trait-ohne-Generics).

    **Locked deterministic block (7 zusätzliche Deps + TransactionDao = 8 Felder im struct):**

    ```rust
    gen_service_impl! {
        struct HelperTokenServiceImpl: HelperTokenService = HelperTokenServiceDeps {
            HelperTokenDao: HelperTokenDao<Transaction = Self::Transaction> = helper_token_dao,
            AssemblyDao: AssemblyDao<Transaction = Self::Transaction> = assembly_dao,
            AuditLogDao: AuditLogDao<Transaction = Self::Transaction> = audit_log_dao,
            PermissionService: PermissionService<Context = Self::Context> = permission_service,
            PermissionDao: PermissionDao<Transaction = Self::Transaction> = permission_dao,
            SessionService: SessionService = session_service,
            UuidService: UuidService = uuid_service,
            TransactionDao: TransactionDao<Transaction = Self::Transaction> = transaction_dao,
        }
    }
    ```

    `PermissionDao` IST in dem Block (D-17 erfordert es): wir rufen direkt `self.permission_dao.ensure_user_exists("helper:<token_id>", HELPER_USER_PROCESS).await?` mit dem eigenen Process-Tag `"helper-token-redeem"`, statt den `ensure_user_and_create_session_with_claims`-Wrapper zu verwenden, der hardcoded den `"inventur-token-auto-register"`-Tag aus session.rs:182-188 setzt. Anschließend ruft der Service `self.session_service.create_session_with_claims(...)` (low-level) für die eigentliche Session-Erzeugung. Das wahrt D-17 (forensisch separable Helper-Redemption-Spur).

    **Fallback-Bedingung (NICHT erwartet):** Falls beim Build die generierte Deps-Trait-Definition durch Mockall-Limit oder fehlende `Send + Sync`-Bound auf `SessionService` fehlschlägt, schreibe struct + impl-Block per Hand (Pattern: `pub struct HelperTokenServiceImpl<Deps: HelperTokenServiceDeps> { pub helper_token_dao: Arc<Deps::HelperTokenDao>, ... }` + `impl<Deps: HelperTokenServiceDeps> HelperTokenServiceImpl<Deps> { pub fn new(...) -> Self { ... } }`) und dokumentiere die Abweichung in einem Datei-Header-Kommentar. Die HelperTokenServiceDeps-Trait-Definition bleibt in jedem Fall identisch.

    **Service-Trait-Implementation** (nach gen_service_impl!-Block):

    ```rust
    #[async_trait]
    impl<Deps: HelperTokenServiceDeps> HelperTokenService for HelperTokenServiceImpl<Deps> {
        type Context = Deps::Context;
        type Transaction = Deps::Transaction;

        async fn create_helper_token(
            &self,
            assembly_id: Uuid,
            submission: &HelperTokenSubmission,
            context: Authentication<Self::Context>,
        ) -> Result<HelperTokenCreated, ServiceError> {
            let tx = self.transaction_dao.use_transaction(None).await?;

            // Permission + user_id (Phase-1-Pattern).
            let user_id = self
                .permission_service
                .current_user_id(context.clone())
                .await?
                .unwrap_or_else(|| "SYSTEM".to_string());
            self.permission_service
                .check_permission(ADMIN_PRIVILEGE, context)
                .await?;

            // Assembly-Lifecycle-Guard: token erzeugen ist nur in Preparation oder Open zulässig.
            let assembly = self
                .assembly_dao
                .find_by_id(assembly_id, tx.clone())
                .await?
                .ok_or(ServiceError::EntityNotFound(assembly_id))?;
            if assembly.status == AssemblyStatus::Closed {
                return Err(ServiceError::Conflict(Arc::from(
                    "Cannot create helper_token: assembly status is Closed",
                )));
            }

            // Codegen + hash + qr_svg.
            let code = generate_crockford_code();
            let token_hash = sha256_hex(&code);
            let payload = format!("{}helper?code={}", app_url().trim_end_matches('/'), code);
            // Hinweis: trim_end_matches('/') vermeidet Double-Slash falls APP_URL mit / endet.
            // Zwischen domain und path setzen wir manuell '/' — `format!("{}/helper?...", base)` ginge auch,
            // aber mit explizitem trim+`/helper?` ist die URL deterministisch.
            let qr_svg = render_qr_svg(&format!("{}/helper?code={}", app_url().trim_end_matches('/'), code))?;

            // Build entity.
            let now = time::OffsetDateTime::now_utc();
            let created = time::PrimitiveDateTime::new(now.date(), now.time());
            let entity = HelperTokenEntity {
                id: self.uuid_service.new_v4().await,
                assembly_id,
                memo: submission.memo.clone(),
                token_hash: Arc::from(token_hash.as_str()),
                created,
                used_at: None,
                session_id: None,
                revoked_at: None,
                deleted: None,
                version: self.uuid_service.new_v4().await,
            };

            crate::audited_create!(
                self,
                self.helper_token_dao,
                &entity,
                HELPER_TOKEN_PROCESS_CREATE,
                &user_id,
                tx
            );

            self.transaction_dao.commit(tx).await?;

            Ok(HelperTokenCreated {
                token: HelperToken::from(&entity),
                code: Arc::from(code.as_str()),
                qr_svg: Arc::from(qr_svg.as_str()),
            })
        }

        async fn list_for_assembly(
            &self,
            assembly_id: Uuid,
            context: Authentication<Self::Context>,
        ) -> Result<Arc<[HelperToken]>, ServiceError> {
            let tx = self.transaction_dao.use_transaction(None).await?;
            self.permission_service
                .check_permission(ADMIN_PRIVILEGE, context)
                .await?;
            let entities = self
                .helper_token_dao
                .all_for_assembly(assembly_id, tx.clone())
                .await?;
            self.transaction_dao.commit(tx).await?;
            let domain: Vec<HelperToken> = entities.iter().map(HelperToken::from).collect();
            Ok(Arc::from(domain))
        }

        async fn revoke_helper_token(
            &self,
            assembly_id: Uuid,
            token_id: Uuid,
            context: Authentication<Self::Context>,
        ) -> Result<HelperToken, ServiceError> {
            let tx = self.transaction_dao.use_transaction(None).await?;
            self.permission_service
                .check_permission(ADMIN_PRIVILEGE, context)
                .await?;

            // Assembly-Status-Guard (D-23).
            let assembly = self
                .assembly_dao
                .find_by_id(assembly_id, tx.clone())
                .await?
                .ok_or(ServiceError::EntityNotFound(assembly_id))?;
            if assembly.status == AssemblyStatus::Closed {
                return Err(ServiceError::Conflict(Arc::from(
                    "Cannot revoke helper_token: assembly status is Closed",
                )));
            }

            // Token-Existenz.
            let mut token = self
                .helper_token_dao
                .find_by_id(token_id, tx.clone())
                .await?
                .ok_or(ServiceError::EntityNotFound(token_id))?;

            // Path-Konsistenz: token muss zu der genannten Assembly gehören.
            if token.assembly_id != assembly_id {
                return Err(ServiceError::EntityNotFound(token_id));
            }

            // D-03: Revoke nur wenn used_at IS NULL.
            if token.used_at.is_some() {
                return Err(ServiceError::Conflict(Arc::from("already_used")));
            }
            // Idempotenz: revoke eines bereits revoked Tokens schlägt fehl mit Conflict.
            if token.revoked_at.is_some() {
                return Err(ServiceError::Conflict(Arc::from("already_revoked")));
            }

            // Mutation.
            let now = time::OffsetDateTime::now_utc();
            token.revoked_at = Some(time::PrimitiveDateTime::new(now.date(), now.time()));
            token.version = self.uuid_service.new_v4().await;

            // D-08: revoke wird NICHT auditiert. Direct DAO-Update.
            self.helper_token_dao
                .update(&token, "helper_token.revoke", tx.clone())
                .await?;

            self.transaction_dao.commit(tx).await?;
            Ok(HelperToken::from(&token))
        }

        async fn redeem_helper_token(
            &self,
            code: &str,
        ) -> Result<HelperRedeemSuccess, ServiceError> {
            // 1. Format-validation (D-24-400).
            validate_code_format(code)?;

            // 2. Hash.
            let token_hash = sha256_hex(code);

            let tx = self.transaction_dao.use_transaction(None).await?;
            let now = time::OffsetDateTime::now_utc();
            let now_pdt = time::PrimitiveDateTime::new(now.date(), now.time());

            // 3. Atomic redeem (D-25).
            let redeem_result = self
                .helper_token_dao
                .atomic_redeem(&token_hash, now_pdt, tx.clone())
                .await?;

            let (token_id, assembly_id) = match redeem_result {
                Some(t) => t,
                None => {
                    // 4. Differential lookup_status (D-24).
                    let status = self
                        .helper_token_dao
                        .lookup_status(&token_hash, tx.clone())
                        .await?;
                    self.transaction_dao.commit(tx).await?; // close TX cleanly
                    return match status {
                        None => Err(ServiceError::EntityNotFound(Uuid::nil())), // 404
                        Some((_, Some(_))) => Err(ServiceError::Conflict(Arc::from("revoked"))), // 403
                        Some((Some(_), None)) => Err(ServiceError::Conflict(Arc::from("already_used"))), // 410
                        Some((None, None)) => {
                            // Should not happen — atomic_redeem would have succeeded.
                            // Defensive: treat as unknown.
                            Err(ServiceError::EntityNotFound(Uuid::nil()))
                        }
                    };
                }
            };

            // 5. Assembly-Status-Check (D-18/D-24-403).
            let assembly = self
                .assembly_dao
                .find_by_id(assembly_id, tx.clone())
                .await?
                .ok_or(ServiceError::EntityNotFound(assembly_id))?;
            if assembly.status != AssemblyStatus::Open {
                // Token wurde bereits "verbrannt" durch atomic_redeem — D-18 akzeptiert das.
                // RESEARCH Pitfall 6: D-18 garantiert Session-Invalidate; verbrannter Token ist OK.
                self.transaction_dao.commit(tx).await?;
                return Err(ServiceError::Conflict(Arc::from("assembly_not_open")));
            }

            // 6. Synthetischer User pro Token (D-17).
            let helper_user_id = format!("helper:{}", token_id);

            // 7. Claims-JSON (D-16).
            let claims_json = serde_json::json!({
                "kind": "helper",
                "assembly_id": assembly_id.to_string(),
            })
            .to_string();

            // 8a. Synthetischen Helper-User mit eigenem Process-Tag registrieren (D-17).
            //     Der wrapper session_service.ensure_user_and_create_session_with_claims
            //     verwendet hardcoded "inventur-token-auto-register" (session.rs:182-188).
            //     Wir wollen forensisch separierbar bleiben → eigener Process-Tag.
            //     permission_dao.ensure_user_exists hat Signatur:
            //       async fn ensure_user_exists(&self, user_id: &str, process: &str) -> Result<(), DaoError>
            self.permission_dao
                .ensure_user_exists(&helper_user_id, HELPER_USER_PROCESS)
                .await?;

            // 8b. Session erzeugen mit claims (low-level, D-15/D-16/D-18).
            //     session_service.create_session_with_claims hat Signatur:
            //       async fn create_session_with_claims(&self, user_id: &str,
            //         expires_in_seconds: i64, claims: Option<String>) -> Result<UserSession, ServiceError>
            let session = self
                .session_service
                .create_session_with_claims(
                    &helper_user_id,
                    HELPER_SESSION_LIFETIME_SECS,
                    Some(claims_json),
                )
                .await?;

            // 9. set_session_id im SELBEN TX (Pitfall 3).
            self.helper_token_dao
                .set_session_id(token_id, &session.session_id, tx.clone())
                .await?;

            self.transaction_dao.commit(tx).await?;

            Ok(HelperRedeemSuccess {
                session_id: session.session_id.clone(),
                assembly_id,
                expires_at: session.expires_at,
            })
        }
    }
    ```

    **Datei 2: `genossi_service_impl/src/lib.rs`** — füge `pub mod helper_token;` alphabetisch zwischen `document_storage` und `macros` ein (oder analog der bestehenden Reihenfolge):
    ```rust
    pub mod application;
    pub mod assembly;
    pub mod audit_log;
    pub mod audit_macros;
    pub mod document_storage;
    pub mod helper_token;     // NEU
    pub mod macros;
    // ... rest
    ```

    Tests in `#[cfg(test)] mod service_tests` (Mockall-basiert; Pattern: genossi_service_impl/src/assembly.rs Test-Sektion). MIND. 6 Tests. Exemplarisch:

    1. `test_create_helper_token_emits_audit_log_with_correct_process`:
       - Setup: MockHelperTokenDao erwartet `create()` mit beliebigem entity
       - MockAuditLogDao erwartet `get_latest_hash` + `create_entries` mit process="helper_token.create" (genau in einer der entries)
       - MockPermissionService.check_permission(admin, _) returnt Ok(())
       - MockAssemblyDao.find_by_id returnt assembly mit status=Open
       - MockUuidService.new_v4 returnt deterministische UUIDs
       - Aufruf create_helper_token + Assert: result.code.len() == 10, result.qr_svg.starts_with("<")

    2. `test_create_helper_token_rejects_when_assembly_closed`:
       - Setup: MockAssemblyDao returnt assembly mit status=Closed
       - Assert: Err(Conflict(_))

    3. `test_revoke_rejects_already_used_token`:
       - Setup: MockHelperTokenDao.find_by_id returnt token mit used_at=Some(_)
       - MockAssemblyDao returnt assembly mit status=Open
       - Assert: Err(Conflict(arc.contains("already_used")))

    4. `test_revoke_rejects_when_assembly_closed`:
       - Setup: MockAssemblyDao returnt assembly mit status=Closed
       - Assert: Err(Conflict(_))

    5. `test_redeem_rejects_invalid_code_format`:
       - Aufruf redeem_helper_token("abc") (lowercase, zu kurz)
       - Assert: Err(ValidationError(_))

    6. `test_redeem_returns_410_for_already_used_via_lookup`:
       - Setup: MockHelperTokenDao.atomic_redeem returnt None
       - MockHelperTokenDao.lookup_status returnt Some((Some(now), None))
       - Assert: Err(Conflict(arc.contains("already_used")))

    7. `test_redeem_returns_403_for_revoked_via_lookup`:
       - Setup: atomic_redeem→None, lookup_status→Some((None, Some(now)))
       - Assert: Err(Conflict(arc.contains("revoked")))

    8. `test_redeem_returns_404_for_unknown_via_lookup`:
       - Setup: atomic_redeem→None, lookup_status→None
       - Assert: Err(EntityNotFound(_))

    Hinweis zu Mockall-Konstruktion: SessionService hat KEIN Transaction-Associated-Type. MockSessionService kann mit `mockall::automock!` direkt generiert werden. Falls die Plan-04-Implementation `MockHelperTokenService` bereits über `#[automock]` exportiert, ist das die Mock-Quelle für die Service-Tests.

    Wenn die Test-Mocks zu komplex werden (alle 7 Deps stubben), reduziere auf die 4 wichtigsten Tests (1, 3, 5, 6) und überlasse die Vollständigkeit den E2E-Tests in Plan 08.
  </action>
  <verify>
    <automated>cargo test -p genossi_service_impl helper_token 2>&1 | tail -30 && cargo build --workspace 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p genossi_service_impl` exit 0
    - `cargo build --workspace --features mock_auth` exit 0
    - `cargo test -p genossi_service_impl helper_token` exit 0 mit ≥ 4 Service-Tests UND ≥ 7 Helper-Function-Tests aus Task 1 (insgesamt ≥ 11)
    - `grep -c "gen_service_impl!" genossi_service_impl/src/helper_token.rs` == 1
    - `grep -c "struct HelperTokenServiceImpl" genossi_service_impl/src/helper_token.rs` == 1
    - `grep -c "impl<Deps: HelperTokenServiceDeps> HelperTokenService for HelperTokenServiceImpl" genossi_service_impl/src/helper_token.rs` == 1
    - `grep -c "audited_create!" genossi_service_impl/src/helper_token.rs` == 1
    - `grep -c '"helper_token.create"' genossi_service_impl/src/helper_token.rs` ≥ 2 (Konstante + Macro-Aufruf-Argument)
    - `grep -c "atomic_redeem" genossi_service_impl/src/helper_token.rs` ≥ 1
    - `grep -c "lookup_status" genossi_service_impl/src/helper_token.rs` ≥ 1
    - `grep -c "set_session_id" genossi_service_impl/src/helper_token.rs` ≥ 1
    - `grep -c "create_session_with_claims" genossi_service_impl/src/helper_token.rs` >= 1
    - `grep -c "ensure_user_and_create_session_with_claims" genossi_service_impl/src/helper_token.rs` == 0  # D-17: wir gehen NICHT über den inventur-token-auto-register-Wrapper
    - `grep -c "permission_dao.ensure_user_exists" genossi_service_impl/src/helper_token.rs` >= 1  # D-17: synthetischer Helper-User mit eigenem Process-Tag
    - `grep -c "HELPER_USER_PROCESS" genossi_service_impl/src/helper_token.rs` >= 2  # const + Aufruf
    - `grep -c "helper-token-redeem" genossi_service_impl/src/helper_token.rs` == 1  # const-Wert verifiziert
    - `grep -c '"already_used"' genossi_service_impl/src/helper_token.rs` ≥ 2 (revoke + redeem-410)
    - `grep -c '"revoked"' genossi_service_impl/src/helper_token.rs` ≥ 1
    - `grep -c '"assembly_not_open"' genossi_service_impl/src/helper_token.rs` ≥ 1
    - `grep -c "kind.*helper.*assembly_id" genossi_service_impl/src/helper_token.rs` ≥ 1 (Claims-JSON-Schema D-16)
    - `grep -c 'format!("helper:{}"' genossi_service_impl/src/helper_token.rs` ≥ 1 (D-17 user_id-pattern)
    - `grep -c 'tracing::debug!.*code' genossi_service_impl/src/helper_token.rs` == 0 (Anti-Pattern: kein Klartext-Log)
    - `grep -c "pub mod helper_token" genossi_service_impl/src/lib.rs` == 1
  </acceptance_criteria>
  <done>
    Voll-funktionsfähige HelperTokenServiceImpl mit allen 4 Methoden, gen_service_impl!-DI, audited_create!-Integration, atomic-redeem-Orchestrierung mit differential lookup_status für D-24-Mapping. Mockall-basierte Unit-Tests grün. Klartext-Code wird nirgends geloggt (verifiziert per grep).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service→token storage | Klartext-Code wird in HelperTokenCreated.code an REST-Layer weitergegeben (1× Response), aber NIE in DB persistiert. SHA256(code) ist die einzige DB-sichtbare Form. |
| service→session | Session wird via SessionService::ensure_user_and_create_session_with_claims erzeugt; user_id ist `helper:<token_id>` (synthetisch); claims-JSON ist Backend-konstruiert (kein Client-Input). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Information Disclosure | tracing::debug! leakt Klartext-Code | mitigate | Anti-Pattern in Action explizit; grep-Acceptance-Criterion belegt: kein `tracing::debug!.*code`. Klartext lebt nur in lokalen Variables und im Response-Body. |
| T-02-05-02 | Tampering | Race-Condition zwischen atomic_redeem und set_session_id | mitigate | Pitfall 3: BEIDE UPDATES laufen in derselben TX (`tx.clone()`). Bei Crash zwischen den UPDATEs → Rollback der gesamten TX → Token bleibt Open. |
| T-02-05-03 | Spoofing | Forged claims-JSON erlaubt assembly_id-Manipulation | mitigate | claims-JSON wird vom Service konstruiert (`json!({"kind":"helper","assembly_id": assembly_id.to_string()})`). assembly_id kommt aus `helper_token_dao.atomic_redeem()`-RETURNING-Wert (DB-Source-of-Truth), nicht aus Client-Input. |
| T-02-05-04 | Elevation of Privilege | Helfer-Session hat admin-Permissions | mitigate | session_service.ensure_user erzeugt User mit user_id=`helper:<token_id>` ohne admin-Rolle; PermissionDao.has_privilege returnt false. Plan 02 stellt zusätzlich sicher, dass AuthContext::Helper in check_permission kein Bypass-Pfad ist. |
| T-02-05-05 | Denial of Service | Brute-Force-Code-Guessing | mitigate | 50-bit Crockford-Entropie + UNIQUE-Index (Plan 01) macht Brute-Force impractical. Plan 07 ergänzt Pro-IP-Rate-Limit auf /api/helper/redeem. |
| T-02-05-06 | Tampering | Race-Bedingung zwischen Redeem und Assembly-Close | accept | Pitfall 6 (RESEARCH): D-18 invalidiert die Session sofort beim ersten verify_user_session-Aufruf, falls Assembly inzwischen Closed ist. "Verbrannter" Token ist akzeptabel — Token ist One-Time-Use, das wahrt HLPR-04. |
</threat_model>

<verification>
- `cargo build --workspace --features mock_auth` grün
- `cargo test -p genossi_service_impl helper_token` grün mit ≥ 11 Tests (7 Helper + 4 Service)
- `cargo clippy --workspace --all-targets` ohne neue Warnings
- `cargo fmt --check` grün
- `grep -c 'tracing::debug!.*code' genossi_service_impl/src/helper_token.rs` == 0 (Klartext-Log-Verbot)
- `grep -c 'tracing::info!.*code' genossi_service_impl/src/helper_token.rs` == 0
- Bestehende Tests (Phase 1, AssemblyService etc.) bleiben grün
</verification>

<success_criteria>
- HelperTokenServiceImpl ist voll funktionsfähig mit allen 4 Methoden (D-21, D-22)
- create_helper_token ruft `audited_create!(..., "helper_token.create", ...)` (HLPR-07; D-07)
- redeem_helper_token implementiert D-25-atomic-redeem mit differential D-24-Status-Mapping
- Atomic-Redeem + set_session_id liegen in derselben TX (Pitfall 3 vermieden)
- Session-Erzeugung mit user_id=`helper:<token_id>` und claims `{"kind":"helper","assembly_id":"..."}` (D-15/D-16/D-17)
- Klartext-Code wird nicht persistiert und nicht geloggt (D-11)
- ServiceError-Discriminator-Strings (already_used / revoked / assembly_not_open) sind stabil und in Plan 07 zu HTTP-Status mapbar (D-24)
- ≥ 11 Unit-Tests grün (Helper-Functions + Service-Methoden)
</success_criteria>

<output>
Erstelle nach Abschluss `.planning/phases/02-helfer-token-session-authcontext-helper/02-05-SUMMARY.md` mit:
- Liste der Konstanten + Helper-Funktionen
- 4 Service-Methoden mit kurzem Audit-/Pitfall-Status
- ServiceError-Discriminator-Tabelle für Plan 07 (D-24-Mapping)
- Hinweis für Plan 07: HelperTokenServiceImpl ist konstruktor-bereit; benötigte Deps sind 7 (kein direkter PermissionDao, da via SessionService abgedeckt)
- Hinweis für Plan 06: SessionService::extract_auth_context muss claims-JSON `{"kind":"helper",...}` ableiten — Schema ist hier dokumentiert
</output>
