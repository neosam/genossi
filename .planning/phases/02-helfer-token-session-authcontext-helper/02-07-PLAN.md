---
phase: 02-helfer-token-session-authcontext-helper
plan: 07
type: execute
wave: 3
depends_on: [02-04, 02-05, 02-06]
files_modified:
  - genossi_rest/src/helper_token.rs
  - genossi_rest/src/lib.rs
  - genossi_bin/src/lib.rs
autonomous: true
requirements: [HLPR-01, HLPR-02, HLPR-06]
must_haves:
  truths:
    - "POST /api/assembly/{assembly_id}/helper-tokens (admin) — create endpoint, returnt 201 + HelperTokenCreateResponseTO (D-21)"
    - "GET /api/assembly/{assembly_id}/helper-tokens (admin) — list endpoint, returnt 200 + Vec<HelperTokenTO> (D-21)"
    - "POST /api/assembly/{assembly_id}/helper-tokens/{token_id}/revoke (admin) — revoke endpoint, returnt 200 + HelperTokenTO bei Success, 409 bei used/closed (D-21, D-23, D-03)"
    - "POST /api/helper/redeem (PUBLIC) — redeem endpoint, returnt 200 + Set-Cookie + RedeemResponse bei Success, 400/404/410/403 nach D-24"
    - "Set-Cookie auf /api/helper/redeem: Name `app_session`, HttpOnly, SameSite=Strict, Secure, Max-Age=86400 (D-22, D-18)"
    - "ServiceError->RestError-Mapping erweitert: Conflict(\"already_used\") -> 410 Gone, Conflict(\"revoked\"|\"assembly_not_open\") -> 403 Forbidden, ValidationError -> 400, EntityNotFound -> 404"
    - "RestError hat zwei neue Varianten: Gone(String) und Forbidden(String) (D-24)"
    - "Pro-IP-Rate-Limit auf /api/helper/redeem via tower_governor (Phase-1-Pattern aus join_rate_layer kopiert; ~10/min/IP)"
    - "ApiDoc nestet (path = \"/api/assembly/{assembly_id}/helper-tokens\", api = helper_token::ApiDoc) und (path = \"/api/helper\", api = helper_token::PublicApiDoc) (D-21, D-22)"
    - "RestStateImpl in genossi_bin hat assembly_dao und transaction_dao auch für SessionServiceDependencies sichtbar (Plan 06 erweitert SessionServiceDeps um diese)"
    - "RestStateImpl hat helper_token_service: Arc<HelperTokenService>"
    - "impl HelperTokenRestState for RestStateImpl exposed helper_token_service()"
    - "APP_URL ist auch im mock_auth-Build verfügbar via std::env::var (Default in Service-Layer); kein Fail-Fast beim Server-Start (RESEARCH-A4: Plan 05 hat Default-Wert)"
    - "Unauthenticated /api/helper/redeem ist NICHT durch require_authentication-Middleware geschützt — Pattern aus application::generate_public_route"
  artifacts:
    - path: genossi_rest/src/helper_token.rs
      provides: "HelperTokenRestState-Trait, 4 Axum-Handler (3 Vorstand + 1 public), 2 Router-Funktionen, 2 ApiDocs"
      exports: ["HelperTokenRestState", "generate_route", "generate_public_route", "ApiDoc", "PublicApiDoc"]
    - path: genossi_rest/src/lib.rs
      provides: "RestError::Gone/Forbidden Varianten, ServiceError-Mapping erweitert, helper_token-Module + Router-Nest + ApiDoc-Nest, redeem_rate_layer"
    - path: genossi_bin/src/lib.rs
      provides: "HelperTokenService DI-Wiring, RestStateImpl-Field + AssemblyDao/TransactionDao-Sharing mit SessionService"
  key_links:
    - from: "genossi_rest/src/lib.rs router"
      to: "Helper-Endpoints"
      via: ".nest(\"/api/assembly/{assembly_id}/helper-tokens\", helper_token::generate_route::<RestState>())"
      pattern: 'nest\("/api/assembly/\\{assembly_id\\}/helper-tokens"'
    - from: "genossi_rest/src/lib.rs router"
      to: "Public Redeem mit Rate-Limit"
      via: ".nest(\"/api/helper\", helper_token::generate_public_route::<RestState>().layer(redeem_rate_layer))"
      pattern: 'nest\("/api/helper"'
    - from: "genossi_bin RestStateImpl"
      to: "HelperTokenService"
      via: "impl genossi_rest::helper_token::HelperTokenRestState for RestStateImpl"
      pattern: "impl genossi_rest::helper_token::HelperTokenRestState"
---

<objective>
HTTP-Schicht und DI-Wiring für das Helper-Token-Aggregat. Vier Axum-Handler in `genossi_rest/src/helper_token.rs`, zwei Router-Funktionen (Vorstand + Public), zwei ApiDocs, Router-Nest mit Pro-IP-Rate-Limit auf dem Redeem-Endpoint, plus erweiterte ServiceError->RestError-Mappings für 410/403, plus Service-Wiring in `genossi_bin/src/lib.rs`. Wave 3, depends on Plans 04, 05, 06.

Purpose: Endpoints durch das Genossi-Standard-Pattern (error_handler + utoipa::path + extract_auth_context bzw. tower_governor) verfügbar machen. Public-Redeem-Endpoint folgt dem `application::generate_public_route`-Pattern (kein extract_auth_context). DI-Wiring erweitert RestStateImpl für HelperTokenService UND erweitert SessionServiceDependencies um AssemblyDao + TransactionDao (für Plan 06's D-18-Status-Check).

Output: Voll funktionsfähige REST-API für Helper-Token-CRUD + Redeem. Plan 08 testet das Verhalten end-to-end.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-helfer-token-session-authcontext-helper/02-CONTEXT.md
@.planning/phases/02-helfer-token-session-authcontext-helper/02-RESEARCH.md
@.planning/phases/02-helfer-token-session-authcontext-helper/02-PATTERNS.md
@.planning/phases/02-helfer-token-session-authcontext-helper/02-04-SUMMARY.md
@.planning/phases/02-helfer-token-session-authcontext-helper/02-05-SUMMARY.md
@.planning/phases/02-helfer-token-session-authcontext-helper/02-06-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@genossi_rest/src/assembly.rs
@genossi_rest/src/application.rs
@genossi_rest/src/lib.rs
@genossi_rest/src/session.rs
@genossi_bin/src/lib.rs
@genossi_service_impl/src/helper_token.rs
@genossi_service/src/helper_token.rs

<interfaces>
<!-- HelperTokenService trait (Plan 04 + Plan 05): -->
```rust
async fn create_helper_token(&self, assembly_id: Uuid, sub: &HelperTokenSubmission, ctx: Authentication<Self::Context>) -> Result<HelperTokenCreated, ServiceError>;
async fn list_for_assembly(&self, assembly_id: Uuid, ctx: Authentication<Self::Context>) -> Result<Arc<[HelperToken]>, ServiceError>;
async fn revoke_helper_token(&self, assembly_id: Uuid, token_id: Uuid, ctx: Authentication<Self::Context>) -> Result<HelperToken, ServiceError>;
async fn redeem_helper_token(&self, code: &str) -> Result<HelperRedeemSuccess, ServiceError>;
```

<!-- ServiceError-Discriminator-Strings für D-24-Mapping (Plan 05 SUMMARY): -->
- already_used → 410 Gone
- revoked → 403 Forbidden
- assembly_not_open → 403 Forbidden
- ValidationError → 400 Bad Request
- EntityNotFound → 404 Not Found

<!-- RestError-Enum aktuelle Varianten (genossi_rest/src/lib.rs:75-82): -->
```rust
pub enum RestError {
    NotFound,
    BadRequest(String),
    Conflict(String),
    Unauthorized,
    UnsupportedMediaType(String),
    InternalError(String),
}
```

<!-- ServiceError->RestError-Mapping (genossi_rest/src/lib.rs:90-105) — bestehend: -->
```rust
impl From<genossi_service::ServiceError> for RestError {
    fn from(e: genossi_service::ServiceError) -> Self {
        match e {
            genossi_service::ServiceError::EntityNotFound(_) => RestError::NotFound,
            genossi_service::ServiceError::ValidationError(items) => {
                let messages = ...;
                RestError::BadRequest(messages.join(", "))
            }
            genossi_service::ServiceError::PermissionDenied => RestError::Unauthorized,
            genossi_service::ServiceError::Conflict(msg) => RestError::Conflict(msg.to_string()),
            _ => RestError::InternalError(format!("{:?}", e)),
        }
    }
}
```

<!-- Public-Endpoint-Pattern (genossi_rest/src/application.rs:117-204): -->
```rust
pub async fn public_join<S: ApplicationRestState>(...) -> Response {
    error_handler((async {
        // KEIN extract_auth_context-Aufruf
        // ...
    }).await)
}

pub fn generate_public_route<S: ApplicationRestState>() -> Router<S> {
    Router::new().route("/join", post(public_join::<S>))
}
```

<!-- Router-Setup (genossi_rest/src/lib.rs:475-485, 651-657): -->
```rust
let join_rate_config = Arc::new(GovernorConfigBuilder::default().per_second(12).burst_size(5).finish().unwrap());
let join_rate_layer = GovernorLayer { config: join_rate_config };
// ...
let join_router = application::generate_public_route::<RestState>().layer(join_rate_layer);
let app = app
    .nest("/api/public", join_router)
    .with_state(rest_state.clone());
```

<!-- Set-Cookie pattern (genossi_rest/src/session.rs:28-58): -->
```rust
use tower_cookies::Cookie;
let cookies = request.extensions().get::<Cookies>().expect("Cookies extension not set");
let cookie = Cookie::build(Cookie::new("app_session", session_id))
    .path("/")
    .expires(expires)
    .http_only(true)
    .same_site(tower_cookies::cookie::SameSite::Strict)
    .secure(true);
cookies.add(cookie.into());
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: REST handlers + Router-Funktionen + ApiDocs in genossi_rest/src/helper_token.rs</name>
  <files>genossi_rest/src/helper_token.rs</files>
  <read_first>
    - genossi_rest/src/assembly.rs (gesamte Datei — Vorlage für Vorstand-Handler-Pattern, RestState-Trait, ApiDoc, generate_route)
    - genossi_rest/src/application.rs (Zeilen 117-204 — Public-Endpoint-Pattern; generate_public_route)
    - genossi_rest/src/lib.rs (Zeilen 50-73 extract_auth_context; 75-163 error_handler + RestError)
    - genossi_rest/src/session.rs (Zeilen 28-58 Cookie-Build-Pattern)
    - genossi_service/src/helper_token.rs (Plan-04-Service-Trait)
    - genossi_rest_types/src/lib.rs (Plan-04-TOs: HelperTokenTO, HelperTokenCreateResponseTO, CreateHelperTokenRequest, RedeemRequest, RedeemResponse)
    - .planning/phases/02-helfer-token-session-authcontext-helper/02-CONTEXT.md (D-21, D-22, D-23, D-24)
    - .planning/phases/02-helfer-token-session-authcontext-helper/02-PATTERNS.md (Sektionen: genossi_rest/src/helper_token.rs + genossi_rest/src/helper_redeem.rs)
  </read_first>
  <behavior>
    - HelperTokenRestState-Trait stellt helper_token_service() bereit
    - 4 Handler: create_helper_token (POST), list_helper_tokens (GET), revoke_helper_token (POST .../{token_id}/revoke), redeem_helper_token (POST /redeem PUBLIC)
    - create returnt 201, list/revoke returnt 200, redeem returnt 200 mit Set-Cookie
    - generate_route gibt 3 Vorstand-Routen zurück (admin-protected)
    - generate_public_route gibt 1 Public-Route zurück (no auth)
    - validate_create_helper_token_request prüft memo not-empty + max 256 chars
    - Differenzierte ServiceError->HTTP-Mapping wird im redeem-Handler ENTWEDER über bestehende RestError::Conflict gemacht (mapping in lib.rs erweitert) ODER über lokale match-Klausel im Handler
  </behavior>
  <action>
    Erstelle `genossi_rest/src/helper_token.rs` als neue Datei. Strukturlayout:

    **Datei-Anfang: Imports + RestState-Trait + Validation-Helpers**

    ```rust
    use axum::{
        body::Body,
        extract::{Path, State},
        http::{header, HeaderValue},
        response::Response,
        routing::{get, post},
        Extension, Json, Router,
    };
    use genossi_rest_types::{
        CreateHelperTokenRequest, HelperTokenCreateResponseTO, HelperTokenStatusTO, HelperTokenTO,
        RedeemRequest, RedeemResponse,
    };
    use genossi_service::helper_token::{HelperTokenService, HelperTokenSubmission};
    use genossi_service::{ServiceError, ValidationFailureItem};
    use std::sync::Arc;
    use tracing::instrument;
    use utoipa::OpenApi;
    use uuid::Uuid;

    use crate::{error_handler, Context, RestError, RestStateDef};

    pub trait HelperTokenRestState: Clone + Send + Sync + 'static {
        type HelperTokenService: HelperTokenService<Context = crate::ContextType>
            + Send + Sync + 'static;
        fn helper_token_service(&self) -> Arc<Self::HelperTokenService>;
    }

    fn validate_create_helper_token_request(body: &CreateHelperTokenRequest) -> Result<(), Vec<ValidationFailureItem>> {
        let mut errors = Vec::new();
        let memo = body.memo.trim();
        if memo.is_empty() {
            errors.push(ValidationFailureItem {
                field: Arc::from("memo"),
                message: Arc::from("missing"),
            });
        } else if memo.chars().count() > 256 {
            errors.push(ValidationFailureItem {
                field: Arc::from("memo"),
                message: Arc::from("too_long (max 256)"),
            });
        }
        if errors.is_empty() { Ok(()) } else { Err(errors) }
    }
    ```

    **Vorstand-Handler 1: create_helper_token (POST /, returns 201)**

    ```rust
    #[instrument(skip(rest_state))]
    #[utoipa::path(
        post, tag = "Helper Tokens", path = "",
        params(("assembly_id" = Uuid, Path, description = "Assembly ID")),
        request_body = CreateHelperTokenRequest,
        responses(
            (status = 201, description = "Helper token created (one-time qr_svg + code in body)", body = HelperTokenCreateResponseTO),
            (status = 401, description = "Unauthorized"),
            (status = 404, description = "Assembly not found"),
            (status = 409, description = "Conflict (assembly Closed)"),
            (status = 422, description = "Validation Error"),
        ),
    )]
    pub async fn create_helper_token<RestState: RestStateDef + HelperTokenRestState>(
        rest_state: State<RestState>,
        Extension(context): Extension<Context>,
        Path(assembly_id): Path<Uuid>,
        Json(body): Json<CreateHelperTokenRequest>,
    ) -> Response {
        error_handler((async {
            let auth = crate::extract_auth_context(Some(context))?;
            validate_create_helper_token_request(&body).map_err(|errs| {
                RestError::BadRequest(format!("Validation failed: {:?}", errs))
            })?;
            let submission = HelperTokenSubmission {
                memo: Arc::from(body.memo.trim()),
            };
            let created = rest_state
                .helper_token_service()
                .create_helper_token(assembly_id, &submission, auth)
                .await?;

            // Build the create-response: HelperTokenTO + code + qr_svg (one-time, D-21).
            let token_to = HelperTokenTO {
                id: created.token.id,
                assembly_id: created.token.assembly_id,
                memo: created.token.memo.to_string(),
                status: HelperTokenStatusTO::Open,
                used_at: created.token.used_at,
                revoked_at: created.token.revoked_at,
                created: Some(created.token.created),
                version: created.token.version,
            };
            let response = HelperTokenCreateResponseTO {
                token: token_to,
                code: created.code.to_string(),
                qr_svg: created.qr_svg.to_string(),
            };

            Ok(Response::builder()
                .status(201)
                .header("Content-Type", "application/json")
                .body(Body::new(serde_json::to_string(&response)?))
                .unwrap())
        }).await)
    }
    ```

    **Vorstand-Handler 2: list_helper_tokens (GET /, returns 200)**

    ```rust
    #[instrument(skip(rest_state))]
    #[utoipa::path(
        get, tag = "Helper Tokens", path = "",
        params(("assembly_id" = Uuid, Path, description = "Assembly ID")),
        responses(
            (status = 200, description = "List of helper tokens for this assembly", body = [HelperTokenTO]),
            (status = 401, description = "Unauthorized"),
        ),
    )]
    pub async fn list_helper_tokens<RestState: RestStateDef + HelperTokenRestState>(
        rest_state: State<RestState>,
        Extension(context): Extension<Context>,
        Path(assembly_id): Path<Uuid>,
    ) -> Response {
        error_handler((async {
            let auth = crate::extract_auth_context(Some(context))?;
            let tokens = rest_state
                .helper_token_service()
                .list_for_assembly(assembly_id, auth)
                .await?;
            // Map domain HelperToken to TO via inline construction (no Entity available here).
            let to_list: Vec<HelperTokenTO> = tokens.iter().map(|t| {
                let status = if t.revoked_at.is_some() {
                    HelperTokenStatusTO::Revoked
                } else if t.used_at.is_some() {
                    HelperTokenStatusTO::Used
                } else {
                    HelperTokenStatusTO::Open
                };
                HelperTokenTO {
                    id: t.id,
                    assembly_id: t.assembly_id,
                    memo: t.memo.to_string(),
                    status,
                    used_at: t.used_at,
                    revoked_at: t.revoked_at,
                    created: Some(t.created),
                    version: t.version,
                }
            }).collect();
            Ok(Response::builder()
                .status(200)
                .header("Content-Type", "application/json")
                .body(Body::new(serde_json::to_string(&to_list)?))
                .unwrap())
        }).await)
    }
    ```

    **Vorstand-Handler 3: revoke_helper_token (POST /{token_id}/revoke, returns 200)**

    ```rust
    #[instrument(skip(rest_state))]
    #[utoipa::path(
        post, tag = "Helper Tokens", path = "/{token_id}/revoke",
        params(
            ("assembly_id" = Uuid, Path, description = "Assembly ID"),
            ("token_id" = Uuid, Path, description = "Helper Token ID"),
        ),
        responses(
            (status = 200, description = "Token revoked", body = HelperTokenTO),
            (status = 401, description = "Unauthorized"),
            (status = 404, description = "Not found"),
            (status = 409, description = "Conflict (token already used or assembly closed)"),
        ),
    )]
    pub async fn revoke_helper_token<RestState: RestStateDef + HelperTokenRestState>(
        rest_state: State<RestState>,
        Extension(context): Extension<Context>,
        Path((assembly_id, token_id)): Path<(Uuid, Uuid)>,
    ) -> Response {
        error_handler((async {
            let auth = crate::extract_auth_context(Some(context))?;
            let token = rest_state
                .helper_token_service()
                .revoke_helper_token(assembly_id, token_id, auth)
                .await?;
            let status = if token.revoked_at.is_some() {
                HelperTokenStatusTO::Revoked
            } else if token.used_at.is_some() {
                HelperTokenStatusTO::Used
            } else {
                HelperTokenStatusTO::Open
            };
            let to = HelperTokenTO {
                id: token.id,
                assembly_id: token.assembly_id,
                memo: token.memo.to_string(),
                status,
                used_at: token.used_at,
                revoked_at: token.revoked_at,
                created: Some(token.created),
                version: token.version,
            };
            Ok(Response::builder()
                .status(200)
                .header("Content-Type", "application/json")
                .body(Body::new(serde_json::to_string(&to)?))
                .unwrap())
        }).await)
    }
    ```

    **Public Handler 4: redeem_helper_token (POST /redeem, PUBLIC, Set-Cookie)**

    ```rust
    #[instrument(skip(rest_state))]
    #[utoipa::path(
        post, tag = "Helper Redeem", path = "/redeem",
        request_body = RedeemRequest,
        responses(
            (status = 200, description = "Redeem successful, session cookie set", body = RedeemResponse),
            (status = 400, description = "Invalid code format"),
            (status = 403, description = "Token revoked or assembly not Open"),
            (status = 404, description = "Token unknown"),
            (status = 410, description = "Token already redeemed"),
            (status = 429, description = "Rate limit exceeded"),
        ),
    )]
    pub async fn redeem_helper_token<RestState: RestStateDef + HelperTokenRestState>(
        rest_state: State<RestState>,
        Json(body): Json<RedeemRequest>,
    ) -> Response {
        error_handler((async {
            // NO extract_auth_context — this is a PUBLIC endpoint (D-22).
            // Differential ServiceError-Mapping (D-24): we do an explicit match here
            // because the standard From<ServiceError> for RestError doesn't differentiate
            // the helper_token Conflict-payloads (already_used / revoked / assembly_not_open).
            let result = rest_state
                .helper_token_service()
                .redeem_helper_token(&body.code)
                .await;

            let success = match result {
                Ok(s) => s,
                Err(ServiceError::ValidationError(_)) => return Err(RestError::BadRequest("invalid_code_format".to_string())),
                Err(ServiceError::EntityNotFound(_)) => return Err(RestError::NotFound),
                Err(ServiceError::Conflict(payload)) => {
                    let p = payload.as_ref();
                    if p == "already_used" {
                        return Err(RestError::Gone("already_used".to_string()));
                    } else if p == "revoked" || p == "assembly_not_open" {
                        return Err(RestError::Forbidden(p.to_string()));
                    } else {
                        return Err(RestError::Conflict(p.to_string()));
                    }
                }
                Err(other) => return Err(other.into()),
            };

            // Build Set-Cookie header (D-22, D-18 max-age 86400):
            // app_session=<session_id>; Path=/; HttpOnly; SameSite=Strict; Secure; Max-Age=86400
            let cookie_value = format!(
                "app_session={}; Path=/; HttpOnly; SameSite=Strict; Secure; Max-Age=86400",
                success.session_id
            );

            // Format expires_at as ISO8601 string (Unix timestamp -> ISO).
            let expires_at_iso = time::OffsetDateTime::from_unix_timestamp(success.expires_at)
                .map_err(|e| RestError::InternalError(format!("invalid expires_at: {}", e)))?
                .format(&time::format_description::well_known::Iso8601::DEFAULT)
                .map_err(|e| RestError::InternalError(format!("format expires_at: {}", e)))?;

            let response_body = RedeemResponse {
                assembly_id: success.assembly_id,
                expires_at: expires_at_iso,
            };

            Ok(Response::builder()
                .status(200)
                .header("Content-Type", "application/json")
                .header(header::SET_COOKIE, HeaderValue::from_str(&cookie_value)
                    .map_err(|e| RestError::InternalError(format!("invalid cookie value: {}", e)))?)
                .body(Body::new(serde_json::to_string(&response_body)?))
                .unwrap())
        }).await)
    }
    ```

    **Router-Funktionen + ApiDocs am Datei-Ende:**

    ```rust
    pub fn generate_route<RestState: RestStateDef + HelperTokenRestState>() -> Router<RestState> {
        Router::new()
            .route("/", get(list_helper_tokens::<RestState>).post(create_helper_token::<RestState>))
            .route("/{token_id}/revoke", post(revoke_helper_token::<RestState>))
    }

    pub fn generate_public_route<RestState: RestStateDef + HelperTokenRestState>() -> Router<RestState> {
        Router::new().route("/redeem", post(redeem_helper_token::<RestState>))
    }

    #[derive(OpenApi)]
    #[openapi(
        paths(create_helper_token, list_helper_tokens, revoke_helper_token),
        components(schemas(HelperTokenTO, HelperTokenStatusTO, HelperTokenCreateResponseTO, CreateHelperTokenRequest))
    )]
    pub struct ApiDoc;

    #[derive(OpenApi)]
    #[openapi(
        paths(redeem_helper_token),
        components(schemas(RedeemRequest, RedeemResponse))
    )]
    pub struct PublicApiDoc;
    ```

    **Tests** in `#[cfg(test)] mod tests` (Validation-Tests; Handler-Tests folgen in Plan 08 e2e):
    ```rust
    #[cfg(test)]
    mod tests {
        use super::*;

        #[test]
        fn test_validate_create_helper_token_request_valid() {
            let body = CreateHelperTokenRequest { memo: "Anna".to_string() };
            assert!(validate_create_helper_token_request(&body).is_ok());
        }

        #[test]
        fn test_validate_create_helper_token_request_empty_memo() {
            let body = CreateHelperTokenRequest { memo: "".to_string() };
            let err = validate_create_helper_token_request(&body).unwrap_err();
            assert!(err.iter().any(|e| e.field.as_ref() == "memo" && e.message.as_ref() == "missing"));
        }

        #[test]
        fn test_validate_create_helper_token_request_whitespace_memo() {
            let body = CreateHelperTokenRequest { memo: "   ".to_string() };
            let err = validate_create_helper_token_request(&body).unwrap_err();
            assert!(err.iter().any(|e| e.field.as_ref() == "memo"));
        }

        #[test]
        fn test_validate_create_helper_token_request_too_long_memo() {
            let body = CreateHelperTokenRequest { memo: "a".repeat(257) };
            let err = validate_create_helper_token_request(&body).unwrap_err();
            assert!(err.iter().any(|e| e.field.as_ref() == "memo" && e.message.as_ref().contains("too_long")));
        }
    }
    ```
  </action>
  <verify>
    <automated>cargo test -p genossi_rest helper_token 2>&1 | tail -25 && cargo build -p genossi_rest 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p genossi_rest` exit 0 (vorausgesetzt Plan 06 Server-Lib-Update + Plan-07-Task-2 sind merged; Standalone-Build kann fehlschlagen wegen RestError-Varianten, die in Plan 07 Task 2 hinzukommen)
    - `cargo test -p genossi_rest helper_token` exit 0 mit ≥ 4 Validation-Tests
    - `grep -c "pub trait HelperTokenRestState" genossi_rest/src/helper_token.rs` == 1
    - `grep -c "pub async fn create_helper_token" genossi_rest/src/helper_token.rs` == 1
    - `grep -c "pub async fn list_helper_tokens" genossi_rest/src/helper_token.rs` == 1
    - `grep -c "pub async fn revoke_helper_token" genossi_rest/src/helper_token.rs` == 1
    - `grep -c "pub async fn redeem_helper_token" genossi_rest/src/helper_token.rs` == 1
    - `grep -c '\.status(201)' genossi_rest/src/helper_token.rs` ≥ 1
    - `grep -c '\.status(200)' genossi_rest/src/helper_token.rs` ≥ 3
    - `grep -c "tag = \"Helper Tokens\"" genossi_rest/src/helper_token.rs` ≥ 3
    - `grep -c "tag = \"Helper Redeem\"" genossi_rest/src/helper_token.rs` ≥ 1
    - `grep -c "extract_auth_context" genossi_rest/src/helper_token.rs` == 3 (3 Vorstand-Handler; redeem hat KEIN extract_auth_context)
    - `grep -c "SET_COOKIE\|set_cookie" genossi_rest/src/helper_token.rs` ≥ 1
    - `grep -c "HttpOnly" genossi_rest/src/helper_token.rs` ≥ 1
    - `grep -c "SameSite=Strict" genossi_rest/src/helper_token.rs` ≥ 1
    - `grep -c "Max-Age=86400" genossi_rest/src/helper_token.rs` ≥ 1
    - `grep -c "RestError::Gone" genossi_rest/src/helper_token.rs` ≥ 1
    - `grep -c "RestError::Forbidden" genossi_rest/src/helper_token.rs` ≥ 1
    - `grep -c "already_used" genossi_rest/src/helper_token.rs` ≥ 1
    - `grep -c "revoked\|assembly_not_open" genossi_rest/src/helper_token.rs` ≥ 1
    - `grep -c "pub fn generate_route" genossi_rest/src/helper_token.rs` == 1
    - `grep -c "pub fn generate_public_route" genossi_rest/src/helper_token.rs` == 1
    - `grep -c "pub struct ApiDoc" genossi_rest/src/helper_token.rs` == 1
    - `grep -c "pub struct PublicApiDoc" genossi_rest/src/helper_token.rs` == 1
  </acceptance_criteria>
  <done>
    Vier Axum-Handler kompilieren mit korrekten Status-Codes (201/200/200/200), Set-Cookie auf Redeem, differentiated Error-Mapping (400/404/410/403). Validation-Tests grün. Plan-07-Task-2 (lib.rs Erweiterung) muss vorher die RestError-Varianten Gone+Forbidden ergänzen — Build wird nach Task 2 grün.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RestError-Varianten + Module-Wiring + Router-Nest + redeem_rate_layer in genossi_rest/src/lib.rs</name>
  <files>genossi_rest/src/lib.rs</files>
  <read_first>
    - genossi_rest/src/lib.rs (gesamte Datei, besonders 75-163 RestError + error_handler, 232-258 ApiDoc-Nest, 412-419 create_app type-bound, 475-485 join_rate_config, 653-657 join_router-nest, 678-687 start_server type-bound)
    - .planning/phases/02-helfer-token-session-authcontext-helper/02-CONTEXT.md (D-21, D-22, D-24)
    - .planning/phases/02-helfer-token-session-authcontext-helper/02-PATTERNS.md (Sektion `genossi_rest/src/lib.rs`)
    - .planning/phases/02-helfer-token-session-authcontext-helper/02-RESEARCH.md (Pitfall 7 — tower_governor Per-Route)
  </read_first>
  <behavior>
    - `pub mod helper_token;` ist als Modul-Deklaration registriert
    - RestError-Enum hat zwei neue Varianten: Gone(String) für 410, Forbidden(String) für 403
    - error_handler matched die zwei neuen Varianten und liefert HTTP 410 / 403 mit Body
    - ApiDoc-nest-Liste enthält (path = "/api/assembly/{assembly_id}/helper-tokens", api = helper_token::ApiDoc)
    - ApiDoc oder eine `create_app`-merge nimmt die PublicApiDoc auf (analog zur application::PublicApiDoc-Merge)
    - create_app type-bound enthält `+ helper_token::HelperTokenRestState`
    - start_server type-bound enthält `+ helper_token::HelperTokenRestState`
    - Router-Build enthält `.nest("/api/assembly/{assembly_id}/helper-tokens", helper_token::generate_route::<RestState>())` (für die Vorstand-Endpoints, hinter Auth-Middleware)
    - Public-Block enthält Pro-IP-Rate-Limit auf /api/helper/redeem mit `redeem_rate_layer` (10/min/IP); .nest("/api/helper", helper_token::generate_public_route::<RestState>().layer(redeem_rate_layer))
  </behavior>
  <action>
    Modifiziere `genossi_rest/src/lib.rs` mit sieben Edits:

    **1. Modul-Deklaration** (oben, nach `pub mod helper_token;` alphabetisch nach `audit_timestamp` und vor `auth`):
    ```rust
    pub mod application;
    pub mod assembly;
    pub mod audit_log;
    pub mod audit_timestamp;
    pub mod auth;
    pub mod auth_middleware;
    pub mod backup;
    // ...
    pub mod helper_token;     // NEU
    // ...
    ```

    **2. RestError-Enum-Erweiterung** (Zeilen 75-82):

    Bestehender Code:
    ```rust
    pub enum RestError {
        NotFound,
        BadRequest(String),
        Conflict(String),
        Unauthorized,
        UnsupportedMediaType(String),
        InternalError(String),
    }
    ```

    Neuer Code:
    ```rust
    pub enum RestError {
        NotFound,
        BadRequest(String),
        Conflict(String),
        Unauthorized,
        UnsupportedMediaType(String),
        InternalError(String),
        /// 403 Forbidden — used by helper-redeem when token is revoked or assembly not Open (D-24).
        Forbidden(String),
        /// 410 Gone — used by helper-redeem when token has already been used (D-24).
        Gone(String),
    }
    ```

    **3. error_handler-Erweiterung** (Zeilen 131-163):

    Bestehender Match-Block (vor dem Default _) ergänzen mit zwei neuen Arms:

    ```rust
    pub fn error_handler(result: Result<Response, RestError>) -> Response {
        match result {
            Ok(response) => response,
            Err(RestError::NotFound) => Response::builder()
                .status(404).body(Body::from("Not found")).unwrap(),
            Err(RestError::BadRequest(msg)) => Response::builder()
                .status(400).body(Body::from(msg)).unwrap(),
            Err(RestError::Conflict(msg)) => Response::builder()
                .status(409).body(Body::from(msg)).unwrap(),
            Err(RestError::Unauthorized) => Response::builder()
                .status(401).body(Body::from("Unauthorized")).unwrap(),
            Err(RestError::UnsupportedMediaType(msg)) => Response::builder()
                .status(415).header("Content-Type", "application/json").body(Body::from(msg)).unwrap(),
            // NEW: 403 + 410 (D-24)
            Err(RestError::Forbidden(msg)) => Response::builder()
                .status(403).body(Body::from(msg)).unwrap(),
            Err(RestError::Gone(msg)) => Response::builder()
                .status(410).body(Body::from(msg)).unwrap(),
            Err(RestError::InternalError(msg)) => {
                tracing::error!("Internal error: {}", msg);
                Response::builder()
                    .status(500).body(Body::from("Internal server error")).unwrap()
            }
        }
    }
    ```

    **4. ApiDoc-nest** (im `#[derive(OpenApi)] #[openapi(nest(...))]`-Block, ~Zeile 232-256):

    Füge zwei neue Zeilen ein, alphabetisch zwischen application und assembly bzw. nach assembly:
    ```rust
    nest(
        // ...
        (path = "/api/applications", api = application::ApiDoc),
        (path = "/api/assembly", api = assembly::ApiDoc),
        (path = "/api/assembly/{assembly_id}/helper-tokens", api = helper_token::ApiDoc),  // NEU
        // ...
    )
    ```

    **5. PublicApiDoc-merge** in create_app (vor `let swagger_router = ...`, analog zu application::PublicApiDoc::openapi() Zeile 437-439):
    ```rust
    let public_join_doc = application::PublicApiDoc::openapi();
    api_doc.merge(public_join_doc);

    // NEU: Helper-Redeem PublicApiDoc
    let helper_redeem_doc = helper_token::PublicApiDoc::openapi();
    api_doc.merge(helper_redeem_doc);
    ```

    **6. create_app + start_server type-bound** (Zeilen 412-419, 678-687):

    In beiden Funktions-Headern füge `+ helper_token::HelperTokenRestState` zur Bound-Liste hinzu, alphabetisch nach `assembly::AssemblyRestState`:

    ```rust
    pub async fn create_app<
        RestState: RestStateDef
            + public_stats::PublicStatsState
            + application::ApplicationRestState
            + assembly::AssemblyRestState
            + helper_token::HelperTokenRestState   // NEU
            + audit_log::AuditRestState
            + audit_timestamp::TimestampRestState,
    >(
        rest_state: RestState,
    ) -> Router { ... }
    ```

    Identisch in start_server.

    **7. Router-Build + redeem_rate_layer** (im Hauptbody von create_app):

    a) **Vorstand-Endpoints**: Nach dem `.nest("/api/assembly", assembly::generate_route::<RestState>())` Eintrag (Zeile 566), füge eine neue Zeile für die Helper-Token-Endpoints ein:
    ```rust
    .nest("/api/assembly", assembly::generate_route::<RestState>())
    .nest("/api/assembly/{assembly_id}/helper-tokens", helper_token::generate_route::<RestState>())   // NEU
    .nest("/api/audit", audit_log::generate_route::<RestState>())
    ```

    Hinweis: Axum-Routing-Konvention — der nested-Router ÜBER `/api/assembly/{assembly_id}/helper-tokens` empfängt `Path((assembly_id, token_id))`-Extractor wenn die innere Route `/{token_id}/revoke` ist. Pattern aus `genossi_rest/src/assembly.rs:349-361` ist Vorlage.

    b) **redeem_rate_config** — füge nach `let join_rate_layer = GovernorLayer { config: join_rate_config };` (Zeile ~485) eine analoge redeem_rate_config + redeem_rate_layer ein:
    ```rust
    // 10 req/min on /api/helper/redeem (RESEARCH Discretion: brute-force protection)
    let redeem_rate_config = Arc::new(
        GovernorConfigBuilder::default()
            .per_second(6)        // 60s window / 6s per request = 10/min steady-state
            .burst_size(10)       // allow short bursts for re-tries
            .finish()
            .unwrap(),
    );
    let redeem_rate_layer = GovernorLayer {
        config: redeem_rate_config,
    };
    ```

    c) **Public-Router-Block**: Im "Public routes (no auth required)"-Block (Zeile ~652-657), füge die Helper-Redeem-Route ein, ALL `.layer(redeem_rate_layer)` UND analog zum join_router pattern:
    ```rust
    // Public routes (no auth required)
    let join_router = application::generate_public_route::<RestState>().layer(join_rate_layer);
    let helper_redeem_router = helper_token::generate_public_route::<RestState>().layer(redeem_rate_layer);   // NEU
    let app = app
        .nest("/api/public", public_stats::generate_route::<RestState>())
        .nest("/api/public", join_router)
        .nest("/api/helper", helper_redeem_router)   // NEU
        .with_state(rest_state.clone());
    ```

    Hinweis zum Pattern: `tower_governor::GovernorLayer` benötigt eventuell `clone()` wenn der Layer mehrfach verwendet wird; siehe Zeile 483-485 join_rate_layer-Pattern.

    Stelle sicher, dass keine bestehende Test in `genossi_rest::tests` (Zeilen 699-712) regrediert. Falls neue RestError-Varianten in einem Mock-Test exhaustiv gematched werden, ergänze dort den Helper-Arm.
  </action>
  <verify>
    <automated>cargo build -p genossi_rest 2>&1 | tail -15 && cargo test -p genossi_rest 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p genossi_rest` exit 0
    - `cargo test -p genossi_rest` exit 0 — alle bisherigen Tests bleiben grün
    - `grep -c "pub mod helper_token;" genossi_rest/src/lib.rs` == 1
    - `grep -c "Forbidden(String)" genossi_rest/src/lib.rs` ≥ 1
    - `grep -c "Gone(String)" genossi_rest/src/lib.rs` ≥ 1
    - `grep -c "RestError::Forbidden" genossi_rest/src/lib.rs` ≥ 1
    - `grep -c "RestError::Gone" genossi_rest/src/lib.rs` ≥ 1
    - `grep -c '\.status(403)' genossi_rest/src/lib.rs` ≥ 1
    - `grep -c '\.status(410)' genossi_rest/src/lib.rs` ≥ 1
    - `grep -c "/api/assembly/{assembly_id}/helper-tokens" genossi_rest/src/lib.rs` ≥ 1
    - `grep -c "helper_token::HelperTokenRestState" genossi_rest/src/lib.rs` ≥ 2 (create_app + start_server)
    - `grep -c "helper_token::PublicApiDoc" genossi_rest/src/lib.rs` ≥ 1
    - `grep -c "redeem_rate_config\|redeem_rate_layer" genossi_rest/src/lib.rs` ≥ 2
    - `grep -c '"/api/helper"' genossi_rest/src/lib.rs` ≥ 1
    - `grep -c "helper_token::generate_route\|helper_token::generate_public_route" genossi_rest/src/lib.rs` ≥ 2
  </acceptance_criteria>
  <done>
    Module + RestError-Varianten + Router + ApiDoc-Merge + Rate-Layer sind verkabelt; cargo build des genossi_rest-Crates erfolgreich; bestehende Tests regredieren nicht.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DI-Wiring in genossi_bin/src/lib.rs (HelperTokenService + SessionServiceDeps-Erweiterung)</name>
  <files>genossi_bin/src/lib.rs</files>
  <read_first>
    - genossi_bin/src/lib.rs (gesamte Datei — besonders 88-102 SessionServiceDependencies, 122-167 type-aliases + AssemblyServiceDependencies, 312-352 RestStateImpl-Struct, 355-650 ::new() service-wiring, oidc-spezifische Sektionen rund um session_service)
    - genossi_service_impl/src/helper_token.rs (Plan 05 — HelperTokenServiceDeps signature)
    - genossi_service_impl/src/session.rs (Plan 06 — SessionServiceDeps signature mit erweitertem AssemblyDao + TransactionDao)
    - genossi_rest/src/helper_token.rs (Plan 07 Task 1 — HelperTokenRestState trait)
    - .planning/phases/02-helfer-token-session-authcontext-helper/02-PATTERNS.md (Sektion `genossi_bin/src/lib.rs`)
  </read_first>
  <behavior>
    - genossi_bin baut komplett ohne Fehler in beiden Feature-Builds (mock_auth + oidc)
    - SessionServiceDependencies wird (im oidc-Feature-Block, Zeilen 88-102) um AssemblyDao + TransactionDao erweitert
    - HelperTokenServiceDependencies-Struct + impl HelperTokenServiceDeps + type-alias HelperTokenService existieren
    - RestStateImpl hat ein neues Feld helper_token_service: Arc<HelperTokenService>
    - RestStateImpl::new(pool) instanziiert HelperTokenServiceImpl mit allen 7 Deps
    - impl genossi_rest::helper_token::HelperTokenRestState for RestStateImpl exposed helper_token_service()
    - cargo build -p genossi_bin grün
  </behavior>
  <action>
    Modifiziere `genossi_bin/src/lib.rs` mit sieben Edits, exakt nach RESEARCH §"Integration Points" und Phase-1-Pattern:

    **1. SessionServiceDependencies erweitern (Zeilen 88-102, hinter cfg(feature = "oidc") aktuell):**

    WICHTIG: Plan 06 erfordert AssemblyDao + TransactionDao auch im mock_auth-Build, weil SessionServiceImpl::extract_auth_context im Helfer-Pfad einen Status-Check macht. Wenn der mock_auth-Build den `MockSessionServiceImpl` (kein Deps-Struct) benutzt, ist diese Erweiterung nur im oidc-Build erforderlich.

    Aktueller Code (Zeilen 88-102):
    ```rust
    #[cfg(feature = "oidc")]
    impl genossi_service_impl::session::SessionServiceDeps for SessionServiceDependencies {
        type Context = Context;
        type Transaction = Transaction;
        type PermissionDao = PermissionDao;
    }
    ```

    Neuer Code:
    ```rust
    #[cfg(feature = "oidc")]
    impl genossi_service_impl::session::SessionServiceDeps for SessionServiceDependencies {
        type Context = Context;
        type Transaction = Transaction;
        type PermissionDao = PermissionDao;
        type AssemblyDao = AssemblyDao;        // NEU für D-18 Status-Check
        type TransactionDao = TransactionDao;  // NEU
    }
    ```

    **2. HelperTokenServiceDependencies hinzufügen** (analog AssemblyServiceDependencies, Zeilen 149-167; einsortieren nach AssemblyService-Type-Alias auf Zeile 167):

    ```rust
    type HelperTokenDao = genossi_dao_impl_sqlite::helper_token::HelperTokenDaoImpl;

    pub struct HelperTokenServiceDependencies;
    unsafe impl Send for HelperTokenServiceDependencies {}
    unsafe impl Sync for HelperTokenServiceDependencies {}

    impl genossi_service_impl::helper_token::HelperTokenServiceDeps for HelperTokenServiceDependencies {
        type Context = Context;
        type Transaction = Transaction;
        type HelperTokenDao = HelperTokenDao;
        type AssemblyDao = AssemblyDao;
        type AuditLogDao = AuditLogDao;
        type PermissionService = PermissionService;
        type SessionService = SessionService;
        type UuidService = UuidService;
        type TransactionDao = TransactionDao;
    }

    type HelperTokenService =
        genossi_service_impl::helper_token::HelperTokenServiceImpl<HelperTokenServiceDependencies>;
    ```

    Hinweis: Wenn Plan 05 zu HelperTokenServiceDeps zusätzlich PermissionDao deklariert, dann auch hier `type PermissionDao = PermissionDao;` ergänzen. Folge der konkreten Plan-05-Output-Schnittstelle.

    **3. RestStateImpl-Field** (in der `pub struct RestStateImpl`-Definition Zeile 313-352):

    Füge ein neues Feld ein, alphabetisch nach `assembly_service`:
    ```rust
    helper_token_service: Arc<HelperTokenService>,   // NEU
    ```

    **4. ::new()-Wiring** (im RestStateImpl::new Body, nach dem assembly_service-Block ~Zeile 502):

    ```rust
    let assembly_dao = Arc::new(AssemblyDao::new(pool.clone()));
    let assembly_member_snapshot_dao = Arc::new(AssemblyMemberSnapshotDao::new(pool.clone()));
    let assembly_service = Arc::new(genossi_service_impl::assembly::AssemblyServiceImpl {
        // ... existing ...
    });

    // NEU: HelperToken DAO + Service
    let helper_token_dao = Arc::new(HelperTokenDao::new(pool.clone()));
    let helper_token_service = Arc::new(genossi_service_impl::helper_token::HelperTokenServiceImpl {
        helper_token_dao,
        assembly_dao: assembly_dao.clone(),
        audit_log_dao: audit_log_dao.clone(),
        permission_service: permission_service.clone(),
        session_service: session_service.clone(),
        uuid_service: uuid_service.clone(),
        transaction_dao: transaction_dao.clone(),
    });
    ```

    **WICHTIG (Plan-06-Konsequenz):** SessionServiceImpl im oidc-Build benötigt jetzt assembly_dao + transaction_dao. Suche im `::new()` nach der oidc-spezifischen session_service-Konstruktion (Zeile ~448-451):

    Alter Code:
    ```rust
    #[cfg(feature = "oidc")]
    let session_service = Arc::new(genossi_service_impl::session::SessionServiceImpl {
        permission_dao: permission_dao.clone(),
    });
    ```

    Neuer Code:
    ```rust
    #[cfg(feature = "oidc")]
    let session_service = Arc::new(genossi_service_impl::session::SessionServiceImpl {
        permission_dao: permission_dao.clone(),
        assembly_dao: assembly_dao.clone(),
        transaction_dao: transaction_dao.clone(),
    });
    ```

    Hinweis: Die `assembly_dao`-Variable existiert in `::new()` erst NACH dem assembly_service-Block (Zeile ~492). Plan 07 muss die session_service-Konstruktion nach dem assembly_dao-Block verschieben (oder den assembly_dao-Block nach vorne) — je nach Cargo-Build-Reihenfolge. Konkretes Vorgehen:
    1. Lies die aktuelle Reihenfolge in `genossi_bin/src/lib.rs::new()`
    2. Wenn `assembly_dao` Zeile 492 erst nach `session_service` (Zeile 448) konstruiert wird: ziehe `assembly_dao` und `transaction_dao` (Zeile 357) NACH oben (vor session_service-Konstruktion). Falls transaction_dao bereits vor session_service konstruiert ist (Zeile 357), reicht es, `assembly_dao` voranzustellen.
    3. Stelle sicher, dass die Verwendung von `assembly_dao.clone()` in beiden Aufrufstellen (session_service-Konstruktion + assembly_service-Konstruktion + helper_token_service-Konstruktion) konsistent ist.

    **5. Self { ... }-Block** (im Konstruktor-Return, nach `assembly_service,`):

    ```rust
    helper_token_service,
    ```

    **6. impl HelperTokenRestState for RestStateImpl** (am Datei-Ende, parallel zu `impl ApplicationRestState for RestStateImpl` Zeilen ~976-997):

    ```rust
    impl genossi_rest::helper_token::HelperTokenRestState for RestStateImpl {
        type HelperTokenService = HelperTokenService;

        fn helper_token_service(&self) -> Arc<Self::HelperTokenService> {
            self.helper_token_service.clone()
        }
    }
    ```

    **7. APP_URL-Verfügbarkeit** (RESEARCH-A4): Plan 05 hat den Default `"http://localhost:3000/"` direkt im Service-Code via `std::env::var("APP_URL").unwrap_or_else(|_| ...)`. KEINE zusätzliche Änderung im genossi_bin nötig — der Mock-Auth-Build kann APP_URL aus der Env (oder dem Default) lesen, ohne fail-fast im OIDC-Pfad. Falls in OIDC-Build ein striktes fail-fast gewünscht wäre, könnte man am Server-Start einen Check ergänzen — aber CONTEXT D-12 sagt "Plan-Detail" und Plan 05 wählt den pragmatischen Default-Pfad. Dokumentiere diese Entscheidung im Plan-07-SUMMARY.
  </action>
  <verify>
    <automated>cargo build -p genossi_bin --features mock_auth 2>&1 | tail -15 && cargo build -p genossi_bin --no-default-features --features oidc 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p genossi_bin --features mock_auth` exit 0
    - `cargo build -p genossi_bin --no-default-features --features oidc` exit 0
    - `cargo build --workspace` exit 0
    - `grep -c "type HelperTokenDao = genossi_dao_impl_sqlite::helper_token::HelperTokenDaoImpl" genossi_bin/src/lib.rs` == 1
    - `grep -c "pub struct HelperTokenServiceDependencies" genossi_bin/src/lib.rs` == 1
    - `grep -c "impl genossi_service_impl::helper_token::HelperTokenServiceDeps for HelperTokenServiceDependencies" genossi_bin/src/lib.rs` == 1
    - `grep -c "helper_token_service: Arc<HelperTokenService>" genossi_bin/src/lib.rs` ≥ 1
    - `grep -c "let helper_token_dao = Arc::new" genossi_bin/src/lib.rs` == 1
    - `grep -c "HelperTokenServiceImpl {" genossi_bin/src/lib.rs` ≥ 1
    - `grep -c "impl genossi_rest::helper_token::HelperTokenRestState for RestStateImpl" genossi_bin/src/lib.rs` == 1
    - `grep -c "fn helper_token_service" genossi_bin/src/lib.rs` ≥ 1
    - `grep -c "type AssemblyDao = AssemblyDao" genossi_bin/src/lib.rs` ≥ 2 (eines in Phase-1-AssemblyServiceDeps, eines in HelperTokenServiceDeps; ggf. drittes in SessionServiceDeps wenn oidc)
    - In oidc-Build: `grep -A 8 "impl genossi_service_impl::session::SessionServiceDeps" genossi_bin/src/lib.rs | grep -c "type AssemblyDao\|type TransactionDao"` ≥ 2
    - `cargo test --workspace --features mock_auth 2>&1 | tail -5 | grep -c "test result.*passed"` ≥ 1 — bestehende Phase-1-Tests bleiben grün
  </acceptance_criteria>
  <done>
    Komplette DI-Kette steht in beiden Feature-Builds. cargo build --workspace grün. Bestehende Phase-1-Tests laufen weiter durch (keine Regressions). RestStateImpl hat helper_token_service-Field und exposed es via Trait-Impl. Plan 08 (E2E-Tests) kann gegen den voll verdrahteten Server testen.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Vorstand-Endpoint | JSON via serde + admin-Permission-Check (PermissionService). Vorstand-Routes hinter Auth-Middleware. |
| client→Public-Redeem-Endpoint | KEINE Auth-Middleware; Pro-IP-Rate-Limit (10/min) via tower_governor; ServiceError-Discriminator-Strings für 400/404/410/403-Mapping. |
| Set-Cookie | app_session-Cookie wird nur bei erfolgreichem Redeem gesetzt; HttpOnly + SameSite=Strict + Secure + Max-Age=86400. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07-01 | Spoofing | Forged session-cookie via XSS | mitigate | HttpOnly verhindert JavaScript-Lesezugriff; Secure verhindert HTTP-Transport; SameSite=Strict verhindert Cross-Site-Request-Forgery. |
| T-02-07-02 | Denial of Service | Brute-Force auf /api/helper/redeem | mitigate | Pro-IP-Rate-Limit 10/min via redeem_rate_layer. Pitfall 7: separater GovernorLayer auf der spezifischen Route. |
| T-02-07-03 | Information Disclosure | RestError::Gone-Body leakt Debug-Info | mitigate | Body-Strings sind stable error-codes ("already_used", "revoked", "assembly_not_open"); kein internes State-Leak. |
| T-02-07-04 | Tampering | Helfer-Cookie aus dem Browser für Vorstand-Endpoints missbrauchen | mitigate | Vorstand-Endpoints rufen extract_auth_context → check_permission("admin", auth) → Helper-Sessions liefern PermissionDenied (D-20 Stub aus Plan 02; Plan 06 prüft Helper-Sessions kommen NICHT durch die `Context`-Pipeline mock_auth/oidc, weil `Context` ist `MockContext`/`AuthenticatedContext`). |
| T-02-07-05 | Denial of Service | Massive QR-SVG-Generierung im Vorstand-Pfad | accept | qrcode-Crate ist O(message-length); Vorstand-Pfad ist admin-protected, kein DoS-Risiko. |
| T-02-07-06 | Elevation of Privilege | Public-Redeem ohne Auth-Pfad bypassed Vorstand-Permissions | mitigate | redeem_helper_token-Service (Plan 05) gibt nur HelperRedeemSuccess zurück (eine GV-spezifische Session) — keine admin-Privilegien. |
</threat_model>

<verification>
- `cargo build --workspace --features mock_auth` grün
- `cargo build --workspace --no-default-features --features oidc` grün
- `cargo test -p genossi_rest helper_token` grün (Validation-Tests)
- `cargo test --workspace` keine Regressions in Phase-1-Tests
- `cargo clippy --workspace --all-targets` ohne neue Warnings
- `cargo fmt --check` grün
- Manueller Smoke-Check (optional): DATABASE_URL=sqlite:/tmp/genossi-smoke.db cargo run --bin genossi und curl http://localhost:3000/swagger-ui/ zeigt Helper-Endpoints
</verification>

<success_criteria>
- 4 Endpoints registriert: POST/GET `/api/assembly/{aid}/helper-tokens`, POST `/api/assembly/{aid}/helper-tokens/{tid}/revoke`, POST `/api/helper/redeem` (D-21, D-22)
- Vorstand-Endpoints prüfen admin via Service-Layer; Public-Endpoint hat KEIN extract_auth_context (D-22)
- Set-Cookie auf Redeem mit korrekten Attributen (D-22, D-18)
- ServiceError->RestError-Mapping erweitert: Gone(string)/Forbidden(string) und differential im redeem-Handler-Body (D-24)
- redeem_rate_layer (10/min/IP) auf /api/helper/redeem (Pitfall 7)
- ApiDoc enthält Helper-Token-Schemas und nestet beide ApiDocs (Vorstand + Public)
- DI-Wiring in genossi_bin instanziiert HelperTokenServiceImpl mit 7 Deps
- SessionServiceDependencies (oidc-Build) erweitert um AssemblyDao + TransactionDao
- `cargo build --workspace` grün in beiden Feature-Builds
- Bestehende Phase-1-E2E-Tests laufen weiter durch (Wave-4 e2e-Tests in Plan 08 testen Phase-2-spezifisch)
</success_criteria>

<output>
Erstelle nach Abschluss `.planning/phases/02-helfer-token-session-authcontext-helper/02-07-SUMMARY.md` mit:
- Endpoints-Liste mit Status-Code-Mapping (D-24-Tabelle)
- DI-Wiring-Diff (HelperTokenService-Konstruktion + SessionServiceDeps-Erweiterung)
- redeem_rate_layer-Konfiguration
- APP_URL-Default-Verhalten (Plan 05 Service-Layer-Default; kein fail-fast in genossi_bin)
- Hinweis für Plan 08 (E2E): Server hat alle 4 Endpoints; MockSessionServiceImpl erkennt Helper-Cookie-Format
</output>
