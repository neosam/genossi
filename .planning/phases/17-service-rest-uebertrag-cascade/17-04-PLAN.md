---
phase: 17-service-rest-uebertrag-cascade
plan: 04
type: execute
wave: 4
depends_on: [17-03]
files_modified:
  - genossi_bin/tests/membership_adjust_e2e.rs
autonomous: true
requirements: [TRSF-01, TRSF-02, TRSF-03, TRSF-04, TRSF-05, TRSF-07, AUDT-02, PERM-03]
requirements_addressed: [TRSF-01, TRSF-02, TRSF-03, TRSF-04, TRSF-05, TRSF-07, AUDT-02, PERM-03]
tags: [rust, e2e, race-test, audit-verify, integration]
must_haves:
  truths:
    - "All 8 E2E tests from CONTEXT.md SC #5 are present in genossi_bin/tests/membership_adjust_e2e.rs."
    - "Test 1 (Teil-Uebertrag Happy-Path): assert actions.len() == 2, from.current_shares decreases, from.exit_date is null."
    - "Test 2 (Voll-Uebertrag Cascade): assert actions.len() == 3, last action is Austritt, from.exit_date == transfer_date, from.current_shares == 0."
    - "Test 3 (Self-Transfer 400): assert StatusCode::BAD_REQUEST and body contains 'cannot transfer to self'."
    - "Test 4 (Recipient cancelled 409): assert StatusCode::CONFLICT and body contains 'recipient already cancelled'."
    - "Test 5 (Recipient not found 404): assert StatusCode::NOT_FOUND with non-existing UUID."
    - "Test 6 (Audit Doppel-Assertion): single transaction_id + count of MemberAction entries == 2 (Teil) or 3 (Voll); /api/audit/verify returns valid==true."
    - "Test 7 (Race Same-Direction): tokio::join! two identical POSTs; sorted statuses == [200, 409|500]; NEVER [200, 200]."
    - "Test 8 (Race Cross-Direction): tokio::join! A->B + B->A; sum of current_shares preserved; audit-chain valid; not [409|500, 409|500]."
    - "Mini-sleep tokio::sleep(Duration::from_millis(1)) is present BEFORE BOTH race tests' tokio::join! (Pool-Warm-up, Pitfall #11)."
    - "Helper fn transfer_shares_body(to, shares, transfer_date) is added (analog partial_repayment_body)."
  artifacts:
    - path: "genossi_bin/tests/membership_adjust_e2e.rs"
      provides: "8 new E2E tests covering all Phase-17 success criteria"
      contains: "test_transfer_shares_partial_happy_path"
  key_links:
    - from: "membership_adjust_e2e.rs::test_transfer_shares_*"
      to: "Phase-17 service+REST stack (Plans 17-01..03)"
      via: "POST /api/members/{from_id}/transfer-shares + GET /api/audit/verify"
      pattern: "/transfer-shares"
---

<objective>
Schreibe 8 E2E-Tests in `genossi_bin/tests/membership_adjust_e2e.rs`, die das Phase-17-Verhalten end-to-end ueber den realen HTTP-Server gegen SQLite (in-memory) absichern. Decke alle 8 REQ-IDs (TRSF-01..05, TRSF-07, AUDT-02, PERM-03) plus beide Race-Patterns aus D-17-06 ab.

Purpose: Die Mock-Unit-Tests aus Plan 17-02 verifizieren die Service-Logik isoliert. Die E2E-Tests verifizieren das Zusammenspiel von REST + Service + DAO + Audit-Hashchain unter echten Bedingungen. Race-Tests sind die einzige Verteidigung gegen Concurrency-Bugs, die in Unit-Tests nicht reproduzierbar sind.

Output:
- 8 neue `#[tokio::test]` Funktionen in `genossi_bin/tests/membership_adjust_e2e.rs` (additiv, am Datei-Ende)
- Helper `fn transfer_shares_body(...)` analog `partial_repayment_body`
- Helper `assert_transfer_audit_trail(client, server, expected_action_count)` fuer Doppel-Assertion
- `cargo test --test membership_adjust_e2e` alle Tests gruen
- Race-Test-Konsistenz-Checks bleiben deterministisch ueber 10 Runs
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-service-rest-uebertrag-cascade/17-CONTEXT.md
@.planning/phases/17-service-rest-uebertrag-cascade/17-PATTERNS.md
@.planning/phases/17-service-rest-uebertrag-cascade/17-03-PLAN.md
@genossi_bin/tests/membership_adjust_e2e.rs
@genossi_bin/tests/e2e_tests.rs

<interfaces>
From genossi_bin/tests/membership_adjust_e2e.rs (existing helpers — REUSE, do not duplicate):
- `async fn setup() -> TestServer` (lines 39-53) — starts in-memory SQLite server.
- `fn sample_member(member_number, first_name) -> MemberTO` (lines 59-87).
- `async fn create_active_member(client, server, member_number, first_name) -> MemberTO` (lines 89-109).
- `async fn put_member_current_shares(client, server, member, target_shares) -> MemberTO` (lines 487-511) — used to set from.current_shares to 5 or 2 etc.
- `fn today_march_15()`, `fn today_august_15()`, `fn current_year_dec_31()` (lines 128-150).
- `fn partial_repayment_body(...)` (lines 477-482) — pattern template for transfer_shares_body.
- `test_partial_repayment_audit_chain_verify` (lines 782-832) — pattern for audit verify.
- `test_partial_repayment_cancelled_member_block_409` (lines 746-780) — pattern for cancelled-member 409.

From genossi_bin/tests/e2e_tests.rs:
- Race-Test Vorbild: `test_mark_paid_out_race_one_succeeds_one_conflicts` lines 12474-12596 (Phase 9 D-12). Race-Pattern: 1ms sleep + tokio::join! + sorted status assertions.

From genossi_rest_types (via re-export in tests):
- `VerifyResponseTO` (line 1751 per PATTERNS.md) — used for /api/audit/verify response.

HTTP Endpoints used:
- POST /api/members/{from_id}/transfer-shares (Plan 17-03)
- GET  /api/members/{id}
- POST /api/members/{member_id}/cancel (Phase 15 — for cancelled-recipient setup)
- GET  /api/audit?process=member-adjust.transfer (or similar query — verify in PATTERNS / Phase 8 tests)
- GET  /api/audit/verify

Helper to ADD:
```rust
fn transfer_shares_body(to: &Uuid, shares: i32, transfer_date: &str) -> Value {
    serde_json::json!({
        "to_member_id": to.to_string(),
        "shares": shares,
        "transfer_date": transfer_date,
    })
}

/// D-17-05 Doppel-Assertion fuer Audit-Trail des Uebertrags.
async fn assert_transfer_audit_trail(
    client: &reqwest::Client,
    server: &TestServer,
    expected_action_count: usize,
) {
    // (a) Atomarity: alle Audit-Eintraege fuer process=member-adjust.transfer
    //     teilen sich genau EINE transaction_id.
    // (b) MemberAction-Count = expected_action_count.
    // Implementiere via GET /api/audit-Endpoint und JSON-Parsing der entries-Array.
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test-Helpers + 5 Standard-E2E-Tests (Happy-Paths + Validation/Conflict)</name>
  <files>genossi_bin/tests/membership_adjust_e2e.rs</files>
  <read_first>
    - .planning/phases/17-service-rest-uebertrag-cascade/17-CONTEXT.md (Domain §"In scope" 8 E2E-Tests-Liste; Specifics §"E2E-Test-Naming-Konvention")
    - .planning/phases/17-service-rest-uebertrag-cascade/17-PATTERNS.md (Section 6 Body-helper + Happy-Path + alle 5 Standard-Test-Patterns)
    - genossi_bin/tests/membership_adjust_e2e.rs (existing setup + create_active_member + put_member_current_shares + partial_repayment_body + test_partial_repayment_happy_path_h1 als Template Z. 543-573; test_partial_repayment_cancelled_member_block_409 Z. 746-780)
  </read_first>
  <behavior>
    - Helper `transfer_shares_body(to: &Uuid, shares: i32, transfer_date: &str) -> Value` ist am gleichen Ort wie partial_repayment_body definiert.
    - Test 1 `test_transfer_shares_partial_happy_path`: setup A (current_shares=5) + B (current_shares=1); POST shares=2; assert 200, actions.len()==2, from.current_shares==3, from.exit_date is null, to.current_shares==3.
    - Test 2 `test_transfer_shares_full_with_exit_date_cascade`: setup A (current_shares=3) + B (current_shares=1); POST shares=3; assert 200, actions.len()==3, last action.action_type==Austritt, from.current_shares==0, from.exit_date == transfer_date.
    - Test 3 `test_transfer_shares_self_transfer_400`: POST with body.to_member_id==a_id; assert 400, body contains "cannot transfer to self".
    - Test 4 `test_transfer_shares_recipient_cancelled_409`: setup A + B; cancel B via POST /api/members/{b_id}/cancel; POST transfer A->B; assert 409, body contains "recipient already cancelled".
    - Test 5 `test_transfer_shares_recipient_not_found_404`: POST with to_member_id=Uuid::new_v4() (never created); assert 404.
    - cargo test --test membership_adjust_e2e gruen.
  </behavior>
  <action>
1. Oeffne `genossi_bin/tests/membership_adjust_e2e.rs`. Lese die existing Helpers + Tests komplett (Datei ist ~889 Zeilen laut PATTERNS.md). Notiere besonders:
   - Wo `partial_repayment_body` definiert ist (Z. 477-482).
   - Wo `put_member_current_shares` definiert ist (Z. 487-511).
   - Wie `test_partial_repayment_cancelled_member_block_409` (Z. 746-780) das `cancel`-Endpoint nutzt — kopiere diesen Cancel-Setup-Pattern fuer Test 4.

2. Fuege am ENDE der Datei (nach allen existierenden Tests) eine neue Section ein:

```rust
// ============================================================
// Phase 17 - Uebertrag (Transfer Shares)
// SC #5: 8 E2E-Tests
// ============================================================

fn transfer_shares_body(to: &Uuid, shares: i32, transfer_date: &str) -> Value {
    serde_json::json!({
        "to_member_id": to.to_string(),
        "shares": shares,
        "transfer_date": transfer_date,
    })
}
```

3. Test 1 - Teil-Uebertrag Happy-Path:

```rust
#[tokio::test]
async fn test_transfer_shares_partial_happy_path() {
    let server = setup().await;
    let client = reqwest::Client::new();

    let a = create_active_member(&client, &server, 1200, "FromA").await;
    let a = put_member_current_shares(&client, &server, &a, 5).await;
    let b = create_active_member(&client, &server, 1201, "ToB").await;

    let a_id = a.id.expect("a.id");
    let b_id = b.id.expect("b.id");

    let transfer_date = today_march_15();

    let resp = client
        .post(server.url(&format!("/api/members/{}/transfer-shares", a_id)))
        .json(&transfer_shares_body(&b_id, 2, &transfer_date.to_string()))
        .send()
        .await
        .expect("POST transfer-shares");
    assert_eq!(resp.status(), StatusCode::OK, "Happy-Path muss 200 returnen");

    let body: Value = resp.json().await.expect("decode TransferSharesResponseTO");

    let actions = body["actions"].as_array().expect("actions array");
    assert_eq!(actions.len(), 2, "Teil-Uebertrag = 2 Actions (Abgabe + Empfang)");

    // from
    assert_eq!(
        body["from"]["current_shares"].as_i64().expect("from.current_shares"),
        3,
        "from.current_shares = 5 - 2"
    );
    assert!(
        body["from"]["exit_date"].is_null(),
        "Teil-Uebertrag darf KEIN exit_date setzen"
    );

    // to (sample_member shares_at_joining = 1, +2 = 3)
    assert_eq!(
        body["to"]["current_shares"].as_i64().expect("to.current_shares"),
        3,
        "to.current_shares = 1 + 2"
    );
    assert!(
        body["to"]["exit_date"].is_null(),
        "to bleibt aktiv"
    );
}
```

4. Test 2 - Voll-Uebertrag mit exit_date-Cascade:

```rust
#[tokio::test]
async fn test_transfer_shares_full_with_exit_date_cascade() {
    let server = setup().await;
    let client = reqwest::Client::new();

    // A = 3 Anteile (Voll-Uebertrag-Source), B = 1 Anteil.
    let a = create_active_member(&client, &server, 1210, "FullFromA").await;
    let a = put_member_current_shares(&client, &server, &a, 3).await;
    let b = create_active_member(&client, &server, 1211, "FullToB").await;

    let a_id = a.id.expect("a.id");
    let b_id = b.id.expect("b.id");
    let transfer_date = today_march_15();

    let resp = client
        .post(server.url(&format!("/api/members/{}/transfer-shares", a_id)))
        .json(&transfer_shares_body(&b_id, 3, &transfer_date.to_string()))
        .send()
        .await
        .expect("POST transfer-shares full");
    assert_eq!(resp.status(), StatusCode::OK, "Voll-Uebertrag muss 200 returnen");

    let body: Value = resp.json().await.expect("decode");
    let actions = body["actions"].as_array().expect("actions");

    // D-17-01: Voll-Uebertrag = 3 Actions.
    assert_eq!(actions.len(), 3, "Voll-Uebertrag = 3 Actions");

    // D-17-03: 3. Action ist Austritt mit transfer_member_id=Some(b_id), effective_date=Some(transfer_date).
    let last_action = &actions[2];
    assert_eq!(
        last_action["action_type"].as_str().expect("action_type"),
        "Austritt",
        "3. Action = Austritt"
    );
    assert_eq!(
        last_action["transfer_member_id"].as_str(),
        Some(b_id.to_string().as_str()),
        "Austritt.transfer_member_id = b_id (D-17-03 — divergiert von Phase-15 CANC)"
    );
    assert_eq!(
        last_action["effective_date"].as_str().expect("effective_date"),
        transfer_date.to_string().as_str(),
        "Austritt.effective_date = transfer_date (TRSF-05 sofort wirksam)"
    );

    // D-17-02 + TRSF-04: from.current_shares == 0; from.exit_date == transfer_date (via recalc_dates).
    assert_eq!(
        body["from"]["current_shares"].as_i64().expect("from.current_shares"),
        0,
        "Voll-Uebertrag: from.current_shares = 0"
    );
    assert_eq!(
        body["from"]["exit_date"].as_str().expect("from.exit_date"),
        transfer_date.to_string().as_str(),
        "Voll-Uebertrag: from.exit_date = transfer_date"
    );

    // to bleibt aktiv und bekommt +3.
    assert_eq!(
        body["to"]["current_shares"].as_i64().expect("to.current_shares"),
        4,
        "to.current_shares = 1 + 3"
    );
    assert!(body["to"]["exit_date"].is_null(), "to bleibt aktiv");
}
```

5. Test 3 - Self-Transfer 400:

```rust
#[tokio::test]
async fn test_transfer_shares_self_transfer_400() {
    let server = setup().await;
    let client = reqwest::Client::new();

    let a = create_active_member(&client, &server, 1220, "Self").await;
    let a = put_member_current_shares(&client, &server, &a, 5).await;
    let a_id = a.id.expect("a.id");
    let transfer_date = today_march_15();

    let resp = client
        .post(server.url(&format!("/api/members/{}/transfer-shares", a_id)))
        .json(&transfer_shares_body(&a_id, 1, &transfer_date.to_string()))
        .send()
        .await
        .expect("POST self-transfer");
    assert_eq!(
        resp.status(),
        StatusCode::BAD_REQUEST,
        "Self-Transfer muss 400 returnen (TRSF-07 / D-17-08)"
    );
    let body = resp.text().await.unwrap_or_default();
    assert!(
        body.contains("cannot transfer to self"),
        "Body muss Hinweis enthalten; got: {}",
        body
    );
}
```

6. Test 4 - Recipient-cancelled 409 (Setup-Pattern aus test_partial_repayment_cancelled_member_block_409):

```rust
#[tokio::test]
async fn test_transfer_shares_recipient_cancelled_409() {
    let server = setup().await;
    let client = reqwest::Client::new();

    let a = create_active_member(&client, &server, 1230, "ActiveFrom").await;
    let a = put_member_current_shares(&client, &server, &a, 5).await;
    let b = create_active_member(&client, &server, 1231, "ToBeCancelledB").await;

    let a_id = a.id.expect("a.id");
    let b_id = b.id.expect("b.id");

    // B kuendigen (Phase-15 cancel-Endpoint). Pattern aus
    // test_partial_repayment_cancelled_member_block_409.
    let cancel_resp = client
        .post(server.url(&format!("/api/members/{}/cancel", b_id)))
        .json(&serde_json::json!({
            "willensbekundung_date": today_march_15().to_string(),
        }))
        .send()
        .await
        .expect("POST cancel B");
    assert_eq!(cancel_resp.status(), StatusCode::OK, "Cancel B muss 200");

    // Jetzt Transfer A -> B versuchen.
    let resp = client
        .post(server.url(&format!("/api/members/{}/transfer-shares", a_id)))
        .json(&transfer_shares_body(&b_id, 2, &today_march_15().to_string()))
        .send()
        .await
        .expect("POST transfer A->B");
    assert_eq!(
        resp.status(),
        StatusCode::CONFLICT,
        "PERM-03: gekuendigter Empfaenger muss 409 (D-17-07)"
    );
    let body = resp.text().await.unwrap_or_default();
    assert!(
        body.contains("recipient already cancelled"),
        "Body muss Hinweis enthalten; got: {}",
        body
    );
}
```

7. Test 5 - Recipient-not-found 404:

```rust
#[tokio::test]
async fn test_transfer_shares_recipient_not_found_404() {
    let server = setup().await;
    let client = reqwest::Client::new();

    let a = create_active_member(&client, &server, 1240, "FromA").await;
    let a = put_member_current_shares(&client, &server, &a, 5).await;
    let a_id = a.id.expect("a.id");
    let fake_b_id = Uuid::new_v4();

    let resp = client
        .post(server.url(&format!("/api/members/{}/transfer-shares", a_id)))
        .json(&transfer_shares_body(&fake_b_id, 2, &today_march_15().to_string()))
        .send()
        .await
        .expect("POST transfer fake recipient");
    assert_eq!(
        resp.status(),
        StatusCode::NOT_FOUND,
        "Nicht-existierender Empfaenger muss 404"
    );
}
```

8. WICHTIG bei Cancel-Endpoint-Body: Falls Phase-15 cancel-Body andere Felder hat (z.B. `cancellation_date` oder `transfer_member_id`), exakte JSON-Schluessel aus `test_partial_repayment_cancelled_member_block_409` kopieren — NICHT erfinden.

9. `cargo test --test membership_adjust_e2e test_transfer_shares_partial_happy_path test_transfer_shares_full_with_exit_date_cascade test_transfer_shares_self_transfer_400 test_transfer_shares_recipient_cancelled_409 test_transfer_shares_recipient_not_found_404` ausfuehren.

10. Bei Fehlern: Imports am Top der Datei pruefen (Uuid, Value, StatusCode, reqwest::Client, serde_json existieren bereits — partial_repayment-Tests nutzen sie).
  </action>
  <verify>
    <automated>cargo test --test membership_adjust_e2e test_transfer_shares_partial_happy_path test_transfer_shares_full_with_exit_date_cascade test_transfer_shares_self_transfer_400 test_transfer_shares_recipient_cancelled_409 test_transfer_shares_recipient_not_found_404 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test membership_adjust_e2e test_transfer_shares_partial_happy_path` exits 0
    - `cargo test --test membership_adjust_e2e test_transfer_shares_full_with_exit_date_cascade` exits 0
    - `cargo test --test membership_adjust_e2e test_transfer_shares_self_transfer_400` exits 0
    - `cargo test --test membership_adjust_e2e test_transfer_shares_recipient_cancelled_409` exits 0
    - `cargo test --test membership_adjust_e2e test_transfer_shares_recipient_not_found_404` exits 0
    - `grep -c 'fn transfer_shares_body' genossi_bin/tests/membership_adjust_e2e.rs` == 1
    - `grep -c 'test_transfer_shares_partial_happy_path\|test_transfer_shares_full_with_exit_date_cascade\|test_transfer_shares_self_transfer_400\|test_transfer_shares_recipient_cancelled_409\|test_transfer_shares_recipient_not_found_404' genossi_bin/tests/membership_adjust_e2e.rs` == 5
    - `grep -c '"Austritt"' genossi_bin/tests/membership_adjust_e2e.rs` >= 1 (Voll-Uebertrag-Assertion)
    - `grep -c '"recipient already cancelled"' genossi_bin/tests/membership_adjust_e2e.rs` >= 1
    - `grep -c '"cannot transfer to self"' genossi_bin/tests/membership_adjust_e2e.rs` >= 1
  </acceptance_criteria>
  <done>5 von 8 Tests gruen; Task 2 (Audit-Verify + 2 Race-Tests) macht den Rest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Audit-Doppel-Assertion-Test + 2 Race-Tests (Same- + Cross-Direction)</name>
  <files>genossi_bin/tests/membership_adjust_e2e.rs</files>
  <read_first>
    - .planning/phases/17-service-rest-uebertrag-cascade/17-CONTEXT.md (D-17-05 Doppel-Assertion + D-17-06 Race-Patterns + Specifics §"Test-Doppel-Assertion-Helper" + Cross-Direction-Konsistenz-Check)
    - .planning/phases/17-service-rest-uebertrag-cascade/17-PATTERNS.md (Section 6 Race-Test-Excerpt + Audit-Pair-Verify-Pattern; "Shared Patterns" §"Race-Test Atomicity Pattern")
    - genossi_bin/tests/e2e_tests.rs (Z. 12474-12596: test_mark_paid_out_race_one_succeeds_one_conflicts — wortgenaues Vorbild fuer Same-Direction)
    - genossi_bin/tests/membership_adjust_e2e.rs (test_partial_repayment_audit_chain_verify Z. 782-832 als Audit-Verify-Vorbild)
  </read_first>
  <behavior>
    - Test 6 `test_transfer_shares_audit_pair_verify_doppel_assertion`: nach erfolgreichem Transfer (Teil oder Voll), GET /api/audit?process=member-adjust.transfer (oder analog) ODER GET /api/audit/{entity_type}/{id} liefert die Audit-Eintraege; (a) alle teilen sich genau EINE transaction_id; (b) Anzahl MemberAction-Eintraege == expected_count (2 fuer Teil oder 3 fuer Voll); GET /api/audit/verify → valid==true.
    - Test 7 `test_transfer_shares_race_same_direction_sqlite_busy`: A.current_shares=4; B aktiv; tokio::time::sleep(1ms); tokio::join!(POST shares=2, POST shares=2); sortierte Statuses == [200, 409|500] (kein [200, 200]); finale A.current_shares == 2 (genau 1 erfolgreiche Cascade); audit-verify == valid.
    - Test 8 `test_transfer_shares_race_cross_direction_consistency_check`: A.current_shares=5, B.current_shares=5; tokio::join!(POST A->B shares=2, POST B->A shares=2); akzeptiert [(200, 200), (200, 409|500)]; NIE [409|500, 409|500]; post-check: A.current_shares + B.current_shares == 10 (Start-Summe erhalten); audit-verify == valid.
    - cargo test --test membership_adjust_e2e (alle 8 Tests) gruen ueber 3 Runs.
  </behavior>
  <action>
1. Lese `test_partial_repayment_audit_chain_verify` (Z. 782-832) — das ist das exakte Vorbild fuer GET /api/audit + GET /api/audit/verify + JSON-Parsing. Notiere die genaue Query-Param-Syntax (`?process=...` oder `?entity_type=...`).

2. Schreibe Helper `assert_transfer_audit_trail` (mit Re-Use des Audit-Query-Patterns aus Schritt 1):

```rust
/// D-17-05 / AUDT-02 Doppel-Assertion fuer den Uebertrag-Audit-Trail.
///
/// - (a) Atomarity: alle Audit-Eintraege fuer process="member-adjust.transfer"
///   teilen sich genau EINE transaction_id.
/// - (b) MemberAction-Entity-Count = expected_action_count (2 fuer Teil, 3 fuer Voll).
async fn assert_transfer_audit_trail(
    client: &reqwest::Client,
    server: &TestServer,
    expected_action_count: usize,
) {
    // Hole alle Audit-Eintraege fuer den Phase-17-Process.
    // Hinweis: Verifiziere die exakte Query-Param-Syntax aus
    // test_partial_repayment_audit_chain_verify. Fallback: GET /api/audit
    // ohne Filter, dann clientseitig filtern.
    let resp = client
        .get(server.url("/api/audit"))
        .send()
        .await
        .expect("GET /api/audit");
    assert_eq!(resp.status(), StatusCode::OK);
    let body: Value = resp.json().await.expect("decode audit list");
    let entries = body["entries"].as_array().expect("entries array");

    // Filter auf Phase-17-Process.
    let transfer_entries: Vec<&Value> = entries
        .iter()
        .filter(|e| e["process"].as_str() == Some("member-adjust.transfer"))
        .collect();

    assert!(
        !transfer_entries.is_empty(),
        "es muessen Transfer-Audit-Entries existieren"
    );

    // (a) Alle teilen sich EINE transaction_id.
    let first_tx_id = transfer_entries[0]["transaction_id"]
        .as_str()
        .expect("transaction_id");
    for e in &transfer_entries {
        assert_eq!(
            e["transaction_id"].as_str().expect("transaction_id"),
            first_tx_id,
            "D-17-05 (a): alle Transfer-Entries muessen gleiche transaction_id haben"
        );
    }

    // (b) MemberAction-Entity-Count == expected_action_count.
    let member_action_entries: Vec<&Value> = transfer_entries
        .iter()
        .filter(|e| e["entity_type"].as_str() == Some("MemberAction"))
        .copied()
        .collect();
    // Jede MemberAction kann mehrere Audit-Rows haben (1 pro changed-field).
    // Gruppe nach entity_id, dann zaehle distinkte entity_ids.
    let mut distinct_ids = std::collections::HashSet::new();
    for e in &member_action_entries {
        if let Some(id) = e["entity_id"].as_str() {
            distinct_ids.insert(id.to_string());
        }
    }
    assert_eq!(
        distinct_ids.len(),
        expected_action_count,
        "D-17-05 (b): MemberAction-Count == {} (Teil=2, Voll=3)",
        expected_action_count
    );

    // Defense-in-Depth: Audit-Hashchain bleibt valid.
    let verify_resp = client
        .get(server.url("/api/audit/verify"))
        .send()
        .await
        .expect("GET /api/audit/verify");
    let verify_body: Value = verify_resp.json().await.expect("decode verify");
    assert_eq!(
        verify_body["valid"].as_bool(),
        Some(true),
        "Audit-Hashchain muss valid bleiben"
    );
}
```

WICHTIG: Falls das JSON-Schema andere Feldnamen hat (z.B. `tx_id` statt `transaction_id`, oder `entities: [...]` statt `entries: [...]`), pruefe die EXAKTE Field-Names aus dem existing test_partial_repayment_audit_chain_verify-Test und passe an. NICHT erfinden — der Test ist die Source-of-Truth.

3. Test 6 - Doppel-Assertion (Teil-Variante):

```rust
#[tokio::test]
async fn test_transfer_shares_audit_pair_verify_doppel_assertion() {
    let server = setup().await;
    let client = reqwest::Client::new();

    let a = create_active_member(&client, &server, 1250, "AuditFrom").await;
    let a = put_member_current_shares(&client, &server, &a, 5).await;
    let b = create_active_member(&client, &server, 1251, "AuditTo").await;
    let a_id = a.id.expect("a.id");
    let b_id = b.id.expect("b.id");

    // Teil-Uebertrag: 2 MemberAction-Eintraege.
    let resp = client
        .post(server.url(&format!("/api/members/{}/transfer-shares", a_id)))
        .json(&transfer_shares_body(&b_id, 2, &today_march_15().to_string()))
        .send()
        .await
        .expect("POST transfer for audit-verify");
    assert_eq!(resp.status(), StatusCode::OK);

    assert_transfer_audit_trail(&client, &server, 2).await;
}
```

4. Test 7 - Race Same-Direction (Vorbild: e2e_tests.rs Z. 12474-12596):

```rust
#[tokio::test]
async fn test_transfer_shares_race_same_direction_sqlite_busy() {
    let server = setup().await;
    let client = reqwest::Client::new();

    // A=4, B=1 — bei shares=2 koennen NICHT beide gleichzeitig durchgehen.
    let a = create_active_member(&client, &server, 1260, "RaceFromA").await;
    let a = put_member_current_shares(&client, &server, &a, 4).await;
    let b = create_active_member(&client, &server, 1261, "RaceToB").await;
    let a_id = a.id.expect("a.id");
    let b_id = b.id.expect("b.id");

    let url = server.url(&format!("/api/members/{}/transfer-shares", a_id));
    let body = transfer_shares_body(&b_id, 2, &today_march_15().to_string());

    // Pool-Warm-up (Pitfall #11).
    tokio::time::sleep(std::time::Duration::from_millis(1)).await;

    // D-17-06: Beide POSTs parallel via tokio::join!.
    let (resp_a, resp_b) = tokio::join!(
        client.post(&url).json(&body).send(),
        client.post(&url).json(&body).send(),
    );
    let r_a = resp_a.expect("race a");
    let r_b = resp_b.expect("race b");
    let status_a = r_a.status();
    let status_b = r_b.status();

    let mut statuses = [status_a, status_b];
    statuses.sort_by_key(|s| s.as_u16());

    assert_eq!(
        statuses[0],
        StatusCode::OK,
        "D-17-06: genau ein Race-Aufruf muss 200 sein; got {:?}",
        statuses
    );
    assert!(
        statuses[1] == StatusCode::CONFLICT || statuses[1] == StatusCode::INTERNAL_SERVER_ERROR,
        "D-17-06: Race-Verlierer muss 409 ODER 500 sein; got {:?}",
        statuses
    );
    assert!(
        !(status_a == StatusCode::OK && status_b == StatusCode::OK),
        "D-17-06: NIE [200, 200] (waere Double-Cascade)"
    );

    // Post-Konsistenz: A.current_shares == 4 - 2 == 2 (genau 1 erfolgreiche Cascade).
    let a_after_resp = client
        .get(server.url(&format!("/api/members/{}", a_id)))
        .send()
        .await
        .expect("GET A");
    let a_after: Value = a_after_resp.json().await.expect("decode A");
    assert_eq!(
        a_after["current_shares"].as_i64().expect("a.current_shares"),
        2,
        "Race-Sieger reduziert A.current_shares genau einmal"
    );

    // Audit-Chain bleibt valid.
    let verify: Value = client
        .get(server.url("/api/audit/verify"))
        .send()
        .await
        .expect("GET verify")
        .json()
        .await
        .expect("decode verify");
    assert_eq!(verify["valid"].as_bool(), Some(true), "Audit-Chain valid");
}
```

5. Test 8 - Race Cross-Direction:

```rust
#[tokio::test]
async fn test_transfer_shares_race_cross_direction_consistency_check() {
    let server = setup().await;
    let client = reqwest::Client::new();

    // A=5, B=5; cross-transfer 2 in beide Richtungen.
    let a = create_active_member(&client, &server, 1270, "CrossA").await;
    let a = put_member_current_shares(&client, &server, &a, 5).await;
    let b = create_active_member(&client, &server, 1271, "CrossB").await;
    let b = put_member_current_shares(&client, &server, &b, 5).await;
    let a_id = a.id.expect("a.id");
    let b_id = b.id.expect("b.id");

    let url_ab = server.url(&format!("/api/members/{}/transfer-shares", a_id));
    let url_ba = server.url(&format!("/api/members/{}/transfer-shares", b_id));
    let body_ab = transfer_shares_body(&b_id, 2, &today_march_15().to_string());
    let body_ba = transfer_shares_body(&a_id, 2, &today_march_15().to_string());

    tokio::time::sleep(std::time::Duration::from_millis(1)).await;

    let (resp_ab, resp_ba) = tokio::join!(
        client.post(&url_ab).json(&body_ab).send(),
        client.post(&url_ba).json(&body_ba).send(),
    );
    let r_ab = resp_ab.expect("race ab");
    let r_ba = resp_ba.expect("race ba");
    let status_ab = r_ab.status();
    let status_ba = r_ba.status();

    // D-17-06: NIE beide 409|500 (waere Total-Deadlock).
    let both_failed = (status_ab == StatusCode::CONFLICT || status_ab == StatusCode::INTERNAL_SERVER_ERROR)
        && (status_ba == StatusCode::CONFLICT || status_ba == StatusCode::INTERNAL_SERVER_ERROR);
    assert!(
        !both_failed,
        "D-17-06: NIE Total-Deadlock; got [{}, {}]",
        status_ab, status_ba
    );

    // Konsistenz: Anteile-Summe bleibt erhalten (a_start + b_start = 10).
    let a_after: Value = client
        .get(server.url(&format!("/api/members/{}", a_id)))
        .send()
        .await
        .expect("GET A")
        .json()
        .await
        .expect("decode A");
    let b_after: Value = client
        .get(server.url(&format!("/api/members/{}", b_id)))
        .send()
        .await
        .expect("GET B")
        .json()
        .await
        .expect("decode B");
    let total_after = a_after["current_shares"].as_i64().expect("a.current_shares")
        + b_after["current_shares"].as_i64().expect("b.current_shares");
    assert_eq!(
        total_after, 10,
        "Cross-Race: Anteile-Summe muss erhalten bleiben (5+5=10); got A={}, B={}",
        a_after["current_shares"], b_after["current_shares"]
    );

    // Audit-Chain bleibt valid.
    let verify: Value = client
        .get(server.url("/api/audit/verify"))
        .send()
        .await
        .expect("GET verify")
        .json()
        .await
        .expect("decode verify");
    assert_eq!(verify["valid"].as_bool(), Some(true), "Audit-Chain valid");
}
```

6. `cargo test --test membership_adjust_e2e test_transfer_shares` ausfuehren (alle 8 Tests). Falls assert_transfer_audit_trail wegen JSON-Schema-Mismatch fehlschlaegt, exakte Field-Names aus existing test_partial_repayment_audit_chain_verify uebernehmen.

7. Falls Race-Tests intermittierend rot werden (z.B. [409, 409]), pruefe ob das Setup eine zu enge Race-Window hat — typischerweise reicht der 1ms-Sleep aber bei in-memory SQLite und tokio::join! gut. Bei mehrfachen Fehlern: D-17-06-Lesson dass [409|500, 409|500] verboten ist, aber [200, 409|500] genauso wie [200, 200] erlaubt ist (Cross-Direction).

8. `cargo test --test membership_adjust_e2e 2>&1 | tail -25` zeigt alle 8 Tests; verifiziere "8 passed".
  </action>
  <verify>
    <automated>cargo test --test membership_adjust_e2e test_transfer_shares 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test membership_adjust_e2e test_transfer_shares` exits 0 mit "test result: ok. 8 passed; 0 failed" (oder alle 8 Tests einzeln gruen)
    - `grep -c 'test_transfer_shares_audit_pair_verify_doppel_assertion' genossi_bin/tests/membership_adjust_e2e.rs` == 1
    - `grep -c 'test_transfer_shares_race_same_direction_sqlite_busy' genossi_bin/tests/membership_adjust_e2e.rs` == 1
    - `grep -c 'test_transfer_shares_race_cross_direction_consistency_check' genossi_bin/tests/membership_adjust_e2e.rs` == 1
    - `grep -c 'fn assert_transfer_audit_trail' genossi_bin/tests/membership_adjust_e2e.rs` == 1
    - `grep -c 'tokio::join!' genossi_bin/tests/membership_adjust_e2e.rs` >= 2 (Same + Cross)
    - `grep -c 'Duration::from_millis(1)' genossi_bin/tests/membership_adjust_e2e.rs` >= 2 (Pool-Warm-up beide Race-Tests)
    - `grep -c 'audit/verify' genossi_bin/tests/membership_adjust_e2e.rs` >= 3 (Doppel-Assertion + 2 Race-Tests)
    - `grep -c 'member-adjust.transfer' genossi_bin/tests/membership_adjust_e2e.rs` >= 1
    - `grep -c 'NIE \[200, 200\]\|NIE Total-Deadlock\|sum of current_shares preserved\|Anteile-Summe' genossi_bin/tests/membership_adjust_e2e.rs` >= 2
    - Determinismus-Check: 3 aufeinanderfolgende Runs `for i in 1 2 3; do cargo test --test membership_adjust_e2e test_transfer_shares_race 2>&1 | tail -3; done` zeigen jeweils 2 PASS
  </acceptance_criteria>
  <done>Alle 8 E2E-Tests gruen; Phase-17-Verhalten ist end-to-end abgesichert; v1.1-Audit-Hashchain bleibt valid; Race-Defense ist verifiziert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-Client -> Test-HTTP-Server (in-memory SQLite) | E2E-Tests simulieren echte Client-Requests inkl. JSON, Path-Params, Race-Patterns |
| Parallel-tokio-tasks -> single SQLite pool | Race-Tests testen ob SingleTx-Pattern Concurrency-sicher ist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04-01 | Tampering | Race-Test akzeptiert silently false-positives | mitigate | Sortierte Status-Assertions + explizite NIE-Klauseln (!(status_a == OK && status_b == OK)); Post-Check current_shares-Konsistenz; Audit-Chain verify. |
| T-17-04-02 | Repudiation | Audit-Trail-Manipulation unentdeckt | mitigate | Doppel-Assertion (transaction_id-Unicity + Action-Count) + /api/audit/verify nach jedem Race-Test. |
| T-17-04-03 | Denial of Service | Test-Server in-memory laeuft fuer parallel tokio-tasks aus dem Pool | accept | sqlite::memory: mit cache=shared im setup() — verifizieren via PATTERNS.md §6 Race-Pattern-Lessons. |
| T-17-04-04 | Tampering | Cancel-Endpoint-Body in Test 4 hat andere Felder als angenommen | mitigate | Action-Anweisung sagt: kopiere exakt aus test_partial_repayment_cancelled_member_block_409 (existing). NICHT erfinden. |
| T-17-04-05 | Information Disclosure | Audit-Endpoint-JSON-Schema in assert_transfer_audit_trail kann von Pattern abweichen | mitigate | Action-Anweisung sagt: pruefe exakt die Field-Names aus test_partial_repayment_audit_chain_verify und passe an. Bei Mismatch: Test selbst sagt richtigen Pfad. |
| T-17-04-06 | Elevation of Privilege | Test-Server umgeht ADMIN_PRIVILEGE-Check | accept | setup() etabliert ohnehin admin-Context (existing helper). Falls Test 4 fehlschlaegt mit 401 statt 409, ist das ein klares Signal das setup-Pattern aus partial_repayment-Tests kopiert werden muss. |
| T-17-04-07 | Tampering | Test 8 Cross-Direction-Race produziert [200, 200] bei ausgeglichener Konsistenz | accept | Per D-17-06 ist [200, 200] bei Cross-Direction ERLAUBT, weil A->B und B->A keine konkurrierenden Locks auf dem GLEICHEN Member-Row haben. Die NIE-Klausel ist nur [409|500, 409|500]. |
</threat_model>

<verification>
- `cargo test --test membership_adjust_e2e` exits 0 (alle Phase-15/16/17-Tests gruen)
- `cargo test --workspace` exits 0 (Workspace-Regression)
- Race-Test-Determinismus ueber 3 Runs (Acceptance)
- v1.1-Audit-Hashchain bleibt valid: jeder Test endet mit `/api/audit/verify` -> valid==true
</verification>

<success_criteria>
- 8 E2E-Tests aus CONTEXT.md SC #5 sind implementiert und alle gruen
- Helper transfer_shares_body + assert_transfer_audit_trail existieren
- Race-Tests sind deterministisch (3 Runs ohne Flake)
- Plan 17 vollstaendig abgeschlossen — Phase 18 (Frontend) kann auf den HTTP-Endpoint zugreifen
</success_criteria>

<output>
After completion, create `.planning/phases/17-service-rest-uebertrag-cascade/17-04-SUMMARY.md` mit:
- Anzahl Tests + Zeitdauer der Test-Suite (cargo test --test membership_adjust_e2e Output)
- Falls assert_transfer_audit_trail JSON-Field-Names anpassen musste, dokumentiere die finalen Field-Names (fuer Future-Phasen-Reference)
- Falls Race-Tests intermittierend rot wurden, dokumentiere mit Workaround (z.B. PRAGMA busy_timeout via Tech-Debt-Item)
- Bestaetigung dass cargo test --workspace gruen und v1.1-Audit-Hashchain ueberall valid
- Phase-17-Closure-Notiz: alle 8 REQ-IDs (TRSF-01..05, TRSF-07, AUDT-02, PERM-03) durch Service- + REST- + E2E-Tests abgedeckt
</output>
