---
phase: 17-service-rest-uebertrag-cascade
plan: 03
type: execute
wave: 3
depends_on: [17-02]
files_modified:
  - genossi_rest_types/src/lib.rs
  - genossi_rest/src/membership_adjust.rs
  - genossi_rest/src/member.rs
autonomous: true
requirements: [TRSF-01, TRSF-07]
requirements_addressed: [TRSF-01, TRSF-07]
tags: [rust, axum, rest, dto, utoipa, openapi]
must_haves:
  truths:
    - "TransferSharesRequestTO exists in genossi_rest_types/src/lib.rs with to_member_id: Uuid, shares: i32, transfer_date: time::Date (ISO8601 serde)."
    - "TransferSharesResponseTO exists with actions: Vec<MemberActionTO>, from: MemberTO, to: MemberTO."
    - "Handler `transfer_shares` exists in genossi_rest/src/membership_adjust.rs and calls rest_state.membership_adjust_service().transfer_shares(...)."
    - "Handler returns 200 OK + TransferSharesResponseTO on success."
    - "Sub-route POST /{from_id}/transfer-shares is registered in genossi_rest/src/member.rs::generate_route BEFORE the /{id} catch-all (D-14-08 / C-17-CF-06)."
    - "OpenAPI annotations include status responses 200, 400, 401, 404, 409, 500."
    - "ApiDoc registration includes transfer_shares in paths(...) and TransferSharesRequestTO + TransferSharesResponseTO in components(schemas(...))."
  artifacts:
    - path: "genossi_rest_types/src/lib.rs"
      provides: "TransferSharesRequestTO + TransferSharesResponseTO DTOs"
      contains: "pub struct TransferSharesRequestTO"
    - path: "genossi_rest/src/membership_adjust.rs"
      provides: "transfer_shares Axum handler with #[utoipa::path] annotations"
      contains: "pub async fn transfer_shares"
    - path: "genossi_rest/src/member.rs"
      provides: "Sub-route /transfer-shares registered BEFORE /{id} catch-all"
      contains: "/transfer-shares"
  key_links:
    - from: "genossi_rest/src/member.rs::generate_route"
      to: "genossi_rest/src/membership_adjust.rs::transfer_shares"
      via: ".route('/{from_id}/transfer-shares', post(crate::membership_adjust::transfer_shares::<RestState>))"
      pattern: "post\\(crate::membership_adjust::transfer_shares"
    - from: "genossi_rest/src/membership_adjust.rs::transfer_shares"
      to: "MembershipAdjustService::transfer_shares (Plan 17-02)"
      via: "rest_state.membership_adjust_service().transfer_shares(...).await?"
      pattern: "transfer_shares\\("
---

<objective>
Mache `transfer_shares` als REST-Endpunkt erreichbar. Definiere die DTOs, schreibe den Axum-Handler mit OpenAPI-Annotationen, und registriere die Sub-Route `POST /api/members/{from_id}/transfer-shares` vor der `/{id}`-Catch-All-Route.

Purpose: Plan 17-02 hat den Service vollständig implementiert. Dieser Plan macht ihn als HTTP-Endpoint erreichbar, mit korrekten Error-Mappings (D-17-10), OpenAPI-Dokumentation für Swagger-UI und korrekter Route-Reihenfolge (D-14-08-Lesson). Frontend (Phase 18) und E2E-Tests (Plan 17-04) bauen auf dieser HTTP-API auf.

Output:
- `TransferSharesRequestTO` + `TransferSharesResponseTO` in `genossi_rest_types/src/lib.rs`
- Handler `pub async fn transfer_shares<RestState>(...)` in `genossi_rest/src/membership_adjust.rs` mit Utoipa-Annotation
- Sub-Route in `genossi_rest/src/member.rs::generate_route` registriert vor `/{id}`
- ApiDoc-Update für Swagger-UI-Sichtbarkeit
- `cargo build --workspace` grün
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-service-rest-uebertrag-cascade/17-CONTEXT.md
@.planning/phases/17-service-rest-uebertrag-cascade/17-PATTERNS.md
@.planning/phases/17-service-rest-uebertrag-cascade/17-02-PLAN.md
@genossi_rest_types/src/lib.rs
@genossi_rest/src/membership_adjust.rs
@genossi_rest/src/member.rs

<interfaces>
From genossi_rest_types/src/lib.rs (existing — DO NOT redefine):
- `MemberTO`, `MemberActionTO` — used in TransferSharesResponseTO.
- `iso8601_date_required` module (lines 84+) — used in TransferSharesRequestTO.transfer_date.
- Patterns: `PartialRepaymentRequestTO` (lines 546–561) and `PartialRepaymentResponseTO` (lines 563–574) are exact templates.

From genossi_rest/src/membership_adjust.rs (existing handlers):
- `partial_repayment` (lines 129–184) — exact handler template.
- `error_handler((async { ... }).await)` wrapper pattern.
- `crate::extract_auth_context(Some(context))?` helper.
- `RestStateDef` trait + `rest_state.membership_adjust_service()` accessor.
- Existing `ApiDoc` registration (lines 186–198) — needs paths(...) extension.

From genossi_rest/src/member.rs:
- `generate_route<RestState>()` function (lines 28–80 area). Three existing sub-routes (lines 64–77) BEFORE catch-all `/{id}` (line ~79).
- Pitfall-1 doc-comment at lines 30–36 documents the sub-route ordering rule.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DTOs TransferSharesRequestTO + TransferSharesResponseTO definieren</name>
  <files>genossi_rest_types/src/lib.rs</files>
  <read_first>
    - .planning/phases/17-service-rest-uebertrag-cascade/17-CONTEXT.md (Domain Request/Response-DTOs)
    - .planning/phases/17-service-rest-uebertrag-cascade/17-PATTERNS.md (Section 3 DTO-Excerpts)
    - genossi_rest_types/src/lib.rs (existing PartialRepaymentRequestTO Z. 546-561, PartialRepaymentResponseTO Z. 563-574, iso8601_date_required Modul Z. 84+)
  </read_first>
  <behavior>
    - TransferSharesRequestTO ist `#[derive(Clone, Debug, Serialize, Deserialize, ToSchema)]` mit drei pub fields: to_member_id: Uuid, shares: i32, transfer_date: time::Date.
    - transfer_date nutzt iso8601_date_required-Serde (`serialize_with`/`deserialize_with`) — selbe Konvention wie PartialRepaymentRequestTO.willensbekundung_date.
    - TransferSharesResponseTO ist `#[derive(Clone, Debug, Serialize, Deserialize, ToSchema)]` mit drei pub fields: actions: Vec<MemberActionTO>, from: MemberTO, to: MemberTO.
    - Beide Structs sind in der lib.rs `pub use` re-exported (oder direkt als `pub struct`-Definition reichen aus, falls die Datei eine flat module-structure hat).
    - cargo build --workspace gruen.
  </behavior>
  <action>
1. Oeffne `genossi_rest_types/src/lib.rs`. Suche `PartialRepaymentRequestTO` (laut PATTERNS.md Section 3 Z. 546-561) und `PartialRepaymentResponseTO` (Z. 563-574).

2. Direkt nach `PartialRepaymentResponseTO` (oder am Ende der DTO-Definitionen) fuege ein:

```rust
/// Request-Body fuer `POST /api/members/{from_id}/transfer-shares` (TRSF-01).
///
/// `shares` muss im Bereich `1..=from.current_shares` liegen (Service-Validation).
/// `shares == from.current_shares` triggert Voll-Uebertrag mit Austritt-Cascade
/// (D-17-01). `to_member_id` darf NICHT gleich dem Path-Parameter `from_id`
/// sein (TRSF-07 / D-17-08, Self-Transfer-Block).
#[derive(Clone, Debug, Serialize, Deserialize, ToSchema)]
pub struct TransferSharesRequestTO {
    pub to_member_id: Uuid,
    /// Anzahl der zu uebertragenden Anteile (1..=from.current_shares).
    #[schema(example = 2)]
    pub shares: i32,
    /// Wirksamkeitsdatum des Uebertrags (TRSF-05 sofort wirksam, kein H1/H2).
    /// Muss in [today.year(), today.year()+1] liegen (Phase-15 D-15-05 Re-use).
    #[serde(
        serialize_with = "iso8601_date_required::serialize",
        deserialize_with = "iso8601_date_required::deserialize"
    )]
    #[schema(example = "2026-06-15")]
    pub transfer_date: time::Date,
}

/// Response-Body fuer `POST /api/members/{from_id}/transfer-shares` (C-17-CF-07).
///
/// `actions.len()` ist 2 (Teil-Uebertrag) oder 3 (Voll-Uebertrag inkl. Austritt).
/// `from` + `to` enthalten die aktualisierten Members nach Tx-Commit (inkl.
/// neuer `version` und ggf. `exit_date` bei Voll-Uebertrag), so dass das Frontend
/// (Phase 18) mit einem Round-Trip beide Member-Detail-Views refreshen kann.
#[derive(Clone, Debug, Serialize, Deserialize, ToSchema)]
pub struct TransferSharesResponseTO {
    pub actions: Vec<MemberActionTO>,
    pub from: MemberTO,
    pub to: MemberTO,
}
```

3. Verifiziere dass `Uuid`, `Serialize`, `Deserialize`, `ToSchema`, `time::Date`, `iso8601_date_required`, `MemberTO`, `MemberActionTO` bereits am Top der Datei importiert sind. PATTERNS.md sagt "no new module needed" — d.h. alle Imports existieren.

4. Falls die Datei eine `pub use {DTO}` Re-Export-Sektion hat (typisch fuer module-organized lib.rs), fuege `TransferSharesRequestTO` und `TransferSharesResponseTO` dort hinzu. Wenn die Datei flach ist (Direct-`pub struct`-Definitions), reicht die obige Einfuegung.

5. `cargo build --workspace --all-features`.
  </action>
  <verify>
    <automated>cargo build --workspace --all-features 2>&1 | tail -3 && grep -c 'pub struct TransferSharesRequestTO' genossi_rest_types/src/lib.rs && grep -c 'pub struct TransferSharesResponseTO' genossi_rest_types/src/lib.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace --all-features` exits 0
    - `grep -c 'pub struct TransferSharesRequestTO' genossi_rest_types/src/lib.rs` == 1
    - `grep -c 'pub struct TransferSharesResponseTO' genossi_rest_types/src/lib.rs` == 1
    - `grep -c 'to_member_id: Uuid' genossi_rest_types/src/lib.rs` >= 1
    - `grep -c 'pub shares: i32' genossi_rest_types/src/lib.rs` >= 1
    - `grep -c 'iso8601_date_required' genossi_rest_types/src/lib.rs` >= 4 (existing fields + new transfer_date use)
    - `grep -c 'transfer_date: time::Date' genossi_rest_types/src/lib.rs` >= 1
    - `grep -c 'actions: Vec<MemberActionTO>' genossi_rest_types/src/lib.rs` >= 1
  </acceptance_criteria>
  <done>DTOs sind definiert und compilen; Plan 17-04 (E2E) kann sie fuer JSON-Parsing nutzen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Handler transfer_shares + ApiDoc-Registrierung</name>
  <files>genossi_rest/src/membership_adjust.rs</files>
  <read_first>
    - .planning/phases/17-service-rest-uebertrag-cascade/17-CONTEXT.md (Domain REST-Endpoint + D-17-10 Error-Mapping-Tabelle)
    - .planning/phases/17-service-rest-uebertrag-cascade/17-PATTERNS.md (Section 4 vollstaendiger Handler-Excerpt + ApiDoc-Registrierung)
    - genossi_rest/src/membership_adjust.rs (existing partial_repayment-Handler Z. 129-184, ApiDoc Z. 186-198, Imports Z. 1-32)
  </read_first>
  <behavior>
    - Imports erweitert um `TransferSharesRequestTO`, `TransferSharesResponseTO`.
    - Handler `transfer_shares<RestState: RestStateDef>` mit #[instrument(skip(rest_state))] und #[utoipa::path(...)] Annotation.
    - Handler-Body wrappt `rest_state.membership_adjust_service().transfer_shares(...)` Call in `error_handler((async { ... }).await)`.
    - Service-Call-Argumente: from_id (Path), req.to_member_id, req.shares, req.transfer_date, extract_auth_context(Some(context))?, None (tx).
    - Response-Body ist `TransferSharesResponseTO { actions: actions.iter().map(MemberActionTO::from).collect(), from: MemberTO::from(&from), to: MemberTO::from(&to) }` -> serde_json -> Body.
    - Utoipa-responses listen 200, 400, 401, 404, 409, 500 (per D-17-10).
    - ApiDoc-Block (existing `#[derive(utoipa::OpenApi)]` struct) wird erweitert: paths(...) bekommt `transfer_shares`, components(schemas(...)) bekommt `TransferSharesRequestTO` und `TransferSharesResponseTO`.
    - cargo build --workspace gruen.
  </behavior>
  <action>
1. Oeffne `genossi_rest/src/membership_adjust.rs`. Suche die existing Imports am Top der Datei. Ergaenze den `use genossi_rest_types::{...}` Block um `TransferSharesRequestTO` und `TransferSharesResponseTO`:

```rust
use genossi_rest_types::{
    /* existing — bewahren */
    TransferSharesRequestTO, TransferSharesResponseTO,
};
```

(Den existing Import-Block ergaenzen — NICHT duplizieren. Wenn die Datei einen multi-line `use genossi_rest_types::{ ... };` Block hat, fuege die neuen Symbol-Namen alphabetisch oder ans Ende ein.)

2. Suche den existing `partial_repayment` Handler (PATTERNS.md sagt Z. 129-184). Direkt darunter (oder gleich vor dem ApiDoc-Block) fuege den neuen Handler ein:

```rust
#[instrument(skip(rest_state))]
#[utoipa::path(
    post,
    tag = "Members",
    path = "/{from_id}/transfer-shares",
    params(("from_id" = Uuid, Path, description = "Sender Member ID (path parameter)")),
    request_body = TransferSharesRequestTO,
    responses(
        (status = 200, description = "Transfer successful (returns 2 actions for Teil-Uebertrag, 3 for Voll-Uebertrag inkl. Austritt)", body = TransferSharesResponseTO),
        (status = 400, description = "Validation error: self-transfer (TRSF-07), shares out of range (1..=from.current_shares), or transfer_date outside [today.year(), today.year()+1]"),
        (status = 401, description = "Unauthorized — kein Login oder keine admin-Rolle (D-15-12: PermissionDenied wird auf 401 gemappt)"),
        (status = 404, description = "From oder To Member nicht gefunden / soft-deleted"),
        (status = 409, description = "Recipient already cancelled (PERM-03 / D-17-07) ODER optimistic-locking conflict"),
        (status = 500, description = "SQLITE_BUSY mid-cascade (Race-Test Verlierer-Pfad, D-17-06)"),
    ),
)]
pub async fn transfer_shares<RestState: RestStateDef>(
    rest_state: State<RestState>,
    Extension(context): Extension<Context>,
    Path(from_id): Path<Uuid>,
    Json(req): Json<TransferSharesRequestTO>,
) -> Response {
    error_handler(
        (async {
            let (actions, from, to) = rest_state
                .membership_adjust_service()
                .transfer_shares(
                    from_id,
                    req.to_member_id,
                    req.shares,
                    req.transfer_date,
                    crate::extract_auth_context(Some(context))?,
                    None,
                )
                .await?;
            let response = TransferSharesResponseTO {
                actions: actions.iter().map(MemberActionTO::from).collect(),
                from: MemberTO::from(&from),
                to: MemberTO::from(&to),
            };
            Ok(Response::builder()
                .status(200)
                .header("Content-Type", "application/json")
                .body(Body::new(serde_json::to_string(&response)?))
                .unwrap())
        })
        .await,
    )
}
```

3. Suche die existing `ApiDoc`-Struct (PATTERNS.md sagt Z. 186-198). Erweitere beide Listen:

```rust
#[derive(utoipa::OpenApi)]
#[openapi(
    paths(cancel_membership, increase_shares, partial_repayment, transfer_shares),
    components(schemas(
        CancelMembershipRequestTO,
        IncreaseSharesRequestTO,
        MembershipAdjustResponseTO,
        PartialRepaymentRequestTO,
        PartialRepaymentResponseTO,
        TransferSharesRequestTO,
        TransferSharesResponseTO,
    )),
    tags((name = "Members", description = "Phase 15-17 v1.2 membership-adjust endpoints"))
)]
pub struct ApiDoc;
```

(Erhalte die existing Liste; fuege transfer_shares zu paths() und TransferSharesRequestTO / TransferSharesResponseTO zu components(schemas()) hinzu.)

4. Verifiziere dass `MemberActionTO`, `MemberTO`, `Response`, `Body`, `error_handler`, `extract_auth_context`, `RestStateDef`, `State`, `Extension`, `Path`, `Json`, `Uuid`, `Context` bereits am Top importiert sind (sie sind, weil partial_repayment sie nutzt).

5. `cargo build --workspace --all-features`. Bei Fehler: vergleiche Handler-Signatur exakt mit partial_repayment.
  </action>
  <verify>
    <automated>cargo build --workspace --all-features 2>&1 | tail -3 && grep -c 'pub async fn transfer_shares' genossi_rest/src/membership_adjust.rs && grep -c 'transfer_shares' genossi_rest/src/membership_adjust.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace --all-features` exits 0
    - `grep -c 'pub async fn transfer_shares' genossi_rest/src/membership_adjust.rs` == 1
    - `grep -c 'TransferSharesRequestTO' genossi_rest/src/membership_adjust.rs` >= 3 (import + request_body + handler arg + ApiDoc)
    - `grep -c 'TransferSharesResponseTO' genossi_rest/src/membership_adjust.rs` >= 3 (import + response body + ApiDoc)
    - `grep -c 'paths(cancel_membership, increase_shares, partial_repayment, transfer_shares)' genossi_rest/src/membership_adjust.rs` == 1
    - `grep -c '"/{from_id}/transfer-shares"' genossi_rest/src/membership_adjust.rs` == 1
    - `grep -c 'status = 200' genossi_rest/src/membership_adjust.rs` >= 1 (handler-spezifisch)
    - `grep -c 'status = 409' genossi_rest/src/membership_adjust.rs` >= 1 (PERM-03)
  </acceptance_criteria>
  <done>Handler ist definiert + ApiDoc-Registrierung erweitert. Plan-Task-3 (Route-Wiring) macht ihn aufrufbar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Sub-Route /transfer-shares in member.rs::generate_route registrieren (VOR /{id})</name>
  <files>genossi_rest/src/member.rs</files>
  <read_first>
    - .planning/phases/17-service-rest-uebertrag-cascade/17-CONTEXT.md (REST-Endpoint Sub-Route + C-17-CF-06 D-14-08-Lesson)
    - .planning/phases/17-service-rest-uebertrag-cascade/17-PATTERNS.md (Section 5 Route-Registrierung-Excerpt + Pitfall-1-Kommentar Z. 30-36)
    - genossi_rest/src/member.rs (Z. 28-82 generate_route mit existing Sub-Routes cancel, increase-shares, partial-repayment vor /{id} catch-all)
  </read_first>
  <behavior>
    - Eine neue `.route("/{from_id}/transfer-shares", post(crate::membership_adjust::transfer_shares::<RestState>))` wird in `generate_route` registriert.
    - Sie steht NACH den existing `/partial-repayment`, `/cancel`, `/increase-shares` Sub-Routes UND VOR der `/{id}`-Catch-All-Route.
    - cargo build --workspace gruen.
    - Existing Pitfall-1-Comment Z. 30-36 bleibt unveraendert (dokumentiert die Ordering-Regel).
  </behavior>
  <action>
1. Oeffne `genossi_rest/src/member.rs`. Suche `generate_route` (laut PATTERNS.md beginnt es etwa Z. 28). Lies den Block von Z. 28 bis ~Z. 82.

2. Identifiziere:
   - Die Stelle, wo die letzte Literal-Sub-Route (`/partial-repayment` oder analog) registriert wird.
   - Die Catch-All `/{id}`-Route (typischerweise `GET /{id}`, `PUT /{id}`, `DELETE /{id}`).

3. Fuege direkt VOR der ersten `/{id}`-Route die neue Route ein:

```rust
        // Phase 17 v1.2 (D-17 / C-17-CF-06): Sub-Route fuer Uebertrag.
        // MUSS vor /{id} registriert sein (D-14-08-Lesson) — axum-Routing-Defense.
        .route(
            "/{from_id}/transfer-shares",
            post(crate::membership_adjust::transfer_shares::<RestState>),
        )
```

WICHTIG: Der konkrete Stil der Route-Kette haengt vom Code ab. Wenn die existing Routes mit `.route("/{id}/partial-repayment", post(crate::membership_adjust::partial_repayment::<RestState>))` enden, kommt unsere neue Route direkt danach mit dem gleichen Stil. Die Pattern-Mapper-Korrektur sagt: Path-Parameter heisst `{from_id}` (statt `{id}`) damit der Handler-Signatur klarer ist — beides ist syntaktisch gueltig.

4. Falls `post` aus `axum::routing::post` noch nicht importiert ist (was unwahrscheinlich ist, weil partial-repayment es nutzt), ergaenze den Import.

5. `cargo build --workspace --all-features`.

6. Sanity-Check via grep dass die Route-Ordering nicht versehentlich nach `/{id}` rutscht: `grep -n '/{id}\|/{from_id}/transfer-shares' genossi_rest/src/member.rs` zeigt die Reihenfolge.
  </action>
  <verify>
    <automated>cargo build --workspace --all-features 2>&1 | tail -3 && grep -n '/transfer-shares\|"/{id}"' genossi_rest/src/member.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace --all-features` exits 0
    - `grep -c '/transfer-shares' genossi_rest/src/member.rs` >= 1
    - `grep -c 'crate::membership_adjust::transfer_shares' genossi_rest/src/member.rs` >= 1
    - Route ordering check: `awk '/\/transfer-shares/{ts=NR} /"\/\{id\}"/{id=NR} END{exit (ts>0 && id>0 && ts<id)?0:1}' genossi_rest/src/member.rs` exits 0 (transfer-shares declared before /{id} catch-all)
    - `grep -c 'Pitfall 1' genossi_rest/src/member.rs` >= 1 (existing comment preserved)
  </acceptance_criteria>
  <done>Endpoint ist aufrufbar; Plan 17-04 E2E-Tests koennen POSTs absetzen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP-Client -> REST (axum) | untrusted JSON, untrusted Path-Param |
| REST-Handler -> Service-Layer | typed DTO, authenticated Context, untrusted i32 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03-01 | Spoofing | unauthenticated POST | mitigate | Auth-Middleware injiziert Extension<Context>; extract_auth_context(Some(context))? lehnt None ab; Service-Layer ADMIN_PRIVILEGE-Funnel (Plan 17-02). |
| T-17-03-02 | Tampering | Self-Transfer via path-vs-body mismatch (from_id im Path, body.to_member_id == from_id) | mitigate | TRSF-07 / D-17-08 — Service-Layer validate_transfer_inputs prueft `from_id == to_id`; REST-Handler reicht beide unveraendert durch. Plan 17-04 Test verifiziert E2E. |
| T-17-03-03 | Information Disclosure | OpenAPI exponiert interne Felder | accept | TransferSharesResponseTO enthaelt nur Member + MemberAction TOs, die ohnehin via /api/members/{id} und /api/members/{id}/actions zugaenglich sind. Konsistent mit Phase 15/16. |
| T-17-03-04 | Denial of Service | Endloser POST-Loop oder Riesen-Payload | accept | Globale rate-limiter `api_rate_layer` (60 burst, 1/sec refill) drosselt; JSON-Body-Size-Limit per Axum-Default. |
| T-17-03-05 | Tampering | Route-Ordering-Pitfall: /transfer-shares wird vom /{id}-Catch-All gefressen | mitigate | C-17-CF-06 / D-14-08-Lesson — Sub-Route MUSS vor /{id} registriert sein. Acceptance-Criterium verifiziert via awk Reihenfolge. |
| T-17-03-06 | Elevation of Privilege | Frontend-only-Validation umgangen (negative shares im POST-Body) | mitigate | shares: i32 wird von serde deserialisiert ohne Range-Check; Service-Layer validate_transfer_inputs (Plan 17-01/02) lehnt `shares <= 0` ab; verified durch Plan 17-04 Test 3 (Self-Transfer 400). |
| T-17-03-07 | Spoofing | OpenAPI exponiert from_id in URL fuer Audit-Replay | accept | URL-only mit POST-method ist nicht idempotent; Optimistic-Locking + Race-Defense (Plan 17-02 Single-Tx) ist die echte Verteidigung. |
</threat_model>

<verification>
- `cargo build --workspace --all-features` exits 0
- `cargo clippy -p genossi_rest_types -p genossi_rest --all-targets 2>&1 | grep -E "^error" | wc -l` == 0
- Route-Ordering grep-gate: transfer-shares vor /{id} (siehe Acceptance)
- ApiDoc enthaelt transfer_shares Path + DTOs
</verification>

<success_criteria>
- DTOs in genossi_rest_types/src/lib.rs definiert
- Handler in genossi_rest/src/membership_adjust.rs mit korrekten Utoipa-Annotationen
- Sub-Route in genossi_rest/src/member.rs::generate_route vor /{id}
- Swagger-UI zeigt POST /api/members/{from_id}/transfer-shares
- Plan 17-04 kann auf den HTTP-Endpoint zugreifen
</success_criteria>

<output>
After completion, create `.planning/phases/17-service-rest-uebertrag-cascade/17-03-SUMMARY.md` mit:
- Exakte Linien-Range der DTOs in genossi_rest_types/src/lib.rs
- Exakte Linien-Range des Handlers in genossi_rest/src/membership_adjust.rs
- Exakte Linien-Range der Route-Registrierung in genossi_rest/src/member.rs (mit Beweis dass sie VOR /{id} ist)
- ApiDoc-Diff (vorher/nachher)
- Bestaetigung dass cargo build gruen
</output>
