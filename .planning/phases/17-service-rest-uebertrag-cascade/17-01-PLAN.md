---
phase: 17-service-rest-uebertrag-cascade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - genossi_service/src/membership_adjust.rs
  - genossi_service_impl/src/membership_adjust.rs
autonomous: true
requirements: [TRSF-07]
requirements_addressed: [TRSF-07]
tags: [rust, axum, service-trait, validation, pure-function]
must_haves:
  truths:
    - "MembershipAdjustService trait exposes async fn transfer_shares with the exact signature from D-17/C-17-CF-01 (pure additive growth, no new trait)."
    - "validate_transfer_inputs is a pub(crate) pure function in genossi_service_impl/src/membership_adjust.rs that returns Vec<ValidationFailureItem> with no DAO/IO calls (D-17-09)."
    - "Self-transfer (from_id == to_id) produces a ValidationFailureItem with field 'to_member_id' (D-17-08/TRSF-07)."
    - "shares <= 0 produces a ValidationFailureItem with field 'shares' and message about 'at least 1'."
    - "shares > from_current_shares produces a ValidationFailureItem with field 'shares'."
    - "shares == from_current_shares (Voll-Übertrag-Boundary) is VALID and returns an empty error vec."
    - "TRANSFER_PROCESS = 'member-adjust.transfer' constant is declared near the existing process-string constants (D-17-04)."
  artifacts:
    - path: "genossi_service/src/membership_adjust.rs"
      provides: "MembershipAdjustService::transfer_shares trait method declaration"
      contains: "async fn transfer_shares"
    - path: "genossi_service_impl/src/membership_adjust.rs"
      provides: "validate_transfer_inputs pure function + TRANSFER_PROCESS constant + 6 edge-case unit tests"
      contains: "fn validate_transfer_inputs"
  key_links:
    - from: "genossi_service_impl/src/membership_adjust.rs"
      to: "genossi_service/src/membership_adjust.rs"
      via: "trait impl block (existing MembershipAdjustServiceImpl<Deps>) — trait method added in this plan is implemented in plan 17-02"
      pattern: "impl.*MembershipAdjustService.*for MembershipAdjustServiceImpl"
---

<objective>
Lege die Vertragsfläche für Phase 17 fest: erweitere das `MembershipAdjustService`-Trait um `transfer_shares`, definiere die Pure-Function `validate_transfer_inputs` mit allen Edge-Case-Unit-Tests, und führe die geteilte Process-String-Konstante ein.

Purpose: Trait + Pure-Function-Validator + Audit-Process-String sind das Fundament. Plan 17-02 baut die Pipeline darauf, Plan 17-03 das REST-Frontend, Plan 17-04 die E2E-Tests. Diese Trennung erlaubt deterministische TDD-Edge-Case-Tests ohne DB-Mocks (D-17-09) und friert die Trait-API ein, bevor die Impl beginnt.

Output:
- Trait-Methode `transfer_shares` mit exakter Signatur in `genossi_service/src/membership_adjust.rs`
- Konstante `TRANSFER_PROCESS = "member-adjust.transfer"` in `genossi_service_impl/src/membership_adjust.rs`
- Pure-Function `validate_transfer_inputs(from_id, to_id, shares, from_current_shares) -> Vec<ValidationFailureItem>`
- 6 Edge-Case-Unit-Tests, alle grün
- `cargo build` bleibt grün (Service-Impl-Stub für `transfer_shares` darf `unimplemented!()` returnen — wird in Plan 17-02 gefüllt)
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-service-rest-uebertrag-cascade/17-CONTEXT.md
@.planning/phases/17-service-rest-uebertrag-cascade/17-PATTERNS.md
@genossi_service/src/membership_adjust.rs
@genossi_service_impl/src/membership_adjust.rs

<interfaces>
<!-- Trait method to ADD (from 17-PATTERNS.md §1). Note: shares is i32 to match MemberEntity.current_shares (NOT i64 as CONTEXT D-17 says — this is the Pattern-Mapper correction). -->

To genossi_service/src/membership_adjust.rs (additive, inside the existing #[automock] trait — same Trait, new method):

```rust
/// Uebertraegt `shares` Anteile von `from_id` an `to_id`. Sofort wirksam,
/// kein H1/H2-Stichtag (TRSF-05). Bei Voll-Uebertrag (`from.current_shares == shares`)
/// wird zusaetzlich ein `MemberAction::Austritt(from)` mit
/// `transfer_member_id = Some(to_id)` erzeugt (D-17-01/03).
///
/// Returns tuple `(actions, from, to)`:
/// - `actions`: 2 (Teil) oder 3 (Voll) `MemberAction`-Eintraege (Abgabe, Empfang,
///   optional Austritt). C-17-CF-08 — domain-Werte, kein DTO-Wrapping.
/// - `from`/`to`: aktualisierte Member nach Tx-Commit.
async fn transfer_shares(
    &self,
    from_id: Uuid,
    to_id: Uuid,
    shares: i32,
    transfer_date: time::Date,
    context: Authentication<Self::Context>,
    tx: Option<Self::Transaction>,
) -> Result<(Vec<MemberAction>, Member, Member), ServiceError>;
```

To genossi_service_impl/src/membership_adjust.rs (additive — near existing process-string constants like CANCEL_PROCESS / INCREASE_PROCESS / PARTIAL_REPAYMENT_PROCESS):

```rust
/// Shared Audit-Process-String fuer ALLE Cascade-Writes (D-17-04 / AUDT-02).
const TRANSFER_PROCESS: &str = "member-adjust.transfer";
```

To genossi_service_impl/src/membership_adjust.rs (additive — near other pure-function validators like validate_partial_repayment_shares):

```rust
/// Pure-Function range-validator fuer Uebertrag (D-17-09 / TRSF-07).
pub(crate) fn validate_transfer_inputs(
    from_id: Uuid,
    to_id: Uuid,
    shares: i32,
    from_current_shares: i32,
) -> Vec<ValidationFailureItem> {
    let mut errors = Vec::new();
    if from_id == to_id {
        errors.push(ValidationFailureItem {
            field: Arc::from("to_member_id"),
            message: Arc::from("cannot transfer to self"),
        });
    }
    if shares <= 0 {
        errors.push(ValidationFailureItem {
            field: Arc::from("shares"),
            message: Arc::from("shares must be at least 1"),
        });
    }
    if shares > from_current_shares {
        errors.push(ValidationFailureItem {
            field: Arc::from("shares"),
            message: Arc::from(format!(
                "shares ({}) exceeds from.current_shares ({})",
                shares, from_current_shares
            )),
        });
    }
    errors
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Trait-Methode transfer_shares deklarieren + TRANSFER_PROCESS-Konstante</name>
  <files>genossi_service/src/membership_adjust.rs, genossi_service_impl/src/membership_adjust.rs</files>
  <read_first>
    - .planning/phases/17-service-rest-uebertrag-cascade/17-CONTEXT.md (D-17-04, D-17-09, C-17-CF-01, Pipeline-Schritte 1-15)
    - .planning/phases/17-service-rest-uebertrag-cascade/17-PATTERNS.md (Section 1 + Section 2 Process-string constant)
    - genossi_service/src/membership_adjust.rs (existing trait + automock attribute — line where partial_repayment is declared, around line 65)
    - genossi_service_impl/src/membership_adjust.rs (existing const declarations near top; existing impl block for MembershipAdjustServiceImpl<Deps>)
  </read_first>
  <behavior>
    - Behavior 1: Das MembershipAdjustService-Trait listet die neue Methode transfer_shares mit der EXAKTEN Signatur aus den Interfaces (oben). Keine Default-Implementation. Wegen #[automock] auf dem Trait wird automatisch ein Mock generiert.
    - Behavior 2: In genossi_service_impl/src/membership_adjust.rs steht die Konstante `const TRANSFER_PROCESS: &str = "member-adjust.transfer";` in der Nähe der existierenden Process-Konstanten (analog zu `CANCEL_PROCESS`, `INCREASE_PROCESS`, `PARTIAL_REPAYMENT_PROCESS`).
    - Behavior 3: Der bestehende `impl<Deps> MembershipAdjustService<...> for MembershipAdjustServiceImpl<Deps>` block enthält eine Stub-Implementation `async fn transfer_shares(...) -> ... { unimplemented!("Plan 17-02 implements this") }`, damit `cargo build` grün bleibt und Plan 17-02 die Pipeline füllen kann.
    - Behavior 4: `cargo build --workspace` bleibt erfolgreich.
  </behavior>
  <action>
1. Öffne `genossi_service/src/membership_adjust.rs`. Lokalisiere den `#[automock]`-`#[async_trait]`-Trait `MembershipAdjustService` und füge NACH der existierenden `partial_repayment`-Methode die neue Trait-Methode ein:

```rust
/// Uebertraegt `shares` Anteile von `from_id` an `to_id`. Sofort wirksam,
/// kein H1/H2-Stichtag (TRSF-05). Bei Voll-Uebertrag (`from.current_shares == shares`)
/// wird zusaetzlich ein `MemberAction::Austritt(from)` mit
/// `transfer_member_id = Some(to_id)` erzeugt (D-17-01/03).
///
/// Returns tuple `(actions, from, to)`:
/// - `actions`: 2 (Teil) oder 3 (Voll) `MemberAction`-Eintraege (Abgabe, Empfang,
///   optional Austritt). C-17-CF-08 — domain-Werte, kein DTO-Wrapping.
/// - `from`/`to`: aktualisierte Member nach Tx-Commit.
async fn transfer_shares(
    &self,
    from_id: Uuid,
    to_id: Uuid,
    shares: i32,
    transfer_date: time::Date,
    context: Authentication<Self::Context>,
    tx: Option<Self::Transaction>,
) -> Result<(Vec<MemberAction>, Member, Member), ServiceError>;
```

Falls Imports fehlen, ergänze sie minimal — `time::Date` ist bereits da (partial_repayment nutzt sie); `Member` und `MemberAction` sind über die existing `use crate::member::Member;` und `use crate::member_action::MemberAction;` bereits importiert.

2. Öffne `genossi_service_impl/src/membership_adjust.rs`. Suche die existing Process-String-Konstanten (lines 32–48 laut PATTERNS.md). Füge in derselben Block-Sektion eine neue Konstante hinzu:

```rust
/// Shared Audit-Process-String fuer ALLE Cascade-Writes des Uebertrags (D-17-04 / AUDT-02).
/// Filter `WHERE process = 'member-adjust.transfer'` findet ALLE Writes eines
/// Uebertrag-Vorgangs (2 oder 3 MemberAction-Creates + 2 Member-Updates).
const TRANSFER_PROCESS: &str = "member-adjust.transfer";
```

3. In derselben Datei (`genossi_service_impl/src/membership_adjust.rs`), suche den `impl<Deps> MembershipAdjustService<...> for MembershipAdjustServiceImpl<Deps>`-Block. Nach der existing `partial_repayment`-Methode füge einen Stub für `transfer_shares` hinzu (Plan 17-02 ersetzt diesen Body):

```rust
async fn transfer_shares(
    &self,
    _from_id: Uuid,
    _to_id: Uuid,
    _shares: i32,
    _transfer_date: time::Date,
    _context: Authentication<Self::Context>,
    _tx: Option<Self::Transaction>,
) -> Result<(Vec<MemberAction>, Member, Member), ServiceError> {
    unimplemented!("Plan 17-02 implements the 15-step pipeline")
}
```

4. Führe `cargo build --workspace` aus. Falls Compile-Fehler durch fehlende Imports auftreten, ergänze nur das Nötige (typischerweise nichts — partial_repayment hat die gleichen Typen).

5. Falls TRANSFER_PROCESS in dieser Datei als ungenutzt markiert wird, prefix die Konstante NICHT mit `_` — sie wird in Plan 17-02 verwendet, und `#[allow(dead_code)]` ist akzeptabel als Übergangs-Marker, ist aber NICHT erforderlich, weil eine `const` nicht als dead-code geflagt wird.
  </action>
  <verify>
    <automated>cargo build --workspace --all-features 2>&1 | tail -5 && grep -c 'async fn transfer_shares' genossi_service/src/membership_adjust.rs && grep -c 'TRANSFER_PROCESS' genossi_service_impl/src/membership_adjust.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace --all-features` exits 0
    - `grep -c 'async fn transfer_shares' genossi_service/src/membership_adjust.rs` == 1
    - `grep -c 'const TRANSFER_PROCESS: &str = "member-adjust.transfer"' genossi_service_impl/src/membership_adjust.rs` == 1
    - `grep -c 'unimplemented!' genossi_service_impl/src/membership_adjust.rs` >= 1 (Stub-Marker; Plan 17-02 entfernt das)
    - `grep -c 'transfer_member_id = Some(to_id)' genossi_service/src/membership_adjust.rs` >= 1 (Doc-Kommentar)
  </acceptance_criteria>
  <done>Trait-Methode signiert, Konstante deklariert, Stub-Impl erlaubt cargo build. Plan 17-02 kann jetzt die Pipeline implementieren ohne Trait-Drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure-Function validate_transfer_inputs + 6 Edge-Case-Tests</name>
  <files>genossi_service_impl/src/membership_adjust.rs</files>
  <read_first>
    - .planning/phases/17-service-rest-uebertrag-cascade/17-CONTEXT.md (D-17-08, D-17-09, specifics: 6 Edge-Cases)
    - .planning/phases/17-service-rest-uebertrag-cascade/17-PATTERNS.md (Section 2: validate_transfer_inputs pattern; Section "Pure-Function Validation Pattern" mit Source lines 540–568)
    - genossi_service_impl/src/membership_adjust.rs (existing `validate_partial_repayment_shares` für Pattern-Vergleich; existing #[cfg(test)] mod tests mit pure-function tests lines 692–741)
  </read_first>
  <behavior>
    - Behavior 1: `validate_transfer_inputs(from_id, to_id, shares, from_current_shares)` ist `pub(crate)`, NICHT `pub` (Pattern aus `validate_partial_repayment_shares`).
    - Behavior 2: Self-Transfer (`from_id == to_id`) → ValidationFailureItem mit `field == "to_member_id"` und `message == "cannot transfer to self"`.
    - Behavior 3: shares=0 oder shares=-1 → ValidationFailureItem mit `field == "shares"` und `message` enthält "at least 1".
    - Behavior 4: shares > from_current_shares → ValidationFailureItem mit `field == "shares"` und `message` enthält "exceeds".
    - Behavior 5: shares == from_current_shares (Voll-Übertrag-Boundary, n=5, from=5) → leerer Vec (valid).
    - Behavior 6: shares < from_current_shares mit shares >= 1 (Teil-Übertrag, n=1, from=5) → leerer Vec (valid).
    - Behavior 7: Mehrere gleichzeitige Verletzungen (z.B. self-transfer UND shares=0) → Vec mit beiden Failures.
    - Behavior 8: `cargo test -p genossi_service_impl --lib membership_adjust::tests` läuft alle neuen Tests grün.
  </behavior>
  <action>
1. Öffne `genossi_service_impl/src/membership_adjust.rs`. Suche die existing `validate_partial_repayment_shares` (laut PATTERNS.md lines 540–568) und füge die neue Pure-Function direkt darunter ein:

```rust
/// Pure-Function range-validator fuer Uebertrag (D-17-09 / TRSF-07).
///
/// Wirft `ValidationError` bei:
/// - `from_id == to_id` (TRSF-07 self-transfer)
/// - `shares <= 0` (mindestens 1 Anteil)
/// - `shares > from_current_shares` (Voll-Uebertrag-Boundary inkl., n == current
///   ist GUELTIG — Voll-Uebertrag-Branch wird im Service ausgewertet, D-17-01)
pub(crate) fn validate_transfer_inputs(
    from_id: uuid::Uuid,
    to_id: uuid::Uuid,
    shares: i32,
    from_current_shares: i32,
) -> Vec<genossi_service::ValidationFailureItem> {
    use std::sync::Arc;
    use genossi_service::ValidationFailureItem;

    let mut errors = Vec::new();
    if from_id == to_id {
        errors.push(ValidationFailureItem {
            field: Arc::from("to_member_id"),
            message: Arc::from("cannot transfer to self"),
        });
    }
    if shares <= 0 {
        errors.push(ValidationFailureItem {
            field: Arc::from("shares"),
            message: Arc::from("shares must be at least 1"),
        });
    }
    if shares > from_current_shares {
        errors.push(ValidationFailureItem {
            field: Arc::from("shares"),
            message: Arc::from(format!(
                "shares ({}) exceeds from.current_shares ({})",
                shares, from_current_shares
            )),
        });
    }
    errors
}
```

Beachte: Wenn die Datei bereits `use std::sync::Arc;` und `use genossi_service::ValidationFailureItem;` am Top hat (sie tut es — `validate_partial_repayment_shares` nutzt sie), kannst du die `use`-Statements aus dem Funktionskörper entfernen und Top-of-File-Imports nutzen. Verifiziere via Read.

2. Suche `#[cfg(test)] mod tests` (laut PATTERNS.md lines 692–741). Direkt nach den bestehenden `validate_partial_repayment_shares_*`-Tests füge die 6 neuen Tests ein:

```rust
// --- D-17-09 / TRSF-07: validate_transfer_inputs ---

#[test]
fn test_validate_transfer_n_zero_invalid() {
    let from = uuid::Uuid::new_v4();
    let to = uuid::Uuid::new_v4();
    let errs = super::validate_transfer_inputs(from, to, 0, 5);
    assert_eq!(errs.len(), 1, "n=0 must produce exactly one error");
    assert_eq!(&*errs[0].field, "shares");
    assert!(errs[0].message.contains("at least 1"));
}

#[test]
fn test_validate_transfer_n_negative_invalid() {
    let from = uuid::Uuid::new_v4();
    let to = uuid::Uuid::new_v4();
    let errs = super::validate_transfer_inputs(from, to, -1, 5);
    assert_eq!(errs.len(), 1, "n=-1 must produce exactly one error");
    assert_eq!(&*errs[0].field, "shares");
}

#[test]
fn test_validate_transfer_n_equal_current_shares_valid() {
    let from = uuid::Uuid::new_v4();
    let to = uuid::Uuid::new_v4();
    let errs = super::validate_transfer_inputs(from, to, 5, 5);
    assert!(
        errs.is_empty(),
        "Voll-Uebertrag-Boundary (n == current_shares) must be valid; got {:?}",
        errs
    );
}

#[test]
fn test_validate_transfer_n_exceeds_current_shares_invalid() {
    let from = uuid::Uuid::new_v4();
    let to = uuid::Uuid::new_v4();
    let errs = super::validate_transfer_inputs(from, to, 6, 5);
    assert_eq!(errs.len(), 1, "n>current_shares must produce exactly one error");
    assert_eq!(&*errs[0].field, "shares");
    assert!(errs[0].message.contains("exceeds"));
}

#[test]
fn test_validate_transfer_self_invalid() {
    let id = uuid::Uuid::new_v4();
    let errs = super::validate_transfer_inputs(id, id, 1, 5);
    assert!(
        errs.iter().any(|e| &*e.field == "to_member_id"),
        "self-transfer must produce a to_member_id error; got {:?}",
        errs
    );
    assert!(
        errs.iter().any(|e| e.message.contains("cannot transfer to self")),
        "must include the canonical message; got {:?}",
        errs
    );
}

#[test]
fn test_validate_transfer_n_one_valid() {
    let from = uuid::Uuid::new_v4();
    let to = uuid::Uuid::new_v4();
    let errs = super::validate_transfer_inputs(from, to, 1, 5);
    assert!(
        errs.is_empty(),
        "n=1 (Teil-Uebertrag) must be valid; got {:?}",
        errs
    );
}

#[test]
fn test_validate_transfer_multiple_violations_accumulate() {
    let id = uuid::Uuid::new_v4();
    // Self-transfer AND shares=0 — beide Fehler müssen gesammelt werden.
    let errs = super::validate_transfer_inputs(id, id, 0, 5);
    assert_eq!(errs.len(), 2, "two distinct violations must accumulate; got {:?}", errs);
}
```

3. Führe `cargo test -p genossi_service_impl --lib membership_adjust::tests::test_validate_transfer_n_zero_invalid test_validate_transfer_n_negative_invalid test_validate_transfer_n_equal_current_shares_valid test_validate_transfer_n_exceeds_current_shares_invalid test_validate_transfer_self_invalid test_validate_transfer_n_one_valid test_validate_transfer_multiple_violations_accumulate` aus.

4. Falls ein Test fehlschlägt, korrigiere die Funktion ODER den Test — die Funktion ist der Source-of-Truth. Stelle sicher dass `cargo build --workspace` grün bleibt.
  </action>
  <verify>
    <automated>cargo test -p genossi_service_impl --lib membership_adjust::tests::test_validate_transfer 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p genossi_service_impl --lib membership_adjust::tests::test_validate_transfer` exits 0
    - Test output shows "test result: ok. 7 passed; 0 failed" (or all 7 individual `test_validate_transfer_*` tests pass)
    - `grep -c 'pub(crate) fn validate_transfer_inputs' genossi_service_impl/src/membership_adjust.rs` == 1
    - `grep -c 'test_validate_transfer_n_equal_current_shares_valid' genossi_service_impl/src/membership_adjust.rs` == 1
    - `grep -c 'test_validate_transfer_self_invalid' genossi_service_impl/src/membership_adjust.rs` == 1
    - `grep -c '"cannot transfer to self"' genossi_service_impl/src/membership_adjust.rs` >= 1
    - `cargo build --workspace --all-features` exits 0
  </acceptance_criteria>
  <done>Pure-Function liefert deterministische Validation-Ergebnisse ohne DAO-Mock; Plan 17-02 kann sie als ersten Pipeline-Schritt nach Permission-Check + Tx-Begin einbauen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP-Client → REST → Service (transfer_shares) | untrusted shares + from_id + to_id + transfer_date kommen durch dieses Plan-Fundament; tatsächliche Verifikation erfolgt in Plan 17-02 (Pipeline) und Plan 17-03 (REST-Handler) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01-01 | Tampering | validate_transfer_inputs Signatur-Drift zwischen Trait und Impl | mitigate | Trait-Methode und Stub-Impl haben identische Signatur; cargo build erzwingt das. Acceptance-Criteria prüft mit grep auf exakte Signatur. |
| T-17-01-02 | Information Disclosure | Pure-Function leaked from_current_shares im Error-Message ("shares (6) exceeds from.current_shares (5)") | accept | from_current_shares ist bereits via GET /api/members/{id} sichtbar (ADMIN_PRIVILEGE-Funnel später in Plan 17-02 ist ohnehin Voraussetzung). Konsistent mit `validate_partial_repayment_shares`-Pattern (Phase 16). |
| T-17-01-03 | Denial of Service | Pure-Function unbounded loop / Allocation | accept | Kein Loop, fixe O(1)-Allocation für Vec<ValidationFailureItem> mit max 3 Einträgen. Keine Recursion. |
| T-17-01-04 | Spoofing | TRSF-07 Self-Transfer-Bypass via Frontend | mitigate | TRSF-07 → D-17-08: validate_transfer_inputs prüft `from_id == to_id` IM SERVICE; Frontend-Check ist nur UX. Test `test_validate_transfer_self_invalid` zementiert. |
| T-17-01-05 | Elevation of Privilege | TRANSFER_PROCESS-Konstante manipuliert | accept | const &'static str; Rust-Compile-time. Audit-Trail-Manipulation würde Code-Modification erfordern. |
</threat_model>

<verification>
- `cargo build --workspace --all-features` exits 0
- `cargo test -p genossi_service_impl --lib membership_adjust::tests::test_validate_transfer` exits 0 mit 7 PASS
- `cargo clippy -p genossi_service_impl -p genossi_service --all-targets 2>&1 | grep -E "(warning|error):" | wc -l` produces no NEW warnings beyond the pre-existing baseline
- Acceptance-Criteria pro Task sind grep-verifiert
</verification>

<success_criteria>
- MembershipAdjustService-Trait hat `transfer_shares` mit exakter D-17/CONTEXT-Signatur
- TRANSFER_PROCESS-Konstante deklariert
- validate_transfer_inputs Pure-Function mit 7 grünen Unit-Tests
- Stub-Impl erlaubt `cargo build --workspace` grün
- Keine Trait-Drift mehr möglich, wenn Plan 17-02 die Pipeline füllt
</success_criteria>

<output>
After completion, create `.planning/phases/17-service-rest-uebertrag-cascade/17-01-SUMMARY.md` mit:
- Welche Imports/Use-Statements ergänzt wurden (für Plan 17-02 Referenz)
- Exakte Datei-Linien-Range der eingefügten Trait-Methode (für Plan 17-02 grep)
- Exakte Datei-Linien-Range der Pure-Function (für Plan 17-02 use-statement)
- Bestätigung, dass der Stub-Impl-Body `unimplemented!("Plan 17-02 implements the 15-step pipeline")` enthält
</output>
