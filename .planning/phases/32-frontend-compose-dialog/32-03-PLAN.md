---
phase: 32-frontend-compose-dialog
plan: 03
type: execute
wave: 2
depends_on: [32-02]
files_modified:
  - genossi-frontend/src/component/mail_compose/template_selector.rs
  - genossi-frontend/src/page/application_compose.rs
  - genossi-frontend/src/page/mod.rs
  - genossi-frontend/src/router.rs
autonomous: true
requirements: [APMAIL-04, APUI-01, APUI-02]
must_haves:
  truths:
    - "Es gibt eine dedizierte Compose-Route/Vollseite (kein Modal-in-Modal), admin-gated via RequirePrivilege(PRIVILEGE_ADMIN)"
    - "Die Seite komponiert die bestehenden mail_compose/*-Bausteine (kein geforktes UI)"
    - "Die Seite oeffnet mit der Antragsteller-Vorlage 'Zahlungserinnerung' vorbefuellt; der TemplateSelector zeigt nur Antragsteller-Vorlagen"
    - "Live-Vorschau ruft debounced preview_application_mail und zeigt aufgeloeste Platzhalter; der Senden-Klick ist confirm-before-send"
    - "Der Senden-Button ist waehrend des Requests deaktiviert; kein form-onsubmit (div+onclick+r#type:button)"
    - "Prominente 'zuletzt gesendet'-Zeile als Anti-Doppelversand-Guard; nach Erfolg zurueck zur Detailansicht + Toast"
  artifacts:
    - "genossi-frontend/src/page/application_compose.rs (NEU): ApplicationCompose-Page"
    - "genossi-frontend/src/router.rs: Route::ApplicationCompose { id: String }"
    - "genossi-frontend/src/component/mail_compose/template_selector.rs: filter_type + initial_template_id Props (additiv)"
  key_links:
    - "ApplicationCompose -> preview_application_mail/send_application_mail/get_application_communications (Plan 02)"
    - "TemplateSelector.filter_type -> filter_templates_by_type (Plan 02) -> MailTemplateTO.template_type"
    - "Erfolg -> show_toast(Key::MailJobCreated) -> nav.push(Route::ApplicationsPage)"
---

<objective>
Die dedizierte Application-Mail-Compose-Vollseite (D-01), 1:1 nach dem `mail_page.rs`-Vorbild
zusammengesetzt aus den bestehenden `mail_compose/*`-Bausteinen (APUI-02), mit Antragsteller-gefiltertem
und vorbefuelltem TemplateSelector (D-03), debouncte Live-Vorschau ueber den Backend-Render-Kernel (D-04),
und der bewusste "Vorschau -> Senden"-Bestaetigungsschritt ohne Zusatz-Modal (D-05).

Purpose: `component/application_detail.rs` ist selbst ein `Modal` mit verschachtelten Confirm/Reject-Modals
-> Modal-in-Modal ist ein Anti-Pattern (D-01). Daher eine eigene Route/Seite (spiegelt `MailPage`), die den
Anti-Doppelversand-Guard prominent zeigt und den confirm-before-send ohne separaten Dialog erfuellt.
Output: neue Page `application_compose.rs` + Route + additive TemplateSelector-Props.
</objective>

<execution_context>
@/home/neosam/programming/projects/genossi/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/projects/genossi/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-frontend-compose-dialog/32-CONTEXT.md
@.planning/phases/32-frontend-compose-dialog/32-RESEARCH.md
@.planning/phases/32-frontend-compose-dialog/32-PATTERNS.md
@.planning/phases/32-frontend-compose-dialog/32-UI-SPEC.md
@.planning/phases/32-frontend-compose-dialog/32-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TemplateSelector additiv um filter_type + initial_template_id erweitern (Component-First, backward-kompatibel)</name>
  <files>genossi-frontend/src/component/mail_compose/template_selector.rs</files>
  <read_first>
    - genossi-frontend/src/component/mail_compose/template_selector.rs (aktuelle Signatur: on_select ~17, on_select_id ~18 (props default); interne templates-Signal + list_mail_templates ~21-27; select-Optionen ~40-50)
    - genossi-frontend/src/api.rs (filter_templates_by_type + MailTemplateTO.template_type aus Plan 02)
    - genossi-frontend/src/page/mail_page.rs (bestehende TemplateSelector-Nutzung ~396-419 — darf NICHT brechen)
    - .planning/phases/32-frontend-compose-dialog/32-RESEARCH.md (Pattern 3, Pitfall 4)
  </read_first>
  <action>
    Ergaenze `TemplateSelector` additiv um zwei `#[props(default)]`-Props: `filter_type: Option<String>` und
    `initial_template_id: Option<String>`. Wenn `filter_type` gesetzt ist, wende `api::filter_templates_by_type`
    auf die intern geladenen Templates an, bevor die `<option>`s gerendert werden (bei `None` unveraendertes
    Verhalten -> bestehende MailPage-Nutzung bleibt identisch). Wenn `initial_template_id` gesetzt ist, setze es
    als initial ausgewaehlten Wert des `<select>` (der `selected`-State), damit die vorbefuellte Vorlage im
    Dropdown reflektiert wird. Beide Props sind additiv und default-frei fuer bestehende Aufrufer. KEIN
    Umstyling, KEIN Fork.
  </action>
  <acceptance_criteria>
    - template_selector.rs enthaelt `filter_type` und `initial_template_id` als `#[props(default)]`.
    - Bei gesetztem filter_type werden die Optionen ueber `filter_templates_by_type` gefiltert.
    - Bestehende MailPage-Nutzung kompiliert unveraendert (keine neuen Pflichtprops).
    - `nix develop --command cargo build -p genossi-frontend` kompiliert.
  </acceptance_criteria>
  <verify>
    <automated>nix develop --command cargo build -p genossi-frontend</automated>
  </verify>
  <done>TemplateSelector hat additive filter_type/initial_template_id-Props; MailPage-Nutzung unveraendert; Frontend kompiliert.</done>
</task>

<task type="auto">
  <name>Task 2: Neue Compose-Vollseite application_compose.rs (ApplicationCompose) + page/mod.rs Re-Export</name>
  <files>genossi-frontend/src/page/application_compose.rs, genossi-frontend/src/page/mod.rs</files>
  <read_first>
    - genossi-frontend/src/page/mail_page.rs (Blueprint: Imports ~1-31; Signal-State subject/body/body_html/sending/selected_template_id ~58-77; RequirePrivilege+AccessDeniedPage ~198-200; TemplateSelector-Assemblage ~392-419; WysiwygEditor+plain_to_html ~442; TemplatePreview ~458; Debounce TimeoutFuture ~282; Send-Button div/button+onclick disabled + ErrorAlert + show_toast ~512-633)
    - genossi-frontend/src/api.rs (send_application_mail/preview_application_mail/get_application_communications/list_mail_templates/get_application; last_outbound_summary aus Plan 02)
    - genossi-frontend/src/page/mod.rs (Modul-Deklarationen + pub use, Vorbild `mail_page`/`MailPage` ~13/38)
    - genossi-frontend/src/page/repayment_phases.rs (form-onsubmit-Reload-Vermeidung: div+onclick+r#type:"button")
    - genossi-frontend/src/component/toast.rs (show_toast), genossi-frontend/src/component/error_alert.rs (ErrorAlert)
    - .planning/phases/32-frontend-compose-dialog/32-UI-SPEC.md (Layout-Geruest, Spacing/Typografie/Color-Tokens, UI-State-Coverage)
    - .planning/phases/32-frontend-compose-dialog/32-RESEARCH.md (System-Datenfluss Compose-Seite; A2/A3-Discretion)
  </read_first>
  <action>
    Erstelle `genossi-frontend/src/page/application_compose.rs` mit `#[component] pub fn ApplicationCompose(id:
    String) -> Element`. Struktur exakt nach dem UI-SPEC-Layout-Geruest: `RequirePrivilege { privilege:
    PRIVILEGE_ADMIN, fallback: AccessDeniedPage }` -> `div.flex.flex-col.min-h-screen` -> `TopBar` ->
    `div.flex-1.container.mx-auto.px-4.py-8` -> `h1.text-3xl.font-bold.mb-6` -> Banner -> Compose-Card
    `div.bg-white.rounded-lg.shadow.p-6.mb-6`. Signal-State analog `mail_page.rs`: subject, body, body_html,
    sending, selected_template_id, dazu application + communications + preview_result. Beim Mount (spawn/use_effect):
    `get_application(id)`, `get_application_communications(id)` und `list_mail_templates`; die auf
    template_type="application" gefilterte Default-Vorlage (Seed …0003) waehlen und subject/body/body_html +
    selected_template_id vorbefuellen (D-03). Assembliere `MailSubjectInput`, `TemplateSelector { filter_type:
    Some("application"), initial_template_id: Some(default_id), on_select, on_select_id }`, `WysiwygEditor` mit
    `plain_to_html`, `TemplatePreview`. Live-Vorschau (D-04): bei Editor/Subject-Aenderung einen
    `gloo_timers::future::TimeoutFuture`-Debounce (~300-500ms, Discretion) starten, dann `preview_application_mail`
    rufen und das Ergebnis in TemplatePreview zeigen; waehrend Pending die letzte aufgeloeste Vorschau stehen
    lassen (kein Flackern). Zeige die prominente "zuletzt gesendet"-Zeile aus `last_outbound_summary(&communications)`
    (Key::LastSentSummary mit Betreff+Status+Datum; leer -> Key::NeverSent) als Anti-Doppelversand-Guard ueber dem
    Senden-Button. Der Senden-Trigger ist ein reines `button`/`div` mit `onclick` + `r#type: "button"` (KEIN
    `form onsubmit` — Reload-Falle, Vorbild repayment_phases.rs), `disabled: *sending.read() ||
    subject.read().is_empty()` (kein Doppelversand, D-05). onclick: `sending.set(true)`, `send_application_mail(id,
    subject, body, body_html, template_id)`; bei Fehler `ErrorAlert` zeigen (nie stilles 200); bei Erfolg
    `show_toast`(Key::MailJobCreated) + `nav.push(Route::ApplicationsPage {})`. Registriere das Modul + Re-Export in
    page/mod.rs (`pub mod application_compose;` + `pub use application_compose::ApplicationCompose;`). Halte alle
    Spacing/Typo/Color-Werte an die UI-SPEC-Tokens (blauer Akzent nur fuer den Senden-Button + Fokus-Ringe).
  </action>
  <acceptance_criteria>
    - genossi-frontend/src/page/application_compose.rs existiert mit `pub fn ApplicationCompose` und ist von RequirePrivilege(PRIVILEGE_ADMIN) umschlossen.
    - Die Seite nutzt MailSubjectInput, TemplateSelector (filter_type=Some application), WysiwygEditor+plain_to_html, TemplatePreview (kein geforktes UI).
    - Der Senden-Trigger nutzt kein `form onsubmit`; er hat `r#type: "button"` und ein `disabled`, das `sending` und leeren Subject beruecksichtigt.
    - Preview laeuft ueber `preview_application_mail` (debounced), nicht ueber die member-scoped preview_mail.
    - page/mod.rs re-exportiert `ApplicationCompose`.
    - `nix develop --command cargo build -p genossi-frontend` kompiliert.
  </acceptance_criteria>
  <verify>
    <automated>nix develop --command cargo build -p genossi-frontend</automated>
  </verify>
  <done>Compose-Vollseite kompiliert, komponiert die mail_compose-Bausteine, filtert/preselektiert Antragsteller-Vorlagen, debounced Preview, disabled-during-send ohne form-onsubmit, Erfolg -> Toast + Rueckkehr.</done>
</task>

<task type="auto">
  <name>Task 3: Route::ApplicationCompose in router.rs registrieren</name>
  <files>genossi-frontend/src/router.rs</files>
  <read_first>
    - genossi-frontend/src/router.rs (Route-Enum + Reihenfolge; `MailPage {}` `#[route("/mail")]` ~62-63; `pub use crate::page::MailPage;` ~16; spezifische-vor-generischen-Regel, z.B. /mail/jobs vor /mail/jobs/:id ~67-70)
    - genossi-frontend/src/page/mod.rs (nach Task 2: ApplicationCompose re-exportiert)
    - .planning/phases/32-frontend-compose-dialog/32-PATTERNS.md (router.rs-Abschnitt)
  </read_first>
  <action>
    Registriere in router.rs eine neue Route-Variante `#[route("/applications/:id/compose")] ApplicationCompose
    { id: String }` und ergaenze `pub use crate::page::ApplicationCompose;` (Muster MailPage). Platziere die Route
    so, dass die spezifische `/applications/:id/compose`-Route nicht mit der generischen `/applications`-Route
    kollidiert (Reihenfolge-Regel beachten). Der Variantenname MUSS mit der Page-Komponente `ApplicationCompose`
    (Task 2) uebereinstimmen (Dioxus-Router bindet Variante an gleichnamige Komponente).
  </action>
  <acceptance_criteria>
    - router.rs enthaelt `ApplicationCompose { id: String }` mit `#[route("/applications/:id/compose")]` und den passenden `pub use`.
    - `nix develop --command cargo build -p genossi-frontend` kompiliert (Router-Makro findet die Komponente).
  </acceptance_criteria>
  <verify>
    <automated>nix develop --command cargo build -p genossi-frontend</automated>
  </verify>
  <done>Route::ApplicationCompose { id } registriert und an die Compose-Page gebunden; Frontend kompiliert.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 32-03)

- `genossi-frontend/src/page/application_compose.rs` (NEU) — `ApplicationCompose`-Page-Komponente
- `Route::ApplicationCompose { id: String }` (genossi-frontend/src/router.rs) + `pub use ... ApplicationCompose`
- `pub mod application_compose;` + Re-Export in genossi-frontend/src/page/mod.rs
- Additive TemplateSelector-Props `filter_type`, `initial_template_id` (genossi-frontend/src/component/mail_compose/template_selector.rs)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Compose-Route | Unauthentifizierter/nicht-admin Zugriff auf die Compose-Seite muss abgewiesen werden |
| Frontend -> API | send/preview-Aufrufe gegen admin-gated Endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-05 | Spoofing / Access Control | ApplicationCompose-Route | high | mitigate | Seite in `RequirePrivilege(PRIVILEGE_ADMIN)` gewickelt (Muster mail_page.rs:198), Fallback AccessDeniedPage; Backend zusaetzlich admin-gated (Ph.31 D-10) |
| T-32-02 | Elevation / Misuse (Doppelversand/Spam) | Senden-Button | high | mitigate | `disabled: *sending.read() || subject.is_empty()` waehrend Request; prominente "zuletzt gesendet"-Guard-Zeile direkt ueber dem Button (D-05/D-06) |
| T-32-04 | Repudiation (stiller Fehlversand) | Send-Fehlerpfad | medium | mitigate | Fehler via ErrorAlert sichtbar (nie stilles 200); confirm-before-send = sichtbare aufgeloeste Vorschau + expliziter Klick (APMAIL-04) |
| T-32-SC | Tampering (Supply Chain) | Paket-Installs | low | accept | Keine neuen Pakete (nur bestehende mail_compose/gloo/dioxus) |
</threat_model>

<verification>
- `nix develop --command cargo build -p genossi-frontend` kompiliert (Page + Route + TemplateSelector-Props).
- `nix develop --command cargo test -p genossi-frontend` gruen (Plan-02-Helfer weiterhin gruen).
- Manuell (WASM, deferred UAT via `dx serve`): Compose-Seite oeffnet vorbefuellt, Preview aktualisiert debounced, Senden deaktiviert waehrend Request, Erfolg -> Rueckkehr + Toast.
</verification>

<success_criteria>
- Dedizierte Compose-Vollseite (kein Modal-in-Modal), admin-gated (APUI-01/D-01).
- Assembliert bestehende mail_compose/*-Bausteine (APUI-02).
- Antragsteller-gefiltert + Zahlungserinnerung vorbefuellt (D-03).
- Debounced Live-Vorschau mit aufgeloesten Platzhaltern; confirm-before-send (APMAIL-04/D-04/D-05).
- disabled-during-send ohne form-onsubmit-Reload (D-05).
</success_criteria>

<test_approach>
Die WASM-UI ist nicht unit-testbar; die zugrundeliegende Logik (Filter, last-sent, Request-Serialisierung) ist in Plan 02 unit-getestet und wird hier konsumiert. Build-Gate (`cargo build -p genossi-frontend`) beweist Typ-/Router-/Prop-Korrektheit. Interaktives WASM-Verhalten (Preview-Debounce, disabled-during-send, Rueckkehr) wird in der Vorstands-Smoke-Session (`dx serve`) vor dem Milestone-Merge verifiziert.
</test_approach>

<output>
Create `.planning/phases/32-frontend-compose-dialog/32-03-SUMMARY.md` when done
</output>
