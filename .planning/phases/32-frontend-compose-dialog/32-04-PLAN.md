---
phase: 32-frontend-compose-dialog
plan: 04
type: execute
wave: 3
depends_on: [32-01, 32-02, 32-03]
files_modified:
  - genossi-frontend/src/util/email.rs
  - genossi-frontend/src/util/mod.rs
  - genossi-frontend/src/page/member_details.rs
  - genossi-frontend/src/component/communication_timeline.rs
  - genossi-frontend/src/component/application_detail.rs
autonomous: true
requirements: [APMAIL-03, APUI-01, APUI-03]
must_haves:
  truths:
    - "Auf der Application-Detailansicht gibt es einen 'E-Mail senden'-Button; bei fehlender application.email ist er deaktiviert + annotiert (nie stiller Fehlversuch)"
    - "Der Button navigiert auf die Compose-Route scoped auf die application_id"
    - "Die Kommunikations-Historie erscheint via unveraenderter prop-getriebener CommunicationTimeline als eigener Abschnitt"
    - "Eine prominente 'zuletzt gesendet: Betreff — Status am Datum'-Zeile steht neben dem Button (sonst NeverSent)"
    - "Ein Klick auf einen Timeline-Eintrag zeigt den echten gespeicherten Body (rendered_html_body/rendered_body), nicht neu-gerendert"
    - "Die bestehende Member-Nutzung von CommunicationTimeline bleibt im Verhalten unveraendert"
  artifacts:
    - "genossi-frontend/src/util/email.rs (NEU): geteiltes is_email_empty + Tests"
    - "genossi-frontend/src/component/communication_timeline.rs: additiver on_entry_click-Prop"
    - "genossi-frontend/src/component/application_detail.rs: Button + last-sent + Timeline-Abschnitt + Body-Detail-Panel"
  key_links:
    - "Button -> nav.push(Route::ApplicationCompose { id }) (Plan 03)"
    - "get_application_communications (Plan 02) -> last_outbound_summary -> Anzeige"
    - "CommunicationTimeline.on_entry_click -> Body-Panel -> entry.rendered_html_body/rendered_body (Plan 01)"
---

<objective>
Die `application_detail.rs`-Erweiterungen (D-02/D-06): der "E-Mail senden"-Button (deaktiviert/annotiert
bei fehlender Adresse, APMAIL-03), die prominente "zuletzt gesendet"-Zeile, der Kommunikations-Historie-
Abschnitt via unveraenderter `CommunicationTimeline` (APUI-03), und das Body-Detail-Panel, das den echten
gespeicherten Body zeigt (D-06). Der Klick-auf-Body wird additiv ueber einen optionalen `on_entry_click`-Prop
in `communication_timeline.rs` ermoeglicht, ohne die Member-Nutzung zu brechen (Landmine 3).

Purpose: `application_detail.rs` ist ein Modal; der Button darf daher NUR navigieren (D-01, kein Modal-in-Modal),
nicht die Compose-Seite einbetten. Das Body-Panel zeigt den **gespeicherten** rendered_body (nie Live-Re-Render,
der vom real Gesendeten abweichen koennte). `is_email_empty` wird als geteiltes, getestetes Utility gehoben
(Component-First, keine Duplikat-Logik).
Output: geteiltes is_email_empty-Util, additiver Timeline-Klick-Prop, erweitertes application_detail.
</objective>

<execution_context>
@/home/neosam/programming/projects/genossi/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/projects/genossi/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-frontend-compose-dialog/32-CONTEXT.md
@.planning/phases/32-frontend-compose-dialog/32-RESEARCH.md
@.planning/phases/32-frontend-compose-dialog/32-PATTERNS.md
@.planning/phases/32-frontend-compose-dialog/32-UI-SPEC.md
@.planning/phases/32-frontend-compose-dialog/32-01-SUMMARY.md
@.planning/phases/32-frontend-compose-dialog/32-02-SUMMARY.md
@.planning/phases/32-frontend-compose-dialog/32-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: is_email_empty in geteiltes util/email.rs heben (+ Tests) und member_details.rs umstellen</name>
  <files>genossi-frontend/src/util/email.rs, genossi-frontend/src/util/mod.rs, genossi-frontend/src/page/member_details.rs</files>
  <read_first>
    - genossi-frontend/src/page/member_details.rs (private `fn is_email_empty` ~41; die 5 bestehenden Unit-Tests ~1584-1610; Aufrufstelle im Button-Block ~403)
    - genossi-frontend/src/util/mod.rs (aktuelle Modul-Deklarationen, z.B. `pub mod format;`)
    - genossi-frontend/src/util/format.rs (Muster fuer reine Util-Funktion + Tests im selben File)
  </read_first>
  <behavior>
    - is_email_empty(None) == true; ("") == true; ("   ") == true; ("a@b.de") == false; ("  a@b.de  ") == false.
  </behavior>
  <action>
    Erstelle `genossi-frontend/src/util/email.rs` mit `pub fn is_email_empty(email: Option<&str>) -> bool`
    (Logik identisch zur bestehenden member_details-Version: None oder nach Trim leer -> true) und verschiebe die
    5 bestehenden Unit-Tests dorthin. Deklariere `pub mod email;` in util/mod.rs. Stelle member_details.rs auf
    `use crate::util::email::is_email_empty;` um, entferne die dortige private Funktion und ihre nun verschobenen
    Tests (keine Duplikat-Logik, Component-First). Das Verhalten des Member-Buttons bleibt identisch.
  </action>
  <acceptance_criteria>
    - genossi-frontend/src/util/email.rs enthaelt `pub fn is_email_empty` + die 5 Tests.
    - util/mod.rs deklariert `pub mod email;`.
    - member_details.rs importiert `crate::util::email::is_email_empty` und definiert die Funktion nicht mehr selbst.
    - `nix develop --command cargo test -p genossi-frontend` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>nix develop --command cargo test -p genossi-frontend</automated>
  </verify>
  <done>Geteiltes is_email_empty in util/email.rs (getestet); member_details nutzt es; keine Duplikat-Definition; Tests gruen.</done>
</task>

<task type="auto">
  <name>Task 2: communication_timeline.rs additiv um on_entry_click erweitern (Landmine 3, backward-kompatibel)</name>
  <files>genossi-frontend/src/component/communication_timeline.rs</files>
  <read_first>
    - genossi-frontend/src/component/communication_timeline.rs (Signatur `CommunicationTimeline(entries: Vec<CommunicationEntryTO>)` ~7-8; render_entry mit hartem `Link { to: Route::MailJobDetail/InboxDetail }` ~44-104)
    - genossi-frontend/src/page/member_details.rs (bestehende Nutzung `CommunicationTimeline { entries: ... }` ~1426 — darf NICHT brechen)
    - .planning/phases/32-frontend-compose-dialog/32-RESEARCH.md (Pitfall 3 Variante a)
    - .planning/phases/32-frontend-compose-dialog/32-PATTERNS.md (communication_timeline-Abschnitt)
  </read_first>
  <action>
    Erweitere `CommunicationTimeline` additiv um `#[props(default)] on_entry_click:
    Option<EventHandler<CommunicationEntryTO>>`. Wenn gesetzt, rendert die Betreff-Zelle statt des harten `Link`
    ein `div`/`span` mit `onclick`, das `on_entry_click.call(entry.clone())` aufruft (gleiche Link-Optik:
    `text-blue-600 hover:underline cursor-pointer`); wenn `None`, bleibt exakt der bestehende `Link`-Pfad
    (Member-Nutzung unveraendert -> APUI-03 "unveraendert" gewahrt). Reiche den Prop von `CommunicationTimeline`
    durch `render_entry` an die Zeile durch (Signatur von `render_entry` entsprechend erweitern). KEIN Umstyling
    des restlichen Tables, KEINE geaenderten Spalten.
  </action>
  <acceptance_criteria>
    - communication_timeline.rs enthaelt `on_entry_click` als `#[props(default)]`.
    - Ohne Handler wird weiterhin der bestehende `Link`-Pfad gerendert (member_details.rs-Nutzung kompiliert ohne Aenderung).
    - Mit Handler ruft der Klick `on_entry_click.call(entry)`.
    - `nix develop --command cargo build -p genossi-frontend` kompiliert.
  </acceptance_criteria>
  <verify>
    <automated>nix develop --command cargo build -p genossi-frontend</automated>
  </verify>
  <done>Additiver on_entry_click-Prop; Member-Pfad (kein Handler) unveraendert; Application-Pfad kann Klick abfangen; kompiliert.</done>
</task>

<task type="auto">
  <name>Task 3: application_detail.rs — Button + last-sent + Timeline-Abschnitt + Body-Detail-Panel</name>
  <files>genossi-frontend/src/component/application_detail.rs</files>
  <read_first>
    - genossi-frontend/src/component/application_detail.rs (Props `application`/`on_close`/`on_changed`/`on_edit` ~16-22; `app_id = application.id` ~31; ist selbst ein `Modal` ~34; Action-Button-Block ~128-160; verschachtelte Confirm/Reject-Modals ~164-227)
    - genossi-frontend/src/page/member_details.rs (Button-Muster ~400-431: disabled+title+italic-Hint; Timeline-Nutzung ~1426; get_member_communications-Aufruf ~237)
    - genossi-frontend/src/api.rs (get_application_communications + last_outbound_summary aus Plan 02)
    - genossi-frontend/src/util/email.rs (is_email_empty aus Task 1)
    - genossi-frontend/src/router.rs (Route::ApplicationCompose aus Plan 03)
    - genossi-frontend/rest-types/src/lib.rs (CommunicationEntryTO.rendered_html_body/rendered_body aus Plan 01)
    - .planning/phases/32-frontend-compose-dialog/32-UI-SPEC.md (Copywriting: LastSentSummary/NeverSent/SentMailBody; long-text-Backstop max-h-96 overflow-auto whitespace-pre-wrap; Spacing/Color-Tokens)
  </read_first>
  <action>
    Erweitere `application_detail.rs` um vier Elemente. (1) BUTTON (D-02, APMAIL-03): einen "✉ E-Mail senden"-
    Button im Action-Bereich, Muster member_details.rs: `let email_empty = is_email_empty(application.email.as_deref());`
    -> `disabled: email_empty`, `title` = Key::NoEmailAddressHint wenn leer, sonst leer; onclick (nur wenn nicht
    leer) `nav.push(Route::ApplicationCompose { id: app_id.to_string() })` (navigator via `use_navigator`); bei
    leerer Adresse zusaetzlich ein italic-grauer Hinweis (Key::NoEmailAddressHint) daneben — nie ein stiller
    Fehlversuch. (2) KOMMUNIKATION laden: die Eintraege beim Mount via spawn/`use_resource`
    `get_application_communications(app_id)` in ein Signal laden (Muster get_member_communications). (3) LAST-SENT +
    TIMELINE-ABSCHNITT (D-06, APUI-03): eine prominente Zeile aus `last_outbound_summary(&communications)` —
    Key::LastSentSummary mit Betreff+Status+Datum, sonst Key::NeverSent (italic-grau) — neben dem Button; darunter
    ein eigener Abschnitt mit `CommunicationTimeline { entries: communications.read().clone(), on_entry_click: ... }`.
    (4) BODY-DETAIL-PANEL (D-06): der on_entry_click-Handler setzt ein `selected_entry`-Signal; wenn gesetzt, wird
    ein Inline-Expand-Panel (KEIN weiteres Modal — application_detail ist bereits Modal, vermeidet Modal-in-Modal)
    mit Titel Key::SentMailBody gerendert, das den **gespeicherten** `entry.rendered_html_body` (bzw. Fallback
    `entry.rendered_body`) in einem begrenzten Scroll-Container `max-h-96 overflow-auto whitespace-pre-wrap` zeigt —
    NIE neu-gerendert; mit Schliessen-Aktion. Halte Spacing/Typo/Color an die UI-SPEC-Tokens (blauer Akzent nur fuer
    den Senden-Button). Die bestehenden Confirm/Reject-Modals bleiben unveraendert.
  </action>
  <acceptance_criteria>
    - application_detail.rs enthaelt einen Senden-Button mit `disabled` an `is_email_empty(application.email...)` und onclick `nav.push(Route::ApplicationCompose { id: ... })`.
    - Bei leerer Adresse wird der Button deaktiviert und ein Hinweis (Key::NoEmailAddressHint) gerendert.
    - Die Kommunikation wird via `get_application_communications` geladen und als `CommunicationTimeline` mit `on_entry_click` gerendert.
    - Eine last-sent-Zeile nutzt `last_outbound_summary` (Key::LastSentSummary bzw. Key::NeverSent).
    - Das Body-Panel zeigt `rendered_html_body`/`rendered_body` in `max-h-96 overflow-auto whitespace-pre-wrap` (kein Re-Render), Titel Key::SentMailBody.
    - `nix develop --command cargo build -p genossi-frontend` kompiliert; `nix develop --command cargo test -p genossi-frontend` gruen.
  </acceptance_criteria>
  <verify>
    <automated>nix develop --command cargo test -p genossi-frontend</automated>
  </verify>
  <done>application_detail hat Button (disabled/annotiert bei fehlender Adresse), last-sent-Zeile, Timeline-Abschnitt und Inline-Body-Panel (echter gespeicherter Body); Member-Timeline unveraendert; kompiliert + Tests gruen.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 32-04)

- `genossi-frontend/src/util/email.rs` (NEU) — geteiltes `pub fn is_email_empty` + Tests; `pub mod email;` in util/mod.rs
- `CommunicationTimeline.on_entry_click: Option<EventHandler<CommunicationEntryTO>>` (additiver Prop, communication_timeline.rs)
- `application_detail.rs`: "✉ E-Mail senden"-Button (nav -> Route::ApplicationCompose), last-sent-Zeile (Key::LastSentSummary/NeverSent), Timeline-Abschnitt, Inline-Body-Detail-Panel (Key::SentMailBody)
- member_details.rs auf geteiltes is_email_empty umgestellt (Duplikat entfernt)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin-UI -> Body-Anzeige | Anzeige des echten gesendeten Bodys (Antragsteller-PII) im Detail-Panel |
| Frontend -> API | Laden der Communications ueber den admin-gated Endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-01 | Information Disclosure | Body-Detail-Panel (rendered_body/PII) | high | mitigate | Body kommt nur aus `get_application_communications` (admin-gated, Ph.31 D-10); recipient/body an application_id gebunden; application_detail wird ohnehin nur im Admin-Kontext gerendert |
| T-32-03 | Tampering/XSS | rendered_html_body im Panel | medium | mitigate | Body ist beim Speichern ammonia-sanitisiert (Ph.23 D-03); Anzeige in begrenztem Scroll-Container; kein Live-Re-Render von User-HTML |
| T-32-04 | Repudiation (stiller Fehlversand) | Senden-Button ohne Adresse | high | mitigate | Button `disabled` + Hinweis (is_email_empty, Key::NoEmailAddressHint); Backend-422 als Backstop (Ph.31 D-12) — nie stiller Fehlversuch (APMAIL-03) |
| T-32-SC | Tampering (Supply Chain) | Paket-Installs | low | accept | Keine neuen Pakete (nur bestehende Komponenten/dioxus) |
</threat_model>

<verification>
- `nix develop --command cargo test -p genossi-frontend` gruen (is_email_empty-Tests wandern mit, bleiben gruen).
- `nix develop --command cargo build -p genossi-frontend` kompiliert (Button/Timeline/Panel + Route-Nutzung + rendered-Felder).
- Member-Timeline-Nutzung (member_details.rs) kompiliert unveraendert (on_entry_click default None).
- Manuell (deferred UAT via `dx serve`): Button disabled ohne Adresse; Klick oeffnet Compose; last-sent zeigt Betreff+Status+Datum; Timeline-Klick zeigt echten Body; lange/HTML-Bodies bleiben im Scroll-Container (long-text-Backstop UI-SPEC).
</verification>

<success_criteria>
- Senden-Button auf application_detail, deaktiviert/annotiert bei fehlender Adresse (APMAIL-03/APUI-01).
- Navigation auf die scoped Compose-Route (D-02).
- Historie via unveraenderter prop-getriebener CommunicationTimeline (APUI-03).
- Prominente "zuletzt gesendet: Betreff — Status am Datum"-Zeile (D-06).
- Klick -> echter gespeicherter Body, kein Re-Render (D-06).
- Member-Nutzung unveraendert.
</success_criteria>

<test_approach>
Reine Logik (is_email_empty verschoben + weiter getestet; last_outbound_summary/Filter aus Plan 02) traegt die Testabdeckung. Component-Verhalten via Build-Gate (Prop-/Route-/Feld-Typkorrektheit). Interaktives WASM-Verhalten (disabled-Button, Navigation, Body-Panel, long-text-Backstop) in der `dx serve`-Vorstands-Smoke-Session vor Milestone-Merge.
</test_approach>

<output>
Create `.planning/phases/32-frontend-compose-dialog/32-04-SUMMARY.md` when done
</output>
