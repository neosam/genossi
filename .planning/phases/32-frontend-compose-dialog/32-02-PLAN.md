---
phase: 32-frontend-compose-dialog
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - genossi-frontend/src/api.rs
  - genossi-frontend/src/i18n/mod.rs
  - genossi-frontend/src/i18n/de.rs
  - genossi-frontend/src/i18n/en.rs
autonomous: true
requirements: [APMAIL-04, APUI-02]
must_haves:
  truths:
    - "Es gibt dedizierte api.rs-Funktionen send_application_mail / preview_application_mail / get_application_communications (nicht member-umgeleitet)"
    - "MailTemplateTO traegt template_type; Antragsteller-Vorlagen sind clientseitig filterbar (reine testbare Funktion)"
    - "Die 'zuletzt gesendet'-Ableitung (Betreff+Status+Datum, sonst NeverSent) ist eine reine testbare Funktion"
    - "Neue i18n-Keys existieren in BEIDEN Locales De + En"
  artifacts:
    - "genossi-frontend/src/api.rs: send_application_mail/preview_application_mail/get_application_communications + lokale Request/Response-Structs"
    - "genossi-frontend/src/api.rs: MailTemplateTO.template_type + filter_templates_by_type + last_outbound_summary (mit Unit-Tests)"
    - "genossi-frontend/src/i18n: Key::LastSentSummary, Key::NeverSent, Key::SentMailBody (De + En)"
  key_links:
    - "Compose-Page (Plan 03) -> api.rs-Fns -> Phase-31-Endpoints (/applications/{id}/mail[/preview], /communications)"
    - "TemplateSelector-Filter (Plan 03) -> filter_templates_by_type -> MailTemplateTO.template_type"
    - "last-sent-Guard (Plan 03/04) -> last_outbound_summary -> Vec<CommunicationEntryTO>"
---

<objective>
Dedizierte Frontend-API-Schicht + testbare reine Hilfsfunktionen + i18n-Keys als Wave-1-Fundament fuer
die Compose-Seite (Plan 32-03) und die application_detail-Erweiterungen (Plan 32-04).

Purpose: Requirement APUI-02 verlangt **dedizierte** api.rs-Funktionen (keine Umleitung der
member-scoped `preview_mail`/`send_bulk_mail`). Landmine 1 (doppelte rest-types-Crate) erzwingt
**lokale** Request/Response-Structs in api.rs (Send/Preview-Typen sind im Frontend nicht verfuegbar).
Landmine 2 (D-03) erfordert `template_type` auf der Frontend-MailTemplateTO + client-seitigen Filter.
Die WASM-untestbare UI wird durch reine, unit-getestete Hilfsfunktionen abgesichert (Projektregel:
jede Aenderung braucht Tests).
Output: 3 api.rs-Funktionen + lokale Structs, MailTemplateTO.template_type, 2 reine Helfer mit Tests,
3 neue i18n-Keys in De+En.
</objective>

<execution_context>
@/home/neosam/programming/projects/genossi/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/projects/genossi/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-frontend-compose-dialog/32-CONTEXT.md
@.planning/phases/32-frontend-compose-dialog/32-RESEARCH.md
@.planning/phases/32-frontend-compose-dialog/32-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dedizierte api.rs-Funktionen + lokale Request/Response-Structs + MailTemplateTO.template_type</name>
  <files>genossi-frontend/src/api.rs</files>
  <read_first>
    - genossi-frontend/src/api.rs (Vorbild `PreviewRequest`/`PreviewResponse` ~1116-1151 und `preview_mail` ~1153-1175; `check_response` ~139; `get_application` ~810; `list_mail_templates`/`MailTemplateTO` ~1494-1522)
    - genossi_rest_types/src/lib.rs (SendApplicationMailRequest ~1172, PreviewApplicationMailRequest ~1185, PreviewApplicationMailResponse ~1196 — Wire-Referenz, im Frontend NICHT importierbar)
    - genossi_rest/src/application.rs (Routen ~636-654: POST /{id}/mail, POST /{id}/mail/preview, GET /{id}/communications)
    - genossi_mail/src/rest_templates.rs (template_type: String ~34 — Backend liefert das Feld bereits)
    - .planning/phases/32-frontend-compose-dialog/32-RESEARCH.md (Code Examples: Signaturen; Pitfall 1/4)
  </read_first>
  <action>
    Ergaenze in api.rs drei **dedizierte** async-Funktionen, jede spiegelt einen Phase-31-Endpoint und nutzt
    die etablierte Konvention `reqwest::Client::new().post(url).json(&req).send().await?` -> `check_response`
    -> `resp.json()` (exakt `preview_mail`): `send_application_mail(config, id, subject, body, body_html,
    template_id)` gegen `POST /api/applications/{id}/mail` (Erfolg = check_response ok, Rueckgabe `()`);
    `preview_application_mail(config, id, subject, body, body_html)` gegen `POST /api/applications/{id}/mail/preview`,
    Rueckgabe eine lokale `ApplicationPreviewResponse { subject, body, body_html: Option<String> }`;
    `get_application_communications(config, id) -> Vec<rest_types::CommunicationEntryTO>` gegen
    `GET /api/applications/{id}/communications`. Definiere die Request-Typen als **lokale** private Structs in
    api.rs (`#[derive(serde::Serialize)]`) mit `#[serde(skip_serializing_if = "Option::is_none")]` fuer
    `body_html`/`template_id` — genau wie `PreviewRequest` (Landmine 1: Backend-Request-Typen sind im Frontend
    nicht verfuegbar). KEINE Umleitung der member-scoped `preview_mail`/`send_bulk_mail`. Erweitere die
    Frontend-`MailTemplateTO` (api.rs ~1494) additiv um `#[serde(default)] pub template_type: String` (Backend
    liefert es; `default` haelt Backward-Compat mit Legacy-Responses). Ergaenze einen Serialisierungs-Test, der
    beweist, dass eine Send-Request-Serialisierung mit `body_html: None` und `template_id: None` diese Schluessel
    weglaesst (skip_serializing_if greift).
  </action>
  <acceptance_criteria>
    - api.rs enthaelt `pub async fn send_application_mail`, `pub async fn preview_application_mail`, `pub async fn get_application_communications` mit den beschriebenen Signaturen.
    - Die URLs enthalten `/api/applications/`, nicht `/api/mail/preview` (keine Member-Umleitung).
    - api.rs enthaelt lokale Request-Structs mit `skip_serializing_if` fuer optionale Felder; `MailTemplateTO` enthaelt `template_type`.
    - Serialisierungs-Test beweist Weglassen von `body_html`/`template_id` bei None.
    - `nix develop --command cargo test -p genossi-frontend` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>nix develop --command cargo test -p genossi-frontend</automated>
  </verify>
  <done>Drei dedizierte api.rs-Funktionen gegen die Application-Endpoints, lokale Structs, MailTemplateTO.template_type, Serde-Test gruen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reine Hilfsfunktionen filter_templates_by_type + last_outbound_summary (+ Unit-Tests)</name>
  <files>genossi-frontend/src/api.rs</files>
  <read_first>
    - genossi-frontend/src/api.rs (MailTemplateTO nach Task 1; bestehende `#[cfg(test)]`-Testmuster im Crate — Vorbild `is_email_empty`-Tests in member_details.rs; `rest_types::CommunicationEntryTO`)
    - genossi-frontend/rest-types/src/lib.rs (CommunicationEntryTO-Felder: direction, subject, date, outbound_status)
    - .planning/phases/32-frontend-compose-dialog/32-RESEARCH.md (Pattern 3 Filter; Pitfall 6 zuletzt-gesendet-Ableitung)
    - migrations/sqlite/20260820000001_seed_zahlungserinnerung_template.sql (Seed-UUID …0003, template_type='application')
  </read_first>
  <behavior>
    - filter_templates_by_type: Liste aus 'member'- und 'application'-Templates, gefiltert auf "application", enthaelt nur die Application-Vorlagen inkl. Seed …0003; leere Liste -> leer.
    - last_outbound_summary: Vec<CommunicationEntryTO> (outbound, ORDER BY date DESC) -> Some((subject, outbound_status, date)) aus entry[0]; leere Liste -> None (Aufrufer zeigt NeverSent).
  </behavior>
  <action>
    Ergaenze in api.rs zwei **reine** (WASM-unabhaengige) Funktionen: `filter_templates_by_type(all:
    &[MailTemplateTO], template_type: &str) -> Vec<MailTemplateTO>` (case-sensitiver Gleichheitsvergleich auf
    `template_type`) und `last_outbound_summary(entries: &[rest_types::CommunicationEntryTO]) ->
    Option<(String, Option<String>, String)>` (nimmt das erste Element als zuletzt gesendet — Subject, Status,
    Datum-String; `None` bei leerer Liste). Schreibe zuerst die Unit-Tests aus `<behavior>`, dann die
    Implementierung. Diese Helfer kapseln die einzige testbare Logik der Compose/Detail-Anzeige (D-03-Filter +
    D-06-last-sent-Guard) und werden von Plan 03/04 konsumiert.
  </action>
  <acceptance_criteria>
    - api.rs enthaelt `filter_templates_by_type` und `last_outbound_summary` mit den beschriebenen Signaturen.
    - Unit-Tests decken ab: Filter behaelt nur Application-Templates; leere Liste; last_outbound_summary Some (erstes Element) und None (leer).
    - `nix develop --command cargo test -p genossi-frontend` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>nix develop --command cargo test -p genossi-frontend</automated>
  </verify>
  <done>Beide reinen Helfer implementiert und unit-getestet (Filter + last-sent, inkl. Empty-Faelle).</done>
</task>

<task type="auto">
  <name>Task 3: Neue i18n-Keys in beiden Locales (De + En)</name>
  <files>genossi-frontend/src/i18n/mod.rs, genossi-frontend/src/i18n/de.rs, genossi-frontend/src/i18n/en.rs</files>
  <read_first>
    - genossi-frontend/src/i18n/mod.rs (Key-Enum; bestehende Keys MailSend ~248, MailSending ~249, MailJobCreated ~267, MailSendButton ~354, NoEmailAddressHint ~355, CommunicationNone ~392)
    - genossi-frontend/src/i18n/de.rs (De-Uebersetzungen — Reihenfolge/Format der Arms)
    - genossi-frontend/src/i18n/en.rs (En-Uebersetzungen)
    - .planning/phases/32-frontend-compose-dialog/32-UI-SPEC.md (Copywriting Contract: exakte Copy)
    - genossi-frontend/CLAUDE.md (i18n: nur En + De existieren; beide Locales im selben Commit befuellen)
  </read_first>
  <action>
    Fuege dem Key-Enum in i18n/mod.rs drei neue Varianten hinzu: `LastSentSummary` (Label/Praefix zum Bau der
    Zeile "Zuletzt gesendet: {Betreff} — {Status} am {Datum}"), `NeverSent` ("Noch keine E-Mail gesendet"),
    `SentMailBody` ("Gesendete E-Mail"). Ergaenze fuer JEDE der drei Varianten einen Arm in **beiden** Locales
    de.rs und en.rs (De ist primaer; kein Locale::En-Fallback-Bug — beide im selben Commit). De-Copy laut
    UI-SPEC; En sinngemaess ("Last sent", "No email sent yet", "Sent email"). Keine bestehende Key wird
    umbenannt; wiederverwendet werden MailSendButton, NoEmailAddressHint, MailSend, MailSending,
    MailJobCreated, CommunicationNone.
  </action>
  <acceptance_criteria>
    - i18n/mod.rs Key-Enum enthaelt `LastSentSummary`, `NeverSent`, `SentMailBody`.
    - de.rs UND en.rs haben je einen Arm fuer alle drei neuen Keys (kein Non-Exhaustive-Match-Fehler).
    - `nix develop --command cargo build -p genossi-frontend` kompiliert (vollstaendige Match-Abdeckung beweist Locale-Paritaet).
  </acceptance_criteria>
  <verify>
    <automated>nix develop --command cargo build -p genossi-frontend</automated>
  </verify>
  <done>Drei neue i18n-Keys in beiden Locales; Frontend kompiliert (Match-Exhaustiveness beweist De+En-Paritaet).</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 32-02)

- `api::send_application_mail`, `api::preview_application_mail`, `api::get_application_communications` (genossi-frontend/src/api.rs)
- lokale Request-Structs (Send/Preview) + `ApplicationPreviewResponse` (api.rs)
- `MailTemplateTO.template_type: String` (api.rs)
- `api::filter_templates_by_type`, `api::last_outbound_summary` (reine, getestete Helfer)
- `Key::LastSentSummary`, `Key::NeverSent`, `Key::SentMailBody` (i18n/mod.rs + de.rs + en.rs)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend -> API | Neue api.rs-Funktionen rufen ausschliesslich die admin-gated Application-Mail-Endpoints (Phase 31 D-10) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-05 | Spoofing / Access Control | Neue api.rs-Aufrufe | high | mitigate | Alle drei Endpoints sind serverseitig admin-gated (`MANAGE_MEMBERS_PRIVILEGE`, Ph.31 D-10); die Funktionen fuegen keine neue ungated Surface hinzu; UI-Gate folgt in Plan 03/04 (RequirePrivilege) |
| T-32-04 | Repudiation | send_application_mail ohne E-Mail | medium | mitigate | Backend liefert 422 bei fehlender Adresse (Ph.31 D-12); `check_response` propagiert den Fehler statt still zu schlucken |
| T-32-SC | Tampering (Supply Chain) | Paket-Installs | low | accept | Keine neuen Pakete (reqwest/serde/gloo bereits im Workspace) |
</threat_model>

<verification>
- `nix develop --command cargo test -p genossi-frontend` gruen (Serde-Test + Helfer-Tests).
- `nix develop --command cargo build -p genossi-frontend` kompiliert (i18n-Match-Exhaustiveness).
- Grep-Check: die drei Application-URLs enthalten `/api/applications/` (keine Member-Umleitung).
</verification>

<success_criteria>
- Dedizierte, nicht-umgeleitete api.rs-Funktionen fuer Send/Preview/Communications existieren (APUI-02).
- Preview-Funktion ruft den Backend-Render-Kernel (APMAIL-04-Fundament).
- template_type + Filter + last-sent-Ableitung sind vorhanden und getestet.
- Alle neuen i18n-Keys in De + En.
</success_criteria>

<test_approach>
Reine Logik in freie Funktionen gezogen (Filter, last-sent) und unit-getestet; Serde-Verhalten der Request-Structs getestet; i18n-Paritaet via Match-Exhaustiveness beim Build. Alle Kommandos via `nix develop --command`.
</test_approach>

<output>
Create `.planning/phases/32-frontend-compose-dialog/32-02-SUMMARY.md` when done
</output>
