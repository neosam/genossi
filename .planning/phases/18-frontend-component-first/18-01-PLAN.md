---
phase: 18-frontend-component-first
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - genossi-frontend/rest-types/src/lib.rs
autonomous: true
requirements:
  - UI-02
  - UI-04
  - CANC-06
requirements_addressed:
  - UI-02
  - UI-04
  - CANC-06
tags:
  - frontend
  - dto
  - rest-types

must_haves:
  truths:
    - "Frontend kann `MemberSlimTO`, `CancelMembershipRequestTO`, `IncreaseSharesRequestTO`, `MembershipAdjustResponseTO`, `PartialRepaymentRequestTO`, `PartialRepaymentResponseTO`, `TransferSharesRequestTO`, `TransferSharesResponseTO` aus `rest_types` importieren ohne Backend-`utoipa`-Abhaengigkeit (L-2 Mitigation)."
    - "JSON-Roundtrip mit den Backend-Endpoints aus Phase 15-17 funktioniert (Date-Serde-Format `YYYY-MM-DD` matched `iso8601_date_required`)."
    - "Bestehende Frontend-rest-types-Symbole (`MemberTO`, `MemberActionTO`, `SalutationTO` etc.) bleiben unveraendert."
  falsehoods:
    - "Die neuen DTOs leben ausserhalb von `rest-types` (z.B. nur in `api.rs`) — verbietet Wiederverwendung in `membership_adjust_modal.rs`."
    - "Backend-`utoipa::ToSchema`-Derives werden 1:1 kopiert — bricht den Frontend-Build (keine utoipa-Dependency)."
  artifacts:
    - path: "genossi-frontend/rest-types/src/lib.rs"
      provides: "8 neue DTOs + Re-Exports + Tests"
      contains: "pub struct MemberSlimTO"
  key_links:
    - from: "genossi-frontend/rest-types/src/lib.rs"
      to: "genossi-frontend/src/api.rs"
      via: "use rest_types::{...}"
      pattern: "pub struct (MemberSlimTO|CancelMembershipRequestTO|IncreaseSharesRequestTO|MembershipAdjustResponseTO|PartialRepaymentRequestTO|PartialRepaymentResponseTO|TransferSharesRequestTO|TransferSharesResponseTO)"
---

<objective>
Frontend-`rest-types`-Crate um die 8 Phase-15/16/17-DTOs erweitern, die das `MembershipAdjustModal` und die neuen `api.rs`-Funktionen brauchen. Closing-Anker fuer Landmine L-2 (Frontend-rest-types ist eine separate handgepflegte Teilkopie des Workspace-`genossi_rest_types`).

Purpose: Ohne diese DTOs scheitert jede `cargo check`-Iteration in Wave 2 (api.rs braucht die Typen, Modal-Component braucht die Response-Shapes).
Output: 8 neue `pub struct/enum` Definitionen + Unit-Tests fuer JSON-Roundtrip + Re-Exports.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-frontend-component-first/18-CONTEXT.md
@.planning/phases/18-frontend-component-first/18-PATTERNS.md
@.planning/phases/18-frontend-component-first/18-UI-SPEC.md

<interfaces>
<!-- Backend-Quellschema (genossi_rest_types/src/lib.rs:512-609) — Phase-15/16/17 Request/Response-DTOs. -->
<!-- Frontend kopiert OHNE utoipa::ToSchema und OHNE custom `iso8601_date_required`-Serde. -->

Backend canonical (genossi_rest_types/src/lib.rs):
```rust
// Phase 15 D-15-11 / D-15-15 — Cancel + Increase Request/Response
pub struct CancelMembershipRequestTO {
    #[serde(with = "iso8601_date_required")]
    pub willensbekundung_date: time::Date,
}
pub struct IncreaseSharesRequestTO {
    #[serde(with = "iso8601_date_required")]
    pub willensbekundung_date: time::Date,
    pub shares: i32,
}
pub struct MembershipAdjustResponseTO {
    pub action: MemberActionTO,
    pub member: MemberTO,
}

// Phase 16 D-16-16 — Partial-Repayment Request/Response
pub struct PartialRepaymentRequestTO {
    #[serde(with = "iso8601_date_required")]
    pub willensbekundung_date: time::Date,
    pub shares: i32,
}
pub struct PartialRepaymentResponseTO {
    pub entry: RepaymentEntryTO,         // <-- siehe Hinweis unten
    pub member: MemberTO,
    pub phase: Option<RepaymentPhaseTO>, // <-- siehe Hinweis unten
}

// Phase 17 C-17-CF-07 — Transfer Request/Response
pub struct TransferSharesRequestTO {
    pub to_member_id: uuid::Uuid,
    pub shares: i32,
    #[serde(with = "iso8601_date_required")]
    pub transfer_date: time::Date,
}
pub struct TransferSharesResponseTO {
    pub actions: Vec<MemberActionTO>,
    pub from: MemberTO,
    pub to: MemberTO,
}

// Phase 14 D-14-12 — DSGVO-konformer Slim-TO
pub struct MemberSlimTO {
    pub id: uuid::Uuid,
    pub member_number: i64,
    pub salutation: Option<SalutationTO>,
    pub title: Option<String>,
    pub first_name: String,
    pub last_name: String,
}
```

WICHTIG: `RepaymentEntryTO` und `RepaymentPhaseTO` liegen NICHT in `genossi-frontend/rest-types/`, sondern direkt in `genossi-frontend/src/api.rs:2231-2280`. Fuer `PartialRepaymentResponseTO` brauchen wir sie aber in `rest-types`. Loesungs-Variante (gewaehlt unten): Wir **deserialisieren `PartialRepaymentResponseTO.entry`/`.phase` direkt mit den api.rs-Strukturen NICHT** — stattdessen definieren wir `PartialRepaymentResponseTO` mit `serde_json::Value` oder mit re-deklarierten Slim-Schemas. Da Frontend nur 3 Felder von `entry`/`phase` braucht (entry.id + phase.fiscal_year + phase.id), nutzen wir `serde_json::Value` als zero-coupling Pattern — siehe `<action>` unten.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 8 DTOs + Re-Exports in genossi-frontend/rest-types/src/lib.rs hinzufuegen</name>
  <read_first>
    - genossi-frontend/rest-types/src/lib.rs (komplett — verstehe Pattern und welche Imports bereits existieren)
    - .planning/phases/18-frontend-component-first/18-PATTERNS.md (Section 9 "Landmine L-2" — Copy-Snippets)
    - .planning/phases/18-frontend-component-first/18-CONTEXT.md (D-18 Carry-Forward C-18-CF-04..08 — Response-Shapes)
    - genossi_rest_types/src/lib.rs (Zeilen 480-630, um die Backend-Originale zu sehen — NUR zur Verifikation, NICHT 1:1 kopieren)
  </read_first>
  <files>genossi-frontend/rest-types/src/lib.rs</files>
  <action>
Am ENDE der Datei (nach Zeile ~800, vor `#[cfg(test)] mod tests`-Block falls vorhanden, sonst ans File-Ende) folgenden Block einfuegen. NUR Serde + Clone + Debug Derives — KEIN `utoipa::ToSchema`, KEIN `iso8601_date_required`-Custom-Serde (Default `time::Date`-Serde des Features `serde-human-readable` produziert `YYYY-MM-DD` und matched das Backend-Format — verifiziert anhand bestehendem `MemberActionTO.date` Zeile 351, das seit Phase 4 ohne Custom-Serde funktioniert).

```rust
// ─── Phase 18 ─── Membership-Adjust DTOs (Phase 15/16/17 Request/Response-Shapes) ────
// Frontend-Kopie von genossi_rest_types/src/lib.rs:480-630 OHNE ToSchema + OHNE iso8601_date_required.
// time::Date Default-Serde liefert `YYYY-MM-DD` (matched Backend-iso8601_date_required-Format).
// Landmine L-2 Mitigation.

/// Phase 14 D-14-12 — DSGVO-konformer Slim-TO fuer Transfer-Recipients
/// (whitelist 7 Felder, keine Email/PII).
#[derive(Clone, Debug, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct MemberSlimTO {
    pub id: uuid::Uuid,
    pub member_number: i64,
    #[serde(skip_serializing_if = "Option::is_none", default)]
    pub salutation: Option<SalutationTO>,
    #[serde(skip_serializing_if = "Option::is_none", default)]
    pub title: Option<String>,
    pub first_name: String,
    pub last_name: String,
}

/// Phase 15 D-15-11 — Cancel-Membership-Request-Body fuer `POST /api/members/{id}/cancel`.
#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
pub struct CancelMembershipRequestTO {
    pub willensbekundung_date: time::Date,
}

/// Phase 15 D-15-15 — Increase-Shares-Request-Body fuer `POST /api/members/{id}/increase-shares`.
#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
pub struct IncreaseSharesRequestTO {
    pub willensbekundung_date: time::Date,
    pub shares: i32,
}

/// Phase 15 D-15-11 / D-15-15 — gemeinsame Response-Shape fuer Cancel + Increase.
#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
pub struct MembershipAdjustResponseTO {
    pub action: MemberActionTO,
    pub member: MemberTO,
}

/// Phase 16 D-16-16 — Partial-Repayment-Request-Body fuer `POST /api/members/{id}/partial-repayment`.
#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
pub struct PartialRepaymentRequestTO {
    pub willensbekundung_date: time::Date,
    pub shares: i32,
}

/// Phase 16 D-16-16 — Partial-Repayment-Response. `phase` ist `Some(...)` wenn Auto-Anlegen passierte.
/// `entry` und `phase` als `serde_json::Value` (Zero-Coupling) — Frontend braucht nur
/// `entry.id` und `phase.fiscal_year`/`phase.id`. Die vollstaendigen Repayment-TOs leben in
/// `genossi-frontend/src/api.rs:2231-2280` (historisch dort, nicht in rest-types).
#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
pub struct PartialRepaymentResponseTO {
    pub entry: serde_json::Value,
    pub member: MemberTO,
    #[serde(skip_serializing_if = "Option::is_none", default)]
    pub phase: Option<serde_json::Value>,
}

/// Phase 17 C-17-CF-07 — Transfer-Shares-Request-Body fuer `POST /api/members/{from_id}/transfer-shares`.
#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
pub struct TransferSharesRequestTO {
    pub to_member_id: uuid::Uuid,
    pub shares: i32,
    pub transfer_date: time::Date,
}

/// Phase 17 C-17-CF-07 — Transfer-Shares-Response. 2 actions bei Teil-Uebertrag, 3 bei Voll-Uebertrag.
#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
pub struct TransferSharesResponseTO {
    pub actions: Vec<MemberActionTO>,
    pub from: MemberTO,
    pub to: MemberTO,
}
```

Falls die Datei bereits einen `#[cfg(test)] mod tests`-Block hat: Block VOR `mod tests` einfuegen.
  </action>
  <verify>
    <automated>cd genossi-frontend/rest-types &amp;&amp; cargo check 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub struct MemberSlimTO" genossi-frontend/rest-types/src/lib.rs` == 1
    - `grep -c "pub struct CancelMembershipRequestTO" genossi-frontend/rest-types/src/lib.rs` == 1
    - `grep -c "pub struct IncreaseSharesRequestTO" genossi-frontend/rest-types/src/lib.rs` == 1
    - `grep -c "pub struct MembershipAdjustResponseTO" genossi-frontend/rest-types/src/lib.rs` == 1
    - `grep -c "pub struct PartialRepaymentRequestTO" genossi-frontend/rest-types/src/lib.rs` == 1
    - `grep -c "pub struct PartialRepaymentResponseTO" genossi-frontend/rest-types/src/lib.rs` == 1
    - `grep -c "pub struct TransferSharesRequestTO" genossi-frontend/rest-types/src/lib.rs` == 1
    - `grep -c "pub struct TransferSharesResponseTO" genossi-frontend/rest-types/src/lib.rs` == 1
    - `grep -c "utoipa::ToSchema" genossi-frontend/rest-types/src/lib.rs` returns 0 fuer den NEUEN Block (existing usages bleiben — Block-spezifisch: `grep -A 2 "Phase 18" genossi-frontend/rest-types/src/lib.rs | grep -c utoipa` == 0)
    - `cd genossi-frontend/rest-types &amp;&amp; cargo check` exit 0
  </acceptance_criteria>
  <done>Frontend-rest-types-Crate kompiliert mit allen 8 neuen DTOs; bestehende Symbole unveraendert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: JSON-Roundtrip-Tests fuer die 8 neuen DTOs</name>
  <read_first>
    - genossi-frontend/rest-types/src/lib.rs (Test-Block am Ende — Pattern fuer bestehende RoundTrip-Tests)
  </read_first>
  <files>genossi-frontend/rest-types/src/lib.rs</files>
  <behavior>
    - Test 1: `serde_json::to_string(&MemberSlimTO { ... })` -> `serde_json::from_str::<MemberSlimTO>(...)` liefert Original.
    - Test 2: CancelMembershipRequestTO Roundtrip mit `willensbekundung_date: 2026-06-15` -> JSON enthaelt `"willensbekundung_date":"2026-06-15"`.
    - Test 3: IncreaseSharesRequestTO Roundtrip mit `shares: 5` -> JSON enthaelt `"shares":5`.
    - Test 4: TransferSharesRequestTO Roundtrip mit allen 3 Feldern.
    - Test 5: MembershipAdjustResponseTO Roundtrip (action + member nested).
    - Test 6: PartialRepaymentResponseTO mit `phase = Some(serde_json::json!({"id":"...","fiscal_year":2026}))` -> JSON enthaelt `phase`-Schluessel; mit `phase = None` -> kein `phase`-Schluessel im Output (Default-Skip-If-None).
    - Test 7: TransferSharesResponseTO mit 2 actions Roundtrip.
    - Test 8: MemberSlimTO mit `salutation: None, title: None` -> JSON ohne diese Schluessel (Skip-If-None verifiziert).
  </behavior>
  <action>
Am Ende von `#[cfg(test)] mod tests` (oder als neuer `mod tests`-Block, falls keiner existiert) folgende 8 Unit-Tests anhaengen. Pattern uebernommen aus den bestehenden Roundtrip-Tests Zeile ~2640 in `genossi_rest_types` (gleicher Stil).

```rust
#[cfg(test)]
mod phase_18_dtos_tests {
    use super::*;

    fn sample_member() -> MemberTO {
        MemberTO {
            id: Some(uuid::Uuid::from_u128(1)),
            member_number: 42,
            first_name: "Hans".into(),
            last_name: "Mueller".into(),
            salutation: None,
            title: None,
            email: None,
            company: None,
            comment: None,
            street: None,
            house_number: None,
            postal_code: None,
            city: None,
            join_date: time::Date::from_calendar_date(2025, time::Month::January, 1).unwrap(),
            shares_at_joining: 1,
            current_shares: 5,
            current_balance: 0,
            action_count: 0,
            migrated: false,
            exit_date: None,
            bank_account: None,
            status: MemberStatusTO::Normal,
            created: None,
            deleted: None,
            version: None,
        }
    }

    fn sample_action() -> MemberActionTO {
        // Re-use whichever MemberActionTO constructor matches the existing fixture pattern.
        // If a helper does not exist, build with minimal valid fields:
        MemberActionTO {
            id: Some(uuid::Uuid::from_u128(2)),
            member_id: uuid::Uuid::from_u128(1),
            action_type: ActionTypeTO::Austritt,
            date: time::Date::from_calendar_date(2026, time::Month::June, 15).unwrap(),
            effective_date: Some(
                time::Date::from_calendar_date(2026, time::Month::December, 31).unwrap()
            ),
            shares_change: 0,
            transfer_member_id: None,
            note: None,
            created: None,
            deleted: None,
            version: None,
        }
    }

    #[test]
    fn member_slim_to_roundtrip() {
        let slim = MemberSlimTO {
            id: uuid::Uuid::from_u128(1),
            member_number: 42,
            salutation: Some(SalutationTO::Herr),
            title: Some("Dr.".into()),
            first_name: "Hans".into(),
            last_name: "Mueller".into(),
        };
        let json = serde_json::to_string(&slim).unwrap();
        let back: MemberSlimTO = serde_json::from_str(&json).unwrap();
        assert_eq!(slim, back);
    }

    #[test]
    fn member_slim_to_skips_none_fields() {
        let slim = MemberSlimTO {
            id: uuid::Uuid::from_u128(1),
            member_number: 7,
            salutation: None,
            title: None,
            first_name: "Anna".into(),
            last_name: "Weber".into(),
        };
        let json = serde_json::to_string(&slim).unwrap();
        assert!(!json.contains("\"salutation\""), "salutation should be skipped: {json}");
        assert!(!json.contains("\"title\""), "title should be skipped: {json}");
    }

    #[test]
    fn cancel_request_serializes_iso_date() {
        let req = CancelMembershipRequestTO {
            willensbekundung_date: time::Date::from_calendar_date(2026, time::Month::June, 15).unwrap(),
        };
        let json = serde_json::to_string(&req).unwrap();
        assert!(json.contains("\"willensbekundung_date\":\"2026-06-15\""), "got: {json}");
    }

    #[test]
    fn increase_request_roundtrip_with_shares() {
        let req = IncreaseSharesRequestTO {
            willensbekundung_date: time::Date::from_calendar_date(2026, time::Month::March, 1).unwrap(),
            shares: 5,
        };
        let json = serde_json::to_string(&req).unwrap();
        assert!(json.contains("\"shares\":5"));
        let back: IncreaseSharesRequestTO = serde_json::from_str(&json).unwrap();
        assert_eq!(back.shares, 5);
    }

    #[test]
    fn transfer_request_full_roundtrip() {
        let req = TransferSharesRequestTO {
            to_member_id: uuid::Uuid::from_u128(2),
            shares: 3,
            transfer_date: time::Date::from_calendar_date(2026, time::Month::July, 1).unwrap(),
        };
        let json = serde_json::to_string(&req).unwrap();
        let back: TransferSharesRequestTO = serde_json::from_str(&json).unwrap();
        assert_eq!(back.shares, 3);
        assert_eq!(back.to_member_id, uuid::Uuid::from_u128(2));
        assert!(json.contains("\"transfer_date\":\"2026-07-01\""));
    }

    #[test]
    fn membership_adjust_response_roundtrip_nested() {
        let resp = MembershipAdjustResponseTO {
            action: sample_action(),
            member: sample_member(),
        };
        let json = serde_json::to_string(&resp).unwrap();
        let back: MembershipAdjustResponseTO = serde_json::from_str(&json).unwrap();
        assert_eq!(back.member.member_number, 42);
        assert_eq!(back.action.action_type, ActionTypeTO::Austritt);
    }

    #[test]
    fn partial_repayment_response_phase_some_serializes_phase() {
        let resp = PartialRepaymentResponseTO {
            entry: serde_json::json!({"id":"00000000-0000-0000-0000-000000000003","fiscal_year":2026}),
            member: sample_member(),
            phase: Some(serde_json::json!({"id":"00000000-0000-0000-0000-000000000004","fiscal_year":2026})),
        };
        let json = serde_json::to_string(&resp).unwrap();
        assert!(json.contains("\"phase\""), "phase should be present when Some: {json}");
    }

    #[test]
    fn partial_repayment_response_phase_none_skips_phase() {
        let resp = PartialRepaymentResponseTO {
            entry: serde_json::json!({"id":"00000000-0000-0000-0000-000000000003"}),
            member: sample_member(),
            phase: None,
        };
        let json = serde_json::to_string(&resp).unwrap();
        assert!(!json.contains("\"phase\""), "phase should be skipped when None: {json}");
    }

    #[test]
    fn transfer_response_two_actions_roundtrip() {
        let resp = TransferSharesResponseTO {
            actions: vec![sample_action(), sample_action()],
            from: sample_member(),
            to: sample_member(),
        };
        let json = serde_json::to_string(&resp).unwrap();
        let back: TransferSharesResponseTO = serde_json::from_str(&json).unwrap();
        assert_eq!(back.actions.len(), 2);
    }
}
```

ANMERKUNG: Falls `MemberActionTO` ein anderes Feld-Set hat (z.B. kein `effective_date`-Field), passt der Executor das Fixture `sample_action()` per `grep -n "pub struct MemberActionTO" -A 30 genossi-frontend/rest-types/src/lib.rs` an die existierende Definition an. Erforderlich ist nur dass das Fixture compileable ist; die Tests verifizieren das *Schema* der neuen DTOs, nicht das von MemberActionTO.
  </action>
  <verify>
    <automated>cd genossi-frontend/rest-types &amp;&amp; cargo test --lib phase_18_dtos_tests 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cd genossi-frontend/rest-types &amp;&amp; cargo test --lib phase_18_dtos_tests` zeigt `9 passed; 0 failed` (8 Tests + ggf. Helper-Test counts variieren)
    - Mindestens 8 distinct test names: member_slim_to_roundtrip, member_slim_to_skips_none_fields, cancel_request_serializes_iso_date, increase_request_roundtrip_with_shares, transfer_request_full_roundtrip, membership_adjust_response_roundtrip_nested, partial_repayment_response_phase_some_serializes_phase, partial_repayment_response_phase_none_skips_phase, transfer_response_two_actions_roundtrip
    - `grep -c "fn member_slim_to_roundtrip" genossi-frontend/rest-types/src/lib.rs` == 1
  </acceptance_criteria>
  <done>Alle 9 Roundtrip-Tests gruen; ISO-8601-Date-Format `YYYY-MM-DD` verifiziert; Skip-If-None Semantik verifiziert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend↔Backend HTTP | JSON-Body von Phase 18 Modal (CancelRequest, IncreaseRequest, PartialRequest, TransferRequest) wird vom Backend Vorstand-only validiert (Phase 15/16/17 ADMIN_PRIVILEGE-Funnel + Server-Date-Bounds-Check) |
| WASM↔Browser | Datepicker-Inputs werden vom Browser validiert; Frontend-Validation ist Defense-in-Depth, Backend ist Single-Source-of-Truth |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01-01 | Tampering | DTO-Schema-Mismatch (Frontend↔Backend) | low | mitigate | Roundtrip-Tests in Task 2 verifizieren JSON-Format; iso8601_date_required-Default-Serde via `time::Date` matched Backend-Format. CI bricht ab wenn Schema drift. |
| T-18-01-02 | Information Disclosure | MemberSlimTO leakt PII | low | accept | Backend-DAO (Phase 14 D-14-12) hat 7-Feld-Whitelist auf SELECT-Ebene; Frontend-DTO ist nur passive Empfaengerseite. |
| T-18-01-03 | Spoofing | Manipulierte JSON-Bodies vom Frontend | low | accept | Backend-ADMIN_PRIVILEGE-Funnel + Server-Validation (Phase 15 D-15-05..08) prueft alle Felder erneut; Frontend-DTO ist nur Convenience. |

Keine `high`-Severity-Threats — die Pure-DTO-Erweiterung hat keine Code-Execution-Pfade.
</threat_model>

<verification>
- Frontend rest-types Crate kompiliert mit `cargo check`
- Alle 9 Roundtrip-Tests gruen
- Keine bestehenden Tests rot
- Bestehende DTOs (MemberTO, MemberActionTO, SalutationTO etc.) unveraendert
</verification>

<success_criteria>
- 8 neue DTOs in genossi-frontend/rest-types/src/lib.rs
- 9 Roundtrip-Tests gruen
- iso8601-Date-Format (YYYY-MM-DD) verifiziert
- Wave 2 (api.rs + Modal) ist entblockt
</success_criteria>

<output>
After completion, create `.planning/phases/18-frontend-component-first/18-01-SUMMARY.md` with:
- Liste der 8 hinzugefuegten DTOs
- Test-Output von `cargo test --lib phase_18_dtos_tests`
- Hinweis: `RepaymentEntryTO`/`RepaymentPhaseTO` werden NICHT nach rest-types kopiert (existieren in api.rs); `PartialRepaymentResponseTO` nutzt `serde_json::Value` als Zero-Coupling-Pattern fuer entry/phase
- Bestaetigung: Wave 2 (api.rs in Plan 05) kann jetzt importieren
</output>
