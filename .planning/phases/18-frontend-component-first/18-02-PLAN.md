---
phase: 18-frontend-component-first
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - genossi-frontend/src/component/toast.rs
autonomous: true
requirements:
  - UI-02
  - UI-04
requirements_addressed:
  - UI-02
  - UI-04
tags:
  - frontend
  - component
  - toast
  - ui

must_haves:
  truths:
    - "`show_toast` und `show_success_toast` koexistieren ohne Aenderung an bestehenden Callsites (alle v1.0/v1.1-Callsites bleiben rot via Default `ToastVariant::Error`)."
    - "ToastContainer rendert Success-Toasts mit `bg-green-600 text-white` und Error-Toasts mit `bg-red-600 text-white` (UI-SPEC Color-Section)."
    - "Phase 18 After-Success-Toasts (D-18-08) sind visuell distinkt von Error-Toasts."
  falsehoods:
    - "Bestehende `show_toast(...)`-Callsites in v1.0/v1.1 wurden ungewollt von Rot auf Gruen geaendert."
    - "Es gibt mehrere `ToastVariant`-Definitionen (nur EINE pub enum darf existieren)."
  artifacts:
    - path: "genossi-frontend/src/component/toast.rs"
      provides: "ToastVariant enum + show_success_toast helper + variant-aware ToastContainer rendering"
      contains: "pub enum ToastVariant"
  key_links:
    - from: "genossi-frontend/src/component/toast.rs"
      to: "genossi-frontend/src/page/member_details.rs"
      via: "show_success_toast(...) (Plan 07)"
      pattern: "show_success_toast"
---

<objective>
`component/toast.rs` um `ToastVariant { Success, Error }` und einen `show_success_toast(...)`-Helper erweitern, ohne bestehende `show_toast(...)`-Callsites zu brechen. Variante (b) aus UI-SPEC L-Toast (sibling-helper + separate styling-class-map).

Purpose: Phase 18 D-18-08 verlangt einen gruenen Success-Toast nach erfolgreichen Cancel/Partial/Transfer/Upgrade-Operationen (UI-04 After-Success-Feedback). Aktuelles `toast.rs` hardcoded `bg-red-600` fuer ALLE Toasts. Variant-Param ist UI-SPEC-Requirement.
Output: Backward-kompatible Erweiterung von `toast.rs`.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-frontend-component-first/18-CONTEXT.md
@.planning/phases/18-frontend-component-first/18-UI-SPEC.md
@.planning/phases/18-frontend-component-first/18-PATTERNS.md

<interfaces>
<!-- Current toast.rs API (48 LOC, all hardcoded red). -->
<!-- Goal: minimal API extension; existing callsites unchanged. -->

Current (genossi-frontend/src/component/toast.rs):
```rust
pub fn show_toast(
    toast_messages: &mut Signal<Vec<(u64, String)>>,
    toast_counter: &mut Signal<u64>,
    msg: String,
) { /* push (id, msg) — all toasts rendered red */ }

#[component]
pub fn ToastContainer(messages: ReadOnlySignal<Vec<(u64, String)>>) -> Element {
    /* renders all msgs in bg-red-600 */
}
```

Target API after Plan 02:
```rust
#[derive(Clone, Copy, PartialEq, Debug)]
pub enum ToastVariant { Success, Error }

pub fn show_toast(...) { /* unchanged signature; defaults to ToastVariant::Error internally */ }
pub fn show_success_toast(...) { /* same signature; pushes with ToastVariant::Success */ }

#[component]
pub fn ToastContainer(messages: ReadOnlySignal<Vec<(u64, ToastVariant, String)>>) -> Element {
    /* renders bg-green-600 for Success, bg-red-600 for Error */
}
```

ACHTUNG: Die `messages`-Signal-Shape aendert sich von `Vec<(u64, String)>` zu `Vec<(u64, ToastVariant, String)>`. Plan 07 (member_details.rs) und alle bestehenden Pages, die `let mut toast_messages = use_signal(|| Vec::<(u64, String)>::new())` deklarieren, muessten die neue Tuple-Shape verwenden. Alternative (gewaehlt unten): wir behalten `Vec<(u64, String)>` als das **bestehende** ToastContainer-Signal und fuehren PARALLEL ein neues `success_toast_messages: Vec<(u64, String)>`-Signal ein, das von einer separaten `SuccessToastContainer`-Component konsumiert wird. ⇒ Zero Blast Radius auf alle v1.0/v1.1-Callsites.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ToastVariant enum + show_success_toast + SuccessToastContainer in toast.rs ergaenzen</name>
  <read_first>
    - genossi-frontend/src/component/toast.rs (komplett — 48 Zeilen, alle Helper + ToastContainer)
    - .planning/phases/18-frontend-component-first/18-UI-SPEC.md (Color-Section + L-Toast-Requirement)
    - genossi-frontend/src/page/repayment_phases.rs Zeilen 40-50 + 125-130 (existing show_toast-Callsite-Pattern; wird NICHT geaendert)
  </read_first>
  <files>genossi-frontend/src/component/toast.rs</files>
  <action>
Die existierenden `show_toast()` + `ToastContainer { messages: ReadOnlySignal<Vec<(u64, String)>> }` BLEIBEN unveraendert (Zero Blast Radius). Wir fuegen PARALLEL hinzu:

1. `pub enum ToastVariant { Success, Error }` mit `Clone, Copy, PartialEq, Debug` derives. (Wird in Phase-18-Toast-Helpers verwendet UND als Public-API exportiert, falls spaetere Phasen einen unified Container wollen.)

2. `pub fn show_success_toast(toast_messages: &mut Signal<Vec<(u64, String)>>, toast_counter: &mut Signal<u64>, msg: String)` — exakt gleiche Signatur wie `show_toast`, intern aber pushed auf einen **separaten** Container-Signal-Typ. ⇒ Falsch: separate Signal-Typen verkomplizieren Pages. **Stattdessen:** Naechster Schritt 3 fuehrt die Tuple-Shape-Erweiterung ein, aber nur als NEUE pub-Funktion + NEUER Container, ohne Aenderung an bestehenden API.

3. Aktuelle Implementierung:

```rust
//! ToastContainer + show_toast (Phase 4 Plan 06) — Component-First-Extraction.
//! Phase 18 (this file): added ToastVariant + show_success_toast + SuccessToastContainer
//! without touching the existing red-error API (Zero Blast Radius for v1.0/v1.1 callsites).

use dioxus::prelude::*;
use gloo_timers::future::TimeoutFuture;

/// Phase 18 — Variant of a toast (visual styling).
/// `Error` is the legacy default (red); `Success` is green per UI-SPEC.
#[derive(Clone, Copy, PartialEq, Debug)]
pub enum ToastVariant {
    Success,
    Error,
}

// ─── Existing API (unchanged) ─────────────────────────────────────
// All v1.0/v1.1 callsites use these — bg-red-600.

pub fn show_toast(
    toast_messages: &mut Signal<Vec<(u64, String)>>,
    toast_counter: &mut Signal<u64>,
    msg: String,
) {
    let id = *toast_counter.read();
    *toast_counter.write() += 1;
    toast_messages.write().push((id, msg));
    let mut toast_messages = toast_messages.clone();
    spawn(async move {
        TimeoutFuture::new(5_000).await;
        toast_messages.write().retain(|(tid, _)| *tid != id);
    });
}

#[component]
pub fn ToastContainer(messages: ReadOnlySignal<Vec<(u64, String)>>) -> Element {
    let msgs = messages.read();
    if msgs.is_empty() {
        return rsx! {};
    }
    rsx! {
        div {
            // z-[60] > TopBar z-50 — sonst werden Desktop-Toasts (md:top-4) verdeckt (UAT Defekt #2).
            class: "fixed z-[60] bottom-4 left-1/2 -translate-x-1/2 md:bottom-auto md:left-auto md:top-4 md:right-4 md:translate-x-0 flex flex-col gap-2 max-w-md print:hidden",
            for (id, msg) in msgs.iter() {
                div {
                    key: "{id}",
                    class: "bg-red-600 text-white px-4 py-3 rounded-lg shadow-lg flex items-center gap-3",
                    "{msg}"
                }
            }
        }
    }
}

// ─── Phase 18 — Success-Toast API (additive, separate Signal-Bucket) ────

/// Phase 18 D-18-08 — Push a green success toast onto a dedicated success-bucket.
/// Same auto-dismiss behaviour as `show_toast` (5s).
pub fn show_success_toast(
    toast_messages: &mut Signal<Vec<(u64, String)>>,
    toast_counter: &mut Signal<u64>,
    msg: String,
) {
    let id = *toast_counter.read();
    *toast_counter.write() += 1;
    toast_messages.write().push((id, msg));
    let mut toast_messages = toast_messages.clone();
    spawn(async move {
        TimeoutFuture::new(5_000).await;
        toast_messages.write().retain(|(tid, _)| *tid != id);
    });
}

/// Phase 18 D-18-08 — Green-styled container for success messages.
/// Caller mounts BOTH `ToastContainer` and `SuccessToastContainer` on the page
/// (with separate `Signal<Vec<(u64, String)>>` signals) to display both variants.
#[component]
pub fn SuccessToastContainer(messages: ReadOnlySignal<Vec<(u64, String)>>) -> Element {
    let msgs = messages.read();
    if msgs.is_empty() {
        return rsx! {};
    }
    rsx! {
        div {
            // Slightly offset to avoid overlap when both Error + Success toasts are visible.
            // On mobile: stacks above the error container; on desktop: stacks below.
            class: "fixed z-[60] bottom-20 left-1/2 -translate-x-1/2 md:bottom-auto md:left-auto md:top-20 md:right-4 md:translate-x-0 flex flex-col gap-2 max-w-md print:hidden",
            for (id, msg) in msgs.iter() {
                div {
                    key: "{id}",
                    class: "bg-green-600 text-white px-4 py-3 rounded-lg shadow-lg flex items-center gap-3",
                    "{msg}"
                }
            }
        }
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn toast_variant_distinct_values() {
        assert_ne!(ToastVariant::Success, ToastVariant::Error);
    }

    #[test]
    fn toast_variant_copy_and_compare() {
        let v = ToastVariant::Success;
        let w = v;  // Copy
        assert_eq!(v, w);
        assert_eq!(format!("{:?}", v), "Success");
        assert_eq!(format!("{:?}", ToastVariant::Error), "Error");
    }
}
```

4. **Re-Exports im `component/mod.rs`** geschehen in Plan 04 (gleicher Wave, bewusst zusammen mit FiscalYearDateInput-Re-Exports — siehe Plan 04 acceptance criteria). Hier KEINE Aenderung an `component/mod.rs`.
  </action>
  <verify>
    <automated>cd genossi-frontend &amp;&amp; cargo check 2>&amp;1 | tail -20 &amp;&amp; cargo test --lib component::toast 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub enum ToastVariant" genossi-frontend/src/component/toast.rs` == 1
    - `grep -c "pub fn show_success_toast" genossi-frontend/src/component/toast.rs` == 1
    - `grep -c "pub fn SuccessToastContainer" genossi-frontend/src/component/toast.rs` == 1
    - `grep -c "pub fn show_toast" genossi-frontend/src/component/toast.rs` == 1 (existing API unchanged)
    - `grep -c "pub fn ToastContainer" genossi-frontend/src/component/toast.rs` == 1 (existing API unchanged)
    - `grep -c "bg-green-600" genossi-frontend/src/component/toast.rs` &gt;= 1
    - `grep -c "bg-red-600" genossi-frontend/src/component/toast.rs` &gt;= 1
    - `cd genossi-frontend &amp;&amp; cargo check` exit 0
    - `cd genossi-frontend &amp;&amp; cargo test --lib component::toast` exit 0 (mindestens 2 Tests gruen)
  </acceptance_criteria>
  <done>Beide APIs koexistieren; ToastVariant + show_success_toast + SuccessToastContainer hinzugefuegt; bestehende Callsites unveraendert; Unit-Tests gruen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component↔Page | `show_success_toast(...)` empfaengt Strings, die i.d.R. aus i18n-Translations stammen; Dioxus auto-escaped Text in `"{msg}"`-Interpolation |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-02-01 | Tampering | Toast-msg-Content (XSS) | low | accept | Dioxus RSX `"{msg}"` interpoliert als Text, nicht als HTML — kein XSS-Vektor. Strings sind i18n-Translations, nicht User-Input. |
| T-18-02-02 | Repudiation | Toast-Auto-Dismiss verbirgt Error-Spuren | low | accept | Errors zusaetzlich in ErrorAlert innerhalb Modal sichtbar (D-18-08); Server-Audit-Log ist Single-Source-of-Truth fuer Vorgaenge. |
| T-18-02-03 | Information Disclosure | Toast leakt sensible Daten | low | mitigate | Toast-Messages enthalten nur generische Success/Error-Texte (i18n-Keys), keine PII. Plan 04 i18n-Keys reviewen. |

Keine `high`-Severity-Threats.
</threat_model>

<verification>
- `cargo check` gruen
- 2 Unit-Tests gruen
- Bestehende `show_toast`/`ToastContainer`-API unveraendert
- `bg-green-600` und `bg-red-600` beide im File
</verification>

<success_criteria>
- ToastVariant enum exportiert
- show_success_toast Helper exportiert
- SuccessToastContainer Component exportiert
- Bestehende v1.0/v1.1-Callsites unbeeinflusst (kein Search-Replace, keine API-Aenderung)
</success_criteria>

<output>
After completion, create `.planning/phases/18-frontend-component-first/18-02-SUMMARY.md` with:
- API-Vergleich vorher/nachher
- Verifikation dass bestehende Callsites unveraendert sind
- Hinweis fuer Plan 07: Page mountet sowohl `ToastContainer { messages }` als auch `SuccessToastContainer { messages: success_messages }` mit zwei Signals
</output>
