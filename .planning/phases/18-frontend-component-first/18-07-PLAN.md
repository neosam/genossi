---
phase: 18-frontend-component-first
plan: 07
type: execute
wave: 3
depends_on: ["18-01", "18-02", "18-03", "18-04", "18-05", "18-06"]
files_modified:
  - genossi-frontend/src/page/member_details.rs
  - .planning/phases/18-frontend-component-first/18-MANUAL-UAT.md
autonomous: false
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - CANC-06
requirements_addressed:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - CANC-06
tags:
  - frontend
  - page
  - integration
  - uat
must_haves:
  truths:
    - "page/member_details.rs enthaelt einen Admin-only Button Mitgliedschaft anpassen (UI-01) gated via RequirePrivilege mit privilege admin (C-18-CF-11)."
    - "Click auf den Button setzt show_adjust_modal Signal-bool auf true."
    - "Wenn show_adjust_modal true ist wird Modal mit MembershipAdjustModal gemountet."
    - "on_success-Handler schliesst Modal ruft refresh_members.await und zeigt gruenen Success-Toast via show_success_toast (D-18-08)."
    - "Page hat success_toast_messages Signal Vec u64 String success_toast_counter Signal u64 SuccessToastContainer gemountet (L-6 Mitigation)."
    - "today-Variable wird im MemberDetails-Component genau einmal am Top-Scope berechnet (NICHT in jeder use_signal-Closure dupliziert) und an MembershipAdjustModal weitergereicht. Heutige Duplikate in den join_date- und action_date-use_signal-Closures (member_details.rs:82-90 + 138-148) werden bewusst NICHT angefasst, weil sie historisch gewachsen sind und L-7 Cross-File-Refactor-Risiko vermeiden — nur die NEUE Phase-18-Berechnung wird zentralisiert."
    - "ManualUAT-Sektion in .planning/phases/18-frontend-component-first/18-MANUAL-UAT.md mit mindestens 4 Browser-Test-Szenarien (1 pro Operation) + Negative-Pfade + Sign-Off-Checkliste."
  falsehoods:
    - "Button wird in der Mitgliederliste angezeigt (Roadmap explizit nur Member-Detail-Page Audit-Bewusstsein)."
    - "RequirePrivilege nutzt manage_members-Privilege (muss admin sein per C-18-CF-11)."
    - "Modal wird gemountet ohne Modal-Wrapper aus component/modal.rs (verletzt Single-Source-of-Truth fuer Overlay-Styling)."
    - "After-Success ruft show_toast statt show_success_toast (Error-Variant rot statt Success gruen)."
    - "today-Berechnung wird ein DRITTES Mal in member_details.rs dupliziert — Phase 18 MUSS die zentrale today-Variable nutzen."
  artifacts:
    - path: "genossi-frontend/src/page/member_details.rs"
      provides: "Button + Modal-Mount + ToastContainer-Erweiterung + zentrale today-Variable"
      contains: "MembershipAdjustModal"
    - path: ".planning/phases/18-frontend-component-first/18-MANUAL-UAT.md"
      provides: "Browser-Test-Anleitung (6 Szenarien + Sign-Off)"
      contains: "UAT-Szenario 1"
  key_links:
    - from: "genossi-frontend/src/page/member_details.rs"
      to: "genossi-frontend/src/component/membership_adjust_modal.rs"
      via: "MembershipAdjustModal Props member today on_close on_success"
      pattern: "MembershipAdjustModal"
    - from: "genossi-frontend/src/page/member_details.rs"
      to: "genossi-frontend/src/component/toast.rs"
      via: "show_success_toast + SuccessToastContainer"
      pattern: "show_success_toast"
---

<objective>
Final-Integration: Button auf der Member-Detail-Page + Modal-Mount + Toast-Infrastruktur + zentrale today-Variable + ManualUAT-Browser-Test-Anleitung als persistente Datei.

Purpose: Closing-Anker fuer UI-01 (Single-Button auf Member-Detail), Roadmap-SC-4 (Button + i18n + ManualUAT). Schliesst die Phase ab.
Output: ~30-50 LOC Aenderung in member_details.rs + dokumentierte UAT-Szenarien als persistente Datei `18-MANUAL-UAT.md`.

**today-Reuse-Befund (verifiziert):** member_details.rs hat KEINE wiederverwendbare top-level `today`-Variable. Statt dessen wird `today` IN den `use_signal(|| ...)`-Closures (Zeilen 82-90 fuer `join_date` und Zeilen 138-148 fuer `action_date`) lokal berechnet. Diese existierenden Duplikate werden NICHT angefasst (Cross-File-Refactor-Risiko, L-7). Plan 07 fuehrt eine NEUE, EINMALIGE today-Variable am Top des `MemberDetails`-Component-Bodys ein und reicht sie nur an den Modal weiter. Falls in einem Future-Refactor die existing Duplikate konsolidiert werden sollen, ist das ein separates Aufraum-Ticket.

Diese Plan ist als `autonomous: false` markiert, weil Task 3 (UAT) zwingend manuell durch den Vorstand-User ausgefuehrt werden muss. Tasks 1+2 sind autonom (Code + Doc); der `<done>`-Status haengt jedoch vom UAT-Browser-Walk-Through ab — daher ist die Phase erst final nach User-Bestaetigung.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/genossi3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-frontend-component-first/18-CONTEXT.md
@.planning/phases/18-frontend-component-first/18-PATTERNS.md
@.planning/phases/18-frontend-component-first/18-UI-SPEC.md
@.planning/phases/18-frontend-component-first/18-06-SUMMARY.md

<interfaces>
<!-- From Plan 06: -->
```rust
use crate::component::{MembershipAdjustModal, show_success_toast, SuccessToastContainer};
```

<!-- From v1.1 (existing): -->
```rust
use crate::component::{Modal, ToastContainer, show_toast};
use crate::auth::RequirePrivilege;
use crate::service::member::refresh_members;
```

<!-- Modal wrapper (genossi-frontend/src/component/modal.rs:3-7): -->
```rust
pub struct ModalProps { pub children: Element }
#[component]
pub fn Modal(props: ModalProps) -> Element;
```

<!-- RequirePrivilege (auth.rs:34-48): -->
```rust
#[component]
pub fn RequirePrivilege(
    privilege: &'static str,
    children: Element,
    #[props(default)] fallback: Option<Element>,
) -> Element;
```

<!-- Existing member_details.rs today-Pattern (Z. 82-90 — js_sys::Date — wird NICHT geaendert): -->
```rust
let mut member = use_signal(|| {
    let today = js_sys::Date::new_0();
    let year = today.get_full_year() as i32;
    // ...
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Button + Modal-Mount + SuccessToastContainer + zentrale today-Variable auf Member-Detail-Page</name>
  <read_first>
    - genossi-frontend/src/page/member_details.rs (komplett — 1560 LOC, vor allem den `#[component] pub fn MemberDetails`-Body und den `rsx!`-Baum am Ende)
    - genossi-frontend/src/page/member_details.rs Zeilen 75-160 (MemberDetails-Component-Body, insbes. Z. 82-90 fuer existierende today-Berechnung in join_date-use_signal und Z. 138-148 fuer action_date-use_signal — beide DUPLIZIEREN today lokal, beide werden NICHT veraendert)
    - genossi-frontend/src/page/repayment_phases.rs Zeilen 40-50 + 64-130 (Pattern fuer RequirePrivilege + Button + Modal-Mount + ToastContainer)
    - .planning/phases/18-frontend-component-first/18-PATTERNS.md Section 4 (member_details.rs Pattern komplett)
    - .planning/phases/18-frontend-component-first/18-06-SUMMARY.md (Modal-Props-Signatur)
  </read_first>
  <files>genossi-frontend/src/page/member_details.rs</files>
  <action>

1. **Imports erweitern** (oben in der Datei, dort wo `use crate::component::...` bereits steht):

```rust
use crate::component::{
    /* existing imports... */,
    MembershipAdjustModal,
    SuccessToastContainer,
    show_success_toast,
};
use crate::auth::RequirePrivilege;
// refresh_members ist evtl. schon importiert — falls nicht:
use crate::service::member::refresh_members;
```

2. **Im `#[component] pub fn MemberDetails(...)`-Body**, dort wo die anderen `use_signal`-Calls deklariert sind, folgende Signals hinzufuegen:

```rust
// Phase 18 — Mitgliedschaft-anpassen Modal-State.
let mut show_adjust_modal = use_signal(|| false);
// Phase 18 — Success-Toast-Bucket (separates Signal, weil ToastContainer + SuccessToastContainer
// nebeneinander leben — Plan 02 Toast-Variant-Erweiterung).
let mut success_toast_messages = use_signal(|| Vec::<(u64, String)>::new());
let mut success_toast_counter = use_signal(|| 0u64);
```

Falls `toast_messages` + `toast_counter` (Error-Variant) bereits existieren: unveraendert lassen. Falls NICHT (member_details.rs hat aktuell KEINEN ToastContainer — L-6): zusaetzlich:

```rust
let mut toast_messages = use_signal(|| Vec::<(u64, String)>::new());
let mut toast_counter = use_signal(|| 0u64);
```

3. **Zentrale today-Variable am Top-Scope des Component-Body** (DIREKT nach `let i18n = use_i18n();` und vor allen use_signal-Calls, damit sie an mehrere Stellen weitergereicht werden kann; in dieser Datei aktuell nicht vorhanden — wir fuehren sie NEU ein):

```rust
// Phase 18 — heutiges Datum fuer Datepicker-Bounds (Single-Source-of-Truth fuer diese Page).
// HINWEIS: member_details.rs:82-90 und :138-148 berechnen today bereits separat INNERHALB
// von use_signal()-Closures (fuer join_date und action_date). Diese werden bewusst NICHT
// veraendert (L-7 Cross-File-Refactor-Risiko). Die hier eingefuehrte `today`-Variable wird
// ausschliesslich an MembershipAdjustModal weitergegeben. Future-Cleanup-Ticket fuer
// Konsolidierung der drei today-Calls ist optional.
let today: time::Date = {
    let d = js_sys::Date::new_0();
    let year = d.get_full_year() as i32;
    let month_idx = d.get_month() as u8 + 1;
    let day = d.get_date() as u8;
    match time::Month::try_from(month_idx) {
        Ok(month) => time::Date::from_calendar_date(year, month, day)
            .unwrap_or_else(|_| {
                time::Date::from_calendar_date(2025, time::Month::January, 1).unwrap()
            }),
        Err(_) => time::Date::from_calendar_date(2025, time::Month::January, 1).unwrap(),
    }
};
```

4. **Im rsx!-Baum**, an einer logischen Stelle (z.B. im Header neben „Bearbeiten"-Button, oder im „Aktionen"-Bereich — Planner-Discretion, der Executor entscheidet basierend auf bestehender UI-Struktur):

```rust
RequirePrivilege {
    privilege: "admin",
    button {
        r#type: "button",
        class: "px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white rounded text-sm font-medium min-h-[44px]",
        onclick: move |_| show_adjust_modal.set(true),
        "{i18n.t(Key::MembershipAdjustButtonLabel)}"
    }
}
```

5. **Am ENDE des rsx!-Baums** (vor dem schliessenden Top-Level-`div { ... }` der gesamten Page) folgenden konditionalen Modal-Mount + ToastContainer-Mounts hinzufuegen. **Wichtig**: `member.read().clone()` Snapshot fuer das Modal — Live-Signal-Drilling in die Modal-Component vermeiden.

```rust
// Phase 18 — Mitgliedschaft-Adjust Modal (conditional, only when toggled on)
if *show_adjust_modal.read() {
    {
        let member_snapshot = member.read().clone();
        let success_msg = i18n.t(Key::MembershipAdjustSuccess).to_string();
        rsx! {
            Modal {
                MembershipAdjustModal {
                    member: member_snapshot,
                    today: today,
                    on_close: move |_| show_adjust_modal.set(false),
                    on_success: move |_| {
                        show_adjust_modal.set(false);
                        let msg = success_msg.clone();
                        let member_id_opt = member.read().id;
                        spawn(async move {
                            refresh_members().await;
                            if let Some(id) = member_id_opt {
                                let config = CONFIG.read().clone();
                                if let Ok(data) = api::get_member(&config, id).await {
                                    *member.write() = data;
                                }
                            }
                        });
                        show_success_toast(
                            &mut success_toast_messages,
                            &mut success_toast_counter,
                            msg,
                        );
                    },
                }
            }
        }
    }
}

// Phase 18 — Toast containers (Error + Success, separate buckets per Plan 02).
ToastContainer { messages: toast_messages.into() }
SuccessToastContainer { messages: success_toast_messages.into() }
```

**HINWEIS** zur on_success Closure: `member` ist ein Signal-MemberTO in der bestehenden Page. `member.read().id` liefert `Option<Uuid>`. `*member.write() = data` schreibt die neuen Memberdaten zurueck und triggert Re-Render (current_shares/exit_date werden sichtbar).

**HINWEIS** zur Closure-Move-Semantik: Signals sind `Copy` in Dioxus 0.6 (Hook-Returns). Falls Borrow-Conflict beim `show_adjust_modal.set(false)` + `success_toast_messages.write()` im selben Closure-Body — die `&mut` mut-Refs auf die Signals sind die Standard-API (Plan 02 hat `show_success_toast(toast_messages: &mut Signal<...>, ...)` so signiert).

6. **Verifizieren**: Bestehende Tests in der Page bleiben gruen. `cargo check` ohne Warnings; `cargo test -p genossi-frontend` (falls Frontend-Tests existieren).
  </action>
  <verify>
    <automated>cd genossi-frontend &amp;&amp; cargo check 2>&amp;1 | tail -20 &amp;&amp; cargo test --lib page::member_details 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MembershipAdjustModal" genossi-frontend/src/page/member_details.rs` &gt;= 2 (import + RSX-mount)
    - `grep -c "show_adjust_modal" genossi-frontend/src/page/member_details.rs` &gt;= 3
    - `grep -c "RequirePrivilege" genossi-frontend/src/page/member_details.rs` &gt;= 1
    - `grep -c 'privilege: "admin"' genossi-frontend/src/page/member_details.rs` &gt;= 1
    - `grep -c "show_success_toast" genossi-frontend/src/page/member_details.rs` &gt;= 1
    - `grep -c "SuccessToastContainer" genossi-frontend/src/page/member_details.rs` &gt;= 1
    - `grep -c "ToastContainer" genossi-frontend/src/page/member_details.rs` &gt;= 1
    - `grep -c "refresh_members" genossi-frontend/src/page/member_details.rs` &gt;= 1
    - `grep -c "MembershipAdjustButtonLabel" genossi-frontend/src/page/member_details.rs` &gt;= 1
    - **Zentrale today-Variable** (NEU eingefuehrt am Component-Top): `grep -B 1 -A 12 "Phase 18 — heutiges Datum" genossi-frontend/src/page/member_details.rs | grep -c "let today: time::Date" ` == 1
    - **Modal-Mount nutzt today**: `grep -A 2 "MembershipAdjustModal {" genossi-frontend/src/page/member_details.rs | grep -c "today: today"` == 1
    - **Dioxus-Reload-Bug-Pattern** fuer den neuen Button: `grep -B 2 -A 5 "MembershipAdjustButtonLabel" genossi-frontend/src/page/member_details.rs | grep -c 'r#type: "button"'` &gt;= 1
    - `cd genossi-frontend &amp;&amp; cargo check` exit 0
    - `cd genossi-frontend &amp;&amp; cargo test --lib page::member_details` exit 0 (bestehende Tests gruen)
  </acceptance_criteria>
  <done>Button + Modal-Mount + beide Toast-Container + zentrale today-Variable in member_details.rs integriert; Admin-only Gate aktiv; cargo check/test gruen.</done>
</task>

<task type="auto">
  <name>Task 2: ManualUAT-Anleitung als persistente Datei 18-MANUAL-UAT.md schreiben</name>
  <read_first>
    - .planning/phases/18-frontend-component-first/18-CONTEXT.md (Specifics §ManualUAT-Browser-Test-Anleitung-Format-Vorschlag)
    - .planning/phases/18-frontend-component-first/18-UI-SPEC.md (Copywriting Contract — DE-Spalte fuer Verify-Texte)
    - .planning/phases/04-frontend-component-first/04-UAT-CHECKLIST.md falls existiert (Phase-4-Vorbild)
  </read_first>
  <files>.planning/phases/18-frontend-component-first/18-MANUAL-UAT.md</files>
  <action>
Neue Datei `.planning/phases/18-frontend-component-first/18-MANUAL-UAT.md` anlegen mit folgendem Inhalt (Markdown, 6 UAT-Szenarien + Sign-Off — alle Strings aus UI-SPEC-DE-Copy):

```markdown
---
phase: 18
artifact: manual-uat
status: pending-signoff
created: 2026-06-07
---

# Phase 18 — Manual UAT (Browser Walk-Through)

> Browser-Test-Anleitung fuer die Phase-18-Integration. Auszufuehren als Vorstand
> (Admin-Privilege) nach Plan 07 Task 1 (Code-Integration komplett).

## Voraussetzungen

1. Backend laufen lassen: `cargo run --bin genossi`
2. Frontend Tailwind-Watch + dev-server: `cd genossi-frontend && npx tailwindcss -i ./input.css -o ./assets/tailwind.css --watch & dx serve --hot-reload`
3. Im Browser einloggen als **Vorstand** (Admin-Privilege).
4. Mindestens 2 aktive Members und 1 gekuendigter Member in der DB (`exit_date IS NULL` bzw. `NOT NULL`).

## UAT-Szenario 1: Kuendigung

1. Navigiere zu `/members/{id}` eines aktiven Members (`exit_date IS NULL`).
2. **Verifiziere**: Button „Mitgliedschaft anpassen" ist sichtbar.
3. Klick Button → Modal oeffnet mit Sub-Choice (4 Karten: Kuendigung, Teil-Rueckgabe, Uebertrag, Aufstocken).
4. Klick „Kuendigung" → Sub-View oeffnet mit Titel „Kuendigung" (rote Heading), Datum-Input **vorbelegt mit heute** (SC-2), Vorschau live nach Selektion.
5. Datum-Input setzen auf **2026-06-15** (im aktuellen GJ).
6. **Verifiziere Vorschau**: `"{Vorname Nachname}: {X} Anteile (unveraendert) · Stichtag: 31.12.2026 (H1) · Auszahlung in Phase FY2026"`.
7. Datum-Input auf **2026-08-15** → Vorschau-Stichtag wechselt auf `31.12.2027 (H2) · Phase FY2027`.
8. Datum-Input auf **2024-01-01** (out-of-range) → Input-Border rot + Error-Text „Datum liegt ausserhalb...", Submit disabled, Vorschau verschwindet.
9. Datum zurueck auf 2026-06-15. Klick roter Submit „Kuendigung ausloesen".
10. **Verifiziere**: Modal schliesst, gruener Toast „Kuendigung wurde ausgeloest.", Member-Detail rerendert mit neuem `exit_date`.

## UAT-Szenario 2: Teil-Rueckgabe (inkl. Auto-Anlegen-Phase)

1. Anderer aktiver Member mit `current_shares >= 3`. Button klicken.
2. „Teil-Rueckgabe" waehlen.
3. Datum 2026-06-15, Anteile **1** (von z.B. 5).
4. **Verifiziere Vorschau**: `"{Name}: 5 → 4 Anteile (nach Auszahlung) · Stichtag: 31.12.2026 · Phase FY2026"`.
5. Anteile auf **5** (= current_shares) → Inline-Fehler „Du kannst nicht mehr Anteile zurueckgeben als das Mitglied besitzt. Fuer Voll-Rueckgabe nutze „Kuendigung"." — Submit disabled, Vorschau leer.
6. Anteile zurueck auf 2. Submit klicken.
7. **Verifiziere**: Modal schliesst, gruener Toast „Teil-Rueckgabe wurde eingetragen." (oder Auto-Create-Variante).
8. **Optional**: Wechsel auf `/repayment-phases` → neue Phase FY2026 mit Status `Preparation` und Entry fuer dieses Mitglied muss existieren.

## UAT-Szenario 3: Uebertrag (Teil + Voll inkl. Voll-Warnung)

1. Aktive Members mit z.B. 5 Anteilen (Source-Member A). Button klicken → „Uebertrag".
2. Datum 2026-06-15, Anteile 2.
3. **Verifiziere**: Empfaenger-Search erscheint nach kurz „Laedt..." Spinner. Gekuendigte Members NICHT in Liste.
4. Empfaenger B (3 Anteile) waehlen.
5. **Verifiziere Vorschau**: `"A {Name}: 5 → 3 Anteile · B {Name}: 3 → 5 Anteile · Datum: 15.06.2026"`. KEINE Voll-Uebertrag-Warnung.
6. Anteile auf **5** (= A.current_shares) → Vorschau: `"A: 5 → 0 Anteile · ..."` + orange-fette Warnung `"⚠ Voll-Uebertrag — {A Name} tritt am 15.06.2026 aus"`.
7. Anteile zurueck auf 2. Empfaenger auf den Source-Member selbst → Inline-Fehler „Empfaenger muss ein anderes Mitglied sein.", Submit disabled.
8. Empfaenger zurueck auf B. Submit „Uebertrag ausfuehren".
9. **Verifiziere**: Modal schliesst, gruener Toast „Uebertrag wurde ausgefuehrt.", A.current_shares=3, B.current_shares=5 (beim naechsten Page-Besuch).

## UAT-Szenario 4: Aufstockung

1. Aktiver Member, Button → „Aufstocken".
2. Datum 2026-06-15, Anteile 3.
3. **Verifiziere Vorschau**: `"{Name}: 5 → 8 Anteile · Datum: 15.06.2026"`.
4. Submit „Anteile aufstocken".
5. **Verifiziere**: Modal schliesst, gruener Toast „Aufstockung wurde eingetragen.", current_shares=8.

## UAT-Szenario 5: Negative-Pfade

1. **Submit ohne Datum**: Datum leer setzen → Submit MUSS disabled bleiben in jeder Sub-View.
2. **Out-of-range Datum**: 2024-01-01 in Cancel-Sub-View → Border rot, Submit disabled.
3. **Back-Navigation**: Sub-View → „← Zurueck zur Auswahl" → Sub-Choice mit reseted Feldern.
4. **Abbrechen**: „Abbrechen"-Button schliesst Modal ohne Submit (kein Toast, kein Refresh).
5. **Server-Error** (optional): Falls 409 (z.B. bereits-cancelled): ErrorAlert INNERHALB Modal (NICHT Toast), Modal bleibt offen, Submit enabled fuer Retry.

## UAT-Szenario 6: i18n DE/EN Switch (falls Locale-Switcher verfuegbar)

1. Locale auf EN → Button „Adjust membership", Modal-Title „Adjust membership", Sub-Choice „Cancellation / Partial repayment / Transfer / Increase shares".
2. Vorschau-Text in EN: `"{name}: {X} shares (unchanged) · Effective: 2026-12-31 (H1) · Payout in phase FY2026"`.

## Sign-Off

- [ ] Szenario 1 (Kuendigung) PASS
- [ ] Szenario 2 (Teil-Rueckgabe) PASS
- [ ] Szenario 3 (Uebertrag inkl. Voll-Warnung) PASS
- [ ] Szenario 4 (Aufstockung) PASS
- [ ] Szenario 5 (Negative-Pfade — alle 5) PASS
- [ ] Szenario 6 (i18n DE/EN) PASS (sofern Switcher verfuegbar)
- [ ] Browser-Console: keine JavaScript-Errors / Dioxus-Warnings
- [ ] Network-Tab: alle POST/GET liefern 2xx (oder erwartete 4xx fuer Negative-Pfade)

**Tester:** _______________________
**Datum:** _______________________
**Ergebnis:** ☐ PASS ☐ FAIL (Issues unten dokumentieren)

### Gefundene Issues (falls FAIL)

| # | Szenario | Erwartet | Beobachtet | Hotfix-Plan |
|---|----------|----------|------------|-------------|
|   |          |          |            |             |
```
  </action>
  <verify>
    <automated>test -f .planning/phases/18-frontend-component-first/18-MANUAL-UAT.md &amp;&amp; grep -c "## UAT-Szenario" .planning/phases/18-frontend-component-first/18-MANUAL-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - Datei `.planning/phases/18-frontend-component-first/18-MANUAL-UAT.md` existiert
    - `grep -c "## UAT-Szenario" .planning/phases/18-frontend-component-first/18-MANUAL-UAT.md` &gt;= 6
    - `grep -c "Kuendigung\|Teil-Rueckgabe\|Uebertrag\|Aufstockung" .planning/phases/18-frontend-component-first/18-MANUAL-UAT.md` &gt;= 4
    - `grep -c "Voll-Uebertrag" .planning/phases/18-frontend-component-first/18-MANUAL-UAT.md` &gt;= 1
    - `grep -c "Sign-Off" .planning/phases/18-frontend-component-first/18-MANUAL-UAT.md` &gt;= 1
    - `grep -c "Mitgliedschaft anpassen" .planning/phases/18-frontend-component-first/18-MANUAL-UAT.md` &gt;= 1
  </acceptance_criteria>
  <done>UAT-Dokument mit 6 Szenarien + Sign-Off-Checkliste angelegt; bereit fuer manuellen Vorstand-Walk-Through.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser-UAT manuell durchfuehren und Sign-Off in 18-MANUAL-UAT.md eintragen</name>
  <files>.planning/phases/18-frontend-component-first/18-MANUAL-UAT.md</files>
  <action>
HUMAN-ACTION: Lokal das Backend (`cargo run --bin genossi`) und das Frontend (`dx serve --hot-reload` + Tailwind-Watch) starten. Im Browser als Vorstand einloggen und alle 6 UAT-Szenarien aus `.planning/phases/18-frontend-component-first/18-MANUAL-UAT.md` durcharbeiten. Sign-Off-Checkboxes ankreuzen, Tester-Name + Datum eintragen.

Bei FAIL: Issue in der Sign-Off-Tabelle protokollieren und einen Hotfix-Plan (gaps closure) erzeugen via `/gsd-plan-phase 18 --gaps`.
  </action>
  <verify>
    <automated>grep -E "Ergebnis:.*PASS" .planning/phases/18-frontend-component-first/18-MANUAL-UAT.md || echo "PENDING UAT signoff"</automated>
  </verify>
  <acceptance_criteria>
    - User hat alle 6 Szenarien im Browser ausgefuehrt
    - User hat Sign-Off-Checkliste in `18-MANUAL-UAT.md` ausgefuellt
    - Result-Zeile zeigt `**Ergebnis:** ☑ PASS` (oder bei FAIL: dokumentierte Issues und Folge-Plan)
  </acceptance_criteria>
  <done>Manueller Vorstand-UAT-Walk-Through abgeschlossen, Sign-Off in `18-MANUAL-UAT.md` eingetragen, Phase 18 ready fuer `/gsd-verify-phase 18`.</done>
  <resume-signal>Antworte „UAT bestanden — Sign-Off eingetragen" oder beschreibe gefundene Issues fuer Hotfix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser↔Page | Button-Click ist Vorstand-only via `RequirePrivilege`-Gate (C-18-CF-11); Modal-Toggle ist lokales Signal, kein Auth-Tampering-Vektor |
| Page↔Modal | Member-Snapshot via Clone (kein Live-Reference); on_success-Callback updated lokales Signal + triggert Refresh |
| Page↔Toast-Container | Success-Toast aus i18n-Translation, kein User-Input |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-07-01 | Spoofing | Non-Admin sieht Button via DOM-Manipulation | low | accept | `RequirePrivilege` rendert Button NUR fuer Admin. Selbst wenn Button via DevTools erzwungen wird, Backend-Auth (ADMIN_PRIVILEGE-Funnel) verhindert jeden API-Call (HTTP 401/403). |
| T-18-07-02 | Tampering | show_adjust_modal-Signal via DevTools auf true gesetzt | low | accept | Modal zeigt User-Input-Felder; jede Submission scheitert Server-side ohne Admin-Auth. UI-State-Manipulation ist kein Security-Vektor in einer Vorstand-only-App. |
| T-18-07-03 | Information Disclosure | Member-Snapshot enthaelt PII waehrend Modal offen | low | accept | Modal ist Vorstand-only (RequirePrivilege-Gate); Snapshot ist in-memory und wird beim Modal-Close GC'd. |
| T-18-07-04 | Repudiation | Modal-Close ohne Audit-Trail bei Server-Error | low | accept | Server-Error wird in ErrorAlert angezeigt; Vorstand sieht die Fehlermeldung explizit. Audit-Trail laeuft auf Server-Seite (audited_*!-Macros) — keine Frontend-Repudiation-Risiken. |
| T-18-07-05 | Denial of Service | refresh_members + get_member parallel triggern Backend-Load | low | accept | Pro Submit nur ein Refresh-Cycle; vergleichbar mit existing v1.1-Pages. |

Keine `high`-Severity-Threats.
</threat_model>

<verification>
- Cargo check + test gruen
- Bestehende member_details.rs Tests unverändert
- 18-MANUAL-UAT.md mit 6 Szenarien + Sign-Off angelegt
- Manual UAT alle 6 Szenarien PASS (per Sign-Off)
- ROADMAP-SC-4 (Button + i18n + ManualUAT) erfuellt
- Zentrale today-Variable einmal eingefuehrt; bestehende today-Duplikate in :82-90/:138-148 nicht angefasst
</verification>

<success_criteria>
- Phase 18 vollstaendig integriert
- Roadmap Success-Criteria 1-4 erfuellt:
  - SC-1: MembershipAdjustModal shared Component + Sub-Choice (4 flat) + 4 Sub-Views ✓
  - SC-2: Datepicker mit GJ-Bounds + default today() (Modal-Body Plan 06) + MemberSearch-Reuse ✓
  - SC-3: Vorschau-Section pro Sub-View mit konkreten Zahlen ✓
  - SC-4: Admin-only Button auf Member-Detail-Page + i18n DE/EN ≥20 Keys (46 implementiert) + ManualUAT ✓
- Alle 5 Requirement-IDs adressiert: UI-01, UI-02, UI-03, UI-04, CANC-06
- Phase 18 ready fuer `/gsd-verify-phase 18`
</success_criteria>

<output>
After completion, create `.planning/phases/18-frontend-component-first/18-07-SUMMARY.md` with:
- File-Aenderungen-Liste in member_details.rs (vor allem: added Signals, zentrale today-Berechnung, Button-RSX, Modal-Mount, Toast-Container-Mounts)
- UAT-Sign-Off-Resultat (Verweis auf `18-MANUAL-UAT.md`)
- Bestaetigung: alle 5 Phase-18-Requirements (UI-01..04, CANC-06) implementiert
- Liste der Plan-Filenames die zu dieser Phase gehoeren (01..07)
- Roadmap-Update-Vorschlag fuer ROADMAP.md (Status Phase 18 = Complete nach UAT-PASS)
</output>
